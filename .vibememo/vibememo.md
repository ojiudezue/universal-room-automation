# Universal Room Automation — VibeMemo

*Last updated: 2026-06-23 | Version 1 | Contributors: ojiudezue*

## How This Started

URA is a Home Assistant custom integration managing 31 rooms across 5 zones with 7 domain coordinators (Presence, Energy, HVAC, Safety, Security, Music Following, Notifications). It automates room behavior based on occupancy detection, Bayesian prediction, energy optimization, and cross-coordinator signals.

## Key Decisions

**Single-threaded DB write worker (v3.22.8):** SQLite only allows one writer at a time. Instead of multiple connections fighting for the lock, all writes serialize through one asyncio.Queue processed by one persistent connection. Reads use independent transient WAL connections. This eliminated all write contention from the v3.18.4 "database is locked" crisis.
→ [001](users/ojiudezue/entries/001_db_single_writer_architecture.json)

**Write worker as background task with graceful shutdown (v4.2.15-16):** The write worker runs forever — using `async_create_task` made HA wait indefinitely for startup completion. Changed to `async_create_background_task`. On shutdown/reload, pending writes are now flushed (5s budget, fresh connection) instead of failed with RuntimeError. Data integrity preserved across restarts.
→ [005](users/ojiudezue/entries/005_db_write_worker_startup_fix.json)

**Startup catch-up prune removed (v4.2.14):** Added in v4.2.8 to clear orphaned table backlogs, delayed to 30 min in v4.2.13, removed in v4.2.14. Regardless of delay, it saturated the write queue for 15+ minutes on every boot, blocking all DB reads. Nightly 2:30 AM maintenance is the sole cleanup mechanism. The one-time backlog it was designed to clear is gone.

**Energy measurement trust hierarchy (v4.2.16+):** Three independent measurement sources — Envoy (solar gateway), Emporia Vue (mains CTs), SmartHub (utility meter) — don't agree. Envoy drifts ~5 kWh/day from utility billing. Trust hierarchy for cumulative accuracy: SmartHub > Emporia/SPAN > Envoy. Envoy remains primary for real-time (sub-second resolution). SmartHub will calibrate bill predictions. Emporia mains will populate the empty `grid_import_2` column in energy_history for historical cross-validation. Shipping divergence reporting first (B-Lite), deferring auto source switching (B3) until 2-4 weeks of data validates the pattern.
→ [003](users/ojiudezue/entries/003_energy_measurement_trust_hierarchy.json)

## Current Architecture

- **Write path:** 44 write methods → `_db()` → asyncio.Queue → single background write worker → one persistent SQLite connection. Graceful 5s flush on cancel.
- **Read path:** 43 read methods → `_db_read()` → transient WAL connections. Zone sensors cached 5 min, Bayesian accuracy cached 30 min.
- **Startup:** First-cycle writes deferred 5 min with 0-60s jitter. No catch-up prune. Bayesian predictor registered before DB load (survives init failure). Write worker is a background task (doesn't block startup). ~5 min warmup.
- **Energy:** Envoy primary for real-time, CostTracker uses Emporia mains for billing. Utility meter integration planned (B-Lite).
- **EV control:** TOU pause + grid cap pause. Battery drain auto-pause planned (Cycle A).
- **DB:** 810 MB, WAL mode, 30s busy_timeout. Nightly 2:30 AM prune (7 tables, rotating start, 5-min budget). SQLite-over-SMB reads blocked by persistent write worker — known limitation, not a code bug.

## Open Questions

- After B-Lite divergence data, is automatic prediction source switching safe for financial predictions? Should Envoy be demoted from primary for cumulative tracking?
- If room count grows past 40, will warmup become unacceptable?
- (Write batching shipped for optimizer findings in v5.2.2; generalizing it to other channels remains open.)

## Recent Arc — through v4.7.29 (2026-06-08)

The v4.2.x architecture above is stable; the v4.7.x line is feature + correctness work on top, governed by a tightening review discipline. The throughline: **energy/TOU strategy correctness** and a **review protocol that scales with regression risk**.

**v4.7.28 EV off-peak + v4.7.29 day-boundary TOU (Bug Class #51).** Cars weren't charged by morning — the off-peak branch was resume-only, stranding night plug-ins with no pause-set; rewrote to ensure-on with guard precedence + persisted pause-sets (bundled with a W/kW reconciliation audit: three display sensors ~1000x off reading mis-named `*_kw` properties). Then the operator caught grid import during summer *post-peak* mid_peak while the battery idled: the legacy hold-for-peak branch held for BOTH mid_peak windows, never reconciled with the v4.5.0 day-boundary lookahead. New primitive: hold only when a real peak precedes the next off_peak. Bug class generalized: any solar-cycle decision assuming a period is "ahead" without a midnight-crossing lookahead. → [018](users/ojiudezue/entries/018_v4728_v4729_ships_tier2db_policy_day_boundary_bug_class.json)

**Process: Tier 2-DB for all regression-prone work** — operator formalized (CLAUDE.md) that the 3-framing-disjoint-review protocol applies to ALL regression-prone work; the value is framings that can't share blind spots, not the "DB" label. (Same session: a multi-reboot scare root-caused to non-URA causes; Shipwatch bootstrapped on the machine.)

## Marathon hygiene sweep — v4.7.30 → v4.7.32.1 (2026-06-08)

A single long session worked the whole hygiene/backlog queue to completion across four releases plus live HA-side config fixes. Throughline: **correctness fixes for silently-dead features, governed by verify-don't-assume**. → [019](users/ojiudezue/entries/019_v4730_v4731_v4732_marathon_zone_resolution_heatcool_span_prune.json)

**v4.7.30** — compliance sensors silently pinned at 100.0 (async DAO called un-awaited from a sync property → swallowed → hardcoded fallback); fixed via the async_update cache pattern, plus the HVAC post-peak coast release. **v4.7.31 — Bug Class #53:** the motionless-occupant veto had been **dead since it shipped** — `ZoneManager.zones` is id-keyed but lookups used the house-zone *name*, and the tests mocked the dict name-keyed, matching the bug (fixture-authority failure). New `_resolve_hvac_zone` (id → name → merged-name), proven truth-preserving by a context-wide caller review. **v4.7.32/.1** — OverrideArrester now always returns to heat_cool (the one legacy "Arrester v10" concept it had dropped), bundled with a reversible SPAN baseline prune (backup-then-delete; `.32.1` widened a too-narrow scope guard found in live validation).

**The recurring lesson: verify-don't-assume against stale state.** Two near-misses, both caught by fresh live reads: `notify.madronehapushover` was assumed broken (stale memo) but is a working legacy notify service; the SPAN prune-guard assumed a scope format that was wrong. The operator repeatedly nudged ("I'm not sure it's gone", "I still get pushovers", "not working off a stale snapshot"). Sensitive ops now read the live source at action time, never a memo or a minutes-old Samba mount — and the SPAN delete is reversible (backup table + dump + snapshot).

**Also:** backlog #9 ruled a no-op and the durable EV philosophy captured (solar-first → never drain battery into car → off_peak cheapest); the "Entertainment + Master Suite" zone confirmed an inert bad-fix husk (no delete-zone flow exists); live HA-side config fixes landed outside the repo. Remaining from this era: load-shedding (1 CRIT: EV shed tier shares `_paused_by_us` with the EVSE TOU controller).

## v4.7.33 A-F5 + Optimization Coordinator scoping (2026-06-08, cont.)

Closed the A-F5 follow-up the marathon had deferred, then scoped URA's largest planned subsystem. → [020](users/ojiudezue/entries/020_v4733_af5_ttl_plus_optimization_coordinator_scoped.json)

**v4.7.33 — A-F5 TTL suppression.** The OverrideArrester suppressed its own climate writes via a set popped on the *first* state event — but v4.7.32 made `_revert_override` fire *two* service calls under one `suppress()`, leaking the second into override detection. Fix: a 5s TTL window. Once review found the primitive has **9 external callers** (hvac.py/hvac_predict.py), it was elevated Tier 1→Tier 2-DB; that surfaced a HIGH — a fixed window now *swallows* a genuine user override landing mid-window. The clean resolution: since URA never writes `preset_mode=manual`, a mid-window non-manual→manual transition is unambiguously the user → pass it through. LIVE + validated (PR #374).

**Four inspections, one keeper.** Operator flagged a Frigate camera *motion* sensor (`device_class=motion`) living in zone_1's `CONF_ZONE_CAMERAS` — a list documented person-detection-only — which `check_zone_occupancy_confidence` trusts wholesale with no device_class filter, feeding the stale-sensor guard. He removed it from config; the durable code guard is a low-pri backlog plan. The other three (DB-write-worker boot transient, fan-during-solar-banking, zone-state-across-restart) were ruled NO-OP.

**Bayesian audit:** exactly ONE shipped engine (the coordinator-level `BayesianPredictor`, occupancy, live + healthy at 97% data-quality / 85.6% hit-rate) and the per-room "comfort ParameterBelief" is design-only scaffolding (zero code). The operator's "two Bayesian systems" is really one shipped + one unbuilt.

**Optimization Coordinator — fully scoped (v2 plan).** The session's centerpiece. Decisions, all operator-driven: **agentic-first** (lead with the acting loop on existing sensors, default-ship **L1 Shadow** = log-and-score-no-actuation, discover the health-computation delta empirically — not a health-foundation cycle); a **six-rung graduated autonomy ladder** in the operator's 3→1→2→4 ordering (reversible-device → config+veto → config-immediate → unbounded) plus a **matrix** (rung × per-domain × confidence + rate-cap/quiet-hours + restart-persistent kill switch), parsimony-bounded; **direct actuation** reusing the just-hardened `suppress()` handshake so it doesn't fight siblings ("else how will we even accomplish the goal?"); **LLM Tier-2 in Phase 2**, provider-agnostic via `ai_task` (live HA has claude/openai/google/ollama) with a cheap **Ollama-triage → premium-deep** cost route; and — after the prompt-management design over-built into a disk-file/variant subsystem and the operator called it **"expensive"** — a **lean** one-field editable prompt in `entry.options` with a const fallback. Tier 2-DB from cycle one. The build starts the next session.

## Optimization Coordinator shipped v5.x — and a production incident (2026-06-09)

Built the whole Optimization Coordinator overnight and shipped it staged as a **major version** v5.0.0→v5.3.0 (skeleton / LLM Tier-2 / dimensions+digest / Prediction-Validation reader), each a Tier 2-DB cycle. → [021](users/ojiudezue/entries/021_optimization_coordinator_v5_shipped_writeflood_incident_remediation.json) Then it **took the live house down**: the cycle persisted findings one-by-one *and* Sensor-Health fired one finding per boot-unavailable room, so each cycle dumped O(N) writes into URA's single DB write queue, starving core writes (35s timeouts, a connection held >120s); the operator's UI parent-reload tripped the watchdog. **Rolled back to v4.7.33**, fixed forward (v5.2.2 batched persist) — and an adversarial review caught a **second equal-sized O(N) channel** (`_consider_apply`→`ura_activity_log` per finding) that the first fix missed. **The durable lesson:** a per-cycle write-VOLUME review must enumerate and gate *every* write channel by table, not just the obvious one — "L1 Shadow" bounds actuation, not the coordinator's own write load. Also: the LLM `object`-selector schema was rejected by Anthropic (`additionalProperties`), fixed to a `findings_json` text field (verified live before shipping). The operator then caught that the Bayesian "remove 2" missed non-"bayesian"-named room-prediction sensors — refined verdict: remove `presence_next_state` (dead) + `peak_occupancy_time` (replaced), but **keep `next_occupancy_in/time`** (no Bayesian ETA equivalent — don't delete without a replacement). Bayesian System B's B3/B4 remain unbuilt. Closed by switching the model to **Fable-5** (builder/reviewer/planner agents too).

## The five-deploy marathon — OC Phase 5 complete + the model tiering decision (2026-06-10)

A continuous ~24h run shipped **five validated releases** and finished the Optimization Coordinator. → [022](users/ojiudezue/entries/022_v531_v534_marathon_oc_phase5_complete_model_tiering.json) **v5.3.1** fixed a live energy-poisoning incident the zone/house sensor sweep exposed: Wh-reporting sources summed as kWh (master_suite read 1,671 kWh "today" at 1 kW — exactly 1000×), and a deeper *intent* error — three of the four B4 attribution tiers summed raw lifetime counters into a today-scope equation (coverage delta: −839M kWh, rated "Excellent"). The unit bug class (#30) had been fixed five times on power surfaces; the energy surface had simply never been swept. **v5.3.2** gave routine awareness a real brain — a frequency model over 60 days of `house_state_log` replacing the placeholder that had returned "unknown" forever; review caught a CRITICAL pre-deploy (naive-UTC stamps re-parsed as local would have trained every cell 5–6h off, and the test stub encoded the inverse semantics so the suite certified the bug). Its first live prediction — arriving→home_night @ 97.2% at 10 PM — was correct. **v5.3.3** shipped OC Pillar B: the operator-approved knob table (zero new CONF keys), plain-English autonomy rungs, confirm-guarded escalation, four admin buttons, translations. **v5.3.4** shipped Pillar A — and its Tier 2-DB panel earned its cost again: the sibling veto handshake as built was *advisory-only* (vetoes purged unread after actions ran) and broke L1 silence on the live house; both CRITICAL-class, both fixed with end-to-end proofs. Same release: the prediction kill-list (~50k recorder writes/day eliminated via a timestamp-class refit — replace-first, never delete-first) and B4 live-health repairs (root-caused, not clamped blindly: negative predictions were *correct net-of-solar data wearing the wrong name*).

Two operator recalibrations closed the day. First, the optimizer's status word: five dead-sensor HIGHs at house_score 55 read "critical," which "made it not as useful" — now "critical" requires a critical-severity finding (confirmed live: same pile reads "degraded"). Second, and durable: **model tiering**. Measured over the marathon, builders burned ~1.6M tokens yet every cycle still needed 6–16 review findings fixed, while ~700k of bounded Fable-5 reviewer tokens caught every shipped-incident-class bug. The asymmetry — *the pipeline catches builder mistakes; nothing catches reviewer misses* — became policy: Fable-5 for judgment (orchestrator, reviewer, planner), opus-4-7 for building, haiku for validating. The reviewer is the deliberately super-powered agent. One humbling coda: the operator's two-minute hands-on caught a hole four model review passes graded as "≤30s poll lag" — the escalation buttons never refreshed availability at all. Operator validation is its own review layer.

## Envoy boot-decoupling + the attainability redesign marathon (2026-06-12/13)

A dead Enphase Envoy broke the v5.3.6 post-deploy boot — all 40 entries stranded — but the release was exonerated by diff. → [023](users/ojiudezue/entries/023_v537_v538_envoy_decoupling_attainability_redesign_marathon.json) Root cause was v4.2.29's coupling: `after_dependencies:[enphase_envoy]` let HA's stage-2 timeout cancel URA's queued setups when enphase_envoy hung, and a one-shot Envoy validation lost a ~17s boot race so the EnergyCoordinator silently never registered. The broken boots also exposed RestoreEntity unavailable-coercion (Bug Class #52: `unavailable` last-state restored as OFF, disabling all 6 intended-ON EC switches). **v5.3.7** fixed all three — registry-based three-way validation, EC always registers when enabled (runtime is already None-safe, the boot gate was the only hard coupling), `after_dependencies` removed, restore guards — and proved itself the same night under the exact fault it was built for.

Then the deeper one. A good-solar day where the EVs' off-peak ensure-on charging consumed *all* production drove the battery to 10% entering peak, while the forecast-class-only arbitrage gate never looked at the buffer. A live manual-arbitrage exercise (operator + assistant) rode today's peak (10%→45%) and measured the Enphase actuation constants — reserve-bump charge onset ~22min, charge_from_grid cloud enable ~35min, full rate ~16kW — which then became **v5.3.8**: a new `attain` arbitrage phase that fires on *realized* solar-divergence regardless of forecast class (operator principle: "no solar **reaching** the battery → need for arbitrage"), plus D1b mid-peak continuation (the state-matrix "no charging outside off_peak" invariant narrowed, not deleted: "arbitrage IS arbitrage... if we can't cover mid_peak we absolutely need to cover peak"). v5.3.8 was the heaviest cycle the repo has run — **1 build + 4 fix-ups + 7 reviews** (two full framing-disjoint Tier-2-DB passes + a focused pass-3) — because builders **twice** shipped HOLD as a one-tick emission the drain fallback immediately unwound, and **twice** gated reboot-recovery on a RAM latch that boots False. The breakthrough was brief discipline: when two builders fail the same requirement, prescribe the **mechanism** (HOLD is a STATE routed before the predicate AND the fallback; reboot recovery derives from **hardware** state, not a RAM latch), not the behavior. Test authority eroded just as hard — review proved deleting the entire solar term passed 26/26 and HOLD reserve=0 passed everything — so mutation-anchored tests became a standing gate (7 mandatory mutations on the final fix-up). Both LIVE. Two durable hazards banked: toggling the EC-enabled switch triggers a watchdog restart, and EC reverts manual Enphase flips every cycle — battery decisions must live *inside* the decision loop. Load shedding was split: correctness fixes stay a normal cycle, capability expansion (pool sub-tiers, forecast coupling) becomes a foundations-first/IP track.

## The three-cycle march, the OC-LLM config fix, and the inclement-weather design (2026-06-13→15)

A long continuation session shipped a clean three-item march and then designed the most product-shaped energy feature yet. → [024](users/ojiudezue/entries/024_march_3cycles_oc_llm_config_inclement_weather_design.json)

**The march (all LIVE):** v5.3.9 (#16 arbitrage solar-attainability three-rung ladder — rung-0 suppress / rung-1 redirect-EV / rung-2 grid — reusing the v5.3.8 attain projection, plus a bidirectional breaker-safety chokepoint that *also closed a latent v5.3.8 attain breaker hole*); v5.4.0 (#10 HC pre-conditioning master toggle + OC observability suite); v5.4.1 (#15 load-shed correctness — the EVSE pause-ownership collision moved onto a dedicated set, the v5.3.9 pattern); v5.4.2 (cosmetic rename + reorder). Two things stood out. **The 3-framing-disjoint protocol had its strongest demonstration in #15** — Reviews A/B/C each found a *different* defect class in the same diff (precedence holes / two orphan-restore CRITs, one introduced by the build's own first-pass fix / a written-but-never-read manual-off no-op), none caught by the passing test suite. And **the Opus-4.8 reviewer** (Fable-5 retired; reviewer now exceeds the builder) caught CRITICAL regressions the validator + three full reviews + the orchestrator's own spot-check all missed — a silently-deleted live TOU-dispatch in #16, a degenerate shadow-accuracy oracle in #10 that would have trended falsely to 100%. The pipeline catches builder mistakes; only a strong reviewer catches the reviewer-adjacent ones.

**"Is Haiku good enough?"** The OC LLM tier kept failing (empty responses + 45s timeouts). Root cause was *config, not model*: the HA Anthropic AI-Task ran on the "recommended" preset, whose token/thinking budget starved the forced structured output. A direct `ai_task.generate_data` test with an explicit `claude-haiku-4-5` + `max_tokens 4096` + bounded thinking returned clean schema-valid JSON. The durable lesson: don't judge a model's reasoning when the calls fail at the transport/budget layer.

**Inclement-weather reserve (designed, plan-final, not yet built).** Discovered URA is blind to Enphase Storm Guard and its own `storm_forecast` is a single-weather-entity hardcoded-condition check that ignores the operator's configured 3-way cross-check. The operator's thesis — "to beat Enphase we use the alert (same NWS feed they get) *with* local weather" — drove a full design. The load-bearing calls, all operator-shaped: **outage-relevance event-type is the PRIMARY classifier gate, not Severity** (a Severe Flood Watch is high-severity but a weak outage predictor for an elevated house — gating on Severity *is* the Enphase over-hold); **hold is a depth ladder** (full/partial/allow-discharge) modulated by tier × TOU × solar-horizon, because a warning means something different in the morning with solar ahead than at night with 6–12h of dark; **"recoverable" is solar-*surplus*-based** (reusing the #16 attainability primitive — "solar must feed the house before the battery"); and **hold duration is bounded by the alert's own expiry**, never a fixed timer (the Enphase 2-day stale-lock failure). An EV cross-check returned a clean bill of health. The whole thing is the clearest sign yet that the energy strategy has matured a reusable "solar reaching the battery" primitive and a product sensibility about *which* warnings should cost you money.

## Bathroom-exhaust intelligence + the "safety bound vs gated automation" split (2026-06-22→23)

After two quiet v5.5.x deploys this session — exposing the previously-hidden arbitrage grid-import guard in config flow (v5.5.6, default-off, harmonized against the EV grid-import cap) and an incremental DB vacuum (v5.5.7) — the main work was a Tier-3 cycle to make bathroom exhaust fans *self-contained in rooms*. → [025](users/ojiudezue/entries/025_bathroom_exhaust_humidity_unification_tier3_gate_boundary.json)

**The build.** Humidity/exhaust fans were decoupled from the HVAC coordinator entirely — removed from `hvac_fans.py` so `automation.py` is the sole owner (invariant I1: exactly-one-owner), ending a long-standing orphan bug where an HVAC-coordinated room with fan-control off left humidity fans owned by nobody. On top: EMA humidity-spike detection (a rate-of-rise catch so a high absolute threshold still trips on a shower), presence/usage-proportional post-vacancy runtime (the operator's Guest-Toilet "longer visit → longer purge" pattern), a Wet-Room flag (auto-true for bathrooms; exempts the exhaust from comfort-fan sleep-off), and a reframe of the per-room temps as a desired *comfort range* scored on both bounds (closing a Bug Class #53 where only the cool bound was read).

**The load-bearing decision — where does a bathroom exhaust's independence end?** It surfaced through review as the cycle's spine. The operator settled on **Option 2: venting is automation a user can disable** (gated by the room's master automation switch *and* a dedicated humidity-fan toggle; ManualMode suppresses it), **but the max-runtime safety cap is a universal bound** that force-offs a runaway fan regardless of every toggle. This is URA's first explicit split between *automation a user owns* and *a safety bound that transcends user intent* — a pattern future safety bounds (breaker, thermal) should follow rather than living inside an automation gate.

**The process earned its Tier-3 bar.** Four framing-disjoint reviews all said FIX-FIRST; the fix-up resolved them but its structural changes *exposed two deeper gate leaks* (the humidity call was still trapped one gate up under master-automation; the reload-seed sat below the cap so the cap couldn't fire on a boot-with-toggle-off fan) — the 4th-pass-finds-N+1 lesson, again. A systemic test gap was named and partly closed: no test drove the real `_async_update_data`, so the entire coordinator gate stack shipped green; the orchestrator's own source mutation then caught that even after the fix the gate boolean could regress `and`→`or` untested, closed by extracting the decision into a pure, importable helper. **Bug Class #53 (computed-but-not-consumed) hit three times in this single cycle** — a dead presence timer, an unscored comfort bound, and the gate-trapped call — confirming it as URA's dominant recurring failure mode and the reason mutation-anchored + integration-level tests are now standing gates. Packaged as **v5.6.0**, bundling the parked cover-log-spam rate-limit fix; live-validation write-back to follow.
