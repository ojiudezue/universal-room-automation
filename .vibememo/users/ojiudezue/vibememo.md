# URA — VibeMemo (ojiudezue)

*Last updated: 2026-06-23 | Version 1 | Contributors: ojiudezue*

## DB Write Architecture: Why Single-Threaded and Why That's Right

URA's database layer uses a single-threaded asyncio write worker (v3.22.8) with one persistent SQLite connection. All writes serialize through an asyncio.Queue. Reads use independent transient connections via WAL mode.

This was chosen after v3.18.4's "database is locked" crisis — 25+ rooms writing concurrently caused SQLite lock contention. The single-worker queue mirrors SQLite's fundamental constraint: only one writer at a time. Five hardening releases (v3.22.8 → v3.22.11 → v4.0.17) made it production-solid.
→ [001](entries/001_db_single_writer_architecture.json)

## Startup & Shutdown: Lessons from v4.2.8–v4.2.16

The DB write queue architecture went through a crisis cycle (Apr 2026). An unbounded DELETE in `prune_prediction_results` held the write queue for >120 seconds, causing HA unresponsiveness and hardware reboots. This exposed three compounding issues: unbounded prunes, high-frequency DB reads from sensor polls, and orphaned cleanup methods.

A startup catch-up prune was added (v4.2.8) to clear backlogs, then delayed to 30 min (v4.2.13), then removed entirely (v4.2.14) — it saturated the write queue on every boot regardless of delay, blocking all DB reads for 15+ minutes. Nightly 2:30 AM maintenance is now the sole cleanup mechanism.

The write worker itself was blocking HA startup (v4.2.15) because `async_create_task` tracks tasks for startup completion — a forever-running task never completes. Fixed with `async_create_background_task`. Shutdown was also lossy: all pending writes were failed with RuntimeError on cancel. v4.2.16 added graceful flush with a 5-second time budget — opens a fresh connection, executes remaining writes, then drains failures for anything left.
→ [005](entries/005_db_write_worker_startup_fix.json)

## Energy Measurement: Three Sources, One Truth

URA has three independent energy measurement sources. They don't agree.

1. **Enphase Envoy** — solar gateway with net consumption CT. Sub-second resolution. Primary source for real-time decisions. But cumulative totals drift ~5 kWh/day from the utility meter.
2. **Emporia Vue** — mains panel CTs measuring actual grid import (`mainsfromgrid`) and export (`mainstogrid`). Historically more aligned with utility billing than Envoy.
3. **SmartHub (utility company)** — the meter the electric company bills from. Daily midnight readings. The ground truth for what you actually pay.

The trust hierarchy for cumulative accuracy: SmartHub > Emporia/SPAN > Envoy. Envoy remains primary for real-time because of its resolution, but its cumulative drift means bill predictions based purely on Envoy are off by ~$18/month.

Decision: integrate SmartHub as bill prediction calibration source (B-Lite: divergence reporting first, auto-switching deferred). Populate the empty `grid_import_2` DB column with Emporia mains power for historical cross-validation.
→ [003](entries/003_energy_measurement_trust_hierarchy.json)

## EV Charging: Completing Autonomous Control

EV charge control had two pause reasons: TOU scheduling (pause during peak/mid-peak) and grid import cap (pause when import exceeds threshold). Missing: battery drain protection. The user manually paused charging every evening when the EV drew power from the Enphase battery instead of the grid.

Adding a third pause reason (`_paused_by_battery_drain`) with configurable SOC threshold completes the autonomous control loop. Follows the exact code pattern of the existing grid cap pause.
→ [004](entries/004_ev_battery_drain_autopause.json)

## Battery Strategy v4.5.0: Phased State Machine + EC Nexus Framing

Through May 2026 the battery strategy hit structural failure. The five-knob system (`reserve_soc`, four `drain_target_*` sliders, `arbitrage_trigger`, `arbitrage_target`) had three coupled problems: arbitrage_trigger=20 sat below drain_target_poor=30 so arbitrage was unreachable from above (rarely fired); when it did fire, charged energy drained back to drain_target during morning off-peak before peak hits at 16:00, wasting the buffer; and "drain less when forecast is bad" structurally conflicts with "charge more when forecast is bad" — the same threshold can't serve both jobs.

A multi-turn design conversation on 2026-05-06/07 produced a phased state machine: **WAIT → CHARGE → HOLD → DISCHARGE**. WAIT means "do nothing, battery operates normally"; CHARGE fires `arbitrage_charge_lead_time_min` (default 360 = 6h) before the next high-rate transition with a fresh forecast re-check that aborts cleanly if class improved; HOLD locks reserve_level at peak_buffer_target until the high-rate window starts; DISCHARGE preserves existing logic. Drain targets stay as the fallback when arbitrage is OFF — byte-for-byte v4.3.4 behavior preserved on every "arbitrage disabled" row of the State Matrix. The matrix in `PLANNING_v4.5.0_battery_strategy_redesign.md` is the implementation spec.

Two key user corrections shaped the design: (1) "Only 1 use Ura for now. So we don't need backward compatibility" → drop ALL mode-toggle / calibration-phase / migration-cycle scaffolding; just ship the new behavior. (2) "Earlier start is better if same day" → lead_time defaults to 360 min not 120, biasing the CHARGE phase to fire after morning intraday solar telemetry confirms forecast.

The cycle introduces the **"EC cost-minimization nexus"** framing — Energy Coordinator is the central decision-maker for every controllable load (battery, EV, eventually appliances), and the umbrella objective is total energy cost minimization. v4.5.0's D4 mutual-exclusion (`_paused_by_arbitrage` set on `EVChargerController`) is the precedent v4.7.x B5 (Appliance Scheduler) will copy onto each appliance controller. Saw-tooth charge rate cap was originally D4; it would flap on Enphase's binary `charge_from_grid` switch and doesn't address breaker safety. Replaced with arbitrage/EV mutual-exclusion that establishes the load-coordination pattern.

Seven advanced topics deferred to v4.6.x (Bayesian peak_buffer_target, charge-rate control via barneyonline HACS, solar-aware partial top-up, intraday-confirmed dynamic lead time, season-variable buffer, per-window economic gate, cycle-wear amortization). Each captured with "why deferred" + "what it would unlock" so v4.6.x has a prioritized backlog when v4.5.0 calibration completes.
→ [006](entries/006_v450_battery_strategy_redesign_pivot.json)

## Transition Doc Skill — Hazard Maps for the Next Session

The v4.5.0 conversation produced ~9 mistake-and-correction pivots. The final plan reflected only the destination; the rejected paths were lost. Recognized this as a recurring pattern — planning conversations regularly produce significant pivots that the artifact alone doesn't preserve, and the next session opening that artifact cold has no protection against re-deriving the same seductive false starts.

Built `.claude/skills/transition-doc/` (with both `SKILL.md` and a publishable `README.md`) — a user-invocable slash command that generates a structured "transition notes" doc capturing mistakes-in-order with user quotes, confirmed-good decisions, hot-zone implementation risks, and a what-good/bad-looks-like checklist for the next session. Output lives next to the plan it accompanies (e.g., `PLANNING_v4.5.0_TRANSITION_NOTES.md`). The first such doc was hand-written for v4.5.0 as the proof-of-pattern; future cycles use `/transition-doc`.

Establishes a three-layer model for cross-session knowledge: durable memory (cross-cutting principles), VibeMemo (load-bearing decisions with structured rationale), transition docs (per-cycle session narrative). Different shapes for different jobs.
→ [007](entries/007_transition_doc_skill_pattern.json)

## Solar-Gain Cover Controller (v3.8.4-H3) — Retroactive WHY

The HVAC coordinator's `CoverController` (`hvac_covers.py`) was shipped in v3.8.4-H3 (Mar 2025) to close common-area blinds during peak afternoon solar hours in cooling season — Apr-Oct, 13:00-18:00 local, close at 85F outdoor / open at 80F (5F hysteresis), 2hr manual override respect, 120s command-window dedup. README_v3.8.4 documented the WHAT in 41 lines but the WHY was never written down: the constants are calibrated to a Texas climate + west-facing windows + the user's cover hardware movement times, and the dual-controller design (this one + per-room cover automation) was a deliberate boundary that requires every per-room cover behavior to be ALSO applied in `hvac_covers.py` or the two paths drift.

The drift bit hard on 2026-05-09: v4.5.0.4 made the per-room cover dispatch tilt-aware for venetian blinds, v4.5.6 made the per-room "already in target" gate helpers tilt-aware too, but neither cycle threaded `CONF_COVER_TYPE` through `hvac_covers.py:_command_covers`. So when HVAC reopened the covers at 18:00 today (solar window over), it called `cover.open_cover` on Study A and Master Bedroom — and venetian blinds raised the whole blind to position=100 with slats stuck at tilt=0 instead of tilting slats open at position=0. Same Bug Class #33 (Partial Fix — Sibling Helpers Skipped) that's already in QUALITY_CONTEXT.md, third hit. v4.5.9 (forthcoming) closes the gap.

The retroactive capture preserves four decisions: (1) house-level controller separate from per-room because the trigger conditions are house-level and cooling-season AC is the largest controllable load on a Texas tariff; (2) outdoor-temp trigger over solar-irradiance forecast for simplicity and because heat already accumulating is what AC must remove; (3) bulk-transition single-bool state machine because per-cover state would just produce identical decisions at higher complexity; (4) dual-source cover discovery (CM-level explicit list + room-derived) so the user neither has to enumerate every cover nor is limited to room covers. Constants live in `hvac_const.py` as a single source of truth.
→ [008](entries/008_solar_gain_cover_controller_v384_h3.json)

## 2026-05 → 2026-06 Continuation (entries 009–025)

*Bridged from the structured JSON entries on 2026-06-23 — the per-entry prose had lapsed at 008 while 17 load-bearing entries accumulated. Synthesized thematically, not one-section-per-entry, to stay readable. The JSON entries remain the authoritative detail; this is the connective "why" tissue.*

### Climate & HVAC livability (009–012)
The cover-drift from entry 008 closed in **v4.5.9**: HVAC cover dispatch became tilt-aware AND intent-respecting — the controller stopped treating discovered covers as a homogeneous bulk and now tracks per-cover intent + a closed-set, after the operator flagged HVAC opening covers the room never wanted open [009]. **v4.5.10** promoted 7 hardcoded HVAC values to runtime Numbers + 5 form-only thresholds + a solar-gain-cover master switch [010]. **v4.5.11/.12** replaced the old "still hot" AC reset trigger with an energy-aware overshoot detector (temp-at-target AND sustained kWh-rate) plus a soft nudge before hard reset, restart-surviving compressor protection, master-OFF-on-install — shipped in two slices to get protection live fast while observability wrapped a week of real data [011, 012].

### The quality-bar spine (012, 013, 014, 018)
The v4.5.11.x marathon (5 deploys / 14h) forced a durable correction: the operator reset the review philosophy to **"2 independent staff-level reviews using best practices; bug-classes only guard against repeating old mistakes"** — mental execution over checklist-matching — and a runtime smoke-test framework was built [012]. **Bug Class #46** got its canonical fix: drop eager setup-time migrations, derive the migrated value lazily at read time (zero setup side-effects) [013]. After v4.7.4.3 shipped with literal conflict markers, the **pre-deploy zero-bugs gate** became mandatory (conflict-grep + py_compile + tests + baseline-diff before every deploy.sh) [014]. Later the operator formalized **Tier-2-DB (3 framing-disjoint reviews) for ALL regression-prone work**, not just DB cycles — different framings can't share blind spots [018]. (This spine is what entry 025's Tier-3 protocol extends to a 4th adversarial-completeness pass.)

### Energy & arbitrage evolution (015, 016, 017, 018, 023, 024)
A long thread hardened URA's energy brain. Zone Manager UX split house-zones (picker) from the HVAC coordinator's silent canonical merge, auto-mirroring shared-thermostat zones ("Definitely C") [015]. EVSE charging became an idempotent house-wide backstop — re-pause every tick regardless of who turned it on, asymmetric SOC thresholds — after self-pause-induced solar-overflow bouncing [016]; then proactive off-peak ensure-on so cars actually charge overnight, with the vestigial comfort sliders kept and folded into the Optimization Coordinator's comfort dimension rather than deleted [017]. **v4.7.28/.29** shipped EV off-peak + a ~1000x energy unit/sign reconciliation, then fixed a day-boundary-blind summer mid_peak hold (Bug Class #51) [018]. A flaky Envoy drove **v5.3.7** boot-decoupling (after_dependencies removal, registration-race + RestoreEntity-poisoning fixes, Bug Class #52), and a live manual-arbitrage exercise exposed a forecast-class-only gate ignoring realized solar divergence → **v5.3.8** "attain" phase, the heaviest cycle the repo has run (1 build + 4 fix-ups + 7 reviews) because builders twice shipped HOLD as a one-tick emission instead of a state [023]. **v5.3.9/5.4.0/5.4.1** marched the attain ladder + HC pre-conditioning + load-shed correctness, and the **inclement-weather reserve** was fully designed to replace Enphase Storm Guard with the operator's own NWS-alert + local-weather fusion — hold as a depth ladder × TOU × solar-horizon, gated on outage-relevance not severity [024].

### The Optimization Coordinator saga (020, 021, 022, 024)
URA's largest subsystem was scoped agentic-first (graduated autonomy, provider-agnostic LLM) [020], shipped overnight as a MAJOR **v5.0.0→v5.3.0** … and took the house DOWN: per-finding one-by-one writes + Sensor-Health firing per boot-unavailable room saturated the single shared write queue → core writes starved → watchdog restart → **rollback to v4.7.33**. Remediated v5.2.2 (batch persist + boot-settle gate), an adversarial review catching a SECOND O(N) write channel. **Durable lesson: per-cycle write-VOLUME must be reviewed/gated per write-channel, not just by bug-class** [021]. Phase 5 then completed across v5.3.1–v5.3.4 (energy normalization, routine-aware forecaster, autonomy admin surface, sibling-veto handshake), and the operator set **model tiering** — the judgment tier (orchestrator/reviewer/planner, later Opus-4.8) catches incident-class bugs cheaper models miss; builders/validator stay cheaper [022]. The OC LLM's own failures were later root-caused to **config, not model** — the HA AI-Task "recommended" preset starved structured output; explicit Haiku + token/thinking budget fixed it [024].

### Presence correctness & the recurring failure mode (019, 020)
A hygiene marathon (**v4.7.30–.32.1**) closed the DO queue and surfaced **Bug Class #53 (computed-but-not-consumed)**: the motionless-occupant veto was silently DEAD because ZoneManager was keyed by zone_id but looked up by name [019]. A Frigate motion sensor was trusted wholesale as "camera person detection" (no device_class filter) → operator removed it from config, code guard backlogged [020]. The recurring meta-lesson threaded through both: **verify, don't assume** (stale memos, scope-format guesses) — the discipline behind the No-Fabrication / institutional-context-first rules.

### Ship march v5.6.0→v5.7.2 + the silent-actuator lesson [026]
Entry 025's cycle SHIPPED (v5.6.0) along with v5.6.1-3 strings, v5.7.0 WS-A guest-detection trust (lost-but-away admitted to the away veto; path-α byte-identical), and v5.7.1 Energy Saver Pre-Cool (PV-gated unification retiring solar banking; one migration L3 FAIL tracked). Then the arc's real teaching: "lighting broke after the fan changes" was actually a **dead Shelly relay** — URA detected occupancy perfectly and its service calls no-op'd against an unavailable entity, while the per-room unavailable-entities tracker read 0 because it only watched *inputs*. v5.7.2 shipped the fix (actuator coverage + structured reason tokens incl. `offline_since_restart`), live-validated when the flaky Shelly re-dropped on cue. The triage decision pattern: **D1 visibility ship / D2 reconcile study-then-design (live-intent-only, never snapshots — RestoreEntity is restart-only, core has no reconnect primitive) / D3 auto-reload parked on the OC autonomy ladder** (reloads proven near-useless for off-WiFi devices: 47/48 Shelly entries stayed "loaded" while devices were dark). New durable split, sibling to 025's: input failures degrade *detection*, actuator failures degrade *actuation* — different visibility, different recovery.

### Bathroom-Exhaust Intelligence (Tier 3) [025] — since shipped as v5.6.0, see [026]
Humidity/exhaust fans decoupled to always room-owned (one owner, invariant I1), plus EMA spike detection + presence-proportional purge + a Wet-Room flag + comfort-RANGE scoring. The load-bearing decision is the **independence boundary**: venting is automation a user can disable (master-automation + toggle #3; ManualMode suppresses it), but the **max-runtime safety cap is a universal bound** that transcends every toggle — URA's first explicit "ungated safety bound vs gated automation" split, a pattern future safety bounds should follow. Tier-3's focused re-passes again earned their keep, finding gate-leak N+1 sites the fix-up itself exposed, and named a systemic gap: cycles touching the coordinator gate stack need tests that drive the real `_async_update_data`, not just direct-handler calls. Bug Class #53 hit 3× in this single cycle (dead D3 timer, unscored heat bound, gate-trapped call). Not yet shipped — awaiting fix-up verification + operator deploy checkpoint.

## Open Questions

- After 2-4 weeks of B-Lite divergence data, is automatic prediction source switching (B3) safe for financial predictions?
- Should Envoy be demoted from primary for cumulative tracking entirely, using Emporia mains as the base?
- Non-blocking fire-and-forget writes (Option C from 002) — eliminates timeouts entirely but changes error handling model
- Write batching (Option D from 002) — groups writes into single transactions, reduces count by ~70%
- v4.5.0 calibration: does `arbitrage_charge_lead_time_min=360` produce the right safety margin? Adjust in v4.5.1 based on observed charge durations + HOLD efficacy.
- Does barneyonline charge-rate-control HACS work on user's Enphase firmware? If yes, v4.6.x rewrites D4 to allow concurrent battery + EV charging.
- After 3-4 transition docs, is the pattern producing artifacts future sessions actually consume? If not, replace with embedded rationale or more aggressive vibememo entries.
- The retroactive WHY pattern (entry 008) — should we systematically backfill the rest of pre-vibememo URA? Candidates: per-room cover automation (v3.6.39 5-mode redesign + v3.6.40 mapping fix), HVAC override arrester (Bug Class avoidance), URA mirror pattern (RestoreEntity = runtime, entry.options = seed only — already in feedback memory but not in vibememo). Backfill is cheap when triggered by a lookback like today's; expensive to do as a standalone exercise.

### Current (2026-06)
- Bathroom-exhaust Tier-3 cycle [025] is in its 2nd fix-up — does the Option-2 gate boundary hold under the full coordinator-gate cross-product, and do the new `_async_update_data` integration tests actually fail when a gate-trapped call site is neutered? (Ship-blocking until verified + operator checkpoint.)
- Inclement-weather reserve [024] is plan-final but NOT built — pre-req D0 (operator wires the Dynamic Weather entity into the 3-way cross-check) before build.
- Guest-mode false positive: an away person stamped `tracking_status=LOST` is excluded from "trusted" + an outdoor "Outside" zone phantom-census occupant → false GUEST. Fix A (lost-but-away counted as away) vs Fix B (exclude outdoor zones from census) — not yet built; override currently pinned.
- Jaya bedroom occupancy was fixed (BLE room-mapping + zigbee mmwave) but now FLAPS on jittery Bermuda area placement — tune Bermuda vs add an occupancy debounce?
- Is the per-cycle write-VOLUME gate from the OC incident [021] now a standing review item, or still ad-hoc? It only fires when a reviewer thinks to ask.
- Model tiering [022, 024]: with the Opus-4.8 reviewer now exceeding the 4.7 builder, is the reviewer reliably the strongest link — and should the builder move up too?

**Update 2026-07-13 late [029]:** The failover-map open questions above are ANSWERED — by a 10-minute recorder probe, not a build. D3/D4 rejected on measured staleness; battery-power sign resolved passively; cloud grid-power exposed as a broken derivation (never admit). Operator coined the two disciplines now in CLAUDE.md: **Measure Before You Build** (probe-first gate for empirically-scoped cycles) and **hand-build the fixture before automating**. Week-window probe re-run due 2026-07-20 = final go/no-go on the slimmed map (SOC + slow-production + auditability substrate).

**Update 2026-07-17 EOD [030]:** The EVSE drain-precedence build ("Battery-Aware EV Charging" — operator ratified Aware over First; the eval *weighs*, it doesn't fix a priority) hit four framing-disjoint Tier-3 reviews and all four said FIX-FIRST — with a diagnostic split: the invariant core (floor supremacy, gate order, write-verify) survived falsification and all 17 mutation anchors held, while every CRIT/HIGH lived in the actuation lifecycle *seams* — a reversion condition that self-triggers off DP's own pause (the v5.15.0 flap reborn), a second plug-in never paused (only reviewer D found it; the fourth framing paid again), a house_load stub that fell through a slice hand-off brief, no night gate at all. Fix-up 1/2 landed (8f2bfc9e), then a hard stop killed the session mid-flow; resume used git as ground truth because the pickup memo was a day stale — now itself a recorded lesson. Remaining before the combined deploy checkpoint (with cloud-reliance D2): fix-up 2/2, D's re-pass on the new exit surface, orchestrator mutation verification, record + README. Ships CONF_DP_ENABLE OFF.

**Update 2026-07-17 night [031]:** Double-ship day closed naked: v5.20.0 + v5.21.0 both live and validated, BAEC ACTIVATED by the operator (~20:47) after shadow-mode + a live API-driven proof that the options toggle flips the physical switch with no reload. The Envoy re-termination closed the last two provisional validations in one stroke — recorder exclusion proven (states flow, statistics frozen at 666 min) and BOTH D2 legs organically exercised (cloud_fallback at boot, then primary_envoy with the cloud-lag alert live at 2776s). Operator redirected placement twice and was right twice: BAEC folded into the Energy step (the sectioned-form prior art existed — my "form→menu conversion needed" claim was wrong), and the D2 thresholds became labeled operator knobs. Lesson pinned: Institutional Context First applies to my own claims about UI structure, not just proposed constants. Remaining organic watch: the first real BAEC transition tonight.

**Update 2026-09-04/05 [089] — 6.0.0 identity arc pivots to BLE-primary.** After the v5.94.x device/entity de-frag arc closed (085–088: reorg shipped, then a 3-hotfix tail — the shell removal was dead-on-arrival because it tuple-unpacked `device.identifiers` and bond/homekit 3-tuples raised ValueError; live-registry ground truth drove each fix), the identity cycle began — and measure-before-build overturned the whole premise. Live probes: Frigate face is INTERMITTENT (not dead, not steadily alive — proven ~135 named recognitions in an 08-30/31 burst, then the service flapped unavailable), egress-face `person_id` attach is ~0% (1/7265) because faces fire at INTERIOR cameras while lingering, ~never within the 45s door-crossing window. The reliable named crossing signal is BLE `person.<slug>` home↔away transitions (~106/14d, Bermuda-sourced). Decision (operator: "parsimony should win if compelling"): **BLE-primary + face-corroboration**, realized by EXTENDING the existing `_resolve_egress_face_identity` resolver, not rebuilding. Identity model clarified by the operator: Oji/Ezinne/Jaya = BLE+face residents; Ziri = away-for-months (expected-silent); **Ojini = his mum = a face-known GUEST with no BLE** (`guest:<name>` class, must be named not dropped). Fail-safe (his #1 concern) is a hard requirement: a centralized `_face_suppressed_now()` checkpoint every face path routes through. The plan-review + 3 build reviews earned their keep hard — caught 4 independent fail-safe leaks (wrong health entity `binary_sensor` vs `sensor.frigate_status_2`, frozen-face, ungated raw census, ungated presence path) + 2 zero-config attribution bugs (Ojini→Oji; a wall-tablet `"ble"`-substring forging BLE legs). Fix-up centralized suppression + closed both. **Open at the ship checkpoint:** the *flapping*-frozen-face residual — FS-2's `age(now−last_changed)` gate is evaded by a sensor that re-stamps on each flap; close via a `person not_home` veto backport + flap guard. Frigate memory corrected from "down" → INTERMITTENT. D1 (real-time MQTT `frigate_events` face bridge) is the follow-on after the Frigate hardware restart.
