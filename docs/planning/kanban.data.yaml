meta:
  last_reconciled: '2026-09-04'
  history_file: docs/planning/kanban.history.yaml
  artifact_url: https://claude.ai/code/artifact/5748808f-5f16-41e8-a455-c3c59ed40149
  target_host: urakanban.phalanxmadrone.com
  columns:
  - inbox
  - pre_planning
  - planned
  - in_progress
  - review
  - shipped_organic
  - waiting_operator
  - waiting_me
  - parked
  last_reconciled_note: Reconciled 2026-08-17 overnight pass. NOTHING SHIPPED since v5.78.0 — newest tag
    is v5.78.0 and manifest reads v5.78.0. README_v5.79.0.md exists but is an UNSHIPPED pre-deploy draft,
    which is why the rung-3 staleness rule (last_reconciled older than newest README) fired as a FALSE
    POSITIVE. The generator should compare against SHIPPED versions (git tag / manifest), not merely the
    presence of a README file.
  sequence_2026_08_17: 'Operator sequence pick 2026-08-17: (1) FINISH the in-flight census/guest/presence
    cycles — cycle 1 CENSUS-GHOST-DEDUP-1 shipped v5.79.0 (L3/L8 organic Wed); cycle 2 CENSUS-ACCURACY-1
    building (D1+D2) -> reviews -> deploy; cycle 3 EXTERIOR-GUEST-EGRESS-1 D0 probe running -> gates build.
    (2) Related FAST-FOLLOWS: DOC-DRIFT-ZONE-AWAY-1 (docs, running now), GUEST-ROOM-LOCATION-MATCH-1 (verified),
    then the code fast-follows D3-BEHAVIOURAL-COVERAGE-1 / PATHBETA-VESTIGIAL-1 / GUEST-IDENTITY-PHONE-LEFT-BEHIND-1
    (queue AFTER cycle-2 merge to avoid worktree-base conflicts). (3) THEN memory epic finish-out: MEMORY-WRITERS-1.
    Parallelize read-only/docs work; serialize code work behind the cycle-2 merge.'
cards:
- id: FORECAST-ACCURACY-UNKNOWN-MASK-1
  title: forecast_accuracy sensor reads 'unknown' while it actually means the forecaster is BADLY inaccurate (rolling accuracy <=0), masking a real signal
  thread: energy
  status: done
  shipped_version: v5.92.2
  disposition_2026_09_01: 'DONE — discriminator met live at deploy-time (not a soak). sensor.ura_energy_
    coordinator_forecast_accuracy = 35.9 (numeric, was unknown) + status=stale + eval_age_days=2;
    adjustment_factor=1.3 unchanged (control path byte-identical); 0 new URA errors. README_v5.92.2 carries
    the validation table. Soak-exit binary: discriminator met -> dispose done, no lingering organic.'
  build_review_2026_09_01: 'Built (feature/forecast-accuracy-unmask @ 8db574674, 10 tests, :850-mutation→RED
    verified). Build-review B (control-path) = SHIP (byte-identity confirmed, energy.py not even in the diff).
    Build-review A (correctness) = FIX-REQUIRED, one HIGH: the sensor.py mask fix has ZERO test coverage (all
    tests drive AccuracyTracker, none the sensor) → D1 neuter-deletable (hollow anchor #62). Fix pass in flight:
    add 4-signature sensor tests (RED on native_value neuter + ladder reorder); make test7 discriminating or
    drop; test3 fixture -200→-75 (real value); correct the vacuous ±200-clamp docstrings; + B LOWs (dt_util.now
    fallback, _LOGGER.debug on bare excepts, stale line cite). Production LOGIC confirmed correct by both.'
  created: 2026-09-01 16:15
  updated: 2026-09-01 18:10
  refinement_status: refined
  approval: implied
  approved_by: 'operator 2026-09-01 ("Approved to build not just to card. Since we''re in the code area.")'
  tags: [no-fabrication-verify, tier-2db]
  refinement:
  - 'Investigation confirmed the metric is DECISION-AFFECTING, not display-only: adjustment_factor (clamp
    [0.7,1.3]) multiplies _predicted_consumption_kwh, consumed by the DP house-load estimator (energy.py:4338).
    So this is Tier 2-DB, not a cosmetic sensor fix. Two confirmed defects (>0-else-None mask conflating
    NO-DATA with POOR; pct_error blowup on near-zero predictions poisoning the 7-window mean) + a stale-eval
    VISIBILITY gap (not a scheduler bug — data-availability drops null actual_kwh). Plan: PLANNING_forecast_accuracy_fix.md.'
  problem_solution:
  - 'Problem: sensor.ura_energy_coordinator_forecast_accuracy shows unknown, but it has 30 samples and is
     active (last eval 2026-08-30) — it is NOT missing data. The value is hidden because rolling_accuracy
     computed to zero-or-negative, and the sensor code hides any accuracy <=0 as unknown. So the operator
     cannot tell ''no forecast yet'' from ''the forecaster is very inaccurate.'' Underneath, the forecaster''s
     average percent error is >=100% over the window (rolling_accuracy = 100 - abs(avg_pct_error)). Solution:
     (a) surface a poor-but-real accuracy instead of masking <=0 as unknown; (b) investigate WHY accuracy is
     that bad — likely the pct_error formula dividing by max(predicted,0.1) blows up on near-zero predictions,
     or the (known-partial) forecaster genuinely predicts ~0 some days; (c) check the daily eval — last_eval
     is 2 days stale (08-30), it may have stopped accruing.'
  origin: {date: '2026-09-01', gist: operator side-quest — why is forecast_accuracy unknown}
  why: 'Live: state=unknown, attrs samples=30 status=active adjustment_factor=1.3 last_eval_date=2026-08-30.
    Code: rolling_accuracy=100-abs(avg_pct_error) (energy_forecast.py rolling_accuracy); sensor native_value
    returns None when accuracy<=0 (sensor.py:11373); pct_error=error/max(abs(predicted),0.1)*100 (energy_forecast.py:850)'
  refinement:
  - 'PLAN-REVIEW = FIX-REQUIRED (correct catch): pct_error feeds BOTH the sensor AND adjustment_factor (the DP
    house-load controller), so D2''s new denominator is a CONTROL-LOOP change, not a display fix — it
    asymmetrically damps under-prediction correction (the exact arm get_adjustment_factor exists to boost) ->
    systematic downward bias in DP house_load, plus a >10% first-boot step breaching the plan''s own gate.
    RESOLUTION (reviewer-recommended, adopted): SPLIT THE METRICS — keep pct_error (control) byte-identical, add
    pct_error_bounded consumed ONLY by rolling_accuracy + the sensor. This delivers the operator''s ask (unmask
    the real accuracy signal) with ZERO control-path change -> drops from Tier 2-DB to a Tier 1-2 display fix,
    and dissolves HIGH-1 (_solar_forecast_error_baseline keeps reading the unchanged pct_error). Also: recompute
    bounded metric from stored predicted/actual on restore (not clamp); fix STALE_EVAL_DAYS >= /=1; real
    POOR_THRESHOLD_PCT (~50); verify live entity id in D0.'
  - 'Rev 2 WRITTEN + CONFIRM-REVIEWED: split is CLEAN (control path genuinely isolated via return-dict
    energy_forecast.py:862; restore split sound; no decision leak; no schema migration; Tier 1-2). One small
    plan-level gap being fixed: M1 — the byte-identity test must drive the PRODUCTION evaluate_accuracy
    (assert pct_error==44900 while pct_error_bounded==200) so it goes RED if line 850 is mutated; test 7 alone
    stays green on an in-place clamp. + L1 (eval_age_days None>=2 ordering) / L2 (invariant wording: signature
    not value) / L3 (complete rolling_accuracy consumer map). No design re-review needed after.'
  next: 'Planner applying M1/L1/L2/L3 (ac7c6c69882c02fd6). Then BUILD-READY: D1 mask + D3 stale-visibility +
    bounded sensor-only metric + 9 tests incl. the production-computation byte-identity anchor. Control path untouched.'
  refs: ['docs/planning/PLANNING_forecast_accuracy_fix.md', 'sensor.py:11344/11373', 'energy_forecast.py:794/833/850 rolling_accuracy+evaluate_accuracy', 'energy.py:2812 daily eval, :4338 DP house-load consumer, :9668 property', 'PLANNING_v4.7.x_advanced_energy_management.md E5 origin']
- id: DOC-MOUNT-PATH-STALE-1
  title: CLAUDE.md + skills reference a Samba mount path that does not exist on this machine (/Users/ojiudezue
    vs the real /Users/okosisi) — a username migration left stale paths across docs + a vibememo user dir
  thread: platform
  status: inbox
  created: 2026-09-01 17:40
  updated: 2026-09-01 17:40
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: instructions tell an agent the HA config is mounted at /Users/ojiudezue/ha-config, but that
     path does not exist on this machine (verified: ls -> No such file or directory); the live mount is
     /Users/okosisi/ha-config (.HA_VERSION present). An agent following the doc hits a missing mount and may
     fall back to a wrong path (this exact trap nearly caused the EC-1 builder to mint _2 duplicate entities).
     The stale username ojiudezue appears in CLAUDE.md (2x) and across ~10 skill files + .vibememo. Solution:
     correct the MOUNT PATHS to /Users/okosisi. NOTE this is two DISTINCT problems, do not blind find-replace:
     (a) stale filesystem mount paths (safe to fix), and (b) the vibememo USER IDENTITY dir .vibememo/users/ojiudezue/
     + FORMAT.md schema refs (renaming that dir is a structural vibememo change, NOT a path fix) — scope separately.'
  origin: {date: '2026-09-01', gist: EC-1 plan-review side-finding — documented mount path does not exist}
  why: 'Verified by ls: /Users/ojiudezue/ha-config/.HA_VERSION absent; /Users/okosisi/ha-config/.HA_VERSION
    present (8 bytes, mounted). git grep ojiudezue spans CLAUDE.md + .claude/skills/*/SKILL.md + .vibememo/.'
  constraints:
  - Do NOT blind-replace ojiudezue->okosisi; the .vibememo/users/ojiudezue/ dir is a user identity, not a path.
  next: 'Operator decision: fix the mount PATHS (CLAUDE.md Data Source Verification + skills) to /Users/okosisi;
    handle the vibememo user-dir rename separately (or leave — it is an identity, not a path). Low urgency, real footgun.'
  refs: ['CLAUDE.md Data Source Verification section', 'AUDIT/plan-review of EC-1 2026-09-01']
- id: HA-2026-9-VIA-DEVICE-COMPAT-1
  title: HA 2026.9 broke ALL coordinator entities — deprecated `via_device` DeviceInfo param is now a hard
    error; every coordinator entity failed to add (live outage)
  thread: platform
  status: shipped_organic
  disposition_2026_09_03: 'DONE — outage RESOLVED by v5.92.3. Live post-restart: house_state=home_day,
    ev_charging_status=charging (both were unavailable, now fresh 10:39:49); CM entry loaded; zero new "Error
    adding entity None" post-restart. Discriminator met at deploy-time. Cosmetic device nesting deferred to
    the device/entity architecture plan (via_device_id path).'
  shipped_version: v5.94.0
  created: 2026-09-03 10:30
  updated: 2026-09-03 10:30
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("Yes. build and fix and deploy.")'
  tags: [hotfix, no-fabrication-verify, ha-2026.9-compat]
  problem_solution:
  - 'Problem: after the HA 2026.9 upgrade, every URA Coordinator-Manager entity (Battery Strategy, EV Charging,
     House State, Security, Energy Situation, all coordinator numbers/switches/selects/buttons) went unavailable.
     2026.9 turned the deprecated DeviceInfo `via_device` parameter into a hard RuntimeError, so all 109 coordinator
     entities failed to register ("Error adding entity None", 319x in the live log). Room entities (no via_device)
     were fine. Solution: remove all 109 `via_device=(DOMAIN, ...)` lines; identifiers unchanged so nothing
     re-registers, entities add cleanly again. Cosmetic device-tree nesting lost — restore properly later via
     via_device_id (see the device/entity architecture plan).'
  origin: {date: '2026-09-03', gist: operator dashboard showed widespread Unavailable/NaN after 2026.9 upgrade; live diagnosis found the via_device RuntimeError}
  why: 'Live traceback: RuntimeError "device_registry.async_get_or_create with a deprecated via_device parameter;
    use via_device_id". 109 DeviceInfo via_device declarations across 10 files. Reload did not help (re-runs the
    failing add). Not caused by the v5.92.x cycles (v5.92.2 validated with House State working pre-upgrade).'
  next: 'SHIPPING v5.92.3 (build verified: 109->0, deletions-only diff, py_compile clean). Deploy + restart +
    live-validate coordinator entities repopulate. Follow-up: proper device nesting under 2026.9 patterns folded
    into the device/entity architecture plan.'
  refs: ['docs/readmes/README_v5.92.3.md', 'feature/via-device-2026-9-hotfix@260a5b9dc']
- id: ENVOY-DRAIN-ARM-STALE-CT-1
  title: Drain-pause does NOT ARM a new pause under a stale (not unavailable) battery CT — a genuinely
    discharging battery with low SOC can be drained by the EV during a blind-CT window
  thread: energy
  status: parked
  created: 2026-09-03 11:10
  updated: 2026-09-03 11:10
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("Accepted" — accept the gap + card it)'
  tags: [energy, regression-prone, ha-stale-telemetry]
  problem_solution:
  - 'Problem: with arm-on-unknown dropped from the Envoy shared-staleness cycle (its "fix" strictly worse — it
     stranded the EV overnight, HIGH-1), a battery that is genuinely discharging while its Envoy CT is STALE
     (frozen value, not "unavailable") + SOC low will NOT get a new drain-pause armed, so the EV keeps drawing
     it toward reserve until the CT recovers. Solution (deferred): a bounded arm-under-stale (arm only after N
     consecutive unknown ticks with low SOC, released by must_start_by / CT-recovery), OR distinguish stale from
     dead at the producer. Accepted-as-gap for now: hardware inverter reserve still protects the battery floor,
     and the existing drain pause still HOLDS across a stale window (release-side fix shipped in the cycle).'
  origin: {date: '2026-09-03', gist: Envoy Tier-3 review D-MED-3 — arm-side hole; arm-on-unknown reversed as unsafe; operator accepted the gap}
  why: 'Envoy re-review-1 HIGH-1: arm-on-unknown had no nighttime release (must_start_by does not touch
    _paused_by_battery_drain) -> overnight strand. Dropping it leaves the arm-side gap. must_start_by + hardware
    reserve are the backstops. Bounded-arm is the proper fix, deferred (marginal benefit vs ingredient risk).'
  constraints:
  - 'Any arm-under-stale MUST have an enumerated release (the exact failure that killed arm-on-unknown).'
  next: 'Revive when: a real blind-CT battery-drain event is observed, OR the operator wants active protection
    during stale windows. Design a bounded arm-under-stale with a guaranteed release + hysteresis (no oscillator).'
  refs: ['docs/planning/PLANNING_shared_power_read_staleness.md', 'Envoy Tier-3 reviews A/B/C/D + re-reviews 2026-09-03']
- id: BREAKER-GRIDCAP-STALE-TELEMETRY-1
  title: Breaker-guard + grid-cap behavior under STALE (not unavailable) Envoy telemetry — needs a proper
    design, split out of the Envoy shared-staleness cycle after it over-corrected
  thread: energy
  status: parked
  created: 2026-09-01 20:30
  updated: 2026-09-01 20:30
  refinement_status: initial
  approval: unreviewed
  tags: [tier-3, no-fabrication-verify, regression-prone]
  problem_solution:
  - 'Problem: when an Envoy power sensor freezes at a stale-but-valid number (not "unavailable"), the 12kW
    breaker guard and the grid-import cap need to decide what to do — but a naive "treat stale as a trip / skip
    the function" is WRONG: it locks the whole night''s arbitrage charge on a guard that is off by default,
    manufactures false over-caps (the battery''s own grid-charge stops being subtracted), and removes the only
    release path for grid-cap pauses (stranding the EV). Solution: a careful, separate cycle that (a) makes
    _effective_import_kw propagate "battery charge unknown" instead of substituting 0, (b) gates the breaker
    trip on the guard being actually enabled (finite threshold), (c) aborts a stale tick WITHOUT sticky-locking
    the chunk, (d) preserves grid-cap release/discharge under stale, (e) reconciles with the v5.17.5 anti-abort
    precedent (a stale blip should HOLD, not abort). Until then, breaker/grid-cap keep develop behavior; the
    staleness helper still SURFACES net_power staleness via the D-OBS observability tile.'
  origin: {date: '2026-09-01', gist: split out of ENVOY-PRODUCTION-STALE-1 after 4 Tier-3 reviews found the breaker/grid-cap fail-closed over-corrected}
  why: 'B-CRIT-1 (locks chunk on default-disabled guard), D-HIGH-1 (_effective_import_kw 0-substitution → false
    over-cap even guard-enabled), D-HIGH-2 (sticky lock = whole night lost), B-HIGH-1 (grid-cap EV strand),
    B-HIGH-2 (ATTAIN unwind reverses v5.17.5 D-MED-1). Marginal-benefit: LOW value (guard off by default; rare
    physical-breaker-trip-during-blind-telemetry) vs HIGH ingredient risk (arbitrage state machine, shared
    primitive _effective_import_kw, sticky latches). Defer, do it right.'
  constraints:
  - 'Tier 3 — arbitrage state machine + shared primitive. Honor the v5.17.5 anti-abort precedent.'
  parked_alts:
  - 'Fix it inside the Envoy shared-staleness cycle — REJECTED: would keep that cycle regression-prone and block
    the clean solar/drain/observability wins; the marginal benefit does not pay for the ingredient risk now.'
  next: 'Revive when: a real blind-telemetry breaker-trip is observed, OR the operator enables the grid-import
    guard and wants stale-safe behavior. Design: battery-unknown propagation in _effective_import_kw + isfinite-guard
    gate + non-sticky abort + grid-cap discharge-under-stale. Reuse C''s _run_attain_branch anchor recipe
    (scratchpad/proposed_bc_anchor.py, precedent test_arbitrage_grid_import_guard_expose.py:525-670).'
  refs: ['docs/planning/PLANNING_shared_power_read_staleness.md', 'Envoy Tier-3 reviews A/B/C/D 2026-09-01']
- id: IDENTITY-FUSION-PRODUCER-1
  title: Identity fusion producer — BLE-primary egress person_id + face corroboration + producer-outage fail-safe (D2/D3/D4)
  thread: identity
  status: in_progress
  created: 2026-09-05 00:40
  updated: 2026-09-05 00:40
  refinement_status: refined
  approval: explicit
  approved_by: 'operator 2026-09-05 ("Ship and card")'
  tags: [tier-2db, identity, fail-safe]
  problem_solution:
  - 'Problem: egress person_id attach was ~0% (1/7265) because face is intermittent and fires at interior cameras,
     ~never within the door-crossing window. Solution: make the always-on BLE person.<slug> home<->away transition
     the PRIMARY named crossing signal in the existing resolver, with face as confidence corroboration and a
     known-face-guest class; add a centralized producer-outage fail-safe (suppress all face paths when the
     producer is down/drilled) so a Frigate flap can never misattribute or stall.'
  origin: {date: '2026-09-05', gist: 6.0.0 identity arc, producer half — pivoted to BLE-primary after measure-before-build}
  why: 'Face-first was built on a refuted premise; BLE transitions are the reliable named crossing edge. Fail-safe
    is the operator #1 concern. Extend the resolver, do not rebuild.'
  next: 'Ship v5.95.0 -> live fail-safe drill (Frigate up) + attach-rate validation -> README write-back. Follow-ons:
    D1 real-time MQTT face bridge (after Frigate restart), IDENTITY-FLAPPING-FACE-VETO-1 (residual), the 6.0.0 consumers.'
  refs: ['docs/planning/PLANNING_identity_fusion_producer_2026_09.md', 'docs/readmes/README_v5.95.0.md']
  sibling_of: [FRIGATE-SUBLABEL-FACE-BRIDGE-1, IDENTITY-FLAPPING-FACE-VETO-1]
- id: IDENTITY-FLAPPING-FACE-VETO-1
  title: Fail-safe residual — a FLAPPING-frozen Frigate face sensor evades the staleness gate and can misattribute a crossing
  thread: identity
  status: planned
  created: 2026-09-05 00:30
  updated: 2026-09-05 00:30
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-05 ("Ship and card") — ship the fusion cycle, card this residual as a fast-follow'
  tags: [identity, fail-safe, tier-2]
  problem_solution:
  - 'Problem: the v5.95.0 identity fail-safe suppresses face identity when the producer is DOWN (frigate_status_2
     unavailable) or the face name is SILENTLY frozen (sensor holds an old value, last_changed genuinely old).
     It does NOT catch a FLAPPING-frozen sensor — one that flaps unavailable<->name and re-stamps last_changed on
     each flap, so the age(now-last_changed) staleness gate sees age~0 and lets the stale name through. While
     Frigate reports healthy, a stuck "Oji" can then be attributed to a different person (e.g. Jaya) crossing in
     the window with no BLE leg and no person-status veto. Solution: backport the person.<slug>=not_home veto
     (exists in the enhanced-census path, camera_census.py:4269-4290) onto the resolver multi-leg path, plus a
     flap/corroboration guard (reject a face leg whose entity is currently flapping / require corroboration for a
     single stale-suspect leg). The veto backport was deferred in v5.95.0 only because it collides with the
     observability outcome-label (vetoed vs no_leg) that a regression test asserts.'
  origin: {date: '2026-09-05', gist: Review D D-1 + orchestrator flapping-analysis at the v5.95.0 ship checkpoint}
  why: 'The producer-OUTAGE fail-safe (operator #1 concern) is closed + verified; this flapping-while-healthy
    variant is a narrower data-quality edge, but it corrupts person_id (the value the whole 6.0.0 arc is built
    on), so it must be closed — just not blocking the cycle that makes the fail-safe real.'
  constraints:
  - 'Backporting the not_home veto must NOT break test_d2b_vetoed_outcome_distinct_from_no_leg — update the
    observability outcome labels alongside (that coupling is why it was deferred).'
  - 'Only closes the away-subset via the veto; the flap-guard/corroboration handles the claimed-person-home case.'
  parked_alts: []
  knobs: []
  parsimony: {problem: 'flapping-frozen face can misattribute a crossing while Frigate reports healthy', verdict: BUILD}
  next: 'Tier-2 fast-follow after v5.95.0: backport person-not_home veto onto the resolver multi-leg path + a flap/corroboration guard; update the vetoed/no_leg observability labels; mutation-anchored test replaying the flapping-frozen repro (stuck Oji re-stamped + Jaya crosses -> NOT attributed to Oji).'
  refs: ['transit_validator.py::_resolve_egress_face_identity (FS-2 staleness gate)', 'camera_census.py:4269-4290 (enhanced-census not_home veto to backport)', 'docs/planning/PLANNING_identity_fusion_producer_2026_09.md (D4/§0)', 'docs/reviews review-D findings']
  spawned_from: FRIGATE-SUBLABEL-FACE-BRIDGE-1
  sibling_of: [FRIGATE-SUBLABEL-FACE-BRIDGE-1]
- id: FRIGATE-SUBLABEL-FACE-BRIDGE-1
  title: Frigate 0.17 recognizes resident faces but the NAME never reaches a URA-joinable entity — the real
    gate for the whole egress-identity (6.0.0) arc
  thread: identity
  status: investigating
  created: 2026-09-03 15:10
  live_correction_2026_09_04: 'GROUND-TRUTH PROBE (ssh ha, 2026-09-04) REFUTES this card''s stated reason.
    The premise "all 23 sensor.*_last_recognized_face_2 unavailable + frigate_status_2 disconnected" is
    STALE — Frigate face is LIVE and NAMING residents right now: 15 _last_recognized_face_2 sensors active
    7d, aggregate Oji 66 / Jaya 31 / Ojini 27 / Ezinne 10 / Ziri 1 (family_room Oji 41/Jaya 15/Ojini 8/
    Ezinne 5). YET person_entry_exit_events person_id attach is STILL 1 of 7,265. So the bug is NOT sensor
    availability and NOT "face down" — the names are demonstrably present on the _2 sensors and the egress
    JOIN still does not stamp them. REFRAME the investigation: why does the join fail to attach a name that
    IS present? Leading hypothesis = timing/real-time match — the _2 HA sensor updates on Frigate cadence,
    not synchronized to the egress-crossing instant, so the join window misses it; the MQTT sub_label
    (frigate/events, real-time) is the fix (name matched to the crossing moment). This card remains the
    FIRST deliverable of the 6.0.0 arc, with corrected framing. Also correct the depends_on cluster premise
    (EGRESS-IDENTITY-JOIN-GAP-1 et al. cite "face down" / "sensors unavailable" — refuted).'
  updated: 2026-09-03 15:10
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("Yes" — card the real gate; parked the Protect bridge for this)'
  tags: [measure-before-build, no-fabrication-verify, identity, tier-2]
  problem_solution:
  - 'Problem: the egress-identity producer is built, wired, and enabled, but it names almost nobody — 1 of 7253
     door crossings all-time (~0%) carry a person_id. The reason is NOT enrollment (Frigate recognizes all 4
     residents across cameras) and NOT the producer code — it is that Frigate 0.17 surfaces the recognized NAME
     as an MQTT sub_label on frigate/events, while the resolver joins on the LEGACY entities sensor.<cam>_last_
     recognized_face(_2), which are all currently unavailable (and frigate_status_2 reads disconnected). So the
     name never reaches a URA-consumable entity. Solution: bridge the Frigate 0.17 sub_label into a live entity/
     attribute the resolver can read (a template/MQTT sensor named sensor.<cam>_last_recognized_face_2, drop-in
     for camera_census._resolve_face_legs, OR extend _resolve_face_legs to read the sub_label source directly),
     then re-measure the producer attach rate as the discriminator.'
  origin: {date: '2026-09-03', gist: live investigation refuted the enrollment/bridge framing; found 23 face entities unavailable + frigate disconnected as the real gate}
  why: 'Live 2026-09-03: protect_list_known_faces + Protect smart-detections show face-rec UP (Ziri 80%, Oji 85%
    today); person_entry_exit_events = 1/7253 person_id all-time; all 23 sensor.*_last_recognized_face_2 =
    unavailable; frigate_status_2 = disconnected. transit_validator._resolve_egress_face_identity (transit_validator.py:1133,
    called :1447) + camera_census._resolve_face_legs (camera_census.py:2699, suffixes _last_recognized_face[_2]
    Frigate / _face_recognized[_2] Protect) are the join sites. D1 Protect bridge PARKED (parsimony); this is the
    Frigate primary path (was task #11, uncarded on the board until now).'
  constraints:
  - 'PRECONDITION: Frigate must be healthy — frigate_status_2 reads disconnected right now; confirm Frigate is
    up (homelab/host check, operator-side) before measuring sub_label output.'
  - 'MEASURE-BEFORE-BUILD: probe the live Frigate 0.17 sub_label shape on MQTT (frigate/events / tracked_object_update)
    — which of the 23 cameras carry resident names, at what latency — BEFORE building the bridge.'
  blocks:
  - EGRESS-IDENTITY-JOIN-GAP-1
  next: 'Investigate (measure): with Frigate confirmed up, probe frigate/events MQTT for person sub_labels
    (names + per-camera coverage + latency) via the existing frigate_mqtt bridge automation; then decide
    template/MQTT sensor drop-in vs extend _resolve_face_legs; then re-measure person_entry_exit_events attach rate.'
  refs: ['transit_validator.py:1133/1447', 'camera_census.py:2699 _resolve_face_legs', 'docs/planning/PLANNING_egress_identity_producer.md', 'live investigation 2026-09-03', 'reference_frigate1_retired_2suffix_permanent', 'FRIGATE-LEG-NAMING-1']
- id: SCALE-LEAN-ROOM-PROFILE-1
  title: Closets/hallways carry the full ~105-entity Smart Room profile — a lean profile for simple room
    types could cut ~1000+ registry rows (boot + .storage + registry-size lever)
  thread: platform
  status: investigating
  created: 2026-09-03 16:40
  updated: 2026-09-03 16:40
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("Card that. Very interesting.")'
  tags: [measure-before-build, scale, no-fabrication-verify]
  sibling_of: [CONFIG-SUBENTRIES-MIGRATION-1, DEVICE-INFO-HELPER-CONSOLIDATION-1]
  spawned_from: DEVICE-ENTITY-REORG-1
  problem_solution:
  - 'Problem: every room config entry — including closets and hallways — is registered as a full "Smart Room"
     with ~105 entities (AV Closet 105, Stair Closet 106, Study A Closet 105, Exercise Room Closet 105), the
     same profile as a full room, even though a closet has no HVAC zone, no media, no complex presence. Across
     ~15 simple room-types that is ~1500 registry rows for spaces that need a fraction. It inflates the entity
     registry (4626 rows total) → boot/reload parse time + .storage size (runtime/recorder is softened because
     ~1953 entities are disabled-by-default, but the registry-size + boot cost is real). Solution: a LEAN room
     profile for simple room-types (closet/hallway) that creates only the entities those spaces actually use.'
  origin: {date: '2026-09-03', gist: operator scale question during the device/entity reorg — closets carry full room entity profiles}
  why: 'Data-backed from the live Devices page: closets/hallways show ~105 entities each, same as full rooms.
    D0 baseline: 4626 entities, ~1953 disabled_by, 71 unavailable. Registry size + boot time are the scale
    surfaces the reorg itself does not touch.'
  constraints:
  - 'MEASURE-BEFORE-BUILD: probe enabled-entity count per room-TYPE + boot time + registry/.storage size FIRST,
    then decide the lean-profile entity set. Do not assume which entities a closet needs — enumerate.'
  next: 'Probe: enumerate enabled vs disabled entities per room_type (closet/hallway vs full room); measure boot
    time + core.entity_registry size. Then scope a lean profile (fewer platforms/entities for simple types).
    Separate cycle — NOT part of the device/entity reorg.'
  refs: ['docs/planning/AUDIT_device_entity_split_ownership_2026_09_03.md', 'live Devices page 2026-09-03']
- id: MENU-CONSISTENCY-1
  title: Config/options-flow menus are inconsistent (Zones use a dropdown to pick, CM uses a menu) — standardize
    on menus + consistent icon-in-label usage
  thread: platform
  status: shipped_organic
  shipped_version: v5.94.0
  created: 2026-09-03 17:10
  updated: 2026-09-03 18:05
  refinement_status: refined
  approval: explicit
  approved_by: 'operator 2026-09-03 ("Finally audit the menus. Make them consistent... We should use menus. I think standard.")'
  tags: [ux-consistency, no-fabrication-verify]
  problem_solution:
  - 'Problem: the config/options flows are inconsistent in how the operator picks WHAT to configure — Zones use
     a dropdown/select, the Coordinator Manager uses a menu — and icon-in-menu-label usage is uneven. Solution:
     standardize on MENUS (async_show_menu) for choosers across all flows (room/zone/CM/house/ZM), and make icon
     usage consistent everywhere it applies. Audit-first to size + confirm what "icons in menu labels" concretely
     means in this UI.'
  origin: {date: '2026-09-03', gist: operator menu-consistency directive during the device/entity reorg}
  why: 'Operator: menus are the standard; dropdown-to-pick (Zones) is the odd one out. Audit in flight to map
    every chooser (menu vs dropdown) + icon usage + size the standardization.'
  spawned_from: DEVICE-ENTITY-REORG-1
  sibling_of: [DEVICE-ENTITY-REORG-1, MENU-ZONE-PICKER-1]
  refinement:
  - 'Audit (agent ae6ad690f4, read-only) → URA is ALREADY async_show_menu everywhere EXCEPT two instance-pickers.
    The "Zones use a dropdown" = manage_zones (config_flow.py:7900) routes to a SelectSelector LIST *form* to pick
    a zone; CM lists domains directly in-menu. ai_rule_list (:11246) is a second form-picker (DROPDOWN). Icon
    coverage patchy: ~5 un-iconned labels + ✓/✅ drift. → SPLIT: fold icons here, card zone-picker separately.'
  - 'Icon-normalization DONE + committed in the reorg branch (feature/device-entity-defrag @ 5f39dab96): 11 label
    edits across strings.json + translations/en.json (setup_zone/add_zone/signal_responses iconned; config-flow
    init_chain_*/init_ai_* twins iconned to match options-flow; finish ✓→✅). Pure translation strings, no code
    path, no test impact — ships with the reorg. This card now tracks only the icon fold-in (→ done on reorg ship).'
  next: 'Icon fold-in committed on the reorg branch; closes when the reorg ships. Zone-picker→menu conversion is
    tracked separately as MENU-ZONE-PICKER-1 (Tier-2 fast-follow).'
  refs: ['config_flow.py (flow choosers)', 'docs/planning/PLANNING_device_entity_architecture_2026_9.md',
    'docs/planning/DECISION_LOG_device_entity_cycle_2026_09_03.md (adjudication #19)']
- id: DEVICE-SHELL-CLEANUP-1
  title: v5.94.0 left 3 empty duplicate coordinator device records on the parent entry + a same-identifier
    nesting mis-wire — remove shells, fix the D-NEST sweep resolution
  thread: platform
  status: done
  shipped_version: v5.94.3
  created: 2026-09-03 21:20
  updated: 2026-09-03 21:20
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("1" — chose code fix-forward v5.94.1 over manual surgery)'
  tags: [tier-2db, device-registry, no-fabrication-verify]
  problem_solution:
  - 'Problem: after the v5.94.0 de-frag moved all coordinator entities onto the Coordinator-Manager config
     entry, three now-empty duplicate device records (Coordinator Manager, Security, Music Following) are left
     dangling on the parent "Universal Room Automation" entry — HA never auto-removes a device whose last entity
     moved to a different config entry. Separately, the device-nesting sweep gets confused when two devices share
     the same identifier and can nest the real coordinators under the empty shell. Solution: in the CM setup,
     remove the empty shells (guarded: 0 entities + owned only by the parent entry), and fix the sweep to always
     nest under the real/populated device so the tree is correct and cannot recur. After the fix the house device
     sits alone in its own config entry as the tree root.'
  origin: {date: '2026-09-03', gist: v5.94.0 live validation — de-frag worked at entity level, 3 empty shells + via_device mis-wire remain}
  why: 'Verified via live registry + a deep HA-source code read: parent entry no longer forwards coordinator
    platforms (so shell removal is durable), and the sweep''s last-writer-wins on duplicate identifiers is the
    root of the mis-nesting. Nothing is functionally broken; this is device-tree cleanup.'
  constraints:
  - 'Remove ONLY devices with 0 entities AND config_entries=={parent_entry_id} (sole owner) AND a coordinator
    identifier — iterate devices.values(), never async_get_device (returns the shared index slot = maybe the real device).'
  - 'Order: remove shells BEFORE the D-NEST stamp; the stamp re-indexes survivors (heals the transient un-index
    from the shared-identifier __delitem__).'
  - 'Whole House device (80 entities, identifier integration) must never be a removal candidate.'
  knobs: []
  parsimony: {problem: '3 empty duplicate device cards + real coordinators nested under an empty shell', verdict: BUILD}
  next: 'DONE + validated live 2026-09-04 (v5.94.3): 56 URA devices, 3 empty shells removed, one device per coordinator, tree correctly nested, real entities intact, clean boot. Root cause was a 2-tuple identifier-unpack crashing on bond/homekit 3-tuples (v5.94.1/.2 no-op''d silently); fixed in v5.94.3 with defensive indexing + 3-tuple regression test.'
  refs: ['__init__.py CM branch ~:4194-4267', '_devices.py async_stamp_via_device_tree ~:162-185',
    'docs/planning/DECISION_LOG_device_entity_cycle_2026_09_03.md', 'docs/readmes/README_v5.94.0.md']
  spawned_from: DEVICE-ENTITY-REORG-1
  sibling_of: [DEVICE-ENTITY-REORG-1]
- id: MENU-ZONE-PICKER-1
  title: Zone instance-picker is a SelectSelector form, not a menu — convert manage_zones (and optionally ai_rule_list)
    to async_show_menu for chooser consistency
  thread: platform
  status: planned
  created: 2026-09-03 18:05
  updated: 2026-09-03 18:05
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-09-03 ("We should use menus. I think standard.") — scoped out of the reorg by orchestrator adjudication #19 as a Tier-2 fast-follow'
  tags: [ux-consistency, tier-2, no-fabrication-verify]
  problem_solution:
  - 'Problem: to pick WHICH zone to configure, the options flow shows a dropdown/list form (a SelectSelector),
     while picking a coordinator is a plain menu — so the two "pick one of several" choosers do not match, and the
     operator flagged the zone dropdown as the odd one out. Solution: convert the zone picker (manage_zones) to a
     menu (async_show_menu) with one menu option per zone, matching how coordinators are picked. Optionally do the
     same for the "view/delete AI rule" picker so all instance-pickers are menus.'
  origin: {date: '2026-09-03', gist: menu-audit finding — the only two non-menu choosers are instance-pickers}
  why: 'Menus are the URA standard; the zone/rule instance-pickers are the last forms. Split out of the Tier-3
    device-tree reorg deliberately: it threads flow-logic contracts unrelated to the device tree, so folding it in
    would widen a device-cycle blast radius for no reason.'
  constraints:
  - 'Must preserve the v4.7.5 raw-vs-canonical zone contract — the guarding AST test test_v475_d2_picker_does_not_call_iter_canonical
    must still pass (or be updated deliberately).'
  - 'Must preserve _selected_zone_name state (set at config_flow.py:7857) before routing to zone_config_menu, and
    the shared-thermostat suffix/banner logic (_render_shared_thermostat_banner :7915).'
  - 'Dynamic zone names are arbitrary user strings → menu_option keys need sanitizing/mapping; async_show_menu
    dynamic-label friction (no translation key per zone) must be handled via description_placeholders or label map.'
  parked_alts: []
  knobs: []
  parsimony: {problem: 'zone/rule instance-pickers are forms while every other chooser is a menu — inconsistent UX the operator called out', verdict: BUILD}
  next: 'Tier-2 cycle: convert manage_zones (config_flow.py:7900-7913) to async_show_menu with dynamically-built
    menu_options; update the v4.7.5 guard test; decide whether to also convert ai_rule_list (:11246).'
  refs: ['config_flow.py:7900 (manage_zones)', 'config_flow.py:11246 (ai_rule_list)',
    'quality/tests/... test_v475_d2_picker_does_not_call_iter_canonical',
    'docs/planning/DECISION_LOG_device_entity_cycle_2026_09_03.md (adjudication #19)']
  sibling_of: [MENU-CONSISTENCY-1, CONFIG-SUBENTRIES-MIGRATION-1]
- id: DEVICE-ENTITY-REORG-1
  title: Device/entity de-fragmentation + nesting reorg (HA 2026.9) — the hub cycle that spawned the scale /
    helper-consolidation / per-item-reload follow-ups
  thread: platform
  status: shipped_organic
  shipped_version: v5.94.0
  created: 2026-09-03 16:50
  updated: 2026-09-03 16:50
  refinement_status: refined
  approval: explicit
  approved_by: 'operator 2026-09-03 (drive to ship gate; Tier 3; operator holds ship decision)'
  tags: [tier-3, ha-2026.9-compat, no-fabrication-verify]
  problem_solution:
  - 'Problem: HA 2026.9 forced stripping all via_device nesting (v5.92.3), leaving the device tree flat; the
     live registry then revealed coordinator devices SPLIT across the parent + CM config entries (orphan-on-delete)
     + a dead Music-Following device. Solution: de-fragment coordinator entities to single CM ownership, delete
     the dead device, restore nesting via via_device_id, fix the model race, collapse the 2 divergence-risky
     DeviceInfo dups. Tier 3 (A/B build-reviews + validator CLEAN done; C/D + operator checkpoint pending).'
  origin: {date: '2026-09-03', gist: 2026.9 via_device strip + operator dashboard review surfaced the split-ownership defect}
  why: 'D0 probe: 17-entity migration set, all unique_id-SAFE. Validator CLEAN (0 new failures, 5 de-frag gates
    RED-on-neuter). See DECISION_LOG_device_entity_cycle_2026_09_03.md for every adjudication.'
  spawns: [SCALE-LEAN-ROOM-PROFILE-1, DEVICE-INFO-HELPER-CONSOLIDATION-1]
  sibling_of: [HA-2026-9-VIA-DEVICE-COMPAT-1, CONFIG-SUBENTRIES-MIGRATION-1]
  next: 'Complete Tier-3: run C (test-authority) + D (adversarial-completeness) reviews once the operator
    adjudication set is closed; state the falsifiable invariant; then the operator ship checkpoint → deploy →
    mondo review + live validation. Plan: PLANNING_device_entity_architecture_2026_9.md (Rev-2).'
  refs: ['docs/planning/PLANNING_device_entity_architecture_2026_9.md', 'docs/planning/DECISION_LOG_device_entity_cycle_2026_09_03.md', 'docs/planning/AUDIT_device_entity_split_ownership_2026_09_03.md']
- id: DEVICE-INFO-HELPER-CONSOLIDATION-1
  title: Consolidate the ~100 inline DeviceInfo() literals to one _*_device_info() helper per identity (the
    reorg collapsed only the 2 divergence-risky ones)
  thread: platform
  status: parked
  created: 2026-09-03 16:50
  updated: 2026-09-03 16:50
  refinement_status: initial
  approval: unreviewed
  tags: [tech-debt, ha-hygiene]
  problem_solution:
  - 'Problem: coordinator device identity is authored inline at ~100 DeviceInfo() sites (hvac 38, CM 23, energy
     22, presence 21, notification 11, …), so a future rename risks divergent device records. Solution: route
     every entity''s device through the single _devices.py helper per identity. DEVICE-ENTITY-REORG-1 collapsed
     only music_following + notification_manager (the two with a real divergence-incident history, v5.10.0); the
     rest was scoped OUT to keep that cycle Tier-3-sized.'
  origin: {date: '2026-09-03', gist: scoped OUT of DEVICE-ENTITY-REORG-1 (10x the estimate, not the split cause)}
  why: 'Not the cause of the split defect; a mechanical ~100-site refactor. Fold into the next platform cycle
    that already edits the target files (opportunistic, like ENTITYDESC-RUNTIMEDATA-HYGIENE-1).'
  spawned_from: DEVICE-ENTITY-REORG-1
  sibling_of: [ENTITYDESC-RUNTIMEDATA-HYGIENE-1, SCALE-LEAN-ROOM-PROFILE-1]
  parked_reason: 'Opportunistic — attach to the next coordinator/platform cycle that already edits these files.'
  next: 'No action now (parked). Also fold in the CoordinatorEnabledSwitch (switch.py:236) latent second-author
    + the OccupantCountSensor double-definition (sensor.py:1807/:2917) shadowing bug when this runs.'
  refs: ['docs/planning/PLANNING_device_entity_architecture_2026_9.md D2 OUT section']
- id: ROOM-AUTOMATION-MODE-SELECT-UNAVAILABLE-1
  title: All 38 per-room automation_mode selects read UNAVAILABLE house-wide (pre-existing >=1 day, not the v5.92.0 deploy)
  thread: presence
  status: shipped_organic
  shipped_version: v5.92.1
  resolution_2026_09_01: 'NOT A DEFECT — expected. AutomationModeSelect was deliberately DELETED 2026-07-26 (select.py:102-110): an inert knob with NO consumer; the real per-room enable control is switch.<room>_automation. Per Bug Class #46 (never delete registry entries from code) the integration intentionally does NOT auto-clean them, so the 38 select.<room>_automation_mode rows persist as unavailable/restored. FIX = optional one-time registry cleanup (remove the 38 orphaned rows) — offered to operator; no code. Closed.'
  created: 2026-09-01 00:40
  updated: 2026-09-01 01:30
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: every room''s Automation Mode control (select.<room>_automation_mode) reads unavailable across
     all 38 rooms, while sibling entities in the same rooms work. It is a core per-room control gone dead
     house-wide. Solution: find why the select platform / the automation_mode entity''s available property (or
     restore path) fails to come up, and fix so the control is usable again.'
  origin: {date: '2026-09-01', gist: URA-created output-entity unavailable/unknown audit — Group 1a}
  why: 'CONFIRMED NOT a v5.92.0 regression: select.kitchen_automation_mode has been unavailable since
    2026-08-30 14:46 (>1 day before the 08-31 20:25 deploy restart) and did not recover across it. Strongest
    finding of the URA-output audit; sibling sensors in the same rooms are available, so it is select-platform
    /availability-specific, not an unloaded entry.'
  next: 'Investigate select platform setup + the automation_mode entity available/restore path; determine why
    all 38 are unavailable since 08-30 14:46. Was anything changed/deployed around then?'
  refs: ['URA-output unavailable/unknown audit 2026-09-01', 'select.<room>_automation_mode x38']
- id: ROOM-ENTITY-STALE-CONFIG-1
  title: 4 URA room configs reference entities that no longer exist in HA (404) — repoint 3, remove 1
  thread: presence
  status: shipped_organic
  shipped_version: v5.92.1
  verified_and_locked_2026_09_01: 'Read-only verify done. kitchen_2 + up_guest_room_2 = 404 dead (repoint
    room_media_player -> kitchen_3 / up_guest_room_3, both live idle). MBR fan rf304_25 is dead AND in TWO
    fields (data.fans + options.manual_switches) -> repoint BOTH to fan.ceilingfan_fanimaton_rf_masterbedroom.
    Jaya dead contact is in Jaya door_sensor AND the CM security_entry_sensors (audit missed the security ref).
    OPERATOR DECISION 2026-09-01: REMOVE Jaya door_sensor entirely; REPOINT the CM security_entry_sensors dead
    contact -> ..._contact_2 (live). Mechanism: .storage config edits take effect on RESTART (reload uses
    in-memory) -> apply all edits as a controlled step in the NIGHT-LIGHT deploy-restart (batched, clobber-free).'
  batch: 'With NIGHT-LIGHT-NO-OFF-PATH-1 (operator 2026-08-31) — same room-config/light surface.'
  created: 2026-08-31 19:15
  updated: 2026-09-01 00:40
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: four rooms point at entity IDs that HA no longer has (renamed/retired), so URA silently
     references dead handles. These are the only true URA-side items in the room-entity audit (everything
     else is offline hardware). Solution: repoint 3 to the live successors and remove 1 that has no distinct
     successor — a config change via options-flow, no code.'
  origin: {date: '2026-08-31', gist: room device unknown/unavailable audit — STALE section}
  why: 'Live audit (ssh ha) found 4 config refs returning 404, each with a clean target: Kitchen
    room_media_player media_player.kitchen_2 -> kitchen_3; Upstairs Guestroom up_guest_room_2 ->
    up_guest_room_3; Master Bedroom manual_switches fan.ceilingfan_fanimaton_rf304_25_masterbedroom ->
    fan.ceilingfan_fanimaton_rf_masterbedroom; Jaya Bedroom door_sensor
    binary_sensor.openclose_aquara_zigbee_jayabedroom_contact -> REMOVE (only _contact_2 exists and it is
    already the Jaya Bedroom window_sensor, so no distinct door contact).'
  next: 'Operator applies the 4 config edits via options-flow (or approves the orchestrator doing them via
    ha_config). Low-risk config, no code, no review tier.'
  refs: ['room device audit 2026-08-31 (STALE CONFIG section)']
- id: WATERLEAK-TRIO-UNKNOWN-1
  title: Three water-leak sensors (laundry, upstairs-guest bath, Ziri bath) all went unknown together at 08-30 16:46 — one event, safety
  thread: presence
  status: investigating
  created: 2026-08-31 19:15
  updated: 2026-08-31 19:15
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: three Zigbee water-leak sensors flipped to unknown at the SAME timestamp (08-30 16:46) — a
     safety input class. A simultaneous multi-sensor drop is one integration/network/battery event, not
     three faults. Solution: investigate the common cause (Zigbee coordinator hiccup / integration reload /
     coincident battery) and restore; decide if a trip-wire on water-leak availability is warranted.'
  origin: {date: '2026-08-31', gist: room device audit — water-leak trio}
  why: 'Safety sensors going blind silently is high-consequence-low-probability. The synchronized 16:46
    drop points at a shared cause. sensor.<room>_unavailable_entities does track these (inputs), but no
    alert fires on water-leak-specifically going unavailable.'
  next: 'Investigate the 08-30 16:46 common cause (Zigbee/integration). Restore. Consider a water-leak
    availability trip-wire -> NM (per No-Soak).'
  refs: ['room device audit 2026-08-31 (LOW/safety section)']
- id: EGRESS-IDENTITY-JOIN-GAP-1
  consumer_link_note_2026_09_03: 'RE-POINTED 2026-09-03: the 8 consumers no longer depend on the PARKED Protect
    bridge (D1-PROTECT-FACE-BRIDGE-ADDON-1). Real gate = FRIGATE-SUBLABEL-FACE-BRIDGE-1 (Frigate 0.17 sub_label
    -> URA-joinable entity) feeding THIS producer. Live verify 2026-09-03: producer built+wired+enabled but
    attach rate 1/7253 all-time (~0) because all 23 sensor.*_last_recognized_face_2 are unavailable + frigate_status_2
    disconnected. All thresholds PROVISIONAL; re-measure attach after the Frigate bridge before building any consumer.'
  PROTECT_API_2026_08_28: 'Operator: check the Protect API reference. FOUND (via unifi-protect MCP): the
    Protect API DOES expose named face recognition — protect_list_smart_detections / protect_list_events /
    protect_get_event return recognized_person_id + recognized_person_name + recognized_person_confidence
    when the face is an enrolled Known Face. Proven live: Oji named 82% at 21:32. So the empty Alarm-Manager
    webhook is the WRONG path; the API is the right one (reachable now, no HA-integration limit). BUT
    enrollment is the bottleneck (protect_list_known_faces): only 4 named (Oji 46-det + Ziri 40-det strong;
    Ade/Shola 1-2 det marginal), JAYA + EZINNE NOT enrolled, and ~90+ unnamed auto-clusters (face_103..265).
    The operator 07:27 crossing matched face_265 (UNNAMED cluster, 90% conf) not the Oji Known Face -> no
    name. COMPLEMENTARY COVERAGE: Frigate names Jaya/Ezinne/Oji; Protect names Oji/Ziri + is stronger at
    door angles. DESIGN IMPLICATION: fuse Protect-API + Frigate (keyed camera+time, higher-conf named hit)
    — beats either alone. OPERATOR-SIDE unlock: enroll Jaya+Ezinne, merge recurring unnamed clusters.'
  ROOT_CAUSE_2026_08_27: 'FOUND (subagent, cited to source). NOT a wiring defect — the person_id producer
    exists and is passed to the writer, but it drops to None at a mis-keyed join. Chain: writer
    database.py:3903 log_entry_exit_event (writes whatever it is handed) <- caller transit_validator.py:1336
    (passes person_id) <- PRODUCER transit_validator.py:1279 _resolve_egress_face_identity (def :1120, the
    drop site). That resolver requires a Frigate recognized-face on the SAME camera stem as the crossing
    camera, fresh within FACE_MATCH_WINDOW_S=60s (const.py:2162). But crossings fire on EXTERIOR cams
    (doorbell_lite / front_door_aerial / madrone_g6_entry) — overhead/doorbell/back-of-head cams that
    produce ~3 named faces/week each — so the same-stem 60s join virtually never has a face -> None ->
    NULL on all 7021 rows. Historical layer: the resolver is recent (EXTERIOR-GUEST-FACE-FASTFOLLOW-1 D1,
    2026-08-18); pre-08-18 rows had no producer at all. Why interior person_visits is 100% named: it uses
    a person slug backed by BLE/GPS/WiFi device trackers (phones, always identified), never a camera face.
    Quantified ceiling ~2% (4 named / 229 crossing episodes / 7.6d); post-08-18: 112 crossings, 0 faces
    in the 60s window.'
  PROBE_RESULT_2026_08_27_BUILD: 'Signed-lag measure-first probe DONE (340 crossings/7d, 133 exit/207
    entry; 125 interior named-face events). VERDICT: BUILD — interior-fusion lifts the identity attach
    rate from ~0 to ~63-66% (48h re-tuned regime). Signed-lag hypothesis confirmed: EXITS face-leads
    (median +53s), ENTRIES face-trails (median -14s) -> ASYMMETRIC direction-keyed window, not symmetric.
    Recommended window: exit [-30s,+180s], entry [-300s,+60s] -> 63% yield at ~28% ambiguity (vs symmetric
    +/-300 = 48% ambiguity for the same yield; +/-600 buys ~20pts but ~68% ambiguity -> do NOT). AMBIGUITY
    is the real cap: ~28% of attaches have >=2 distinct resident names in-window -> a tie-break/ABSTAIN is
    LOAD-BEARING (nearest-in-time + confidence margin, else emit NO identity — never guess). TRUST: yield
    tracks face-rec health (7d floor ~27-31%, 48h ceiling ~63%) -> person_id is ADVISORY not authoritative;
    consumers must degrade gracefully when sparse. Timezone: person_entry_exit_events.timestamp is
    naive-UTC (confirmed), recorder ts is UTC epoch.'
  PROTECT_FACE_SOURCE_2026_08_28: 'Operator: why lean on Frigate alone — Protect face rec is wired via a
    webhook. VERIFIED: (1) the named faces URA consumes (sensor.<cam>_last_recognized_face_2) are FRIGATE
    (entity-registry platform=frigate), matching names confirmed (family_room: Jaya/Oji/Ezinne). (2) HA
    unifiprotect integration is LIMITED — exposes face DETECTION (event_type=face on g6) but NOT the name.
    (3) The operator DID wire UniFi Protect Alarm Manager -> HA webhook ura_kp_face_probe (automation URA
    KP Face Webhook Probe, KP-ANNOTATION-1). It IS firing (06:47:37 in lockstep w/ the g6 Protect face
    event) BUT the captured payloads are EMPTY {} — every ura_kp_face_probe_received event has shared_data
    {}. So the pipe triggers but the face name/confidence is NOT in the parsed POST body (likely Alarm
    Manager action not POSTing a JSON body, or form/query-encoded that trigger.json misses). => Protect
    face NAMES are NOT usable yet. FINISH-THE-PROBE task (not a URA build): extend the probe to log raw
    body+headers+trigger.query, fix the Alarm Manager action to POST {name,confidence,camera,ts} JSON,
    confirm names match, THEN fuse Protect as a 2nd egress-identity source alongside Frigate (Protect is
    the better DETECTOR at door angles where Frigate is weak). Until then the egress join is Frigate-only.'
  ARCHITECTURE_2026_08_28_EXTEND_NOT_REBUILD: 'Operator: reuse fusion prior art, do not build a new fusion
    layer. VERIFIED in code — the substrate already exists: camera_resolver.py DetectionLeg (:165) already
    correlates a physical camera Frigate + Protect legs by engine tag; FACE_SUFFIXES (:247-251) already
    includes the Protect suffixes (_smart_detect_face/_face_recognized) alongside Frigate _last_recognized_face
    — the face-capability scan is already multi-platform; the census already does the identity UNION
    (union-cardinality-not-sum, divergence-downgrade). The ONLY Frigate-only spot is the face-NAME reader
    _resolve_face_entity_id (camera_census.py:2615). EXTEND, do not rebuild: (1) a normalized Protect
    face-NAME HA entity via a bridge (webhook-fixed OR poll — URA stays vendor-agnostic, does NOT call the
    Protect API at runtime; coupling lives in the bridge, transport swappable); (2) extend
    _resolve_face_entity_id to read ACROSS the camera legs (Frigate+Protect), highest-confidence named hit,
    reusing DetectionLeg/engine + the census union; (3) extend (not rewrite) _resolve_egress_face_identity
    for the egress geometry (interior-adjacent stems + asymmetric signed-lag window). Net: 1 new HA entity
    (bridge) + extend 2 existing readers + reuse census union. Nuance: Protect _smart_detect_face carries
    DETECTION not NAME today, so the extension still needs the bridge to publish a named Protect entity.'
  BUILD_SCOPE: '(1) Re-key _resolve_egress_face_identity (transit_validator.py:1120) from same-stem to
    INTERIOR-adjacent recognizers (_get_interior_cameras_near :1348, or the interior set). (2) Replace the
    [0,60s] window with the asymmetric direction-keyed window (exit [-30,+180], entry [-300,+60]). (3) ADD
    the tie-break/abstain on >=2 distinct in-window names — LOAD-BEARING (~28%). (4) Keep the existing
    not_home veto + kill-switch (switch.ura_name_people_at_doors). (5) Mark person_id advisory; downstream
    6.0.0 consumers (guest gate, arrival/departure) degrade gracefully on sparse. Tier 2 (identity-affecting
    read-side resolver, additive, kill-switched) -> framing-disjoint reviews.'
  FACE_REC_24H_2026_08_27: 'Post-tuning 24h face-rec (operator re-tuned F2): 71 named events across 11
    cameras. family_room 29 (the strongest interior recognizer — the source the fix fuses from; up from
    ~68/week to ~29/day), master_hallway 20, staircase 7; EGRESS: garage_a 5, doorbell_lite 3, garage_b 1.
    So the tuning strengthened exactly the interior source the interior-fusion join depends on. Signed-lag
    measure-first probe DISPATCHED to size the achievable join rate + window before any build.'
  FIX_PATH_MEASURE_FIRST: 'The join is keyed WRONG for the geometry: it looks for a face on the DOOR cam
    (which rarely recognizes) when it should fuse an INTERIOR face near the crossing (family-room/hallway
    cams: 68 / 30 named/week). Adjacency helper _get_interior_cameras_near exists (:1348, currently returns
    ALL interior). BUT the freshness window must be re-derived from a SIGNED face-vs-crossing lag (a person
    is recognized in the family room BEFORE a garage-EXIT and AFTER a garage-ENTRY; the current age<0->stale
    rule at :1194 rejects the entry case). GATE: (1) measure-first probe of the signed lag before touching
    the resolver; (2) marginal-benefit decomposition — even fused, the ceiling is bounded by interior
    coverage + the window, so the value is modest. This is the 6.0.0 producer gate.'
  title: Face recognition works but egress crossings carry NO identity — person_entry_exit_events.person_id
    is 0 of 7010 rows all-time even post-Frigate-2-reconfig; the recognition->egress-event JOIN is unwired
  thread: security
  status: shipped_organic
  shipped_version: v5.91.4
  created: '2026-08-26 20:45'
  updated: '2026-08-29 13:20'
  refinement_status: refined
  approval: unreviewed
  tags: [no-fabrication-verify, producer-consumer, gate-6.0.0, tier-2db]
  CONSUMER_AUDIT_2026_08_28: 'post-ship consumer-gap audit 2026-08-28 done — see IDENTITY_FUSION_CAMERAS_MANUAL
    consumer map (§5.5); consumers coverage-gated on measured post-deploy yield; sequence = perimeter-name +
    arrival-notify first.'
  DEDUPE_2026_08_28: 'Folded in EGRESS-IDENTITY-PRODUCER-EMITS-NOTHING-1 (was status=investigating) — same defect (person_id never stamped on egress crossings). Findings inherited verbatim: (a) 0 of 6,883 rows all-time; (b) 08-23 case-sensitive _JUNK probe bug over-counted "Unknown" transitions as recognitions, corrected series shows face rec DOWN house-wide since ~08-21 (not just egress); (c) 08-23 storage fix did NOT restore face rec; (d) subsystem-not-storage signature (person detections healthy, face rec ~0); (e) join-window hypothesis UNTESTED because producer was dead; (f) two pre-registered predictions NEITHER fired. Corollary: reason in memory reference_egress_face_coverage_7pct_not_a_ceiling is WRONG — not a geometry ceiling, a live fault. This card now inherits both the geometry re-key + asymmetric-signed-lag window PLUS the pre-existing face-rec-recovery watch.'
  links:
    blocks:
    - PERIMETER-ALERT-NAME-PERSON-1
    - GUEST-GATE-DOOR-IDENTITY-1
    - ARRIVAL-DEPARTURE-NOTIFY-1
    - SECURITY-CENSUS-UNKNOWN-WIRE-1
    - EXTERIOR-GUEST-EGRESS-1
    - KP-ANNOTATION-1
    - EGRESS-INTERIOR-COUNT-REINFORCE-1
    - UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1
    context: 'This card is the 6.0.0 producer gate — the consumer cards read person_id which is currently 0/7010 all-time. Folded in EGRESS-IDENTITY-PRODUCER-EMITS-NOTHING-1 (see DEDUPE_2026_08_28).'
  problem_solution:
  - 'Problem: the house recognizes faces fine (interior person_visits 91616/91616 named; main-entry
    madrone_g6_entry now produces named faces post-reconfig, 0->3 in 6h) — but when a person crosses a
    door/garage, the egress event is logged with NO person_id (0 of 7010 rows all-time, 0 of 62 in the
    last 6h). Identity is recognized but never ATTACHED to the crossing. Solution: wire the recognized
    identity onto the egress-event writer (a producer/join fix), so a door crossing carries who crossed.
    This is the 6.0.0 identity-driven-autonomy gate — it is a CODE gap, NOT the hardware (the reconfig
    did not change it).'
  why: 'Measured twice this session (pre- and post-Frigate-2 reconfig) — egress person_id stays 0/7010.
    The operator hardware reconfig improved recognition (esp. main entry) but the join is unwired. This
    is THE blocker for 6.0.0 (egress keyed to person_id).'
  next: 'INVESTIGATE the producer: read the person_entry_exit_events writer (grep the egress-event insert),
    determine whether it (a) never receives the recognized identity or (b) receives it but drops it on a
    time-window/confidence join. Then scope the join fix. Measure the real recognition rate at egress cams
    to bound the achievable join rate first (measure-before-build).'
  refs:
  - person_entry_exit_events (URA DB) — 0/7010 person_id
  - reference_egress_face_coverage_7pct_not_a_ceiling (memory)
  - docs/planning/PLANNING_egress_identity_producer.md
- id: PERSON-VISITS-WRITE-PAUSE-1
  REFUTED_BENIGN_2026_08_27: 'REFUTED — not a stall, QUIET-BENIGN. The writer/coordinator/DB are all
    healthy (jaya/oji_location updating every ~min at 12:53 UTC; occupancy_events/environmental_data/
    census_snapshots/house_state_log all writing normally; no writer exception in the log). person_visits
    stopped because every BLE-resolvable named resident DEPARTED to Away at ~07:43 CDT (Jaya + Oji phones
    flipped home->not_home ~12:48 UTC; Ezinne/Ziri BLE dead since 00:20). By design person_coordinator.py:915
    only opens a visit when new_room not in (away,unknown,home) — all-Away => no INSERT. The ~5h gap was
    also a timezone false alarm (person_visits stores naive-local CDT vs room_transitions tz-aware — same
    last event). The ~900-row drop = nightly prune. No code action. RESIDUAL (gap, not fault): interior
    identity is structurally BLE-phone-dependent — house is occupied+active now (motion everywhere) yet URA
    shows all residents Away and records zero interior identity. Same identity-coverage theme as
    EGRESS-IDENTITY-JOIN-GAP-1 (identity depends on a source not always present).'
  status_note: 'CLOSED as refuted/benign 2026-08-27; residual BLE-blindness folded into the identity-coverage theme.'
  title: person_visits writes appear to have paused ~5h while egress events keep flowing — latest
    entry_time lagged egress by ~5h at 2026-08-26 measurement; not yet diagnosed
  thread: presence
  status: done
  created: '2026-08-26 20:45'
  updated: '2026-08-26 20:45'
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: during the Frigate re-check, the newest person_visits.entry_time was ~20:59 UTC while
    egress events continued to 01:26 UTC — interior room-visit writes seem to have stopped ~5h earlier
    while other identity writes continue. Could be benign (quiet interior, everyone settled) or a stalled
    writer. Solution: verify whether person_visits is genuinely stalled or just quiet — check the writer
    path and recent activity vs occupancy.'
  why: Surfaced as a side-observation in the egress measurement; flagged not diagnosed. Interior identity
    is the healthy control for the egress gap, so a stall there would matter.
  next: 'Verify: is person_visits stalled or just quiet? Compare recent interior occupancy activity to
    person_visits write cadence; check the writer path for an error/exception since ~21:00 UTC.'
  refs:
  - person_visits (URA DB)
- id: NM-BB-CHATGUID-SELFSEND-1
  title: BlueBubbles v0.7.0 adds send-by-chat-GUID — lets NM target a chat by GUID instead of address,
    decoupling alert sends from the iMessage account so URA stops messaging the operator's own thread
  thread: notifications
  status: investigating
  created: '2026-08-26 11:00'
  updated: '2026-08-28 22:00'
  refinement_status: refined
  approval: unreviewed
  tags: [nm, no-fabrication-verify]
  problem_solution:
  - 'Problem: a message URA sends me shows up TWICE in my iMessage — once when it is sent (my Mac sending
    as me) and once when it arrives (my phone/Mac receiving as me), so every URA alert reads as a duplicate.
    Solution to VERIFY (do NOT assert): three plausible options — (a) route the send through a dedicated
    non-self chat GUID (BlueBubbles v0.7.0 chat_guid addressing) so the message is not addressed to my own
    handle; (b) route my own alerts to a non-iMessage channel (Pushover / Companion) and keep iMessage for
    others; (c) suppress the inbound echo on the HA side so the received copy does not re-fire. Which is
    the correct fix depends on what actually causes the self-echo in the BB integration — read
    ~/Code/bluebubbles-integration-guide.md and the v0.7.0 release notes before scoping.'
  refinement:
  - 'operator gave the REAL symptom (2026-08-28): "A message to myself shows up twice — once when I send
    it and once when I receive it." Card reframed on the observation, not the hypothesized chat_guid mechanism.'
  VERIFIED_CAUSE_2026_08_26: 'Confirmed the self-send mechanism from source: NM _send_imessage
    (notification_manager.py:2259) sends bluebubbles.send_message with payload {addresses: handle, message}
    where handle = the recipient CONF_NM_PERSON_IMESSAGE_HANDLE. When the operator is a recipient, handle
    is his own number; BlueBubbles runs on his Mac under his Apple ID -> sends to self -> note-to-self
    thread. chat_guid (v0.7.0) targets a specific CHAT instead of an address; per the BB integration guide
    (:62) it fixes self-send ONLY if a NON-self chat exists to target (e.g. a dedicated URA-alerts group
    chat), and group GUIDs change on participant change (cache w/ expiry, guide :221). It does NOT decouple
    from the Apple ID. OPEN QUESTION for operator: is there a non-self chat + resolvable GUID? If yes ->
    optional per-recipient CONF_NM_PERSON_IMESSAGE_CHAT_GUID, send {chat_guid} when set else {addresses}.
    If no non-self chat -> chat_guid cannot help; fix differently (suppress self-send, or route own alerts
    to a different channel).'
  ACCURACY_NOTE: 'Orchestrator over-restated the operator hypothesis as documented fact on first pass;
    corrected. v0.7.0 notes = send-by-chat-GUID + README rewrite + lodash bump. No self-send claim.'
  origin:
    date: '2026-08-26'
    gist: 'operator: BlueBubbles v0.7.0 may have a way to decouple messages from the imessage user; check release notes'
  why: A durable fix for the self-send footgun in NM iMessage delivery, if the recipient chats can be
    pinned by GUID. Verify NM's send path + how a chat_guid is obtained before scoping.
  next: 'SMALL CYCLE (operator: "should be a small cycle"). Operator is upgrading to 0.7.0. (1) First
    reproduce/confirm the self-send cause in NM send path (grep notification_manager for send_message +
    how recipients/addresses are built). (2) Determine if a stable non-self chat_guid is resolvable per
    recipient. (3) If yes, wire chat_guid; if the self-send has a different cause, fix that instead. Do
    NOT assume chat_guid is the fix until (1)+(2) confirm it.'
  refs:
  - custom_components/universal_room_automation/domain_coordinators/notification_manager.py
  - '~/Code/bluebubbles-integration-guide.md'
- id: EVSE-SOLAR-IDLE-DERESERVE-1
  title: A finished/idle solar bay reserves ~1.44-2.88 kW that starves a charging sibling — de-reserve
    long-idle bays from the solar-follow parked floor WITHOUT removing them from the claim set
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.3
  created: '2026-08-26 11:10'
  updated: '2026-08-26 11:10'
  refinement_status: refined
  approval: approved
  approved_by: 'operator 2026-08-26 ("I like the narrowing of stop conditions") — narrow fix approved,
    forward-looking (no probe gate; garage_b idle now but will come online).'
  tags: [tier-2, marginal-benefit-narrowed, forward-looking]
  problem_solution:
  - 'Problem: solar-follow reserves a 6A floor for every claimed bay that is not currently drawing. A car
    that finished (or a claimed bay with no car) keeps that ~1.44-2.88 kW reservation, which is subtracted
    from the surplus given to a sibling bay that IS charging — so on a sunny afternoon a live car gets
    throttled by a dead one. Solution: once a bay has drawn ~0 for ~10 min, stop counting it in the parked
    floor (and stop re-writing it), so the charging sibling gets the full surplus. The bay STAYS claimed
    (no discard) so nothing else changes and the re-claim oscillator never arises.'
  why: The marginal-benefit test isolated this as the ONLY real dollar value of the parked Tier-3
    stop-conditions plan. The expensive discard-and-move machinery existed only to solve the oscillator
    that discarding creates; not discarding sidesteps it entirely. See MARGINAL_BENEFIT note on
    EVSE-SOLAR-STOP-CONDITIONS-1.
  knobs:
  - SOLAR_FOLLOW_IDLE_DERESERVE_TICKS (module const, default 10 ~= 10min; kill-switch = huge value reverts
    to today) — de-reserve only after a bay is idle long enough to be presumed finished, not mid-ramp.
  next: 'Build per PLANNING_evse_solar_idle_dereserve.md: _notdraw_ticks counter (mirrors _stale_ticks),
    exclude long_idle from parked_w, skip their write churn, knob + discriminating tests + claim-leg
    byte-identity. Tier 2, 2 framing-disjoint reviews + orchestrator mutation-verify.'
  refs:
  - docs/planning/PLANNING_evse_solar_idle_dereserve.md
  - energy_pool.py:4199 (parked_w allocator)
  - sibling_of EVSE-SOLAR-STOP-CONDITIONS-1
- id: EVSE-SOLAR-STOP-CONDITIONS-1
  title: Solar sessions cannot tell "the car is done" from "the sun is still out" — a finished or unplugged
    car holds its claim until the fleet conditions end
  thread: energy
  status: parked
  created: 2026-08-24 22:30
  updated: 2026-08-26 02:15
  refinement_status: refined
  approval: implied
  approved_by: 'operator 2026-08-26 (AskUserQuestion: oscillator resolution = "suppressed-membership")
    — design gate resolved; mechanism (b) chosen. Build approval still pending the written+reviewed plan.'
  tags:
  - tier-3
  - split-from-cycle
  - oscillator-hazard
  problem_solution:
  - 'Problem: an excess-solar charging session ends only when the whole-house conditions end — the battery
    drops below its threshold or the day''s remaining solar forecast runs out. If the car finishes charging
    at 2pm on a sunny day, or someone unplugs it and drives off, the session keeps running for hours:
    the charger stays claimed, and nothing notices there is nothing to charge. Solution: give a session
    per-car reasons to end — the car is unplugged, or the car has stopped drawing for long enough that
    it is finished — and record WHY each session ended so the behaviour can be tuned from evidence instead
    of guesswork.'
  origin:
    date: '2026-08-24'
    gist: split out of EVSE-SOLAR-FOLLOW-AMPS-1 after three framing-disjoint plan reviews put every design-level
      critical in this half and none in amp modulation
  why: Amp modulation (EVSE-SOLAR-FOLLOW-AMPS-1) is shippable on its own and is where the value is. The
    start/stop rework is where all the risk turned out to be. Keeping them together meant a ready deliverable
    waited behind an unready one.
  FOUNDING_DESIGN_PROBLEM_THE_OSCILLATOR: 'Any per-EVSE stop must fire while the fleet conditions are
    still GOOD — that is the entire point (a finished car on a sunny afternoon). But determine_excess_solar_actions
    claims every EVSE not already in _excess_solar_active, and the claim leg runs BELOW any point where
    such a stop could be evaluated. So: stop at tick N, re-claim at tick N+1, every timer re-arms, repeat
    until dusk. Each cycle also re-runs the amp capture and burns write budget. This is not a detail to
    be discovered during the build — it is the problem the cycle has to solve first. Candidate resolutions,
    none costless: (a) a re-claim cooldown keyed off a last-stop timestamp, consulted IN the claim leg
    — which breaks the byte-identical claim-leg guarantee several other things rely on; (b) keep membership
    and mark the bay suppressed rather than removing it — but then the switch is off while membership
    says active, which is incoherent for every other consumer of that set; (c) decide per-EVSE stops are
    not worth it — an idle or unplugged bay draws nothing, so the only real costs are a held claim and
    a periodic 6 A safe-park write. (c) deserves an honest hearing before (a) or (b) are built.'
  REVIEW_FINDINGS_INHERITED: 'Carried forward from the three Rev-20/21 plan reviews so they are not re-derived.
    (1) INV-STOP-2 ("a peer hold is never a stop") is FALSE against untouched pre-existing code: the peak-clear
    path (energy_pool.py:1357-1374) and the blind-window DROP leg (:1532-1564) both remove EVSEs without
    consulting _stronger_peer_holds or _paused_by_dp. Any invariant of that shape must be scoped to new
    stop paths only, or the cycle fails its own review. (2) There are FIVE sites that discard from _excess_solar_active,
    not four — the fifth is the _prune_removed_evses set-pass (:787-791), reachable because excess_solar
    carries prune_participant=True (energy_pool_owners.py:245-252). (3) There are FOUR add sites, not
    three — the fourth is the DB restore at energy.py:1366, which writes no start reason and stamps no
    session age. (4) A stop condition keyed on the EVSE status ATTRIBUTE cannot use last_changed: status
    lives on the switch entity attributes (energy_pool.py:685-688) and HA bumps last_changed only on STATE
    change, so a switch on for 6 h satisfies any threshold on the first disconnected sample. Stamped first-observation
    dicts are the only correct mechanism. (5) A missing session-age stamp must read as age 0, never as
    infinitely old, or a bay claimed via a site that forgot to stamp becomes permanently immune to every
    stop. (6) energy_pool_owners.py is BEHAVIOR-FROZEN against quality/tests/golden/owner_registry_v1.jsonl.gz
    — adding OwnerDeclaration rows for new per-EVSE dicts changes iter_all enumeration and fails the golden.'
  constraints:
  - Ships AFTER EVSE-SOLAR-FOLLOW-AMPS-1 and after the DP drain-target fix.
  - Must not regress the amp-modulation cycle's byte-identical guarantees without restating them.
  - Tier 3 — cost-and-safety impacting, touches session semantics consumed by TOU, DP and the peer hierarchy.
  parked_alts:
  - A SOC hysteresis band (start at resume_ev_at_battery_soc 95, stop at fill_priority_soc 80) using the
    two knobs number.py:1670 already documents as an asymmetric pair. Designed and reviewed but NOT built
    — it belongs with the stop work, not with modulation. Note number.py states the ordering invariant
    fill_priority_soc < excess_solar_soc is NOT enforced, so any implementation needs a defensive min()
    and a warning.
  - A cessation ledger (per-EVSE last start/stop reason and timestamp, closed vocabulary, surfaced on
    the existing ev_charging_status sensor). The operator explicitly asked for this — "whether it will
    know and record why its doing which" — so it is a requirement of this cycle, not an optional extra.
  knobs:
  - SOLAR_STOP_DISCONNECTED_S (rung 1) — unambiguous signal, act fast
  - SOLAR_STOP_IDLE_S (rung 1) — ambiguous signal (thermal throttle, cell balancing run 5-15 min), act
    slow
  - SOLAR_MIN_ON_S (rung 1) — minimum session age before any stop
  refinement:
  - 'Founding oscillator gate (stop-at-N re-claim-at-N+1) → operator chose (b) SUPPRESSED-MEMBERSHIP:
    keep the bay in _excess_solar_active but mark it suppressed so the claim leg does not re-arm it.
    The card itself flagged (b)''s cost — "switch off while membership says active is incoherent for
    every other consumer of that set" — so the PLAN''s central task is to define, per consumer of
    _excess_solar_active, what suppressed means (skip amp-capture, skip re-command, keep the claim,
    surface a stop reason) and prove no consumer mis-reads a suppressed bay as actively charging.'
  MARGINAL_BENEFIT_2026_08_26: 'Operator asked to run the marginal-benefit test + isolate IF/WHY we need
    it, context-wide on solar-follow goals. RESULT: the value is REAL but NARROW, and the PLAN SCOPE is
    disproportionate. Grounding: solar-follow is REACTIVE (energy_pool.py:4199 parked_w = (n_eligible -
    n_drawing) * SOLAR_FOLLOW_MIN_AMPS * 240 * PHASES; allocatable_w = s_eligible - parked_w). So a
    claimed-but-IDLE bay (finished/unplugged car) RESERVES ~1.44-2.88 kW (6A x 240 x PHASES) that is
    SUBTRACTED from what an actively-charging SIBLING bay gets. THAT is the only dollar harm: on a sunny
    afternoon with garage_a finished-but-claimed while garage_b charges, garage_b is throttled by the
    dead bay parked reservation (or surplus exports cheaply instead). Single-bay case: NO dollar harm
    (idle car draws 0, surplus flows to battery/grid regardless of the phantom claim — ceilings are not
    reservations of physical electrons, only of the MODELLED allocatable_w). Bookkeeping/consumer-noise
    and per-tick 6A safe-park writes are the only other costs, neither dollars/safety.
    KEY INSIGHT: the EXPENSIVE part of the plan (discard-and-move + suppression latch + oscillator
    resolution + 11-consumer ripple, Tier-3) exists ENTIRELY to solve the oscillator that only arises
    because the plan chose to DISCARD from _excess_solar_active. The actual harm lives in the ALLOCATOR
    (parked_w) + the write cadence, NOT in claim membership. A MUCH cheaper fix captures the dollar value:
    exclude a long-idle bay (drawing ~0 for N min) from parked_w AND skip its per-tick write, WITHOUT
    removing it from _excess_solar_active. No discard -> no re-claim -> NO OSCILLATOR -> no suppression,
    no consumer ripple, not Tier-3. Re-include on draw resumption (trivial, still in the set). Loses only:
    de-energizing an unplugged connector (idle Emporia draws nothing) + bookkeeping cleanup (not dollars).
    RECOMMENDATION: PARK the full discard-and-move plan; build the narrow parked-reservation fix instead,
    gated on a measure-first probe (how often are >=2 bays claimed with one finishing while another draws
    on a sunny afternoon? If rare, even the narrow fix may not clear the bar).'
  PLAN_REVIEWED_2026_08_26: 'The Tier-3 plan was ALREADY written (prior session, 665 lines,
    PLANNING_evse_solar_stop_conditions.md). Ran the 2 framing-disjoint plan reviews (completeness +
    build-prediction). BOTH = FIX-PLAN-FIRST. Record: PLAN_REVIEW_evse_solar_stop_conditions.md.
    Converged must-fixes: timeout reads stamped_monotonic never written (INV-STOP-3 unreachable);
    no power-sensor-health gate (degraded sensor turns off a charging car); SOLAR_MIN_ON_S (carded
    knob) dropped; SOC hysteresis dropped; no real test deliverable; byte-identity proof uses
    line-ranges that shift on insert. UNIQUE/structural (completeness): the plan does NOT implement
    the operator''s literal (b) keep-membership — it DISCARDS from _excess_solar_active and moves the
    bay to a separate _solar_follow_suppressed set (a refinement avoiding (b)''s consumer-incoherence),
    but never declares the deviation AND skips the card''s mandated per-consumer enumeration, which
    hides 11 behaviour-changing consumers (TOU pausability, SolarFollowController amp _restore_pass
    collision same-tick, classifier-reason loss, _proactive_offpeak_holds leak).'
  next: 'WAITING ON OPERATOR — marginal-benefit decision: build the NARROW parked-reservation fix
    (exclude long-idle bays from parked_w + skip their write, no discard, no oscillator, Tier-2) instead
    of the full Tier-3 discard-and-move? Measure-first probe recommended. (Original C1 mechanism note: the plan does discard-and-move
    (drop from _excess_solar_active + separate suppressed set), NOT your literal (b) keep-in-set.
    Discard-and-move is arguably sounder (avoids the active-set incoherence you flagged) BUT changes
    11 consumers of the active set that the plan never enumerated. Question: OK to proceed on
    discard-and-move (planner does the full consumer enumeration + all ~12 must-fixes), or hold to
    strict keep-membership? Then plan rework -> re-review -> build. Gated behind HVAC-excursion landing.'
  refs:
  - docs/planning/PLANNING_evse_solar_stop_conditions.md (the plan under review)
  - docs/planning/PLAN_REVIEW_evse_solar_stop_conditions.md (both plan reviews, FIX-PLAN-FIRST)
  - docs/planning/PLANNING_evse_solar_follow_amps.md (the amp-modulation cycle this was split from)
  - docs/planning/AUDIT_excess_solar_and_evse_prior_art.md (the coupling walkthrough the plan omitted)
  - energy_pool.py:1318-1702 (determine_excess_solar_actions)
  - energy_pool_owners.py:245-252 (excess_solar owner declaration)
- id: EVSE-SOLAR-FOLLOW-AMPS-1
  SENSOR_DELTA_MEASURED_2026_08_23: 'MEASURED, not estimated. Emporia mains vs Envoy net-consumption CT,
    7,090 aligned pairs over ~7 days of recorder (probe: scratchpad/delta_probe.py). mean +371 W (Emporia
    higher) | median +259 W | stdev 894 W p10/p90 -158 / +1,014 W      | min/max -10,825 / +16,847 W |delta|
    <=250W 41.3%  <=500W 64.8%  <=1000W 88.0% sign agreement (both export or both import) 88.0% Operator
    estimate was ~0.5 kW: right on central tendency, slightly optimistic on spread — only 65% of samples
    land within 500 W. IT IS TIMING SKEW, NOT DISAGREEMENT — operator hypothesised this and it is CONFIRMED.
    Bucketing |delta| by the slew rate of the fast signal (probe: scratchpad/skew_probe.py, 7,015 pairs):
    slew 0-50 W/min    n=3274  median |delta|   241 W slew 50-200        n=1692                   307
    W slew 200-500       n=1009                   384 W slew 500-1500      n= 818                   619
    W slew 1500-5000     n= 173                 1,453 W slew 5000+         n=  49                 2,024
    W quiet vs busy: 6.7x more divergence when load moves fast. Monotone with rate of change = a 1-minute
    average compared against a ~19s sample. Genuine sensor disagreement would be FLAT across those buckets.
    Note the MEAN holds ~320-365 W across the first four buckets while the MEDIAN climbs: a small real
    offset (different clamp boundaries) sits under a much larger timing component. Two effects; only the
    small one is real. THIS KILLED THE AGREEMENT-GATE DESIGN I WAS DRAFTING. A cross-check veto would
    have fired mostly on skew — vetoing charging precisely when solar was changing fastest, i.e. when
    following it matters most — while LOOKING like a safety feature. Operator''s either-or design is correct.'
  SIGNAL_DESIGN_FINAL_2026_08_23: 'EITHER-OR, NOT CROSS-CHECK. Emporia mains PRIMARY (operator: more accurate
    CT, and it is what Emporia''s own excess-solar control used). Envoy net-consumption CT as FALLBACK.
    Either alone is sufficient; NO agreement gate (see SENSOR_DELTA_MEASURED — a gate would fire on skew).
    The fallback must be genuine, not decorative: the local Envoy has a history of unreliability, which
    is why URA already carries reliability machinery around it. That does NOT make Emporia immune to its
    own failures — both legs need an availability check. DESIGN CONSEQUENCE OF THE SKEW: Emporia is a
    1-MINUTE AVERAGE and slower to react. The controller must step amps CONSERVATIVELY DOWNWARD and CAUTIOUSLY
    UPWARD. Reacting fast to a stale-high reading is how it overshoots into the battery — the exact harm
    this card exists to prevent.'
  TWO_AUDIT_CLAIMS_I_RELAYED_WERE_WRONG_2026_08_23: 'Both corrected by the operator; recorded so the audit
    version is not inherited. (1) "Fill-priority goes inert Oct-Feb" — FALSE. Shoulder (Mar-May, Oct-Nov)
    and winter (Dec-Feb) each define TWO periods, not zero: shoulder off_peak (0-17, 21-24) @0.043481
    and mid_peak (17-21) @0.086442; winter off_peak (0-5, 9-17, 21-24) and mid_peak (5-9, 17-21). So `tou_period
    == "peak"` is simply never true, but the other arms still evaluate. Fill-priority is ACTIVE during
    off-peak daylight — in shoulder that is 0-17h, precisely the hours this controller runs. Mid-peak
    remains 2x off-peak, so the billing distinction it tracks is fully live without a peak period existing.
    Only the LITERAL "never during peak" guard is dormant. (2) "The standing fence forbids a separate
    timer" — I MIS-READ IT. The line (PLANNING_v4.7.x_advanced_energy_management.md:219) sits in a REJECTED-ALTERNATIVES
    list beside MILP-MPC, reinforcement learning and stochastic MPC: "EV-specific optimizer. EVs are just
    controllable loads under existing EVSE switch coordination. No separate stack." It rejects a parallel
    OPTIMISATION STACK, not a timer. An amp controller inside the existing EVSE coordination is what that
    sentence endorses. I turned "no separate optimizer" into "no separate timer" and handed the operator
    a manufactured question. Withdrawn; no ruling needed.'
  DESIGN_CLOSED_2026_08_23: 'CLOSED. Amp modulation IS the deliverable. Simple code, Tier-3 review. WHY
    THE MAGNITUDE MATTERS, not the oscillation — operator corrected my recommendation and was right. I
    had proposed shipping a deadband on the existing release gate instead, on the strength of an audit
    finding that the gate lacks hysteresis. That optimised the symptom I could measure and ignored the
    harm: house 10 kW, solar 15 kW          -> 5 kW genuine surplus binary 48A adds 11.5 kW           ->
    21.5 kW draw vs 15 kW solar -> 6.5 kW OUT OF THE BATTERY matched ~21A adds 5 kW            -> 15.0
    kW draw vs 15 kW solar -> net zero A deadband stops the FLAPPING and leaves the house drawing 21 kW
    against 15 kW of solar. The battery still empties, just smoothly. That is cosmetic on the real failure.
    SERVICE-CAPACITY DIMENSION, which I missed entirely and which the operator raised: binary control
    offers only NOTHING or EVERYTHING, and "everything" is chosen with no reference to what the service
    can carry. Draw ceilings, demand overages, breaker trips. Matching solar keeps total draw bounded
    by what is actually being produced. This is a safety argument, not only an energy one. THE 6A FLOOR
    IS THE TRACKING LIMIT, NOT A LIMITATION: from 11.5 kW down to 1.44 kW the controller follows solar
    as it declines through the afternoon. Below 1.44 kW it stops — a REAL stop, because there genuinely
    is not enough surplus left, not a yo-yo. NIGHT IS OUT OF SCOPE: after sundown car charging competes
    with DP, which is DP''s job and is already built. This controller is a daytime, sun-is-out mechanism
    only. THE RELEASE-GATE HYSTERESIS GAP IS REAL BUT SEPARATE (energy_pool.py:1685-1699, no hysteresis
    and no minimum on-time, so SOC 95->94 drops every claimed EVSE; plus determine_battery_drain_actions
    has no _excess_solar_active check and runs AFTER solar in the tick). Card it separately. It is not
    a substitute for this and not a reason to defer this. TIER 3 = REVIEW COST, NOT CODE COMPLEXITY (operator).
    The build is small: read surplus -> convert to amps -> clamp [6,48] -> deadband -> write, inside an
    already-open session. The Tier-3 spend is proving it cannot perturb TOU, DP, fill-priority, or the
    service ceiling. Keeping the code small is what makes four framing-disjoint reviews affordable.'
  MEASUREMENT_DESIGN_2026_08_23: 'SIGNAL DESIGN SETTLED — Envoy primary, Emporia mains as veto. SPAN is
    EXCLUDED (operator: "Span is unreliable"). Not ranked lower; excluded. WHAT EACH MEASURES — established,
    not assumed. I got this wrong twice before the operator corrected me, so it is written down plainly:
    * Envoy `balanced_net_power_consumption` — signed, net at the ENVOY SYSTEM boundary. Sees production
    AND battery. Live at time of writing: -0.351 kW (exporting). * Emporia `mains_vue_3_power_minute_average`
    — GRID DRAW ONLY. Signed (measured range -12,947 W .. +29,233 W across 7,598 samples; negative = exporting).
    BLIND TO THE ENVOY SYSTEM — it reads the mains CTs. Live: +126 W. * SPAN `span_panel_current_power`
    — panel-level. Excluded. MY EARLIER "ORDER OF MAGNITUDE DISAGREEMENT" WAS WRONG and is retracted.
    5.843 kW / 126 W / 3,122 W are three DIFFERENT BOUNDARIES, all correct simultaneously. I compared
    quantities before establishing what they measured — the same infer-before-verify error as the SPAN-breaker
    actuation claim earlier the same day. WHY THIS PAIRING, AND WHY IT IS NOT ARBITRARY REDUNDANCY: Envoy
    is the better SIZING signal — one signed figure covering production, consumption and battery, so the
    amp target can be computed directly from it. Emporia answers the export question BETTER — not the
    only sensor that can answer it. CORRECTION (operator 2026-08-25): an Envoy entity CAN read whether
    surplus actually left the property — the Envoy net-consumption CT (grid boundary, `sensor.envoy_482543015950_current_net_power_consumption`)
    reads the meter, and it is the design FALLBACK leg. Emporia mains is PRIMARY only because it is more
    trustworthy, NOT because Envoy is structurally blind to the meter. The real limitation is signal-scoped,
    not Envoy-scoped: the ONE signal `balanced_net` includes the battery, so it can read as export while
    the battery drains — which is the yo-yo this card exists to remove. Do not inherit the earlier over-broad
    claim that Envoy "structurally cannot" see export; it can, via a different Envoy CT, and that CT is
    the backup. OPERATOR''S DECISIVE POINT: Emporia mains is what the EMPORIA EVSE''S OWN excess-solar
    control used. So it is not a generic second sensor — it is the measurement the feature being replicated
    was built on. Matching Emporia''s behaviour means matching Emporia''s boundary; sizing on a different
    boundary would give systematically different answers on any afternoon the battery is moving, which
    is most of them. RULE: size on Envoy; charge ONLY while Emporia mains confirms export. Disagreement
    between them IS the battery-masquerade signal — treat it as a stop, not as a sensor fault. Fails safe.
    NOT USABLE: sensor.mainw_vue_balance_power_minute_average has ZERO recorded samples and reads unavailable.
    Entity name is `mainw`, not `mains` — likely typo/dead. Do not design around it.'
  SCOPE_FENCE_2026_08_23: 'OPERATOR: "Hopefully solar follow never touches the drain target. It should
    be unopinionated and run when the EVSE starts during the day while the sun is out." HARD FENCE: modulate
    AMPS ONLY, inside a session the existing excess-solar trigger already opened. Never start a charge,
    never stop one on TOU grounds, never read or write the DP drain target, never coordinate with DP.
    UNAVOIDABLE ARITHMETIC COUPLING — documented, NOT managed: DP gate 6 (energy_drain_precedence.py:652):
    charger_rate_kw <= l1_rate_threshold_kw -> DP_REASON_L1_ONLY. DP_L1_RATE_THRESHOLD_KW = 3.0 (energy_const.py:1359).
    Solar-follow range at 240V: 6A = 1.44 kW .. 48A = 11.5 kW. CROSSOVER ~12.5A. Any solar-follow setting
    below ~12.5A makes DP see a sub-threshold charger and decline to drain. The controller does not touch
    the drain target; charger_rate_kw is simply a DP INPUT that throttling changes. ACCEPTED, NOT COORDINATED
    — a trickle-charging car is arguably not a drain opportunity. It is written down because gate 6 is
    the SAME gate that masked the drain-target defect on 2026-08-20. Once solar-follow runs by day it
    will fire more often, and the next person diagnosing "why did DP not drain" will hit L1_ONLY and risk
    mis-attributing it a second time.'
  OPERATOR_ANSWERS_AND_VERIFIED_FACTS_2026_08_23: 'All four questions answered; each verified against
    the live system rather than accepted, and two of my own claims corrected. Q1 CURRENT FLOOR — MEASURED,
    and it is NOT 2A. The Emporia number entity reports min=6 max=48 step=1 unit=A. At 240V that is a
    HARD FLOOR OF ~1.44 kW. There is no throttle-to-nothing option: below 1.44 kW the only choices are
    STOP or draw from the battery. This kills the "hold at minimum amps" variant outright — it would simply
    move the yo-yo to the bottom of the range. Entity also exposes `status` (Standby/Charging) and `charging_rate`,
    both useful to the controller. Both chargers currently read "Charger is not connected to a vehicle."
    Q2 BATTERY CONTRIBUTION — USE THE EXISTING MECHANISM (operator). It is FILL-PRIORITY, in energy_pool.py,
    and it is TIME-anchored by TOU phase, never inferred from instantaneous PV: peak -> release (TOU pause
    is canonical there) off_peak -> release (fixed cheap-grid window; its comment calls this "cloud-proof")
    mid_peak + peak_ahead -> HOLD  (the pre-peak fill window) mid_peak + no peak ahead -> release The
    solar-follow controller DEFERS to this rather than inventing a battery-contribution rule. NOTE: fill_priority_soc
    and ev_battery_drain_soc are BOTH live at 80 today — same number, two different mechanisms; do not
    conflate them. Q3 STOP CONDITIONS — VERIFIED. Peak already stops it: determine_excess_solar_actions
    says "Only during off-peak or mid-peak (never peak — battery needed)", so the operator prediction
    that peak may stop it before solar runs out is ALREADY TRUE IN CODE. Solcast surfaces exist and are
    the right stop signal: sensor.solcast_pv_forecast_forecast_next_hour       (1306 W at time of writing)
    sensor.solcast_pv_forecast_forecast_next_x_hours    (6606 W) sensor.solcast_pv_forecast_forecast_remaining_today
    (8.17 kWh) Stop on BOTH: multi-tick hysteresis on measured excess AND next-hour forecast below the
    floor. Q4 GARAGE A — MY CLAIM WAS WRONG, CORRECTED BY THE OPERATOR. I said URA actuates through the
    SPAN breaker and therefore could not control Garage A. Untrue. energy_pool.py:170/177 hard-codes "switch":
    "switch.garage_a" / "switch.garage_b" — the EMPORIA switches. The SPAN breaker is not in the control
    path; energy_evse_{a,b}_entity are power SENSORS (sensor.garage_{a,b}_power_minute_average) and the
    EVSE has its own measurement. So the unavailable SPAN breaker is an observability gap on another card,
    NOT a blocker here. BOTH EVSEs are actuatable. I asserted the control path from entity NAMES instead
    of reading the config — the same infer-rather-than-verify error this session has hit repeatedly.'
  EXCESS_SIGNAL_AND_SELF_VALIDATION_2026_08_23: 'What to size the draw against, and how to know the reading
    is trustworthy without depending on a second sensor that is currently dead. PRIMARY: sensor.envoy_482543015950_balanced_net_power_consumption
    — NEGATIVE means exporting. This is the spare-solar figure directly, already net of the whole house,
    with no subtraction for the controller to get wrong. REJECTED AS CROSS-CHECK: sensor.span_panel_mains_emporia_vue_power
    reads 0.0 and is not reporting. Gating on Envoy-vs-Emporia agreement would stop charging ALWAYS until
    that is fixed. OPERATOR DIRECTION — validate by battery state and arithmetic instead. Both identities
    VERIFIED live: consumption 5.843 - production 6.194 = -0.351  ==  balanced_net -0.351   (exact) available
    37,350 Wh / capacity 40,000 Wh = 93.4%  ==  battery 93%        (consistent) So the controller can
    self-check: if balanced_net diverges from (consumption - production), a sensor is stale and it should
    fail safe (stop), with no second meter required. THE BATTERY GATE IS THE REAL DISCRIMINATOR, and the
    live snapshot proves why: the house was EXPORTING 351 W while the battery sat at 93% and FALLING (97%
    at midday, discharge -0.612 kW). Export alone is NOT evidence of true surplus — the battery should
    absorb it first. A controller that sized on export alone would have started charging the car out of
    a draining house battery, which is the exact failure this card exists to remove.'
  title: 'Charge current is binary (48A or off) so solar charging yo-yos the house battery: add excess-following
    current modulation on the two L2 EVSEs'
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.0
  approval: explicit
  approved_by: operator 2026-08-23 (asked for the card + a simple plan + surgical insertion point)
  created: 2026-08-23 16:10
  updated: 2026-08-25 15:20
  refinement_status: initial
  tags:
  - measure-before-build
  - numbers-get-knobs
  - institutional-context
  problem_solution:
  - 'Problem: the operator turned OFF Emporia''s own excess-solar control on the Garage A charger (it
    was never on for Garage B) so that URA is the single controller. That removed a feature URA does not
    have: Emporia was continuously varying the charge current to match whatever solar was spare. URA can
    only switch the charger fully on or fully off. So when URA starts a solar charge it pulls the full
    48A regardless of how much sun is actually spare, the shortfall comes out of the house battery, the
    battery falls to its EV-drain floor, the charge stops, the sun refills the battery, and the whole
    thing starts again — a yo-yo that cycles the house battery for no benefit. Solution: have URA set
    the charger''s current limit to match the measured spare solar, instead of leaving it at maximum.
    Charge fast when there is a lot of sun, slowly when there is little, and stop cleanly rather than
    oscillating.'
  origin:
    date: '2026-08-23'
    gist: 'Operator: "I turned off excess solar control for the emporia garage A charger... What it does
      is (a) turn on the charger when it detects the battery is full so it can empty into the car first,
      (b) modulates the charge current from 2A to 48A based on the amount of excess it sees. By turning
      it off, only URA is in control. And I lose (b). Can we build a function that will do b under excess
      solar conditions?"'
  INSTITUTIONAL_CONTEXT_VERIFIED_2026_08_23: 'Checked before proposing anything. WHAT ALREADY EXISTS AND
    IS REUSED: * Trigger (a) is ALREADY URA''s and is BETTER than Emporia''s. `determine_excess_solar_actions`
    (energy_pool.py:1318) gates on SOC >= threshold AND remaining-forecast >= kWh threshold, and only
    off-peak/mid-peak, never peak. Live knobs: excess_solar_soc default 95, excess_solar_kwh default 5.0.
    The operator is right that a forecast beats Emporia''s instantaneous delta. NOTHING in this card touches
    (a). * A live production signal exists: `solar_production_w()` (energy_battery.py:1586). * The DP
    sticky-yield interaction is already designed and documented (PLANNING_dp_sticky_yields_to_excess_solar.md,
    INV-YIELD-1/2). Any new writer MUST respect it. WHAT DOES NOT EXIST — this is the whole gap: * URA
    has NO current/amp control of any kind. Repo-wide grep of custom_components/ for charging_amps|current_limit|set_charge_rate|max_charging|charger_current
    returns ZERO hits in source. URA drives the EVSEs only through the SPAN breaker switches (switch.span_panel_garage_a_evse_breaker
    / _b_), which are binary. THE KNOB IS ALREADY THERE, UNUSED — the write target exists as a plain HA
    number entity: number.garage_a_evse_emporia_wifi_garagea_current_limit = 48.0 number.garage_b_evse_emporia_wifi_garageb_current_limit
    = 48.0 LIVE SNAPSHOT while writing this card (shows the headroom is real): solar production 13,988
    W | house draw 9,317 W | battery 97% | ev_battery_drain_soc knob = 80 => roughly 4.7 kW spare, while
    the charger would pull ~11.5 kW at 48A/240V. That gap IS the yo-yo: about 6.8 kW would come from the
    house battery.'
  REWRITTEN_REV20_2026_08_24: 'Two framing-disjoint plan reviews returned 39 findings, 5 CRITICAL. Root
    cause was PROCESS, not design: revisions 4-19 layered "authoritative" sections onto an unedited body
    until the document argued with itself -- a live 2x arithmetic fork on the allocation denominator,
    11 pointers standing in for spec text that existed nowhere, and 16 test names plus 20 mutation drills
    defined nowhere. Rewritten as ONE coherent document rather than patched. CRITICAL findings closed:
    (B1) _get_evse_state returns a DICT and the invariant used attribute access -- guaranteed AttributeError
    on the first tick; now subscripted everywhere. (B2) D2 was sited in the release leg, which is inside
    else: and runs only when conditions_met is FALSE -- the idle stop must fire while it is TRUE, so the
    whole deliverable was dead on arrival; now a per-EVSE stop sweep sited BEFORE conditions_met, leaving
    claim and release legs byte-identical. (B3) conditions_met is ONE boolean serving both legs, so the
    SOC band could not be expressed as an edit to it without moving the start gate to 80; now a separate
    continue_ok with a defensive min() because number.py:1670 states the ordering invariant is NOT enforced.
    (B4) the claim that widening mains_export_active had "no live consumers" was FALSE -- two exist at
    energy_pool.py:633 and :1422, inert only while the field is unset and this cycle requires wiring it;
    the function is now left untouched and D1 reads its own signed value. (B10) persistence needed an
    unmentioned ALTER TABLE and offered the builder a choice; now the existing KV path, no schema change,
    no choice. Also closed: current_charging_load_w excluded (fleet total that launders the 7600 W fabrication),
    foreign-writer STOP dropped (would fire on the Emporia cloud echo and kill every session), all stop
    thresholds expressed as DURATIONS against last_changed rather than tick counts (status p90 is 1104
    s, so tick-counting counted our own polling), ledger moved to EVChargerController where its write
    points already are and extended to the peak-clear and blind-window-drop paths.'
  SPLIT_2026_08_24: 'Reduced to D1 amp modulation ONLY. Three framing-disjoint plan reviews on the combined
    Rev-20/21 returned 10 CRITICALs, and every DESIGN-level one was in the start/stop half — including
    a stop/restart oscillator (a per-EVSE stop sited above the claim leg is re-claimed on the next tick,
    forever). The test-authority reviewer, the most positive on the design, wrote that the modulation
    sections hold up and its objections were about whether the verification layer could SEE the design.
    So modulation ships alone and the start/stop work is carded as EVSE-SOLAR-STOP-CONDITIONS-1 with the
    oscillator as its founding problem. D1-side findings all applied: SOLAR_FOLLOW_PHASES named (was an
    unbound symbol in load-bearing arithmetic, 2x blast radius); the allocator now NETS the parked 6 A
    floor before dividing (without it the plan''s own T-ALLOC-1 falsified INV-SF-4 — 8400 W commanded
    against 7000 W of surplus); INV-SF-9 added so D1 honours _observation_mode and _excess_solar_enabled
    (previously the master switch did not disable the feature and observation mode did not contain it,
    Bug Class #23); a boot reconciliation un-throttles a bay stranded at 6 A after a >10 h restart (previously
    no code path back); a stale power reading now HOLDS the bay instead of re-targeting it to MIN; the
    peer guard is re-checked across the write await; and the 48 A clamp — the cycle''s headline safety
    derivation — finally has a test and a drill, having had neither.'
  next: 'PLAN REWRITTEN AND BUILD-READY 2026-08-24. docs/planning/PLANNING_evse_solar_follow_amps.md Rev-20,
    658 lines (was 1371). Ships AFTER the DP fix. Tier 3: four framing-disjoint code reviews + orchestrator
    drill verification + operator checkpoint before deploy. Awaiting operator go to build.'
  OPEN_QUESTIONS_FOR_OPERATOR: 'Asked rather than assumed — each changes the design. Q1 CURRENT FLOOR
    AND WHAT "OFF" MEANS. You said Emporia modulates 2A-48A, but the HA number entity reports no min/max/step
    attributes, and Emporia L2 hardware is usually 6A minimum (J1772 pilot floor). What is the true minimum
    this entity accepts, and does writing below it error, clamp silently, or stop the charge? This matters
    because the controller needs a defined "as low as possible without stopping" state — otherwise low-sun
    conditions become the new yo-yo, just at the bottom of the range instead of the top. Q2 IS THE HOUSE
    BATTERY ALLOWED TO CONTRIBUTE AT ALL? Two different products: (i) STRICT follow — amps sized so battery
    net flow is ~zero; battery never discharges to the car; the drain floor is never reached and the yo-yo
    cannot occur by construction. (ii) SOFT follow — allow some battery contribution down to the ev_battery_drain_soc
    floor (currently 80), trading battery cycles for faster charging. I would build (i) first: it is strictly
    simpler, it removes the failure you described rather than damping it, and (ii) can be added later
    as a knob. But it charges slower on marginal days, so it is your call. Q3 WHAT HAPPENS WHEN SPARE
    SOLAR RUNS OUT? Hold at minimum amps, or open the breaker? Holding keeps the session alive and avoids
    EVSE handshake churn but draws a little from the house; opening is cleaner but risks re-triggering
    the on/off cycle at dusk. Related: is there a minimum-on-time you want respected so a passing cloud
    cannot bounce the charger? Q4 GARAGE A IS CURRENTLY OFFLINE — `switch.span_panel_garage_a_evse_breaker`
    and `sensor.span_panel_garage_a_evse_power` both read `unavailable`, while Garage B is on and drawing
    51.5 W idle. Is A decommissioned, unplugged, or is this the fault behind card EV-GARAGE-A-NOCHARGE-1?
    A controller that writes to an offline charger needs to fail quietly, and I want to know whether A
    is a live target before designing for two.'
  PROPOSED_SHAPE_SIMPLEST_VERSION: 'Deliberately the SIMPLEST thing that removes the yo-yo, per the marginal-benefit
    rule. Not a design commitment until the questions above are answered. 1. A small solar-follow controller
    that runs ONLY while an EVSE is already excess-solar-active (i.e. entirely downstream of the existing
    trigger — it never decides to start or stop a charge, so it cannot perturb the TOU/DP precedence logic
    that took several cycles to get right). 2. Every tick: spare = solar_production_w - house_load_excluding_evse.
    Convert to amps at the measured service voltage. Clamp to [min_amps, 48]. Apply a deadband and a step
    limit so it does not chatter. Write the number entity only when the target differs from the current
    value by more than the deadband — a no-op write every 30s for hours is its own problem. 3. Never write
    while the EVSE is not excess-solar-active. Never write during peak. Never write to an unavailable
    entity. 4. Restore to 48 when the solar session ends, so no other consumer inherits a throttled charger.
    This is load-bearing: leaving the limit at 8A would silently cripple a later scheduled charge. CADENCE:
    the operator asks for 30-60s against the EC 5-minute tick. Do NOT change the EC tick — that is a shared
    primitive feeding TOU, DP, arbitrage and load-shed, and speeding it up would multiply every write
    path in the coordinator. Use a separate lightweight timer scoped to this controller only, active ONLY
    during a solar-follow session. That keeps the blast radius to the session and leaves the coordinator
    cadence untouched. SCOPE: the two L2 EVSEs ONLY. Explicitly NOT the L1 chargers (operator).'
  RISKS: 'Stated up front because this is a new WRITER on a live actuator. * NEW WRITE PATH TO A DEVICE
    URA HAS NEVER WRITTEN TO. Everything URA does to the EVSEs today is breaker on/off. Writing amps is
    a new class of command, on a cloud-backed integration, at 30-60s cadence. Write-verify (energy_write_verify.py)
    exists and should be reused rather than reinvented. * WRITE VOLUME. At 30s that is up to 2,880 writes/day
    per charger if unguarded. The deadband and step limit are not polish, they are the containment. URA
    has a prior incident here — the optimizer DB write-flood that forced a rollback to v4.7.33. * PRECEDENCE.
    The DP sticky-yield rules (INV-YIELD-1/2) and the TOU peak veto must both still hold. Confining the
    controller to already-active sessions is what keeps it out of those paths. * A THROTTLED CHARGER LEFT
    BEHIND is the quiet failure mode: if the restore-to-48 step is missed or a restart lands mid-session,
    the next charge silently runs at a fraction of its rate and nobody notices for days.'
  links:
    related:
    - EVSE-DRAIN-PRECEDENCE-KNOB-80-1
    - EV-GARAGE-A-NOCHARGE-1
    context: EVSE-DRAIN-PRECEDENCE-KNOB-80-1 owns the ev_battery_drain_soc floor (live value 80) that
      this yo-yo bounces off — if strict-follow (Q2 option i) is chosen, that floor stops being reachable
      during solar charging at all. EV-GARAGE-A-NOCHARGE-1 may explain why Garage A reads unavailable,
      which decides whether this controller has one live target or two.
- id: EVSE-DRAIN-PRECEDENCE-KNOB-80-1
  RE_VERIFIED_2026_08_23_card_stands_memory_was_stale: 'I told the operator today that this card "carries
    a root cause memory records as REFUTED" and warned it would mislead a builder. THAT WARNING WAS WRONG
    and is retracted. The card was already correct and already more current than the memory I was citing.
    WHAT HAPPENED: a memory file carries "EVSE drain root cause REFUTED (l1_only fired first...)". I relayed
    it without checking that THIS CARD had already reconciled exactly that point — see HOLD_DEMOTION_REFRAMED
    / the reconciliation block above, which explains that gate 6 (l1_only, energy_drain_precedence.py:652)
    short-circuits ABOVE gate 7 (already_below_target, :656), so the refutation described WHICH GATE WAS
    BINDING on a given night, not whether the defect exists. RE-VERIFIED INDEPENDENTLY IN CODE 2026-08-23,
    not from memory and not from the card: energy_drain_precedence.py:652  gate 6 l1_only      -> returns
    before gate 7 energy_drain_precedence.py:656  gate 7 already_below_target energy.py:4271  drain_target_soc=int(self._ev_battery_drain_soc)
    energy.py:4456  drain_target_soc=int(self._ev_battery_drain_soc) BOTH construction sites source the
    STATIC MANUAL knob. Live values today: number.ura_energy_coordinator_ev_battery_drain_soc = 80   (what
    DP actually drains toward) forecast-based off-peak target                     = 10   (what the design
    intends) So the mis-sourcing is real, present at TWO sites, and unchanged. The gate-ordering finding
    explains why it was MASKED on a particular night; it does not refute it. LIVE MEASUREMENT ATTEMPTED
    AND HONESTLY BOUNDED: sensor.ura_energy_coordinator_ev_charging_plan attributes are empty right now
    (no vehicle connected — both EVSEs report "Charger is not connected to a vehicle"), so the runtime
    drain_target could NOT be read today. Code is the available oracle and it is unambiguous; a runtime
    confirmation is still owed the next time a car is plugged in during an eval window. OPERATOR RULING
    THAT PROMPTED THIS: "Memory is not fully trusted. Code and measurement is. And triangulation of latest
    findings. Not just memory." Applied here: memory was the least current of the three sources, and the
    card body plus the code both outrank it.'
  thread: energy
  status: shipped_organic
  shipped_version: v5.90.1
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: ''Card and fix. Approved.'')'
  REV17_VERIFIED_BUILDING_2026_08_25: 'Operator chose (a) one final Rev-17 -> build. Rev-17 written (terminal
    entry-reset + honest sim: emitted_drain_target hand-set, C6 12/12 ORACLE-A as REAL evidence). Orchestrator
    verification PASSED: determine_mode at energy_battery.py:4590 has ZERO await in its body (:4590-5352),
    so the entry-reset (first line) is synchronous-airtight for ANY caller -> D2-HIGH-1 inter-tick refill
    closed by construction; value-stamp right after the partial_hold clamp (:5322); capture (~:5579) before
    the first await (:5587); both callers confirmed (:5573, :6185). Building as 5.90.1 (PATCH per the
    2026-08-25 cadence). Tier-3: build -> framing-disjoint reviews (small diff + C-entry-reset mutation
    must bite + T-cross-tick-reset drives REAL determine_mode) -> orchestrator verify -> operator checkpoint
    before deploy.'
  BUILD_REVIEW_A_2026_08_25_FIX_BEFORE_SHIP: 'DP build 14988adc7 (branch feature/dp-drain-target-value-stamp).
    Review A (correctness/lifecycle): FIX-BEFORE-SHIP, but the value-stamp CORE is AST-verified airtight
    (entry-reset is body[1], ZERO awaits in determine_mode :4599-5380, earliest return :4824 = all 18
    returns post-reset; capture :5636 await-free before first await :5647; single reader, threading clean;
    R1 :5904/:6039 + R3 :3752 untouched; stamped value is drain_target not hold_reserve — correct for
    the soc<=drain_target predicate). ALL findings are in the ONE new INV-DP-DRAIN-1e release branch (:4436-4451):
    5a HIGH — release omits the peer reversion contract (try_transition HOLD_ONLY + _save_evse_state),
    so carrier lies TRANSITIONED w/ stale must_start_by for a window (self-heals next tick); 5b MED —
    no persist after release (restart restores stale _paused_by_dp); 5c MED — the OSCILLATOR: release-on-None
    + re-pause-on-rescan cycles the charger if hold_depth flickers (NWS entity toggling partial<->full
    at 5-min cadence), NO debounce — EXACTLY the class the sibling EVSE-stop cycle was split over. LOWs:
    _dp_decision_soc plain-assign can now LOWER the DP floor mid-window (intended Rev-17 fresh-target-wins,
    note in README); shadow ledger thins (not_applicable on non-drain ticks — note in validation write-back).
    5a/5b = mechanical fix (mirror the peer reversion site). 5c = OPERATOR DECISION (debounce/dwell vs
    accept+card). Review C (mutation+name-diff) still running. ONE consolidated fix-up after C + the 5c
    call. NOT shipping.'
  BUILD_REVIEW_C_2026_08_25_FIX_BEFORE_SHIP: 'Review C (mutation + name-diff): FIX-BEFORE-SHIP. Battery-side
    value- stamp CONFIRMED solid (entry-reset AST-airtight, stamp after multi-day-max + partial_hold clamp
    -> 50 not 10, the cross-tick test drives the REAL determine_mode = NOT hollow). Coordinator-side gaps:
    C-CRIT-1 = drain_target_soc defaults to None, so the 11 prior-cycle b2c1/b2c2 DP tests (call _dp_decision_tick
    with the OLD signature) hit the 1e gate + _DPSkip before any logic -> 12 NEW regressions (name-diff:
    branch 153 failed vs develop 141, +12, 0 fixed), AND that disarm is WHY C-s coordinator drills 3b/4b/5
    came back SILENT (the net was disabled). Fix: make drain_target_soc REQUIRED keyword-only (missing
    = TypeError) + update the 11 sibling call-sites -> 12 regressions go green. HOLLOW ANCHORS: CAPTURE
    (:5635), R2 fresh-actuation (:4556), and the 1e force-revert (:4443) are guarded only by source-STRING
    greps -> 3b (append _drain_target=999), 4b (int(dts)+7), 5 (if False) all PASS-on-neuter = behaviorally
    UNTESTED. Fix: value-behavioral anchors (stamp 40, knob 20 -> paused at 40) + a real 1e stranded-
    release test. C-HIGH-1: shadow None-gate reordered ahead of the pure eval -> blind-hold tick reports
    no_drain_target_this_tick instead of blind_hold (breaks test_switch_off_blind_hold_marks_shadow_not_applicable);
    fix = move the None-gate AFTER the blind-hold check (blind_hold keeps precedence). Also tighten test_mutation_c_entry_reset_bites
    to require "MUTATION SILENT". CONSOLIDATED FIX-UP (after operator 5c call): required-param + 11 sibling
    tests + behavioral anchors + A-s 5a/5b reversion contract + shadow reorder + 5c -> re-verify name-diff
    to 141 baseline + drills bite -> checkpoint -> deploy 5.90.1. GATING: operator 5c (debounce vs accept+card).'
  FIXUP_DISPATCHED_2026_08_25: 'Operator ruled 5c = (a) DEBOUNCE, 2 ticks (not 3) — "I want responsiveness."
    So the 1e None-release waits for 2 consecutive None-while-TRANSITIONED ticks before releasing (in-memory
    streak, reset on any non-None / state-change; must-start-by remains the independent backstop; tick-1
    holds the pause, no floor update). Consolidated fix-up dispatched to ura-builder on the branch: (1)
    drain_target_soc REQUIRED keyword-only + update the 11 b2c1/b2c2 sibling call-sites (clears the 12
    regressions); (2) behavioral anchors replacing the 5 source-grep tests (stamp 40/knob 20 -> paused
    at 40) + real 1e stranded-release test; (3) 5a/5b mirror the peer reversion contract (try_transition
    HOLD_ONLY + _save_evse_state); (4) 5c 2-tick debounce + test; (5) shadow None- gate moved AFTER blind-hold
    check (precedence); (6) tighten mutation-sanity to require "MUTATION SILENT". Then re-verify: sibling
    files green, name-diff to 141, every drill FAIL-on-neuter -> operator Tier-3 checkpoint -> deploy
    5.90.1.'
  TIER3_REVIEW_2026_08_24_DO_NOT_SHIP: 'Build 9694df524 (branch feature/dp-drain-target-mis-sourcing)
    got its four framing-disjoint Tier-3 reviews. A, B, and D INDEPENDENTLY CONVERGED on the same CRITICAL,
    and the orchestrator VERIFIED the mechanism in source. VERDICT: DO-NOT-SHIP. The build is faithful
    to the plan; the PLAN chose the wrong source. Nothing deployed. Back to planning. THE CONVERGENT CRITICAL
    (A-CRIT-1 / B-HIGH-2 / D-CRIT-1): the fix sources the DP drain target from compose_release_floor,
    whose off-peak value is max(reserve_soc, current_park_floor()). BUT current_park_floor() (energy_battery.py:1770)
    returns _last_reserve_level — the POST-OVERLAY effective commanded reserve, NOT the strategy floor.
    _apply_evse_battery_hold (energy.py:5609, runs BEFORE _dp_decision_tick at :5628 same tick) stamps
    _last_reserve_level = max(existing, _evse_hold_soc, _dp_decision_soc) at :4779/:4868, where _evse_hold_soc
    = battery SOC captured at EVSE-charge start. DP only acts when an EVSE is charging (gate 4) — exactly
    when the hold overlay is active. So the composed floor is pinned to ~live SOC. Legal-config repro:
    off-peak, EV charging, SOC 40, reserve 10, forecast target 10, evse_battery_hold enabled (live today)
    -> composed floor = max(10, 40) = 40 -> gate 7 (soc <= drain_target: 40 <= 40) -> ALREADY_BELOW_TARGET
    -> DP never transitions. The blocking value just moved from static 80 to a live-SOC-tracking floor;
    DP is still locked out and the money-losing behavior is NOT fixed. Worse, it is self-referential:
    _dp_decision_soc folds back into _last_reserve_level, so once a transition squeaks through the revert
    predicate flaps one tick later (reintroduces the INV-DP-DRAIN-1b flap through a new path). WHY EVERYONE
    MISSED IT (incl. the plan AND the orchestrator plan-review): the §7 fixture contract PINS reserve_soc=10
    AND park_floor=10 — the one state where the leak is invisible. The plan saw the hold as a CONSUMER
    of _dp_decision_soc (T1c note, §5 coupling 4) and never as the PRODUCER of the helper input. Bug Class
    #53 consumed-but-changed + a feedback edge. This is the v5.5.3 D-HIGH-1 story: 3 framings converged
    where the plan could not. FIX DIRECTION (plan-level, needs operator/planner decision): source the
    DP drain target off the PRE-OVERLAY strategy/policy floor — max(reserve_soc, current_offpeak_drain_target())
    or the existing _last_reserve_level_desired (energy_battery.py:377, the pre-overlay strategy-desired
    that _result writes). §2 recorded current_offpeak_drain_target was "DELIBERATELY NOT used" — that
    exact decision produced this leak and must be re-litigated. The fixture contract must pin park_floor
    != reserve_soc so the discriminating case is tested. SECONDARY (also fix in replan): B-HIGH-1/D-HIGH-1
    — the :4578 containment guard is DEAD CODE; the bare call at :4478 raises FIRST in the same tick,
    so on an Envoy outage the whole tick aborts, stranding EVSEs in _paused_by_dp AND leaving the carrier
    TRANSITIONED with the must-start-by timer never armed (one-shot deleted). Fix: single defensive computation
    right after the off-peak gate, early-RETURN not raise, reused at all five sites. B-MED-1/A-LOW-1/D-MED-1
    — the helper is passed a stale period arg while the gate re-reads get_current_period(); at a TOU boundary
    the non-off-peak branch silently returns raw reserve. D-MED-2 — the Live acceptance criterion §3b
    points at energy.py:3871, which is the R3 blind-window payload (static knob), NOT a DP snapshot; it
    would read 80 forever (false FAIL). Use energy.py:4021 / the charging-plan snapshot instead. B-LOW-1
    — INV-DP-DRAIN-4 framing is wrong: live-apply DOES exist (set_offpeak_drain mutates _drain_targets
    in place at energy.py:8670), no reload needed. CLEAN across all reviewers: exactly five R2 sites and
    no sixth; R1 (5867/6002) and R3 (3752, energy_pool.py:954/1435) byte-identical (INV-DP-DRAIN-2/3 hold);
    the reserve folds stay monotone max() so no other coordinator is lowered. Review C (mutation drills)
    was still running at report time; its verdict cannot rescue the cycle because the fixture pins the
    leak-hiding state.'
  SIM_2026_08_24_CANDIDATE_WINNER: 'Operator asked to enumerate + SIMULATE all candidate sources before
    replanning (scripts/probes/dp_drain_target_candidate_sim.py). 9-scenario matrix scored each candidate
    against an independent oracle (correct floor = max(reserve_soc, offpeak_drain_target, inclement_floor,
    arbitrage_park); EVSE-hold + DP-stamp excluded), on BOTH fit-eligibility and drain depth. Result:
    C0 static 2/9; C1 compose/park (BUILT) 5/9 +1 depth-wrong; C2 offpeak-only 7/9; C3 max(reserve,offpeak)
    7/9 +1 depth-wrong; C4 _last_reserve_level_desired (+boot fallback max(reserve_soc, current_offpeak_drain_target()))
    9/9 — the ONLY fully-correct candidate. Discriminators: S2/S3/S6/S9 (EV charging, hold captured) kill
    C1 (EVSE-hold contamination); S4/S7 (storm partial_hold, arbitrage park) kill C2/C3 (they over-drain
    through legitimate holds); S5 shows C2/C3 also get depth wrong. SURGICAL CHANGE: source DP drain target
    from _last_reserve_level_desired (energy_battery.py:377 — the pre-overlay STRATEGY-DESIRED _result
    stamps, folds offpeak-drain+inclement+arbitrage but NOT the EVSE-hold overlay) with boot fallback
    max(reserve_soc, current_offpeak_drain_target()) when None. Confirms operator instinct. Replan dispatched
    to ura-planner 2026-08-24.'
  PLAN_REVIEW_ROUND2_2026_08_24_FIX_PLAN_FIRST: 'Rev-12 replan got TWO framing-disjoint Tier-3 plan reviews
    (completeness + adversarial build-prediction). BOTH returned FIX-PLAN-FIRST and converge. Orchestrator
    verified the real _result branches in source (energy_battery.py:4890 backup bypass, 4998 peak soc-5,
    5057 mid-peak soc, 5330 off-peak drain, 5343 off-peak hold soc, 5588 desired write, 5612 _desired_stamped_at).
    CONFIRMED-REAL, must fix before build: (1) STALENESS (the big one): _result stamps _last_reserve_level_desired
    ONLY when reserve_level is not None; the grid-disconnect BACKUP leg (4890) + blind-hold RETURN legs
    bypass it, leaving desired STALE from a prior (maybe peak SOC-pinned) tick -> a grid-outage off-peak
    DP tick reads a stale floor. FIX: freshness-gate on _desired_stamped_at (exists for exactly this);
    treat stale as skip; add freshness invariant. (2) SHADOW SITE :4271 in _run_dp_shadow_eval (4195)
    — different method, before the off-peak gate, DP-disabled only; the compute-once local cannot reach
    it. Needs its own compute + None handling; T1b/C6 respecified. (3) CONTAINMENT early-return is INERT:
    MUST_START_FORCED + paused-idle branches sit AFTER the compute site, so early-RETURN skips them just
    like a raise. Hoist them above, or lazy-compute at consumption. (4) revert snippet DROPS `_soc is
    not None` -> None<=target TypeError -> reintroduces the hazard; keep the guard. (5) boot-fallback
    `or 0` yields reserve 0 (battery-to-empty) if the accessor raises; delete `or 0`. (6) _StubBattery
    lacks reserve_soc; helper reads it outside try -> AttributeError; use getattr, enumerate 3 stub additions.
    (7) fixture scenario3 asserts 80 == live static knob -> mutation C1 passes on it; require 4-way distinctness.
    (8) my SIM ORACLE WAS TAUTOLOGICAL (true_floor==strategy_desired, no reserve>drain row) — 9/9 is not
    independent evidence; rebuild with a real oracle + real branch models. ORCHESTRATOR RESOLUTION (do
    not take reviewers at face value): review#1 CRIT-1 (source MUST floor by reserve_soc) is PARTIALLY
    WRONG — the off-peak drain branch INTENTIONALLY drains below reserve_soc on good-solar nights (drain
    stored solar, refill tomorrow); reserve_soc is the NON-off-peak floor. Flooring by reserve would make
    DP MORE conservative than the strategy and break the fix on excellent nights. The real defect is STALENESS,
    not sub-reserve. OPEN DESIGN DECISION for the operator: should DP be allowed to drain below the hard
    reserve to charge the car on a good-solar night (matching strategy intent), or is the hard reserve
    an absolute EV-charging floor? NEXT: rebuild sim with real branches, resolve that decision, hand planner
    ONE consolidated fix-up, re-review.'
  SOURCE_RULING_2026_08_24: 'OPERATOR RULED: the DP drain target is the OFF-PEAK DRAIN TARGET (forecast
    next-day safe floor, computed after peak/mid-peak, >= reserve in this config), NOT _last_reserve_level_desired.
    Final source: max(reserve_soc, current_offpeak_drain_target()) — which is EXACTLY the formula the
    accessor docstring already names as "the drain-release floor" (energy_battery.py:1733). This makes
    DP drain to precisely what the strategy off-peak emitter drains to (the accessor mirrors the emitter
    incl the multi-day max(D+1,D+2)). Reserve-floor question RESOLVED as moot in this config: drain_targets
    {excellent 10, good 15, moderate 20, poor 30, very_poor 30} are all >= reserve_soc 10, so the max()
    is a free safety net (no-op today, guards a future inverted config). NOT _classify_target_day for
    DP: using the peak-anchored resolver in DP while the emitter still uses calendar-tomorrow would make
    DP pause/allow the EV against a floor the battery is not holding — a NEW divergence, worse than the
    shared staleness. The cross-midnight staleness (accessor + emitter both key on calendar-tomorrow,
    stale after midnight in the cross-midnight off-peak window) is SPLIT OUT to DRAIN-TARGET-DAY-STALENESS-1
    (operator-approved split), which fixes emitter+accessor together via _classify_target_day. This supersedes
    SIM candidate C4 (_last_reserve_level_desired): rebuild the sim oracle around max(reserve_soc, current_offpeak_drain_target())
    and re-score. Consolidated planner fix-up now folds: this source + the round-2 review findings (staleness-of-desired
    no longer relevant since we do not use desired; shadow-site :4271 own compute; containment reshape;
    keep _soc None-guard; no `or 0`; getattr reserve_soc; 4-way-distinct fixtures; harness _dp_drain_target_soc).
    Then fresh Tier-3 plan re-review.'
  DEPENDS_ON_MIDNIGHT_FIX_2026_08_24: 'REFINEMENT (operator insight): DP is NOT evening-only — only gate
    is off_peak AND dp_on (energy.py:4416); off-peak crosses midnight, so DP fires post-midnight (overnight
    charge into AM / post-midnight plug-in). So DP needs the SAME-DAY drain target after midnight like
    attain (peak-anchored: tomorrow before midnight, today after). => DP must consume the PEAK-ANCHORED
    accessor, so DRAIN-TARGET-DAY-STALENESS-1 is now a PREREQUISITE (blocked_by), NOT a parallel/after
    split. Sequence: midnight fix ships first -> DP consumes the corrected current_offpeak_drain_target()
    -> the day-classification slice of the round-2 staleness findings dissolves (the accessor DP reads
    is already correct). SUPERSEDES the earlier "consume stale accessor to match emitter, fix later" compromise
    (which would make DP consistently wrong after midnight).'
  SIM_V2_2026_08_24_NON_TAUTOLOGICAL: 'Rebuilt sim (scripts/probes/dp_drain_target_candidate_sim.py) with
    a NON-tautological oracle = the emitter''s ACTUAL off-peak drain floor (modeled from the real emitter:
    multi-day max(D+1,D+2) + partial_hold clamp), independent of any candidate formula; C1''s EVSE-hold
    contamination modeled explicitly. 8 scenarios, scored on fit AND exact depth. C0 static 0/8 exact;
    C1 compose/park (BUILT) 2/8 exact, 3/8 fit — diverges whenever an EVSE charges (S2/S3/S4/S7), mis-depths
    (S5); C2 current_offpeak_drain_target 8/8 exact (perfect emitter mirror); C3 max(reserve,accessor)
    [RULED] 8/8 fit, 7/8 exact — differs only in the SAFE direction on the never-in-config inverted case
    (S8 reserve15/drain10 -> 15 vs 10). Confirms the ruled source non-tautologically. C2 is the purest
    mirror; C3 adds a safe reserve floor the emitter lacks — operator ruled C3. Consolidated fix-up dispatched
    to ura-planner.'
  REV13_REREVIEW_2026_08_24_FIX_PLAN_FIRST_SIM_WRONG_AGAIN: 'Both Rev-13 Tier-3 re-reviews (completeness
    + build-prediction) returned FIX-PLAN-FIRST and CONVERGED on a CRITICAL that invalidates the ruling.
    Orchestrator owns this: my sim v2 was ALSO flawed — c2_accessor MODELED a partial_hold clamp (max
    with effective_reserve) that the REAL current_offpeak_drain_target() (energy_battery.py:1726-1747)
    does NOT have (it is only max(D+1,D+2); the clamp lives in the EMITTER at :5322). So C3 is NOT 8/8
    — on the inclement S6 fixture the real accessor returns 10 while the emitter parks at 60 (7/8, failing
    the safety case). WORSE (D-CRIT-2): the accessor mirrors ONLY the drain-FALLBACK branch. When arbitrage
    is ON / attain / full_hold / partial_hold, the emitter emits a DIFFERENT reserve_level (peak_buffer_target
    / attain floor / backup) and NEVER reaches the drain branch (source comment :5301-5305: "When arbitrage
    is ON we never reach this branch"). current_park_floor() docstring (:1753-1758) enumerates exactly
    these divergences and says the drain-target accessor "mirrors only the drain-target fallback path".
    So "DP mirrors the emitter via current_offpeak_drain_target()" is FALSE on reachable legal-config
    paths — DP would OVER-CREDIT drain headroom (think it can drain to 10 when the battery holds 60/80)
    -> pause the EV expecting drain that never happens. THE REAL FORK (operator decision needed; do NOT
    let me iterate again without it): (A) narrow the invariant + add an explicit BRANCH GATE — DP only
    transitions when the emitter is actually in the drain-fallback branch (not arbitrage/attain/full_hold/partial_hold),
    reading current_offpeak_drain_target() for that branch only; OR (B) source from _last_reserve_level_desired
    — the pre-overlay strategy floor _result stamps, which DOES include inclement/arbitrage/attain across
    ALL branches and excludes the EVSE-hold overlay — BUT has the bypass-leg STALENESS caveat an earlier
    review flagged (needs freshness gating via _desired_stamped_at). This is exactly the source I pivoted
    AWAY from earlier; the pivot was wrong for the multi-branch reason. Neither is clean. Bug Class #63
    (concept collision) biting the orchestrator twice on the same seam. Secondary: line drift _apply_dp_transition
    is :4873 not :4953; b2c1 has 3 battery stubs (_StubBattery/_SpyBattery/_Boom) + 2 name lists + a b2c3
    harness the plan under-enumerates; shadow acceptance must assert shadow_reason not set-membership;
    tou_period arg unused; very_poor validator (energy.py:8645) now load-bearing — card with this cycle.
    STATUS: DP source UNRESOLVED, NOT building. Surfaced to operator with full candor.'
  OPTION_A_RULED_2026_08_24: 'Operator ruled OPTION A (branch-gate): "DP and Attain are almost mutually
    exclusive in practice." DP transitions ONLY when the off-peak emitter is in its DRAIN-FALLBACK branch;
    when arbitrage/attain/full_hold is active DP declines (the battery is already held high — no drain-
    precedence decision). In the drain branch, target = max(reserve_soc, current_offpeak_drain_target()).
    This makes "mirror the drain-fallback branch" the EXPLICIT contract and resolves the Rev-13 CRITICALs
    (the false whole-emitter-mirror claim). Rev-14 dispatched to ura-planner: branch gate + reason code,
    HONEST sim rebuild (real accessor, no clamp; arbitrage/attain fixtures where DP correctly DECLINES),
    plus the still-applicable findings (lazy-compute containment, shadow not_applicable, harness enumeration
    incl _SpyBattery/_Boom/b2c3, _apply_dp_transition line drift 4873, very_poor card). Then Tier-3 re-review.'
  REV14_REREVIEW_2026_08_24_FIX_PLAN_FIRST_GATE_LEAK: 'Rev-14 branch-gate re-review: FIX-PLAN-FIRST, CRITICAL.
    The negative-inference gate (not_arb AND not_attain AND allow_discharge) is INCOMPLETE — it OPENS
    on paths that are NOT the drain-fallback branch: (C-CRIT-1) arbitrage WAIT explicitly sets _arbitrage_active=False
    at energy_battery.py:3157 -> gate opens but emitter emitted reserve_soc, not the drain floor; legal-config
    repro (arbitrage_enabled, target poor, WAIT phase): accessor 30 vs emitter rides to 10 -> DP mispredicts
    by 20pp, the SAME mis-sourcing this cycle exists to fix. (C-CRIT-1b) 4 more: Envoy-blind hold (:4705),
    attain-reboot defer (:4118) + release (:4131), grid-disconnect backup (:4890). RECOMMENDED FIX (sound,
    more robust): STOP inferring — stamp POSITIVELY inside the drain-fallback branch (one line at ~:5274,
    e.g. _offpeak_branch_tick = decision_tick_seq) and have the gate verify the stamp is THIS tick. Requires
    relaxing the plan-s "no energy_battery.py change" constraint (that constraint is what forced the fragile
    negative inference). This is the durable Bug Class #53 (computed-but-not-consumed / seam) closure.
    ALSO: C-HIGH-1 shadow gate runs before the off-peak gate (opens in peak/mid_peak) — add tou_period=="off_peak"
    to the gate; C-HIGH-2 gate closing while TRANSITIONED strands the pause (suppression-needs-a-discharge)
    — force revert or compare stamped _dp_decision_soc; C-HIGH-3 the v3 sim in_drain_branch is DERIVED
    from the candidate predicate (tautological AGAIN) — set it independently per fixture + add N5 WAIT
    / N6 envoy-blind / N7 attain-release / N8 peak fixtures. Items 2/4/5 verified clean (ordering, containment,
    harness, very_poor card, new constant). STATUS: NOT building; positive- stamp fix needed + re-run
    Tier-3 D pass. DP has now had 1 rolled build + 3 flawed sims + ~12 reviews + 4 plan revisions on this
    seam — surfacing park-or-continue to operator.'
  REV15_DPASS_2026_08_25_VALUE_STAMP: 'Operator chose CONTINUE. Rev-15 positive-BOOL-stamp built + sim
    C5 12/12, but the adversarial-completeness D pass found 2 more real leaks (bool stamp exhaustive as
    a LINE, but its MEANING is not): D-HIGH-1 partial_hold REACHES the drain-fallback branch (only full_hold
    short-circuits at :4903; :4900 comment: "partial_hold/allow_discharge fall through"), stamps True,
    THEN the emitter clamps drain_target up to effective_reserve at :5322 — which current_offpeak_drain_target()
    does NOT mirror. Repro: partial_hold, eff_reserve 50, tomorrow excellent drain 10, SOC 55 -> gate
    opens, DP uses 10, emitter parks 50 -> DP pauses EVSE for SOC<=10 that never comes. The sim N3 fixture
    wrongly modeled partial_hold as in_drain_branch=False, hiding it. D-HIGH-2 determine_mode has TWO
    callers (_decision_cycle_body :5573 AND _evaluate_battery :6185) + an await window before the DP gate
    read, so a concurrent _evaluate_battery at a TOU boundary can stamp for a different decision. CLEAN:
    Rev-14 branch enumeration confirmed (WAIT/envoy/attain/peak do NOT reach :5274); shadow guard holds;
    1e holds; exactly-one stamp LINE. FIX (reviewer-prescribed, the converged end-state): stamp the VALUE
    not a bool — after the partial_hold clamp, adjacent to the emitted _result, set _offpeak_drain_branch_target:
    int|None = drain_target (the ACTUAL post-clamp value); reset None each cycle; gate = "value present";
    DP consumes it VERBATIM -> ELIMINATES _dp_drain_target_soc re-derivation (root of the saga), value-exact
    across ALL branches incl partial_hold. Stamp LATE (after clamp), fail closed. D-HIGH-2: capture the
    value into a LOCAL right after determine_mode returns at :5573 (no await between) + thread as a param
    into _dp_decision_tick + _run_dp_shadow_eval; do NOT re-read the gate inside the tick. D-LOW-1: _get_off_peak_decision
    does not exist — the branch is inline in determine_mode (:4590/:5269-5352); rename. Restate INV-1d
    as "the decision DP is reasoning about was produced by the drain branch" (value-carried). Fix the
    sim: N3 -> in_drain_branch True + clamp in the oracle; re-score. Rev-16 (value-stamp) dispatched.
    This is convergent — each round drove toward "DP just reads the emitter''s real emitted floor," which
    the value-stamp IS. Tier-3, re-review after.'
  REV16_DPASS_2026_08_25_NEW_LEAK_STEPBACK: 'Rev-16 value-stamp built (C6 = strict emitter mirror, 12/12
    ORACLE-A by construction; deletes _dp_drain_target_soc + the bool gate). D-pass found ONE genuinely
    NEW reachable leak = D2-HIGH-1: the mailbox is cleared ONLY by the consumer (_decision_cycle_body
    capture), so the OTHER determine_mode caller (_evaluate_battery :6185, fired on a TOU edge) can REFILL
    it BETWEEN ticks. Legal repro: evaluate() stamps 10 at the 21:00 off-peak edge (good forecast, drain
    branch) -> NWS warning flips the next tick to full_hold (short-circuit :4903, no stamp) -> DP tick
    reads leftover 10 -> pauses EVSE to "drain to 10" while the battery is in storm full_hold not draining.
    TERMINAL FIX (reviewer-prescribed): move the reset to the FIRST statement of determine_mode (:4590,
    before any early return); determine_mode is fully synchronous (no await :4590-5352) so entry-reset
    + immediate capture is airtight for ANY number of callers, closed by construction (strictly stronger
    than the mailbox). Other findings = doc-consistency: §1/§2/§3 still describe the no-reset variant
    (a builder would ship stale-value from §3) -> reconcile to entry-reset; §3a scorecard numbers wrong
    (C2 4/3, C3 3/4, C6 12A/11B) + A/B labels swapped; the sim emitted_drain_target is DERIVED from emitter_drain_floor
    (same fn as ORACLE-A) so C6=oracle is a value-axis TAUTOLOGY — the honest claim is closed-by-construction
    from SOURCE, not sim evidence (gate axis in_drain_branch IS hand-set, honest); reserve_level-equality
    wording wrong on the :5344 at/below-target path (emits hold_reserve not drain_target); extend the
    no-helper grep to _run_dp_shadow_eval; :5573->:5579 citation. STEP-BACK per operator standing instruction
    ("if a new leak appears, step back not Rev-17"). Orchestrator read: the leak is a reset-PLACEMENT
    bug with a TERMINAL fix, the value-stamp APPROACH is sound + converged (leaks all about where the
    reset lives, never the sourcing) — likely the last round, but surfaced to operator for the call: (a)
    one final Rev-17 (entry-reset + doc reconcile) -> build; (b) pause DP, move to solar-follow; (c) operator
    reviews the design first. NOT dispatching Rev-17 without a nod.'
  links:
    related:
    - DRAIN-TARGET-DAY-STALENESS-1
    context: 'DECOUPLED (operator 2026-08-24, after the nervousness re-examination): DP just READS current_offpeak_drain_target()
      and must MIRROR the emitter (DP predicts where the battery actually drains, which follows the emitter).
      So DP does NOT touch the shared primitive and is NOT blocked by the midnight fix. When the midnight
      fix lands it corrects emitter+accessor together and DP inherits the correction automatically. The
      earlier blocked_by was wrong — DP mirroring the emitter is correct relative to actual battery behavior
      regardless of the staleness.'
  created: 2026-08-20 00:20
  updated: 2026-08-25 21:50
  DISPATCH_2026_08_24: 'Plan re-reviewed by orchestrator this session (independent grep of the five R2
    sites, compose_release_floor signature, off-peak gate coverage, and the :4555 MUST_START containment
    hazard — all confirmed source-accurate, zero drift). Operator approved the fan-out. Dispatched to
    ura-builder in an isolated worktree as a Tier-3 build. SHIP is still gated on the mandatory Tier-3
    operator checkpoint BEFORE deploy (operator: "after I will approve to ship").'
  HOLD_DEMOTION_REFRAMED_2026_08_20: 'MY EARLIER "max() SWALLOWS DP" FRAMING WAS WRONG — retracted after
    a second code read at operator''s instruction. DP does NOT have to beat evse_battery_hold in the max()
    fold, because DP PAUSES THE CHARGER (energy.py:4921-4941, switch.turn_off via _claim_pause_dispatch_owner("dp")),
    and the hold is keyed on _is_any_evse_charging(): when charging stops, energy.py:5611-5613 sets _evse_battery_hold_active=False
    and CLEARS _evse_hold_soc=None. So the hold releases as a CONSEQUENCE of the DP pause. The operator''s
    description of the design is accurate and the mechanism is coherent: hold latches SOC at charge start
    -> DP re-evals -> if the plan fits, DP pauses the charger -> hold drops -> house drains the battery
    -> DP releases the pause at computed_start_dt -> charging resumes on cheap off-peak grid and the hold
    re-latches at the now-LOWER SOC (correct: we no longer want battery -> car). CONSEQUENCE FOR SCOPE:
    hold demotion is NOT required for the battery to drain, so it should NOT be bundled reflexively. The
    blocking decision recorded in the 2026-08-20 handoff section 4.1 is VOID. Demotion remains available
    if a later trace shows the hold blocking a case the pause does not cover, but the assurance the operator
    asked for came back: the pre-existing mechanism is right, and the defect is the drain-target application.'
  DRAIN_TARGET_MISSOURCING_RECONFIRMED_2026_08_20: 'RECHECKED at operator''s request and it HOLDS. All
    four TransitionInputs population sites pass the STATIC knob: energy.py:4271, :4456, :4522, :4540 all
    read int(self._ev_battery_drain_soc). The forecast off-peak target (live 10) never reaches DP. Reconciling
    this with the 2026-08-20 REFUTATION: the refutation was NARROWER than it read. It established only
    that mis-sourcing was not the cause of LAST NIGHT''S observation, because gate 6 (l1_only, energy_drain_precedence.py:652)
    short-circuits ABOVE gate 7 (already_below_target, :656) and the charger was physically pinned at
    ~6A. It did NOT refute the defect. The defect is real, code-verified, and was simply masked by a gate
    above it.'
  GATE_CLAIM_CORRECTED_2026_08_20_EVIDENCE: 'OPERATOR CHALLENGED "both gates clear simultaneously" and
    asked for real evidence. The claim was WRONG AS STATED — retracted. What the evidence actually shows,
    from sensor.ura_energy_coordinator_ev_charging_plan (the DP observability sensor, user-facing name
    "EV Charging Plan"): (a) DP HAS NEVER TRANSITIONED — supported. 10 days of state history, 164 records,
    contains ONLY hold_only / hold_pre_eval plus unknown+unavailable at restarts. Never `transitioned`,
    never `must_start_forced`. Live carrier confirms transitioned_at: null. CAVEAT, stated honestly: recorder
    retention is ~10 days, so this proves "not in 10 days", not "never in production life"; and transitioned_at
    is cleared on reversion, so it alone could not distinguish a past transition that reverted. The 10-day
    state history is the stronger leg because `transitioned` is a recorded state and never appears. (b)
    GATE 6 HAD ALREADY CLEARED — my claim that the operator''s Emporia fix would clear it is wrong in
    timing. The 13:40 eval snapshot shows charger_rate_kw = 5.80, already well above l1_rate_threshold_kw
    3.0. l1_only was the binding gate OVERNIGHT, not this afternoon. (c) THE ACTUAL BINDING GATE THIS
    AFTERNOON WAS blind_hold (gate 1), because soc was null — the 13:27 Envoy outage. decision.reason
    = "blind_hold", is_blind_hold: true. (d) THERE IS A FOURTH SUPPRESSOR I HAD NEVER MENTIONED: the sensor
    exposes eval_gate: "not_off_peak". DP only evaluates during off-peak. last_eval_at is 13:40 with eval_age_min
    304 — no eval has run in five hours because of this outer gate. CORRECTED FRAMING: DP is suppressed
    by a STACK of gates and WHICH ONE BINDS ROTATES — not_off_peak by time of day, blind_hold when the
    Envoy drops, l1_only when the charger is throttled, no_charging_evse when unplugged. The mis-sourced
    drain target sits BELOW all of them and has therefore never been reached. That is why it has never
    produced a symptom, and it is also why no single observation of DP sitting in hold_pre_eval ever discriminated
    anything.'
  IMMINENT_ACTIVATION_2026_08_20_LIVE: 'SUPERSEDED IN PART by GATE_CLAIM_CORRECTED above — the "both gates
    clear simultaneously" premise is withdrawn. What SURVIVES: drain_target_soc reads 80 in the live 13:40
    eval snapshot, confirming the mis-sourcing in production data rather than only in code. And the risk
    shape is unchanged even though the timing claim was wrong — IF a night arrives where off-peak + healthy
    Envoy + charging + rate>3kW + soc>80 all hold, DP transitions and stamps a floor of 80. The conditional
    is the honest form. ORIGINAL TEXT: URGENT — the masking gate is about to clear and the live numbers
    now predict a WRONG-DIRECTION actuation on the first qualifying night. Live reads 2026-08-20 ~19:00
    CDT: battery SOC = 96 (Envoy recovered from the 13:27 outage); switch.garage_a charging_rate = 23
    A (~5.5 kW) after the operator removed Emporia excess-solar management. 5.5 kW > l1_rate_threshold_kw
    (3.0) => GATE 6 NO LONGER FIRES. And 96 <= 80 is FALSE => GATE 7 NO LONGER FIRES EITHER. Both gates
    that have suppressed DP for its entire production life clear simultaneously. So on the next night
    the car charges, DP transitions for the FIRST TIME EVER — and _apply_dp_transition (energy.py:4953)
    will stamp _dp_decision_soc = drain_target = 80, i.e. a reserve FLOOR of 80. Predicted behaviour:
    the battery drains only 96 -> 80 and stops, ~16 points instead of ~86, and the reserve floor is then
    pinned at 80 for the whole TRANSITIONED window — during which INV-DP3 max() supremacy guarantees nothing
    can demote it. Second-order: a floor of 80 during off-peak is a floor the strategy will try to ATTAIN,
    so this can command CHARGING from grid at the very moment the plan intends draining. This is the activation
    risk the handoff flagged, now with a date on it. DISCRIMINATING ACCEPTANCE CRITERION (satisfies the
    corollary the last misdiagnosis violated): under the fix, the first DP transition stamps _dp_decision_soc
    = 10 and battery SOC falls below 80 overnight; under the unfixed build it stamps 80 and SOC floors
    at 80. The two are distinguishable in one read of the DP snapshot, and 80-vs-10 is not producible
    by any other candidate mechanism.'
  PLAN_FINALISED_2026_08_24: 'Plan review returned 11 findings, ALL in the test/drill/acceptance sections
    written the same day; all applied. Confirmed sound by review and NOT to be re-litigated: the five
    R2 sites (independent grep), the compose_release_floor helper signature, INV-DP-DRAIN-4 (live-apply
    chain traced number.py -> energy.set_offpeak_drain -> energy.py:8651), and that all five R2 sites
    sit inside the off-peak gate so the non-off-peak branch is unreachable. Fixes applied: (1) per-site
    drills C6-C10 added -- the old set anchored only 3 of 5 R2 sites while claiming "all five must bite";
    (2) T3 and T4 de-hollowed -- both folded their own mutation into their assertion, so T3 could not
    fail and T4 was self-contradictory; (3) T4b added, closing INV-DP-DRAIN-3 which had NO anchor at energy_pool.py:954/:1435;
    (4) the helper static-reserve fallback DELETED as unreachable (floor-is-None implies static-reserve-is-None,
    so the fallback read the same None) and T3b deleted with it since its fixture was a state production
    cannot produce; (5) the :4555 containment hazard specified -- a bare call there propagates to the
    OUTER handler and kills the MUST_START_FORCED safety revert; (6) the Live criterion made PAIRED, since
    "reserve floor does not exceed SOC" reads PASS both under the fix and under DP never transitioning
    at all; (7) very_poor drain target recorded as unable to live-apply (energy.py:8647 validates only
    four qualities) -- pre-existing but this cycle makes it load-bearing.'
  next: Observe the INV-DP-DRAIN-1e 2-tick no-flap release at the next drain-window-end/TOU boundary;
    if clean, dispose to done. Primary discriminator already validated.
  refinement_status: refined
  SCOPING_2026_08_20_ONE_NUMBER_THREE_ROLES: 'THE REAL FINDING — _ev_battery_drain_soc serves THREE INCOMPATIBLE
    SEMANTIC ROLES, two of which point in OPPOSITE DIRECTIONS. R1 PAUSE CEILING (energy.py:5842 EV, :5977
    plugs -> energy_pool.py:3224 `soc_low = battery_soc < soc_threshold`): "if SOC drops below X while
    EV charges, pause the EV" — HIGHER = more protection. R2 DRAIN FLOOR (energy.py:4271/:4456/:4522/:4540/:4555
    -> DP gate :656/:663): "drain DOWN TO X then stop" — LOWER = more drain allowed. R3 RIDE-PROOF FLOOR
    (energy.py:3752 blind-hold proof, energy_pool.py:954/:1435, compared as `env_lower >= drain_target`):
    "LKG envelope lower bound must stay above X to permit riding a blind window" — HIGHER = stricter.
    At 80 both bad effects are maximal TOGETHER: DP never transitions AND the EV drain-pause fires whenever
    the battery discharges below 80%. CONSEQUENCE: "bind DP to the forecast target" is NECESSARY BUT NOT
    SUFFICIENT — doing only that leaves R1 at 80 and the EV still gets paused nightly, a plausible DIFFERENT
    failure the acceptance criteria must discriminate against.'
  PROVENANCE_OF_80_RESOLVED: 'Read live from .storage/core.config_entries entry 01KJEC3FYPYAGBQKZWC94CR8GR:
    energy_ev_battery_drain_soc data=None options=80.0. So 80 is NOT the ctor default (50) and NOT an
    initial-setup data value — it came through an options-flow save. Stored as FLOAT 80.0: number.py:1529
    writes int(value) while the options-flow NumberSelector yields a float => WRITTEN BY THE OPTIONS FLOW,
    not the Number slider. The options flow re-submits every field on every save, so an 80 present at
    any point is re-committed by any unrelated save — consistent with the operator never deliberately
    turning it in 10 days. SMOKING GUN FOR INTENT: energy_fill_priority_soc is ALSO 80.0, and fill-priority
    IS the charge-above threshold. The operator''s stated intent ("80 was meant as a charge-above threshold")
    maps exactly onto fill-priority, which already holds 80. The drain knob at 80 is a CONFLATED/DUPLICATED
    value, not policy.'
  RECOMMENDED_DESIGN_D_SPLIT_THE_ROLES: 'Options (a) delete knob and (b) max(forecast, floor) and (c)
    repurpose as charge-above were all evaluated and REJECTED — (a) silently deletes the R1 deep-discharge
    backstop during expensive-grid windows (energy_pool.py:1790-1793 says it must remain a hard backstop);
    (b) with live values is max(10,80)=80, A NO-OP, the bug survives verbatim; (c) duplicates fill-priority
    which already holds 80. RECOMMENDED (d): R2 sources from a new helper returning compose_release_floor(self._battery,
    period)[0] — NOT the bare current_offpeak_drain_target(), because compose_release_floor already reconciles
    static reserve + current_park_floor() (arbitrage/attain parks, inclement partial holds) and the EV
    drain-pause at :5838 already uses exactly this; using the raw accessor would re-introduce the parallel-re-derivation-blind-to-parks
    bug that energy_battery.py:289-292 closed. THIS IS THE SINGLE MOST IMPORTANT DESIGN CALL IN THE CYCLE.
    R1 keeps the knob (rename the concept away from "drain SOC" — the NAME caused the conflation) and
    its live 80 must come down. R3 follows R1 (operator-wired hard floor); a forecast 10 would make the
    blind-window envelope proof trivially permissive = silent safety weakening. NO NEW KNOBS.'
  BIGGEST_SCOPE_QUESTION_HOLD_DEMOTION: CRITICAL, OPERATOR DECISION NEEDED. evse_battery_hold pins the
    reserve to LIVE SOC while the EV charges. DP transitioning while the hold is active means max(hold_reserve=SOC,
    dp=10) = SOC — DP'S LOWER TARGET IS SWALLOWED BY THE HOLD. So fixing the target ALONE MAY STILL NOT
    DRAIN THE BATTERY. The parked memo project_ev_drain_precedence_cycle.md already names "hold demoted
    to backstop" as the intended design. THE PLAN MUST STATE WHETHER HOLD-DEMOTION IS IN SCOPE; if it
    is not, the acceptance criteria MUST NOT claim "battery drains overnight" — that would be an undiscriminating
    criterion of exactly the kind that produced this whole misdiagnosis.
  ACTIVATION_RISK_NOT_JUST_A_BUGFIX: This fix ACTIVATES A DORMANT STATE MACHINE. At target 10 vs SOC 37,
    drain_soc_pp = 27 and drain_hours ~1.8h, so DP will actually transition FOR THE FIRST TIME IN PRODUCTION.
    The transition path (_apply_dp_transition, EVSE switch.turn_off dispatch :4936, _paused_by_dp, _claim_pause_dispatch_owner("dp"),
    the must-start-by timer) has never run organically at scale. Live validation must watch the ACTUATION
    path, not just the target number.
  HIGHEST_PROBABILITY_BUILD_ERROR: MUST BE AN EXPLICIT NON-GOAL IN THE PLAN. energy.py:5842/:5977 pass
    the composed floor as reserve_soc= AND the static knob as soc_threshold= SIDE BY SIDE. A builder "making
    the drain target consistent" by pointing soc_threshold at the composed value would collapse soc_low
    = soc < 10 and SILENTLY DELETE THE DEEP-DISCHARGE BACKSTOP during peak windows.
  TEST_BLINDNESS_EXPLAINED: 'drain_target_soc=15 is a LITERAL IN THE TEST FIXTURE (test_evse_drain_precedence_session_b2a.py:126-149
    _mk_inputs). NO test anywhere asserts what _DPInputs.drain_target_soc is POPULATED FROM. The two tests
    touching it (test_evse_drain_precedence_session_b2c1_fixup.py:307, test_blind_window_evse_guard.py:166)
    set it on a FAKE coordinator — they ASSUME the wiring they should prove. Hollow-anchor pattern per
    feedback_hollow_test_anchors: a fake whose attribute name matches production is not evidence production
    reads it. T1 (the wiring test) must use values on OPPOSITE SIDES of fixture SOC so a wrong source
    yields ALREADY_BELOW_TARGET and the right one yields a fit — discriminating.'
  TIER_3_RECOMMENDED: 'Tier 3: 5 target-source sites + 2 reserve-fold legs (:4733 update-in-place, :4829
    append — the :4829 leg exists precisely because an earlier cycle missed it) + log/snapshot path =
    10+ emission sites, Bug Class #53 shape; value lands on the COMMANDED Enphase reserve floor (cost
    AND safety); area already carries B2a/B2b-ii/B2c-1/B2c-2 fix-ups and a Tier-3 dead-band cycle with
    a D-HIGH-1. Framings A=per-site arithmetic, B=state-machine integrity + byte-identical R1/R3, C=REAL
    per-site source mutation (8 named mutations enumerated), D=adversarial completeness re-enumerating
    PRE-EXISTING code. Plus TWO plan reviews before build.'
  UNDETERMINED_BLOCKERS_FOR_PLANNER: (1) Do the four energy_offpeak_drain_* Number entities LIVE-APPLY
    into BatteryStrategy._drain_targets, or only write entry.options requiring a reload? _drain_targets
    is ctor-frozen at energy_battery.py:464; __init__.py:5863-5867 lists them in the reload-suppress block
    which SUGGESTS a live-apply path exists. If none does, "the forecast target updates live" is FALSE
    and the fix inherits a reload dependency. MUST be traced before writing acceptance criteria. (2) The
    numeric value of DEFAULT_OFFPEAK_DRAIN_UNKNOWN (the Solcast-dead fallback) was not read and belongs
    in the plan.
  DOCS_DRIFT_TO_FIX_IN_CYCLE: PLANNING_evse_drain_precedence.md (unbound drain_target symbol — the plan-gap
    defect itself); PLANNING_inclement_weather_reserve.md:66,82 cites energy.py:2671/2672 for the drain
    read sites, STALE BY ~3200 LINES; docs/user-manual/ENERGY_COORDINATOR.md:642 says default 50 and describes
    it as R1-only (which BECOMES correct after this cycle); docs/Coordinator/ENERGY_COORDINATOR_MANUAL.md:455.
  ROOT_CAUSE_REFUTED_2026_08_20: 'THE CAUSAL CLAIM ON THIS CARD IS REFUTED. Operator ("wasn''t sure we
    were correct about the bug — triple verify") ordered a real framing-disjoint verification; four independent
    agents ran. The mis-sourced drain target is REAL but is NOT what stopped the overnight drain. WHAT
    ACTUALLY HAPPENED: the DP evaluated every 5 min all night (105 state changes 21:02->07:00, dp_enabled
    true, is_blind_hold false) and short-circuited at the L1-classification gate (energy_drain_precedence.py:652,
    charger_rate_kw <= DP_L1_RATE_THRESHOLD_KW 3.0) -> decision.reason "l1_only" on EVERY overnight snapshot,
    with drain_hours / charge_hours / computed_start_dt all null (it exited BEFORE any target arithmetic).
    The drain target was never consulted. Setting the knob to 10 would have changed NOTHING.'
  WHY_THE_ORIGINAL_TRIPLE_VERIFY_FAILED: 'The 2026-08-20 00:20 "triple-verified" was code + plan + README
    — THREE DOCUMENTS RESTATING ONE HYPOTHESIS, not three independent framings. It never tested whether
    the claimed gate could produce the observed symptom. It cannot: `already_below_target` returns INSIDE
    EVAL_TRANSITION and the machine immediately drops to HOLD_ONLY (energy_drain_precedence.py:858-861),
    so it can never REST in hold_pre_eval. The DP normally CYCLES HOLD_ONLY -> (1 tick) HOLD_PRE_EVAL
    -> (~eval_delay) EVAL_TRANSITION -> HOLD_ONLY, spending most wall-clock in hold_pre_eval. "state ==
    hold_pre_eval" therefore discriminates NOTHING — it is equally consistent with does_not_fit, l1_only,
    missing_inputs, blind_hold and force_charge_active. Lesson: an observation shared by every candidate
    mechanism is not evidence. (CLAUDE.md acceptance-criteria-must-discriminate corollary.)'
  L1_WAS_NOT_A_MISCLASSIFICATION: 'Second hypothesis (orchestrator''s own, that charger_rate_kw was fed
    by a dead/stale producer) ALSO REFUTED. The 1.4 kW was PHYSICALLY REAL: sensor.garage_a_power_minute_average
    (Emporia CT, URA''s actual producer via energy_pool.py:168-182 -> current_charging_load_w energy_pool.py:2286)
    read 1387-1416 W continuously with 60s updates and was NEVER unavailable; the INDEPENDENT SPAN breaker
    CT sensor.span_panel_car_charger_power read 1412-1440 W on a 5s cadence over the same window. Two
    unrelated physical meters agreeing to ~1.5% cannot both be faked by a stale producer. 1425 W / 240
    V = 5.9 A = an L2 charger pinned at its ~6 A minimum. Same sensor produces 6450/6390/5048 W hourly
    means on other days. ORCHESTRATOR MISREAD: I cited the "11 kW import vs 8.4-10.3 kW house load" gap
    as proof of a ~5 kW charger — the gap is 1-2.6 kW, which CORROBORATES 1.4 kW. Also red herrings: sensor.ura_energy_coordinator_ev_charge_rate_garage_{a,b}
    unavailable = deliberate tombstones from EV-SENSOR-CLEANUP-1 (energy.py:9748-9755), NOT a broken feed;
    binary_sensor...l1_charger_garage_a reads the MOES SMART PLUGS (energy_const.py:258), shares nothing
    with charger_rate_kw, does not feed the gate.'
  WHAT_SURVIVES: 'TWO real latent defects, both worth fixing, NEITHER causal for 2026-08-19/20: (1) the
    DP drain target IS mis-sourced — live-confirmed last_eval_snapshot.inputs.drain_target_soc = 80 while
    sensor.ura_energy_coordinator_battery_strategy.current_offpeak_drain_target = 10, two different numbers
    in one integration; (2) PLANNING_evse_drain_precedence.md never binds drain_target to ANY runtime
    source (unbound symbol, example "drain_target = 15") — a genuine Producer-check plan gap. NOTE from
    producer trace: the forecast target is NOT confined to BatteryStrategy as this card claimed — at energy.py:5842
    determine_battery_drain_actions is called with the forecast value as reserve_soc= (release floor)
    AND the static knob as soc_threshold= (pause threshold) SIDE BY SIDE in one call. That adjacency argues
    the DP''s use of the static knob is an oversight; it also means the fix is NOT a simple source swap,
    since the forecast value already holds a distinct role there.'
  COUNTERFACTUAL_FIX_MAY_BE_A_NOOP: Even with drain_target=10, gate 8 (the fit test, energy_drain_precedence.py:709-712)
    needs house_load >= ~4.5 kW at 00:25 with exactly ONE car plugged; with BOTH plugged the constraint
    fails at ANY house load. A sleeping house is plausibly 1.5-3 kW. So a target-only fix likely just
    advances the block from gate 7 to gate 8. Every fit-path test is pinned at 21:30 with house_load 5.91
    (quality/tests/test_evse_drain_precedence_session_b2a.py _mk_inputs), so NO test exercises a late-night
    start — which is why the suite never surfaced this.
  title: 'ROOT CAUSE (code-verified) — DP drain target MIS-SOURCED: drains toward static manual _ev_battery_drain_soc
    (80), NOT the forecast-based off-peak target (10) as designed'
  problem_solution:
  - 'DEFINITIVE (orchestrator read the code directly 2026-08-20, operator-confirmed leg-2 was built +
    should drain to the forecast off-peak target). FINDING: the drain-precedence machine sources its drain
    floor ENTIRELY from the static manual knob _ev_battery_drain_soc (=80) at energy.py:4456, :4522, :4540,
    :4555. The gate energy_drain_precedence.py:655-657 `if int(inputs.soc) <= int(inputs.drain_target_soc):
    _no_fit( ALREADY_BELOW_TARGET)` therefore compares SOC(37) to 80 -> 37<=80 -> never drains, never
    enters TRANSITIONED, never pauses EVSE, reserve stays pinned at 37 via evse_battery_hold.'
  - 'THE ACTUAL DEFECT (not a knob value): _ev_battery_drain_soc is written ONLY by the operator number
    entity (number.py:1482) + config seed (__init__.py:5724) — NOTHING sets it from the forecast. Meanwhile
    the forecast-keyed offpeak_drain_targets (excellent/good/moderate/poor) flows ONLY into BatteryStrategy
    (energy.py:257) where it correctly sets the reserve base (live_desire=10) — it is NEVER passed to
    the DP machine. So the DP re-eval IGNORES the forecast off-peak target and uses a static disconnected
    knob. This contradicts the operator design intent ("drain to the off-peak target which URA sets from
    next-day forecasts; 80 was meant as a charge-above threshold"). The built code has NO two-threshold
    charge-above / drain-to-offpeak logic — 80 IS the sole drain floor. MIS-SOURCED PRODUCER.'
  plan_readme_verified_2026_08_20: PLANNING_evse_drain_precedence.md Knobs table has NO drain-target knob;
    drain_target is an UNBOUND input symbol in D2/D3 (drain_hours=(soc-drain_target)...; example "drain_target=15").
    The plan pinned the CONSUMER (DP arithmetic) but NEVER the PRODUCER (drain_target source). READMEs
    (v5.20/5.21/5.71) also silent. So the build defaulted drain_target to the nearest pre-existing knob
    — the v4.7.6 drain-PAUSE SOC threshold _ev_battery_drain_soc (default 50, now 80), disconnected from
    the forecast. This is a PLAN under-specification (Producer-check gap) that plan-review missed, not
    a code error against a written spec.
  why: This is the real "battery not managed overnight". Leg-1 (hold) works; leg-2 (drain to floor) drains
    to the wrong target and so never fires at realistic overnight SOC. Forecast-adaptivity (the whole
    point) is absent from the DP path. Textbook Producer-AND-Consumer-check failure (operator rule 2026-08-16).
  constraints:
  - 'The stopgap (set knob to 10) works TONIGHT but is static, not forecast-adaptive, and conflates any
    intended charge-above role — a patch, not the fix. Real fix touches the DP state machine + cost behavior
    (regression-prone) -> Tier 2-DB/3, plan-reviewed. After the drain-target gate passes, the fits-check
    (energy_drain_precedence.py:709: drain+charge+margin <= hours_until_end_of_night AND start<=must_start_by)
    is the NEXT gate that must also pass.'
  refs:
  - custom_components/universal_room_automation/domain_coordinators/energy_drain_precedence.py (blocking
    gate :655-657; fits-check :709)
  - custom_components/universal_room_automation/energy.py (dp tick :4326-4540; drain_target :4456; pause+release
    :4511-4524)
  - custom_components/universal_room_automation/number.py (:1420-1482 setter)
  - project_ev_drain_precedence_cycle (memory)
  closed_note: 'ORGANIC-VALIDATED 2026-08-25 21:26-31 CDT: live DP exercise showed latch at current SOC(28)->re-eval->composed
    floor 15 written to hardware (cloud_oracle=15.0), never the static reserve 10. README_v5.90.1 organic
    table. done.'
  reopen_note_2026_08_25: 'REOPENED from a premature done. Post-watch conditions are NOT all met: (1)
    drains to composed floor 15 not static 10 — ORGANICALLY VALIDATED 21:26 CDT (latch 28 -> re-eval ->
    floor 15, cloud_oracle=15.0 hardware-confirmed). (2) INV-DP-DRAIN-1e 2-tick no-flap release on the
    None-target path (drain-window-end / TOU boundary) — NOT yet observed organically; it is mutation-test-anchored
    but unseen live. Hold shipped_organic until a None-release is observed with no adjacent-tick pause<->release
    flip-flop (next off_peak->exit or peak boundary). Then dispose to done.'
- id: BATTERY-RESERVE-CLOUD-ORACLE-FLAP-1
  thread: energy
  status: done
  approval: unreviewed
  created: 2026-08-20 00:00
  updated: '2026-08-29 13:20'
  disposition_2026_08_29: 'operator DECLINED 2026-08-29 — not pursued (board-button decline applied from pending-disposition queue). Card was already the misdiagnosis-corrected framing (37% reserve is the correct EV-hold, not a stuck write) so declining = close; nothing to build.'
  title: MISDIAGNOSIS CORRECTED — 37% reserve is CORRECT EV-hold, not a stuck write; real residual = cloud-oracle
    flap pollutes write-verify diagnostics
  problem_solution:
  - 'CORRECTION (deep end-to-end trace 2026-08-20, supersedes the earlier "stuck write" framing which
    was WRONG): the Envoy enforcing 37% reserve is WORKING-AS-DESIGNED. The EV is actively charging (~8.9
    kW grid import at midnight, solar 0, battery idle at floor) and URA''s `evse_battery_hold` deliberately
    pins the reserve to the current SOC (37%) so the home battery does not drain into the car. PROOF (live
    sensor.ura_energy_coordinator_battery_strategy): optimization_summary="Holding battery at 37% because
    EV is charging"; current_holds_active=[evse_battery_hold]; command_trail.reserve_soc.hold_owner="evse_battery_hold",
    live_desire=10, effective_desired=37. When charging stops the hold clears and reserve returns to 10.'
  - 'ROOT of the MISDIAGNOSIS: ENERGY_CLOUD_FIRST_WRITES=True (energy_const.py:458) -> URA writes the
    CLOUD ORACLE number.iq_battery_hacs_battery_reserve (=37, enforced by Envoy), NOT the local number.enpower_..._reserve_battery_level
    (=10, which URA never drives). Recorder 48h: envoy enforced reserve tracks the iq_battery_hacs oracle
    EXACTLY through every swing; the Enpower entity sat flat at 10. The orchestrator (me) read the un-driven
    Enpower entity and invented a stuck-write. Owned + corrected.'
  - 'REAL RESIDUAL (Tier 1 hotfix candidate): number.iq_battery_hacs_battery_reserve (the cloud oracle)
    flaps to unavailable/unknown several times a day; the write-verify watchdog opens pending_write_stuck
    episodes against a target that momentarily reads the other value during the blip -> produced the misleading
    "pending_reserve=10 stuck 2701s>900s" diagnostic that sent the investigation down the wrong path.
    This is diagnostic NOISE, not a reserve error.'
  why: 'The battery IS being managed correctly (EV precedence). The felt problem was LEGIBILITY: the operator
    (and the orchestrator) could not SEE why the reserve was 37 without a manual multi-hour trace, and
    the visible entity + the flap-polluted "pending stuck" diagnostic actively MISLED. This is an observability
    defect, not a control defect — same lesson as the arrester counters resetting on reload hiding 15
    real reverts.'
  constraints:
  - Do NOT touch the reserve-write path — there is no bug there. The only code change in scope is gating
    the write-verify pending/divergence machinery to skip ticks where the oracle entity is unavailable/unknown
    (energy_write_verify.py _compare / pending-watchdog; it already recognizes these states at energy_battery.py:777,789).
  next: 'TWO follow-ups: (1) POLICY DECISION (operator, no tier) — do you WANT the home battery to help
    power the EV (drain below current SOC while charging)? If yes that is a deliberate change to evse_battery_hold
    precedence = the PARKED "EV drain-precedence cycle" (project_ev_drain_precedence_cycle), NOT a bug.
    Surface the choice, do not code first. (2) TIER-1 HOTFIX — gate write-verify to skip unavailable/unknown
    oracle ticks so the cloud-oracle flap stops manufacturing false pending_write_stuck diagnostics.'
  refs:
  - custom_components/universal_room_automation/energy.py (evse_battery_hold :5604-5613, reserve max()
    :4650/4709-4744, write :7587-7603)
  - custom_components/universal_room_automation/energy_write_verify.py (pending-watchdog to gate)
  - project_ev_drain_precedence_cycle (memory — the parked policy cycle)
  - project_enphase_coupling_tier (memory)
- id: ROUTINE-CARE-DASHBOARD-1
  thread: presence
  status: pre_planning
  approval: unreviewed
  created: 2026-08-19 13:40
  updated: 2026-08-19 13:40
  title: '"Unusual for this person" routine care surface — DASHBOARD color signature, sensor-only (no
    notifications)'
  problem_solution:
  - 'DECISION (operator 2026-08-19, chose use D from the routine-detector menu): surface per-person routine
    health as a DASHBOARD with a COLOR SIGNATURE. Explicitly SENSOR-ONLY, NO notifications — reaffirms
    the original PLANNING_v4.6.1 non-goal ("sensor-only output"; notify default silent). The value is
    an at-a-glance "is anyone unusually off their own normal" care panel, not a nag.'
  why: Highest-value use of the routine signal (aging-in-place / wellbeing / "you have been off-routine
    N days"), and the lowest-risk delivery (display, no trust path, no notify). BUT a care signal that
    cries wolf is worse than none — so the color must be MEANINGFUL, which today it is not (always-red
    dead-letter, 331 unacked, INFO near-noise, everyone shifted for summer).
  constraints:
  - SENSOR + DASHBOARD ONLY. No notifications, no trust/actuation path (operator + original plan).
  - The RED "unusual" state MUST fire against the person's OWN stable baseline and be SUPPRESSED when
    the whole household shifted together (summer). Household-wide change = everyone amber, NOT everyone
    red. This per-person-vs-household disambiguation is the load-bearing deliverable — without it the
    panel is red-always.
  - Absence (Ziri at college / vacation-guard-suppressed) must render GREY "away", never RED — absence
    is not an anomaly.
  knobs: color-state thresholds live on the existing routine_regime_* Number entities (recalibrate, do
    not add parallel knobs) — verify at plan time.
  color_design_draft: GREEN steady (stable vs own baseline) · AMBER drifting (mild/household-wide sustained
    change — informational) · RED unusual (individual anomaly vs a STABLE personal baseline — rare, the
    care signal) · GREY away (absent / vacation-suppressed). Per-person tile shows top_movers as the human
    reason ("more time in Bedroom, less in Kitchen").
  next: 'Depends on ROUTINE-DETECTOR-NO-DISCHARGE-1 (the color is only meaningful once the signal can
    return to stable + is calibrated). Build shape (Tier 2-DB): D0 measure-before-build probe of the live
    JS/severity distribution + per-person-vs-household split; D1 discharge/dedup so cells clear; D2 recalibrate
    + household- shift disambiguation; D3 the color dashboard card. SCOPE FORK pending operator: proper
    (D0-D3) vs interim raw card now. Acceptance must DISCRIMINATE red-unusual from amber-household-shift.'
  refs:
  - ROUTINE-DETECTOR-NO-DISCHARGE-1
  - docs/planning/PLANNING_v4.6.1_anomaly_reconciliation_then_v4.6.2_routine_awareness.md
- id: ROUTINE-DETECTOR-NO-DISCHARGE-1
  thread: presence
  status: inbox
  approval: unreviewed
  created: 2026-08-19 13:15
  updated: 2026-08-19 13:15
  title: RegimeDetector math is faithful but the product fails its own acceptance criterion (no discharge,
    dead-letter ack, INFO near-noise, no consumer)
  problem_solution:
  - 'AUDIT (2026-08-19, code+plan vs live): the Bayesian routine-drift detector (RegimeDetector, JS-divergence,
    56d baseline / 14d recent) implements the plan (PLANNING_v4.6.1_anomaly_reconciliation..._routine_awareness.md)
    FAITHFULLY — every core param matches (regime_detector.py:25-83,229-247,459-479). The live shifts
    are DEFENSIBLE real signal (summer+kids-home, Oji travel, Ziri pre-departure) — not a bug. But it
    FAILS the plan''s own success criterion (line 345: "zero CRITICAL on a stable household after 4-6
    weeks; validate not false-positive"). Root cause is DOWNSTREAM of detection: (1) NO dedup/upsert —
    a still-shifted cell INSERTs a NEW unacked row EVERY nightly run (database.py:6671+), so 331 unacked
    accumulate monotonically; (2) the ONLY discharge is a manual button (button.py:1281) nobody presses
    in an autonomous-operator house — NM only notifies, never acks (notification_manager.py:5036); (3)
    NO re-baseline/regime-adoption path — a legitimately-changed world stays flagged until the 56d baseline
    slowly swallows it; (4) INFO tier (JS>=0.3 over 10 obs) is near-noise -> almost any sustained real
    change reads shifted. Net: a display-only signal (grep: NO trust/decision consumer — sensor.py + NM
    + button only) degraded into an always-red indicator conveying nothing actionable.'
  why: 'Directly ties to two operator rules: "suppression needs a discharge" (this event-driven accumulator
    has no discharge but a button) and README write-back (no README_v4.6.2 exists -> shipped without a
    validation ledger, so the false-positive warm-up the plan mandated was never recorded). The signal
    could be genuinely useful (resident-routine-change -> census/guest/departure) IF it were drained +
    consumed.'
  constraints:
  - Plan non-goal is "sensor-only output" — do NOT wire it to a trust/actuation path without an explicit
    new decision. First fix the discharge + calibration so the signal is trustworthy; THEN consider a
    consumer.
  next: 'MARGINAL-BENEFIT DECOMPOSITION before any build. Candidate fixes ranked: (1) dedup/upsert the
    anomaly row (stop nightly re-INSERT) — kills the 331 accumulation, small; (2) re-baseline/adopt discharge
    so a sustained new-normal clears the backlog (the real "discharge") ; (3) auto-ack/decay for an autonomous
    house; (4) recalibrate INFO threshold. Also: latent 3->5 level AnomalySeverity map trap (sensor.py:14010,
    dead 2->/3-> slots) + write the missing README_v4.6.2 validation ledger. Decide with operator which,
    if any.'
  refs:
  - docs/planning/PLANNING_v4.6.1_anomaly_reconciliation_then_v4.6.2_routine_awareness.md
  - custom_components/universal_room_automation/domain_coordinators/regime_detector.py
  - custom_components/universal_room_automation/database.py
  - ZIRI-COLLEGE-PERSISTENT-AWAY-1
- id: ZIRI-COLLEGE-PERSISTENT-AWAY-1
  thread: presence
  status: inbox
  approval: unreviewed
  created: 2026-08-19 13:00
  updated: 2026-08-19 13:00
  refinement_status: initial
  title: Ziri off to college — a resident is now persistently away (presence/census/schedule implications)
  problem_solution:
  - 'Context (operator 2026-08-19): Ziri has left for college and will become SPARSE — his tracker/BLE/face
    will legitimately be absent for long stretches. Implications to verify, not assume: (1) his stale/absent
    tracker must NOT generate anomalies or drag house-state; (2) census/guest logic must treat him as
    a known-away resident, never fold his absence into a guest/unidentified count; (3) does any schedule/
    Bayesian model treat his changed routine correctly, or silently as "away" (see investigation).'
  why: A resident transitioning to persistently-away is exactly the kind of routine change that can quietly
    poison presence heuristics (phantom guest, stale-tracker anomaly, wrong occupancy prior). Better to
    verify the seams now than debug a phantom later.
  constraints:
  - Read-only investigation first; no code change without a confirmed defect. Do NOT remove Ziri as a
    tracked person — he is a resident who returns (breaks, summers).
  - 'Pattern (operator 2026-08-19): mostly OUT, with occasional returns (may come back a bit before the
    semester kicks off; breaks). So the signal is INTERMITTENT presence on a mostly-away baseline, NOT
    a clean one-shot departure.'
  bayesian_finding_2026_08_19: 'VERIFIED (Explore + live): URA HAS a real per-person routine-drift detector
    (RegimeDetector, JS-divergence, nightly, regime_detector.py). Ziri routine_status is currently `shifted`
    (146 unacked events) — it caught the PRE-DEPARTURE room-mix change (recent: more in own Bedroom/Bathroom,
    less in Media/Breakfast Nook/Patio). GAP: a CLEAN persistent absence is SUPPRESSED by design — _is_vacation_cell()
    treats zero recent visits as a vacation proxy -> cell forced `stable`, no event (regime_detector.py:459-479);
    10-obs floor also nulls divergence. So as Ziri goes fully away the `shifted` status will REVERT to
    quiet and he is treated as away_typical. NO positive "resident departed / persistent absence" alert
    exists. INTERMITTENT-RETURN RISK (the operators pattern): each return week repopulates the recent
    window -> can RE-FIRE divergence -> noisy shifted<->stable flip-flops; and after a full semester the
    56d baseline decays toward away, so a break return could itself flag as a shift (inverse).'
  next: 'Investigate/verify the three live seams, then report: (a) does a weeks-stale Ziri tracker trip
    any anomaly/NM path; (b) HIGHEST-VALUE — does a resident returning after weeks away briefly ARM GUEST
    before BLE/face re-recognizes him (guest false-positive on a returning resident); (c) whether to add
    a "resident long-term-absence" state (distinct from vacation-suppress) so census/guest/notifications
    treat a known-away resident correctly without flip-flop. Marginal-benefit gate before speccing (c).'
  refs:
  - docs/planning/KANBAN.md
  - custom_components/universal_room_automation/domain_coordinators/regime_detector.py
  - custom_components/universal_room_automation/bayesian_predictor.py
- id: STEP-SHADOW-EVIDENCE-WATCH-1
  thread: diagnostics
  status: inbox
  ACTION_TAKEN_2026_08_20_TFLOOR_RAISED: 'OPERATOR: "Raise T_floor, re-observe briefly — Do it." DONE
    AND VERIFIED LIVE at 19:33 CDT. number.ura_chatter_t_floor 1.0 -> 5.0 via number.set_value; knob confirmed
    5.0; per-room chatter_telemetry now reports t_floor: 5 across every room (garage_a, living_room checked).
    K left at 10, CHATTER_OBSERVATION_WINDOW_S 300. MODE DELIBERATELY LEFT AT `shadow` per operator standing
    instruction ("Chatter mode is not on unless we find evidence of its success"). This turn produces
    the evidence; it does not act on it. PRE-WRITE SAFETY CHECK (done because the egress-camera write
    had just been caught as unsafe): CONF_CHATTER_T_FLOOR_S is in _NM_A2_KEYS (__init__.py:5686) which
    is spread into OPTIONS_RELOAD_SUPPRESS_KEYS (:5960), AND the CM options listener total-flushes the
    knob cache unconditionally on every CM options update (:6632-6647). So the write is in-place, no CM
    reload, no parent-entry cascade. Confirmed by the live attribute changing without a restart. ONE OBSERVATION
    NOTE: sub_floor_burst_count is still 0 immediately after the change and that is EXPECTED, not a failure
    — scoring is forward-only and the existing deque entries were recorded under the old 1.0s floor. The
    300s observation window means the counters self-clear quickly. The next burst on binary_sensor.garageopener_gdoblaq_wifi_garagea_motion
    is the first real test: at 2-3s cycling against a 5s floor, a burst like the 17:45-17:50 one (~44
    transitions) should produce sub_floor_burst_count well above K=10 and flip would_quarantine to true
    — WITHOUT acting, because mode is shadow. THAT is the success evidence to check before any `act` decision.
    NEXT CHECK: read chatter_telemetry on sensor.garage_a_unavailable_entities after the next garage-door
    activity. If would_quarantine goes true, the mechanism is vindicated and the gate decision becomes
    real. If it stays 0 through an observed burst, the defect is deeper than units and the card escalates.'
  ROOT_CAUSE_FOUND_2026_08_20_T_FLOOR_UNITS: 'MY "IT FOUND NOTHING, WRONG FAILURE MODE" VERDICT IS WRONG
    — WITHDRAWN. The operator pushed back ("Check the garage motion sensor. We did have legit reasons
    and sensor targets for this design") and the pushback was correct. The detector is neither broken
    nor aimed at the wrong pathology. It is MIS-SET BY ONE KNOB, and the knob is a UNITS problem. THE
    TARGET SENSOR IS CHATTERING VIOLENTLY, exactly as the operator remembered. binary_sensor.garageopener_gdoblaq_wifi_garagea_motion,
    12h history: 311 transitions in dense bursts. The 17:45:45 -> 17:50:58 burst alone is ~44 transitions
    — ON ~3s, OFF ~2s, repeating for five minutes straight. Thirteen separate bursts across the day (09:19,
    11:32, 11:45, 11:50, 12:27, 14:41, 14:59, 15:37, 16:07, 16:11, 17:13, 17:17, 17:45). This is a textbook
    chatterer and it is precisely what the mechanism was built to catch. WHY IT SCORES ZERO: the scoring
    test is `if interval < t_floor` where interval is in SECONDS (chatter_detector.py:518-520) and t_floor
    is in SECONDS (CONF_CHATTER_T_FLOOR_S / DEFAULT_CHATTER_T_FLOOR_S = 1.0, const.py:3847). The live
    operator override number.ura_chatter_t_floor = 1.0, so a transition only counts as an "impossibility
    event" if it happened LESS THAN ONE SECOND after the previous one. The garage sensor cycles at 2-3
    seconds. 2.0 > 1.0, so every single one of those 311 transitions is scored as legitimate. NOTHING
    IN A REAL HOUSE FLIPS FASTER THAN ONE SECOND. That is why the count is zero house-wide rather than
    merely low — the floor is set below the physical noise floor of the hardware, so the detector is structurally
    incapable of firing. And because _effective_t_floor_default() OVERRIDES the per-family defaults when
    set (chatter_detector.py:378-382), this single rung-2 knob flattens pir / mmwave / opener / reed all
    to 1.0s at once. Note the family defaults are ALSO all 1.0 (const.py:3848-3853), so this is a bad
    DEFAULT, not just a bad override — the knob is currently sitting exactly on it. THE FIX IS A KNOB
    TURN, NOT A CYCLE. Raise number.ura_chatter_t_floor to something above the hardware''s real re-arm
    time — 5s would make the garage burst score 44 sub-floor events against K=10 in a 300s window (CHATTER_OBSERVATION_WINDOW_S)
    and trip immediately. Recommend deriving the value from measured inter-transition intervals per family
    rather than guessing, since the families were collapsed to one number and probably should not be.
    CONSEQUENCE FOR THE FORCING GATE: the shadow period proved nothing either way — it was run with detection
    effectively disabled. Turning on `act` today would still act on an empty set; declaring it moot would
    discard a mechanism that has a real, live, well-matched target. The honest third option is: raise
    T_floor, re-observe in shadow for a short window, THEN decide. OPERATOR''S REGRET IS WELL-FOUNDED
    — "I wish we had checked before our last few restarts." Worth noting for the postmortem: the counters
    are in-memory and the observation window is only 300s, so restarts cost little here; the loss was
    the days of shadow, not the restarts.'
  HAND_CHECK_2026_08_20: 'SUPERSEDED IN ITS CONCLUSION by ROOT_CAUSE_FOUND above — the measurements below
    are accurate but my interpretation of them was wrong. ORIGINAL: OPERATOR AUTHORISED a hand check ahead
    of the 08-21 09:00 forcing gate ("already approved for us to turn it on if it proved its value...
    Did it find chatterers accurately? We built an elaborate mechanism for it"). RESULT: IT FOUND NOTHING.
    Not inaccurately — literally zero. EVIDENCE, read live from the per-room chatter_telemetry attribute
    across every room exposing it (game_room, garage_a, kitchen, living_room, master_bedroom, upstairs_guestroom,
    study_a): chatter_would_quarantine_count = 0 in EVERY room, and would_quarantine = false for EVERY
    individual sensor. sub_floor_burst_count is 0 or 1 everywhere against K = 10. Nothing is remotely
    near threshold. Knobs live: K = 10.0, T_floor = 1.0, mode = shadow. WHY THIS IS DAMNING RATHER THAN
    REASSURING — it scored ~0 on the sensors we INDEPENDENTLY KNOW are misbehaving, which is the discriminating
    test: * binary_sensor.mmwave_zigbee_studya_presence — a DIFFERENT detector logged duty-cycle-stuck
    on it 3,043 times in five hours. Chatter scores it transition_count 2, would_quarantine false. * binary_sensor.jaya_3_presence
    — same duty-cycle-stuck warning, 522 times. Not flagged. * The four corridor sensors behind HVAC-PRESET-FLAP-1
    (45-62 transitions/24h) and the Garage Hallway camera person sensor (89 cycles/24h) — none flagged.
    * The single highest transition_count found anywhere was 82 (binary_sensor.garageopener_gdoblaq_wifi_garagea_motion)
    and even that has sub_floor_burst_count 0. ROOT CAUSE OF THE MISS (hypothesis, stated as such): the
    metric counts BURSTS BELOW A ONE-MINUTE FLOOR and needs ten of them. This house''s actual sensor pathologies
    are (a) stuck-ON, which produces FEWER transitions not more — structurally invisible to a burst counter
    — and (b) slow chatter spread across hours, which never clusters under a 1-minute floor. The detector
    is well-built for a failure mode this house does not have. HONEST CAVEAT: there was a restart at 16:57
    today. If the counters reset then, these figures may cover only ~2 hours rather than the full shadow
    period, which would weaken the negative. The 82-transition reading argues the window is longer, but
    I have not proven it. What would settle it: read the same telemetry after a clean 24h with no restart,
    or check whether the detector persists counters across restart. RECOMMENDATION FOR THE 09-00 GATE:
    do NOT flip to `act`. Acting on an empty detection set changes nothing and would bank a false "shipped
    and working". The honest options are RECALIBRATE (lower K / raise T_floor so this house''s slow chatter
    registers, and add a stuck-ON arm — note the duty-cycle detector ALREADY does stuck-ON well, so the
    real question may be whether chatter should merge into it rather than sit beside it) or DECLARE MOOT.
    Operator decision.'
  approval: unreviewed
  created: 2026-08-19 12:00
  updated: 2026-08-19 12:00
  refinement_status: initial
  title: Check STEP shadow chatter evidence during nightly board maintenance (until 08-21 forcing gate)
  problem_solution:
  - 'Problem: STEP (v5.85.0) ships SHADOW-only; the 2026-08-21 flip-to-act/declare-moot decision is only
    as good as the evidence accumulated by then. Nothing re-raises that evidence between now and the gate
    except the scheduled cloud routine. Solution: a nightly read-only check folded into board maintenance
    that sweeps the shadow telemetry and logs a running tally, so the 08-21 call is not made on a surprise.'
  why: A broken producer (zero telemetry = detector not firing) or a false-positive (would_quarantine
    on a healthy sensor) must surface BEFORE the gate fires, not at it. This complements — does not replace
    — the cloud routine trig_01XZno8URQxUmuiNJczBjKaw; two independent re-raisers.
  constraints:
  - SHADOW read-only — do NOT flip the mode. Producer-health first — empty telemetry is a detector/wiring
    finding, not "no chatter."
  next: 'Nightly until 2026-08-21: read select.ura_chatter_mode (must stay shadow); sweep each room''s
    sensor.<room>_unavailable_entities chatter_telemetry for burst_count>0 and would_quarantine:true counts;
    scan URA logs for chatter DEBUG/NM + errors. Log a one-line tally here. On 08-21 this tally + the
    cloud gate feed the flip/moot call.'
  refs:
  - docs/readmes/README_v5.85.0.md
  - SENSOR-HEALTH-SURFACING-1
  - CHATTER-OBSERVE-CONTROL-D7-1
- id: ROADMAP-STALE-AGENTIC-LAYER-1
  thread: planning
  status: waiting_operator
  approval: unreviewed
  created: 2026-08-18 02:45
  updated: '2026-08-28 22:00'
  lane_note_2026_08_28: 'ROADMAP_v12.md now written (2026-08-28) — the roadmap-refresh half is discharged. What remains is operator green-light on scope/priority for the room-to-room AGENTIC layer, which v12 names as the next-MINOR-capability track. Hence waiting_operator.'
  refinement_status: initial
  title: Roadmap is stale (says v4.0.0 next; we are at v5.80.0) + the room-to-room agentic layer is unplanned
  problem_solution:
  - 'Problem: ROADMAP_v11.md (written at v3.22.0) says "Next: Bayesian Predictive Intelligence v4.0.0"
    but we are at v5.80.0 — ~2 major versions and dozens of cycles (energy arbitrage, guest/census, presence
    fusion) shipped WITHOUT updating the roadmap. And the operator-envisioned ROOM-TO-ROOM AGENTIC layer
    (rooms communicate + form agentic workflows on top of the now-shipped hierarchical-memory foundation)
    has NO vision/plan doc — only a movement-detection "TransitionCoordinator" concept exists in VISION_v7.
    Solution: (1) refresh the roadmap to reflect the real v4-v5 trajectory + current state; (2) if the
    room-to-room agentic layer is a priority, write its vision/epic doc (memory epic is its foundation).'
  why: A stale roadmap means new work is scoped without a current north star, and the operator vision
    (agentic rooms) has no plan to execute against — it will stay a passing mention until it is a document.
  constraints:
  - Docs/planning only. The agentic-layer vision needs OPERATOR input on scope/priority before a plan
    — do not spec it unilaterally.
  next: 'Operator: is the room-to-room agentic layer a near-term priority? If yes, scope a VISION/epic
    doc on top of the hierarchical-memory foundation. Separately: refresh ROADMAP_v11 -> v12 to reflect
    v4-v5 reality.'
  refs:
  - docs/ROADMAP_v11.md
  - docs/VISION_v7.md
  - docs/planning/ARCHITECTURE_hierarchical_memory.md
  - MEMORY-PROGRAM-EPIC
  audit_ledger_2026_08_18: 'AUDIT_roadmap_undone_worthwhile.md now provides the "already shipped" ledger
    for the roadmap rewrite: mark ROADMAP v9/v10/v11 + VISION_v7 + ROADMAP_REMAINING as HISTORICAL; most
    v3.22 "future" shipped under other names (arbitrage hardening->v5.5.x, guest actuation->v5.16.0, weather/inclement->v5.5.0,
    optimizer->its campaign, Bayesian B1/B2->v4.0.2). Undone-worthwhile carded: SENSOR-HEALTH-SURFACING-1,
    APPLIANCE-COST-DEFERRAL-1, UNLOAD-SYMMETRY-TASK-HYGIENE-1, CONFIG-SUBENTRIES-MIGRATION-1(parked),
    ENTITYDESC-RUNTIMEDATA-HYGIENE-1(parked).'
- id: MEMORY-ZONE-PHANTOM-WRITER-1
  thread: memory
  status: parked
  approval: unreviewed
  created: 2026-08-18 02:20
  updated: 2026-08-18 02:20
  refinement_status: initial
  title: 'Optional memory writer: zone_phantom (F2 zone-vs-house divergence has zero witnesses)'
  problem_solution:
  - 'Problem: nothing records when an HVAC zone reads occupied while the house is away (the F2 zone-vs-house
    divergence) — that tier has no memory witnesses at all. Solution: ONE additive writer zone_phantom,
    copy-adapt of AwayBlockEpisodeTracker (open/close coalescer + boot reconcile), memory-ineligible by
    construction, rate-bounded. Tier 2, 2 reviews. Knobs: ZONE_PHANTOM_WRITER_ENABLED, ZONE_PHANTOM_MIN_HOLD_S=300
    (rung 1).'
  why: Fills the only actionable memory-coverage gap. But there is NO active problem needing it today,
    so its marginal benefit is low until an F2-shape divergence recurs.
  constraints:
  - 'PARKED per marginal-benefit: build only when organic evidence of an F2 zone-vs-house divergence recurs.
    Do not build speculatively overnight.'
  next: 'REVISIT TRIGGER: a real F2 zone-occupied-while-house-away divergence is observed. Then build
    the writer (plan is PLANNING_memory_writers.md).'
  refs:
  - docs/planning/PLANNING_memory_writers.md
  - custom_components/universal_room_automation/memory_writers.py:186
- id: EXTERIOR-GUEST-FACE-FASTFOLLOW-1
  thread: presence
  status: shipped_organic
  shipped_version: v5.81.0
  approval: explicit
  created: 2026-08-18 00:55
  updated: 2026-08-18 02:25
  refinement_status: initial
  title: Face-identity arm for exterior->interior arrival — Protect Alarm Manager webhook -> HA -> family-room/garage
    named recognition
  approved_by: operator 2026-08-18 ("Build face independent... get and evaluate paths to build the face
    part now or later (fast follow)")
  problem_solution:
  - 'Problem: to know an UNKNOWN vs KNOWN person entered, we need face IDENTITY on the arrival path. The
    front-door cam sees no named faces, but people enter via the GARAGE into the FAMILY ROOM, where Protect
    DOES recognize residents by name — that identity just is not in HA (core unifiprotect exposes face
    as detection-only). Solution: wire Protect Alarm Manager Face-ID webhook -> an HA webhook trigger
    -> a ura_face_identified event (name + camera + time), then let URA consume it as a local face-identity
    producer for the interior-arrival identity signal. ~20 lines of automation, near-zero maintenance
    (per the spike).'
  why: 'Completes the "build both" intent: the face-independent arm (cycle 3) says SOMEONE arrived; this
    says WHO (known resident vs unknown), which is the actual guest/security discriminator.'
  constraints:
  - 'LOCAL sources only (no cloud). Probe-first: capture ONE real Alarm Manager face POST (webhook.site/HA
    trace) to confirm the name field + whether confidence is present, BEFORE wiring URA. Path ranking
    in RESEARCH_protect_face_to_ha.md: 4b webhook (recommended) > unifi-protect-bridge HACS > uiprotect
    direct.'
  - Re-measure coverage against GARAGE entries + family-room arrival, NOT the front door (the first probe
    measured the wrong cam).
  next: 'HOLD: consultation (duplicate-vs-complementary) running -> report to operator -> operator go
    -> then build.'
  refs:
  - docs/planning/RESEARCH_protect_face_to_ha.md
  - EXTERIOR-GUEST-EGRESS-1
  webhook_live_2026_08_18: 'OPERATOR 2026-08-18: the Protect Alarm Manager face webhook is LIVE. So the
    recommended path (4b) precondition is MET. Next probe-first step: capture ONE real face POST (HA webhook
    trace / the ura_face_identified event) to confirm the name field + confidence, then wire URA to consume
    it. The open-watch for the organic face-webhook payload is now the concrete capture step.'
  webhook_verified_2026_08_18: 'WEBHOOK RECEIVING SIDE VERIFIED: automation.ura_kp_face_webhook_probe
    (webhook_id ura_kp_face_probe, local-only POST) captures payloads verbatim -> ura_kp_face_probe_received
    event + system_log. Test POST returned 200 and logged the payload. PENDING (Wed, real face): real
    Alarm Manager payload shape + name/confidence fields + confirm Protect-side rule POSTs on a match.
    Probe is ready to capture the first real face event.'
  promoted_2026_08_18: 'PROMOTED to the PRIMARY cycle-3 build (operator: "let''s build it first and see").
    Reframe: we ALREADY have single-source identity via Frigate-2 face (sensor.<cam>_last_recognized_face_2,
    resolvable after v5.80.0 D2). Protect face (via the verified webhook) ADDS CORROBORATION (second source).
    So the build = cross-correlate Frigate-2 face + Protect face into a person-identity signal, tied to
    egress. PRE-BUILD GATE (operator): consult the existing egress-awareness + known-person (KP) code
    and CONFIRM this is COMPLEMENTARY not duplicating, and REPORT to the operator BEFORE building (AUDIT_egress_face_identity_prior_art.md
    running). No build until the operator reviews the verdict + gives go.'
  prebuild_consultation_2026_08_18: 'CONSULTATION DONE (AUDIT_egress_face_identity_prior_art.md, 1ff87322b)
    — VERDICT: COMPLEMENTARY, not duplicate. (1) EgressDirectionTracker resolves DIRECTION only; person_id
    hard-coded None at both emit sites (transit_validator.py:1106,:1121) — identity gap is real + by-design.
    (2) Downstream consumers ALREADY read person_id (defaults "unidentified", sensor.py:4190) — the slot
    is wired, just always None. (3) Face identity is read AD-HOC in census only (camera_census.py:2470,3283),
    no central producer — EXTEND those helpers, do not rewrite. (4) The Protect webhook event ura_kp_face_probe_received
    has ZERO URA consumers (dormant probe) — wiring it is net-new. (5) Three identity notions must stay
    DISTINCT: Frigate face, exterior sub_label (security), BLE room-presence. WIRE-IN SEAMS: S1 stamp
    person_id at :1106/:1121; S2 reuse census Frigate reader; S3 first consumer of ura_kp_face_probe_received;
    S4 downstream sensors unchanged; S5 fuse at census union camera_census.py:1855 with name dedup. COLLISION
    RISKS: (a) census additive/subtractive double-count — the 2026-08-17 GUEST-CENSUS CRIT precedent;
    integrate at the union NEVER a parallel count; (b) Frigate<->Protect same-crossing double-fire needs
    cross-platform dedup (mirror transit_validator.py:1063-1073); (c) the "remove after probe" automation
    becomes LOAD-BEARING once S3 wires it.'
  plan_2026_08_18: 'PLAN PLANNING_egress_face_identity.md — Tier 2-DB. SPLIT: D1 (ship now) Frigate person_id
    stamp (transit_validator.py:1106/:1121) + census union fuse (camera_census.py:1855) reusing existing
    face readers; D2 (gated on Wed payload) Protect corroboration, kill switch PROTECT_CORROBORATION_ENABLED
    default OFF, do NOT hard-code payload fields. Invariant I1: identified_count = |union of name sets|,
    never a sum. Plan review before build.'
  d2_gate_2026_08_18: 'D2 (Protect corroboration) gate: NO cron (operator: "don''t cron, just fire it
    yourself best you can"). The probe automation fires ura_kp_face_probe_received + logs the payload
    verbatim; the HA RECORDER durably retains that event+payload ~10 days, so no live-catch is needed.
    DURABLE GATE = THIS CARD: any session-start reconcile on/after Wed 2026-08-19 MUST run the D2-GATE
    check: query the recorder for a real face payload, and if present run the measure-before-build gate
    (append §D2-payload appendix to PLANNING_egress_face_identity.md -> GO build D2 / PARK if payload
    poor). Do NOT hard-code a guessed payload field (fabrication class). D1 itself is in fix-up (3 reviews
    DO-NOT-SHIP/SHIP-with-fix).'
  l1_validated_2026_08_18: 'SHIPPED v5.81.0 + LIVE. L1 PASS (boot clean, zero URA ERROR; switch OFF/dormant
    byte-identical — egress_identities_stamped=0, census as v5.80.0; observability attrs egress_face_ids_active
    + egress_identities_stamped live on persons_entered_today). L2/L3 ORGANIC-PENDING: Wed occupancy +
    flip CONF_EGRESS_IDENTITY_ENABLED=True, watch real person_id (URA slug) on entry crossing + no phantom
    guest on exit. D2 Protect corroboration = session-pickup gate (recorder holds payload).'
- id: CENSUS-FACE-MISS-WATCH-1
  thread: presence
  status: investigating
  approval: unreviewed
  created: 2026-08-18 00:34
  updated: '2026-08-29 13:20'
  disposition_2026_08_29: 'operator sent to INVESTIGATE 2026-08-29 (board-button investigate applied from pending-disposition queue). Discriminator remains: on occupancy, confirm face_lookup_missing_count DROPS when residents are present + recognized; if it stays high with recognized residents home, that is the real resolution gap (not the empty-house benign case already explained in interpretation_2026_08_18).'
  refinement_status: initial
  title: Census face-lookup misses ~12/tick on an empty house — investigate on occupancy
  problem_solution:
  - 'Problem: after the v5.80.0 D2 fresh-face fix, the census reports face_lookup_missing_count = 12 per
    tick even with the house EMPTY (no faces to look up). It fails CLOSED so the count stays correct (no
    wrong -1 credit), but 12 cameras'' face entities not resolving is more than expected and may mean
    the _2/registry resolution still misses some cameras (possibly the interior-cam naming inconsistency,
    or cameras with no face sensor being probed anyway).'
  - 'Solution: on occupancy (Wed), check WHICH cameras miss and why — is the face path probing cameras
    that have no face sensor (benign, make it not count them) or failing to resolve a face sensor that
    exists (a real resolution gap to fix)? Discriminate before acting.'
  why: The v5.80.0 fresh-face fix is supposed to REVIVE face dedup; a high miss rate could mean it only
    partially works. Not a correctness risk (fail-closed) but the fix's value depends on faces resolving.
  constraints:
  - 'WATCH only — do not chase now (operator: card investigations, march the cycles). Interpret on occupancy.'
  next: 'On occupancy: confirm the count DROPS when residents are recognized. If it stays high with recognized
    residents present, investigate resolution. Optional: split the counter (absent vs no-face-now).'
  refs:
  - docs/readmes/README_v5.80.0.md
  - reference_frigate1_retired_2suffix_permanent.md
  interpretation_2026_08_18: 'EXPLAINED: face_lookup_missing_count increments when a camera''s face sensor
    reads unavailable/unknown/empty/none = "NO recognized face right now" (camera_census.py:2502), NOT
    only when the entity is absent. On an EMPTY house no camera has a recognized face, so ~every checked
    camera counts -> 12 is EXPECTED and BENIGN (fail-closed correct: no face => no -1 credit). It does
    NOT indicate a naming/resolution bug. THE DISCRIMINATING TEST IS OCCUPANCY: if the count STAYS high
    while residents are home AND being face-recognized, THAT is a real resolution gap. Minor observability
    nit: the counter conflates "entity absent" with "no face now" — could split into two counters for
    a clean health signal. Downgraded from concern to expected-on-empty; re-check on occupancy.'
- id: FRIGATE-LEG-NAMING-1
  thread: security
  status: inbox
  approval: unreviewed
  title: Frigate live/dead leg naming is INCONSISTENT across cameras — bare _person_occupancy is live
    F2 on interior, dead F1 on perimeter
  created: 2026-08-18 02:05
  updated: 2026-08-18 02:25
  refinement_status: initial
  problem_solution:
  - 'Problem: after Frigate 1 was retired, the INTERIOR cameras were renamed (dead F1 -> _person_occupancy_f1retired,
    live F2 -> bare _person_occupancy) but the PERIMETER cameras were NOT (live F2 stayed _person_occupancy_2,
    dead F1 kept the bare name and froze on 08-13). So the SAME bare `_person_occupancy` name means the
    LIVE camera feed on some cameras and a DEAD frozen feed on others. Any code that identifies the live
    Frigate leg by name pattern will silently use the DEAD leg for one group.'
  - 'Solution: standardise the naming (rename perimeter to match interior, or vice versa) OR make URA
    resolve the live leg by recency/registry never by name. Verify URA''s resolver + the census/perimeter
    paths pick the LIVE leg for BOTH groups (on perimeter it currently subscribes the dead F1 corpse alongside
    live F2 = skews agreement telemetry).'
  why: A name that means opposite things on different cameras is a latent silent-failure generator — it
    already caused me to misread interior cams as "Protect-only" and underlies the census/perimeter corroboration
    work.
  constraints:
  - Renaming entities has blast radius (every consumer of the old name). Prefer recency/registry resolution
    in code if renaming is risky. Verify against the cycle-2 D2 face fix (does it pick the live leg for
    interior cams, whose binary leg is bare while their face leg is still _2?).
  next: 'LATENT LOW (no live bug). Future: make _prefer_canonical tiebreak by recency not name. No action
    now.'
  refs:
  - reference_frigate1_retired_2suffix_permanent.md (corrected)
  - camera_resolver.py:214
  - PERIMETER-PHANTOM-XCORR-1
  audit_verdict_2026_08_18: 'AUDIT (AUDIT_frigate_dead_leg_correctness.md): NO dead-leg reads. The dead
    F1 legs are FULLY REMOVED from the registry (404), not frozen-present — so the naming inconsistency
    causes NO live correctness bug. Every read site is registry/state-gated; an absent dead name -> None.
    Census SAFE (live leg both groups). Perimeter SAFE. DOWNGRADED to LATENT LOW: the only residual is
    _prefer_canonical (camera_resolver.py:335) preferring the BARE name over _N — a name-based liveness
    assumption. No collision today (dead leg gone). IF an F1 perimeter leg ever reappears alongside live
    _2, census would pin to the frozen leg. Fix (future, not urgent): tiebreak by last_updated recency,
    not canonical name.'
- id: PERIMETER-PHANTOM-XCORR-1
  title: Perimeter person alerts fire with no person in the snapshot, sent twice, and not cross-checked
    across NVRs
  thread: security
  status: inbox
  approval: unreviewed
  created: 2026-08-17 23:58
  updated: 2026-08-18 02:40
  refinement_status: refined
  problem_solution:
  - 'Problem: a single perimeter camera crying "person" pages at FULL severity (CRITICAL when the house
    is away) even when no other camera watching the area agreed — because the alert records which cameras
    fired but throws that agreement away when deciding how loud to be. Verified by hand: both 08-16 alerts
    were single-source with empty snapshots.'
  - 'Solution (Tier 3, NARROW remit per operator): make the already-recorded cross-camera/NVR agreement
    MATTER to the notification severity — a single-source detection DEMOTES (floor LOW, never silenced,
    so a lone-camera real threat still pages); corroborated detections keep/raise severity. Where a camera
    has only one NVR, fall back to the existing track-linker spatial confidence. Plus the dashboard work
    to surface corroboration / single-vs-multi source. NOTHING ELSE — not a perimeter-detection redesign.'
  why: 'Perimeter alerts are a security surface — false alarms train the operator to ignore them (alert
    fatigue), and a double-send doubles the noise. Cross-NVR correlation is the same principle the operator
    just mandated for cycle-3 face ID: never trust a single camera.'
  constraints:
  - 'SECURITY-relevant — treat false-negative risk carefully: a cross-check must SUPPRESS phantoms without
    dropping a real intruder seen by only one NVR (a real person may be in only one camera''s view). So
    cross-correlation raises CONFIDENCE / severity, it does not gate the alert to zero.'
  operator_note: 'Operator 2026-08-17: "either phantom detections or the snapshot was delayed. Are they
    properly cross correlated? I believe Camera <silver?> does this for free." (term "Camera silver" unclear
    — operator believes an NVR/product already does cross-correlation natively; needs clarification.)'
  evidence: '4 iMessage screenshots (IMG_6989-6992, 2026-08-16 alerts). Sensors: binary_sensor.hot_tub_person_occupancy_2
    + binary_sensor.front_side_ptz_person_occupancy_2 — note the _2 Frigate-2 migration suffix, same class
    as the census fix.'
  operator_reframe_2026_08_19: 'LIVE EVENT confirms the snapshot-delay branch of the 08-17 operator_note.
    Operator walked IN via the garage, got a g5_bullet_person_detected alert @12:09:57 whose SNAPSHOT
    (frame-stamped 12:09:52, ~5s before the alert) showed an EMPTY driveway — he was not in it. So "person
    + empty snapshot" is NOT necessarily a phantom; here it is a REAL person whose SNAPSHOT IS MIS-TIMED
    (sensor register + HA propagation lag -> snap grabs a frame after/before the person is in view). IMPLICATION:
    >=2 root causes (phantom vs real-person-late-snapshot). The scoped cross-NVR severity-demote would
    WRONGLY demote real late-snapshot events like this one. Operator: "hand examine this again." Fix direction
    may be snapshot PRE-ROLL / capture-at-detection-timestamp, not (only) corroboration.'
  next: 'HAND-EXAMINE FIRST (operator 2026-08-19) — measure detection->snapshot->alert lag on real events
    (g5_bullet on-time vs snapshot frame stamp vs alert-fire time; does the snap pull a live-at-fire frame
    or a buffered/pre-roll frame; does HA propagation delay move the person out of frame). THEN split
    the fix: (A) snapshot TIMING (pre-roll / at-detection-instant) for the real-person-missed case; (B)
    cross-NVR severity-demote for the genuine-phantom case. Do NOT ship (B) alone — it would suppress
    real events like the 12:09 garage walk-in. Prior scope kept: wire _sensor_engine agreement -> severity
    (single-source demote floor LOW, corroborated keep/raise; single-NVR -> track-linker confidence) +
    dashboard surfacing.'
  refs:
  - custom_components/universal_room_automation/perimeter_alert.py
  - EXTERIOR-GUEST-EGRESS-1 (cross-NVR theme)
  code_verified_2026_08_17: 'CODE CHECKED (perimeter_alert.py). FINDINGS: (1) Multi-leg logic (:449-558)
    is COVERAGE + DEDUP, not corroboration — it subscribes to every integration on a camera (Frigate base
    + _2 + Protect) and dedups them; legs are OR''d, so ANY single leg firing alerts. NO corroboration
    gate. This is CORRECT for the "don''t gate to zero" rule (a real intruder in one camera must still
    alert). (2) SEVERITY GAP (the real bug): severity = house_state × track_class × camera_class (:1131-1160).
    NO confidence/corroboration axis. A single-source phantom when AWAY fires at full CRITICAL — same
    as an all-NVRs-agree real threat. (3) The agreement data EXISTS but is DISCARDED: per-leg engine tags
    recorded as "disagreement telemetry" (:537-538, _sensor_engine) but never read by the severity resolver
    = Bug Class #53 computed-but-not-consumed. (4) The linker DOES escalate/demote (:1162-1187) but that
    is cross-CAMERA spatial (person moving across property), NOT cross-NVR (multi-integration agreement
    on ONE camera); and the demote requires camera_count>=2 (:1167), so a single-camera phantom is NEVER
    demoted.'
  tags:
  - tier-3
  tier: Tier 3 — narrow remit
  db_verified_2026_08_17: 'BY-HAND DB VERIFY (recorder, 2026-08-16 windows): BOTH alerts were DEFINITIVELY
    SINGLE-SOURCE. At 15:15:56 UTC ONLY binary_sensor.hot_tub_person_occupancy_2 fired — no other NVR.
    At 15:21:16 UTC ONLY front_side_ptz_person_occupancy_2 fired — no other NVR (garage_a fired 15:20:13,
    unrelated area). Positive proof of the single-source diagnosis. CONTRAST: a REAL event on 08-17 15:12-15:13
    shows front_side_ptz firing repeatedly ALONGSIDE master_hallway/upstairs_hall/staircase/garagehallway
    = multi-camera corroboration existed for a real person. So the machinery CAN corroborate; the 08-16
    alerts genuinely had none. NUANCE for the build: cross-NVR corroboration is only POSSIBLE where multiple
    NVRs overlap on a camera (e.g. the pool overhead 4 integrations). hot_tub and front_side_ptz appear
    single-NVR (Frigate-2 only) — for those the severity signal must fall back to the track-linker (spatial)
    confidence, since no NVR-agreement signal exists.'
  double_send_corrected_2026_08_17: 'CORRECTION (operator): the "double-send" is NOT a URA dedup bug.
    BlueBubbles texts the operator through his OWN number, so each message shows once as SENT and once
    as RECEIVED on the same thread = own-number loopback. URA sends ONCE. Fix is BB-side (dedicated/dummy
    Apple ID on the host Mac), not URA. Removed from URA scope.'
  remit_2026_08_17: 'OPERATOR SCOPE 2026-08-17: "Tier 3 work with a very narrow remit to make thrown away
    corroboration matter to notifications. And any dashboard work." So: (1) wire the existing _sensor_engine
    agreement telemetry (:537-538) into the severity resolver (:1131-1160) — demote single-source (floor
    LOW), keep/raise corroborated; (2) dashboard surfacing of corroboration state. Tier 3 because it is
    security-relevant, touches the NM/severity shared primitive, and threads a value into a decision site.
    Remit is NARROW — do not expand into detection tuning, street-masking, OSD, or friendly-names (those
    are separate small UX items, NOT this Tier-3 cycle).'
  corroboration_definition_correction_2026_08_17: 'CORRECTION (operator caught orchestrator over-claim).
    The 08-17 "multi-camera corroboration" claim was WRONG: front_side_ptz (exterior) co-fired with master_hallway/upstairs_hall/staircase/garagehallway
    (INTERIOR) — but those interior cameras are NOT in EXTERIOR_ADJACENCY_GRAPH (const.py:1773, 12 exterior
    cams only), so the linker CANNOT group them. They were INDEPENDENT co-occurring detections (interior
    movement + an exterior trigger), not a corroborated track. NOT a bad path grouping (the graph excludes
    interior cams) — but the orchestrator wrongly read co-occurrence as corroboration. PRECISE DEFINITION
    for the build: corroboration for a PERIMETER alert = (a) multi-NVR agreement on the SAME camera, OR
    (b) adjacent EXTERIOR camera confirmation via EXTERIOR_ADJACENCY_GRAPH. INTERIOR cameras are NEVER
    a corroboration source for a perimeter alert. Do not let a co-firing interior sensor raise a perimeter
    detection confidence.'
  dual_nvr_disagreement_proof_2026_08_17: 'CORRECTION + STRONG PROOF (operator: "Nah Unifi and frigate
    2"). hot_tub + front_side_ptz are DUAL-NVR, both legs LIVE: Protect (_person_detected, ~120-150 changes,
    last 02:50) + Frigate-2 (_person_occupancy_2, last 02:50). (Third leg _person_occupancy = Frigate-1
    base, DEAD since 08-13 16:37 F1 retirement — stale corpse leg.) URA SUBSCRIBES BOTH (camera_resolver.py:214-216
    _PERSON_SUFFIXES includes _person_occupancy AND _person_detected). ON 08-16 THE TWO NVRS DISAGREED:
    hot_tub Frigate-2 on 15:15:56 -> off 15:15:58 (2-SECOND blip), Protect NEVER fired; front Frigate-2
    on 15:21:16->15:22:05, Protect NEVER fired. So URA had the live disagreement (1 of 2 NVRs) and PAGED
    CRITICAL anyway — the _sensor_engine agreement telemetry was recorded and DISCARDED by the severity
    resolver. This is the exact Tier-3 gap with a concrete live proof case. NOT a coverage gap (both subscribed+live).
    RETIRES the earlier "single-NVR fallback" nuance for these cams — they are dual-NVR with a real agreement
    signal; the track-linker fallback only matters for GENUINELY single-NVR cameras. NOTE for build: a
    2-second single-NVR blip that the co-watching NVR refused to confirm is the textbook demote case.'
  agreement_rate_measured_2026_08_17: 'QUANTIFIED (front_side_ptz, 5 days): Frigate-2 (_person_occupancy_2)
    = 479 on-events; Protect (_person_detected) = 51 on-events. Agreement: 35 of 51 Protect events (69%)
    have a Frigate-2 within 60s. But from Frigate-2''s side, only ~35 of 479 (~7%) overlap Protect ->
    ~93% of Frigate-2 detections are SINGLE-SOURCE. READING: Protect is the conservative/accurate detector;
    when it fires, Frigate usually agrees. Frigate-2 emits a ~10x FLOOD of extra single-source detections
    Protect ignores (likely the public road / a tree / shadow in the Frigate detection zone). BOTH the
    08-16 alerts AND the 08-17 event were part of that 93% single-source Frigate flood; a genuinely corroborated
    event exists (the 35 both-fired cases) and those are the real-person events. IMPLICATIONS: (1) the
    corroboration-into-severity fix would demote the 93% Frigate-only flood, keep/raise the 35 corroborated;
    (2) SEPARATE operator config action: the front_side_ptz FRIGATE-2 detection is over-sensitive vs Protect
    — worth tuning its zone/threshold (probably includes the public street). The Protect leg is the trustworthy
    one here.'
  fleet_pattern_2026_08_18: 'FLEET SAMPLE (13 dual-leg cameras, 5d, Frigate-2/Protect on-event ratio):
    front_side_ptz is an ISOLATED OUTLIER at 11.4x (455 vs 40). Every other dual-leg camera is 0.56x-3.4x,
    and MOST have Protect firing MORE than Frigate (garage_a 0.56, front_door_aerial 0.59, doorbell 0.62,
    g5_bullet 0.65, upstairs_hall 0.73, madrone_g6_entry 0.77, playroom 0.82, rear_ptz 0.89). Mildly elevated:
    back_yard 3.4x, hot_tub 2.1x. CONCLUSION: NOT a systemic Frigate-over-trigger; front_side_ptz specifically
    is the problem — its Frigate zone almost certainly includes the PUBLIC ROAD (Protect leg on the same
    camera is tighter). ~91 Frigate events/day on that one camera = the bulk of phantom pages. FIX (separate
    from the URA severity work): mask the street / tighten the front_side_ptz Frigate detection zone.
    COVERAGE NOTE for the Tier-3 build: several interior cams are PROTECT-ONLY (family_room, foyer_fisheye,
    master_hallway, living_room, stairs_top — no Frigate-2 leg), so cross-NVR agreement cannot exist there;
    single-leg cams must fall back to track-linker confidence. Frigate-2-only: armcrest, reolinkstudybporchptz.'
  fleet_correction_2026_08_18: 'OPERATOR CORRECTIONS 2026-08-18: (1) "All unmasked at the moment" — the
    front-PTZ mask/zone hypothesis is WRONG; no camera is masked. front_side_ptz 11.4x over-trigger cause
    is UNKNOWN (candidate: PTZ MOVEMENT — a panning/zooming PTZ creates whole-frame apparent motion Frigate
    reads as person; Protect handles PTZ motion better — UNVERIFIED). Operator will REBOOT the front PTZ
    and re-check tomorrow. (2) "All cameras have at least Protect and Frigate-2" — my "interior cams are
    Protect-only" claim was WRONG: interior cams DO have live Frigate-2, named bare _person_occupancy
    (not _2) — see FRIGATE-LEG-NAMING-1. Corrected interior ratios (F2/Protect, 5d): family_room 436/1331=0.33,
    master_hallway 555/1312=0.42 — Protect fires MORE, consistent with the fleet. front_side_ptz remains
    the SOLE outlier at 11.4x. Cross-NVR corroboration IS possible on interior cams (both legs live).'
  dead_leg_claim_retracted_2026_08_18: 'RETRACTION: earlier notes said the dead Frigate-1 bare leg is
    a "stale corpse leg counted as coverage" that skews leg-agreement telemetry. WRONG — the audit found
    the dead F1 legs are REMOVED from the registry, so resolve_detection_legs cannot return them as a
    leg. The corroboration telemetry is NOT polluted by a dead leg. The Tier-3 severity gap (agreement
    recorded but not consumed) still stands; only the dead-leg-inflation sub-claim is void.'
  ptz_rebooted_2026_08_18: Operator REBOOTED the front PTZ 2026-08-18 (~02:40). Re-check the front_side_ptz
    Frigate-2/Protect ratio (was 11.4x, sole fleet outlier) after the reboot — if it drops toward the
    fleet norm (~0.5-1x), the over-trigger was a PTZ state/motion artifact cleared by reboot; if it stays
    11x, it is a persistent Frigate zone/config issue on that camera. Do the re-check tomorrow, not tonight.
- id: DOC-DRIFT-ZONE-AWAY-1
  title: Coordinator manuals stale vs code on zone-away / away-veto (PRESENCE_COORDINATOR.md badly stale)
  thread: docs
  status: done
  approval: unreviewed
  origin:
    date: '2026-08-17'
    gist: Operator asked to check manuals against code for house-zone vs HVAC-zone away; AUDIT_zone_away_house_vs_hvac.md
      (7f54068fb) found 4 drifts.
  problem_solution:
  - P1 PRESENCE_COORDINATOR.md away-veto section (:1233) documents ONLY the v4.7.14 predicate — missing
    D8 (face_recognized_count/census_count clauses), the ENTIRE Path β LOST-admitted veto, and the LOST
    six-state matrix; infer() pseudocode (:285-300) no longer resembles code. This is the doc a future
    session consults about away logic — it would actively mislead. S1 rewrite the away-veto + matrix sections
    against current presence.py:1059-1208 + the matrix classifier.
  - P2 HVAC_MANAGEMENT_EXPLAINER.md:34 preset-map ref stale (:303 -> :780) + stale "guest planned v4.7.x"
    note. S2 fix refs.
  - P3 HVAC_COORDINATOR_MANUAL.md §3.1 omits the per-zone vacancy override (hvac.py:1544-1554) forcing
    away preset while house home. S3 add it.
  - 'P4 ABSENCE: no manual documents (a) the ZonePresenceTracker three-tier OR (BLE->sensor->camera) as
    the house-zone away mechanism, nor (b) that HVAC-zone occupancy = OR of room-level occupied bools
    (hvac_zones.py:146/546), NOT the zone tracker. S4 document both (the house-vs-HVAC divergence source).'
  why: These manuals are the institutional-context surface reviewers and planners consult. Stale away-veto
    docs directly caused wasted cycles this session (assumptions about away logic had to be re-derived
    from code). Keeping them current is cheap insurance against re-litigation.
  constraints:
  - Docs-only, no code. Verify every rewritten claim against file:line (the drift was caused by docs NOT
    tracking code — do not reintroduce drift).
  next: DONE (673e91b07). PRESENCE_COORDINATOR.md away-veto + LOST-matrix + house-zone OR rewritten; HVAC
    explainer preset-map ref + HVAC manual §3.1 vacancy override + HVAC-zone-occupancy-source gaps fixed.
    Orchestrator spot-check confirmed path α "ignores room sensors" vs base "hard gate" — asymmetry correct.
  refs:
  - docs/planning/AUDIT_zone_away_house_vs_hvac.md (7f54068fb)
  - docs/Coordinator/PRESENCE_COORDINATOR.md
  - docs/Coordinator/HVAC_COORDINATOR_MANUAL.md
  updated: 2026-08-17 23:40
- id: GUEST-ROOM-CONFIG-1
  title: 'Guest-room designation was wrong: a BATHROOM was flagged is_guest_room (unflagged 2026-08-17)'
  thread: presence
  status: done
  shipped_version: v5.79.0
  approval: explicit
  approved_by: operator 2026-08-17 ("Findlay down guest bathroom. Misconfiguration" / "Unflag")
  origin:
    date: '2026-08-17'
    gist: Operator flagged the Down Guest Bathroom designation as a misconfiguration while reviewing which
      rooms should carry the guest role.
  problem_solution:
  - 'Problem: a downstairs guest BATHROOM was marked as a "guest room". Because the check that excludes
    known residents was broken, ANY 30-minute stay in that bathroom would have flipped the whole house
    into guest mode — and a bathroom gets used often, including at night.'
  - 'Solution: unmark the bathroom. The only rooms that can trigger guest mode are now the two actual
    guest bedrooms. (Details and the flow-API gotcha are in the card notes and refs.)'
  why: 'Config correctness, and a prerequisite for D2 being safe: D2 makes the guest-room gate the SOLE
    arm for GUEST, so the designated set becomes load-bearing.'
  constraints:
  - 'INCIDENT NOTE (orchestrator, disclose don''t bury): the FIRST exploratory options-flow call submitted
    basic_setup with schema defaults and thereby triggered the ROOM-NAME-DESYNC write-through, RENAMING
    the entry from "Down Guest Bathroom" to "Guest Bedroom 1 Bathroom" and materialising data->options.
    Values preserved and the rename arguably fixes a title/room_name desync, but it was an unintended
    live-config mutation from a flow I had not read first. config_flow.py:9193 should have been read BEFORE
    calling, not after. No-Fabrication applies to APIs, not just to code claims.'
  - 'API GOTCHA worth remembering: URA''s basic_setup options step signals SUCCESS via async_abort(reason="reconfigure_successful")
    — deliberately, so the update listener does not fire twice (config_flow.py:9205-9209). The MCP tool
    surfaces that as an ERROR. The naive reaction is to retry, and retrying a write that already succeeded
    is how config gets double-applied. Partial input IS safe: the step does merged_options = {**entry.options,
    **user_input}.'
  next: 'None — done. Verified live: designated set = Guest Bedroom 1, Upstairs Guestroom.'
  refs:
  - custom_components/universal_room_automation/config_flow.py:9193
  - custom_components/universal_room_automation/const.py:386
  created: 2026-08-17 21:20
  updated: 2026-08-17 23:30
  refinement_status: initial
- id: GUEST-ROOM-LOCATION-MATCH-1
  title: 'Precondition for designating a guest room: person location must match CONF_ROOM_NAME (D2-INFO-2)'
  thread: presence
  status: inbox
  approval: unreviewed
  origin:
    date: '2026-08-17'
    gist: Review D2 (guest_census_review_D2_completeness.md) INFO-2, surfaced re-reviewing the repaired
      oracle.
  problem_solution:
  - P1 `_is_known_person_in_room` excludes a resident by comparing person_coord.data[name]["location"]
    against CONF_ROOM_NAME. `_resolve_person_room` no-mapping fallback returns the raw bermuda_area string.
    If a room is flagged is_guest_room=True but its residents route through that fallback, location !=
    CONF_ROOM_NAME, the exclusion silently returns False, and GUEST arms on a resident after 30 min —
    the exact failure this cycle fixes, re-opened by config. S1 before designating ANY guest room, verify
    a known person standing in it resolves location == that room name (not a bermuda_area). This is an
    OPERATOR PRECONDITION, not a code fix.
  why: Under D2 the room set is load-bearing; a designation whose location vocabulary does not match makes
    the sole safety check inert for that room.
  constraints:
  - Inert today (fix verified for the currently-designated rooms whose locations DO match). Becomes live
    only on a new/mismatched designation.
  next: 'VERIFIED PASSING (pre-deploy check 2026-08-17): live person locations are CONF_ROOM_NAME verbatim
    ("Garage A", "Breakfast Nook"); both designated rooms map correctly (guest_bedroom_2->"Guest Bedroom
    2", bedroom->"Guest Bedroom 1"). The exclusion is live for the current set. ONE residual: a live in-guest-room
    confirmation (a resident actually standing in one) is not possible with the house empty — organic
    re-check on occupancy (Wed). Otherwise closable.'
  refs:
  - docs/reviews/code-review/guest_census_review_D2_completeness.md
  - custom_components/universal_room_automation/domain_coordinators/presence.py
  updated: 2026-08-17 23:25
- id: GUEST-IDENTITY-PHONE-LEFT-BEHIND-1
  title: Guest-room identity exclusion is BLE-only, so a resident without their phone arms GUEST after
    30 min
  thread: presence
  status: inbox
  approval: unreviewed
  origin:
    date: '2026-08-17'
    gist: Confirmed by the oracle-fix builder on orchestrator instruction to report-not-fix; surfaced
      when the operator noted "The guest room presence known person exclusion via BLE is crucial and good
      finding".
  problem_solution:
  - P1 `_is_known_person_in_room` reads person_coord.data[name]["location"], which is Bermuda BLE-sourced
    (person_coordinator.py:471-541, 1036-1096). A resident physically IN the guest room whose phone is
    elsewhere — or who carries no BLE beacon — resolves to location != room_name, so the exclusion returns
    False and GUEST arms on a resident after 30 min. The exclusion inherits the phone-left-behind failure
    mode wholesale. S1 (candidate, NOT a plan) cross-check room occupancy / the existing phone-left-behind
    machinery before concluding "unknown occupant".
  why: Under D2 this exclusion is the SOLE safety check between guest-room occupancy and GUEST mode, so
    its blind spots are guest-mode false positives. The GUEST_KNOWN_STICKY_S=120 latch shipped in the
    oracle fix absorbs BLE FLAP but does nothing for a phone that is simply elsewhere — different failure,
    not covered.
  constraints:
  - 'Prior art EXISTS and is NOT reachable from this path: v5.78.0 Gap-B wired phone-left-behind detection
    to room occupancy, and PersonPhoneLeftBehindSensor exists. The new helper does a raw single-substrate
    lookup with no cross-check. Verify both surfaces before designing — do not build a third mechanism.'
  - Deliberately scoped OUT of the guest-census cycle to avoid scope creep; report-only was the instruction.
  next: 'After the guest-census cycle ships and the repaired oracle has organic evidence: quantify first
    — how often does a resident occupy a designated guest room while BLE places them elsewhere? Measure
    before designing.'
  refs:
  - custom_components/universal_room_automation/domain_coordinators/presence.py
  - docs/readmes/README_v5.78.0.md
- id: STUCK-SENSOR-1
  title: Flapping mmWave evades stuck-exclusion; fix via corroboration-gated exclusion at the ROOM tier
  thread: presence
  status: shipped_organic
  shipped_version: v5.75.0
  approval: explicit
  approved_by: 'operator 2026-08-09 ("and what comes after it") — the chatter/reliability cycle that follows
    SENSOR-CAPABILITY-1, absorbing the mmwave-Tier3 D6 parked deliverable. NOTE: this approval does NOT
    extend past the SignalTrustLedger''s own GO criteria — its criterion 4 (golden-tap fixtures) is unmet
    and is a hard blocker independent of approval.'
  tags:
  - tier-2db
  - no-fabrication-verify
  - context-wide-scoping
  origin:
    date: '2026-08-09'
    gist: operator diagnosed a stuck Zigbee mmWave holding master occupancy; asked why I did not see it
  why: 'URA''s duty-cycle detector DOES catch stuck sensors and logs: ''Sensor <x> duty-cycle stuck (on-ratio
    exceeded over rolling window) — NOTIFY-ONLY, not excluded from occupancy''. It then KEEPS USING the
    stuck sensor for occupancy. Detection without consequence. Tonight that produced: master bedroom reported
    occupied while its inputs read off -> comfort fans ran in an empty bedroom (operator not home); Garage
    B occupied off a chattering ratgdo; Living Room and Study A both flagged stuck. Occupancy feeds HVAC,
    lighting and presence trust, so the blast radius is wide.'
  my_miss: 'I READ these exact warnings hours earlier and dismissed them as ''routine, not a crash'' —
    I was scanning for crashes, so a WARNING that was not a crash read as noise. The message named its
    own defect and I skimmed it. Rule: a warning that says STUCK is never routine; read content, not severity
    class.'
  gaps:
  - '1. NO CONSEQUENCE: stuck sensors are not excluded from the occupancy substrate.'
  - '2. NO SURFACE: the only signal is a WARNING log line — no sensor, no NM note, no dashboard. Invisible
    unless a human is tailing logs at the right moment.'
  - '3. NO PERSISTENCE: like the echo counter, any stuck-state tally resets on restart, and we restarted
    7+ times today.'
  fix:
  - 1. Graduate D2 to exclusion behind the house-state gate the code always planned ('exclusion graduates
    in a later cycle behind a house-state gate'). The sleeping-person objection holds for bedrooms in
    sleep/home_night; it does NOT hold when the house is away.
  - 2. Propagate stuck-sensor state to zone + house tiers — today they cannot see it at all.
  - '3. Fix the independence assumption: correlated stuck sensors must count as ONE confirmation source,
    not N. Two occupied rooms whose sensors are both flagged stuck is one signal.'
  parsimony:
    problem: a stuck sensor silently fabricates occupancy and drives fans/HVAC/lighting in empty rooms
    verdict: BUILD
    note: detection already exists; this is wiring a consequence + a surface to it, not new inference
  next: 'BLOCKED on SENSOR-CAPABILITY-1 — do not scope exclusion until capability/role are separated,
    else the corroborator must be hardcoded as PIR (the defect). Then: per-room corroborator capability
    map from AUDIT_mmwave_only_rooms_2026-07-31.md; six no-PIR rooms need a non-PIR corroborator or stay
    notify-only.'
  depends_on:
  - SENSOR-CAPABILITY-1
  CORRECTION: '2026-08-09: my first version of this card was WRONG on three counts — I asserted mechanism
    from a LOG STRING instead of reading the code (operator: ''a log line does not reality make''). Corrected
    by verification: (1) it is NOT ''detection without consequence'' — the CONTINUOUS rule DOES exclude
    (coordinator.py:1960-1967, comment ''Continuous rule DOES exclude''); (2) D2 duty-cycle being NOTIFY-ONLY
    is a DELIBERATE, well-reasoned choice, not an oversight — ''a sleeping person is ~100% mmWave duty
    cycle with zero PIR, and excluding would vacate sleeping bedrooms''; (3) it is NOT invisible — both
    rules fire an NM note via _fire_stuck_sensor_nm with per-day dedup.'
  verified_findings:
  - 'ROOM TIER: continuous rule excludes but keys off _sensor_on_since, which a FLAPPING sensor resets
    each off-tick -> flapping never accumulates -> never excluded (coordinator.py:1960-1967).'
  - 'D2 duty-cycle catches flapping but is deliberately NOTIFY-ONLY: ''a sleeping person is ~100% mmWave
    duty cycle with zero PIR, and excluding would vacate sleeping bedrooms''. Sound reasoning, not an
    oversight.'
  - Both rules DO fire an NM note (_fire_stuck_sensor_nm, per-day dedup) — this is not invisible.
  - The D2 comment names a PRIOR 'Master Bedroom empty-suite incident' — tonight is a repeat of a known
    failure mode.
  - ZONE (aggregation.py) and HOUSE (presence.py) have no stuck-sensor awareness — verified by grep.
  RETRACTION_2: '2026-08-09 (second correction, operator challenge ''the stuck sensors are not correlated
    afaik''): I claimed the stuck sensors were a CORRELATED failure (same vendor/firmware/night) and built
    a ''falsified independence assumption'' + ''closed loop'' argument on it. VERIFIED FALSE: Living Room
    = Screek Human_Presence_Sensor via esphome (direct); Study A = HOBEIAN 24Ghz via mqtt (through a coordinator).
    Different manufacturer, model, transport, topology. I inferred correlation from CO-OCCURRENCE IN A
    LOG WINDOW alone. Co-occurrence is not correlation. RETRACTED: the house-tier ''2+ rooms occupied
    = unlikely all stuck'' assumption is NOT falsified and stands; the closed-loop claim collapses with
    it.'
  solution:
  - DISCRIMINATOR IS CORROBORATION, NOT HOUSE STATE. House state fails exactly in the hard case (asleep
    during home_night). A sleeping person pins the mmWave AND a corroborator; a stuck mmWave pins only
    itself.
  - 'Exclude a duty-cycle-stuck sensor when NO independent corroborator supports occupancy: bed presence,
    recent PIR, BLE person in room, camera person. D2 already uses PIR corroboration to guard DETECTION
    — extend the same principle to EXCLUSION.'
  - 'Tonight this would have released master immediately: bed presence read OFF while occupancy read ON.'
  - 'CAVEAT: rooms whose ONLY input is a single mmWave have no corroborator and cannot be discriminated
    — for those, notify-only remains correct. This is a per-room capability question, not a global policy.'
  DEDUPE_2026_08_09: 'Chatter/motion-chatter arrived as a candidate NEW card and was folded in here instead
    (adjacency check per kanban dedupe rule): same detector, same exclusion decision, same corroboration
    discriminator. No CHATTER-1 card exists or should.'
  third_class_chatter:
  - 'CLASS 3 = CHATTER (transition-rate). A sensor oscillating at ~50% duty is invisible to BOTH shipped
    rules: every off-tick resets P22 continuous-on, and the on-ratio never reaches D2 85%. Evidence: Garage
    B ratgdo 24h = 3,769 off / 3,765 on / 6 unavailable — real oscillation, not transport.'
  - 'MOTION IS UNSCORED BY DESIGN: D2 candidates are mmwave_sensors + occupancy_sensors only (PIR excluded
    as "our corroboration source"). So a chattering PIR is never diagnosed AND it SHIELDS a stuck mmWave
    — D2 corroboration is satisfied by >=2 PIR transitions in-window, which chatter trivially supplies.
    The anchor can be the thing that is broken. Operator: ratgdo has no PIR; it supervises MyQ garage
    motion, and Zigbee/Z2M can make that flicker or go unavailable.'
  - 'UNREACHABLE ROOMS: Garage B has mmwave_sensors=None, occupancy_sensors=[] -> D2 candidate set is
    empty there. The room where the incident happened is outside the detector regardless of thresholds.'
  shipped_context:
  - D1-D4 ALL SHIPPED v5.35.0 (commit 0192ac2c3, 2026-07-28 23:18 CDT) + v5.35.1 hotfix + v5.35.2 observability.
    Do not re-scope them.
  - 'The shared abstraction that shipped is the NOTIFIER (_stuck_signal_nm, D4), NOT a shared detector:
    "Detection + discount + notify ONLY... never actuates, never mutates detector state". Detection is
    four bespoke impls sharing only a kind= string.'
  - 'The plan PARKED unification with an explicit trigger: "after D1..D4 ship and the shape is proven
    at four sites; separate design cycle." That trigger HAS NOW FIRED (12 days, four live sites).'
  approach: 'PATCH A CENTRAL METHOD, NOT A FIFTH DETECTOR (operator 2026-08-09: "should not do this in
    isolation or it should be a patch of a central class/method" + "do the right thing unified or not").
    Generalise _detect_duty_cycle_stuck into a per-sensor reliability scorer over ALL binary sensor kinds
    scoring on-ratio + transition-rate + unavailable-rate, emitting kind= per class through the existing
    D4 notifier. Do NOT force the census (person COUNT) and frozen-tracker (timestamp AGE) detectors into
    it — different shapes; they keep their own detection and keep sharing D4. Honest scope: one central
    scorer for binary sensors, not one function for everything.'
  FLEET_ROT_RETRACTION: '2026-08-09 (third correction, operator challenge "I don''t think a third of the
    fleet is bad"): I claimed a third of the corroborator fleet was dead, generalising B-2026-08-04-2''
    "5 of 13" — which is 5 of a 13-entry HAND-PICKED ADJACENCY LIST from one probe, not 5/13 of the fleet.
    MEASURED: 503 presence-class binary_sensors, 30 unavailable/unknown = 6.0%, clustering into ~15 physical
    devices. RETRACTED: the "fleet rot gates corroboration logic" argument. STANDS: a 5-unit HOBEIAN cluster
    is down together (hypothesis, not asserted mechanism) — ops item, not a gate.'
  D0_AUDIT_DONE: '2026-08-09: docs/planning/AUDIT_mmwave_only_rooms_2026-07-31.md written (was owed since
    07-31 as D0 of the mmwave Tier-3 cycle and never done; cycle shipped without it). Key results: 5 MMWAVE_ONLY
    rooms + Master Bedroom MMWAVE_NO_PIR = SIX rooms with zero PIR, so D2 can detect but NEVER corroborate
    there (corroborator class empty by config) — this is the structural cause of both the 08-09 incident
    and B-2026-08-04-1 spam. Master bed sensor sits in occupancy_sensors = it is a D2 CANDIDATE, so the
    ideal discriminator is being judged instead of consulted. Study A has an ORPHAN mmwave_sensors key
    no code reads (CONF_MMWAVE_SENSORS == "presence_sensors"), holding an unavailable Athom entity.'
  OPERATOR_DISPOSITIONS_2026_08_09:
  - 'ATHOM (Study A): CLOSED as a no-op. Operator: "Ignore athom now. It''s a no op." URA already does
    not read it (options override every bucket); the ESPHome device entry stays. Do NOT re-raise, do NOT
    propose deleting the entry or cleaning the orphan mmwave_sensors key.'
  - 'HOBEIAN 5-unit cluster: PARKED until operator is home. Operator: "Leave hobeian until I get home.
    It''s mostly z2m hygiene." Not a URA defect and not a gate on any cycle — it is Zigbee2MQTT housekeeping,
    operator-owned, physical. Do NOT re-raise as a blocker on corroboration work; the measured fleet-bad
    rate is 6.0% and does not gate the cycle.'
  rejected:
  - PROPAGATE STUCK STATE TO ZONE/HOUSE — I recommended this and then withdrew it under challenge ('is
    this flow upwards useful?'). If the room tier excludes correctly the corrected occupancy propagates
    naturally and upper tiers are right for free. Propagation adds a cross-tier concept, new state and
    new failure modes to fix something better fixed at source. Only justified as a FALLBACK where the
    room tier genuinely cannot discriminate and upper tiers should down-weight.
  amendment_approved_2026_08_13: 'Operator approved the criterion-4 amendment (offline replay harness
    + hand-built supplement; runtime tap DROPPED — recorded in PLANNING_signal_trust_ledger_abstraction.md).
    Unblock path ACTIVE: build/ledger-golden-replay harness in flight -> then this cycle builds.'
  harness_landed_2026_08_13: 'Replay harness MERGED (fe9bfc845): P22 (13/5) + D2 (56/3, boot-settle unmodelled
    -> discount vs audit 13) FILLED from replay. Remaining criterion-4 work before this cycle builds:
    hand-built supplements for P24/P18/D1/CHATTER (operator sign-off required per amendment) + adjudicate
    D3 bucket (its frozen-tracker detector was DELETED 2026-08-10 — bucket likely obsolete, minimums need
    a plan edit) + P18 NM-ledger ingestion follow-up. Hand-building the supplements is orchestrator work,
    queued behind the circling/QoL wave.'
  approved_2026_08_13: 'OPERATOR APPROVED (during AWAY-BLOCK-1 incident review: "Stuck sensor is approved").
    Spec confirmed to operator: it ACTUATES (corroboration-gated exclusion at room tier), not notify-only.
    Taxonomy caution from the incident: fan-latch is TRUE motion mis-attributed, not sensor pathology
    — verify during plan/build that the exclusion gate keys off the duty FLAG (which fired here) and composes
    with fan-aware demotion, or the build fixes stuck-class only. Remaining blocker: criterion-4 hand-built
    fixture supplements (orchestrator queued, pulled forward) + operator sign-off on supplements + D3-bucket
    obsolescence adjudication.'
  build_dispatched_2026_08_13: 'Plan rev-2 (plan review: HIGH corroborator-window-subsumed -> 900s + still-person
    test; P22 restore-poisoning boot guard; fixture emit-only-when-True + replay pre-deploy gate; merged-accessor
    pin). Build in flight (worktree). Criterion-4 CLOSED same day (signed supplements merged).'
  program_unification_2026_08_18: 'PROGRAM UNIFICATION (operator 2026-08-18): chatter, stuck-on, and flapping-mmWave
    are ASPECTS of ONE sensor-trust/exclusion program — a shared ROOM-TIER "untrust a sensor vote / exclude
    from occupancy fusion" primitive with multiple DETECTORS on top. Detectors differ by gate: CHATTER
    = physics-based, quarantine-always, NO corroboration -> NOT blocked on SENSOR-CAPABILITY-1; STUCK/flapping-mmWave
    = corroboration-gated -> blocked on SENSOR-CAPABILITY-1. Shared primitive = D1; chatter detector can
    land independently; stuck detector sequences after capability work. Do NOT build parallel exclusions.
    Members: SENSOR-HEALTH-SURFACING-1 (chatter), STUCK-SENSOR-1 (stuck/flapping mmWave), SENSOR-CAPABILITY-1
    (capability/role -> corroborator selection).'
  updated: 2026-08-18 16:10
  program: sensor-trust-exclusion
- id: SENSOR-CAPABILITY-1
  RESOLVED_2026_08_23_mislabel_cleared: 'OPERATOR RULING APPLIED — "Move sensor capabilities and to motion
    then." The Garage Hallway declaration tagging the Zigbee RGBW motion sensor as kind camera_presence
    is GONE, verified in the live config entry. HOW: driven through the real options flow (sensors step),
    NOT by editing .storage — a direct file edit would have been clobbered by HA in-memory entry state.
    WHY THE DECLARATION DISAPPEARED RATHER THAN CHANGING VALUE, and this is the correct outcome: the entity
    is wired into motion_sensors, so motion IS its CONF-derived default kind. Per config_flow.py:9677
    a selection equal to the default is a NO-OP and the override is dropped. The sensor now derives as
    motion from its wiring, with no stored declaration that can drift from it — exactly the capability-vs-role
    separation this card exists to enforce. VERIFIED SURGICAL: full options backup taken first (39 lines);
    post-change diff shows exactly one removed block and nothing else. 28 option keys -> 27, sensor lists
    byte-identical. CAVEAT WORTH KEEPING: the flow warned it IGNORED mmwave_sensors as not declared in
    the sensors step schema. It was None before and after so no harm here, but that field is not settable
    through this step — relevant to any future room-wiring change.'
  INSTANCE_2026_08_23_live_mislabel_garage_hallway: 'A LIVE CAPABILITY MISLABEL, still present in the
    running config as of 2026-08-23. Carried here deliberately because SENSCAP-ORPHAN-1 (where it was
    first noted as an unactioned aside) has now CLOSED, and closing it would have taken this with it.
    Garage Hallway sensor_capabilities declares: binary_sensor.rgbw_motion_lux_3rdr_zigbee_garagehallway_occupancy_2
    -> kind "camera_presence" That entity is a ZIGBEE RGBW motion/lux sensor. It is not a camera. This
    is exactly the capability-vs-role confusion this card exists to separate — the kind field is being
    used as a config bucket rather than as a statement about the hardware. NOT FIXED UNILATERALLY, and
    that is deliberate: changing a capability kind changes how the sensor is weighted in presence fusion,
    and the operator has twice corrected my sensor-classification assumptions (most recently "It is a
    pir class mmWave. It is correct."). A silent reclassification of a live presence input is not a config
    tidy-up. NEEDS AN OPERATOR RULING on the correct kind. WHY IT IS NOT URGENT: camera_presence is not
    in _NON_CORROBORATOR_KINDS, so the D-MEDIUM-1 rule does not currently exclude it — the mislabel is
    not silently dropping the sensor from fusion today. It is wrong, not broken.'
  title: Separate sensor CAPABILITY (hardware kind) from analytic ROLE — kind is currently the config
    bucket
  thread: presence
  status: shipped_organic
  shipped_version: v5.65.0
  approval: explicit
  approved_by: operator 2026-08-09 ("Approving sensor capability plan and what comes after it")
  tags:
  - tier-3
  - institutional-context
  - no-fabrication-verify
  - context-wide-scoping
  - numbers-get-knobs
  origin:
    date: '2026-08-09'
    gist: 'operator ruling on whether bed presence moves bucket or code changes: "My instinct is code
      change so we don''t have fixed config buckets. Sensor reality should not pin use and analysis reality
      in software. It should just tell us what the hardware layer is. We could also add more options or
      detail to the kind of sensor in config"'
  why: 'VERIFIED: occupancy_substrate.py:81 _KIND_TO_CONF maps kind 1:1 onto the three CONF lists, and
    const.py:342 TIER1_KINDS = ("motion","mmwave","occupancy"). URA has exactly three sensor kinds and
    they ARE the three config buckets, so the hardware wiring declaration and the analytic role are one
    field. A bed sensor cannot declare itself a bed — it inherits "occupancy" (const.py:335 comment: "Combined
    motion+presence sensors", an ambiguity bucket), and that membership is the ONLY reason it is a D2
    stuck-candidate rather than a corroborator.'
  root_cause_of:
  - 'Master Bedroom: the ideal discriminator (bed presence) is JUDGED instead of CONSULTED.'
  - Six no-PIR rooms have no corroborator NOT because they lack independent evidence but because "corroborator"
    is hardcoded to mean the motion bucket. Game Room, Jaya Bedroom, Living Room, Study A, Study B (MMWAVE_ONLY)
    + Master Bedroom (MMWAVE_NO_PIR).
  - B-2026-08-04-1 overnight notification spam (beds/bedroom mmWave flagged for being state-normal ON).
  unlocks_without_new_hardware:
  - 'Master Bedroom already HAS an ideal corroborator: the bed — independent failure mode, physically
    unspoofable — the moment role stops being pinned to the motion bucket.'
  - Study A room_cameras + BLE become nameable corroborators for the mmWave-only class.
  design:
  - KEEP the three CONF lists as the WIRING layer — no config migration, additive only.
  - 'ADD a per-entity capability descriptor (kind, trust, failure-mode class), inferred from device/integration
    where possible, operator-declarable where genuinely ambiguous (operator: "add more options or detail
    to the kind of sensor in config"). Default = today''s bucket-derived value so behaviour is byte-identical
    until a capability is declared.'
  - 'DERIVE role per question asked: candidate-for-stuck-scoring vs corroborator-for-independence vs creator-vs-extender
    for occupancy. Role is computed, never stored in config.'
  - 'Same cut as the intent/evidence axis (PLANNING_paper_and_oss_fusion_library.md sec 7/7b): bed = strong
    evidence, mmWave = weak witness. Neither is expressible today.'
  parsimony:
    problem: hardware wiring pins analytic role, so the best available corroborator in a room cannot be
      used as one
    verdict: BUILD
    note: prerequisite, not an enhancement — corroboration-gated exclusion on the current vocabulary MUST
      hardcode PIR as corroborator, which is the defect itself
  blocks:
  - STUCK-SENSOR-1
  sibling_of:
  - SIGNAL-TRUST-LEDGER (build-gated)
  depends_on: []
  build_2026_08_09: 'BUILT (worktree commit 141e60939) then REBASED onto current develop as c82290f68
    on branch sensor-cap-rebase. STALE-BASE INCIDENT: the builder worktree was based on 57ba22942 (v5.50.2),
    214 commits behind develop, so its green suite (19f/8125p) described a codebase that no longer exists.
    Cherry-pick onto develop was CLEAN (no conflicts) and _detect_duty_cycle_stuck — the riskiest refactor
    target — was byte-unchanged between base and develop, so a rebase sufficed rather than a redo. Re-validated
    on the real baseline: 21 failed / 8517 passed / 45 skipped / 2 xfailed (develop baseline 21f/8494p;
    +23 = the new tests; failing NAMES unchanged; the A-2/A-3 strict xfails survived). Skill updated with
    a base-verification rule.'
  I1_DEVIATION_for_reviewers: 'The builder KNOWINGLY deviated from byte-identity in one place and flagged
    it: for an entity in BOTH mmwave_sensors and occupancy_sensors (the P15 defensive case), pre-migration
    list-concat DOUBLE-SCORES it (same ring appended twice per tick), which reaches the MIN_TICKS=20 warm-up
    floor in half the ticks. The build dedups to score it exactly once, per the plan wording. This is
    a real behaviour change on that path and therefore a partial I1 violation. Reviewer B must adjudicate:
    take the fix, or preserve the bug for strict byte-identity.'
  build_notes:
  - 'A mutation drill initially PASSED, exposing dead code: the strong_evidence gate was unreachable for
    kind=bed (bed is not in _STUCK_CANDIDATE_KINDS, so it exits earlier). The builder added a test for
    the reachable path (operator declares an mmwave-wired entity as strong_evidence) and re-ran to a named
    failure. The drill protocol caught a hollow anchor before review — working as intended.'
  - D4 uses a JSON textarea (TextSelector multiline) because HA has no dict selector. Pragmatic v1.
  - 'Unverified claim for reviewer B: mid-flight capability override during refresh_subscriptions is untested;
    the builder argues it is safe because the capability layer is not consumed inside the substrate and
    _detect_duty_cycle_stuck re-reads room_config every tick.'
  TIER3_REVIEW_ROUND_2026_08_09:
    A: 'SHIP w/ fix-ups — HIGH-A1 (validator accepted a capability with no kind, silently no-opping the
      cycle headline use case), MED-A2 (failure_mode unvalidated). Verified by reading: _CONF_PRECEDENCE
      matches occupancy_substrate._KIND_PRECEDENCE byte-for-byte; resolve_role total across 7x3x4x3 with
      no silent None; TIER1_CAPABILITIES superset ENFORCED by construction not comment.'
    B: 'SHIP conditional — adjudicated the flagged I1 deviation as TAKE THE FIX (substrate already normalises
      multi-list entities by the same precedence and WARNs; pre-diff double-scoring reached MIN_TICKS
      in half the ticks on half the window). B-MED-1: the carve-out is the whole P15 surface, not just
      mmwave x occupancy. Verified the builder unverified claim: CONF_SENSOR_CAPABILITIES is NOT in OPTIONS_RELOAD_SUPPRESS_KEYS
      so a save triggers a standard room reload; full TIER1_KINDS consumer enumeration clean, no half-migrated
      reader.'
    C: 'SHIP — 13 per-site drills, 3 came back GREEN=untested. C-MED-1 is the 8th hollow-anchor shape:
      wiping the ENTIRE effective_corroborators construction left the suite green, so D3 load-bearing
      claim was unpinned. C also proved the I3 RUNTIME ward has teeth (widening TIER1_KINDS reddens 5
      tests) while the docstring ward is advisory only — both honest about scope.'
    D: 'DO-NOT-SHIP — D-HIGH-1 (I1 violated for hybrid entities under empty overrides) + D-MEDIUM-1 (the
      narrowing question answered YES: an override can evict an entity from BOTH loops).'
  ORCHESTRATOR_ADJUDICATION: 'B/C/D disagreed on ONE line. Resolved by reading source: _STUCK_CANDIDATE_KINDS
    = {mmwave, occupancy} excludes motion (sensor_role.py:73) and _CONF_PRECEDENCE resolves a motion+mmwave
    entity to MOTION, so CANDIDATE_FOR_STUCK returns False on the KIND CHECK ALONE — independent of _seen_corr.
    C drill 13 (delete the branch, suite green) proves it inert. So D-HIGH-1 is a REAL behaviour change
    but MIS-ATTRIBUTED, and BOTH of D''s proposed fixes would have been NO-OPS. Real cause = precedence-aware
    role resolution, which B correctly judged correct on merits. Disposition: keep the behaviour, DELETE
    the dead branch and its FALSE comment, document the real carve-out. Confirmed independently by the
    builder drill 6 (precedence inversion reddens both P15 tests via double-scoring — a different mechanism
    than the dedup set).'
  FIXUP_2026_08_09: 'bed359d5d, ff-merged onto sensor-cap-rebase. Fixed HIGH-A1, MED-A2, D-MEDIUM-1 (validator
    now rejects an override that would leave a motion-wired entity in neither loop), C-MED-1 (byte-identity
    anchor + a behavioural test driving production so bed elevation actually reaches the corroboration
    deque), C-LOW-2/D-HIGH-1 (dead branch + false comment deleted), B-MED-1 (planning section 10.1 rewritten
    + motion x mmwave and motion x occupancy analogue tests), the misfiled-hybrid collision WARN, and
    D-LOW-2. Deferred: A-LOW-3/4, A-NIT-5, C-LOW-1, B-LOW-1/2.'
  ORCHESTRATOR_VERIFIED_2026_08_09: 'Did NOT trust the reports (Tier-3 mandate). Personally: ff-merged;
    confirmed the dead branch and false comment are gone (0 occurrences); ran my OWN mutation on the load-bearing
    site (_CONF_PRECEDENCE inverted, py_compile clean) -> 7 NAMED failures incl. the byte-identity anchor
    and both new P15 analogue tests; restored and confirmed git status clean; full suite 21 failed / 8525
    passed / 45 skipped / 2 xfailed (baseline 21/8517, +8 = the new tests, failing NAMES unchanged).'
  AWAITING: TIER-3 OPERATOR CHECKPOINT BEFORE DEPLOY (mandatory per CLAUDE.md). Not merged to develop.
  known_gap: The misfiled-hybrid collision WARN has no dedicated test asserting the emit path (it fired
    incidentally in a drill). Would need _LOGGER mocking or caplog scaffolding on the coordinator logger.
    Say the word and it gets one.
  next: PLAN WRITTEN 2026-08-09 (docs/planning/PLANNING_sensor_capability_vs_role.md, 477 lines). Tier
    3, four framing-disjoint reviews, operator checkpoint before deploy. AWAITING OPERATOR GO — Tier 3
    shared primitive, not implied-approval eligible.
  status_note: plan complete; build gated on operator go
  refinement:
  - 'CHALLENGE (planner, vs my card sketch): the sketch read as if capability might extend TIER1_KINDS
    and the substrate DISPATCH payload. SHARPENED: keep the substrate channel TIER1_KINDS-valued and run
    capability at the CONSUMPTION site instead. Extending TIER1_KINDS touches every consumer AND trips
    _audit_provenance_invariants (presence.py:384) which RAISES on unknown keys — blast radius O(n) per
    added kind. Consumption-site resolution bounds it to O(1). This is the insight that makes the cycle
    tractable; my sketch would have made it a rewrite.'
  - 'CHALLENGE: my sketch said "inferred from device/integration where possible". SHARPENED: NO auto-inference
    in v1 — operator-declarable only, with an evidence trigger to revisit. Rationale: manufacturer/integration
    sniffing is a rare-fire path with poor observability, and the Bug Class #46 history is lazy defaults
    mis-classifying silently. Note this lands closer to the operator''s own words ("we could also add
    more options or detail to the kind of sensor in config") than my sketch did.'
  - 'CHALLENGE: "kind, trust, failure-mode" as free-form fields. SHARPENED: each constrained to a small
    enumerated vocabulary (TRUST_CLASS_*, FAILURE_MODE_* at rung 1); open strings would defeat the role-query
    matrix''s testability.'
  invariant: I1 — with no CONF_SENSOR_CAPABILITIES declared anywhere, get_all_room_kinds, every SIGNAL_SUBSTRATE_KIND_CHANGED
    dispatch, _room_provenance, _detect_duty_cycle_stuck's return set and every exposed entity attribute
    are BYTE-IDENTICAL to today. I2 roles never persisted across a tick. I3 capability MUST NOT leak into
    the legacy provenance channel.
  riskiest: '_detect_duty_cycle_stuck migration. Its positional signature (motion, mmwave, occupancy)
    is itself a legacy reification of _KIND_TO_CONF. An entity present in BOTH mmwave_sensors AND occupancy_sensors
    (the P15 defensive case, occupancy_substrate.py:214-222) must be scored EXACTLY ONCE by resolve_role,
    matching today''s concatenation semantics. Reviewer C must mutation-cover that collision. Second:
    a future builder adding "bed" to _audit_provenance_invariants''s allowlist would silently undo I3
    — the audit docstring must state the vocabulary is closed by design.'
  refs:
  - docs/planning/AUDIT_mmwave_only_rooms_2026-07-31.md (Finding 6 — root cause)
  - docs/planning/PLANNING_mmwave_corroboration_tier3.md (Amendment 4)
  - docs/planning/PLANNING_signal_trust_ledger_abstraction.md (Addendum 2026-08-09 — ledger assumed this
    layer)
  - custom_components/universal_room_automation/domain_coordinators/occupancy_substrate.py:81
  - custom_components/universal_room_automation/const.py:335,342
  DEDUPE_2026_08_09: 'Four-surface adjacency sweep run before creating this card. Board: adjacent to STUCK-SENSOR-1
    (which it now BLOCKS) — kept separate because the shared surface is OccupancySubstrate/TIER1_KINDS,
    not the stuck detector, and STUCK-SENSOR-1 cannot be built correctly first. BACKLOG.md: B-2026-08-04-1
    is downstream (its memory-baseline exemption still applies per-room after this lands); B-2026-08-04-2
    unrelated (ops). Parked planning-doc deliverables: mmwave-Tier3 D4 (unrelated, still parked), D6 (absorbed
    into chatter work, not here). Catalogs: CATALOG P15 substrate kind-precedence is the primitive being
    changed — extend, do not roll new. NEW, not duplicate.'
  program_unification_2026_08_18: 'PROGRAM UNIFICATION (operator 2026-08-18): chatter, stuck-on, and flapping-mmWave
    are ASPECTS of ONE sensor-trust/exclusion program — a shared ROOM-TIER "untrust a sensor vote / exclude
    from occupancy fusion" primitive with multiple DETECTORS on top. Detectors differ by gate: CHATTER
    = physics-based, quarantine-always, NO corroboration -> NOT blocked on SENSOR-CAPABILITY-1; STUCK/flapping-mmWave
    = corroboration-gated -> blocked on SENSOR-CAPABILITY-1. Shared primitive = D1; chatter detector can
    land independently; stuck detector sequences after capability work. Do NOT build parallel exclusions.
    Members: SENSOR-HEALTH-SURFACING-1 (chatter), STUCK-SENSOR-1 (stuck/flapping mmWave), SENSOR-CAPABILITY-1
    (capability/role -> corroborator selection).'
  updated: 2026-08-23 11:50
  program: sensor-trust-exclusion
- id: BOARD-CURRENCY-1
  title: Forcing-function ladder so the board (and vibememo) cannot lag shipped work
  thread: process
  status: done
  approval: explicit
  approved_by: operator 2026-08-09
  tags:
  - numbers-get-knobs
  - institutional-context
  origin:
    date: '2026-08-09'
    gist: 'operator on the stale board: "A banner is not a forcing function. Is there a harder one? A
      kanban that does not keep current is fairly useless" -> then "yes deploy gate with softer backups
      as well (the other 2 or 3). We should also chain to Vibememo"'
  why: 'Board reconciliation is the ONLY step in the deploy ritual with no forcing function. deploy.sh
    refuses without tests and without a README; NOTHING refuses without a board update, so it is the only
    step running on willpower — and it rotted twice (2026-08-09: board said "build" for XCORR-1 and SNAP-1,
    both already shipped as v5.64.0/v5.63.0). meta.last_reconciled correctly read 2026-08-07 and nothing
    surfaced it. Root cause is NOT a missing rule (the skill carries three hooks covering it).'
  constraints:
  - A failed board/vibememo write MUST NOT abort a deploy that has already pushed — trading a stale board
    for a half-released version is strictly worse. Write AFTER the push succeeds, warn loudly on failure,
    never exit non-zero post-push.
  - deploy.sh is URA-only (CLAUDE.md). Do not co-opt it for Shipwatch or other projects.
  - Every escape hatch must be explicit and logged (--no-cards), never a silent bypass.
  ladder:
  - 'RUNG 1 (HARD — the forcing function): deploy.sh gains --cards ID[,ID...]. REFUSES to deploy when
    absent, printing current in_progress/review cards as candidates. --no-cards escape for pure-docs releases.
    On success it WRITES status: shipped_organic + shipped_version: v<version> for each named card and
    meta.last_reconciled: <today>, committed as part of the release commit. The board update becomes an
    OUTPUT of shipping, not a task.'
  - 'RUNG 2 (HARD, same gate): chain vibememo. The same --cards gate requires/emits a vibememo entry for
    the release (the WHY of the ship), so the decision trail cannot lag either. vibememo = WHY, kanban
    = WHAT/WHERE/NEXT; both are release-coupled from here.'
  - 'RUNG 3 (soft backup): KHOST-1 generator renders a loud STALE banner + warns on build when meta.last_reconciled
    is older than the newest git tag or newest README_v*.md.'
  - 'RUNG 4 (soft backup): session-start staleness check (skill enforcement #2, already exists) — compare
    meta.last_reconciled to git log before reporting status.'
  - 'RUNG 5 (soft backup): recurring overnight agentic pass reconciles the board as its first action before
    picking up overnight-agentic work. WIRED 2026-08-09 as recurring job 0e0703fc, daily 01:23.'
  RUNG5_DURABILITY_GAP: 'The 01:23 job is SESSION-ONLY — in-memory, dies when the Claude session exits,
    and auto-expires after 7 days. So it does NOT yet fully solve the KHOST-1 miss: if the session ends
    before 01:23, the overnight pass silently does not happen — the SAME failure mode as the original
    promise, just with a longer fuse. A durable fix needs a scheduler OUTSIDE the session: launchd on
    the operator Mac or a homelab cron that starts a fresh headless session. Until that exists, rung 5
    is best-effort and must not be relied on; rungs 1-2 (release-coupled, in-repo) are the load-bearing
    ones. Operator decision needed on where the durable scheduler lives.'
  scope_note: 'Rung 1 hardens only the SHIPPED transition. pre_planning->planned->in_progress stays soft
    (turn-end hook). Deliberate: every card found stale on 2026-08-09 was shipped work the board still
    called "build" — the rot is concentrated exactly where rung 1 bites. If in-flight statuses prove to
    rot too, add a second mechanism ON EVIDENCE, not by guess.'
  parsimony:
    problem: the board silently lags shipped work, so picking "next" off it can rebuild already-shipped
      features
    verdict: BUILD
    note: simplest version (banner) was REJECTED by operator as not a forcing function; the marginal cost
      of rung 1 over the banner is one gate in a script that already has one (deploy.sh:32-36)
  knobs: none new (uses existing meta.last_reconciled)
  next: 'DONE. Organic proof LANDED 2026-08-17: the post-push write path executed for real for the first
    time on the v5.79.0 deploy — kanban_ship marked 2 cards shipped + vibememo_ship wrote entry 055 automatically.
    The forcing function is proven end-to-end.'
  review_record_2026_08_09: 'Reviewed DO-NOT-SHIP -> fixed -> re-verified. H1 YAML reflow: safe_dump round-trip
    rewrote the real board 1296->1455 lines, re-wrapping every card''s prose at 80 cols. Replaced with
    a textual line-anchored writer (parse to VALIDATE, edit as TEXT) — NOT ruamel, which is not installed
    and would have put a pip dependency in the release path. Orchestrator independently re-verified on
    the real board: 1323->1324 lines, 2 hunks, 3 lines changed, all 28 other cards byte-identical, YAML
    valid, card count preserved. H2: --dry-run was a command echo, so step 4b would have debuted in production;
    it now runs the REAL kanban_ship + vibememo_ship against a mktemp copy and prints the diff. M1 (--cards
    swallowing a flag), M2 (gitea mirror for the reconcile commit), M3 (--no-verify so a hook flake cannot
    leave the board dirty and block the next deploy), L1/L2/L5 fixed. L3/L4 accepted.'
  residual_fragility:
  - 'A card whose status: line is quoted (status: "planned") would not match the writer regex — the card
    is skipped with a WARN rather than silently mis-written. No such card exists today; worth a lint if
    quoting ever starts.'
  - No behavioral in-suite test for vibememo_ship.py (kanban side has 14). The --repo-root flag is exercised
    only by the dry-run rehearsal; a bug in default root resolution would not be caught.
  meta_note: The first card this gate marks shipped will most likely be itself.
  refs:
  - docs/planning/PLANNING_v4.7.10_deploy_sh_gitea_retrofit.md (precedent for modifying deploy.sh)
  - scripts/deploy.sh:32-36 (existing hard-gate pattern to mirror)
  - .claude/skills/ura-kanban/SKILL.md (Forcing functions section)
  sibling_of:
  - KHOST-1
  DEDUPE_2026_08_09: 'Four-surface sweep run. Board: KHOST-1 adjacent (owns rung 3, the generator) — linked,
    not merged, because rung 1 lives in release machinery not the generator. TRANSIT-DIAG-1 matched on
    "diagnostic" only, unrelated. BACKLOG.md: no match. Planning docs: v4.7.10 deploy_sh retrofit is the
    precedent for HOW deploy.sh gets changed, cited. Catalogs: n/a (process, not a presence primitive).
    NEW, not duplicate.'
  updated: 2026-08-17 23:12
  created: 2026-08-09 00:00
  refinement_status: refined
- id: WATCHDOG-INERT-1
  title: Three of four v5.35.0 stuck-signal detectors are effectively inert (D3 structurally unreachable)
  thread: presence
  status: shipped_organic
  shipped_version: v5.67.0
  approval: unreviewed
  tags:
  - no-fabrication-verify
  - measure-before-build
  - context-wide-scoping
  origin:
    date: '2026-08-09'
    gist: fell out of the ledger golden-fixture yield probe — the short buckets were short because the
      events never happen, which is a statement about the detectors, not about instrumentation
  why: 'MEASURED over 7.46 d recorder + 14 d URA notification_log. D3 frozen-tracker is STRUCTURALLY UNREACHABLE:
    threshold FROZEN_TRACKER_DAYS=2.0 (const.py:3121) but longest HA uptime in-window is 1.02 d across
    30 restarts (2.5 h median gap); restart re-adds trackers with fresh last_updated, so the counter resets
    before it can mature. Oldest of 1793 trackers = 1.93 d. D1 camera-stuck has never fired: interior
    max person_count>0 hold is 0.31 h vs STUCK_CAMERA_HOURS=3.0 (~10x margin). P24 fires ~1x/14 d. Only
    P22 (18 episodes) and D2 (13) are live detectors.'
  sharp_problem: D3 cannot catch the incident it was built for. It exists because of the Ezinne 3-day
    frozen tracker; with HA restarting every ~2.5 h a 3-day freeze is invisible to a detector measuring
    uninterrupted in-memory last_updated age.
  root_cause_link: 'Same defect STUCK-SENSOR-1 flagged and nobody pursued — "NO PERSISTENCE: any stuck-state
    tally resets on restart, and we restarted 7+ times today." The probe proves it is fatal for D3 rather
    than merely degrading.'
  options:
  - 'FIX: measure staleness from a PERSISTED timestamp rather than in-memory last_updated, so restarts
    do not reset the counter. Restores D3 to its intended purpose.'
  - 'RE-THRESHOLD: D1 3.0 h against an observed 0.31 h max is ~10x off; either the threshold is wrong
    for this deployment or the detector is guarding a condition that does not occur here.'
  - 'DROP from the ledger migration set: migrating a detector that has never fired buys nothing and its
    golden fixture is impossible to generate by construction.'
  blocks:
  - SIGNAL-TRUST-LEDGER M4/M6 scoping
  RESTART_VERDICT_2026_08_09: 'RESOLVED — NOT a red flag. Authoritative events-table count is 26 (not
    30; my earlier figure was a heuristic overcount that also caught config-entry reloads). ALL 26 were
    clean stops preceded within 300s by an explicit homeassistant.restart: 25 map 1:1 to release commits
    across the v5.47.0->v5.64.0 train, 1 was a deliberate operator manual restart. ZERO spurious, zero
    unclean starts, zero watchdog signature. The parent-reload watchdog hazard did NOT fire (only 5 reload_config_entry
    calls in-window, targeting enphase_envoy and esphome — neither is URA). Operator rule "if restarts
    are us shipping we are ok to keep going" -> WE ARE OK. It is a deploy log.'
  D3_STILL_UNREACHABLE: 'Corrected uptime stats CONFIRM it: median 3.43 h, mean 6.63 h, max 24.32 h (1.01
    d) vs a 2.0 d threshold. Note the causality — D3 is unreachable BECAUSE we ship this often, so it
    will stay unreachable at this cadence. The fix is detector-side (persist the tally across restarts),
    not slowing releases.'
  D1_VERDICT_2026_08_09: '(i) CORRECTLY RARE — leave the thresholds alone. Interior hold distribution
    over 7.30 d: p50 0.004 h, p90 0.013 h, p99 0.044 h, max 0.27 h against a 3.0 h threshold (11x above
    max, ~68x above p99). All 7 interior candidates are live and evaluated (883 non-zero episodes); boot-settle
    is not binding and the v5.35.0 safety rails gate the census DISCOUNT not the NM, so they cannot explain
    the silence. Re-thresholding would land at ~0.3-0.5 h, below plausible real occupancy — the 3.0/6.0
    h pair is a deliberate "impossible for a real body" bound working as designed.'
  GARAGE_CAMERA_RULING_2026_08_10: 'OPERATOR: "No — garage cameras should feed only egresses and exterior.
    I fear it will be too noisy for interior listings." DECIDED: add camera.garage_a/garage_b to CONF_EGRESS_CAMERAS
    (perimeter_alert), NOT camera_person_entities. ACCEPTED CONSEQUENCE: D1 stuck-camera detection will
    never cover the garages (interior-list-only) — the 6.52h garage_b hold class stays invisible to D1
    by explicit choice. Config apply pending.'
  D1_REAL_FINDING_coverage_not_calibration: 'sensor.garage_b_person_count held >0 for 6.52 h and WOULD
    have crossed both rules — but camera.garage_b and camera.garage_a are in NO URA camera list (not interior,
    not perimeter, not egress), while "Garage B" IS a configured URA room. Widening D1 to perimeter would
    be wrong (front_side_ptz 3.49 h, back_yard 2.97 h are legitimately long outdoors). OPERATOR DECISION:
    add the garage cameras to the interior list? Note Garage B has now surfaced three separate times today
    (chattering ratgdo, empty D2 candidate set, uncovered camera).'
  P24_VERDICT_2026_08_09: '(iii) STRUCTURALLY BLIND on its main leg — not a threshold problem. Duration
    precondition met 27 times across 7 rooms in 7.3 d (~3.7/day); the Tier-1 freshness skip suppressed
    27 of 27 (100%). That 100% is a THEOREM not a statistic: any tick reaching coordinator.py:2417-2418
    with STATE_OCCUPIED=True either just refreshed _last_motion_time = now (:2332) or is inside the timeout
    branch, so signal_age < occupancy_timeout < 2x occupancy_timeout — always fresh. The moment it is
    not, _became_occupied_time is nulled (:2373, :2384) and the guard is False. The single real firing
    (Patio, 08-05, 15.06 h session) came through the is-None branch during a grace_hold. Also: camera/BLE-held
    occupancy can NEVER accumulate failsafe duration — the overrides at :2490/:2597 re-seed _became_occupied_time
    AFTER the check (Ziri Bathroom shows occupied 10.79 h but max recorded session 1.10 h). No staleness
    multiplier fixes it: k<=1 is a no-op gate firing on all 27 legit sessions, k>1 is unreachable. THREE
    OPTIONS written up: move the check after the overrides, re-base on _last_pir_motion_time, or accept
    the narrowed semantics and rewrite the NM text honestly.'
  P24_DIAGNOSABILITY_DEFECT: The NM row persists message="[audit]" with NO room name — identifying the
    firing room required a recorder attribute join. Cheap fix, real cost the next time one fires.
  parsimony:
    problem: three shipped detectors do not detect; one cannot detect by construction
    verdict: BUILD
    note: cheap relative to value — this is dead safety machinery we believe is protecting us
  BATCH_REVIEW_ROUND_2026_08_10: 'b9975cf30 (P24 fix + D3 kill + dropdowns + constant split): A DO-NOT-SHIP
    — CRIT-A1: rebasing freshness on _last_pir_motion_time force-vacates the SIX no-PIR rooms at every
    4h+ session (sleeping child in Jaya''s room, nightly); the "27/27 tautological suppression" was, for
    mmWave-primary rooms, the sleeping-body protection WORKING. HIGH-A2: camera-held rooms force-vacated
    + _failsafe_fired latch locks the camera override out. A verified the deferred-clear surgery itself
    is SOUND (all readers traced, seed-if-None becomes benign no-op). B SHIP-w-fix — HIGH-B1: HOUSE_MANUAL
    instructs setting the DELETED constant at six sites (the EC-backout-memo class); MED-B2: dropdown
    strip-on-default silently deletes trust_class from an unchanged save. C DO-NOT-SHIP — the P24 "mutation-anchored"
    tests are SOURCE-GREP assertions + a _should_fire MIRROR (hollow-anchor instance #10): C inverted
    the fresh branch itself and 38/38 stayed green; drills 4/5 (deferred clear, finalize block) also GREEN=untested;
    title_override and the 188-LoC dropdown merge have zero coverage. Builder had claimed mutation-anchored
    while writing grep-anchors AND skipped the per-site drills — both caught only because C re-ran them.'
  FIXUP_IN_FLIGHT_2026_08_10: 'Single consolidated pass: CRIT-A1 no-PIR guard (mirror _d2_motion_sensors_present;
    failsafe simply does not apply to no-PIR rooms); HIGH-A2 invariant "fires only when no live override
    AND PIR stale" + latch fix; C1/H1/H2/H3 REAL behavior tests with C''s drills 3/4/5 re-run to red as
    the gate; HIGH-B1 runbook rewrite; MED-B2 strip-only-kind. Escape hatch authorized: if the harness
    defeats C1/H1, SPLIT the batch — D3 deletion + constant split ride now, P24 + dropdowns follow.'
  OPERATOR_DECISIONS_2026_08_09_round2:
  - 'GARAGE CAMERAS: "Yes pls. Or maybe to the egress list?" — ANSWERED: the two lists do different things
    and only ONE gives D1 coverage. CONF_CAMERA_PERSON_ENTITIES (interior) is what D1 scores (camera_census.py:1787,1803)
    and is ALSO consumed by the census person_count and by fan_veto.py:353. CONF_EGRESS_CAMERAS feeds
    perimeter_alert.py:332 (alerting) — adding garages there would NOT make D1 see them. So: interior
    is required for the stated goal. SIDE EFFECT to weigh: interior also enrols the garage cameras in
    the census person_count (affects house occupancy / guest detection) and in fan veto. Both may be desirable
    — Garage B IS a configured room — but it is a wider change than "give D1 coverage". Possibly BOTH
    lists, for different reasons. Operator confirm before applying.'
  - 'P24: "Instinct is to fix" — APPROVED to fix. Likely needs BOTH investigation options, not one: (A)
    move the failsafe check AFTER the camera/BLE overrides at :2490/:2597, which currently re-seed _became_occupied_time
    after the check so override-held rooms can never accumulate duration; AND (B) re-base the freshness
    test on _last_pir_motion_time instead of _last_motion_time, which is refreshed to now() at :2332 on
    the same tick and makes the skip a tautology (27/27 suppressed). Fixing only one leaves the other
    leg blind. Needs its own scoped cycle + tier call.'
  - 'D3 FROZEN TRACKER: "Kill it." — APPROVED for deletion, not repair. Removes the detector in person_coordinator.py,
    its NM kind, CONF_FROZEN_TRACKER_DAYS / DEFAULT_FROZEN_TRACKER_DAYS (const.py:3120-3121), its config-flow
    field and its tests. Consistent with the earlier drop from the ledger migration set (M6). Rationale
    on record: structurally unreachable — 2.0 d threshold vs max 1.01 d uptime across 26 deploy-driven
    restarts; it cannot catch the Ezinne 3-day freeze it was built for, and will not at this release cadence.'
  SEQUENCING_NOTE: P24-fix and D3-kill both touch coordinator.py / person_coordinator.py. The SENSOR-CAPABILITY-1
    fix-up is concurrently editing coordinator.py on sensor-cap-rebase. Queue these two BEHIND that merge
    rather than running them in parallel — worktree isolation prevents corruption but not merge conflicts
    in the same function region.
  next: 'OPERATOR DECISION 2026-08-09: DROP D1/D3/P24 from the ledger migration set ("1 want to drop.
    Some are rare. Not a bad thing."). Ledger migration set reduces to M1 (P22), M3 (P18), M5 (D2) + M7
    (P14, hand-built). Open sub-question the operator DID want answered: WHY D1 and P24 do not fire —
    correctly-rare vs mis-thresholded vs structurally-blind. Under investigation in AUDIT_detector_silence_and_restart_causes.md.
    D3 remains structurally unreachable regardless; whether to FIX (persisted timestamp) or leave it is
    a separate call, not blocking.'
  operator_decision_2026_08_09: DROP from migration. Rarity is not itself a defect — a detector guarding
    a condition that genuinely does not occur is working. This does NOT close the question of whether
    the thresholds are right; it only removes them from the ledger cycle so they stop blocking M4/M6 scoping.
  refs:
  - docs/planning/AUDIT_ledger_golden_fixture_yield.md (the probe + orchestrator escalation)
  - custom_components/universal_room_automation/const.py:3099,3121
  DEDUPE_2026_08_09: 'Four-surface sweep: STUCK-SENSOR-1 is adjacent (shares the no-persistence root cause)
    but is about EXCLUSION POLICY for live detectors; this is about detectors that never fire at all —
    different problem, linked not merged. BACKLOG B-2026-08-04-1 is about watchdog SPAM (too many notifications),
    the mirror of this. No planning-doc parked item covers it. NEW.'
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: operator decision 2026-08-09 = DROP D1/D3/P24 from ledger migration set. Card
    tracks a DROP decision, not a shipped feature. UN-WATCHABLE by HA recorder. Next step per card: confirm
    drop is recorded in planning doc and close.'
- id: EV-SENSOR-CLEANUP-1
  title: 'EV sensor surface: charge_rate dupe orphans KILLED (done); residual = wire per-plug L1 real power
    (Emporia) so Moes sockets read measured not the 1440W estimate'
  thread: energy
  status: planned
  operator_correction_2026_09_01: 'REVERSED the remove-the-dupes approach. Do NOT delete
    sensor.ura_energy_coordinator_ev_charge_rate_garage_{a,b}; instead REUSE them — populate them from the
    ev_charging_status per-bay power calc so the data is SURFACED on named sensors instead of buried in
    ev_charging_status attributes. Keep ev_charging_status AS IS (unchanged). Net: the two ev_charge_rate
    sensors stop reading unknown and show live per-bay charge rate (from ev_charging_status.garage_{a,b}.power);
    the standalone entities are reused, not tombstoned. (Emporia per-plug real-power wiring already shipped
    v5.78.0.) Build: repoint the ev_charge_rate sensor native_value to the ev_charging_status source; add a
    small test that each populated sensor reflects the corresponding bay power. Tier 1/2 (additive display).'
  shipped_version: v5.78.0
  approval: implied
  origin:
    date: '2026-08-16'
    gist: 'Operator: "repair if not functional dupes; if so remove" + "dead emporia — which ones?" ->
      AUDIT_ev_sensor_surface.md (60105933a).'
  why: 'charge_rate_garage_a/b are strict-subset dupes of ev_charging_status power attrs (zero consumers
    verified) -> REMOVE (sensor.py:315-316 + classes + orphaned properties). Emporia outage ROOT-FIXED
    2026-08-16: v0.12.2 boto3 pin conflict; v0.12.3 installed + restart -> integration loaded, entities
    live, zero errors. Remaining code item: per-plug optional power entity in _plug_config wired to Emporia
    garage circuit (1440W est stays as fallback) so the Moes outlets read measured; card est-markers already
    honest meanwhile.'
  refinement:
  - 'PLAN WRITTEN (PLANNING_ev_charge_rate_sensor_reuse.md) + key finding: the classes are ALREADY DELETED
    (prior REMOVE cycle shipped — tombstones sensor.py:317-321, energy.py:10211-10216), so the two unknown
    entities are registry ORPHANS with no code behind them. "Reuse" = RE-ADD from scratch + populate, not
    edit-in-place. Source path found: energy.ev_status["garage_{a,b}"]["power"] in WATTS -> publish kW (÷1000,
    mirroring sensor.py:10030 _derive_per_bay_state). Idle = 0.000 kW; None only when EC/bay entry missing.
    CRITICAL build risk: builder MUST adopt the orphans EXACT unique_id from live .storage/core.entity_registry
    (not guess f"{DOMAIN}_ev_charge_rate_garage_a") or HA mints _2 duplicates and leaves the orphans dead
    (reference_frigate1_retired_2suffix_permanent footgun). Tier 2 (2 reviews) for that registry regression risk.'
  - 'PLAN-REVIEW = FIX-REQUIRED, and it tips the decision to KILL. HIGH-2: the producer power field is NOT
    always a measurement — when the Emporia power sensor is dead but the switch says charging, it returns a
    FABRICATED constant EVSE_ESTIMATED_POWER_W=7600 (energy_pool.py:966, energy_const.py:871); publishing that
    as a POWER/MEASUREMENT sensor would write 7.6kW of fiction into HA long-term statistics as if measured, and
    reads a confident 0.000kW when Emporia is unavailable+switch-off (Bug Class #7). HIGH-1: the plan guessed the
    orphan unique_id WRONG (missing the energy_ segment; real = universal_room_automation_energy_ev_charge_rate_garage_{a,b}
    per live registry) — a build following it would mint _2 dupes. Since the classes are already deleted, re-add
    buys only graphable instantaneous kW (marginal; the EV detail card already surfaces per-bay power from the
    attrs, marking switch-status as ~est). RECOMMENDATION: KILL the two orphan rows (one-shot ha_remove_entity,
    like automation_mode) — zero code, zero fiction risk. If per-bay ENERGY history (kWh) is wanted, that is a
    different + more useful sensor, card separately. Side-findings: CLAUDE.md mount path is wrong for this machine
    (see DOC-MOUNT-PATH-STALE-1); docstring sensor.ura_ev_charging_status should be ...coordinator_ev_charging_status.'
  - 'KILL EXECUTED (operator "Lets kill", 2026-09-01): ha_remove_entity bulk removed the two orphan rows
    (removed 2/skipped 0/errors 0); both now 404; codeless orphans so they will NOT return on restart. Q1 of
    AUDIT_ev_sensor_surface.md DONE. Attribute-reuse on dashboards: the per-bay power lives in
    ev_charging_status attrs and the URA v8 EV detail card (EVCARD-1) ALREADY surfaces it (marking switch-status
    values ~est); a v6 card can read the same attrs — no sensor needed. RESIDUAL (audit Q3, this card re-scoped
    to it): wire the Moes L1 sockets to the real Emporia garage-circuit power sensor (keep L1_ESTIMATED_POWER_W=1440
    as fallback) so the outlets read MEASURED not estimated. Emporia integration root-fixed 2026-08-16 (v0.12.3).'
  next: 'Q3 remainder: add optional per-plug power entity in _plug_config wired to the Emporia garage circuit
    (small additive, keeps the 1440W estimate as fallback). Small Tier-1/2; queue behind the in-flight cycles.'
  refs:
  - docs/planning/AUDIT_ev_sensor_surface.md
  live_validation_2026_08_16: 'v5.78.0 LIVE 2026-08-16. L1 PASS (0 errors), L4 PASS (face_recognized_count
    + path_alpha_gate_source live on house-state sensor). L2 PASS-on-state / attribution organic: house
    is away with all 4 persons not_home and census 0 — but the transition coincided with the deploy restart.
    STRONG retrospective evidence: family left 09:29, house stayed home_day until 11:11 (~1h42m stuck-while-empty)
    on v5.77.0 the same morning — the bug was live right up to this deploy. L3 not verifiable with an
    empty house (no populated per-person rows); L5/L6 organic. Side-effect: census source_agreement now
    both_agree (was single_source).'
- id: EVCARD-1
  title: EV charging detail card for the URA v8 Energy tab
  thread: dashboarding
  status: waiting_operator
  approval: explicit
  approved_by: operator 2026-08-09
  origin:
    date: '2026-08-09'
    gist: '"add an EV charging detail card to the Ura v8 energy tab. Style well. Detail cards are a bit
      sensor words vomit. Best judgement because of space though."'
  why: EV charging is a first-class energy behaviour (drain precedence, must-start-by, TOU exposure) with
    no dedicated surface on the v8 energy tab.
  constraints:
  - 'STYLE IS PART OF THE ASK. Existing detail cards are "sensor word vomit" — dense raw-entity dumps.
    Do NOT add another. Design for scanning: state first, numbers second, raw entities last or hidden.'
  - Space-constrained — operator explicitly delegated judgement on what to cut. Curate ruthlessly; completeness
    is not the goal.
  - Reuse existing EV/EVSE entities and precedence concepts; do not invent new sensors for the card.
  next: 'REDESIGNED 2026-08-16 after operator verdict "design is poor — review the main sensors." Full
    sensor discovery this time: ev_charging_status per-EVSE attrs (is_on/power/charging/ pause_reason_human
    — power WORKS while the standalone ev_charge_rate_* sensors sit unknown), ev_charging_plan DP state
    machine (must_start_by_dt, last_eval_snapshot), battery_strategy (next_decision_boundary, force-charge,
    soc), L1-charger plug binary, resume-at-SOC number, current_energy_rate. New card: verdict header
    w/ TOTAL live kW from status attrs; per-charger table (plug/state/kW) sourced from attrs not the dead
    Emporia sensors; NEW anomaly line "plugged but not drawing" (would have surfaced the Garage-A overnight
    no-charge incident); Plan line + must-start-by; battery-drain resume threshold when paused; Next TOU
    boundary w/ expected EV action; grid ¢/kWh; force-charge 30-min tile added. Cut: raw Emporia/SPAN
    entities (unavailable), eval snapshot internals, arbitrage/grid-cap lists when empty. Applied views[2].sections[8],
    write_committed + post_write_verified, template eval-verified live. No vehicle-side integration exists
    (BMW/Tesla sweep empty) — vehicle SOC/target impossible until one is added. AWAITING OPERATOR RE-REVIEW.
    FIX 2026-08-16: operator flagged the constant Outlets 2.9 kW — it is a BY-DESIGN estimate (energy_pool
    emits L1_ESTIMATED_POWER_W=1440 per on-socket with power_source "switch_status"; audit AUDIT_ev_sensor_surface.md
    verdict 3), not a measurement. Card now renders any power_source=="switch_status" value as an estimate
    ("~2.9 est" row, header "2.9 kW (~est)") so estimated never masquerades as measured; real per-plug
    power arrives when the Emporia integration recovers + per-plug wiring ships. Re-applied + render-verified.'
  applied_render_2026_08_09: '## ⏸ Paused / TOU peak/mid-peak pause / [Garage A yes|Paused|0.0 kW] [Garage
    B —|Off|0.0 kW] [Outlets (2) —|TOU peak/mid-peak pause|—] / **Plan:** Hold Only · held 53h — 7 lines,
    zero None/unavailable/unknown, all four conditional lines correctly absent.'
  fix_2026_08_09_held_label: 'Operator: "What does held 53h mean?" — it was WRONG. Verified in source:
    since is stamped on every DP state transition (energy_drain_precedence.py:265) and HOLD_ONLY CLEARS
    hold_started_at as a "clean reversion" (:269-274); DPState docstring says HOLD_ONLY = "Default; master
    switch off OR eval said no". So HOLD_ONLY is the RESTING state — nothing held — and "held 53h" asserted
    the opposite of the truth (a reader would think their car had been blocked for two days). Replaced
    with a state-aware line: hold_only -> "idle — no hold on charging · unchanged 53h"; transitioned ->
    "EVSEs paused for battery drain since <time>" (transitioned_at); otherwise -> "· holding <n>m" (hold_started_at).
    Verified live + storage re-read (str.replace is a silent no-op on a miss and the write reports success
    regardless).'
  ARRESTER_TILE_2026_08_10: 'Operator aside: temp override arrester onto the ura-v8 HVAC (Climate) tab
    for quick access. DONE — new section at climate view position 1 (right under the hero, above the thermostats):
    heading w/ live state badge + full-width toggle tile named "Let my manual changes stick" + one conditional
    status line (engaged-Nm-ago + 6h expiry + sunset semantics when ON; "flip on before adjusting setpoints"
    when OFF). Verified rendering live (switch currently ON, 26m — interesting: it re-engaged today, presumably
    re-enabled after last night''s 6h sunset). Anti-word-vomit rules applied; no raw attr dump.'
  MULT_SPLIT_APPROVED_2026_08_10: 'Operator: "Clean break. Timing is fine." — BLE_CHAIN_HOLD_ENABLED (bool
    kill switch) + separately named D2 staleness multiplier; NO deprecated alias (single-user no-backcompat).
    Rides the P24/D3/dropdown batch.'
  refinement_candidates:
  - 'REDUNDANCY: the headline reason and the Outlets row currently show the same string twice ("TOU peak/mid-peak
    pause") because the outlets are the only endpoints holding a reason. Options: drop the reason from
    the endpoint row, or drop it from the headline when only one distinct reason exists. Left as-is pending
    operator eye — it may read as useful attribution rather than repetition.'
  - Retrofit the same conditional rendering to the sibling Battery Strategy Detail card (~30 min).
  bug_caught_pre_ship: The markdown card auto-detects entities from LITERAL entity IDs in the template.
    This template reaches them through Jinja VARIABLES (states(s)), so auto-detection would have missed
    them and the card would never re-render on state change — it would sit stale until an unrelated repaint.
    Fixed by adding the explicit entity_id watch list. Caught by reading the HA dashboard best-practices
    guide, not by testing.
  design_notes:
  - 'Anti-word-vomit rules applied: (1) narrative first — pause_reason_human leads, and nothing on the
    dashboard consumed that attribute before; (2) CONDITIONAL rendering — must_start_by, force_charge_until,
    excess-solar and fill-target only render when set, so an idle evening is ~6 lines and a busy one grows;
    (3) one row per ENDPOINT not one line per attribute.'
  - 'Found FOUR chargeable endpoints, not two: garage_a + garage_b Emporia EVSEs plus two Moes smartplug
    sockets (the only ones currently holding a real reason, "TOU peak/mid-peak pause").'
  - 'Omitted as noise: the paused_by_* booleans (they are the INPUTS to pause_reason_human), cooldowns,
    evse_config, pause_dispatch_state, all shadow_* eval telemetry, and SPAN lifetime counters reading
    in the tens of millions of Wh.'
  - Sibling Battery Strategy Detail card renders every field UNCONDITIONALLY including None — that is
    the concrete failure mode being improved on. Worth retrofitting the same conditional treatment there
    if the operator likes this one.
  followup_candidate: retrofit conditional rendering to the Battery Strategy Detail card (same section
    group, same defect, ~30 min) — only if the operator endorses this card's style
  DEDUPE_2026_08_09: 'Sweep: dashboarding thread has the PWA + KHOST-1 (kanban board, different surface);
    EV drain-precedence card is queued BACKLOG work about behaviour not display. No existing card covers
    a v8 energy-tab EV surface. NEW.'
  status_correction_2026_08_16: 'Was stale in INBOX — the card was BUILT and applied live to ura-v8 Energy
    tab 2026-08-09; correct state = waiting_operator (refinement review, operator: "I''ll review and we
    can refine").'
- id: EGRESS-CAMERA-DEAD-CONFIG-1
  title: Two of five egress cameras are configured under dead Frigate-1 names — Garage A and Garage B
    contribute nothing to egress detection, on the house's primary entry path
  thread: camera
  status: shipped_organic
  shipped_version: v5.86.0
  updated: 2026-08-20 20:30
  approval: needs_operator
  tags:
  - frigate-leg-naming
  - cross-investigation-synthesis
  - measure-before-build
  origin:
    date: '2026-08-20'
    gist: Surfaced from a log-flood triage, then escalated on inspection. The HA error log carries "Camera
      entity camera.garage_a not found in registry — skipping" and the same for garage_b, 1,015 times
      EACH in a five-hour window. Operator asked whether this was the Frigate-1 retirement biting, given
      how messy slug guessing has been. IT IS — but the failure is stored CONFIG, not code-side slug guessing,
      which matters for the fix.
  evidence: 'Read from the live mount .storage/core.config_entries, integration entry: egress_cameras
    = [camera.madrone_g6_entry, camera.doorbell_lite, camera.front_door_aerial, camera.garage_a, camera.garage_b].
    Checked all 26 configured camera ids across the three lists against .storage/core.entity_registry:
    camera_person_entities 12/12 present, perimeter_cameras 9/9 present, egress_cameras 3/5 present. The
    two missing are camera.garage_a and camera.garage_b, and BOTH have a live suffixed sibling in the
    registry — camera.garage_a_2 and camera.garage_b_2 (confirmed `recording` in the state machine). So
    it is precisely the F1-retired / _2-permanent hazard from reference_frigate1_retired_2suffix_permanent.md.
    OPERATOR CONTEXT: these two cameras "were reclassified recently" — i.e. they were moved INTO the egress
    list at some point using the pre-retirement bare names. That explains why exactly two entries are
    stale while the other 24 are clean: this is not decay across the board, it is one write of a list
    using dead names. CODE PATH: camera_census._get_interior_camera_list / _get_integration_camera_list
    read the stored list and call CameraIntegrationManager.resolve_configured_cameras (camera_census.py:462-482),
    which does ent_reg.async_get(entity_id) and, on None, WARNs and `continue`s. No fallback, no _2 retry,
    no aggregation of the failure.'
  why_it_matters: 'NOT just log spam — that was my first read and it was wrong. This is silent coverage
    loss on the entry path that matters most. Two of five egress cameras resolve to nothing, so Garage
    A and Garage B feed ZERO person-detection binary sensors into egress. The memory reference_egress_face_coverage_7pct_not_a_ceiling.md
    records that the real entry route for this house is GARAGE + FAMILY ROOM (the earlier ~7% figure was
    measured on the wrong camera, the front door). So the two dead egress cameras are on the primary entry
    route, and any egress / identity / arrival coverage number computed since they were reclassified is
    understated by an unknown amount. CROSS-INVESTIGATION JOIN: this should be checked BEFORE any further
    egress coverage measurement or any consumer work gated on egress person_id, because it may partly
    explain a coverage shortfall that was previously attributed to face-recognition reach. RECONCILES
    WITH PRIOR ART, does not contradict it: AUDIT_frigate_dead_leg_correctness.md (2026-08-18) concluded
    "URA is NOT reading dead Frigate legs anywhere" and that verdict STANDS — it was about READ paths
    returning frozen values, and it correctly found that removed entities resolve to nothing rather than
    to stale data. What that audit did not ask is whether resolving to nothing is itself harmful. Here
    it is: no corruption, but a silently missing input. Its own latent Finding L1 (camera_resolver._prefer_canonical
    prefers the bare name, which on a perimeter/exterior camera is the DEAD F1 name) is the sibling of
    this, in code rather than config.'
  DECISION_2026_08_20_RIDE_NEXT_DEPLOY: 'OPERATOR: "Egress cameras. Ride the next deploy." ACCEPTED, and
    it is the right call for a reason I got wrong first time. I had recommended fixing it tonight via
    the options flow as a no-deploy change. THAT ADVICE WAS UNSAFE AND IS WITHDRAWN: CONF_EGRESS_CAMERAS
    is DELIBERATELY EXCLUDED from INTEGRATION_OPTIONS_RELOAD_SUPPRESS_KEYS (__init__.py:5986-5990 — PerimeterAlertManager
    caches these lists at setup with no refresh signal, so they were left on the legacy reload path on
    purpose). A save therefore falls through to "scheduling reload" on the INTEGRATION (parent) entry,
    which is the documented 2026-08-07 outage mechanism: a Camera Census save cascading a synchronous
    reload to ~40 child entries, ~5-minute event-loop stall, supervisor watchdog restart. Riding a deploy
    costs nothing because the reload happens anyway. CARRY-FORWARD FOR THE DEPLOY: repoint egress_cameras
    camera.garage_a -> camera.garage_a_2 and camera.garage_b -> camera.garage_b_2 in the same window as
    the restart. VERIFY BOTH WAYS — the warning stopping is necessary but NOT sufficient; also confirm
    the resolved egress person binary_sensor count rises by those two cameras'' sensors, which is what
    proves coverage returned rather than the message merely being silenced.'
  next: 'Tier 1 config fix first, then a Tier 2 code question — deliberately separated so the live gap
    closes tonight without waiting on a build. (1) IMMEDIATE, no deploy: repoint egress_cameras entries
    camera.garage_a -> camera.garage_a_2 and camera.garage_b -> camera.garage_b_2 via the options flow.
    Verify by re-reading the log for the warning and confirming it stops, AND by confirming the resolved
    person binary_sensor count for egress rises by the two cameras'' sensors — the second check is the
    one that proves coverage came back rather than just silencing the message. Do NOT hand-edit .storage.
    (2) THEN decide the code question: should resolve_configured_cameras fall back to the _N sibling when
    the canonical name is absent? Argument for: makes the whole class self-healing and matches the operator''s
    "slug guessing has been messy" concern. Argument against, and it is strong: silent fallback is how
    you end up trusting a camera you did not configure, and prior art already warns that _prefer_canonical
    guessing in the OTHER direction is a latent hazard. RECOMMENDED SHAPE: do not auto-substitute. Instead
    (a) aggregate the warning to once per resolve-cycle per entity instead of every tick — 1,015 repetitions
    of a static config fact is the actual log defect — and (b) surface unresolved configured cameras as
    a DIAGNOSTIC (a count + list on a census/diagnostic sensor) so a dead config entry is visible on a
    dashboard instead of only in a log nobody reads. Detection, not silent repair. (3) SWEEP, cheap and
    worth doing once: the same dead-name check across every OTHER stored entity list URA holds (room occupancy_sensors,
    motion, lux, alert_lights, zone cameras), not just the three camera lists. This class is "stored config
    naming an entity that no longer exists", and cameras are unlikely to be the only place a rename or
    retirement left one behind. Report the count even if zero.'
- id: SENSCAP-ORPHAN-1
  organic_evidence: '2026-08-23 CONFIRMED — founding case discharged. Live config entry for Garage Hallway
    now reads occupancy_sensors = [] : the camera person sensor (binary_sensor.staircase_person_occupancy)
    has been REMOVED and the form SAVED. Pre-fix this was impossible — the stale per-entity dropdown re-injected
    an orphan declaration and wedged the save permanently with sensor_capabilities_invalid. The removal
    completing IS the acceptance criterion. Verified by reading .storage/core.config_entries directly,
    not by asking the operator — the operator correctly pointed out this was checkable from device/entry
    properties all along. Also confirms the (a) half of the card''s open questions: the mmWave the operator
    asked to ADD, binary_sensor.mmwave_temp_hum_lux_garagehallway_presence, IS present in motion_sensors.'
  title: A room sensor can never be removed once capabilities are in play — the stale per-entity dropdown
    re-injects an orphan declaration and wedges the form permanently
  thread: config
  status: done
  shipped_version: v5.85.1
  updated: 2026-08-23 11:50
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: "please fix the fact that I cannot clear the camera person
    sensor. This error shows not matter what I do.")'
  tags:
  - wire-in-anchor
  - fix-ready-rides-next-deploy
  origin:
    date: '2026-08-20'
    gist: Operator, working in the Garage Hallway room options flow, could not remove binary_sensor.staircase_person_occupancy
      (the noisy Frigate camera person sensor identified earlier in this session as the worst input in
      that room — 89 on/off cycles/24h vs 53 for the PIR sibling). Every submit returned sensor_capabilities_invalid
      regardless of what they changed. Screenshot supplied.
  root_cause: 'CONFIRMED, and it blocks removal of ANY sensor from ANY room that has a capability dropdown
    rendered — not just this room and not just camera sensors. The per-entity capability dropdowns (`caps_kind__<entity_id>`)
    are built from the room''s PRE-EDIT CONF lists (config_flow.py:9687-9697). When the operator removes
    a sensor and submits, that sensor''s dropdown is STILL in the submission because the form was rendered
    before the edit. The fold loop then calls derive_capability() for it, which returns None — rule 3,
    "the entity is not part of this room''s Tier-1 wiring" (sensor_capability.py:187-188) — so `_kind
    == _default_kind` is False, the no-op branch is missed, and the else-branch WRITES a declaration for
    an entity that is no longer wired. The immediately following re-validate then rejects it via validate_capabilities_payload''s
    `if entity_id not in known` check (sensor_capability.py:292-297). The save is refused, the step re-renders
    with the same stale dropdown, and the loop is closed: the removal can never be completed. Exactly
    the operator''s "no matter what I do". Same wedge is reachable through the JSON blob rather than the
    dropdown, because the FIRST validate (config_flow.py:9491) runs before the fold — so the prune has
    to happen on both paths.'
  fix: 'Implemented, uncommitted, ready to ride the next deploy. Three edits in config_flow.py async_step_sensors:
    (1) compute `_entities_now` — the union of the three lists AS SUBMITTED; (2) prune JSON-authored declarations
    for de-wired entities before the first validate, logging what was dropped at INFO; (3) in the fold
    loop, skip any dropdown whose entity is not in `_entities_now` and drop any declaration it still carries.
    Dropping is the CORRECT semantics rather than a workaround — the operator has just declared the entity
    is no longer wired into the room, and the validator''s own error text tells them to fix it by editing
    the CONF lists, which is precisely what they did.'
  NEAR_MISS_2026_08_20: 'THE FIRST VERSION OF THIS FIX BROKE FOUR EXISTING TESTS and the targeted suites
    did not show it. test_v4516_failsafe_freshness H3a-H3d extract the dropdown-merge region from config_flow.py
    VERBATIM and exec it standalone (production source is that test''s oracle, by design). My `_entities_now`
    was bound in the enclosing scope, OUTSIDE the extracted region, so the slice raised NameError. Fixed
    by deriving it inside the merge region from motion/mmwave/occupancy, which that harness seeds — keeping
    the block self-contained, the same constraint that already makes it elide its own relative import
    there. IT WAS ONLY VISIBLE IN THE FULL-SUITE NAME-DIFF against a clean baseline worktree. Two process
    notes, because either would have let it ship: (1) The first background diff run was TRUNCATED TO 12
    LINES by the task runner, losing the failure list entirely. Re-run in foreground. (2) A COUNT comparison
    would have actively misled: baseline 141 failed / 9211 passed vs first-fix 145 failed / 9211 passed.
    The +4 reads naturally as "my four new tests failed". It was four EXISTING tests breaking while my
    four never ran at all. Only the NAME diff separates those two stories. This is the concrete case for
    the standing "baselines are name-diffs, not count-diffs" rule. FINAL STATE: baseline 141 failed /
    9211 passed / 17 errors; with fix 141 failed / 9215 passed / 17 errors; name diff EMPTY in both directions.
    Diff is purely additive — 73 insertions, 0 deletions. Baseline worktree removed after use.'
  verification: 'New file quality/tests/test_senscap_orphan_removal.py — 4 tests, built on the existing
    HA-mock options-flow harness, driving the REAL async_step_sensors handler with the live Garage Hallway
    wiring as the fixture. Founding case = remove the camera sensor while its stale dropdown is still
    submitted. Two byte-identity guards assert a no-removal submission still validates clean and that
    a live declaration is never discarded. Fourth test asserts the orphan is not merely silenced but absent
    from the persisted capabilities — guarding the other failure direction, where silencing the error
    while still writing the orphan would block every FUTURE save of that room. MUTATION DRILL RUN (not
    a source grep — the hollow-anchor rule): both guards neutered with bytecode writing disabled and __pycache__
    cleared; result `2 failed, 2 passed`, and the failure carries the operator''s exact error string plus
    the validator''s own message ("entity ... is not in this room''s motion / mmwave / occupancy CONF
    lists"). Guards restored, 0 mutation markers remaining, file recompiles, and 76 tests pass across
    test_senscap_orphan_removal + test_sensor_capability_and_role + test_cycle_b_config_flow + test_baec_config_flow_round_trip.'
  next: 'Tier 1 — 1 file + 1 new test file, single defect, no new capability, no cross-coordinator reach.
    Ship with the next deploy alongside the EGRESS-CAMERA-DEAD-CONFIG-1 repoint; they pair naturally because
    both are config-surface corrections that need one restart between them. AFTER DEPLOY the operator
    can complete the removal through the UI normally. TWO THINGS TO RAISE WITH THE OPERATOR, neither actioned:
    (a) the mmWave they asked to ADD, binary_sensor.mmwave_temp_hum_lux_garagehallway_presence, is ALREADY
    in motion_sensors — that half of the request needs no change; (b) the room''s one capability declaration
    tags binary_sensor.rgbw_motion_lux_3rdr_zigbee_garagehallway_occupancy_2 as kind "camera_presence",
    but that entity is a Zigbee RGBW motion/lux device, not a camera. It looks like a mis-tag from an
    earlier attempt to label the actual camera sensor and picking the wrong row. It validates (camera_presence
    is not in _NON_CORROBORATOR_KINDS so the D-MEDIUM-1 rule does not fire) but it is very likely wrong,
    and it changes how that sensor is trusted. Confirm intent before clearing it.'
- id: HVAC-HOT-ENTRY-LATENCY-1
  title: Occupant walked into an 80F zone and beat URA to the thermostat by 3 minutes — entry dwell does
    not care how hot the room is
  thread: hvac
  status: pre_planning
  updated: 2026-08-20 22:45
  approval: needs_operator
  tags:
  - measure-before-build
  - operator-corrected-me
  origin:
    date: '2026-08-20'
    gist: Operator asked why zone 2 was not being arrested. I gave two wrong answers — that the equipment
      was idle and that URA had written the manual itself. Operator refuted both ("The equipment is not
      idle", "I think a human did that. Nothing in Ura would point at 71", "Jaya definitely yanked the
      thermostat") and was right on both counts.
  evidence: 'RECONSTRUCTED FROM climate.up_hallway_zone_2 ATTRIBUTE HISTORY, 19:45-22:20 CDT. 19:45 preset
    `away`, 68/80, cooling then idle. Room climbs to 80F. ~20:18 Jaya enters (zone_2_status continuous_occupied_hours
    1.8 as of 22:06 back-computes to ~20:18; occupied_rooms = Guest Bedroom 2 + Jaya Bedroom). 20:20:39
    away -> MANUAL, target_temp_high 80 -> 78. 20:21:08 78 -> 76.  20:42:12 76 -> 71. A stepwise 80/78/76
    ladder inside 30 seconds then a further cut 20 min later is a person pressing a down-arrow. Nothing
    in URA emits that ladder, and 71 is not a value URA would write. RESOLVED ON ITS OWN: 22:12:57 hold_activity
    -> sleep, 22:13:20 preset `sleep` 70/75 cooling, 22:15:14 preset `home` 70/76, hvac_action cooling,
    conditioning active_cool. MY "EQUIPMENT IDLE" CLAIM WAS FALSE — physical cross-check at 22:35: span_panel_ac1_power
    2784 W, ac_2 2738 W, ac_3 2988 W, universal_room_automation_hvac_system_demand 100%, hvac_direction
    cooling. The plant was running hard the whole time. I had read the Bryant `hvac_action`/`conditioning`
    attributes as plant state; on this system they track the ZONE call, not the equipment. SECOND time
    today I trusted a Bryant cloud attribute as an oracle without a physical cross-check (the first was
    last_changed as a liveness signal). The lesson is not "that attribute is bad", it is: for anything
    about EQUIPMENT, read the wattage.'
  CORRECTION_NOT_UNAIDED_2026_08_21: 'MY "RESOLVED ON ITS OWN" CLAIM WAS WRONG — operator: "It didn''t
    recover unaided. I got tired of wasting time and money and intervened via the Bryant app." The DB
    confirms the operator''s account and refines it. ura_activity_log has exactly ONE zone_2 row in the
    whole 01:00-04:00 UTC window: 2026-08-21T03:15:13Z = 22:15:13 CDT, action=preset_change, "Upstairs
    preset sleep -> home (house=guest)". So the sequence was: 22:12:57 hold_activity -> sleep and 22:13:20
    preset -> sleep 70/75 = THE OPERATOR''S BRYANT APP WRITE, which replaced `manual` with a real preset;
    then 22:15:13 URA made its own preset_change sleep -> home. URA did NOT break the manual. A human
    did, and URA resumed control two minutes later. THIS IS A CLEAN CONFIRMATION OF THE LOCKOUT MECHANISM
    rather than a refutation: URA could not escape `manual` on its own (should_change_preset returns False
    while preset == manual), and the moment a non-manual preset appeared it acted immediately. Do not
    let a future session read the 22:15 recovery as self-healing.'
  ARRESTER_IS_UNAUDITABLE_2026_08_21: 'THE LEDGER I PROMISED DOES NOT EXIST — answering the open question
    is blocked, not pending. Pulled the DB directly on the HA host over ssh (the Samba mount cannot open
    it: WAL). Findings: (a) The HVAC coordinator''s ENTIRE action vocabulary in ura_activity_log is {fan_off,
    fan_on, pre_arrival, preset_change, preset_change_suppressed}. There is NO override / arrest / shave
    action at all. The arrester writes NOTHING to the activity log, so overrides_today is in-memory only
    and there is no after-the-fact record of a detection. (b) No arrester notification in notification_log
    for the 20:20 CDT window. (c) `preset_change_suppressed` fired exactly ONCE in 24h and for an unrelated
    reason ("Upstairs preset flip -> away suppressed during waking", 06:00 CDT). So the MANUAL LOCKOUT
    IS ALSO SILENT — URA declined to write presets to zone 2 for ~2 hours and logged nothing at all. CONSEQUENCE:
    the (a)-vs-(b) question from HVAC-HOT-ENTRY-LATENCY-1 — suppression-TTL swallow vs ARREST-COMFORT-1
    deferral — CANNOT be answered retrospectively. The only routes left are forward-looking: raise the
    arrester logger to INFO and catch the next occurrence, or add an activity-log row on detect/defer/revert.
    The second is the real fix and is arguably its own small card: a governance mechanism that reverts
    operator actions but keeps no auditable record of what it decided is not reviewable after the fact.'
  root_cause: 'A RESPONSIVENESS failure, not an arrester failure. The zone was legitimately `away` with
    the away ceiling at 80F. Jaya arrived at ~20:18 into an 80F room. zone_entry_dwell is 5.0 minutes
    = exactly one decision tick, so the EARLIEST URA could have flipped away -> home was ~20:23. She reached
    the thermostat at 20:20:39 — about two minutes after walking in, and about three minutes before URA
    would have acted. This is precisely the operator requirement stated earlier the same evening: "We
    want it to react quickly to someone being there if its hot." It is unmet, and this is the first live,
    dated, fully-reconstructed instance of the cost. It is also the strongest evidence yet for the TEMPERATURE-CONDITIONAL
    DWELL idea already recorded on HVAC-PRESET-FLAP-1: when a zone is sitting at its away ceiling and
    someone walks in, a flat five-minute wait is indefensible; when the zone is already comfortable, the
    wait is free. Same knob, conditioned on the one variable that decides whether waiting costs anything.'
  open_question: 'DID THE ARRESTER DETECT THE OVERRIDE? UNRESOLVED — do not let the next session assume
    either way. The 20:20:39 away->manual transition is the arrester''s documented trigger (hvac_override.py:2069-2071),
    yet the Upstairs zone still reports overrides_today: 0 while Entertainment + Master Suite reports
    3 with last_direction warmer. The manual episode has since COMPLETED, so a detected-then-deferred
    override would have incremented by now. That points at NOT DETECTED, which would be a real regression
    — operator''s instinct was "This feels like a regression." BUT THE LOG CANNOT SETTLE IT AND I NEARLY
    MIS-READ IT AS PROOF. The arrester emits "Override detected on %s" at INFO. A search returns nothing
    — however a follow-up check shows there are ZERO URA INFO lines in the log at all; the log carries
    WARNING and above only. So the absence is not evidence, it is the same undiscriminating-observation
    trap as the DP hold_pre_eval misdiagnosis this morning. TWO LIVE CANDIDATES, NOT SEPARATED: (a) `_is_genuine_manual`
    swallowed it because URA had suppressed that entity within SUPPRESS_TTL_SECONDS just before 20:20:39
    — note the cloud echo visible at 20:20:48 where hold_activity bounces manual->away->manual, which
    is exactly the shape that makes a suppression window overlap a real human edit. That would be a genuine
    defect. (b) ARREST-COMFORT-1 deferred the arrest BY DESIGN because the manual was comfort-qualified
    at 80F — which would be correct behaviour and no bug at all. HOW TO SETTLE IT (either is cheap): read
    the arrester ledger rows in the URA DB for tonight, or raise the arrester logger to INFO and catch
    the next occurrence. NOTE the DB was NOT reachable this session — no ura-sqlite MCP connected, and
    sqlite cannot open the live DB read-only over the Samba mount because it is in WAL mode. Copy it locally
    or reconnect the MCP.'
  next: 'Two separable pieces of work; do NOT bundle them until the open question is settled. (1) The
    responsiveness fix (temperature-conditional dwell) belongs with HVAC-PRESET-FLAP-1 and is now evidence-backed
    rather than speculative. (2) The arrester question is a triage step, not a cycle — answer it first,
    because if (a) is true it is a suppression-window defect that would also explain other missed overrides,
    and if (b) is true there is nothing to fix and the card closes. ALSO NOTE FOR THE RECORD: the zone
    recovered without intervention at 22:15. Whatever else is true, this was not a stuck state.'
- id: CARRIER-STALE-POLL-REFRESH-1
  title: ha_carrier goes stale for up to 1.8h and only a RELOAD clears it — operator asked for a periodic
    refresh; CUT from the pipeline-hardening cycle, carded so the request is not dropped
  thread: hvac
  status: pre_planning
  created: 2026-08-22 15:30
  updated: 2026-08-24 16:45
  refinement_status: refined
  REMEDIATION_RUN_2026_08_24: 'Operator granted permission for the full remediation sequence incl the
    reload leg. Design: detached script does READ-ONLY detection on the HA host (/tmp/carrier_blind_watch2.py,
    pid 29697, 6h window, exits on first confirmed episode); the remediation is driven by the orchestrator
    via home-assistant MCP when the detector fires (auth + exact entry_id handled safely, live reload
    under control) rather than embedded in the detached script. Live-state check at launch: NO zone confirmed-blind
    right now (z1 cooling/3.6kW blower0; z2 cooling/1.8kW; z3 idle/1.7kW/blower736 but UNDER the 2200W
    thr = likely fan circulation, not a compressor-blind episode). Awaiting next episode. Remediation
    sequence when it fires: (1) homeassistant.update_entity on the blind zone climate entity -> reread;
    (2) if still blind, homeassistant.reload_config_entry on the ha_carrier entry ONLY -> reread; record
    update_entity_worked + reload_worked to settle the cheap-vs-costly design question. MUST BE POLLED:
    ssh ha "cat /tmp/carrier_blind_watch2.out".'
  PROBE_C_RESULT_2026_08_24_CONFIRMED: 'The detached blind-episode detector FIRED and exited on the first
    confirmed episode: zone_2 (climate.up_hallway_zone_2) reporting hvac_action=idle, blower_rpm=0 while
    drawing 2710.7 W, temp 80F against target 76F, at 2026-08-23T21:24:20. This CONFIRMS the sustained-blind
    defect live (idle reported during active conditioning). NOTE: the detector script as run only captured/emitted
    the blind state — the update_entity-then-reload remediation legs (per PROBE_C_STILL_WORTH_RUNNING)
    were NOT exercised in this run, so the open question (does homeassistant.update_entity clear it, or
    only a reload?) is STILL OPEN. Promoting investigating -> pre_planning: the defect is confirmed real
    and sustained; what remains is the cheap-vs-costly remediation decision, which is a design/scope question,
    not a measurement question. Next probe run must exercise the remediation legs to settle update_entity
    vs reload before any periodic-refresh design.'
  approval: unreviewed
  tags:
  - third-party-defect
  - operator-requested
  - probe-pending
  OPERATOR_REQUEST: 'Operator 2026-08-22: "I just found that reloading the carrier integration made it
    show reality. Not required for nudging but definitely probably required for hvac ops. Else we will
    lose responsiveness. Thinking of adding a periodic integration refresh to our operations." THE REQUEST
    IS NOT REFUSED — it is SEPARATED. It was cut from PLANNING_ac_ramp_pipeline_hardening as D9 so the
    cycle does not carry a near-zero-prior deliverable across six sections, NOT because it lacks merit.
    Carded so it survives the cut.'
  THE_DEFECT_MEASURED: 'ha_carrier reports a CONFIDENT WRONG `hvac_action: idle` while the compressor
    draws kilowatts. Measured over 7.4 days, duration-weighted, orchestrator-reproduced with an independent
    implementation: 12.2% / 7.1% / 12.9% of high-draw time per zone. SUSTAINED, not lag — 91-95% of blind
    TIME is in episodes over 10 minutes, max 96/108/68 min, across 8 distinct days and all three zones.
    >99% of blind time reports `idle`, not unknown/None. CONFIRMED NOT A CLOUD PROBLEM: the Carrier APP
    showed "Cooling" for zones 1 and 3 at the same moment the integration showed "Idle" (operator screenshots,
    14:53). The cloud HAS the right answer. ha_carrier derives hvac_action solely from `status_zone.conditioning`
    (/config/custom_components/ha_carrier/climate.py:190-195). RULED OUT: the post-write intercept guard
    — its own docstring says volatile status (temperature, humidity, CONDITIONING, blower) is "deliberately
    excluded".'
  WHY_update_entity_IS_UNLIKELY_TO_WORK: The mechanism EXISTS — ha_carrier entities are CoordinatorEntity
    (carrier_entity.py:17), so update_entity -> async_update() -> coordinator.async_request_refresh().
    BUT that is THE SAME FETCH PATH as the periodic poll, and DEFAULT_UPDATE_INTERVAL_MINUTES = 30 (const.py:46).
    Blind episodes ran 96, 108 and 68 minutes — roughly THREE scheduled polls inside one blind window
    without clearing it. Polling sooner cannot fix a value that survived three polls. RELOAD does clear
    it (operator-observed). Reload tears down and rebuilds the carrier_api client and its session; refresh
    reuses them. That points at a STALE CLIENT/SESSION object — a fault class update_entity does not touch.
  PROBE_C_STILL_WORTH_RUNNING: 'A blind-episode detector is running (polls all three zones every 40s,
    requires 3 consecutive blind reads). When it fires: capture state, call homeassistant.update_entity,
    re-read; if unchanged, reload the config entry and re-read. ONE episode settles it. Blind episodes
    occurred on 8 of 8 observed days, so the wait is short. THE RESULT MATTERS EVEN THOUGH D9 IS CUT:
    if update_entity works, this card becomes a cheap periodic task. If only reload works, the card becomes
    a harder cost/benefit call (reload has lifecycle blast radius — see feedback_parent_reload_watchdog_hazard)
    or an upstream bug report. DO NOT SHIP A REFRESH THAT DOES NOT REFRESH.'
  SCOPE_NOTE_WHY_IT_IS_SEPARABLE: 'The pipeline-hardening cycle does NOT need this. Its Gate-4 fix routes
    detection through SPAN power draw via _read_kwh_rate, which is independent of anything ha_carrier
    reports. That independence is a stated non-goal in that plan and is mutation-drilled. What this card
    serves is the OPERATOR-STATED separate concern: HVAC ops responsiveness generally — preset application,
    dwell/vacancy resolution and anything else that reads climate state may still be acting on a stale
    picture for up to 1.8 hours. That is a real cost and it is NOT addressed by the ramp fix.'
  parsimony:
    problem: a third-party integration reports stale HVAC state for up to 1.8h; only a reload clears it
    verdict: INVESTIGATE
    note: probe first; the fix may be upstream rather than ours. Do not build a periodic no-op.
  PROBE_C_WAS_NOT_ACTUALLY_RUNNING_2026_08_23: 'Card said "detector running" -- it was NOT. The 08-22
    15:02 run was single-shot with a 3300s (55-min) deadline and printed to a background stdout that did
    not survive the session. So it expired ~15:57 on 08-22 having produced NO recorded result, and this
    card has been parked on a probe that stopped existing 19 hours ago. Recording because the failure
    mode is generic: a probe whose result lands only on stdout is indistinguishable, a day later, from
    a probe that found nothing. Probe output must go to a FILE. RELAUNCHED 2026-08-23 as blind_watch_v2.py:
    6-hour horizon (was 55 min), unbuffered line-flushed emit to a file, exits on FIRST confirmed episode
    so the harness re-invokes me and I can run the intervention (update_entity -> re-read -> reload ->
    re-read) live via MCP while the episode is still open. Detection criteria unchanged from the original:
    per-zone SPAN draw above threshold (1500W z1 / 2200W z2,z3) with hvac_action != cooling, 3 consecutive
    40s reads. Base rates say the wait is short: blind episodes occurred on 8 of 8 observed days, 91-95%
    of blind time in episodes over 10 minutes.'
  PROBE_C_RELOCATED_TO_HA_HOST_2026_08_23: Second launch was KILLED before firing (no output). Root cause
    of the fragility, the probe was tethered to my session, so anything that reaps my background processes
    also reaps the probe. Relocated to run DETACHED ON THE HA HOST itself, nohup python3 -u /tmp/carrier_blind_watch.py
    writing /tmp/carrier_blind_watch.out, 6h horizon, exits on first confirmed episode. Now survives my
    session entirely. NOTE only /tmp is writable for the ssh user on this box, /config /share /root /data
    /media are all read-only, so the script does not survive an HA restart or addon restart, relaunch
    if that happens. TRADE-OFF, because it is detached nothing notifies me when it fires, so this must
    be POLLED, cat /tmp/carrier_blind_watch.out. Committed copy at scripts/probes/carrier_blind_episode_detector.py.
  next: Probe C RELAUNCHED 2026-08-23 (6h horizon, file-backed, fires on first episode). On fire, run
    update_entity then re-read, then reload then re-read, live while the episode is open. Then choose
    among (a) periodic update_entity if it works; (b) periodic reload with the blast-radius cost explicitly
    accepted; (c) upstream bug report to ha_carrier with the measured evidence; (d) accept and design
    around it as the ramp fix already does. Also still unsized, whether OTHER URA consumers of climate
    state (preset application, dwell, vacancy) are materially degraded during blind episodes.
  links:
    related:
    - RAMP-GATE4-HVAC-ACTION-LEVER-LEAK-1
    context: Same root defect. That card covers URA no longer DEPENDING on the bad signal for ramp detection;
      this card covers making the signal itself fresher for everything else that reads it. Fixing either
      does not fix the other.
  refs:
  - /config/custom_components/ha_carrier/climate.py:190-195
  - /config/custom_components/ha_carrier/const.py:46
  - carrier_entity.py:17
- id: RAMP-GATE4-HVAC-ACTION-LEVER-LEAK-1
  title: 'Ramp detection is vetoed by hvac_action == cooling — a CLOUD-REPORTED CLAIM gating a power-based
    feature. Measured: Bryant reported idle for 18 min while drawing 4.2 kW.'
  thread: hvac
  status: shipped_organic
  shipped_version: v5.89.0
  created: 2026-08-22 14:05
  updated: 2026-08-23 14:30
  refinement_status: initial
  approval: unreviewed
  tags:
  - measured-not-inferred
  - lever-leak
  - operator-instinct-confirmed
  OPERATOR_CAUGHT_THIS: 'Operator, on being told detection gates on hvac_action: "Hmm I thought it was
    draw and target attainment. Is that a third thing. Does feel like we should change it. Maybe we should
    model that carrier Bryant actuation a bit more. It might be a lever leak." THEY WERE RIGHT ON ALL
    THREE COUNTS. Draw and target attainment ARE gates — but there is a THIRD gate AHEAD of both, it is
    a hard veto, and "lever leak" is the precise description.'
  THE_GATE_VERIFIED: 'Read from source, not relayed. `check_ac_reset` (hvac_override.py:2684) gate ladder,
    from its own docstring plus the code: Gate 4  ->  if zone.hvac_action != "cooling": continue        (~hvac_override.py:2779)
    Gate 6  ->  overshoot / target attainment Gate 7  ->  kwh_rate > zone threshold for N consecutive
    samples Gate 4 RUNS FIRST. Detection can never reach the draw test unless the thermostat CLAIMS to
    be cooling. The ramp is not deciding not to act — it is never asked.'
  MEASURED_INSTANCE: 'Zone 1, 2026-08-22 13:29-13:47 CDT, from the HA recorder: 13:29  hvac_action=idle       11
    W 13:30  hvac_action=idle    2,105 W 13:32  hvac_action=idle    3,265 W 13:37  hvac_action=idle    3,947
    W 13:42  hvac_action=idle    4,181 W 13:47  hvac_action=idle    4,236 W 8 of 10 climate state-writes
    showed idle while above 1.5 kW. Room was 80F against a 76F target, blower_rpm ~1000, `conditioning:
    idle` on the thermostat too. A blower alone is a few hundred watts; 4.2 kW is compressor-scale. So
    the compressor ran for ~18 minutes while the thermostat reported idle, and Gate 4 vetoed detection
    throughout. CAVEATS, stated because the sample is small: the climate entity only writes on CHANGE,
    so this is 10 samples clustered in one cycle-start. It may be CLOUD LAG that resolves in minutes rather
    than a sustained misreport. And in THIS instance nothing was lost — the room was 4F ABOVE target,
    so the ramp correctly should not fire anyway. The blindness only costs something when the same misreport
    happens AT setpoint, which is when the ramp is supposed to act. Note also that 914 nudges fired in
    30 days, so detection plainly works much of the time.'
  THE_STRUCTURAL_ARGUMENT: 'Independent of how large the effect turns out to be, the DESIGN is inverted.
    `hvac_action` is a MOMENTARY, CLOUD-REPORTED CLAIM from a Carrier/Bryant cloud this repo already knows
    is unreliable (it 504s and drops all three zones together — see ARRESTER-CLOUDFLAP-FALSEPOS-1). POWER
    DRAW IS GROUND TRUTH: the compressor either is or is not consuming kilowatts. Gating a POWER-BASED
    feature on a cloud claim about power is the coupling that fails silently. PROPOSED TRUST ORDER (direction,
    not a spec — do not build before the sizing probe returns): - DRAW = ground truth that the compressor
    is running. - hvac_mode / state (heat_cool vs heat) = which direction the system is working in. Stable
    CONFIG rather than momentary state, so it is safe to gate on — and it is what actually prevents a
    cooling-nudge being applied during heating, which is the real job Gate 4 was doing. - hvac_action
    = CORROBORATION AT MOST, never a veto. It is the only input that is both momentary AND cloud-reported.
    Operator framing to preserve: "model that carrier Bryant actuation a bit more" — the fix is to represent
    what the equipment is actually doing, not to trust what the cloud says it is doing.'
  WHY_THIS_MAY_REDIRECT_THE_WHOLE_CYCLE: 'AC-RAMP-NO-RECURRENCE-ESCALATION-1 assumes the pipeline is DETECT
    -> NUDGE -> (never escalates). Operator framing of the problem was: "either nudges did not work, we
    did not escalate to resets, or something is off." The probe work of 2026-08-22 has now REJECTED the
    count-based escalation trigger (see that card §7.1) and found no discriminating signal for the incident
    night. If Gate 4 vetoes a material share of genuine high-draw time, then the answer is the THIRD option
    — something is off — and the hardening direction is DETECTION RELIABILITY, not escalation. Every hour
    spent on recurrence triggers would have been aimed at the wrong stage of the pipeline.'
  parsimony:
    problem: a cloud-reported momentary claim can silently veto the entire power-based ramp feature
    verdict: INVESTIGATE
    note: sizing probe running; do NOT change the gate before it returns. A negative result (Gate 4 vetoes
      very little) is a valid outcome and would close this.
  next: 'Await the sizing probe: per zone, what share of time-above-threshold is vetoed by hvac_action
    != cooling; what share of THAT is also at-or-below setpoint (the costly subset, i.e. the exact modulation-floor
    case); and whether blind episodes look like short cloud lag or sustained misreport. THEN decide. If
    the effect is small, close this card. If large, the fix is the trust-order inversion above and it
    likely precedes any escalation work.'
  links:
    related:
    - AC-RAMP-NO-RECURRENCE-ESCALATION-1
    - ARRESTER-CLOUDFLAP-FALSEPOS-1
    context: The escalation card is currently blocked because its count-based trigger was rejected by
      data; this card may explain WHY the pipeline underperforms and would take precedence over it. ARRESTER-CLOUDFLAP-FALSEPOS-1
      documents the same Carrier/Bryant cloud being unreliable, which is the reason hvac_action should
      not hold a veto.
  refs:
  - hvac_override.py:2684 (check_ac_reset gate ladder)
  - hvac_override.py:2779 (Gate 4)
  organic_evidence:
  - '2026-08-23 watch-pass: v5.89.0 NOT INSTALLED as of watch pass (running v5.88.1). Unwatchable until
    operator installs HACS update. H1 PENDING.'
- id: BORROW-BANKING-LEASE-NOT-RELEASED-1
  title: A banking borrow sat 2h past its lease expiry without release — nudge borrows return cleanly,
    banking did not
  thread: hvac
  status: done
  shipped_version: v5.91.2
  disposition_2026_08_28: 'SHIPPED v5.91.2 (HVAC-D1 _auto_release_sweep at hvac.py:1241, wired via backref at :391). Lease-expiry auto-release for kind=banking now exists — mirrors nudge. Only residual is the live observation of a real lease_expiry firing, which rides HVAC-D1 organic watch (not a separate card). Closing.'
  approval: needs_operator
  updated: '2026-08-28 22:00'
  tags:
  - operator-observed
  - live-instance
  origin:
    date: '2026-08-22'
    gist: Operator asked the direct question "So are borrows working or not?" during the AC-ramp pipeline-hardening
      cycle. Checking the borrow ledger (NOT the stale climate entity) answered yes for nudge and surfaced
      this as a live exception.
  measured: 'From sensor.ura_hvac_coordinator_governed_thermostat_borrows state history, read at 2026-08-22
    20:46 CDT. NUDGE BORROWS — HEALTHY, two independent oracles agreeing: 02:08 state 1 active_borrows[nudge
    zone_1] -> 02:10 state 0 started{nudge:1} returned{nudge:1} 03:02 state 1 active_borrows[nudge zone_1]
    -> 03:04 state 0 started{nudge:2} returned{nudge:2} restore_failed_today {} empty in both. Month-scale
    from ac_ramp_events: 912 of 914 nudge_started rows paired with a nudge_restored inside 30 min; only
    2 unmatched since 07-23. THE EXCEPTION — one banking borrow open past expiry: kind=banking, site=S12_pre_cool,
    zone_2 (climate.up_hallway_zone_2) excursion_id banking:zone_2:1787435377461 started  2026-08-22T21:49:37Z
    (16:49 CDT) expires_at 2026-08-22T23:49:37Z (18:49 CDT)  -- a 2h lease still active at 20:46 CDT =
    ~4h open, ~2h PAST EXPIRY returned_today {} empty; no sensor state row written since 16:49. So whatever
    is meant to release an EXPIRED lease did not fire. This is the first banking-kind borrow visible anywhere
    in the ledger.'
  finding: 'TWO CANDIDATE MECHANISMS, NOT YET DISTINGUISHED — they need different fixes, so do not scope
    until one is ruled out: (a) LEDGER-ONLY LEAK. The thermostat was actually restored but return_excursion
    failed to clear the record, so the sensor shows a phantom active borrow. Evidence FOR: zone_2 target_temp_high
    currently reads 76, which is EXACTLY the pre_target_high captured in the borrow snapshot. If the pre-cool
    had lowered the target and never returned it, 76 is not what we would expect. (b) GENUINELY OPEN.
    The borrow is live and something else wrote 76 independently. DISCRIMINATOR: read the excursion primitive
    persistence (the lease row) directly rather than the sensor, and check whether an auto_release / lease-expiry
    path exists for kind=banking at all — the nudge path returns via its own explicit return_excursion
    at the end of _restore_after_nudge, which banking (S12_pre_cool) does not share. A plausible shape
    is that banking relies ENTIRELY on lease expiry while nudge never needs it, so an expiry bug would
    be invisible on the nudge path and total on the banking path. VERIFY THIS, do not assume it — it is
    a hypothesis from reading the call sites, not a measurement. NOTE on preset snapshot: pre_preset was
    away and the zone now reads home. That is NOT evidence of a restore failure — the house state can
    legitimately have moved away->home in four hours. Do not score it either way.'
  next: 'Scope: add a lease-expiry auto-release for kind=banking (writes an ended_ts + restores preset),
    mirroring nudge. Verify no existing banking-release call in hvac code first. Buildable; investigation
    done 2026-08-25.'
  WEAK_EVIDENCE_WARNING: 'restore_failed_today is NOT a reliable all-day signal. The daily counters were
    observed resetting mid-day: started_today went {nudge:2} at 03:04 -> {} at 13:27, which looks like
    they do not survive a restart. So an empty restore_failed_today only covers the period since the last
    restart, not the day. The nudge health conclusion above rests on the month of ac_ramp_events rows,
    which DO persist — not on this field.'
  NOT_THE_PRESET_QUESTION: 'Distinct from the preset-landing question, which is UNRESOLVED for a different
    reason: ha_carrier does not write HA state after a preset service call (climate.py:381 has no async_write_ha_state
    / async_request_refresh; DEFAULT_UPDATE_INTERVAL_MINUTES = 30), so preset restore cannot be verified
    through the HA state object at any delay under the poll interval. See CARRIER-STALE-POLL-REFRESH-1.
    Every preset-restore FAIL verdict recorded before the pipeline-hardening cycle is inadmissible and
    must not be cited as evidence against the borrow.'
  refinement_status: refined
  investigation_2026_08_25: 'RESOLVED to failure-mode (a): kind=banking has NO working release/end path.
    Live DB: hvac_excursion_state holds 2 OPEN banking rows; hvac_excursion_events (the ENDED table) has
    0 banking rows (only 7 ''compromise''). Banking excursions start and never end. Nudge restores via
    a DIFFERENT table (ac_ramp_events, 74 restore_ok) — which is why ''nudge returns cleanly, banking
    did not''. Investigation the card gated on is COMPLETE; ready to scope a fix: give banking a lease-expiry/auto-release
    path (mirror nudge''s restore + the borrow-kind ending S14 will also want). Confirm the code has no
    banking release call before building.'
- id: AC-RAMP-NO-RECURRENCE-ESCALATION-1
  RESET_MECHANISM_EVIDENCE_2026_08_22_PM: 'ORCHESTRATOR ERROR CORRECTED FIRST, because it was argued to
    the operator and must not be inherited: I argued escalation was pointless on the grounds that a hard
    reset turns the AC off for 60s while a nudge already buys ~19 min of off-time. That is the WRONG AXIS
    and the operator rejected it correctly -- "it is tunable" and "it creates total ramp down and re-evaluation
    of setpoint maintenance energy. The turning off is not the point." The value of a reset is CONTROL-STATE
    CLEARING (modulation-floor release), not off-duration. The duration argument is withdrawn. It should
    never be cited against this card again. NEW EVIDENCE FOR THE CARD MECHANISM (SPAN per-zone circuits,
    7-day recorder window, n=290 nudges): post-recovery draw compared against pre-nudge draw, tight windows
    (t-12..t-1 min vs t+25..t+45 min, on-samples only): zone_1 n=148 before 2657W after 2444W ratio 0.965
    | LOWER(<0.9) 28% SAME(0.9-1.1) 52% HIGHER 20% zone_2 n= 83 before 2323W after 2260W ratio 0.983 |
    LOWER 25% SAME 61% HIGHER 13% zone_3 n= 59 before 2652W after 2562W ratio 0.977 | LOWER 29% SAME 58%
    HIGHER 14% READ: the nudge does NOT clear control state. The unit comes back at the SAME output stage
    the majority of the time; median improvement ~3%, inside noise. This is a DIRECT power-side confirmation
    of the card mechanism ("the nudge never clears the state that is holding the floor"), previously argued
    only from return-TIME (median 365s to 50% of baseline). Two independent framings now agree. WHETHER
    A RESET DOES BETTER IS STILL UNTESTED -- and this is the honest bound: * Only ONE of the 9 hard resets
    falls inside the 7-day fine-grained recorder window. n=1. * Falling back to hourly statistics (which
    DO reach back to 2025-03) gives: HARD RESET n=9 median after/before 0.68; NUDGE n=884 median 0.86;
    NO-INTERVENTION CONTROL n=6122 median 0.87. * Reset LOOKS better. It is NOT significant. The nine
    reset values are 0.13 1.83 0.64 0.77 1.04 0.57 0.38 0.68 1.51 -- spread entirely inside the control
    IQR (0.59-1.16) at n=9. * THE METHOD-KILLER, and the reason no conclusion may be drawn from that table:
    the hourly method scores NUDGES at 0.86 against a control of 0.87 -- i.e. NO EFFECT -- when the fine-grained
    data proves nudges take draw to ~0.006 kW for 19 minutes every single time. A method with no power
    to detect an effect it is known to be looking at cannot be trusted about resets either. DO NOT use
    hourly statistics for this question. Fine-grained states only. NEW AND MATERIAL -- THE 60s OFF-WINDOW
    MAY BE BELOW THE ACTUATION FLOOR: measured command-to- physical-response lag (nudge command -> AC
    circuit draw falls below 30% of baseline, SPAN, fine-grained): zone_1 p50 72s p90 131s | zone_2 p50
    83s p90 142s | zone_3 p50 101s p90 159s. AC_RESET_OFF_DURATION_SECONDS is 60. THE OFF-WINDOW IS SHORTER
    THAN THE MEDIAN TIME THE UNIT TAKES TO RESPOND TO A COMMAND AT ALL. If that lag applies symmetrically
    to the off command, a 60s reset may be partially or wholly absorbed by the response deadband -- which
    would mean the historical resets never delivered the mechanism this card is built on, and their weak/noisy
    signal is EXPECTED rather than evidence against the mechanism. CAVEAT, stated so it is not over-read:
    this lag bundles cloud round-trip WITH compressor response and does not separate them; D10 (deferred)
    is the deliverable that would. But it is a strong prior that the rung-3 knob default must be RAISED
    off 60s, and it reframes the 9 historical resets as probably-underpowered. SERIES BOUNDARY FOR ANY
    FUTURE ANALYSIS: the nudge knobs changed once. started->evaluated gap by ISO week: W30 = 8.00 min,
    W31 onward = 6.00 min (duration 2 min + eval delay 240s). Any analysis spanning 2026-07-28 must treat
    W30 (168 nudges) as a separate series. 738 of 914 nudges are on the current setting.'
  ESCALATION_PROBE_SPEC_2026_08_22: 'WHAT WOULD SETTLE IT -- operator already proposed the shape ("can
    we reset manually like the nudge button and learn something"). This is a PROBE, not a cycle, and it
    is the go/no-go gate on building any escalation trigger. Per measure-before-build. DESIGN: fire N
    hard resets deliberately via the manual path, each matched against nudge controls in the same zone
    under similar conditions, and measure with FINE-GRAINED SPAN states (not hourly): (a) post-recovery
    draw ratio, same windows as the n=290 nudge measurement above, so the numbers are directly comparable
    to the table in the block above; (b) reset_outcome (floor_survived vs justified_ramp) from the D6
    temp discriminator; (c) kwh_rate_settle (the draw-side companion, see CURRENT_CYCLE_ARMOR below);
    (d) sweep the off-duration knob across at least 60s / 120s / 180s, because the actuation-lag finding
    above says 60s may be below the floor. A probe that only tests 60s risks concluding "resets do not
    work" when the real answer is "60s does not work". TARGET n: 10-15 resets with matched controls. Days,
    not weeks. Respect the existing per-day and min-interval caps -- do NOT disable compressor protection
    to run the probe; goal 3 of v4.5.11 ("rapid compressor cycling is the worst possible failure mode")
    still binds. DECISION RULE, stated in advance so it cannot be retrofitted: build the escalation trigger
    ONLY IF reset post-recovery ratio is materially below the nudge ratio (0.97) at a duration that is
    operationally acceptable. If resets match nudges at every duration tested, the modulation-floor- clearing
    mechanism is refuted and this card closes NO-BUILD. DO NOT build a trigger before this probe returns.
    Four separate probes have now proposed escalation triggers on mechanisms that did not survive contact
    with data; the failure has always been building the trigger before establishing that the thing being
    escalated TO actually works.'
  CURRENT_CYCLE_ARMOR_2026_08_22: 'WHAT THE IN-FLIGHT PIPELINE-HARDENING CYCLE CONTRIBUTES TO THIS CARD.
    This card is DELIBERATELY NOT in that cycle -- no escalation trigger is being built -- but the cycle
    lays every prerequisite, so the probe above becomes runnable the moment it ships: * D6 -- room temp
    at reset-start and at restore+AC_RESET_OUTCOME_SETTLE_S, producing reset_outcome (floor_survived /
    justified_ramp / inconclusive). THE drift discriminator this card names as a PREREQUISITE. Without
    it a survived-floor is indistinguishable from a justified ramp and the probe cannot score anything.
    * kwh_rate_settle -- NEW, requested mid-build 2026-08-22. Draw captured at the SAME settle moment
    as D6 temp, same read path, same UPDATE, no new timer. Rationale: D6 temp answers "was the subsequent
    ramp justified"; it does NOT answer "did the unit come back at a LOWER STAGE", which is the actual
    reset mechanism. Without this, reset rows stay unanalysable on the power axis exactly as the 9 historical
    resets are today. * D7 -- AC_RESET_OFF_DURATION_SECONDS promoted from rung-1 module constant to a
    rung-3 Number entity. This is what makes the duration SWEEP in the probe possible with zero code.
    Given the actuation-lag finding, this is now load-bearing for the probe, not merely hygiene. * D5
    -- hard-reset row enrichment (preset/mode/restore_ok on the reset path). Reset rows today carry almost
    nothing, which is part of why n=9 was so thin. * D3 -- soft-nudge daily cap. Bounds the runaway (31/day
    observed against a specced 6) while the escalation question stays open, so the open card is not costing
    energy meanwhile. * D-GATE4 -- draw-based cooling predicate. Removes the stale-cloud blind window
    (12.2/7.1/12.9% of high-draw time) that would otherwise corrupt the probe sample. NET: after this
    cycle, the probe is a measurement exercise. Before it, the probe is unbuildable.'
  RESET_DRIFT_CONSTRAINT_2026_08_22: 'OPERATOR REFINEMENT, and it changes how reset success must be MEASURED:
    "If the temp delta is high it can come back with the temp already elevated above setpoint. At which
    case it will make its own decision - justifiably." NOTE THIS IS A DIFFERENT CLAIM FROM THE ONE THE
    PROBE REFUTED. DELTA_T_PROBE tested whether the nudge TRIGGER is load-driven and found it is not (draw
    at setpoint flat ~2.5 kW across 80-100F outdoor). This claim is about the RESET OFF-WINDOW, where
    drift rate genuinely does scale with delta and with the zone thermal mass. The earlier refutation
    does NOT apply here; do not cite it against this. THE MEASUREMENT TRAP — two outcomes are INDISTINGUISHABLE
    from the power trace alone and mean OPPOSITE things: (a) unit returns, ramps to full, ROOM STILL AT
    SETPOINT  -> the modulation floor SURVIVED the reset. Reset FAILED. (b) unit returns, ramps to full,
    ROOM DRIFTED ABOVE SETPOINT -> real demand exists. Reset WORKED; the unit is behaving correctly. Scoring
    these the same would classify a correctly-behaving system as a failure and escalate against it — i.e.
    cycle a compressor for no reason, which goal 3 of the original design calls "the worst possible failure
    mode". This is the SAME class of error as instantaneous-vs-durable `effective` for nudges: measuring
    the instant instead of the outcome. THE DISCRIMINATOR, and it needs no new instrumentation: RECORD
    ROOM TEMP AT RESET-START AND AT RESET-END. The delta across the off-window IS both the drift measurement
    and the discriminator. If temp_end > target, case (b) and the subsequent ramp is expected — do not
    count it against the reset. CONSEQUENCE FOR THE OFF-DURATION KNOB: it should NOT be a flat constant.
    The safe window is bounded by drift rate, which varies by zone (zone 3 — kitchen + hallways, high
    infiltration and the transit zone — will drift materially faster than zone 1) and by conditions. Start
    with a measured constant per the knob ladder, but design the evaluation so an adaptive or per-zone
    window can be added later WITHOUT re-defining reset success. Capture the drift data from day one even
    if the window stays fixed — that is what makes the later tuning possible.'
  LIVE_INSTANCE_AND_MECHANISM_2026_08_22: 'OPERATOR PERFORMED ANOTHER MANUAL RESET at ~02:00 CDT on 2026-08-22,
    hours after v5.88.0 shipped. RULED OUT AS A REGRESSION FIRST (the deploy touched every thermostat
    write path, so this had to be excluded before attributing it to the known gap): borrow machinery healthy
    — started_today {nudge: 1}, returned_today {nudge: 1}, restore_failed_today {} empty, active_borrows
    [] empty; zone_1 on preset `sleep` NOT `manual`, so the founding lockout defect is NOT recurring post-deploy.
    This is the known escalation gap. OPERATOR DESCRIPTION, verbatim and worth preserving because it articulates
    the MECHANISM better than the card previously did: "I saw high wattage when the setpoint had been
    reached. You would expect the ac to ramp down it a variable compressor. Yet it was 2+kw running. When
    that happens I tend to reset. Off and back to auto to reclaim some energy. If I am lucky it is short
    enough that when it gets back it is the same temp and it read that as no need to ramp back up to full
    power." WHY THE RESET WORKS AND THE NUDGE DOES NOT — this is the missing half of the design rationale.
    A NUDGE changes the SETPOINT temporarily; on restore the unit resumes its PRIOR CONTROL STATE and
    climbs back (measured: median 365s to return above 50% of baseline). A RESET clears that control state
    — the compressor restarts its loop, re-reads current temp against target, finds itself ALREADY AT
    SETPOINT, and elects low demand instead of resuming the previous ramp. That is why the hard reset
    is the correct escalation for a MODULATION-FLOOR failure and why repeating the nudge 47 times cannot
    fix it: the nudge never clears the state that is holding the floor. COROLLARY — RESET DURATION IS
    A LOAD-BEARING PARAMETER, not an arbitrary constant. The operator "if I am lucky it is short enough"
    is doing real work: the off-window must be long enough for the unit to drop its control state, SHORT
    enough that room temp has not drifted so far that it justifiably ramps back to full. Too long and
    the reset defeats itself. THE CONSTANT — LOCATED (operator was right that one exists; the orchestrator
    first search missed it by grepping for the NAME it expected rather than the concept): AC_RESET_OFF_DURATION_SECONDS:
    Final = 60   # hvac_const.py:493, "seconds to hold off during reset" consumed at hvac_override.py:2906
    as the async_call_later delay before _restore_after_reset, and interpolated into the NM alert text
    at :2915. IT IS RUNG 1 (module constant) — NOT exposed in config_flow, options_flow or as a Number
    entity. Per the knob ladder it belongs at RUNG 3: this is policy the operator would legitimately tune
    by observation (their manual technique IS this parameter), not a safety bound that should require
    review to change. Sibling constants for reference: AC_RESET_MAX_PER_DAY=2 (:491), DEFAULT_HVAC_AC_HARD_RESET_MIN_INTERVAL=120
    min (:538), AC_RESET_STUCK_MINUTES. NO TEMP CAPTURE EXISTS AT RESET START OR END — verified across
    hvac_override.py:2890-2968. So the drift discriminator described below CANNOT be computed for any
    past reset, and the 11 historical resets (2026-07-22 -> 2026-08-15) mostly predate the recorder 7.5-day
    window, so they are not reconstructible either. Capturing temp at reset-start/end is therefore a PREREQUISITE
    deliverable, not an optional enrichment — without it there is no way to tell a survived-floor from
    a justified ramp, and no baseline to tune the window against. PRIORITY EFFECT: this is now a MEASURED,
    RECURRING, OPERATOR-COSTING defect with a live instance days apart (2026-08-21 overnight, 2026-08-22
    02:00). It is doing manual labour that the automation was designed to do.'
  title: The AC ramp-down nudges forever and never escalates — 47 "effective" nudges overnight while the
    house burned 49 kWh; hard reset has not fired since 2026-08-15
  thread: hvac
  status: shipped_organic
  shipped_version: v5.89.0
  updated: 2026-08-23 14:30
  approval: needs_operator
  tags:
  - measure-before-build
  - operator-observed
  origin:
    date: '2026-08-21'
    gist: 'Operator: "I woke up to the ACs drawing 10kw this morning after cooling overnight. With charging
      and this we had burned 49kw overnight before the day started. I manually stopped and restarted the
      AC and when they cycled back up they reclaimed only half of that because the rooms had reached set
      point long ago." Asked whether AC reset is working — they believed it was on.'
  design_intent: 'From PLANNING_v4.5.11 (the objective doc, read to orient). The v4.5.11 cycle deliberately
    FLIPPED the failure mode: the old AC Reset triggered on "AC running but room never reaches setpoint"
    (undersized/low-refrigerant), which never fires in this house. The real Texas failure mode is "AC
    REACHES setpoint, then keeps cooling and burning kWh past the natural cycle-end" — the exact thing
    the operator saw this morning. The designed ladder is: detect overshoot + sustained kWh -> SOFT NUDGE
    (+1.5F for a few minutes so the variable-speed Bryant ramps down without cycling) -> if the nudge
    FAILS (kWh still high after ~10 min observe) -> escalate to HARD RESET (off, 60s, restore). Caps specced
    as 6 soft nudges + 2 hard resets per zone per day, 30 min between nudges, 2 h between hard resets,
    per-zone lockout. Goal 3 verbatim: "Hard reset retained as escalation only... because rapid compressor
    cycling is the worst possible failure mode."'
  measured: 'All from the live DB on the HA host (ssh; the Samba mount cannot open it — WAL). OVERNIGHT
    2026-08-20 18:00 -> 2026-08-21 08:00 CDT: 47 nudge_started events — zone_1 22, zone_2 10, zone_3 15.
    EVERY ONE of the 47 nudge_evaluated rows is classified effective=1, avg kwh_rate_before 2.51 kW ->
    avg kwh_rate_after 0.18 kW. Typical row: current_temp 76.0, target_high 76.0 (i.e. AT setpoint), kwh_avoided
    ~0.7-0.97 per nudge. DAILY TOTALS in ac_reset_state: zone_1 soft_nudge_count 31 on 08-20, 34 on 08-19,
    already 14 by 07:05 today; zone_2 20 on 08-20; zone_3 12 today. HARD RESETS: hard_reset_count = 0
    for every zone on 08-18, 08-19, 08-20, 08-21. In ac_ramp_events, hard_reset_started/completed occur
    11 times total, spanning 2026-07-22 17:51 -> 2026-08-15 04:54, and ZERO in the six days since.'
  finding: 'TWO SEPARATE DEFECTS, and neither is "the feature is off" — the soft nudge is working hard
    and is being credited for it. (1) NO RECURRENCE TRIGGER. Escalation to hard reset is gated on ONE
    condition only — the nudge being classified INEFFECTIVE (hvac_override.py:3510-3520, `if escalate:`).
    Every nudge succeeds: 2.51 kW collapses to 0.18 kW. Then ~30 minutes later the load returns and the
    cycle repeats, 47 times in one night. The design has a concept of "this nudge failed" but NO concept
    of "this nudge keeps succeeding and the problem keeps coming back." So the second rung of the ladder
    is structurally unreachable in the steady state that actually dominates this house. The operator''s
    manual off/on IS the hard reset — performed by hand because the automation would never get there.
    (2) THE SOFT-NUDGE DAILY CAP WAS SPECCED BUT NEVER BUILT. The plan says 6 soft nudges per zone per
    day. There is no such constant and no enforcement anywhere — DEFAULT_HVAC_AC_HARD_RESET_DAILY_LIMIT
    (=2) exists at hvac_const.py:527, but there is no soft-nudge sibling. Observed 31/day against a specced
    cap of 6. The 30-minute min interval IS being honoured (that is why the count lands near 2/hour),
    so the interval knob shipped and the cap did not. SAVINGS ACCOUNTING IS THEREFORE SUSPECT: ~0.7-0.97
    kWh "avoided" x 47 nudges implies ~35 kWh avoided overnight, on a night the operator measured 49 kWh
    actually burned. Each nudge is credited for a load that returns half an hour later. Do NOT treat ac_ramp_savings_*
    as validated until the double-count question is settled — that is a third, quieter finding.'
  RESOLVED_NO_REGRESSION_2026_08_21: 'THE 08-15 CUT-OFF IS NOT A REGRESSION — settled by the discriminating
    query, so nobody should hunt for a code change. Since 2026-08-15 there have been 308 nudge_evaluated
    rows: 307 effective, ONE ineffective. That single ineffective row is 2026-08-15T04:53:14 on zone_2
    (kwh 2.02 -> 1.98, kwh_avoided=0.000) — and it is precisely the one that produced the last hard_reset_started
    at 04:53:14 / completed 04:54:15. So the escalation path is ARMED AND WORKING; it simply has not met
    its trigger in six days because the nudge keeps succeeding. Operator''s instinct that AC reset is
    "on" is correct — it is on, it is functioning, and that is exactly why it never fires. This collapses
    the card to ONE defect (no recurrence rung) plus the missing soft-nudge cap, and removes the phantom
    "something broke on 08-15" thread entirely.'
  open_question_RESOLVED: 'WHY DID HARD RESET STOP ON 2026-08-15? ANSWERED above — it did not stop, its
    trigger stopped occurring. It fired 11 times between 07-22 and 08-15 and never since. Checked git
    log 08-13 -> 08-17: every commit in that window is guest-census work, nothing touching HVAC or the
    ramp path, so there is no obvious code cause. The benign explanation is that it simply never met its
    trigger again because nudges kept classifying effective — in which case 08-15 is a coincidence, not
    a regression, and defect (1) fully explains today. The discriminating check is whether any nudge_evaluated
    row since 08-15 has effective=0; if none does, the trigger was never armed and there is no regression
    to hunt.'
  DELTA_T_PROBE_2026_08_21: 'OPERATOR HYPOTHESIS TESTED AND LARGELY REFUTED — "Adaptive nudging is interesting
    but I think the physics means it might not work. Ie it''s just hot. Maybe temp aware nudging is better
    ie it depends on the delta btw external and internal zone temp." The data does NOT support "it''s
    just hot", which is good news: it means the recurrence is pathological and worth escalating against.
    PROBE (no new instrumentation — external_conditions has 8,422 rows back to 2026-05-23 with outside_temp,
    joined to ac_ramp_events nudge_started within a 1h window, 2026-07-01 onward, 939 nudges): | outdoor
    F | nudges | avg kW at nudge | avg (room temp - target) | | 70 | 25 | 1.56 | 0.00 |  | 75 | 361 |
    2.14 | -0.01 |  | 80 | 196 | 2.56 | -0.01 | | 85 | 126 | 2.56 | 0.00 |  | 90 | 106 | 2.51 | -0.08
    |  | 95 | 107 | 2.57 | -0.04 | | 100 | 18 | 2.76 | -0.33 | TWO FINDINGS. (1) Draw at setpoint is essentially
    FLAT at ~2.5 kW from 80F to 100F — a 20-degree span of outdoor temperature over which heat gain changes
    enormously. Load-driven demand would climb; this does not. It has the shape of a MODULATION FLOOR
    — a minimum output the unit will not drop below — which is precisely the failure mode v4.5.11 was
    written for. (2) Normalised by exposure (nudges per external_conditions sample in each bucket): 70F
    0.068, 75F 0.243, 80F 0.234, 85F 0.179, 90F 0.130, 95F 0.167, 100F 0.222. The rate is flat-to-DECLINING
    from 75F to 90F. The condition fires at least as often on MILD nights as on hot ones. Under an "it''s
    just hot" model this curve should climb steeply. It does not. CAVEATS, stated because they bound the
    claim: (a) SELECTION BIAS — nudges only fire when kW has already exceeded the 1.30 threshold, so the
    low end of the distribution is truncated and a flat average is partly forced by construction. The
    clean version of this measurement is unconditioned kW-at-setpoint sampling from the energy tables,
    not from nudge moments. (b) the 95F/100F buckets have small exposure (639 and 81 samples) and are
    noisy. (c) max kW DOES rise with outdoor temp (2.51 -> 4.22), so load clearly modulates the top of
    the range even though the mean does not. CONSEQUENCE FOR DESIGN: sizing the nudge by delta-T buys
    little, because delta-T does not explain the variance we care about. Recommend NOT building temp-aware
    nudge sizing. Delta-T still earns a place as an ESCALATION VETO (do not hard-reset when it is very
    hot outside — a restart there means a long recovery and compressor protection matters more), which
    is a cheap guard rather than a model.'
  RECOMMENDATION_EFFECTIVE_REDEFINITION_2026_08_21: 'Operator: "We have to redefine effective so we can
    reach reset when it matters. Suggestions?" Four options, ranked, with the marginal-benefit argument.
    OPTION 2 — RECURRENCE COUNTER (RECOMMENDED, ship first). Do NOT redefine `effective` at all — it is
    a TRUE statement about the nudge (2.51 kW really does collapse to 0.18 kW). Instead add a SECOND,
    orthogonal escalation trigger: N nudge_started events on the same zone within rolling window W ->
    escalate to hard reset, subject to the existing hard-reset caps. Knobs N and W as Number entities
    (rung 3). Why first: it is the smallest change, it perturbs neither the classification nor the savings
    arithmetic, its behaviour is trivially predictable from the existing event stream, and it directly
    produces resets in exactly the situation the operator described (47 nudges overnight would trip it
    early). OPTION 1 — DURABLE EFFECTIVENESS. Redefine effective as "the load stayed down for D minutes
    after restore", D a knob set above the observed ~30-minute recurrence. Implement by retroactively
    downgrading a nudge to `ineffective_recurrent` when the same zone re-triggers detection within D.
    Truer to the word "effective" and it makes the savings number honest. More invasive: it changes the
    meaning of a persisted classification and therefore the savings ledger. OPTION 3 — ENERGY BUDGET.
    effective = zone kWh over a rolling window stayed under budget B. Closest to what the operator actually
    complained about (49 kWh), but introduces a new per-zone number to tune with no prior basis for its
    value. Park until 2 has run. OPTION 4 — DELTA-T NORMALISED EXPECTATION (the operator''s idea). Refuted
    as an effectiveness definition by the probe above — expected draw is near-flat, so this collapses
    to roughly a constant threshold and adds a model for little discriminating power. Keep delta-T only
    as the escalation veto. CAPS, per operator ("Cap makes sense but configurable. Nudges are very cheap
    so high. Resets are what worry about. But even those are low. But it has a knob"): implement the MISSING
    soft-nudge daily cap as a Number, defaulted high (e.g. 40-60/zone/day — today runs 31-34 and is not
    the problem). Hard-reset caps ALREADY EXIST as Number entities — ac_hard_reset_daily_limit (default
    2, number.py:2435-2441) and ac_hard_reset_min_interval (default 120 min) — so the balance-finding
    the operator wants is already tunable without code.'
  AC_RAMP_IS_A_MANUAL_INDUCER_2026_08_21: 'BIGGEST FIND OF THIS THREAD — it connects the AC-ramp work
    to HVAC-MANUAL-PRESET-CONTRACT-1. Operator ruling: "All of this hvac ops should escape via preset
    writes." It does not, and the failure is SELF-LATCHING. A nudge cycle is TWO RAW SETPOINT WRITES:
    _perform_soft_nudge emits set_temperature (hvac_override.py:3099) and _restore_after_nudge emits set_temperature
    (:3197). On a Carrier/Bryant a raw setpoint write flips preset_mode to `manual` — the code says so
    itself at :3095-3097. There IS a preset-preserving restore (FIX B2, :3218-3237). It has two compounding
    defects: (1) ORDERING RACE — the setpoint restore at :3197 is blocking=False and the preset write
    follows immediately; on a cloud-polled Bryant the setpoint can land AFTER the preset and its manual
    side effect overwrites it. This is the documented zone-1 clobber: 01:42:05.138 restore lands `sleep`,
    01:42:05.647 back to `manual` — 509 ms — and that was the last non-manual preset of the day. (2) SELF-DISARM
    (the latch) — the snapshot at :3086-3089 is `if _pre_preset != "manual": store else: POP`. If the
    zone is ALREADY manual when a nudge starts the snapshot is CLEARED, so that restore does nothing.
    Lose the race once and every later nudge stores nothing, restores nothing, and reinforces the state.
    The repair mechanism disarms itself the first time it fails. SCALE MAKES IT THE PRIME SUSPECT: 31-43
    nudges/day on zone_1 alone = 60-86 raw setpoint writes per day per zone, each a race, one loss permanent.
    It explains zone_1''s 14h35m manual block far better than S14 — and unlike S14 (rare) or DPM (switched
    OFF), the AC ramp runs dozens of times a day right now. THE HARD RESET IS ALSO NON-COMPLIANT: _perform_ac_reset
    restores hvac_mode only (climate.set_hvac_mode :2834 + :2869 retry), no preset write anywhere. The
    DB agrees — every hard_reset_completed row has action_taken=`restored_mode=heat_cool`. So even the
    escalation we are trying to reach leaves the zone off-preset. REQUIRED, and in the SAME cycle as the
    recurrence trigger — building escalation on a path that induces manual would multiply the problem:
    (a) deterministic restore ordering (await/verify the setpoint before writing the preset); (b) snapshot
    the INTENDED preset from the preset manager instead of an observed non-manual one, so a stuck zone
    can still recover; (c) hard reset asserts a preset on exit, not just an hvac_mode.'
  OBSERVABILITY_AUDIT_2026_08_21: 'Operator: "Inspect observability for potential improvements." WHAT
    IS GOOD: ac_ramp_events is a genuinely rich per-event ledger (4 event types per cycle: detection_fired
    -> nudge_started -> nudge_restored -> nudge_evaluated) with temps, kW before and after, both daily
    counters, lockout flag, notes and effective. ac_reset_state holds per-zone-day counters, in-flight
    nudge state and lockout. Live sensors cover ramp state and last action per zone, nudges/hard-resets
    today, false-positive rate, kWh avoided today/total, per-zone kW rate, and four savings sensors. Well
    above this repo''s average — the v4.5.12 cycle did its job. THE GAPS, ranked: (1) PRESET RESTORE SUCCESS
    IS COMPLETELY UNOBSERVED — highest value, and the reason the manual-induction above went unnoticed
    for months. ac_ramp_events has NO preset_before / preset_after columns, so "did the restore actually
    put the preset back?" is unanswerable retrospectively. Two additive columns make the defect measurable
    without a code read. (2) NO RECURRENCE METRIC — nothing exposes "nudges on this zone in the last N
    hours". The recurrence trigger needs it as a first-class value, and the operator needs it to tune
    N and W. (3) NO DURABILITY METRIC — nothing records how long the load stayed down after restore, which
    is precisely what durable effectiveness measures. (4) NO NEGATIVE-DECISION TRAIL — no event for "escalation
    considered and declined" (cap, min-interval, disabled, comfort-deferred). Today the only way to learn
    why a reset did not happen is to reason from source, which is how this whole thread started. (5) SAVINGS
    UNRECONCILED — ~35 kWh credited on a night 49 kWh was burned; nothing compares claimed savings to
    metered consumption.'
  LAYERING_AND_RETROACTIVE_2026_08_21: 'Operator: "Can we layer 1 and 2? What do we do with durable effectiveness
    if retroactive?" THEY LAYER CLEANLY — different time scales, different fields. Option 2 (recurrence)
    is the TRIGGER: cheap, immediate, reads only the existing nudge_started stream, and never consults
    `effective`. Option 1 (durable) is the CLASSIFICATION: retroactive, and its job is to stop the savings
    number and false-positive rate overstating. Because 2 never reads the field 1 rewrites, there is no
    interaction to reason about — ship 2 for behaviour, add 1 for truth. ON RETROACTIVE — DO NOT MUTATE
    `effective`. Add nullable `durable` + `durable_minutes`, written at +D when the outcome is known;
    leave `effective` untouched. Why: (a) `effective` is a TRUE statement about that instant (2.51 kW
    really did collapse to 0.18) and overwriting a true observation with a later, different question is
    how ledgers become unreadable; (b) kwh_avoided feeds CUMULATIVE and LIFETIME sensors backed by savings_lifetime_baseline,
    so flipping history needs claw-back arithmetic on a monotonic counter — a classic way to break a savings
    number, and this repo already has a savings-unification cycle behind it; (c) additive is reversible
    and needs no migration, matching the CREATE TABLE IF NOT EXISTS pattern the schema already uses. PAYOFF:
    for a while we can measure the gap between instantaneous and durable effectiveness WITHOUT moving
    any headline number, then decide on evidence whether to re-base savings on `durable`. Measure-before-build
    applied to our own ledger.'
  RESET_BUDGET_WINDOWING_2026_08_21: 'Operator: "I want it responsive. Nudges are cheap. Resets are expensive.
    Nighttime is also the time I want resets to assert. So maybe we time window reset limits so they cannot
    all be burned up during the day?" THE MECHANISM IS A RESERVE, NOT A CAP. A single daily pool of 2
    is exactly the failure the operator anticipates: two afternoon escalations leave nothing for 03:00
    — which is when the waste accumulates unobserved and when a 60-second outage costs the least comfort.
    PROPOSAL — PARTITIONED BUDGETS, NO BORROWING. Two independent counters that cannot draw on each other,
    each with its own knob. Suggested start: DAY 1, NIGHT 2, keeping the existing 120-minute min-interval
    globally. Night gets the larger share because that is where the measured damage is. SCHEMA: ac_reset_state
    is keyed (zone_id, date) and already carries hard_reset_count — add day_reset_count / night_reset_count
    as COLUMNS rather than re-keying. Additive, no migration. WINDOW DEFINITION — recommend WALL CLOCK
    (e.g. 22:00-06:00, boundary as a knob): predictable, auditable, independent of any other subsystem''s
    correctness. House state (sleep/home_night) is more idiomatic and self-adjusting, but it couples COMPRESSOR
    PROTECTION to the presence stack — and this session has already shown house state can sit wrong for
    hours. For a budget guarding hardware, predictability beats elegance. Note house-state as available
    if we want it later. N AND W — observed cadence is ~1 nudge/30 min per zone once pathological. N =
    2 within W = 90 min escalates on the second nudge, i.e. 30-60 min after the pattern starts. Genuinely
    responsive, and it cannot run away because the BUDGET bounds the damage, not the trigger. That is
    the key design point: make the trigger EAGER and the budget STRICT, rather than dulling the trigger
    to protect the compressor. A trigger that fires and is denied is INFORMATION (log it — observability
    gap 4); a dull trigger is silence. SOFT-NUDGE CAP: Number defaulted 40-60/zone/day — above today''s
    31-43 so it is a runaway guard, not a policy lever.'
  CYCLE_ANATOMY_AND_TIMING_2026_08_21: 'OPERATOR: "Nudge takes time to go through its cycle. Understand
    its cycle." / "I have a feeling the timing is off and it should close sooner but remember the decision
    tick is 5m." Correct on both counts — I read a 12-second window and drew a conclusion about a cycle
    I had not mapped. Mapped now, from a forced nudge on zone_1 with the house empty (2026-08-21): | t
    | event | elapsed | | 11:33:48 | nudge_started (+1.5F applied) | 0 | | 11:38:48 | nudge_restored (setpoint
    back, preset restore attempted) | +5 min = DEFAULT_HVAC_AC_NUDGE_DURATION | | 11:48:48 | nudge_evaluated
    | +10 min after restore = AC_NUDGE_EVALUATION_DELAY_S 600s | TOTAL ~15 MINUTES per nudge cycle. Plus
    my D1 settle read at restore+12s. THE WHY OF EACH PHASE, since the numbers are only defensible if
    their purpose is stated: - 5-MIN HOLD exists because the detection premise is kW-based on a VARIABLE-SPEED
    compressor. The +1.5F is a smaller demand signal, and the unit must be given time to actually ramp
    down AND for the power meter to reflect it. Too short and you measure the transient, not the response.
    - 10-MIN EVAL exists because the ONLY escalation trigger is "the nudge failed", and escalation means
    a hard reset — the design''s explicitly worst-case action (goal 3: "rapid compressor cycling is the
    worst possible failure mode"). A long observation window buys a confident verdict before doing something
    expensive. sample_count=119 over 600s ~= one sample per 5s, matching the SPAN meter cadence. - 12-SEC
    SETTLE (D1, new) is unrelated to effectiveness — it is only the preset verdict. THIRD CLASSIFICATION
    DISCOVERED, previously unknown to me: the classifier emits effective / ineffective / **INCONCLUSIVE**.
    My forced nudge returned classification=inconclusive with the `effective` column left NULL, because
    kwh_rate_before was ~0.004 kW (compressor already idle) — there was nothing to reduce, so the effect
    is unmeasurable. Sensible behaviour, and it matters for the redefine-effective work: any recurrence/durability
    redefinition must handle THREE outcomes, not two, and must decide whether inconclusive counts toward
    a recurrence trigger. It also means a forced nudge on an idle compressor is a VALID TELEMETRY test
    but an INVALID effectiveness sample — do not mix the two when gathering the before-number.'
  TICK_VS_TIMER_DECOMPOSITION_2026_08_21: 'OPERATOR: "even that can be shortened across the board to make
    the house more responsive in one go." The lever is real but it does NOT do what it might appear to,
    and the distinction decides whether it helps here: THE 5-MIN DECISION TICK GOVERNS: overshoot DETECTION
    latency, preset application, and the resolution of zone_entry_dwell / vacancy_grace. Shortening it
    makes the house more responsive across all of those in one change — and it is precisely the lever
    that would have helped the 2026-08-20 case where an occupant walked into an 80F zone and reached the
    thermostat about THREE MINUTES before URA could act (HVAC-HOT-ENTRY-LATENCY-1). THE NUDGE CYCLE DOES
    NOT RIDE THE TICK. The 5-min hold, the 10-min eval and the 12s settle are all scheduled via async_call_later,
    independent of the decision cycle. So shortening the tick would NOT shorten the ~15-minute nudge cycle
    at all. These are two separate levers and conflating them would produce a change that feels like it
    should help and does not. COSTS of shortening the tick, to be priced not assumed: more cloud writes
    to the Bryant/Carrier API per hour (and that cloud already 504s), more DB rows, more CPU per hour,
    and every dwell/grace value re-quantised — grace=10 stops being "2 ticks" and becomes 5 ticks at a
    2-min tick, which silently changes damping behaviour the operator deliberately tuned. A tick change
    is therefore NOT a free responsiveness win; it re-bases every timing knob in the HVAC coordinator
    at once. Sequence it as: name the literal (HVAC-TICK-LITERAL-1), then re-express the damping knobs
    in ticks-or-seconds explicitly, THEN consider shortening. CAN THE NUDGE CYCLE ITSELF CLOSE SOONER?
    Answerable from data we already hold, not by guessing. The eval window''s job is to let post_min stabilise;
    if it stabilises well before 600s the window is simply over-long. PROBE (read-only, cheap): for a
    sample of past nudges, replay the recorder power series and compute what post_min / classification
    WOULD have been at 2, 3, 5 and 10 minutes post-restore. The output is the shortest window that preserves
    the effective/ineffective/inconclusive verdict. If 3 minutes reproduces the 10-minute verdict, the
    cycle drops from ~15 min to ~8 min with no loss of confidence — which also roughly doubles how many
    recurrence samples a night yields for the escalation work. Do this BEFORE touching the knob.'
  RAMPDOWN_PROBE_2026_08_21: 'OPERATOR FIELD CLAIM: "In practice I have found that 60-120s means the Bryant
    can react to new high set point easing and ramp down. And that the return it now sees the success
    of the temp and the target and doesn''t ramp up again quickly or more measured." MEASURED AND CONFIRMED,
    precisely. Replayed 142 real nudges (2026-08-18 onward, kwh_rate_before > 1.0 kW so idle-compressor
    cases are excluded) against the HA recorder''s SPAN AC power series at ~5s cadence. RAMP-DOWN, time
    from nudge_started until power falls below 25% of pre-nudge baseline: MEDIAN 72s, p25 52s, p75 102s,
    max 179s. 104/127 within 120s. **127/127 within 180s.** The operator''s 60-120s is dead on and the
    300s (DEFAULT_HVAC_AC_NUDGE_DURATION=5min) hold is roughly 4x longer than the compressor needs. Median
    WASTED hold time = 228 seconds per nudge. POST-RESTORE, time to climb back above 50% of baseline:
    MEDIAN 365s (~6 min), and 7/142 never recovered within 15 minutes. This confirms the second half of
    the claim — on restore the unit does NOT slam back; recovery is measured. That is a real, quantified
    property we can design against rather than an impression.'
  WIDE_CYCLE_REFUTED_LOAD_MATCHED_2026_08_21: '⚠️ SUPERSEDES the block below. I reported the unmatched
    split as "real signal" for the operator''s hunch. THAT WAS PREMATURE — I split on a variable confounded
    with load and read the result before controlling for it. Controlled now, and the hypothesis DOES NOT
    SURVIVE. THE CONFOUND, measured: FAST-ramp nudges have baseline 1747W median; SLOW-ramp 2462W. Correlation
    between ramp-down time and baseline power = +0.543. More power to shed takes longer to shed — so "fast
    ramp" was largely a proxy for "lower load", not for "wasted hold time". The operator''s counter-argument
    (the 1.3kW detection threshold already excludes a near-minimum-modulation compressor) is CORRECT IN
    PRINCIPLE but does not do the work needed: the threshold excludes the idle case, yet the surviving
    population still spans 1.3kW to 4.2kW, and the two groups sit in materially different load regimes
    on either side of it. LOAD-MATCHED RESULT (276 cycles from 2026-08-15, split by ramp-down WITHIN each
    baseline band): | baseline band | n | net FAST | net SLOW | excess FAST | excess SLOW | | 1300-1900W
    | 88 | +0.142 | +0.163 | 0.058 | 0.037 | | 1900-2400W | 60 | +0.279 | +0.312 | 0.005 | 0.000 | | 2400-3000W
    | 78 | +0.364 | +0.366 | 0.000 | 0.000 | | 3000-4500W | 43 | +0.497 | +0.476 | 0.000 | 0.000 | Within
    every band the difference collapses to noise, and in the highest band it INVERTS (fast slightly better).
    The original +0.155 vs +0.353 gap was ENTIRELY a load artifact. Post-restore excess is ~0.000 in every
    band above 1900W; the only non-trivial rebound is in the lowest band where the saving is smallest
    anyway. HONEST LIMIT OF THIS REFUTATION: it tests whether, within a FIXED 300s hold, bottoming out
    early vs late changes the outcome. It does NOT directly test 300s vs 120s, because there are zero
    120s observations in the record. So it rules out a strong effect from extra idle time across the observed
    range (52s-123s to bottom), but cannot rule out that a genuinely shorter hold behaves differently.
    The A/B remains the only way to settle THAT — but the prior is now weak, not supportive, and the A/B
    should be scoped as "confirm no harm" rather than "capture a gain". WHAT SURVIVES UNCHANGED, and is
    the actionable part: shortening the hold from 300s to ~150s costs NOTHING in effectiveness (127/127
    bottom out within 180s, median 72s) and halves the cycle. The justification is CYCLE TIME AND SAMPLE
    RATE — more recurrence samples per night for the escalation work, faster feedback — NOT energy. Do
    not sell it as an energy win.'
  WIDE_CYCLE_EFFECTIVENESS_2026_08_21: '⚠️ SUPERSEDED — see WIDE_CYCLE_REFUTED_LOAD_MATCHED above; this
    block''s split was not load-controlled and its conclusion is withdrawn. ORIGINAL: OPERATOR: "I suspect
    the wide cycle is making it less effective honestly but have no proof." THE DATA SUPPORTS IT BUT DOES
    NOT PROVE IT — and the confound is important enough that I am not going to let this be recorded as
    proven. Split the 127 analysed cycles at the median ramp-down time: | group | ramp-down | net saving/cycle
    | post-restore EXCESS over baseline | | FAST (idled longest at the degraded setpoint) | 52s | +0.155
    kWh | 0.052 kWh | | SLOW (used more of the hold productively)     | 102s | +0.353 kWh | 0.000 kWh
    | The FAST group — the ones that hit bottom quickest and then sat there ~248s doing nothing — show
    LESS THAN HALF the net saving AND the only measurable post-restore rebound. The SLOW group shows zero
    excess. That asymmetry is exactly the shape the operator predicted. THE CONFOUND, stated plainly:
    ramp-down speed is not independent of load. A compressor already near its minimum modulation ramps
    down FAST and also has less absolute power available to shed — which would produce a smaller net saving
    with no reference to wasted hold time at all. So the lower saving in the FAST group has a fully innocent
    explanation. The rebound number is the more suggestive half (0.052 vs 0.000), but it is not immune
    to the same confound. THE CLEAN TEST IS AN A/B, AND IT IS CHEAP because the hold is already a knob
    (CONF_HVAC_AC_NUDGE_DURATION, exposed as a Number): set it to ~120s, gather nudges under real load,
    and compare net saving and post-restore excess against the 300s history on matched baseline power.
    Requires the compressor to actually be RUNNING — a forced nudge on an idle unit returns classification=inconclusive
    and is useless for this. So the experiment waits for load (afternoon heat or the overnight coast),
    not for a code change. SEPARATE AND STRONGER CONCLUSION, independent of the effectiveness question:
    shortening the hold from 300s to ~120-150s is justified by the RAMP-DOWN data alone — 127/127 reached
    bottom within 180s. That change does not depend on resolving the confound. It also shortens the whole
    cycle, which doubles the recurrence samples available per night for the escalation work. COUNTERFACTUAL
    CAVEAT ON THE SAVINGS NUMBER: net saving here is computed against "baseline power continues unchanged",
    which is NOT a valid counterfactual — the compressor would have cycled off by itself eventually. So
    the +0.274 kWh/cycle median is an UPPER BOUND, not a measurement. This is the same weakness that makes
    the shipped ac_ramp_savings_* figures suspect (~35 kWh claimed on a night 49 kWh was burned). 0/127
    cycles were net-negative even on this generous basis.'
  next: 'Cheap discriminating query FIRST (does any post-08-15 nudge_evaluated carry effective=0), because
    it decides whether this is one defect or two. Then the design question for the operator, which is
    a policy call not an implementation detail: what SHOULD happen when a zone needs its Nth effective
    nudge in one night? Candidates — escalate to hard reset on recurrence count (respecting the existing
    2/day + 2h compressor-protection caps, which exist for good reason); or widen the nudge (a bigger
    or longer bump) rather than repeating the same 1.5F; or accept the recurrence and stop crediting savings
    for it. Implement the missing soft-nudge daily cap regardless — a specced safety cap that silently
    never shipped is its own problem, and 31/day against a design of 6 means nobody has been looking at
    this number.'
  organic_evidence:
  - '2026-08-23 watch-pass: v5.89.0 NOT INSTALLED as of watch pass (running v5.88.1). Unwatchable until
    operator installs HACS update. H1 PENDING.'
- id: HVAC-GOVERNED-EXCURSION-1
  STATUS_CORRECTED_2026_08_22: Card read `shipped_organic` while D2+D3 were still UNBUILT. D1 (observability
    columns + settled callback) shipped organically in v5.86.0 and the card was marked shipped off the
    back of it — but D1 was only ever an INPUT to this cycle. The status then sat wrong through an entire
    Tier-3 build, four reviews and six fix-up rounds, and was caught only by the deploy gate refusing
    to accept a card list. This is exactly the failure the kanban skill rule "Status must track reality,
    not just the body" (added 2026-08-21) exists to prevent — and it was sitting on this cycle own card
    the whole time. A multi-deliverable card must not be marked shipped until EVERY deliverable has shipped;
    partial delivery gets a note, not a status change.
  PLAN_REVIEW_B_2026_08_21: 'FRAMING B (adversarial build-prediction): **FIX-THEN-BUILD** — 3 CRITICAL,
    6 HIGH, 5 MEDIUM, 3 LOW. Doc: docs/reviews/code-review/PLANREVIEW_B_governed_excursion_build_prediction.md
    Two of the three CRITICALs land on MY design decisions, not the planner''s. C1 — "INTENDED PRESET"
    HAS NO RESOLVER. My design said snapshot the preset "the preset-manager would apply". No such call
    exists. The value S1 actually writes is a local `effective_preset` computed inline across ~150 lines
    of _apply_house_state_presets (hvac.py:1534->1744, emitted at :1981), mutated by vacancy-grace, runtime_exceeded,
    stale-occupancy, fan-trust, pre-arrival and the ARREST-COMFORT skip. The only thing resembling a resolver,
    PresetManager.get_preset_for_house_state (hvac_preset.py:111), is a bare map lookup that knows NONE
    of that. Consequence if built as written: a zone legitimately forced `away` gets restored to comfort/sleep
    every excursion — URA fighting itself on a 5-minute cadence, on the exact axis this cycle exists to
    govern. The intended preset is an EMERGENT COMPUTATION, not a stored value, and the plan must say
    how it is obtained (extract the ladder into a callable? stamp the value at emit time and read the
    stamp?) before any build. C2 — MY restore_ok_immediate IS MEASURING BEFORE THE WRITE LANDS. The preset
    write is blocking=False (hvac_override.py:3258) and restore_ok_immediate is computed from an immediate
    hass.states.get at :3288-3292 — i.e. before the service call completes. So immediate is ~always 0
    for the preset axis regardless of outcome. THIS RETRACTS MY EARLIER CLAIM that today''s observed (immediate=0,
    settled=0) was "a third failure mode". It is not a third shape; immediate is simply uninformative
    as I specified it, and the only real signal in that row is settled=0. Worse, the plan''s acceptance
    criterion AC1 queries WHERE immediate=1 AND settled=0, which would return ZERO rows for the real defect
    -> ship, query, declare PASS, defect untested. Fix: pass predicate becomes any settled-FAIL; immediate
    demoted to a diagnostic split. C3 — NEEDS OPERATOR SIGN-OFF, see below. KEY HIGHs: H-3 both chokepoints
    default blocking=False (hvac_setpoint.py:124,177) so `await emit_set_temperature(...)` compiles and
    returns before the call lands — the ordering fix is not actually specified; and return_excursion must
    pass NO gate, because a deferred write is DROPPED not queued (hvac_setpoint.py:12-16), which would
    strand the zone mid-excursion. H-2 no specification for exception mid-sequence or partial failure,
    and the state PK is zone_id so a stale row collides with the next begin(). H-4 the 2x30s retry is
    inherited by analogy with no trigger predicate (emit_* returns True on ISSUED, not landed). ADJUDICATIONS
    (both were "options offered where one is correct"): DROP hard_reset_preset_assert from the primitive
    — replace with one emit_set_preset_mode at the verify success branch and delete the enum kind so nobody
    wires it later by analogy. And NO dual-write — use a nullable excursion_id on ac_ramp_events; two
    writers for one fact is the census-double-count shape. ON THE REFUTED RESTART CLAIM: the plan handles
    it correctly — non-goal 6 names the exemplar, cites the audit, and pre-lists the sub-surfaces a reviewer
    might wrongly attack. No finding.'
  OPERATOR_DECISION_NEEDED_C3_2026_08_21: 'CRITICAL 3 is a BEHAVIOUR CHANGE requiring an explicit ruling,
    not a plan edit. The plan proposes deleting `_nudge_pre_preset` as a simplification. That dict is
    filtered ON PURPOSE (hvac_override.py:3107-3112, comment: "we don''t want to fight a user-set manual
    mid-night") and the restore is gated on `_cur_preset == "manual"` at :3253. Making the preset write
    unconditional and manager-sourced DOES fix the self-disarm latch — but it also means an operator who
    sets `manual` at 22:00 gets overwritten at the next nudge, which on the current cadence is within
    ~30 minutes. RELATED PRIOR RULING, but not the same question: the operator already ruled that URA
    reverting deliberate human thermostat adjustments is INTENDED ("Its supposed to. I''m happy with that.
    The arrester already has its own grace and mid point math"). That ruling was about the ARRESTER, which
    has grace and midpoint logic. This is the NUDGE RESTORE, which has neither — it would overwrite immediately
    and silently. Also unstated: interaction with immune holds and with the Temp Arrester Override switch
    on the preset axis. PARKED PENDING OPERATOR ANSWER — do not build either way until answered.'
  title: One governed-write primitive for all three thermostat axes — mode got hardened in v4.7.32, setpoints
    and presets never did, and the same call sites leak on the ungoverned axes
  thread: hvac
  status: done
  shipped_version: v5.88.0
  updated: 2026-08-25 22:20
  approval: needs_operator
  tags:
  - operator-proposed
  - wire-in-anchor
  origin:
    date: '2026-08-21'
    gist: 'Operator: "Consider writing a more robust function that does ac write and or restores that
      guarantees preset write when possible or at least returns via all call sites. I would also consider
      doling in the heat_cool work since it''s related (even though it works well now - it was hardening
      cycle that fixes that and I did not see that set points were as similarly ungoverned)."'
  the_tell: THE OPERATOR'S INSTINCT IS CONFIRMED BY THE v4.7.32 CYCLE'S OWN WORDS. Its test docstring
    (quality/tests/test_v4732_heat_cool_and_span_prune.py) says Part A made "override-revert and AC-reset
    restore always return the zone to heat_cool ... re-absorbs the legacy Arrester v10 concept that URA
    had only partially adopted (it previously force-corrected only the off case)" and — verbatim — "Fixes
    nudges sometimes don't reset the mode". That is THE SAME COMPLAINT, ONE AXIS OVER. In v4.7.32 someone
    noticed the nudge did not always restore the MODE and hardened that axis. Nobody asked whether the
    same nudge restores the PRESET. Same call sites, same failure shape, same writer — one axis fixed,
    the neighbouring one left ungoverned, and it has been leaking ever since at 31-43 nudges/day/zone.
  structural_finding: 'THE TWO AXES ARE GOVERNED IN COMPLEMENTARY, INCOMPLETE WAYS — neither is whole,
    and each has the half the other is missing. HVAC_MODE: has LIFECYCLE governance (every restore path
    force-returns to heat_cool, guarded by _supports_heat_cool, with an invariant test) but NO CHOKEPOINT
    — there are 8 raw hass.services.async_call("climate","set_hvac_mode") sites across 4 python modules
    (coordinator.py 1, hvac.py 1, hvac_egress.py 2, hvac_override.py 4). Any new writer inherits nothing.
    SETPOINT + PRESET: has a CHOKEPOINT (hvac_setpoint.py — emit_set_temperature / emit_set_preset_mode
    enforce the freeze floor, the deadband invariant, and the ARREST-COMFORT gate in one place) but NO
    LIFECYCLE governance. The module docstring says so explicitly and deliberately: "The caller keeps
    its own suppress() / arrester handshake around this call." So the restore, the preset assertion, and
    the arrester handshake are each caller-owned — which is precisely how 9 of 13 setpoint-write sites
    ended up never restoring a preset. A CHOKEPOINT WITHOUT A LIFECYCLE CANNOT PREVENT THIS CLASS. Freeze
    floor and deadband are properties of a SINGLE WRITE, so a per-write chokepoint enforces them perfectly.
    Preset restoration is a property of a PAIR OF WRITES SEPARATED IN TIME — nothing a single-write chokepoint
    can see. That is the actual structural gap, and it is why adding more guards to emit_set_temperature
    would not have caught this.'
  proposal: 'A GOVERNED EXCURSION, not a better write function — because almost every URA thermostat write
    is an EXCURSION: a temporary, intentional departure from the governed state that must return. The
    AC nudge, the arrester compromise shave, the DPM range apply, solar banking, pre-cool, the S14 off-phase
    ceiling, the hard reset — all are "leave the governed state, do a thing, come back". Today each one
    hand-rolls its own return and they fail differently. THE PRIMITIVE OWNS SIX THINGS the callers currently
    own separately: (1) SNAPSHOT the full governed state — mode, setpoints, preset — and critically take
    the preset from the INTENDED state (the preset manager''s target for the current house state), NOT
    merely the observed non-manual preset. This single change kills the self-disarm latch: a zone already
    stuck in `manual` currently snapshots nothing and can never recover; with an intended-state snapshot
    it recovers on the very next excursion. (2) SUPPRESS the arrester for the right kinds, and roll the
    suppression back on a gate-defer. (3) WRITE through the existing chokepoints, inheriting freeze floor
    + deadband + gate unchanged. (4) GUARANTEE THE RETURN in a deterministic ORDER with AWAITED writes:
    setpoints first, then assert preset, then assert heat_cool. Ordering is the whole ballgame — today
    the restore emits setpoint blocking=False and the preset immediately after, and a cloud-polled Bryant
    can land them out of order (the 509 ms zone-1 clobber). (5) RECORD the outcome — preset_before / preset_after
    / restored_ok — so restore success stops being invisible. (6) BACKSTOP the return. CORRECTED 2026-08-21
    — I claimed the nudge restore rides an async_call_later that a restart drops, stranding the zone.
    THAT IS WRONG and the restart-safety audit refuted it: ac_reset_state persists the in-flight nudge
    BEFORE the service call, and async_startup_ramp_audit (hvac_override.py:3925) restores in-flight nudges
    post-boot with elapsed-time arithmetic and two guards. The AC-ramp nudge is in fact the EXEMPLAR of
    restart safety in this codebase, not a victim of it. The requirement stands for the GENERALISED primitive
    — any excursion it owns must carry the same restart-safe in-flight record — but it is a requirement
    to PRESERVE and generalise existing correct behaviour, not to fix a defect. Lesson for the record:
    I inferred a restart hazard from seeing async_call_later without checking for a startup-audit counterpart.
    Same error class as the zone_cameras inference. PRIOR ART TO GENERALISE, NOT INVENT: ac_reset_state
    already carries in_flight_nudge_original_target / in_flight_nudge_started_ts / in_flight_nudge_duration_s
    — a per-zone, restart-safe, in-flight excursion record built for exactly this reason, for exactly
    one writer. The primitive generalises that existing pattern to all writers and all three axes. That
    is the cheapest possible framing and it means the persistence design is already validated. API SHAPE
    — the excursion OUTLIVES the coroutine (the nudge holds for minutes and restores on a timer), so a
    plain `async with` context manager does not fit the main case. Suggested: a begin/return token backed
    by the persisted record, with the context-manager form as sugar for the synchronous cases. On the
    operator''s "guarantees ... or at least returns via all call sites": the existing chokepoints already
    return a `_written: bool` that callers check, so the pattern is established — widen it to a richer
    outcome object and make ignoring it a review smell.'
  SCOPE_DECISION_MODE_EXCLUDED_2026_08_21: 'OPERATOR CHALLENGED MY INCLUSION OF heat_cool — "Given you
    think they''re different might be better to leave that alone if already abstracted?" — and was right.
    I folded the mode axis in without running the marginal-benefit decomposition on my own proposal. Running
    it now: SIMPLEST VERSION (setpoint + preset only) captures the ENTIRE measured defect — the manual
    induction at 31-43 nudges/day/zone, the self-disarm latch, the 509 ms ordering race. Every piece of
    evidence gathered in this thread lives on those two axes. FANCIER VERSION (also absorb mode) buys
    the closing of a THEORETICAL seam, and costs 8 more call sites across 4 modules including coordinator.py
    and hvac_egress.py — files outside the HVAC-override path entirely. It widens the blast radius of
    an already-large Tier 3 cycle to reach code that has nothing to do with the bug. THE EVIDENCE ASYMMETRY
    SETTLES IT. hvac_mode has a clean recorded track record: 11 hard_reset_started rows and 11 hard_reset_completed
    rows, ZERO orphans, plus a force-restore to heat_cool, a _supports_heat_cool guard, a retry path and
    an invariant test. Setpoint/preset have NO record at all — restore success is literally unmeasurable
    because the columns do not exist. Scoping to the unmeasured axis is the disciplined choice. DECISION:
    DROP the migration of the 8 raw set_hvac_mode sites to a chokepoint. KEEP the weaker requirement that
    the primitive must not DROP the heat_cool restore where v4.7.32 already put it. The hard reset will
    touch the primitive anyway (it needs the preset assertion it currently lacks), leaving mode handled
    by existing v4.7.32 code and preset by the primitive — not a seam, because the mode off/restore is
    the excursion''s ACTION while the preset assertion is its RETURN GUARANTEE. They compose without either
    owning the other. THE HEDGE THAT KEEPS THIS REVERSIBLE: include mode_before / mode_after in the OBSERVABILITY
    step alongside preset_before / preset_after, even though mode is excluded from the migration. Same
    column-add, no extra cost, and it makes the decision to leave the mode axis alone EVIDENCE-BACKED
    rather than assumed. If it turns out to leak, we see it and revisit with data. Measure the axis you
    are choosing not to fix. NET: scope drops from ~21 sites to ~13, blast radius contracts to the HVAC-override
    path, and a tripwire stays on the excluded axis.'
  scope_and_tier: 'REVISED — see SCOPE_DECISION_MODE_EXCLUDED above; the 8 set_hvac_mode sites are now
    OUT of scope. Roughly 21 call sites: 13 setpoint-write sites (9 of which never restore a preset) plus
    8 raw set_hvac_mode sites. Migrating >=3 callers to a new shared primitive that changes the shape
    of a persisted record is squarely the Tier 2-DB trigger; combined with the fact that this threads
    a guarantee through many emission sites where the failure mode is ONE MISSED SITE (Bug Class #53),
    and that it touches compressor protection and comfort, this is TIER 3. Framing D (adversarial completeness,
    diff-blind) must re-enumerate the write surface independently — the 13 and the 8 are MY counts and
    are a hypothesis, not a finding. NON-GOAL to state explicitly: do not change freeze-floor or deadband
    behaviour. Those already work and are already correctly placed at the per-write chokepoint; the excursion
    wraps them, it does not replace them.'
  sequencing: 'THIS CARD SUBSUMES THE PRESET-COMPLIANCE HALF OF HVAC-MANUAL-PRESET-CONTRACT-1 — the primitive
    IS the implementation of that contract, so they should not be built twice. Operator floated deferring
    the escape to that carded work; the resolution is the reverse merge: the contract card states the
    RULE, this card builds the MECHANISM, and the AC-ramp card becomes a CONSUMER of it. RECOMMENDED ORDER,
    and the first step is deliberately not a fix: (1) OBSERVABILITY FIRST — additive preset_before / preset_after
    / restore_ok columns on ac_ramp_events plus a recurrence metric. No behaviour change, no risk, and
    it MEASURES the leak before we touch it, so the fix has a real before/after instead of a claim. Measure-before-build
    applied to our own defect. It also gives the Tier-3 cycle its acceptance evidence for free. (2) THE
    PRIMITIVE + MIGRATION (this card). (3) AC-ramp recurrence trigger + partitioned night/day reset budgets,
    on top of a write path that no longer induces manual. Doing (3) before (2) would multiply the leak
    by every new escalation.'
  organic_evidence:
  - '2026-08-23 watch-pass: borrow sensor shows started_today.nudge=34, returned_today.nudge=34, restore_failed_today=empty,
    zones in away (not manual). last_return.restore_ok=null (criterion requires true). Strong positive
    signal but discriminating criterion not yet met. H1 still PENDING.'
  next: 'PENDING 2026-08-23: 34 borrows returned with 0 restore failures; zones not in manual. Awaiting
    last_return.restore_ok=true on a natural nudge to close H1 (live discriminator). Also: reconciliation
    check started_today.nudge == ac_nudges_today entity not resolved.'
  closed_note: 'VALIDATED via live DB 2026-08-25 (ac_ramp_events, 3487 rows): telemetry populated (113
    non-NULL preset_before — not the all-NULL broken state); restore_ok_immediate=1 on 99; clean immediate+delayed
    on 74 (H1 discriminator ''restore_ok=true on natural nudge'' MET). Of 28 restore_ok=0: 24 are the
    documented delayed-poll artifact (immediate=1, cloud poll transiently read manual — the load-bearing
    ''instantaneous read is authoritative'' design) and only 4 are real failures (immediate=0 AND delayed=0,
    ~4%). Primary deliverable proven; the 4-failure tail carded separately. Disposed per soak-exit rule
    — soaked since 08-23, discriminator now evaluable and met.'
- id: HVAC-EXCURSION-D1-BANKING-RELEASE-1
  title: HVAC banking excursions start and never end (2 open / 0 ended) — add a periodic auto-release
    sweep + stale-boot banking release so a borrowed thermostat always gets handed back
  thread: hvac
  status: shipped_organic
  shipped_version: v5.91.2
  created: '2026-08-26 03:20'
  updated: '2026-08-26 03:20'
  refinement_status: refined
  approval: approved
  tags: [descope, tier-2, wire-in-anchor]
  problem_solution:
  - 'Problem: when HVAC banks a thermostat (pre-cool / pre-heat borrow), the borrow could be left open
    forever — measured 2 banking rows open with 0 ever ended. A stuck-open borrow means the zone keeps
    holding a setpoint URA thinks it already handed back. Solution: a periodic auto-release sweep closes
    any borrow past its lease, and a stale-boot pass closes banking rows left dangling across a restart.'
  why: The sound, well-tested part (D1) of the parked HVAC-EXCURSION-RESTORE-UNIFIED-1 cycle. Descoped
    and shipped alone (operator 2026-08-26) after D2/D3/D4 failed 4 Tier-3 reviews with 3 CRITICALs.
  DESCOPE_2026_08_26: 'Built clean on feature/hvac-excursion-d1-only off develop (NOT off the failed
    unified build). Reviewed SHIP (1 read-only pass); 2 in-cycle fix-ups (boot-vs-sweep ordering to kill
    a bounded boot double-emit; restore_ok :no_entity discriminator). 151 excursion/offphase/banking
    tests pass, 0 regressions, 5/5 sites RED-on-neuter incl. the C-8 wire-in anchor.'
  KNOWN_GAP: 'With D3 parked, a pre_preset==manual token closes with restore_ok=None and the zone stays
    manual — the PRE-EXISTING hazard the unified cycle was chartered to solve. D1 does NOT introduce new
    manual-stranding (strict improvement on develop, which dropped the row with no ended-event at all).
    The manual-recovery fix lives in the parked D2/D3/D4 rework.'
  next: 'Ships v5.91.2 (batched w/ arbitrage + dp-verypoor). Organic: confirm banking rows now CLOSE
    (hvac_excursion_events shows ended rows with trigger lease_expiry/stale_boot_release, not just open).'
  refs:
  - docs/planning/PLANNING_hvac_excursion_restore_unified.md (D1)
  - docs/reviews/code-review/hvac_excursion_restore_unified.md
  - feature/hvac-excursion-d1-only @ 48b06eaa7
- id: HVAC-EXCURSION-RESTORE-UNIFIED-1
  title: Unify HVAC excursion restore — auto-release sweep (D1), setpoint-writer governance gate (D2),
    manual-preset recovery (D3, kill-switch OFF), off-phase ceiling governance (D4)
  thread: hvac
  status: parked
  created: '2026-08-26 02:20'
  updated: '2026-08-26 02:20'
  refinement_status: refined
  approval: needs_operator
  tags:
  - tier-3
  - four-review
  - do-not-ship
  problem_solution:
  - 'Problem: HVAC borrows the thermostat (banking pre-cool/heat, off-phase ceilings, manual overrides)
    but several paths never hand it back cleanly — banking excursions start and never end (measured: 2
    open / 0 ended), and some setpoint writes bypass the governed-excursion primitive so a zone can be
    left stranded. Solution: a periodic auto-release sweep that closes expired borrows (D1), a build-time
    gate that proves every setpoint write is governed (D2), an opt-in recovery that returns a stuck-manual
    zone to its house-state preset (D3, ships OFF), and governance for the off-phase ceiling write (D4).'
  BUILD_2026_08_26: 'Built on feature/hvac-excursion-restore @ da3ec8237 (branch off develop). 4 source
    + 3 test files, 17 new tests, 336/336 in-slice green. Baseline tag pre-review-hvac-excursion-restore.'
  REVIEW_2026_08_26_FOUR_FRAMING: 'ALL FOUR framing-disjoint reviews (A local / B async-lifecycle / C
    test-authority-mutation / D adversarial-completeness) = FIX-REQUIRED. Record:
    docs/reviews/code-review/hvac_excursion_restore_unified.md. 3 CRITICAL (two are 3-way convergent):
    (1) S14 orphans its off-phase-ceiling token after the lease-expiry sweep -> per-tick UNGOVERNED
    rewrite / D3-ON flap [A-HIGH-2=B2=D-CRIT-2]; (2) D4 one-shot anti-flap guard is DEAD CODE (added
    then cleared same block) [A-CRIT-1=B5=D-CRIT-3]; (3) the D2 AST governance gate is HOLLOW — governs
    1 of 11 setpoint writers, whitelist keyed on bare fn-name across modules, which INVALIDATES the D3-ON
    interlock [D-CRIT-1, C-1]. Plus 7 HIGH incl. two PRE-EXISTING live ungoverned writers D3 would stomp
    (S9 boot ramp-audit, S10 DPM), async-create-task-no-await discharge, CM bypass, sweep re-entrancy
    double-emit, D3 consumer untested both positions, bare OVERRIDE_NORMAL_DELTA misclassification.
    CLEARED: D3 OFF-path byte-identity HOLDS (3 reviewers); D1 stale-boot BANKING release genuinely fixes
    the measured 2-open/0-ended. D1/D3 helper layers are tested; D4 is untested end-to-end.'
  ORCHESTRATOR_RECOMMENDATION: 'DESCOPE. D1 (auto-release sweep + banking close) is the valuable, mostly-sound,
    SEPARABLE part with a real measured driver; if D4/S14 is parked the CRIT-2 orphan interaction disappears
    and the sweep only closes banking tokens. Recommend: ship a narrow D1-only cycle after fixing its
    sweep re-entrancy (B3) + untracked task (B4) + wire-in test (C-8); PARK D2/D3/D4 (off-phase-ceiling
    governance + recovery + gate) for a dedicated rework where all 3 CRITICALs live and the gate/interlock
    design needs reconsidering. Mirrors the operator EVSE-split preference: do not let the ready deliverable
    (D1) wait behind the unready one (D4). AWAITING OPERATOR: full-fix-up vs descope vs re-plan.'
  DESCOPE_DECISION_2026_08_26: 'Operator chose (B) DESCOPE. D1 (auto-release sweep + stale-boot banking
    release + HIGH-1) SPLIT OUT to a fresh clean build on feature/hvac-excursion-d1-only (building now,
    with the B3 re-entrancy guard + C-4 discriminating HIGH-1 tests + C-8 wire-in anchor). THIS card now
    tracks the PARKED D2/D3/D4 rework. Parked because all 3 CRITICALs live here: D2 AST gate is hollow
    (governs 1/11 writers, module-agnostic whitelist -> invalidates the D3-ON interlock); D4 S14 orphans
    its token after the sweep (per-tick ungoverned rewrite / flap) + the D4 one-shot is dead code; plus
    the pre-existing S9/S10 ungoverned writers D3 would stomp. The gate + interlock DESIGN needs
    reconsidering, not just patching. Reference build (do NOT ship): feature/hvac-excursion-restore @
    da3ec8237. Revival trigger: after D1 ships + soaks, re-plan D2/D3/D4 as a fresh Tier-3 cycle starting
    from a sound governance-gate design (key whitelist by file:function; govern or gate S9/S10 before D3
    can ever flip ON).'
  status_note: D1 split to HVAC-EXCURSION-D1-BANKING-RELEASE (feature/hvac-excursion-d1-only); this card = D2/D3/D4 park.
  next: 'PARKED. Revive as a fresh Tier-3 D2/D3/D4 cycle after D1 ships and soaks. Rework the governance
    gate (file:function-keyed whitelist, credit begin/return anywhere in the lifecycle), govern/gate the
    S9 boot ramp-audit + S10 DPM writers, redesign the S14 token lifecycle so the sweep cannot orphan it,
    make the one-shot real, and only then re-enable a D3 interlock. All 4 review records apply.'
  refs:
  - docs/planning/PLANNING_hvac_excursion_restore_unified.md
  - docs/reviews/code-review/hvac_excursion_restore_unified.md
  - feature/hvac-excursion-restore @ da3ec8237 (reference build — do NOT ship)
  - tag pre-review-hvac-excursion-restore
- id: RESTART-SAFETY-DOCTRINE-1
  title: URA is not universally restart-safe — islands of persistence built ad hoc after each burn, no
    shared standard, and at least three detectors that can never reach their own threshold
  thread: platform
  status: shipped_organic
  shipped_version: v5.87.0
  updated: 2026-08-21 10:05
  approval: needs_operator
  tags:
  - measure-before-build
  - suppression-needs-discharge
  origin:
    date: '2026-08-21'
    gist: 'Operator, on the governed-excursion primitive: "Especially the restartability. I almost want
      to generalize that. Ura is not universally restart safe." Correct, and this session produced four
      independent instances without looking for them.'
  instances_found_without_looking: 'All four surfaced incidentally in ONE session, which is the argument
    that this is systemic rather than a handful of bugs: (1) HVAC-ANOMALY-BLIND-1 — AnomalyDetector._baselines
    is an in-memory dict reset to {} every restart, so minimum_samples=10 is STRUCTURALLY UNREACHABLE
    on an instance that restarts near daily. 3 of 5 metrics have never received a sample. Root cause already
    sitting in docs/BACKLOG.md from the v4.6.10 review. (2) Optimizer bayesian_accuracy — this morning''s
    NM alert. Fills only at six fixed times a day; if the dict is in-memory the same arithmetic applies
    and it can never fill. NOT yet confirmed — the discriminating check is whether it persists. (3) The
    AC-ramp nudge restore — rides an async_call_later. A restart mid-nudge loses the timer and strands
    the zone in the excursion state indefinitely. This is what prompted the operator''s remark. (4) The
    arrester — overrides_today and _immune_holds are in-memory with no ledger, which is why last night''s
    zone-2 question is UNANSWERABLE after the fact (see HVAC-HOT-ENTRY-LATENCY-1). Adjacent, already carded:
    UNLOAD-SYMMETRY-TASK-HYGIENE-1 (setup/unload symmetry) is a sibling but not the same thing — it is
    about teardown, this is about re-entry.'
  what_is_already_right: 'IMPORTANT — this is not a greenfield problem, and the fix should generalise
    the existing good work rather than replace it. URA already does restart-safety correctly in several
    places, each built after someone got burned: ac_reset_state persists daily counters, the in-flight
    nudge record and the lockout flag, and the v4.5.11 plan says exactly why — "otherwise restart loops
    bypass the cap and fry the compressor". DrainPrecedenceState has a persisted carrier with restore_from_blob
    + serialize_for_kv. Number entities write through to entry.options. There are dedicated state tables
    — egress_state, fan_recheck_state, regime_cell_state, energy_state, room_state. The problem is not
    absence of technique. It is that each island was built reactively, in isolation, with no shared standard
    — so every NEW piece of coordinator state starts unsafe by default and stays that way until it causes
    an incident.'
  the_doctrine: 'THE GENERALISATION IS A DECLARATION REQUIREMENT, NOT A PERSISTENCE LIBRARY — and getting
    that distinction right matters, because "persist everything" would be actively wrong. Evidence it
    would be wrong: the Temp Arrester Override switch DELIBERATELY does not persist across restart ("Default-OFF
    is the safe state — an accidental leave-it-on across an outage should not persistently disable governance"),
    with a marker option so the dropped engagement is still SURFACED at next setup. That is a correct,
    considered RESET decision. A blanket persistence rule would have broken it. So the standard is: every
    piece of coordinator state declares which of three it is, and UNDECLARED IS THE BUG. - PERSIST — must
    survive restart (accumulators toward a threshold, daily caps, lockouts, in-flight excursions, ledgers).
    - RESET — deliberately starts clean, WITH a stated reason and, where the drop is operator-visible,
    a surfacing mechanism (the Temp Arrester Override pattern). - REBUILD — derived from live state at
    setup, so persistence is unnecessary. THREE HAZARD CLASSES, which need different treatment and are
    worth naming separately: (a) ACCUMULATORS — counters/baselines that need N samples. Restart resets
    the count; if restart interval < time-to-N, the threshold is unreachable and the feature is silently
    inert forever. This is the nastiest class because it presents as "working, nothing to report". (b)
    PENDING RETURNS — a timer that OWES a restore. Restart drops the debt and the system is left mid-excursion.
    This is the restart case of the standing suppression-needs-a-discharge rule — restart is simply one
    more discharge path that gets forgotten. (c) LIVE SUPPRESSIONS / LOCKS — a flag gating behaviour.
    Fails in BOTH directions (stuck-on or silently released) and the correct answer genuinely differs
    per case, which is exactly why it must be declared rather than defaulted.'
  AUDIT_COMPLETE_2026_08_21: 'AUDIT DONE — docs/planning/AUDIT_restart_safety_classification.md. Findings:
    9 core ACCUMULATOR defects (+ ~6 declared-correct-but-undeclared), 4 PENDING RETURN, 1 LIVE SUPPRESSION/LOCK,
    plus a representative sweep standing in for ~60 async_call_later sites. TWO OF MY FOUR SEED INSTANCES
    WERE REFUTED, which is the audit doing its job: - bayesian_accuracy REFUTED at the persistence layer
    — beliefs restore from DB and the eval writes rows to DB. This morning''s NM "empty" alert is a DOWNSTREAM
    0-rows bug (already logged by the closure at __init__.py:2560-2566), not a restart reset. Separate
    investigation. - AC-ramp nudge restore REFUTED — see the correction on HVAC-GOVERNED-EXCURSION-1.
    It is the EXEMPLAR, not a defect. - AnomalyDetector._baselines PARTIALLY correct: HVAC/presence/music/security
    DO save; safety.py and manager.py''s setup-detector call load_baselines but NEVER save_baselines (F1,
    F2). Real defect, narrower than I claimed. - OverrideArrester CONFIRMED (F5 _immune_holds, F6 deferred
    sunset, F8 override_count_today).'
  DENOMINATOR_MEASURED_2026_08_21: 'THE NUMBER THE AUDIT COULD NOT GET (no shell from that environment)
    — I ran it from the HA recorder: homeassistant_start events, last 14 days. 20 RESTARTS IN 6.9 DAYS
    = 2.9/day. Interval stats over 19 gaps: min 0.18h, p25 1.52h, MEDIAN 5.55h, p75 12.0h, max 34.6h,
    mean 8.7h. TEN OF NINETEEN intervals are under 6 hours. THIS SHARPENS THE WHOLE AUDIT into a rule
    the audit could not state without it: - TICK-DRIVEN accumulators are FINE. A metric sampling on the
    5-minute decision cycle reaches minimum_samples=10 in ~50 minutes, comfortably inside a 5.55h median.
    So HVAC anomaly blindness CANNOT be a restart-reset problem — consistent with the audit finding it
    is a record_observation WIRING gap instead. My original HVAC-ANOMALY-BLIND-1 root-cause attribution
    (inherited from BACKLOG) is therefore also wrong and that card needs re-scoping. - EVENT-DRIVEN accumulators
    are STRUCTURALLY UNREACHABLE. Safety-detector metrics that fire only on safety events, comfort_deviation_hours,
    egress_pause_frequency — anything needing 10 samples of a rare event will essentially never reach
    threshold at a 5.55h median, and will report nominal the entire time. THAT DISTINCTION IS THE ACTIONABLE
    OUTPUT: the hazard is not "in-memory", it is "in-memory AND event-driven". Triage on that axis, not
    on persistence alone.'
  SCOPE_DECISION_NO_CARD_SPRAY_2026_08_21: 'The audit recommends CHECKLIST + one narrow primitive, and
    I agree with that shape — the existing persistence mechanisms are diverse because each is fitted to
    its data shape, and a shared library would flatten correct choices. The real gap is the absence of
    a classification requirement AT THE DECLARATION SITE. BUT THE AUDIT ALSO PROPOSED EIGHT PER-CASE FIX
    CARDS (F1, F2, F5, F6, F7, F8, F10, F16). That contradicts the operator directive — "I don''t want
    cycles that spawn feature debt and more cards if they can be justifiably fixed in cycle" — so it is
    OVERRULED. These get fixed IN THIS CYCLE, not carded. IN SCOPE: (i) F1/F2 — safety.py and manager.py
    must save_baselines, not merely load them; (ii) F5/F6/F8 — arrester immune-holds, deferred sunset,
    and override_count_today made restart-durable AND given a ledger row, which also closes the "arrester
    is unauditable" finding from HVAC-HOT-ENTRY-LATENCY-1 and makes last night''s unanswerable question
    answerable next time; (iii) the DailyCounter(name, persist, reason) helper collapsing the six identical
    _*_today rollover sites (F3, F8, F11, F13, F14, F16); (iv) the `# restart: PERSIST|RESET|REBUILD`
    declaration tag on every self._* in domain_coordinators/*.py __init__, with a CI/pre-commit check
    so undeclared is a build failure rather than a silent default. EXPLICITLY OUT OF SCOPE, and stated
    so it is a decision rather than an omission: the ~60 async_call_later sites are NOT being migrated.
    The representative sweep found the long-fuse subset; those specific ones are in scope, the rest are
    not, and the declaration tag will catch future ones at the point of writing.'
  next: 'BUILD, not more analysis. Order within the cycle: the declaration tag + CI check FIRST (it is
    the thing that stops recurrence and it forces every subsequent edit to declare), then F1/F2, then
    the arrester trio, then DailyCounter. READ-ONLY AUDIT FIRST — do not build a primitive before knowing
    the size and shape of the problem. Sweep coordinator __init__ bodies for instance attributes that
    are counters, dicts, sets or timer handles; classify each as PERSIST / RESET / REBUILD; flag every
    one that is currently undeclared-and-unsafe. Cheap, bounded, no behaviour change, and it produces
    the table that decides whether this warrants a shared primitive or is better served by a checklist
    plus per-case fixes. Output goes to docs/planning/AUDIT_restart_safety_classification.md. THE AUDIT
    MUST ALSO MEASURE THE DENOMINATOR: how often does this instance actually restart? The accumulator
    hazard is entirely a function of restart-interval vs time-to-threshold, so a week of restart timestamps
    turns "structurally unreachable" from an argument into a number. Class (a) is the priority — a detector
    that silently never fires is worse than no detector, because it occupies the slot and reports nominal.
    We have found three candidates in one session and have confirmed only one.'
- id: TEST-SOURCE-MUTATION-KILL-UNSAFE-1
  title: A test writes production source with only a `finally` to restore it — a hard kill leaves the
    repo mutated on disk, and the concurrency guard is exactly what delivers hard kills
  thread: platform
  status: inbox
  updated: 2026-08-21 12:40
  approval: needs_operator
  tags:
  - unrestored-drill
  - test-strategy
  origin:
    date: '2026-08-21'
    gist: 'Surfaced by the D1 observability agent as an ancillary observation it was right to flag: "the
      test suite mutated custom_components/.../energy_pool_owners.py (prune_participant True->False) during
      runs — hollow-test / write-back smell". It reverted the file before committing. I verified the mechanism
      rather than taking the report at face value.'
  mechanism: 'CONFIRMED. quality/tests/test_owner_registry_mutation_matrix.py writes PRODUCTION SOURCE
    — line 58 `path.write_text(mutated, encoding="utf-8")` against custom_components/universal_room_automation/...
    — and restores it in a `finally` at lines 62-63. That is the correct pattern for a framing-C mutation
    test and it is NOT the bug. THE BUG IS THE FAILURE MODE `finally` DOES NOT COVER. `finally` runs on
    exception and on normal exit. It does NOT run on SIGKILL, on a timeout that kills the process group,
    or on a PreToolUse hook aborting the command. So any hard kill mid-mutation leaves production source
    MUTATED ON DISK, silently, with a green working tree from the developer''s point of view until they
    happen to look. AND THE REPO ACTIVELY MANUFACTURES HARD KILLS: there is a PreToolUse Bash guard denying
    CONCURRENT PYTEST (added 2026-08-16, commit 987d46b08). I dispatched three agents in parallel, two
    of their full-suite runs were blocked/killed by that guard, and that is precisely when the mutated
    file was observed. The guard and the mutation test are individually reasonable and jointly hazardous.
    BLAST RADIUS IF IT LANDS UNNOTICED: a mutated production file poisons every subsequent baseline in
    that tree, can be committed by an agent that only stages its own intended files if the mutated file
    is one of them, and — worst case — could ship. This is the "unrestored drill poisons evidence" rule
    with a mechanical cause rather than an agent-discipline cause, which means agent discipline cannot
    fix it.'
  next: 'Fix belongs in the TEST, not in the guard. Candidate approaches, to be chosen on inspection:
    (a) mutate a COPY in a tmp path and point the import at the copy, so production source is never written
    at all — strictly best if the extraction style allows it; (b) if in-place mutation is genuinely required,
    write a sentinel/lock file alongside the mutation and have a session-start check (or a pytest conftest
    fixture) detect-and-restore an orphaned mutation from a prior killed run; (c) at minimum, a conftest
    autouse fixture that snapshots the hashes of any production file the suite is allowed to mutate and
    restores on session start. (a) is the real fix; (b)/(c) are containment. ALSO AUDIT THE SIBLINGS —
    three other test files reference the same production module and may use the same write-back pattern:
    test_owner_registry_persistence.py, test_evse_drain_precedence_session_b2c3_fixup.py, test_blind_window_evse_guard.py.
    Enumerate every test in quality/tests/ that calls write_text/open(...,"w") against a path under custom_components/
    and report the count, then apply the chosen fix to all of them. OPERATOR-FACING NOTE: this is the
    one item from this run that I am carding rather than fixing in-cycle, deliberately. It is not an instance
    of any of the five cards, it is a test-infrastructure hazard rather than a product defect, and fixing
    it inside an unrelated cycle would be the scope creep the operator asked me to avoid. It is cheap
    and should be picked up early — recommend it as the first thing after the five, ahead of the EVSE
    reserve. PROCESS LESSON FOR ME, recorded so it is not relearned: do not run pytest concurrently across
    agents in this repo. The guard exists for a reason and the reason is sharper than "slow" — with source-mutating
    tests present, concurrency causes hard kills and hard kills corrupt the tree. Serialise suite runs;
    parallelise everything else.'
- id: HVAC-TUNABLE-RUNTIME-NOT-SEEDED-1
  title: The 14 HVAC tunable Numbers display the operator's value but the coordinator runs the DEFAULT
    until someone touches the knob — a nudge after a restart used 300s, not the configured 120s
  thread: hvac
  status: shipped_organic
  shipped_version: v5.87.0
  updated: 2026-08-21 12:30
  approval: explicit
  approved_by: operator 2026-08-21 ("kWh threshold and make force use the configuration not the default.
    Fix in cycle if practical")
  tags:
  - restart-safety
  - operator-corrected-me
  origin:
    date: '2026-08-21'
    gist: I forced a nudge to gather D1 telemetry and it ran a 300s hold / 600s eval — the module DEFAULTS
      — while the Number entities plainly read 2 min and 240 s. I first concluded "the knobs are inert",
      then that "the force button ignores config". The operator refused both and told me to re-verify
      for measurement error. Both of my conclusions were wrong; the real mechanism is narrower and worse.
  evidence: 'DECISIVE, from 943 historical nudges plus the one I forced: - 937/943 AUTO nudges hold ~120s
    and 761/942 evaluate at ~240s — the CONFIGURED values. The knobs are honoured, so "inert knobs" is
    REFUTED. - The single 300s hold in the entire history is mine, at 11:33:48, and its logged action_taken
    literally reads "target 80.0->81.5 for 300s" while the auto nudge 63 minutes earlier at 10:30:01 reads
    "for 120s". - The discriminator is NOT force-vs-auto (force_nudge calls the same _perform_soft_nudge
    with triggered_by="manual"; hvac_override.py:3952). It is BEFORE-vs-AFTER RESTART. I deployed v5.86.0
    and restarted at 11:07. The 10:30 nudge (pre-restart) used 120s; mine (post-restart) used 300s. MECHANISM,
    code-verified: - `self._nudge_duration_min = DEFAULT_HVAC_AC_NUDGE_DURATION` (=5) at hvac_override.py:214
    — the runtime field is born at the DEFAULT on every construction. - `hvac.py:234` constructs OverrideArrester
    WITHOUT passing any nudge tunable, and hvac.py never calls set_nudge_duration — verified by grep,
    zero hits. - The Number factory reads options for its DISPLAY value only: `self._value = cast(cm_config.get(conf_key,
    default))` at number.py:2150. The push to the sub-controller (`setattr(sub, runtime_field, ...)`)
    happens in async_set_native_value — i.e. ONLY on a write. - So after every restart the entity shows
    the operator value while the coordinator runs the default, until some write pushes it across. The
    factory docstring says "entry.options is the SOLE source of truth (no RestoreEntity)" — options ARE
    the truth for DISPLAY; nothing rebuilds RUNTIME from them at setup. CLASS: this is RESTART-SAFETY-DOCTRINE-1
    hazard class (c)/REBUILD — a value that should be REBUILT from options at setup and simply is not.
    It is the same shape as the chatter t_floor and DP drain-target findings: an operator-set value that
    never reaches the code that uses it. SCOPE: all 14 factory tunables share this path, so the blast
    radius is every AC-ramp and cover tunable, not just duration. UNVERIFIED and must be checked by the
    builder: why 937/943 landed on 120s despite ~2.9 restarts/day — some write must be pushing them in
    most sessions. Do not ship a fix without explaining that, or the fix is aimed at a mechanism we have
    only half-proven.'
  recommendations_for_posterity_2026_08_21: 'RECORDED AT OPERATOR REQUEST — the tuning recommendations
    from the probe sequence, with their status, so a later session does not re-derive or wrongly re-apply
    them: 1. NUDGE HOLD 300s -> 150s: **WITHDRAWN, DO NOT APPLY.** Based on the DEFAULT constant. Production
    is already configured to 120s, which sits exactly in the operator''s measured 60-120s sweet spot.
    Ramp-down median is 72s, so 120s carries ~48s of margin. Already correct. 2. EVAL WINDOW 600s -> 180s:
    **WITHDRAWN as stated, but a REAL finding underneath.** Production is already 240s, not 600s. The
    finding that survives: post_min (which drives the VERDICT, hvac_override.py:3567) is within 0.05 kW
    of its final value for 99% of cycles by 120s, so the verdict could be reached sooner. BUT post_mean
    drives the SAVINGS MAGNITUDE (:3584), and a shorter window samples only the trough and would INFLATE
    an already-suspect savings figure. Correct fix is to DECOUPLE: verdict early, magnitude over a longer
    window. Code change, not a knob. 3. DETECTION THRESHOLD 1.3 -> ~1.8 kW: **STILL LIVE, and operator-approved.**
    Measured trade-off across 347 cycles: 1.3kW keeps 337 nudges / 94.17 kWh net / 7.42 kWh rebound; 1.8kW
    keeps 258 nudges (74%) / 82.02 kWh (87%) / 4.27 kWh rebound (-42%). Value per nudge rises 0.279 ->
    0.318. The second benefit is larger than the energy one: each nudge is TWO raw setpoint writes and
    therefore a manual-preset risk, so 26% fewer nudges is 26% fewer chances to lose the preset race.
    GATED ON THIS CARD — the per-zone threshold Numbers (_hvac_zone_kwh_threshold_factory, number.py:2467)
    use the SAME factory and therefore the SAME seeding bug, so turning the knob before the seeding is
    fixed would be turning a knob the coordinator may not read. Fix seeding, THEN turn. 4. LOAD-DEPENDENT
    VALUE (context for any future threshold choice): rebound is a function of load level, not of hold
    length. At 1300-1800W baseline, net +0.143 kWh with 0.056 rebound (~a third given back); at 2900W+,
    net +0.470 with ZERO rebound. A nudge at high load is worth roughly 5x one at low load, and 61 of
    164 zone_1 cycles sat in the lowest band.'
  DEFAULT_0_8_RATIONALE_2026_08_21: 'RESOLVED from code, three independent sites — the 0.8 default is
    a 3-TON HEURISTIC and is acknowledged in-code as wrong for 4-ton units: - hvac_zones.py:113-114 "per-zone
    slider value, defaults to 0.8 kW (3-ton heuristic). Tune up for larger units." - hvac_const.py:534-537
    "kwh threshold scales with AC tonnage, the only variable that does, so house-wide would force a 4-ton
    unit to use the 3-ton threshold or vice versa" + `DEFAULT_HVAC_AC_KWH_RATE_THRESHOLD: Final = 0.8  #
    kW (3-ton heuristic; 4-ton ~= 1.0)` - number.py:2462 "Threshold scales with AC tonnage (~25-30% of
    rated power floor). 3-ton units default to 0.8 kW; 4-ton typically wants 1.0 kW." OPERATOR CONFIRMED
    THE TOPOLOGY: zone_1 is a 4-TON unit; zones 2 and 3 are 3-ton. So the single flat default is measurably
    wrong for zone_1 — on a seed failure the 4-ton zone falls to 0.8 kW (~20% of rated power) rather than
    the ~25-30% design point the author intended. The code author expected a per-capacity default and
    never built it. OPEN QUESTION FOR THE OPERATOR, framed by the builder and worth quoting because the
    two readings imply opposite trade-offs: is the dataclass default a DESIGN POINT ("what to use if the
    operator never touched the slider") or a SAFETY NET ("what to use if RestoreState is broken")? If
    the former, a per-zone capacity input is the correct fix and the config-flow already carries a per-zone
    family (ac_load_sensor, ramp_zone_enabled, hvac_const.py:544-550) for it to join. If the latter, always-seed
    + WARNING is sufficient and the default should simply never be reached. Not changed. Not carded as
    work until the operator rules.'
  THRESHOLD_RECOMMENDATION_NEEDS_PER_ZONE_REDO_2026_08_21: '⚠️ MY FLAT 1.8 kW RECOMMENDATION REPEATS THE
    TONNAGE ERROR THE OPERATOR ALREADY CAUGHT ME ON. The trade-off table behind it (337 -> 258 nudges,
    94.17 -> 82.02 kWh net, 7.42 -> 4.27 rebound) POOLED ALL THREE ZONES, so it bands a 4-ton unit with
    two 3-tons in absolute watts — the exact apples-to-apples failure that produced the earlier false
    conclusion about hold length. And the code says explicitly that this knob is the ONE parameter that
    scales with tonnage. Per-zone context already measured: baseline medians zone_1 2104W / zone_2 2028W
    / zone_3 2647W, but per-ton that is 526 / 676 / 882 W-per-ton. zone_1 runs at the LOWEST fraction
    of capacity and contributed 140 of the analysed cycles, so a flat threshold cuts it least where it
    may deserve cutting most. A tonnage-scaled pair (3-ton X, 4-ton ~1.25X per the code''s own 0.8/1.0
    ratio) is the likely right shape. DO NOT APPLY A FLAT 1.8 TO ALL THREE ZONES. Re-run the trade-off
    per zone before turning anything. The knob is already per-zone, so nothing blocks doing it correctly.'
  MECHANISM_CORRECTED_2026_08_21: '⚠️ MY "NOTHING SEEDS IT" FRAMING WAS WRONG IN DETAIL — corrected by
    the builder, which is what the blocking deliverable-1 question was for. A seeding path DOES exist:
    number.py:2187 `async_added_to_hass -> _push_to_controller()` sets the sub-controller runtime field,
    and when the sub-controller is not yet visible at add time it registers a listener on SIGNAL_HVAC_ENTITIES_UPDATE
    (fired from hvac.py:1352 on every decision tick) and retries. That is why 937/943 nudges used the
    configured 120s: the push lands either at async_added_to_hass or on the first HVAC tick after platform
    load. SO THE DEFECT IS A RACE, NOT AN ABSENCE. The ~6 misses are boot orderings where the sub-controller
    was invisible at add time AND the retry did not land before the value was read. My forced nudge at
    11:33 (26 min after a restart) hit exactly that window. This distinction matters for the fix: the
    right move is to make the EXISTING path deterministic, not to add a redundant second seeding path
    that would then need keeping in sync. FIX SHIPPED ON BRANCH: a single seed call at CM setup immediately
    after the HVAC coordinator is registered, iterating `_HVAC_TUNABLE_DISPATCH` — the same dict `_apply_in_place`
    uses — so seed and options-update stay lockstep and a 15th tunable added there is seeded for free.
    Silent no-op on missing sub-controller or absent option key, so the no-change path is byte-identical.'
  ZONE_TUNABLES_FAIL_UNSAFE_2026_08_21: 'SECOND, WORSE INSTANCE — found while checking whether the threshold
    turn was actually unblocked. It was not. The per-zone kWh threshold does NOT use the sub-controller
    path and is NOT in `_HVAC_TUNABLE_DISPATCH`, so the fix above does not cover it. `_hvac_zone_kwh_threshold_factory`
    (number.py:2467) resolves coordinator -> _zone_manager -> zones[zone_id] and sets `zone.kwh_rate_threshold`,
    a dataclass field on ZoneState with **default 0.8** (hvac_zones.py:124), consumed at hvac_override.py:2717
    and stamped into ledger notes at :3041. Production runs 1.30 (visible as `threshold=1.30` in detection_fired
    notes). DIRECTION OF FAILURE IS THE POINT. The duration race fails SAFE — a boot race gives a 300s
    hold instead of 120s, i.e. more conservative. This one fails UNSAFE: a boot race drops the threshold
    from 1.30 to 0.8, making detection MORE sensitive and firing MORE nudges — and every nudge is two
    raw setpoint writes and therefore a manual-preset risk. Same race, opposite hazard, and this is the
    hazardous direction. Sent back in-cycle (operator: "fix in cycle if practical") to extend the deterministic
    seeding to zone-targeted Numbers, with a sweep for siblings of that shape and an ordering check that
    the seed runs after ZoneManager has built its zones.'
  TEST_PROTECTION_GAP_2026_08_21: 'Noted, not actioned: the builder reports quality/tests/test_part2_ec_hc_writeback.py
    at 38 failed / 29 passed BOTH with and without the change — pre-existing, zero delta. But that is
    the suite covering this exact dispatch/writeback mechanism, and it is more than half red, so it offers
    almost no regression protection for changes in this area. Filed as evidence against TEST-STRATEGY-REARCH-1;
    it also raises the value of the targeted tests written in this cycle, since they are effectively the
    only guard.'
  next: 'Fix in-cycle per operator. TWO deliverables: (a) SEED RUNTIME FROM OPTIONS AT SETUP for all 14
    factory tunables — the fix must be at the factory/CM-setup level, not 14 hand-written seeds, and must
    be covered by a test that fails if the seeding call site is removed (wire-in anchor, not a source
    grep). MUST first explain the 937/943 observation above. (b) THEN raise the per-zone detection threshold
    to 1.8 kW and verify the coordinator actually reads it (read back the runtime value, not the entity
    state — the entity was never the liar). NOTE the force button itself needs NO fix: it already calls
    the same code path. The operator''s instruction "make force use the configuration not the default"
    is satisfied by (a), because the default was coming from the un-seeded runtime, not from the force
    path.'
- id: HVAC-TICK-LITERAL-1
  title: HVAC decision cycle is a hardcoded 5-min literal — the quantum that makes zone_entry_dwell=3
    structurally inert and grace_constrained=5 the minimum expressible value
  thread: hvac
  status: inbox
  updated: 2026-08-20 18:10
  approval: needs_operator
  tags:
  - numbers-get-knobs
  - measure-before-build
  origin:
    date: '2026-08-20'
    gist: Surfaced while computing the optimal dwell/grace tuning the operator asked for. Every preset
      transition in 48h of live history lands on a 5-min boundary; the cause is async_track_time_interval(...,
      timedelta(minutes=5)) at hvac.py:1024-1028 — an inline literal with no named constant and no knob.
      It is the quantum for BOTH tunable damping knobs, so the knobs cannot be reasoned about without
      it, and it was invisible in every prior flap analysis.
  next: 'Tier 1 / docs-adjacent first step: promote the literal to a named module constant in hvac_const.py
    (rung 1 on the placement ladder — this should REQUIRE review to change, it is a cloud-API call-rate
    bound, not an operator-tunable). Do NOT make it a Number entity: lowering it multiplies thermostat
    cloud writes across 3 zones. Then state the dwell/grace clamp relationship in one comment: dwell and
    grace below one tick are not expressible, and a value of exactly one tick guarantees single-tick round
    trips. Blocks nothing, but any HVAC-PRESET-FLAP-1 tuning or rate-limit work should land the constant
    first so the reviewer can see the quantum.'
- id: HVAC-PRESET-FLAP-1
  PRECONDITION_ASSUMPTION_REFUTED_2026_08_23: 'THIS CARD''S `next` SAYS: "v5.88.0 shipped the borrow restore
    so URA-induced manual is largely addressed". THAT IS NOW REFUTED BY MEASUREMENT. The borrow restores
    the SETPOINT (912/914) but NOT the preset: 0/10 restores of a real preset take, at any delay out to
    30 minutes. URA-induced manual is NOT addressed — see HVAC-MANUAL-PRESET-CONTRACT-1 ANSWERED_BY_MEASUREMENT_2026_08_23.
    Gate (1) of this card''s ordered preconditions is therefore still OPEN, not largely closed. Do not
    unpark on the strength of the v5.88.0 assumption.'
  STATUS_CORRECTED_TWICE_2026_08_22: 'CORRECTED, and my FIRST correction was also wrong. I moved this
    review -> waiting_operator on the strength of the stale `next` line WITHOUT reading the ruling fields
    already on this card. Operator: "Preset flap. I thought I ruled already." THEY DID, three times, all
    2026-08-20 and all still present above: OPERATOR_DECLINED_TUNING_MOVE (no knob changes until the manual-write
    fix and the sensor swap land, then re-measure), OPERATOR_DESIGN_PRINCIPLE (the fix axis is person-assignment-aware
    + transit-aware, NOT a global debounce, and it must survive zone_3 mixing transit corridors with a
    rarely-used guest bedroom), and OPERATOR_TASK (operator owns the input-side sensor swap). So this
    is BLOCKED ON PRECONDITIONS, which is a different lane from awaiting a decision. LESSON, recorded
    because it repeated within one session: a status change must be driven by the card BODY, not by the
    `next` line. Where body and next disagree, the BODY wins and the next is what needs rewriting.'
  STATUS_CORRECTED_2026_08_22: 'Was `review`; nothing is under review. The card own next field says: "Mechanism
    proven; this is now a DESIGN question, not a diagnosis. Decide the arbitration rule between the coast..."
    — i.e. it is waiting on an OPERATOR DECISION, not on a reviewer. Moved to waiting_operator so it surfaces
    in the right queue. Per the skill rule: status must track reality, not just the body.'
  title: HVAC zone preset flaps home<->away every 5-15 min during occupied evenings (survives Writer-B
    removal)
  thread: hvac
  status: parked
  updated: 2026-08-20 18:10
  refinement_status: refined
  TUNING_SIMULATION_2026_08_20: 'MEASURED, not assumed. Simulated both knobs against the real 48h zone_3
    preset series (77 home/away transitions after excluding manual/sleep regimes; HOME episodes med 15.0
    min / p25 5.0; AWAY episodes med 10.0 / p25 5.0). Raising a knob by D minutes shortens each episode
    by D and eliminates episodes <= D, so the elimination curve is computable exactly. RESULTS: grace
    +10 kills 56% of away-episodes, +20 kills 83%; dwell +7 (3->10) kills 39% of home-episodes, +12 (3->15,
    the cap) kills 46%. BEST ACHIEVABLE INSIDE THE KNOB RANGES (grace 30 / dwell 15) = ~28 transitions/48h,
    down from 77 — a 64% cut but NOT calm, and it costs 30 min of comfort preset after every real vacancy
    in ALL THREE zones. RECOMMENDED TURN: vacancy_grace 10->20, vacancy_grace_constrained 5->15, zone_entry_dwell
    3->10 => ~41/48h (-47%). VERDICT: tuning is a real partial win and should be taken FIRST, but it CANNOT
    reach calm and cannot express the operator design axis (person-assignment-aware) because all three
    knobs are coordinator-manager GLOBAL — verified number.py:410-671, __init__.py:3388-3424, hvac.py
    _vacancy_grace/_zone_entry_dwell are single scalars, no per-zone form field anywhere. So do NOT abandon
    code — but scope it to the write rate limit + per-zone scaling, not a new subsystem.'
  DWELL_MECHANISM_ANSWERED_2026_08_20: 'OPERATOR ASKED: does dwell watch every room in the back hallway
    to see if someone is moving around persistently or staying, and what does it consult? ANSWER: NO —
    it does close to the opposite. Dwell consults exactly ONE field, zone.current_session_start (hvac.py:1761-1762).
    That field is a single zone-wide timestamp set at hvac_zones.py:562-563 on the first refresh where
    zone.any_room_occupied is True, and any_room_occupied is a plain unweighted OR across all 12 rooms
    (hvac_zones.py:146-148, any(r.occupied for r in room_conditions)). It is RESET TO None the instant
    that OR goes False (hvac_zones.py:566). So there is NO per-room memory, NO persistence requirement,
    NO corroboration, and no distinction between one person genuinely settled in Guest Bedroom 1 and twelve
    rooms blipping in turn. It measures only "how long has the zone been continuously non-empty, as sampled
    at 5-min tick boundaries". THIS IS THE MECHANISM BEHIND THE OPERATOR''S OWN OBSERVATION that it "reacts
    too quickly to presence": a single blip in ANY of 12 rooms sets the OR true, starts the session clock,
    and one tick later dwell is satisfied and the zone goes home. The over-eagerness is an INPUT-QUALITY
    problem (an unweighted 12-room OR, one leg of which was a noisy camera person sensor), not a damping-strength
    problem. Operator''s instinct is correct and my damping-first framing was treating the symptom.'
  OPERATOR_DECLINED_TUNING_MOVE_2026_08_20: 'OPERATOR DECLINED the tuning turn for now, with reasoning
    I should have derived and did not: grace=10 is deliberate because it evaluates across TWO ticks; 25
    would be FIVE ticks and is too expensive; and grace_constrained=5 is meant to be a FRACTION of grace,
    not an independent value — so my "raise constrained to 15" broke the intended relationship. Operator
    also confirms entry dwell was 3 and THEY moved it to 5 today, agreeing 3 sat below the tick. STANDING
    GOAL restated verbatim: "Overall I want it to react accurately to presence. like a glove. Presence
    reaction != flapping. unless its coming from the manual writes." So the objective is ACCURACY, not
    damping — and the suspicion is that much of what looks like flapping is actually the manual writes.
    That makes HVAC-MANUAL-PRESET-CONTRACT-1 the measurement precondition, and makes input quality (the
    Garage Hallway sensor swap, already in flight operator-side) the other half. NO KNOB CHANGES until
    those two land and we re-measure.'
  PER_ZONE_CONFIG_CLAIM_CORRECTED_2026_08_20: 'I CLAIMED "there is no per-zone HVAC setting for any of
    this, or for any hvac setting generally". That is TOO BROAD and is corrected. Per-zone HVAC config
    DOES exist — the Dynamic Preset Management family is per-zone: CONF_ZONE_DYNAMIC_PRESET_ENABLED (energy_const.py:628),
    plus per-zone custom range knobs CONF_ZONE_DYNAMIC_PRESET_COOL_HOME_HIGH / _LOW, _COOL_SLEEP_HIGH
    / _LOW and CONF_ZONE_DYNAMIC_PRESET_CUSTOMIZE_BUCKETS (imported at dynamic_preset.py:56-61). What
    is correct is the NARROWER claim: the three DAMPING knobs (vacancy_grace, vacancy_grace_constrained,
    zone_entry_dwell) are coordinator-manager global. CONSEQUENCE FOR "is the juice worth the squeeze"
    on derived per-zone damping: the squeeze is CHEAPER than I implied, because a per-zone HVAC config
    pattern already exists to follow rather than invent. Still gated on re-measurement after the manual
    fix.'
  LIVABILITY_REFRAME_2026_08_20: 'OPERATOR CORRECTED MY OBJECTIVE FUNCTION. I was minimising TRANSITIONS;
    the real objective is comfort + energy, with transition count only a proxy — and the proxy is MISLEADING
    because the two halves of a round trip have opposite costs. Operator verbatim: "We want it to stop
    cooling when no one is around. And we want it to cool when someone is. We want it to react quickly
    to someone being there if its hot. And we want to wait and make sure they have actually left before
    we go away (will trend hotter, away preset is usually up to 80 on the upward bound)." That is an ASYMMETRIC
    requirement: FAST IN, SLOW OUT. zone_entry_dwell is the fast-in knob and raising it directly violates
    "react quickly if its hot" — so my earlier 3->10 dwell recommendation is WITHDRAWN. vacancy_grace
    is the slow-out knob and is the one that matches the requirement. The dangerous half of the flap is
    the SPURIOUS AWAY while a person is still present, because away relaxes toward 80F and is FELT. Non-obvious
    corollary: in a high-transit zone a LONGER grace is probably cheaper, not more expensive — the energy
    saved by avoiding a re-cool from a relaxed setpoint exceeds the cost of holding comfort for the extra
    minutes. The usual "grace costs energy" intuition inverts here.'
  TUNING_RECOMMENDATION_FINAL_2026_08_20: 'REVISED, asymmetric. vacancy_grace 10 -> 25 (kills 89% of away-episodes
    on the measured 48h series). vacancy_grace_constrained 5 -> 15. zone_entry_dwell LEAVE AS IS (see
    live-value correction below) — do not raise it. Predicted: ~77 -> ~52 transitions/48h, and critically
    the eliminated ones are disproportionately the spurious-away half, which is the half that hurts. LIVE-VALUE
    CORRECTION: the 2026-08-20 handoff section 7 records zone_entry_dwell = 3.0. It is actually 5 (number.ura_hvac_coordinator_47_entry_wait,
    read live 19:00 CDT). That is EXACTLY one tick, not below one — so the "structurally inert" claim
    in TICK_QUANTUM_FINDING is too strong for dwell and applies cleanly only to the grace_constrained=5
    case. Dwell at 5 defers by exactly one tick, reliably, every time. ENTITY IDS + ORDER OF OPERATIONS
    (live-apply verified, NO RESTART NEEDED): the setters push to the live coordinator BEFORE the options
    writeback (number.py:484-485 dwell, :559-561 grace, :665-667 constrained), and these keys sit in the
    reload-suppress block (__init__.py:5858), so the value takes effect on the very next 5-min tick. ORDER
    MATTERS: the constrained setter clamps new_value = min(value, normal) at number.py:660 — so set number.ura_hvac_coordinator_48_zone_vacancy_delay_minutes
    to 25 FIRST, then number.ura_hvac_coordinator_49_zone_vacancy_delay_energy_saving_minutes to 15. Setting
    the energy-saving one first silently clamps it to 10 and logs the clamp at INFO.'
  OUTSIDE_THE_BOX_OPTIONS_2026_08_20: 'Operator asked for non-obvious options. Four, ranked. (1) TEMPERATURE-CONDITIONAL
    DWELL — the cleanest resolution of the fast-in/slow-out conflict. Make dwell zero when the zone is
    above its comfort band and full-length when the zone is already comfortable. Satisfies "react quickly
    if its hot" and "damp pointless transits" simultaneously instead of trading them off. Small, local
    to the hvac.py:1756-1766 gate. (2) SOFT-AWAY FIRST STEP — reduce the COST OF BEING WRONG instead of
    trying to be right more often. Today away jumps to the full relaxed bound (~80). Make the first grace-period
    step a partial relaxation and only go full-away after a second confirmation. This bounds the comfort
    damage of a spurious away without needing better occupancy, and it is the highest-leverage idea if
    code is on the table. (3) DERIVED per-zone damping WITHOUT new per-zone config — scale grace by signals
    URA already holds (zone has no zone_persons AND transit rate above a threshold => grace x k). Expresses
    the operator design axis while respecting the fact that there is no per-zone HVAC config surface,
    and needs no config-flow work. (4) The WRITE RATE LIMIT should be reframed: as an occupancy damper
    it duplicates grace/dwell, but as a CLOUD-API CALL BUDGET it is not duplicative — it bounds writes
    from ALL causes including manual-restore and arrester writes. Ship it as a budget, not as a debounce.
    See the feedback-loop finding on HVAC-MANUAL-PRESET-CONTRACT-1 for why it must ship WITH the lockout
    fix.'
  TICK_QUANTUM_FINDING_2026_08_20: 'ROOT ENABLER, previously unstated: the HVAC decision cycle is a HARDCODED
    5-minute literal (hvac.py:1024-1028, timedelta(minutes=5)) — an inline number with no knob, violating
    Numbers-Get-Knobs. Every dwell/grace value is quantized to it. Two consequences: (1) zone_entry_dwell=3
    is BELOW one tick and is therefore structurally near-inert — at the tick that first observes occupancy
    the session age is ~uniform on [0,5), so dwell=3 blocks only ~60% of first ticks and never survives
    a second tick; the "3-min dwell prevents transit flapping" comment has not been true at this tick
    rate. (2) vacancy_grace_constrained =5.0 is EXACTLY one tick — the smallest non-zero grace the system
    can express — which is why coast mode produces the 5-minute round trips: the observed 5-min HOME episodes
    require grace to have expired within one tick of occupancy ending. Coast/shed is precisely when you
    want FEWER cloud writes, and the constrained knob does the opposite. See CARD HVAC-TICK-LITERAL-1.'
  ROOM_SESSION_FLOOR_2026_08_20: 'Corridor room occupancy sessions are NOT brief. garage_hallway_occupied
    24h: n=26 ON-sessions, min 6.2 min, median 8.1, p90 20.2. The ~6-min floor is the room layer''s own
    clearance timeout. This kills the intuitive "raise dwell past the transit length" fix: a dwell that
    clears the median corridor transit needs >8 min, and p90 needs >20 min — above the 15-min slider cap.
    Dwell can damp the flap; it cannot gate transit out.'
  ORGANIC_EVIDENCE_VIOLATED_2026_08_20: 'VIOLATED. Empirical re-measurement 2026-08-20 (live thermostat
    attribute history, 24h, post-v5.73.0) shows the flap class is NOT eliminated — it MOVED ZONES. zone_3
    (Back Hallway): 83 preset changes/24h, 11 in the worst hour, verbatim home 77/70 -> away 80/66 ->
    home at 10:05/10:10/10:25/10:50/11:10/11:25/11:35. Every flip lands on a 5-min evaluate boundary and
    swings the cool target 3F while the compressor runs. Measured AT THE THERMOSTAT (climate.* attribute
    history), not at URA intent sensors — so these are real cloud-API writes, not cosmetic. zone_2 (the
    ORIGINAL 2026-08-09 subject) is now CLEAN: away held 09:35->13:43 with zero flips. zone_1 37 preset
    / 93 setpoint changes are NOT flap — that is the AC-ramp nudge feature working as designed (ac_nudges_today=43,
    false-positive rate 1.3%).'
  OPERATOR_DESIGN_PRINCIPLE_2026_08_20: 'OPERATOR RULING, and this is the fix''s design axis: "Basically
    if no one is assigned to a zone that maps to an hvac zone, we have to treat it differently. Especially
    if it sees a lot of transit. This zone is literally called Back Hallway even though it contains the
    not often used guest bedroom." So the fix is NOT a global debounce turn — it is a PERSON-ASSIGNMENT-AWARE
    and TRANSIT-AWARE zone policy: a zone with no assigned persons and high transit volume must damp differently
    from a zone with residents in it. Note the composition trap the operator flags: zone_3 mixes pure-transit
    rooms (hallways, staircase, breakfast nook, garage hallway) WITH a rarely-used guest bedroom, so a
    blanket "transit zone = ignore quickly" rule would strand the guest bedroom. The policy has to survive
    that mix.'
  CAMERA_QUESTION_ANSWERED_2026_08_20: 'OPERATOR WAS RIGHT TO DOUBT ME. VERDICT: zone_cameras does NOT
    feed HVAC zone occupancy — every use is diagnostic (hvac.py:3276-3286 reverse map, docstring says
    "(diagnostic)"; sensor attrs hvac.py:3810 / hvac_zones.py:600-601; face-arrival counter presence.py:4686;
    and ONE real read presence.py:2138-2145 in check_zone_occupancy_confidence, reachable only from the
    D6 stale-occupancy failsafe hvac.py:1580-1585 where it can only VETO a >4h stuck-sensor force-away).
    It never makes a zone occupied. So "we use camera for zone ops" is FALSE as a feature. BUT a camera
    IS in zone_3''s occupancy path by a DIFFERENT route I had not checked: room Garage Hallway (01KM7E0N5GBR5G5JBJBRD354M5)
    lists binary_sensor.staircase_person_occupancy (Frigate person) in its ROOM occupancy_sensors, and
    room occupancy is a plain OR over motion+mmwave+occupancy (coordinator.py:3134-3180) which rolls up
    via any_room_occupied (hvac_zones.py:146-148). A per-room CONFIG choice, not a zone-camera feature.
    It is measurably the noisiest input in that room: 89 on/off cycles in 24h vs 53 for the Zigbee PIR
    sibling. MY ERROR CLASS: I inferred a mechanism from the PRESENCE of a zone_cameras attribute — plumbing-vs-arithmetic,
    exactly what CLAUDE.md warns about.'
  ZONE_PERSONS_IS_LOAD_BEARING_2026_08_20: 'The operator''s "no one assigned to the zone" instinct is
    confirmed and is STRONGER than treat-it-differently. Empty zone_persons does not merely mean unassigned
    — it makes THREE suppressions INERT for zone_3: (1) night-trust away-suppression hvac.py:1788-1795
    iterates `zone.zone_persons or []` so home_persons stays empty, the gate falls through, and the flip
    to away is ALLOWED during FAN_TRUST_STATES (home_night/sleep/waking, hvac_const.py:645); (2) sleep
    veto aggregation.py:4017-4019 `if not zone_persons: continue`; (3) non-sleep person-home bias aggregation.py:4152-4154
    same early continue. Also no pre-arrival routing hvac.py:3267-3273. So zone_3 is STRUCTURALLY more
    prone to flipping away, independent of cameras. This is the real design axis for the fix.'
  ACTUAL_CHATTER_SOURCE_2026_08_20: 'NOT one bad sensor — four high-traffic circulation rooms OR''d together,
    24h room-occupancy transition counts: breakfast_nook 62, kitchen_hallway_garage 55, garage_hallway
    49, kitchen_hallway 45, garage_a 25, kitchen_pantry 25, guest_bedroom_1 10, laundry_closet 9. climate.back_hallway_zone_3
    logged 639 state rows in 24h. The camera amplifies ONE of the four; it is not the sole cause. zone_3
    membership CONFIRMED to include Guest Bedroom 1 + its bathroom (operator was right), so a blanket
    transit-zone rule would strand the guest bedroom.'
  OPERATOR_CORRECTED_ME_DWELL_EXISTS_2026_08_20: 'OPERATOR: "I remember a dwell gate ie you have to be
    in the zone for a certain amount of time for it to register a person vs a transit. Check hvac_entry_zone_dwell."
    CORRECT — verified. CONF_HVAC_ZONE_ENTRY_DWELL / DEFAULT_ZONE_ENTRY_DWELL_MINUTES=3 (hvac_const.py:372-373),
    Number entity number.ura_hvac_coordinator_zone_entry_dwell (number.py:410-488), config_flow.py:4864/5319,
    live value 3.0, applied hvac.py:1756-1766. Its comment literally reads "v4.2.2: Zone entry dwell —
    prevent preset flapping on brief transits." I had understated this as merely "guards exist" while
    emphasising the missing write-rate-limit; the operator''s recall was more precise than my summary.'
  FLAP_MECHANISM_RESOLVED_2026_08_20: 'READING THE ACTUAL GATES RESOLVES THE CADENCE. The dwell gate carries
    `and effective_preset != "away"  # Don''t block vacancy overrides` — SO THE DWELL ONLY DAMPS away->home,
    BY EXPLICIT DESIGN. The opposite direction is damped only by the vacancy grace (hvac.py:1517-1553).
    Round trip: transit blip -> room occupied -> last_occupied_time resets -> 3-min dwell -> preset home
    -> grace of quiet -> away. ~13 min, matching the observed 5-25 min cadence. AND THE 5-MINUTE FLIPS
    ARE EXPLAINED: grace_minutes switches to self._vacancy_grace_constrained (LIVE 5.0) whenever _energy_constraint_mode
    is in ("coast", "shed") — and this card''s ORIGINAL 2026-08-09 finding was "all inside coast mode".
    Under coast the grace halves and the flap rate roughly doubles. CONCLUSION: primarily a TUNING problem
    on two existing knobs both at-or-below default and too short for a 12-room transit corridor, PLUS
    the one genuine gap (no rate limit on the preset WRITE itself, so nothing bounds write frequency independently
    of the occupancy signal).'
  OPERATOR_TASK_2026_08_20: 'OPERATOR TAKING THE INPUT-SIDE FIX: "I''ll remove it and [add] an mmwave
    pir class sensor. My task." — i.e. remove binary_sensor.staircase_person_occupancy (Frigate person,
    89 cycles/24h) from Garage Hallway room occupancy_sensors and replace with an mmWave/PIR-class sensor.
    THIS REMOVES THE NOISIEST SINGLE INPUT but does NOT resolve the flap on its own — the other three
    chattering corridors (breakfast_nook 62, kitchen_hallway_garage 55, kitchen_hallway 45) are unaffected.
    Re-measure AFTER the swap before tuning, so the knob turn is sized against the real post-swap chatter
    rate rather than today''s.'
  DEBOUNCE_INVENTORY_2026_08_20: 'Guards EXIST and are the right shape — so this is PRIMARILY A TUNING
    problem, with one real gap. (A) Vacancy grace, occupied->away, hvac.py:1517-1553 + :3504-3505: CONF_HVAC_VACANCY_GRACE_MINUTES
    default 15 but LIVE VALUE 10.0 (tuned DOWN); constrained variant live 5.0. (B) Zone entry dwell, away->home,
    hvac.py:1754-1766: CONF_HVAC_ZONE_ENTRY_DWELL live 3.0. WHAT DOES NOT EXIST (searched hvac.py, hvac_zones.py,
    hvac_preset.py, hvac_const.py for DEBOUNCE|HYSTERESIS|MIN_INTERVAL|COOLDOWN|last_preset_change): NO
    min-interval/rate-limit on the preset WRITE itself — should_change_preset hvac_preset.py:202-217 is
    a bare 3-line comparison with no timestamp and no cooldown; NO hysteresis or per-room min-on-time
    on any_room_occupied (bare any(), no smoothing, no N-of-M); NO chatter suppression on the room->zone
    rollup (chatter_detector.py operates on room-coordinator fusion, not this rollup). CONSEQUENCE: a
    single ~20s camera blip on staircase_person_occupancy restarts the 10-min grace clock for all 12 rooms.'
  SUPERSEDED_CLAIM_RETRACTED_2026_08_20: 'I claimed zone_3 is driven by "camera + room-sensor only" occupancy.
    OPERATOR CHALLENGED: "Not sure we use camera for zone ops. Please confirm." I asserted this from the
    PRESENCE of a zone_cameras attribute + an empty zone_persons — which is exactly the plumbing-vs-arithmetic
    error CLAUDE.md warns about. UNVERIFIED AND POSSIBLY WRONG. Verification dispatched 2026-08-20: does
    a camera signal actually contribute to the zone occupancy value HVAC consumes, or is zone_cameras
    merely an exposed display attribute, or does camera feed ROOM occupancy which then rolls up? DO NOT
    DESIGN THE FIX UNTIL THIS IS ANSWERED — the damping target depends on which signal actually chatters.'
  MECHANISM_HYPOTHESIS_2026_08_20: 'zone_3 is the ONLY zone with no zone_persons — it is camera + room-sensor
    only across 12 mostly-transit rooms (hallways, breakfast nook, staircase, garage hallway). Its any_room_occupied
    genuinely chatters and NOTHING DAMPS IT. vacancy_sweeps_today=16. So the v5.73.0 fix likely addressed
    the person-backed path and left the camera-only path undamped. UNVERIFIED: whether the preset-write
    path has ANY debounce/min-dwell at all — that read (hvac_preset.py / hvac.py preset emission) decides
    whether this is a TUNING knob or a MISSING GUARD, and it is the single cheapest next action.'
  DETECTOR_WAS_BLIND: sensor.ura_hvac_coordinator_hvac_anomaly reported `nominal` with 0 anomalies throughout
    — because metrics_active_ratio is 2/5 and short_cycle_rate has sample_count 0. The one metric that
    would have caught this has NEVER received a sample. See HVAC-ANOMALY-BLIND-1 (root cause is the known
    BACKLOG AnomalyDetector in-memory-baseline defect). This is why the regression went unnoticed between
    v5.73.0 and now.
  shipped_version: v5.73.0
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: ''Fix the occupancy chatter.'')'
  tags:
  - no-fabrication-verify
  - measure-before-build
  - context-wide-scoping
  origin:
    date: '2026-08-09'
    gist: 'operator: "The hvac zone is being set to away and there are/were people upstairs" — then "I''ve
      been seeing this issue from the moment we walked in", which falsified the first three mechanisms
      I proposed'
  why: 'MEASURED: nine home<->away preset cycles on zone_2 in two hours of confirmed occupancy, all inside
    coast mode. Presence was correct throughout. Writer B removal (v5.56.0) did NOT stop it — see P1P3
    for the falsification. Real comfort cost: upstairs held at 79-80F with two kids in it after a 24h
    absence on a 96F day, because the away preset ceiling (hi=80) equalled the current temp, so the thermostat
    read "cooling" while doing nothing.'
  DUTY_CYCLE_DEFINITION_2026_08_09: 'PRECISE, read from source (hvac_const.py:392-394): DUTY_CYCLE_WINDOW_SECONDS
    = 20*60 (20-min ROLLING window); DUTY_CYCLE_SHED = 0.50; DUTY_CYCLE_COAST = 0.75. So coast permits
    15 MINUTES OF COMPRESSOR RUNTIME PER 20-MINUTE WINDOW. Accumulates only while hvac_action in (heating,
    cooling); crossing the cap sets runtime_exceeded -> preset forced away (hvac.py:1445); window reset
    clears it -> preset returns to the house-state value. Cooling stops during the away phase so accumulation
    pauses. Enforcement exists ONLY in shed/coast (normal mode: `else: continue`, no cap at all); the
    only exemption is house_state=sleep.'
  RETRACTION_2_max_runtime_minutes: 'I earlier reported "max_runtime_minutes: 120 is the coast cap" and
    then "you hit the cap almost exactly when you noticed". BOTH WRONG. max_runtime_minutes is computed
    in energy.py:6907 from TIME REMAINING IN THE CURRENT TOU PERIOD — an unrelated field on the same sensor
    that I grabbed because the name looked right. It is not the duty cap, and its 120->60 change was the
    TOU period shortening, not the limiter tightening.'
  RETRACTION_3_two_writers: I framed this as "two URA writers disagreeing with no arbitration". WRONG.
    There is ONE mechanism. The home write is not another actor pushing back — it is the same effective_preset
    computation returning the house-state value once runtime_exceeded clears at window reset. The observed
    ~10-min alternation is simply 75% duty expressed at 5-min tick granularity. THE LIMITER IS CYCLING
    BECAUSE IT IS A DUTY-CYCLE LIMITER. This is the feature working as designed, and it invalidates the
    "arbitration rule" fix direction I proposed.
  REFRAMED_PROBLEM: 'If there is a defect it is NOT the cycling. Three narrower candidates, none Tier
    3: (1) MECHANISM — duty cycling by toggling a user-visible comfort preset makes an energy action look
    like a presence failure; that misreading cost three hours tonight and taught the kids the thermostat
    is broken. (2) OFF-PHASE SETPOINT — away carries hi=80, so on an 80F evening the off phase parks at
    AMBIENT. A duty cycle off-phase should hold a small offset (~2F), not abandon the room. This may be
    config, not code, and is the cheapest candidate. (3) COAST DUTY VALUE — is 75%/20min right when the
    zone is occupied? Pure tuning.'
  MEASURE_FIRST_GATE: 'Operator: "Measure first for sure." AUDIT_hvac_duty_cycle_frequency.md in flight:
    how often runtime_exceeded fires per day/zone, how much of it is while OCCUPIED, EPISODE structure
    (10 flips in one evening != 10 flips over two weeks), and whether the rate is recent. Known confound
    stated up front: the reason ledger only exists from v5.56.0 (2026-08-06), so an apparent jump may
    be instrumentation appearing rather than behaviour changing — the probe must say "cannot determine"
    if so. Also checking whether we now spend MORE HOURS IN COAST than before, which would make this an
    energy-side regression rather than an HVAC one. NO BUILD UNTIL THIS RETURNS.'
  LEDGER_RETRACTION_2026_08_09: 'I carded a "reason ledger reads empty / diagnosability regression" finding.
    IT WAS FALSE and is fully RETRACTED. The v5.56.0 reason ledger works exactly as designed — my extraction
    script read a column named `details` when the actual column is `details_json`. I nearly filed a defect
    against our own tooling because of a typo in a throwaway query. Operator caught it: "Make sure you
    read all the JSON. Don''t want to fix what''s not broken." Rule earned: when an instrument built for
    exactly this question returns nothing, suspect the query before the instrument.'
  MECHANISM_PROVEN_2026_08_09: 'Two URA writers alternating with NO arbitration between them. From the
    ledger, zone_2, every row carrying persons=[ziri, jaya]: 23:34 home->away reason=runtime_exceeded       runtime=True
    23:24 away->home reason=house_state_transition runtime=False 23:19 home->away reason=runtime_exceeded       runtime=True
    23:04 away->home reason=house_state_transition runtime=False 22:54 home->away reason=runtime_exceeded       runtime=True
    The coast duty-limiter (hvac.py:2470-2492, active only in shed/coast, cap max_runtime_minutes=120)
    forces away on trip; the house-state preset logic writes home back because the zone is occupied and
    house=home_evening; the duty window rolls, runtime re-accumulates, it trips again. runtime_exceeded
    toggles True/False in lockstep with the flips. Neither writer yields to the other. CONTRAST proving
    the ledger discriminates: at 19:54/19:59 UTC with nobody home the reason reads vacant_past_grace with
    persons=[] — different cause, correctly labelled. NOTE: the reason LADDER (hvac.py:1577) establishes
    precedence for LABELLING a single change; it does NOT arbitrate between two writers that disagree
    across ticks. That is the gap.'
  my_process_failure: 'I proposed three mechanisms in sequence from snapshots — "URA says home, device
    says away", then "the 120-min cap fired", then "it is coast" — and each fit the instant I was looking
    at and died on the next fact. The history query I ran FOURTH should have been FIRST; I had last_changed
    timestamps in the opening read and looked past them. Rule: for any "it keeps doing X" complaint, pull
    the state history BEFORE proposing a mechanism.'
  next: 'BLOCKED ON PRECONDITIONS, not awaiting a decision — the design axis is ALREADY RULED by the operator
    (person-assignment-aware + transit-aware zone policy, explicitly NOT a global debounce). Gates, in
    order: (1) the manual-write half — v5.88.0 shipped the borrow restore so URA-induced manual is largely
    addressed, but HVAC-MANUAL-PRESET-CONTRACT-1 (the decider/writer split) has NOT landed; (2) the operator-owned
    Garage Hallway sensor swap; (3) RE-MEASURE the flap rate after both, because the operator suspicion
    is that much of the apparent flapping IS the manual writes — if so the residual flap is smaller and
    differently shaped than todays numbers suggest. Only then size any knob turn. Do NOT tune before the
    re-measure: grace=10 is deliberate (it spans TWO ticks), grace_constrained=5 is meant to be a FRACTION
    of grace rather than an independent value, and entry dwell was already moved 3 -> 5 by the operator.
    SUPERSEDED PRIOR NEXT (kept for the record, it predates and contradicts the rulings above it): it
    framed this as an open DESIGN question about arbitrating the coast duty-limiter against occupied-zone
    comfort, with candidates (a) exempt the limiter when the zone is occupied and recovering from a large
    delta, (b) let the limiter win but HOLD it so it stops re-writing, (c) consult battery SOC. Those
    remain useful sub-options for the eventual build, but they are not a blocking question — they are
    implementation choices inside the ruled axis.'
  refs:
  - docs/planning/kanban.data.yaml card P1P3 (the falsification)
  - custom_components/universal_room_automation/domain_coordinators/hvac.py:1569-1610 (reason ladder),
    :1660-1675 (ledger row), :2470-2492 (coast duty limiter)
  DEDUPE_2026_08_09: 'Four-surface sweep: P1P3 is the PARENT (closed, spawned this). STUCK-SENSOR-1 is
    a different flap (mmWave sensor stuck, not HVAC preset) — no merge. ARREST-SUNSET-1 shipped and is
    about override sunset, not oscillation. OVERRIDE-NOTIFY-1 is expiry warning. BACKLOG has no HVAC preset-flap
    item. NEW.'
  toggle_retirement_2026_08_14: 'Operator renamed the kill-switch -> "Coast Preset Preservation" (rides
    v5.75.0) and adjudicated it TEMPORARY CONTAINMENT, not a policy knob (nobody legitimately prefers
    the lying preset; the 2F offset Number is the real knob). RETIRE the switch when L3 (occupied-coast
    founding case) AND L4 (arrester echo flat) organically validate: Tier-1 removal (single-user no-backcompat)
    or demote to rung-1 constant. This closes the card fully when it fires.'
- id: ARREST-COMFORT-1
  STATUS_CONTRADICTION_RESOLVED_2026_08_23: 'This card carried an internal contradiction: status shipped_organic
    at v5.69.0, while review_state recorded three DO-NOT-SHIP verdicts and `next` said the Cycle A build
    had not started. RESOLVED FROM EVIDENCE, not from a guess about which field to trust — the STATUS
    was right and the NEXT was stale. Evidence: merge commit fad88439b "Merge ARREST-COMFORT-1 Cycle A:
    comfort-delay grace (occupancy-live predicate, S1-S13 write-site gates, rung-3 knobs, AI-rules climate
    block) + 4x Tier-3 reviews + 19-finding fix-up + anchor pass" — i.e. the three DO-NOT-SHIP verdicts
    were DISCHARGED by the 19-finding fix-up and the cycle then shipped. Release commit ad00fd4aa "v5.69.0:
    Comfort-delay grace". README_v5.69.0.md exists and documents it. `comfort_delay` appears 37 times
    in master hvac_override.py. Both rung-3 knobs are LIVE: number.ura_hvac_coordinator_comfort_delay_grace_minutes
    and ..._comfort_delay_soc_floor. LESSON, same class as a miss earlier in this session but inverted:
    a status was once changed off a stale `next` line; here the `next` line was stale while the status
    was correct. Neither field is authoritative on its own — the ship evidence (merge + release + README
    + live entities) is.'
  title: Override arrester reverts occupant manual cooling requests with no comfort exemption
  thread: hvac
  status: done
  shipped_version: v5.69.0
  approval: approved
  review_state: 'Cycle A built (18b491e01, 28 tests, kids-replay authentic). Tier-3 4x reviews: B SHIP;
    A/C/D DO-NOT-SHIP with DISJOINT blockers — A-CRIT-1 occupancy gate read static zone_persons config
    list not live occupancy; D-CRIT-1 DPM apply ungated + 3 hvac_predict emitters + AI-rules climate bypass
    (real R2-residual site found in coordinator.py, plan had mislocated it); C-H5 hvac.py-side consumer
    mutation-untested. Consolidated 19-finding fix-up dispatched 2026-08-10. AI-rules climate: cheap-block
    chosen (zero live climate rules), route-through-chokepoints parked.'
  origin:
    date: '2026-08-09'
    gist: 'operator: "I think manual were kids trying to cool their space and the arrester stops them"'
  why: 'Observed tonight: zone_2 preset went to `manual` at 16:49 and again at 17:14 — the kids walking
    to the thermostat in an 80F room and asking for cooling. The override arrester reverted both within
    10 and 5 minutes respectively. After 17:19 there are NO further manual writes: they gave up. The system
    trained them that the thermostat does not work.'
  sharp_problem: A manual cool request, from an occupied zone, at 80F, during recovery from a 24h absence,
    is the single highest-quality signal in the building — a human walked to a wall and said they are
    uncomfortable. The arrester treats it as noise to revert. Its only escape hatch is the separate temp_arrester_override
    switch, which the kids do not know exists and which had sunset.
  fix_direction: Exempt (or substantially delay) arrest when the zone is occupied AND the manual change
    moves toward comfort AND the temp delta is large. Arresting inside 5-10 minutes is worse than not
    arresting at all — the occupant never feels an effect, so they learn the control is broken.
  operator_action_taken_2026_08_09: 'temp_arrester_override switched ON and all three zones set to home
    at operator instruction. Verified: ceilings dropped 80 -> 76/77, all three cooling, runtime_exceeded
    cleared. Note the override has a 6h max life and sunsets on some house-state transitions, so this
    is relief, not a fix.'
  OPERATOR_DESIGN_2026_08_10: 'DELAY chosen over exempt. Plus three design inputs verbatim-captured: (1)
    IDENTIFICATION QUESTION (operator): "how will this situation be identified specifically to widen the
    delay/grace?" — the predicate for "occupant comfort request" needs to be explicit: candidate = manual
    setpoint change AND zone any_room_occupied/zone_persons non-empty AND change moves TOWARD comfort
    relative to current temp AND |delta| >= knob. (2) BATTERY-CONDITIONED GRACE: widen grace when the
    battery is genuinely full; return to strict below 80% SOC — resolves the duty-cycle/peak conflict
    by spending stored energy on comfort only when it is demonstrably spare (this is duty-cycle option
    (c) arriving via the arrester door). (3) GRADUATED CONCESSION ("bend towards the requester"): do not
    binary honor-or-revert. Ask 76 from 80 -> grant 76 for 15 min -> step to 78 for 15 min, monitoring
    APPROACH SPEED to setpoint throughout — a negotiation curve. Slow approach (unrecoverable load) should
    inform the step timing. Knobs: grace minutes, step size, step interval, SOC floor (80), delta threshold
    — all named, rung ladder in plan.'
  PLAN_2026_08_11: 'PLANNING_arrester_comfort_delay.md. STAGED: Cycle A = predicate + SOC-gated flat grace
    + coast-precedence guard (~95% of the kids-incident benefit); Cycle B (graduated concession + approach-speed
    observer) PARKED with evidence trigger — rare-fire state machine priced honestly. INV-COMFORT-DELAY
    falsifiable; single SOC accessor consumed by BOTH D1 and D3 (desync = sharpest risk, mutation must
    fail both); probe-first gate (AUDIT_arrester_comfort_delay.md) before build; kids-incident replay
    is the live acceptance criterion. Tier 3.'
  PLAN_REVIEWS_2026_08_11: 'Both NEEDS-REVISION (the protocol''s first subject, and it paid): R1-H1 preset-writes
    clobber a granted setpoint on Bryant preset thermostats — INV-violating by construction, 9 emit sites
    unguarded; R2-H1 dual-setpoint predicate undefined (heat_cool range drag, None crashes, single-target
    unmapped); R2-H2 the plan''s own "single SOC accessor" mitigation was a hollow anchor in blueprint
    form. REV-2 committed: §3.7 S1-S9 per-site verdict table + a NEW emit_set_preset_mode chokepoint (mirror
    of emit_set_temperature) + gate on BOTH surfaces (the revision caught that the review''s preset-only
    chokepoint was itself incomplete); per-mode comfort-relevant-leg predicate w/ worked examples, fail-closed
    on None; SOC evaluated ONCE at grant, comfort_delay_active pure; restart-mid-grace disclosed+accepted
    for Cycle A; shed DOMINATES comfort (INV falsification #6); kill-switch semantics per knob (SOC_FLOOR=0
    = deliberate blackout-risk acceptance w/ boot WARN); kids-replay must drive real _handle_climate_change.'
  PROBE_2026_08_11: 'D1/D2/D3 all GO. Qualifying events 44.4/wk (NOT rare — zone_2 = 43/49); SOC>=80 at
    ~50% of events so the gate is genuinely load-bearing; coast co-fire 13.6/wk -> D3 required; multi-thermostat
    zones = ZERO -> grant key simplifies to zone_id (H2 ambiguity killed by measurement); preset-flap
    co-fire 18% all on the incident evenings (confirms the R1-H1 seam); kids incident found in the qualifying
    set (SOC 99-100, coast, flap co-fire TRUE) = sanity anchor. VOCABULARY FINDING: the arrester writes
    NOTHING to ura_activity_log — it acts silently; the plan''s ledger deliverable is also a diagnosability
    fix. PERISHABLE FIXTURE EXTRACTED: kids-incident recorder rows (purge ~08-16) committed to quality/tests/fixtures/arrester_comfort/.'
  CYCLE_B_ESCALATION_DECISION: 'Probe found Cycle-B''s evidence trigger ALREADY MET (08-08 zone_2: 4 qualifying
    flips in 61 min). OPERATOR CALL: pull D4 graduated concession in-cycle, or keep staged? ORCHESTRATOR
    RECOMMENDATION: KEEP STAGED — the trigger being met justifies Cycle B NEXT, not simultaneous; bundling
    doubles a Tier-3 review surface, and Cycle A''s flat grace already covers the 44/wk common case. Cycle
    B dispatches immediately after A''s live validation.'
  next: 'NOTHING TO BUILD — the stale next that used to sit here ("Cycle A build after FAN-LAYER build
    slots clear") was PRE-BUILD TEXT never updated after the cycle shipped. Remaining work is organic
    proof only: awaiting a genuine occupant manual cooling request. Deliberately NOT simulated — a synthetic
    setpoint change is exactly what the comfort predicate must distinguish from a real one, so passing
    a fake proves nothing.'
  organic_evidence: 'shipwatch 2026-08-11: L3 pending — zero comfort_delay ledger entries in 14h (no qualifying
    manual yet); founding-case proof awaits next kid-thermostat event. Cycle B stays staged on it.'
- id: BLE-WARM-CREATE-1
  title: BLE re-creates bathroom occupancy inside the 10-min warm window on every toilet visit (v5.22.0
    left this open by design)
  thread: presence
  status: done
  CORRECTION_2026_08_31: 'The 2026-08-31 REOPEN was WRONG and is retracted. The CREATE fix DID ship:
    commit 6659049a3 (delete BLE admission motion leg (b) — BLE extends occupancy, never creates it) is
    an ancestor of BOTH develop and master (shipped v5.66.0); f561e3884 records the operator ACCEPTED
    extend-across-restart as intended (D-MEDIUM-1 opt 1). The soak sweep''s 57 count conflated INTENDED
    BLE EXTENDs (the shipped behavior) with the deleted CREATE flash — it did not discriminate create
    from extend. Verified via git merge-base --is-ancestor. Re-disposed DONE. The genuine live residual
    (BLE EXTEND from adjacent-bedroom bleed holding the bath occupied all night/day with no body
    corroboration) is a DISTINCT mechanism the create-fix never addressed — carded as BLE-BLEED-EXTEND-SLEEP-1.'
  shipped_version: v5.66.0
  approval: unreviewed
  tags:
  - no-fabrication-verify
  - measure-before-build
  origin:
    date: '2026-08-10'
    gist: operator in the master toilet; bathroom light came on briefly, reproducibly ("when I enter it"),
      during daytime despite only-when-dark
  why: 'MEASURED from recorder attrs, two events this morning: 09:53:32 and 10:19:35, both occupancy_source=ble,
    ble_persons=[Oji], tier1_provenance all-False, fresh became_occupied_time (CREATE not extend), off
    again 41s/37s later. NOT a v5.22.0 regression — the predicate is intact and both events were admitted
    by its MOTION LEG: last motion 5.5 and 9 min prior, inside BLE_MOTION_CONFIRM_MULTIPLIER(2) x occupancy_timeout(300s)
    = 10 min. The comment says the leg exists to "cover the handoff tick where motion just timed out"
    — but 10 minutes is not a handoff tick.'
  mechanism: 'Every toilet visit passes through the bathroom -> legitimate motion -> bathroom occupancy
    -> times out while operator sits in the toilet -> his BLE still resolves to the master_bathroom area
    (the toilet is inside the bathroom scanner footprint) -> Bermuda flap re-fires BLE within the warm
    window -> occupancy CREATED -> entry actions -> light on -> BLE flap clears -> off ~40s later. Reproducible
    by construction: the pass-through guarantees fresh motion, so the warm window is always open when
    it matters. v5.22.0 fixed the COLD strobe (chain broken, no recent motion); this is the WARM strobe
    it deliberately admitted.'
  daytime_light_finding: NOT a lux bug. sensor...masterbath_illuminance reads 8.5 lx live (cover 1 closed,
    interior room) -> lux_zone=dark is CORRECT. "Only when dark" is lux-based, not sun-based; the room
    genuinely is dark at 10am. Every occupancy row today carries last_lux_zone=dark. The daytime complaint
    is the BLE create firing entry actions, not the lux gate.
  third_writer_ruled_out: 40 HA automations enabled; none targets master bathroom lights (closet + ziri/jaya/guest
    bathrooms have their own; master bath does not). URA sole writer.
  fix_directions_not_built:
  - '(a) NARROW the motion leg to an actual handoff: admit BLE create only within ~1 tick (30-60s) of
    the occupancy-off transition, not 2x timeout from last motion. Smallest change; kills the reproducible
    case; still covers the documented purpose.'
  - '(b) ADJACENT-ROOM ATTRIBUTION: suppress BLE create when the same person''s BLE also explains an ADJACENT
    occupied room (master_toilet occupied=on by the same person throughout). This is the fusion-library
    intent/evidence + adjacency territory — richer, heavier.'
  - '(c) ROLE SPLIT: BLE-created occupancy extends holds/HVAC but does not fire LIGHT entry actions. NOTE:
    sensor_role.RoleQuery.CREATOR_VS_EXTENDER is RESERVED for exactly this — senscap v5.65.0 shipped the
    query surface unconsumed. This card may be its founding consumer.'
  OPERATOR_CHALLENGE_2026_08_10: '"Why break the extend but not create rule at all? It seems like it created
    it, no?" — CORRECT. Leg (b) mechanically IS a create (occupancy off -> on via BLE alone, fresh became_occupied_time).
    Adjudication: (1) its DOCUMENTED purpose (handoff tick) is already covered by the chain leg, whose
    prev-tick read is still True at the timeout tick; (2) its PROTECTIVE case (still body + BLE dropout)
    is mmWave''s job — and in the incident mmWave correctly read False because the room really was empty;
    leg (b) cannot distinguish flap-recovery from adjacent-room BLE bleed, and the floor plan makes bleed
    occur on every toilet visit.'
  REVISED_RECOMMENDATION: 'DELETE leg (b) rather than narrow it — restore the invariant to what its name
    claims. Review must: (i) mutation-verify the tick-ordering claim that chain covers the handoff (comment-trusted
    today); (ii) enumerate rooms WITHOUT mmWave (D0 audit has the list) and check whether any relied on
    leg (b) for still-occupant recovery — those rooms lose the path. Kill-switch semantics survive (MULT=0
    already gates both legs).'
  LUX_RESOLVED_2026_08_10: 'Operator: "bathroom is genuinely bright... I moved the hamper." MEASURED:
    lux flat ~12 lx ALL morning, then 11.7 -> 147.8 -> 201.4 -> 241+ in NINE SECONDS at 12:18:44-53, stable
    ~246 since. The HAMPER occluded the sensor. Every light actuation 07:26-12:10 passed only-when-dark
    against a lying instrument. No code bug; physical fix by operator; daytime firing now self-suppresses.
    Same lesson as details_json: when the reading contradicts the room, suspect the instrument.'
  OPERATOR_GO_2026_08_10: 'DELETE approved: "Unless you see a livable scenario we missed, delete it...
    It is occupancy so go big on reviews and quality." Missed-scenario check done: even PIR-only rooms
    hold a still occupant via the chain leg while BLE is present at the timeout tick; leg (b) matters
    only if BLE ALSO dropped in that exact window (rare-squared), failure mode is lights-off-until- movement
    which PIR recovers. BATCHING: orchestrator call = SEPARATE hotfix (occupancy reviews stay pointed
    at one change); P24/D3/dropdowns ship as their own batch after.'
  REVIEWS_2026_08_10: 'A SHIP (1 LOW: const doc described the D2 kill by the WRONG MECHANISM — outer guard,
    not threshold-collapse; the 4th false-mechanism comment this week, caught in the commit that retired
    three others). B SHIP (boot regression RULED OUT: _last_occupied_state IS restored and _last_motion_time
    is NOT, so leg (b) was already inert on the first post-restart tick — nothing lost; 1 MED: unreachable
    fossil branch; L-B2: the constant name says multiplier but the BLE block now ignores magnitude — split
    into BLE_ADMIT_ENABLED bool + D2 multiplier in a later cycle). C SHIP ZERO findings (its own drill
    reintroduced a motion leg -> 6/7 anchors red with [None] correctly green; kill-switch gate load-bearing;
    oracles independently authored; only ONE ble source-write site exists). D SHIP-WITH-FOLLOWUP: invariant
    HOLDS across the diff surface; capability seam CLOSED (BLE_PRESENCE is corroborator-only, not injected
    into detection lists); ONE pre-existing MEDIUM at the boot/reload boundary.'
  FIXUP_2026_08_10: 'c37d155c3 on the build branch: A-LOW-1 doc line + B-M-B1 fossil deleted with tombstone.
    Orchestrator drill: reintroduced a 600s window myself -> 10 red; restored -> 21 green. Full suite
    22 failed / 8544 passed / 2 xfailed — failing names = the pre-existing wall-clock families (count
    drifted 21->22 with the date, winter-gate boundary class).'
  D_MEDIUM_1_OPERATOR_DECISION_NEEDED: 'The invariant is AMBIGUOUS at the restart boundary. _last_occupied_state
    restores True across a reboot with NO requirement of any in-process tier-1 evidence before the chain
    leg re-admits. Legal repro: occupant walks out during the 30-90s restart downtime, Bermuda still attributes
    them on first scan -> chain re-arms occupancy from BLE alone. PRE-EXISTING (leg (a) since v5.22.0)
    but the diff makes it the SOLE remaining seam. OPTIONS: (1) ACCEPT + PIN — declare extend-across-restart
    intended, restate the invariant with the carve-out, add a pinning test (smallest ingredient; matches
    the "extends INDEFINITELY" design comment; the repro window is narrow and self-corrects when BLE clears).
    (2) GATE — new _had_post_restart_tier1_evidence flag required by the chain leg (tighter invariant;
    adds a new boot-path state flag = rare-fire ingredient). ORCHESTRATOR RECOMMENDATION: option 1. Either
    way D-LOW-1 (zero tests on the post-restart first-tick path) gets its test written to match.'
  next: 'AWAITING TIER-3 OPERATOR CHECKPOINT: (a) go/no-go to merge+deploy; (b) the D-MEDIUM-1 option
    1 vs 2 pick. Branch worktree-agent-afcf959feefd95587 @ c37d155c3, not merged.'
  refs:
  - docs/readmes/README_v5.22.0.md (the reference cycle, same room, 2026-07-18)
  - custom_components/universal_room_automation/coordinator.py:2646-2700 (two-leg admission)
  - const.py:447 (BLE_MOTION_CONFIRM_MULTIPLIER=2)
  DEDUPE_2026_08_10: 'Sweep: v5.22.0 cycle is the PARENT fix (cold strobe) — this is its documented residual,
    not a duplicate. STUCK-SENSOR-1/chatter unrelated (different detector). Fusion-library section 7 intent/evidence
    is the (b)/(c) design home, linked not merged. CREATOR_VS_EXTENDER reserved query = planned consumer.
    NEW.'
  organic_evidence: 'shipwatch 2026-08-11: L2 strongly positive — 22h/~28 Master Bathroom cycles, ZERO
    ble-source occupancy post-deploy; last strobe was 3min PRE-deploy. Confirm at 48h.'
- id: BLE-BLEED-EXTEND-SLEEP-1
  title: Master Bath held occupied all night (441 min) by BLE bleed from the adjacent bedroom, with
    zero body corroboration — a genuine vacancy EXTEND while residents sleep
  thread: presence
  status: shipped_organic
  shipped_version: v5.93.0
  plan_ready_2026_09_01: 'Rev 5 §BUILD-READY. Two plan-reviews (Rev3 blanket→FIX, Rev4 toggle→FIX on config
    application) + confirm-review (Rev5, 1 doc HIGH fixed). Design cleared: per-room CONF_BLE_HOLD_CAP_ENABLED
    (read-site default via ROOM_TYPE_BLE_HOLD_CAP_DEFAULT + CONF_WET_ROOM schema mirror), 120min bath/closet/default,
    distinct ble_hold_cap NM. Queued behind the pytest lane (forecast validator). Next: build (Tier 2-DB+).'
  build_review_2026_09_01: 'BUILT (feature/ble-hold-cap @ f086e75e4) + 3 build-reviews: A SHIP, B SHIP, C FIX-REQUIRED.
    Core cap logic solidly anchored (all decision gates RED-on-neuter). Gaps: C-HIGH-1 NM wire-in neuter-deletable
    (add call-site anchor); C-MED-2 P24-distinctness observes one path; C-MED-3 duration-lookup coincidental-equality
    (#63) untested; A2/B4 NM prints cap constant not observed duration; B6 misleading else-log; README caveats
    (B1 restart resets clock, B5 humidity edge) + L5 wording. A1 anchor = OPERATOR DECISION (2026-09-01 "continue"):
    re-anchor on BLE-HOLD-START (recommended) — cap measures BLE-only sustain since last body, not whole session;
    handles the phantom identically, stops evicting a recently-moving occupant; still a single duration cap, no new
    knob. Fix pass folds A1 + all review fixes; QUEUED behind Envoy rework on the pytest lane.'
  refinement_2026_09_01: 'Operator: BELT-AND-SUSPENDERS — do BOTH levers, not A alone. (A) sleep-gated body-
    corroboration (require motion/mmwave for BLE to extend during sleep) AND (B) a GENERAL long timeout on
    BLE-extend-since-last-body (independent of sleep) as a backstop, so the protection generalizes beyond
    the sleep window. B can be a long timeout. Plan must add lever B alongside A (the plan currently picks
    A-only) before plan-review/build. Two knobs: the sleep-corroboration window + the general max-BLE-extend-
    since-body timeout (both module-const rung).'
  created: 2026-08-31 18:20
  updated: 2026-09-01 17:05
  refinement_status: refined
  approval: unreviewed
  sibling_of: BLE-WARM-CREATE-1
  tags:
  - no-fabrication-verify
  - measure-before-build
  problem_solution:
  - 'Problem: on nights when a phone sits in the master bedroom, the master bathroom reads OCCUPIED for
     the entire sleep window (measured: one continuous 441-minute / 7.3h hold, 22:53->06:14) because the
     sleeping resident''s Bluetooth keeps resolving into the bathroom (the bath scanner also hears the
     adjacent bedroom), refreshing the vacancy timer over and over with NO actual body signal (no motion,
     no mmWave). Solution: require a real body signal (motion/mmWave) to let Bluetooth EXTEND occupancy
     during sleep, or stop the bedroom-adjacent Bluetooth scanner from sustaining the bathroom, so a
     phone alone cannot pin the room occupied all night.'
  origin:
    date: '2026-08-31'
    gist: operator asked why the Master Bath LED is on at night / is vacancy being extended while sleeping
  why: 'Investigation of the operator''s LED report found URA drives NO light at night (the visible LED is
    a device standby indicator URA does not control), but surfaced a REAL adjacent finding: BLE bleed
    holds the bath occupied ~7h with blecorr=none, t1 all-False. Distinct from BLE-WARM-CREATE-1 (CREATE
    via the motion leg on toilet visits) — this is EXTEND (became_occupied_time never resets, timeout leg
    perpetually refreshed by BLE). Actuates no light (sleep gating), but skews humidity-fan presence
    runtime, HVAC presence, and occupancy analytics.'
  constraints:
  - Delicate — touches presence/occupancy fusion (the BLE EXTEND path). Regression-prone; Tier 2-DB+.
  refinement:
  - 'Operator REVERSED from belt-and-suspenders (A+B) to LONG-CAP-ONLY: "The only path I see is a long cap.
    Compatible with sleep because night lights assist moving around, not sustain lighting... compatible with
    any kind of presence cap we have and I think we have one. Find it and compare." -> dropped lever A (sleep
    gate) and the sleep-corroboration knob; Rev 3 is a single duration cap, reusing the EXISTING presence cap.'
  - 'MECHANISM FOUND (why the existing 60-min max-active failsafe never capped the 7.3h phantom): leg (ii) of
    the RESILIENCE-001 invariant at coordinator.py:~4180 — `STATE_OCCUPANCY_SOURCE not in ("camera","ble")` —
    DELIBERATELY exempts BLE-sourced occupancy (comment: a BLE chain-hold is "evidence of presence, not a stuck
    sensor"; force-vacate+latch would lock a visible person out). Confirmed by two disjoint reads (orchestrator
    + plan agent). So the failsafe BLOCK cannot be the cap; the cap must live INSIDE the BLE chain-extend block.
    Rev 3 reused the failsafe DURATION (_get_failsafe_duration_seconds) + NM-fire path there, P24 block byte-preserved.'
  - 'Rev-3 PLAN-REVIEW = FIX-REQUIRED, and it CONVERGES with the operator: a BLANKET cap reusing the 60-min
    bathroom failsafe duration would FALSE-EVICT real occupants — (C1-HIGH-1) a real bather on a >60min still
    soak (mmwave loses the still torso, BLE-only), and (C1-HIGH-2, worse) the SIX no-PIR rooms incl. Master
    Bedroom + Jaya Bedroom where a sleeper''s mmwave drops on a still body — the exact nightly-eviction the P24
    leg-(i) has-PIR exemption was written to prevent. Also: 60min is the SHORTEST number in the table, not the
    operator''s "long cap"; reusing the NUMBER without P24''s three GUARD conjuncts is a Class #63 re-scope; and
    the D2 test harness is not executable (NameError + false-green mutation anchors + hollow T-CAP-6).'
  - 'OPERATOR DESIGN (2026-09-01): BLE extend is legitimately valuable in bedrooms + shared living spaces
    (living/dining) where people are genuinely still for long stretches, so a blanket cap is wrong there. Make
    the cap a PER-ROOM TOGGLE in the room config (config flow + mirrored in options flow), default OFF, but
    default ON for certain room types at SETUP (bathrooms, closets, ...). This directly resolves C1-HIGH-2
    (bedrooms default OFF -> sleepers never evicted) and C1-HIGH-1 (bathrooms opt in, accepting the soak trade).
    Prior art clean: per-room CONF_*_ENABLED toggles are the dominant pattern (fan_control/humidity_fan/sleep_
    protection_enabled); config_flow ALREADY sets per-room-type defaults at setup (occupancy_timeout via
    ROOM_TYPE_TIMEOUTS, config_flow.py:991-995) — the room-type-default-on machinery is REUSED, not new.'
  knobs:
  - 'CONF_BLE_HOLD_CAP_ENABLED (rung 2 config/options flow, per-room bool) — NEW; mirrors the many per-room
    CONF_*_ENABLED toggles. Kill-switch semantics: OFF => BLE extends freely (today''s behavior).'
  - 'ROOM_TYPE_BLE_HOLD_CAP_DEFAULT (rung 1 module const, room_type->bool) — NEW; applied at config-flow setup
    like ROOM_TYPE_TIMEOUTS. bathroom/closet => True, else False.'
  - 'BLE_HOLD_CAP_DURATIONS (rung 1 module const, room_type->seconds) — NEW per review C1-MED-3; MAY default to
    ROOM_TYPE_FAILSAFE_DURATIONS but named separately so the BLE-cap policy can diverge (operator said "long cap"
    — 60min bath is the shortest, not long). Recommend own longer bath default, tunable.'
  - 'Rev 4 WRITTEN (2026-09-01): per-room CONF_BLE_HOLD_CAP_ENABLED toggle + ROOM_TYPE_BLE_HOLD_CAP_DEFAULT
    (bath/closet ON, all else incl. 6 no-PIR rooms OFF -> C1-HIGH-2 resolved by construction) + separate
    BLE_HOLD_CAP_DURATIONS map. Prior art confirmed: mirrors CONF_FAN_CONTROL_ENABLED (const.py:958,
    config_flow.py:1902/1914); reuses the ROOM_TYPE_TIMEOUTS.get setup-default pattern (config_flow.py:991-996);
    options flow lives IN config_flow.py (no separate options_flow.py). Decisions: F1 (no freshness conjunct,
    matches "long cap" brief, self-heals on next Tier-1 fire); N1 (distinct kind="ble_hold_cap" + new
    _fire_ble_hold_cap_nm, drops P24 dedup for diagnostic honesty). Test harness fixed (exec-ns injection,
    mutation anchor #3 re-homed to a real-coordinator unit test, T-CAP-6 replaced with two-path observation);
    L3 targets a real no-PIR room (Master Bedroom, default-OFF) as the non-eviction discriminator; L4 measures
    Tier-1-unprovenanced occupied minutes. New invariant C (setup-default correctness) + config round-trip tests.'
  - 'DURATION CONFIRMED (operator 2026-09-01): 120 min for bathroom + closet (and default). Plan updated to
    BLE_HOLD_CAP_DURATIONS bathroom=120, closet=120, DEFAULT=120min.'
  next: 'Fresh plan-review (config surface changed, Tier 2-DB+) against the 120min durations -> build.
    Plan: PLANNING_ble_bleed_extend_corroboration.md (Rev 4).'
  refs:
  - 'Investigation 2026-08-31 (read-only recorder probe): binary_sensor.master_bathroom_occupied 441-min
    hold 08-29->30, occupancy_source=ble, ble_persons=[Oji]/[Ezinne,Oji], no mmWave/motion; all URA lights OFF'
  - 'URA night LED (NOT the cause): switch.sonoff_1002197ef7_1 MasterBathLED — OFF every night'
- id: NIGHT-LIGHT-NO-OFF-PATH-1
  title: A night_lights-only entity is never turned OFF by URA — the Master Bath under-cabinet light
    stays on 20-29h (all night AND day) until a human/device clears it
  thread: presence
  status: shipped_organic
  shipped_version: v5.92.1
  audit_result: 'AUDIT_room_light_automation.md (2026-08-31). Blast radius = 5 night-ONLY rooms:
    Master Bathroom (founding), Study B, Kitchen (range light — see KITCHEN-NIGHTLIGHT-RANGE-MISCONFIG-1),
    Garage Hallway, Master Bedroom. 15 rooms dual-list (ride the existing CONF_LIGHTS exit — this is why
    other night lights turn off). Fix (Option A, off-on-vacancy, recommended — captures the whole complaint,
    smallest diff, no time-seam): T1 automation.py:1040 _control_lights_exit turn-off set = CONF_LIGHTS
    UNION CONF_NIGHT_LIGHTS; T2 actuator_reconciler.py:798-804 widen vacant-branch membership to the same
    union + update the A-HIGH-1 comment (parity-only; reconciler is reconcile-on-return); T3 automation.py:3317
    shared-space same union. Tier 2-DB. Option B (stay-on-till-wake) needs a NEW sleep-end OFF handler
    (does not exist) = state-transition seam — parked behind an explicit operator trigger.'
  created: 2026-08-31 18:35
  updated: 2026-08-31 19:05
  refinement_status: initial
  approval: unreviewed
  tags:
  - no-fabrication-verify
  - tier-2db
  problem_solution:
  - 'Problem: the Master Bath under-cabinet light (a Sonoff switch configured as the room''s night light
     only, not a regular light) turns ON when someone enters in the dark, then NEVER turns off — it stays
     lit for 20-29 hours straight, across the whole night and into the next day, until a person or the
     device itself happens to switch it off. URA has code to turn night lights ON but NO code path that
     turns a night-light-only entity OFF (all three off-paths only act on the main lights list). Solution:
     give night-light-only entities an OFF path — turn them off on vacancy (kept dim during sleep), or
     off at the sleep-end / dark->bright transition, per the operator''s intended behavior for the strip.'
  origin:
    date: '2026-08-31'
    gist: operator report — Master Bath LED (the Sonoff under-cabinet night light) seems always on at night, and holds during days too
  why: 'ROOT-CAUSED (read-only probe 2026-08-31). switch.sonoff_1002197ef7_1 is in night_lights ONLY
    (not CONF_LIGHTS). URA ON-paths act on night_lights (_control_lights_entry -> _turn_on_night_lights,
    automation.py:991/1021/1133); all THREE OFF-paths are CONF_LIGHTS-scoped and explicitly EXCLUDE
    night-lights-only entities (automation.py:1037 _control_lights_exit; actuator_reconciler.py:793-805
    _resolve_light vacant branch, with the A-HIGH-1 comment at :795 stating night_lights-only is NEVER
    turned off on exit; automation.py:3319 _shared_space_turn_off_all). Writer confirmed: context_user_id
    None on the ON (URA service call, 2s after a dark occupied entry at 20:26); the irregular OFFs occur
    while VACANT = external (human/device). Hold length = time-until-external-off, hence 20-29h. Also: a
    night light lit for SLEEP is never extinguished when sleep ENDS (reconciler returns None both cases).'
  constraints:
  - 'Shared-primitive precedence change (automation.py exit logic + actuator_reconciler _resolve_light MUST
    stay in D2.10 parity, A-HIGH-1). Regression-prone -> Tier 2-DB (3 framing-disjoint reviews).'
  operator_intent_question:
  - 'Design intent for the under-cabinet night light: (A) off on VACANCY like a dim regular light, or (B)
    stay on through the night and off at WAKE/dark->bright? Determines which off-trigger to add. NEEDS OPERATOR.'
  next: 'Operator picks intent (A vs B). Then add the night_lights OFF path mirroring the CONF_LIGHTS exit
    logic (+ reconciler parity), gated to preserve the sleep-dim behavior. Tier 2-DB. Queue behind charge-onset.'
  refs:
  - 'automation.py:991/1021/1037/1133/3319; actuator_reconciler.py:793-805 (:795 A-HIGH-1 comment)'
  - 'Live: switch.sonoff_1002197ef7_1 ON 08-28 06:27->08-29 06:46, 08-29 08:12->08-30 13:32, 08-30 20:26->08-31 16:19'
- id: LIGHT-SLEEP-ENTRYNONE-DIVERGENCE-1
  title: Canonical vs reconciler disagree on night lights in entry=none rooms during sleep (pre-existing parity break)
  thread: presence
  status: pre_planning
  created: 2026-08-31 19:05
  updated: 2026-08-31 19:05
  refinement_status: initial
  approval: unreviewed
  sibling_of: NIGHT-LIGHT-NO-OFF-PATH-1
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: for rooms whose entry-light action is none (e.g. Master Bedroom, Patio, Game Room), the two
     light controllers disagree during sleep: the reconciler would turn the night light ON (its sleep
     branch runs before the entry-action check) but the canonical path never would (it early-returns).
     They are supposed to agree (D2.10 parity). Also, a room with an empty regular-lights list is
     short-circuited entirely in canonical, so a night-only room never gets its night light managed there.
     Solution: pick one behavior for entry=none + sleep + night lights and make both controllers honor it
     (either gate the reconciler sleep branch on entry-action, or make canonical manage night lights
     independent of entry-action) — decide, do not paper over.'
  origin: {date: '2026-08-31', gist: light automation audit F2/F3}
  why: 'AUDIT_room_light_automation.md F2 (MED) + F3 (MED). Predates the night-light off-path cycle; must be
    resolved as part of, or before, the NIGHT-LIGHT-NO-OFF-PATH-1 build so the fix does not entrench the split.'
  next: 'Operator/orchestrator decision on the intended entry=none+sleep night-light behavior; then align
    both canonical + reconciler. Coordinate with NIGHT-LIGHT-NO-OFF-PATH-1 (same files). Tier 2-DB.'
  refs: ['docs/planning/AUDIT_room_light_automation.md F2/F3', 'automation.py:973/980; actuator_reconciler.py:746']
- id: KITCHEN-NIGHTLIGHT-RANGE-MISCONFIG-1
  title: Kitchen night light is configured as the RANGE light (switch_tapo_wifi_kitchenrange) — likely a config mistake
  thread: presence
  status: waiting_operator
  created: 2026-08-31 19:05
  updated: 2026-08-31 19:05
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: the Kitchen room lists its range-hood light (switch_tapo_wifi_kitchenrange) as the room night
     light. Once the night-light off-path fix lands, URA will start turning the RANGE light on at dark
     entry and off on vacancy as if it were an under-cabinet night light — probably not intended. Solution:
     operator confirms whether the range light should be the Kitchen night light; if not, repoint or clear
     it in the room config (options-flow), no code change.'
  origin: {date: '2026-08-31', gist: light automation audit F4}
  why: 'AUDIT_room_light_automation.md F4. Surfaced now because the off-path fix changes how this entity
    behaves; better to correct the config before the fix ships than to drive the range light unexpectedly.'
  next: 'Operator: is switch_tapo_wifi_kitchenrange the intended Kitchen night light? If not, fix the room config.'
  refs: ['docs/planning/AUDIT_room_light_automation.md F4']
- id: FAN-MANUAL-1
  FIX_APPLIED_2026_08_23_config_not_code: 'OPERATOR RULING: "Remove the fan from manual switches. Old
    workaround." APPLIED — config-only, no code cycle needed. The managed fan is removed from manual_switches
    in all THREE affected rooms, so the vacancy exit sweep (automation.py:1265-1290) no longer has a handle
    on a fan the HVAC tier owns: Living Room     [fan.towerfan_dreopilotmaxs_wifi_livingroom] -> [] Jaya
    Bedroom    [fan.fanswitch_treat_wifi_jayabedroom]       -> [] Master Bedroom  removed fan.polyfan_508s_wifi_masterbedroom
    ONLY; PRESERVED fan.ceilingfan_fanimaton_rf304_25_masterbedroom and light.switch_mmwave_inovelli,
    which are genuinely manual-only (not in that room''s `fans` list). Verified: zero rooms now have any
    entity in BOTH `fans` and `manual_switches`. APPLIED VIA THE REAL OPTIONS FLOW (devices step), not
    a .storage edit — a direct file write is clobbered by HA in-memory entry state. SURGICAL CHECK, and
    one honest wrinkle: Living Room 53->53 keys and Jaya 68->68 keys, ONLY manual_switches changed. Master
    Bedroom went 63->67 because I omitted four optional fields and the form materialised its defaults:
    adjacent_rooms None->[], fan_recheck_l2_allowed None->True, room_fan_recheck_enabled None->True, fan_recheck_trust_sensors_ok
    None->True. VERIFIED HARMLESS — const.py:629/636/644 all define those defaults as True, so an absent
    field already resolved to True; the values are now explicit rather than implied. adjacent_rooms []
    and absent are equivalent. No behaviour changed, but the entry is no longer minimal, which is worth
    knowing if anyone diffs it. LESSON for future options-flow writes: pass EVERY field in the step, not
    just the one being changed, or unpassed optionals will materialise at their schema defaults. ORGANIC
    PROOF STILL OWED: the discriminating observation is a vacancy exit occurring while the fan is manually
    ON — the fan must now SURVIVE it. That is exactly the sequence that killed it at 11:27:16 today (776ms
    after the occupancy edge). Until that is observed, this is applied-but-unproven.'
  WRITER_IDENTIFIED_2026_08_23_manual_switches_exit_sweep: 'MECHANISM PINNED. It is NOT the HVAC fan logic
    at all, and BOTH earlier hypotheses on this card (mine: missing branch; then: tier ownership) are
    superseded by this one, which has millisecond evidence. THE WRITER: the room EXIT automation''s manual-switch
    sweep. automation.py:934   occupancy off -> _handle_exit automation.py:960   _handle_exit  -> _control_manual_switches_off()
    automation.py:1265-1290  issues `homeassistant.turn_off` over manual_devices + manual_switches, blocking=False,
    _LOGGER.debug only — with NO consultation of the manual-ON hold, NO FanPolicyOracle actuate() wrap,
    and no hvac_fans involvement whatsoever. THE FAN IS REGISTERED TWICE. Living Room config carries the
    SAME entity in two axes: fans            = [fan.towerfan_dreopilotmaxs_wifi_livingroom] manual_switches
    = [fan.towerfan_dreopilotmaxs_wifi_livingroom] So the HVAC tier grants a manual-ON hold and the room-tier
    vacancy sweep turns it off anyway, because the sweep does not know the hold exists. MILLISECOND EVIDENCE
    (recorder): 11:27:16.095  binary_sensor.living_room_occupied  on -> off 11:27:16.871  fan ... on ->
    off        (776 ms after the vacancy edge; no context_user_id) 11:27:19.730  the 5-min HVAC tick      (2.9
    s LATER — the tick did not do it) Nothing else in the house changed state in that window. TEST 2 EXPLAINED
    WITH NO EXTRA ASSUMPTION: ura_activity_log shows a Living Room occupancy_exit at 11:38:35, seventeen
    seconds BEFORE the 11:38:52 manual ON, and no further exit until the operator OFF at 11:50:59 (which
    carries context_user_id). No exit edge while the fan was on => no sweep => the fan survived, and the
    11:42:13 HVAC tick then opened the 1h hold. Test 1''s ON landed inside an occupied window that expired
    235 s later. SCOPE IS THREE ROOMS, NOT ONE — verified directly in core.config_entries: Living Room     fan.towerfan_dreopilotmaxs_wifi_livingroom
    Master Bedroom  fan.polyfan_508s_wifi_masterbedroom Jaya Bedroom    fan.fanswitch_treat_wifi_jayabedroom
    OPERATOR QUESTION, genuinely undecidable from code: is listing a managed fan in manual_switches DELIBERATE
    (so vacancy always kills it) or leftover config? If deliberate, the fix is that the sweep must consult
    the manual-ON hold. If leftover, the fix is config. Do not assume. MY EVIDENCE WAS UNSOUND AND I AM
    RECORDING THAT: I cited "ZERO hvac_fans log lines in test 1" as proof no branch ran. HA''s retained
    log buffer only reaches back to ~11:39, so that negative was UNOBSERVABLE, not observed. Fourth time
    today I treated absence-of-observation as observation-of-absence. It happens not to change the verdict
    — the confirmed writer never logs above DEBUG — but the reasoning was invalid.'
  MECHANISM_FOUND_2026_08_23_cooldown_gated_detection: 'ATTRIBUTION RESOLVED by re-running the test with
    URA at debug. The earlier PENDING-WITH-ADVERSE-EVIDENCE instance is now EXPLAINED, and the feature
    is PARTIALLY WORKING rather than broken. THE DECISIVE LOG LINE (test 2, fan switched on externally
    at 11:38:52): 11:42:13 INFO [universal_room_automation.domain_coordinators.hvac_fans] "HVAC Fans:
    Living Room turned on during cooldown — cooldown cleared, manual_on_hold_until=2026-08-23T12:42:13-05:00"
    URA DETECTED the external turn-on and granted a ONE-HOUR manual hold. The fan then held for the remainder
    of the observation at 78-79F, i.e. BELOW the 80F fan_temp_threshold — exactly the condition that reverted
    it in test 1. So the manual-ON hold demonstrably works on that path. THE DEFECT IS THE GATE, AND THE
    LOG NAMES IT: the ONLY hvac_fans line in the entire log is the one above, and its own wording is "turned
    on DURING COOLDOWN". Test 1 (11:23:21 on -> 11:27:16 off, temp 78F throughout) produced NO hvac_fans
    line at all — the fan had been off since the 11:11:57 boot, so no cooldown was active, the external
    ON was never registered as manual, no hold was set, and it was reverted ~4 minutes later. CONCLUSION:
    manual-ON is detected when the fan is switched on while a cooldown is running, and apparently NOT
    when it is switched on from a cold/idle state. That is a one-missed-path defect (Bug Class #53 shape),
    not a dead feature — which is why the card must NOT be closed as confirmed NOR scored as a flat violation.
    STRENGTH OF EVIDENCE, stated honestly: one observation each way, plus a log line whose own text names
    the gating condition. That is suggestive and mechanism-consistent, not proven. The CONFIRMING TEST
    is cheap and precise: wait until no cooldown is active and the manual hold has expired (after 12:42),
    verify no cooldown is running, then switch the fan on externally again. If it reverts with no hvac_fans
    line, the cold-start path is confirmed unhandled. ALSO CORRECTS AN EARLIER READING ON THIS CARD: the
    boot warning "Living Room ... is not in HVAC fan_controller._room_fans — room-tier is owning fans"
    led me to suspect the HVAC tier was not involved at all. It IS — the detection above comes from domain_coordinators.hvac_fans.
    The tier warning is still real and still worth chasing (see FAN-LAYER-2), but it is NOT the mechanism
    here.'
  INSTANCE_2026_08_23_operator_owed_manual_test_RUN: 'THE OPERATOR-OWED MANUAL TEST HAS NOW BEEN RUN BY
    CLAUDE — it did not need the operator. Result is ADVERSE but attribution is INCOMPLETE, so this is
    recorded as PENDING-WITH-ADVERSE-EVIDENCE, deliberately NOT `violated`. NON-VACUITY CHECKED FIRST
    (this nearly went unchecked and would have made the result meaningless): Living Room config carries
    fans=[fan.towerfan_dreopilotmaxs_wifi_livingroom], fan_control_enabled=True, fan_temp_threshold=80.0
    — the fan IS URA-managed and CAN be reverted. A "stayed on" result would have proved nothing if URA
    did not own the fan. FOUNDING CONDITION REPRODUCED EXACTLY: binary_sensor.living_room_fan_should_run
    = off since 11:11:59, i.e. URA believed the fan should NOT run, and an external actor switched it
    on anyway. SEQUENCE (2026-08-23 CDT): 11:23:21 fan -> on, via an external service call carrying NO
    context_user_id — indistinguishable from a physical button press, which is the right fidelity for
    this test. 11:27:16 fan -> off. Held ~4 minutes across at least one decision tick, then reverted,
    with should_run still off. WHY THIS IS NOT SCORED AS VIOLATED: no log line names URA as the writer
    of the OFF; the logbook entry is unattributed; the Dreo device/cloud could also have issued it. Consistent-with
    is not proof-of, and this session has had four asserted mechanisms refuted by later measurement. WHAT
    WOULD SETTLE IT: a debug-level URA log attributing the OFF write, OR a repeat of the same test with
    fan_control_enabled=False as a control — if the fan then HOLDS, URA is the writer.'
  INSTANCE_2026_08_23_fans_on_reads_zero_while_fan_runs: 'SECOND FINDING from the same test, folded here
    rather than carded — adjacency sweep found NO existing card mentioning `fans_on`, and this may share
    a mechanism with the revert above (see FAN-LAYER-2 instance of the same date). sensor.living_room_fans_on
    read 0 for the ENTIRE period the fan was physically ON (11:23-11:27), spanning at least one decision
    tick, and never moved off 0 (last_changed stuck at the 11:11:57 boot). A fans-on counter reading zero
    while a managed fan runs is a value that disagrees with physical reality, and anything downstream
    that trusts it inherits the error. NOT YET DISCRIMINATED: tick latency vs the sensor being fed by
    a path that cannot see this fan. The second is more likely given the tier-ownership warning recorded
    on FAN-LAYER-2.'
  links:
    related:
    - FAN-LAYER-2
    context: FAN-LAYER-2 carries the 2026-08-23 tier-ownership warning (Living Room configured for HVAC-tier
      fan management but actually owned by the room tier) plus FanPolicyOracle fallbacks — the leading
      hypothesis for BOTH the manual-ON revert and the fans_on=0 reading on this card.
  title: 'Fans have no manual-ON override: room temp logic reverts a hand-switched fan ("below threshold")'
  thread: hvac
  status: done
  shipped_version: v5.68.0
  approval: explicit
  approved_by: operator 2026-08-10 ("If there is no manual override, design one after considering all
    our fan goals and touch points across room, zone, coordinators")
  tags:
  - context-wide-scoping
  - institutional-context
  - numbers-get-knobs
  origin:
    date: '2026-08-10'
    gist: '"I cant seem to turn on the living room fan manually without it turning off by itself."'
  why: 'EVIDENCE (ura_activity_log): Living Room fan turn-offs are [room/fan_off] "Fans off (below threshold,
    77F)" — the room-tier temperature comfort controller reconciles fan state to its own verdict and does
    not recognise a manual ON. v5.31.0 shipped a manual-OFF cooldown; the ON side was never built. A human
    switching a fan on is the canonical EVIDENCE event (fusion section 7: "interactions are evidence")
    and today it is treated as drift to correct — the same class as ARREST-COMFORT-1 (kids arrested at
    the thermostat) and the same lesson: reverting a human within minutes teaches them the controls are
    broken.'
  scope_note: 'Operator mandate: context-wide. Fan touchpoints to inventory before design: room comfort
    fan control (handle_temperature_based_fan_control + the below-threshold revert), v5.31.0 manual-off
    cooldown (the precedent + its knob), fan_recheck (pause/restore machinery), comfort-fan AWAY veto
    (v5.40.0 D3), sleep-onset bedroom fans (feature/sleep-fans), bathroom humidity exhaust unification
    (v5.6.0 — humidity fans are SOLE-OWNER exempt from the veto), fan interference gate (presence-side
    reader of fan state), zone vacancy sweep, AI-rules executor (R2: fan.turn_on unvetoed — a prior review
    residual), duty-cycle fan_assist flag in the coast constraint.'
  PLAN_2026_08_10: 'docs/planning/PLANNING_fan_manual_on_override.md. Shape: plain timed manual-ON hold
    symmetric to the v5.31.0 manual-OFF cooldown — graduated-concession REJECTED for fans (binary comfort,
    no setpoint to negotiate; margin ~0). Detection REUSES the existing OFF-side mechanism (_last_seen_any_fan_on
    baseline + self-write marker; one new symmetric _fan_on_issued_this_tick flag — no second manual-detection
    path). INV-FMH: for hold_s after an external ON, no URA path emits fan turn_off except four enumerated
    discharges (expiry, external OFF, allowlisted fan-recheck, safety, fan_control toggle-off). Knobs:
    DEFAULT_FAN_MANUAL_ON_HOLD_S=3600 (0=kill) + per-room CONF. Tier 2-DB. SHARPEST RISK is not the hold:
    hvac_fans.py already OVERLOADS manual_off_cooldown_until as a partial ON-marker (confused comment
    at :788) — the field split has >=3 readers and missing one ships a masked diagnostic regression; Review
    B must enumerate all.'
  OPERATOR_RULINGS_2026_08_10: 'Both as recommended: (1) FRESHEST WINS — a manual-ON newer than the sleep
    transition survives it; (2) fan-recheck OFF is ALLOWLISTED via trigger_path, hold remaining-time preserved
    across the pause. Build fully approved (build-implies-ship).'
  REVIEW_ROUND_2026_08_11: 'ALL THREE DO-NOT-SHIP — 1 CRIT + 6 HIGH, disjoint framings, zero overlap.
    C-CRIT-1: the HVAC chokepoint gate (the plan''s headline enforcement) had ZERO coverage — deleting
    it left 8,564 tests green, because every _set_fan_state-reaching test zeroed the hold in fixtures.
    A-HIGH-1: boot-seed guard swallows tick-1 detection → after any entry reload URA kills the fan the
    operator just turned on (the ORIGINAL complaint, resurrected). A-HIGH-2/B-HIGH-2: the reconciler in
    BOTH directions — its ON mis-detected as external (fan pinned 1h), its OFF re-asserts against a held
    fan on its own cadence. B-HIGH-1: zone-vacancy sweep (hvac.py:2418) bypasses the chokepoint entirely
    — the MISSED emission site; a one-line grep (CONF_FANS.*turn_off) would have found it and neither
    plan, build, nor dispatch brief ran it. C-H2: the discharge-(b) test asserted None==None (its hold
    was never opened). C-H1+A-MED-2: recheck extension unanchored AND buggy (mid-pause expiry clears the
    hold, losing the extension). MED-B2: room-tier restart re-adoption claim is provably aspirational.'
  next: 'Config fix APPLIED 2026-08-23 (fan removed from manual_switches in 3 rooms). Awaiting the discriminating
    organic observation: a room vacancy exit while the fan is manually ON, with the fan SURVIVING it.
    Until then applied-but-unproven. No code change needed unless that observation fails.'
  organic_evidence: 'shipwatch 2026-08-11: L2 PENDING ON OPERATOR — manual Living Room fan-ON test still
    owed; no organic 1h+ manual hold observed yet (both v5.68.0 hold + v5.70.0 delegation ride this one
    test).'
  organic_evidence_2: 'OPERATOR CONFIRMED 2026-08-11: "Living room fan is working afaik" — manual fan
    use no longer self-cancels. Closes v5.68.0 L2 + v5.70.0 L3 + v5.72.0 L4 (the one shared operator test).'
- id: KHOST-2
  title: Operator disposition buttons + drag-between-states on the hosted board
  thread: tooling
  status: shipped_organic
  shipped_note: 'Live 2026-08-11: buttons (done/deferred/declined) + column drag on urakanban LAN site;
    queue → pending jsonl → agent session-start apply (operator authority). Orchestrator-verified API
    round-trip. Organic proof: first real operator disposition flowing through to kanban.data.yaml.'
  approval: approved
  origin:
    date: '2026-08-11'
    gist: 'operator: "We have to add operator disposition buttons so the operator can communicate through
      the board. drag btw states | done | deferred | declined"'
  why: 'Board is currently read-only reflection; operator decisions still have to travel through chat.
    Dispositions through the board close the loop: tap/drag → queued → agent applies to kanban.data.yaml
    at session start (agent-in-loop, no unattended repo writer).'
  constraints: LAN-site only (Artifact CSP cannot POST). No auth on LAN site per operator choice. Source
    of truth stays kanban.data.yaml; the queue is pending-intent, not state.
  next: 'UN-WATCHABLE (not shipped): Build the webhost micro-API + JS drag/button interactions. No URA
    HA oracle for this feature. Recover as shipped when board JS is deployed.'
  shipped_version: board-tooling-not-yet-shipped
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: disposition buttons + drag-between-states JS micro-API not yet shipped (next
    field says "Build: webhost micro-API"). shipped_version recovered as board-tooling-not-yet-shipped.
    No HA oracle. UN-WATCHABLE until build lands.'
- id: FRIGATE-RETIRE-1
  title: Retire Frigate-1 — promote Frigate-2 (yolov9t/OpenVINO, zero night ghosts) to primary incl. snapshot
    engine
  thread: security
  status: done
  approval: approved
  origin:
    date: '2026-08-12'
    gist: 'operator: "We should just retire frigate 1 instead of writing more code" + "Frigate 2 is our
      identical backup. We should move snapshots to it" + "Go".'
  why: 'Probe: 100% of night person alerts = frigate-1 single-witness sub-2s IR ghosts; frigate-1 thresholds
    already raised once (07-30 snapshot) and ghosting persists. Frigate-2 runs a DIFFERENT detector (custom
    yolov9t.onnx OpenVINO) with ZERO night ghosts all week on the same cameras. MQTT collision VERIFIED
    FIXED live (frigate/frigate2 prefixes, f1/f2 client ids) — bleed ruled out; ghosts are frigate-1 detector-native.
    Swap beats patch.'
  procedure: 'CONSOL-1 §7 retirement doctrine: (1) capability inventory audit; (2) parity swap — URA perimeter
    sensors + snapshot instance -> frigate-2 with BOTH running; (3) Gate-1 N=5 organic events clean by
    ledger -> disable frigate-1; (4) Gate-2 N=5 more -> delete; (5) tripwire NM on any frigate-1 entity
    firing post-disable. Daytime-detection parity explicitly in the checklist (frigate-1 daytime was healthy).'
  audit_2026_08_12: 'GO. 24/24 cameras identical both hosts (sole gap: ArmCrestASH41B enabled F1/disabled
    F2 — deliberate one-NVR-at-a-time; flip at window open). URA needs ZERO code changes (fused legs subscribe
    both instances; snapshot discovery automatic, learn-on-success). Biggest non-URA consumer: Phase-1
    automation (already dual-prefix MQTT bridge). MANDATORY at window open: (1) ASH41B flip, (2) F2 disk/cache
    tripwire (F2 has 8-day silent-recording-outage history and becomes sole recorder), (3) P1 parity bar
    = agreement-with-Protect not raw counts.'
  window_open_2026_08_12: 'EXECUTED (operator "Go"): (1) F2 recording tripwire automation live (automation.frigate2_recording_tripwire_frigate_retire_1
    — 30min-poll frozen-share check >6h + unavailable-30min leg -> WhatsApp; template-trigger pitfall
    caught: a frozen sensor emits no state changes, so poll+condition, not template trigger). (2) ASH41B
    flipped: enabled F2 / disabled F1 (streaming 5.1fps on F2). (3) ALL 23 F1 cameras disabled via /api/config/save
    (ReolinkStudyBPorchPTZ had no explicit enabled key — caught on verify, second pass). Verified live:
    F1 0/23 enabled, F2 23/23 enabled, zero 0-fps cameras, both HA entries still loaded. Pre/post config
    snapshots local-only at ~/Code/homelab-automation/configs/snapshots/frigate{1,2}/ (gitignored — contain
    plaintext RTSP creds). Revert = re-push pre-retire-window snapshot.'
  operator_refinements_2026_08_12: '(1) FP-gate preference RULED: cross-corroboration (Protect agreement)
    preferred over duration/latency gates — "I don''t like the latency idea. Much prefer x-corroboration."
    Applies if frigate-2 ghosts post-promotion. (2) Evidence chain: hand-check audits are the referable
    record — AUDIT_perimeter_fp_correlation.md (ghost probe) + AUDIT_frigate1_retirement_inventory.md
    (parity/GO), both committed.'
  ura_ref_swap: 'FOUND during window-open verify (audit''s "zero URA changes" was wrong at the entity
    layer): 26 F1-owned entity refs across 4 URA config entries (main entry 14 perimeter cameras, Zone
    Manager 6 occupancy sensors, Garage Hallway 3, Garage B 3). All 26 have live F2 equivalents mapped
    BY REGISTRY OWNERSHIP not name (traps: staircase_motion_2 is F1 -> F2 twin is bare staircase_motion;
    garage_b_motion_2 -> garage_b_motion_3). Swap script ready (scratchpad swap_ura_frigate_refs.py, aborts
    unless exactly 26 hits, backs up first); mapping at f1_to_f2_map.json. BLOCKED on operator authorizing:
    ssh ha core stop -> edit .storage/core.config_entries -> core start. _2->base rename cleanup stays
    post-Gate-2.'
  entity_migration_2026_08_12: 'DONE (operator approved). Mechanism pivot: ha CLI unauthorized for ssh
    user -> did 50 entity-registry renames via API instead of .storage surgery: every dead F1 entity ->
    *_f1retired (reversible), F2 twin -> the id URA references. Bonus: automations.yaml staircase logic
    + ura_v8 dashboard now feed from F2 automatically; the audit''s post-parity _2->base rename step is
    ALREADY DONE for the 24 URA-referenced ids. Two special cases: garage_b_motion base is a Protect PIR
    (untouched; F2 _3 -> _2); staircase_motion base was consumed by 2 automations.yaml template refs ->
    edited refs to _2 + renamed F2 entity into _2 + automation reload (the .storage one-string edit got
    CLOBBERED by HA shutdown flush — known race, caught on verify). Post-restart: 25/25 URA-referenced
    ids F2-owned, zero URA errors, presence/census flowing. Watch-item: camera_census boot scan runs before
    Frigate 2 registers (pre-existing boot-order noise; device registry verified to carry full person-sensor
    suite).'
  gate1_recheck_2026_08_13: 'GATE-1 MET. Night window 08-12 23:00 -> 08-13 05:00 CT: ZERO person alerts
    (vs multiple F1 ghosts every prior night) — the ghost pattern died with F1. Only 2 legit-category
    vehicle deep-night alerts (rear_ptz, Protect-sourced). Clean count: 5 corroborated F2+Protect pairs
    (08-12 afternoon, 1-2s agreement) + decisive clean night = 5/5 + night criterion. Tripwire: 5 polls,
    all correctly failed_conditions (share 83.9%, rotating, usage growing 81.7->83.9 = writes flowing).
    AWAITING OPERATOR APPROVAL to advance: disable Frigate-1 integration + container -> Gate-2 N=5 begins.'
  gate2_started_2026_08_13: 'OPERATOR APPROVED. F1 HA integration entry 01JV6G4E57HT3WH86WSQ4RJT11 DISABLED
    + unloaded (11:4x CT). Container stop handed to operator (host is password-SSH only): ssh okosisi@192.168.13.16
    sudo docker stop frigate double-take. Gate-2 counting begins now: N=5 clean organic perimeter events
    with F1 dark -> operator may DELETE the HA entry and PHYSICALLY DECOMMISSION the box. Reversal until
    then: re-enable the entry + docker start.'
  next: Operator word on final deletion (entry + registry sweep) -> then card closes. Recording tripwire
    stays permanently (F2 is sole recorder).
  reaudit_2026_08_13: 'Operator-requested post-disable re-audit: PASS — 443 URA entity refs, 0 F1-owned;
    snapshot engine self-heals to F2-only; MQTT dual-prefix clean; census/resolver skip disabled entities.
    Findings ALL FIXED live same hour: MED automation.g6_doorbell_analysis motion_entity _2->_3 (was dead
    F1); LOW ura-v8 armcrestash41b->_2 + ura-v6 garage_a->_2 dashboard cards. Latent LOW carded in audit:
    camera_census legacy _discover_full_scan lacks disabled_by filter (not live). FIRST-WITNESS audit:
    F2 prompt (median +1.3s vs Protect, 31% first); caveats: rear_ptz laggard (median +5.8s, 20 missed
    episodes — consider Protect as snapshot trigger there); pool_equipment near-invisible to F2 (2 fires/week,
    native Amcrest is sole witness — recall check needed); back_yard ~100 F2-only episodes/wk uncorroborated
    (FoV-vs-FP spot-check before counting solos clean); 08-11 house-wide F2 daytime outage (predates tripwire;
    tripwire now covers). Docs: AUDIT_f2_pointing_reaudit.md + AUDIT_f2_first_witness_daytime.md.'
  gate2_met_2026_08_14: 'GATE-2 MET + BOX PHYSICALLY POWERED OFF (operator, ahead of formal completion
    — with all software layers already dark, physical-off IS the authentic Gate-2 test and it passed).
    Since F1 disable: 10 dispatches, all healthy — incl. a SAME-SECOND F2+Protect corroborated pair (23:54Z),
    evening cluster, two clean daytime events today. Night window 2: ZERO F2 ghosts; sole night event
    was Protect-sourced rear_ptz 23:17 CT (not the retired failure class — neutral). Tripwire: silent
    through both nights, share still rotating. HA logged ZERO errors about the dead host (disabled entry
    = HA does not notice). Remaining FINAL steps (operator word required, irreversible): (1) DELETE the
    F1 HA config entry; (2) sweep the *_f1retired renamed entities + F1s 965 registry entities; (3) unrack/repurpose
    the box. Container-stop one-liner now moot.'
  decommissioned_2026_08_15: 'Operator go ("Frigate 1 hardware full decomm"; homelab agent repurposing
    the box). URA side EXECUTED: config entry 01JV6G4E57HT3WH86WSQ4RJT11 deleted in-band (ha_remove_helpers_integrations)
    — all 965 F1 entities + 25 *_f1retired renames removed with the entry (live-verified: sample entity
    404s; on-disk registry file lagged, in-memory authoritative). Frigate 2 sole NVR, 964 entities untouched.
    No restart needed. RETIREMENT COMPLETE end-to-end: software swap -> zero-ghost soak -> integration
    disable -> physical off -> hardware decomm, with zero detection-threshold code written.'
- id: NM-REPAGE-IMG-1
  VIOLATION_RETRACTED_2026_08_23: 'THE VIOLATION RECORDED EARLIER TODAY IS WITHDRAWN — it was not supported
    by evidence, and I relayed it to the operator as "live-broken shipped work, silent for nine days".
    That was wrong. WHAT THE README ACTUALLY SAYS: README_v5.73.1 L3 reads "PASS on WhatsApp (organic
    2026-08-14); FAIL on iMessage" — but its EVIDENCE COLUMN contains NO observation of an iMessage failure.
    It restates the prospective criterion ("Next unacknowledged CRITICAL — 5-min re-pages must carry the
    original photo"). The Context note directly beneath says: "criterion stays open until one occurs."
    So L3 was OPEN on the iMessage side, not FAILED. The word FAIL was doing the job of "not yet observed",
    and a watch pass read it as a violation and moved the card shipped_organic -> review. THE OPPORTUNITY
    DID EXIST, so "no chance to observe" is NOT the explanation either. Since 2026-08-12: 249 CRITICAL
    notifications on the imessage channel, 221 of them UNACKNOWLEDGED — and an unacknowledged CRITICAL
    is precisely the 5-min re-page trigger. So iMessage re-pages have fired in volume. WHAT REMAINS GENUINELY
    UNKNOWN: whether those re-pages carried the attachment. `notification_log` records channel/severity/ack
    but NOT attachment presence, so this table cannot answer it either way. The in-suite wire-in IS mutation-anchored
    (transport-kwarg neuter -> 1 named test red, restored green), which is evidence the code path exists
    — not that the image lands at BlueBubbles. CORRECT STATUS: back to shipped_organic with L3-iMessage
    OPEN and UNMEASURED. Not violated, not confirmed. THE REUSABLE DEFECT IS THE README CONVENTION: a
    validation table wrote PASS/FAIL into a verdict cell with nothing in its evidence cell. That verdict
    then propagated into a card status change and into a report to the operator, unchallenged, because
    "FAIL" reads as an observation. Validation tables must carry OPEN / NOT-OBSERVED as first-class verdicts,
    and a PASS/FAIL with an empty evidence cell should be treated as malformed.'
  title: Re-attach stored snapshot on CRITICAL re-pages — text-only repeats are a correctness bug, not
    a design choice
  thread: notifications
  status: shipped_organic
  shipped_version: v5.73.1
  approval: approved
  origin:
    date: '2026-08-12'
    gist: 'operator: "Dont forget the missing images in follow on detections as designed. Intermittency
      on correctness is a bug." — promotes the LOW folded into PERIM-FP-1.'
  why: 'Unack-CRITICAL 5-min re-page loop resends text only; the original snapshot file persists at re-page
    time, so the omission is arbitrary. Operator ruling: if the alert deserves an image, every page of
    it does.'
  scope: '~5 LoC in NM re-page path: reuse the stored snapshot path from the original dispatch (both WhatsApp
    + iMessage attachment keys, BB v0.6). Tier 1. Anchor: wire-in rule applies (call-site neuter must
    red a test).'
  next: 'MEASURE, do not fix — nothing is known to be broken. Determine whether iMessage re-pages actually
    carry the attachment: inspect an outbound BlueBubbles payload at re-page time (log the transport kwargs
    at debug on the next unack CRITICAL, or check BB-side message records for an attachment). 221 unacknowledged
    CRITICALs on the imessage channel since 08-12 means opportunities are plentiful. Only if the attachment
    is genuinely absent does this become a fix.'
  updated: 2026-08-23 14:50
  sharp_problem:
  - '2026-08-23 VIOLATED: README_v5.73.1 L3 = PASS on WhatsApp (organic 2026-08-14) but FAIL on iMessage.
    The iMessage re-page attachment path did not land. Fix owed before card can close.'
  organic_evidence:
  - '2026-08-23 watch-pass: WhatsApp re-page attachment confirmed organic 2026-08-14 (PASS); iMessage
    re-page FAIL per README_v5.73.1 validation table.'
- id: NM-RECOVERY-AGEBOUND-1
  title: Boot recovery resurrects unacked CRITICALs of ANY age — 326 historical twin-eaten rows are a
    resurrection minefield
  thread: notifications
  status: done
  shipped_version: v5.75.2
  approval: unreviewed
  origin:
    date: '2026-08-14'
    gist: 'Found while closing IMSG-IMAGE-FAIL-1: get_active_critical has no freshness bound; the twin-eating
      ack bug left 326 unacked REAL criticals over months. Todays 4 sibling rows acked in-band via the
      (fixed) service; the rest are inert only until a restart walks backward through them.'
  why: A re-page of a weeks-old alert is noise at best, alarm-fatigue at worst. The DB rows themselves
    should stay (analytics).
  fix_sketch: 'Age-bound in get_active_critical (or the recovery caller): ignore unacked CRITICALs older
    than NM_RECOVERY_MAX_AGE_H (rung 1, default ~24h, 0=unbounded). Tier 1 + one twin-scenario test. Makes
    bulk historical ack unnecessary.'
  next: DONE 2026-08-23 — L2 PASS recorded in README_v5.75.2 validation table.
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: README_v5.75.2 L2=PASS — zero CRITICAL dispatches post-boot; 326-row historical
    resurection structurally closed. H1 confirmed.'
- id: SAFEWORD-WINDOW-1
  title: Safe-word ack window — one "duke" covers perimeter alerts for a bounded period (operator-proposed)
  thread: notifications
  status: shipped_organic
  shipped_version: v5.75.2
  approval: operator_proposed
  origin:
    date: '2026-08-14'
    gist: 'operator: "safe word covers all alerts within 1-3 hours so no need for safe words for a while
      no matter the notification? The underlying goal is still to tune the classification of events and
      make sure they are good."'
  why: 'Operator ergonomics during the FP-tuning era: busy afternoons / alert clusters currently need
    per-alert acks.'
  institutional_reuse: 'The silence primitive EXISTS: _silence_until (notification_manager.py:346, gate
    :1351-1352) — the reply-3 30-min silence. Proposal = parametrize duration + scope. NOT a new mechanism.'
  marginal_shape: 'Simplest honest version: "duke" keeps acking the current alert; "duke 2h" (parsed duration,
    cap 3h) sets _silence_until for PERIMETER-CLASS hazards only. Life-safety (smoke/CO/water/intrusion-interior)
    NEVER blanketed — a real intruder alert must not die inside a pool-afternoon mute. NM note confirms
    window start/end; auto-expiry; window state surfaced as attr. Knob: SAFEWORD_SILENCE_MAX_H rung 1
    (cap), duration operator-chosen per use.'
  safety_note: Blanket-mute is a stopgap while classification precision improves (the operator-stated
    underlying goal); scope-limiting to perimeter class keeps the failure mode bounded.
  next: Operator confirms the scoped shape (perimeter-only, duke Nh syntax, 3h cap) -> Tier 2 (NM routing
    = regression-prone).
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: README_v5.75.2 L4=ORGANIC (open) — first real "duke Nh" reply not yet observed.
    Awaiting real perimeter CRITICAL + operator safeword reply. H1 PENDING.'
- id: IMSG-IMAGE-FAIL-1
  title: iMessage security images NOT arriving (organic FAIL of NM-BB-IMAGE-1 L5) + [audit] sentinel leaking
    into operator-visible message bodies
  thread: notifications
  status: done
  shipped_version: v5.75.1
  approval: unreviewed
  origin:
    date: '2026-08-14'
    gist: 'Operator screenshots: WhatsApp carries photos (incl. re-pages — v5.73.1 L3 CONFIRMED organically
      on WA); iMessage shows text-only bubbles reading "Perimeter Alert — Person Detected [audit]" — no
      image AND the [audit] ledger sentinel is visible in the sent body.'
  why: 'Two distinct defects: (1) BB v0.6 attachment keys (v5.73.0 NM-BB-IMAGE-1) not delivering images
    — key contract vs BB server config vs is_allowed_path (probe BB server logs + a manual bluebubbles.send_message
    with attachment to isolate); (2) the [audit] marker was designed as a DB-reader filter tag, not user-visible
    text — its presence in the iMessage body suggests the audit-row variant is being DISPATCHED to the
    channel instead of (or in addition to) the clean-body original. Also explains double-bubbles (both
    sides of the chat = self-chat echo + audit twin?).'
  next: 'Tier-1 investigation: trace _send_imessage payload for a security alert (body composition + attachment
    fields); one manual BB send with a known-good local path; check whether audit-tagged duplicates are
    entering the send path. Fix both in one pass.'
  updated: 2026-08-17 23:55
  resolution_2026_08_17: 'Operator confirmed 2026-08-17: security images ARE arriving. The prune-audit
    "active fail" read was wrong — feature works. Closed.'
- id: ROOM-NAME-UNIQUE-1
  title: Room rename has no name-uniqueness guard — collision collapses name-keyed maps (two rooms fold
    into one occupancy bucket)
  thread: presence
  status: pre_planning
  approval: unreviewed
  origin:
    date: '2026-08-14'
    gist: 'ROOM-NAME-DESYNC-1 Review C adversarial find (D-MED-1): rename Room A to an existing Room B
      name — zero validation; _room_to_zone dict + ZonePresenceTracker.room_names + substrate bucket keys
      all name-keyed -> silent overwrite/merge. Latent + pre-existing; write-through makes it surface
      at save-time (was boot-time).'
  why: Join-key uniqueness is an unenforced invariant every name-keyed tier map depends on.
  fix_sketch: _check_room_name_unique in async_step_basic_setup -> async_show_form error on collision
    (~15 LoC, Tier 1-2). Live-validation D-block for the rename cycle includes a do-not-rename-to-existing
    sanity note meanwhile.
  next: Small cycle after v5.75.0; consider folding into the next config-flow-touching batch.
- id: PWA-CONTROL-LIST-1
  title: Per-room controllable-entities attr so the PWA stops slugify-guessing actuators (AV-Closet-Shelly
    problem)
  thread: dashboarding
  status: done
  approval: operator-approved
  tags:
  - audit-first
  origin:
    date: '2026-07-13'
    gist: Plan inventory audit 2026-08-14 found PLANNING_g1_room_control_list_attrs.md unbuilt + uncarded
      — PWA M2 gap G1.
  note: 'Re-scoped + BUILT 2026-08-16 PWA-side (no URA changes needed): useRoomDevices(slug) in ura-dashboard-pwa
    prefers sensor.<slug>_devices attrs (real configured entity_ids) with observable console.debug fallback
    to the existing G1 control_* attrs (night_lights/climate stay G1-only). Rooms + RoomDetail migrated;
    215/215 tests + build green; branch feature/devices-sensor-ground-truth (ca4f6d8). Deploy decision
    stays with operator.'
  why: PWA guesses a room's actuators by slugifying the room name; wrong for rooms whose real device is
    a differently-named Shelly relay (AV Closet). A per-room control_list attribute (additive, no behavior
    change, no new entities) gives the PWA ground truth. grep confirms zero control_list/controllable
    attrs exist today.
  parsimony:
    problem: PWA cannot reliably map a room to its controllable entities
    verdict: BUILD
  next: 'Operator: deploy the PWA branch when ready (npm run deploy after merge).'
  refs:
  - docs/planning/PLANNING_g1_room_control_list_attrs.md
  duplicate_verdict_2026_08_16: 'Operator: "we have a device list sensor — does that duplicate?" -> YES,
    substantially. Per-room DevicesSensor (sensor.<room>_devices, sensor.py:2035) already exposes categorized
    attrs: lights, fans, humidity_fans, covers, auto/manual devices, media player, power/energy sensors
    — the exact controllable-entities ground truth. RE-SCOPED: no new URA attribute. Work = (a) DONE 2026-08-16:
    enabled the 13 registry-disabled per-room devices sensors (all 39 now enabled; entities appear after
    each room entry next reloads or at next restart); (b) REMAINING: PWA change to consume sensor.<room>_devices
    attrs instead of slugify-guessing (ura-dashboard-pwa repo — align with PWA purpose: operator phone-first
    control surface, M2 gap G1). Old plan doc PLANNING_g1_room_control_list_attrs.md superseded on the
    attr-addition point.'
- id: ZONE-CAM-PERSON-GUARD-1
  title: Durable device_class guard so a Frigate MOTION sensor cannot be trusted as camera person-confirmation
    in zone occupancy-confidence
  thread: presence
  status: done
  approval: unreviewed
  origin:
    date: '2026-07-13'
    gist: 'Plan inventory audit 2026-08-14: PLANNING_zone_camera_person_only_guard.md unbuilt + uncarded;
      the 2026-06-08 live finding was only fixed by config removal, not code.'
  why: CONF_ZONE_CAMERAS entries are trusted as person-confirmation by the presence Source-3 occupancy-confidence
    scorer (-> hvac.py stale-sensor guard). A motion-only Frigate sensor mis-filed there is trusted as
    a person confirm. No device_class filter exists near Source 3. Presence<->HVAC ripple.
  parsimony:
    problem: a non-person camera sensor in CONF_ZONE_CAMERAS is trusted as person-confirmation
    verdict: BUILD
  next: ura-planner; Tier 2 (touches occupancy-confidence scorer).
  refs:
  - docs/planning/PLANNING_zone_camera_person_only_guard.md
  refinement:
  - 'operator 2026-08-15: "I believe we already have a camera motion guard in room code. And we don''t
    use motion for almost anything even if configured" -> context-wide audit (rooms+zones+house) dispatched;
    card dies if existing guard covers it or if the fix is config-only'
  closed_2026_08_15: 'CARD DEAD AS WRITTEN per context-wide audit (AUDIT_zone_cam_guard_necessity.md,
    1e8b27e96). Operator was right: the person-only suffix guard EXISTS (camera_census.py:362-386 + camera_resolver.py:215-236)
    and covers room override + zone tracker; only Source-3 confidence (1-of-4 vote, can only DELAY a force-away)
    reads raw config. The proposed device_class guard would catch NOTHING: the 3 live offenders are Frigate
    *_all_occupancy sensors carrying device_class=occupancy (label scope, not device_class, is the discriminator).
    REAL FIX = 3-entity config swap to person-only siblings, staged as flush-watcher rider zone_cam_swap_after_flush.py
    on today''s deploy restart. Optional future LOW (revive only if config drift recurs): narrow the config-flow
    selector device_class + person-suffix filter at selection time (config_flow.py:8427).'
  rider_update_2026_08_15: 'Rider FAILED at v5.77.0 restart (script flat-scan hit nested zones dicts).
    Rewritten with recursive walk, dry-run verified (exactly the 2 zone edits), re-staged for NEXT restart.
    Rider bug class noted: .storage editors must handle nested structures.'
  rider_applied_2026_08_15: 'APPLIED + VERIFIED at operator-requested restart (~17:25 CDT): flush caught,
    both zone edits landed (Back Hallway + Upstairs -> person-only sensors), post-boot residue 0, swaps
    present TRUE. Card fully closed — nothing outstanding.'
- id: KP-ANNOTATION-1
  title: Known-person annotation + stranger-alert leg — exterior alerts annotate identity ("likely Oji"),
    unknown-face escalates (doorbell-automation successor)
  thread: perimeter
  status: parked
  approval: explicit
  approved_by: 'operator 2026-08-14: three bindings (LOCAL-ONLY identity — Frigate-2 + Protect face, llmvision
    excluded; D3 stranger leg FOLD IN NOW; in-first-message only) + "Agree on exterior member recognition
    conclusion"'
  origin:
    date: '2026-08-14'
    gist: Successor card for the work absorbed from declined KP-ESCALATE-1 + the annotate-not-suppress
      direction. This card was MISSING for a day (capture miss, created 2026-08-15).
  why: 'Exterior alerts have zero member recognition; annotation kills operator triage cost without suppressing;
    stranger leg replaces the retiring doorbell automation. INV-KP: identity never delays/blocks/mutates
    the base alert; absent identity = byte-identical.'
  constraints:
  - no household reference photos leave LAN
  - stranger hazard respects duke-window (security-tier, NOT life-safety bypass) — operator-visible
  - fail-CLOSED false-stranger guard
  refinement:
  - 'audit rec "disable face on outdoor/PTZ" -> operator: NO, exterior recognition IS the goal -> rec
    C revised: exterior people-cams get the detect-stream upgrade first-class; disable only hopeless overhead/utility
    geometries'
  - 'plan review CRIT-1: stranger hazard type would have LEAKED through an open safeword window -> registered
    in NM_SECURITY_HAZARDS + memory-ineligible + explicit severity map'
  - 'plan review HIGH-1: after-enqueue ordering was only probabilistic -> structural after-await + own
    in-flight guard (hanging identity probe can never suppress next-camera dispatch)'
  next: 'Tier 2-DB. Plan rev-3 committed (a28e4568f) — reviewed twice (4e468d37f). Next: D0 read-only
    probe (identity producers per camera, latency histogram, confidence distribution, enrollment coverage,
    doorbell cadence) -> gates build.'
  refs:
  - docs/planning/PLANNING_known_person_annotation.md
  - docs/reviews/code-review/known_person_annotation_plan_review.md
  d0_verdict_2026_08_15: 'PARK v1 ENTIRELY (probe 2bcffbe0a, AUDIT_kp_annotation_d0_probe.md) — producer
    coverage insufficient, plan''s own park branch. 0.0% of 1,532 perimeter person events had identity
    at t=0 (ship gate >=50%); 6 of 9 camera face pipelines emit only boot churn; NO confidence attribute
    exists anywhere (floor underivable, guard inert); enrollment 75% (Ziri 0, Oji 1); NEITHER doorbell
    is in perimeter_cameras (stranger leg has no trigger). Only working producers: hot_tub, pool_equipment,
    g5_bullet, madrone_g6_entry.'
  revival_preconditions: 'OPERATOR/HOMELAB actions, then RE-RUN the probe: (1) fix Frigate-2 face pipeline
    on the 6 dead cameras; (2) enroll Ziri + verify Oji enrollment (1 sighting as first-name token); (3)
    expose a Frigate confidence score OR drop the floor design; (4) add doorbell(s) to perimeter_cameras
    config. Plan is review-hardened and ready the moment the probe passes.'
  crosscheck_2026_08_15: 'Operator tagged faces in BOTH engines this morning. Protect registry (via Protect
    API/MCP — HA exposes NO identity attrs on this install, so URA consumption = Protect API, plan note):
    Oji 21 dets avg-conf 82, Ziri 46 dets (Frigate''s blind spot!), guests Ade+Shola newly named, NO Ezinne/Jaya
    groups — enrollment is COMPLEMENTARY across engines (Frigate: Ezinne 11/Jaya 14, Oji 1/Ziri 0). ISOLATION
    RESULT: all 28 Protect face events/24h are from the Family Room AI Theta alone (foyer fisheye: zero
    — face not running on that camera class); Frigate''s family-room face pipeline is dead on the SAME
    physical camera Protect recognizes on -> camera/feed fine, failures are Frigate-pipeline-side. Revival
    preconditions updated: (1) Frigate face pipeline repair now evidence-anchored to family room; (2)
    cross-engine enrollment completion (Ezinne/Jaya in Protect, Oji/Ziri in Frigate); (3) plan rev needed:
    Protect identity source = API polling/websocket, not HA entities.'
  webhook_probe_2026_08_15: 'Operator approved the probe. HA listener LIVE: automation.ura_kp_face_webhook_probe
    (webhook id ura_kp_face_probe, local-only, payload -> event ura_kp_face_probe_received + system_log).
    Protect-side rule could NOT be created via API (v2 automations endpoint 400s the canonical shape;
    zero v2 rules exist to copy — stopped rather than shape-guess against the security controller). OPERATOR
    STEP (~2min): Protect UI -> Alarm Manager -> new alarm -> trigger Face (known) -> action Webhook ->
    http://192.168.13.13:8123/api/webhook/ura_kp_face_probe POST. Next family-room recognition fires it;
    I read the payload (name? confidence?) -> decides URA wiring. Also noted: tonight''s face_degraded_*
    low-conf events = likely untagged guests; Ezinne/Jaya still unenrolled in Protect.'
  probe_result_2026_08_15: 'PROBE FIRED (operator created "Madrone Face Alarm": Face ID known+unknown,
    Family Room + G6 Entry — the ONLY two Face-ID-capable Protect cams, fisheye silence explained definitively).
    Test payload captured: trigger key (face_known/face_unknown distinguishable!), device MAC, eventId
    + event links, timestamp. NOT in test payload: person name, confidence, thumbnail. DESIGN CONSEQUENCE:
    eventId is the join key — webhook push + one protect event fetch = name+confidence+thumbnail (push
    latency, full data, no polling). AWAITING: first ORGANIC recognition payload to confirm real alarms
    carry no more than tests before locking the fetch-enrichment shape.'
- id: ZIRI3-UNCONFIG-1
  title: RECOVER (not unconfigure) Ziri 3 device from Ziri Bedroom entry (presence + moving_target + VEML7700
    lux) — rides next deploy restart
  thread: presence
  status: waiting_operator
  approval: explicit
  approved_by: operator 2026-08-15 ("Check if in config and if not unconfigure it")
  origin:
    date: '2026-08-15'
    gist: ziri_3_presence stuck-unavailable finding in optimizer score-55 round; device physically dead
      (established 2026-08-05).
  why: 'Dead device dings sensor_health every cycle. Room keeps mmwave_zigbee_ziribedroom_presence for
    presence. Three refs removed: presence_sensors[ziri_3_presence], motion_sensors[ziri_3_moving_target],
    illuminance_sensor=ziri_3_veml7700 (set None — room loses lux until a replacement is configured; operator-visible).'
  constraints:
  - flush-watcher pattern (stop->boot gap), NOT a live .storage edit
  - BLOCKS-CLOSE-OF next deploy — same batch discipline as garage-egress
  refs:
  - scratchpad ziri3_unconfig_after_flush.py
  reversal_2026_08_15: 'Operator: device is still physically in the room — DO NOT unconfigure. Staged
    flush-watcher rider DELETED. History: zero real readings in entire recorder retention (8+ days); node
    does not resolve on network (ESP fully off-WiFi, not flapping). Config refs stay.'
  next: 'OPERATOR: power-cycle the Ziri 3 node (unplug/replug). Then I verify: entities leave unavailable,
    presence/lux flow, optimizer sensor_health finding clears next cycle.'
- id: OPT-META-BOOT-TRANSIENT-1
  title: Optimizer meta-monitor false "cannot see problems" alert — findings_recent reads RAM cache emptied
    by restart while open-count reads durable state
  thread: optimizer
  status: done
  shipped_version: v5.77.0
  approval: unreviewed
  origin:
    date: '2026-08-15'
    gist: 'Operator forwarded the meta alert 20min after the v5.76.0 restart; diagnosis: boot transient,
      not real blindness (3,725 findings/24h in DB, newest minutes old).'
  why: optimization_llm.py:666 builds findings_recent from coordinator._last_findings (in-memory, cleared
    at restart); the meta pass compares it against the durable open-findings count and the LLM narrates
    the mismatch as system blindness -> false HIGH meta finding every restart that coincides with open
    findings.
  fix_options: 'Either (a) corpus falls back to get_recent_optimization_findings DB read when the RAM
    cache is empty, or (b) meta emission suppressed inside a post-boot grace window (suppression-needs-discharge:
    re-fires next cycle after grace). Prefer (a) — honest data beats suppression.'
  next: DONE 2026-08-23 — L3 PASS recorded in README_v5.77.0 validation table.
  refs:
  - custom_components/universal_room_automation/domain_coordinators/optimization_llm.py
  live_validation_2026_08_15: 'v5.77.0 LIVE: L3 PASS — first post-boot meta cycle = cycle_ok only; false-blindness
    HIGH structurally closed. Card can move to done on one more clean restart.'
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: README_v5.77.0 L3=PASS — first meta finding post-boot was cycle_ok LOW only;
    false "cannot see problems" HIGH eliminated. H1 confirmed.'
- id: CENSUS-SUFFIX-FIX-1
  title: 'Census regression ROOT-CAUSED: strict-suffix matchers miss all _2 F2 sensors since F1 death
    (08-13) -> count sensors unmapped -> census pinned at identified count. Fix: disambiguation-tolerant
    matching.'
  thread: presence
  status: done
  shipped_version: v5.77.0
  approval: implied
  origin:
    date: '2026-08-15'
    gist: 'operator: "census used to be more or less accurate — what changed?" Recorder: daily max 6-7
      through 08-12, 4 from 08-13 (F1 entity death). AUDIT_census_accuracy_regression.md (a54379830),
      H1 confirmed, H2/H3/H4 refuted.'
  why: 'All F2 count sensors are _2-suffixed; _PERSON_COUNT_SUFFIX endswith matching (camera_resolver.py:272/1288
    + camera_census.py:400/793) matches none -> binary fallback max-1-per-camera -> unrecognized=max(0,~4-4)=0
    -> total=identified forever. 17 cameras with _2-only person binaries contribute NOTHING. Live proof:
    playroom=4/master_hallway=4/staircase=3 simultaneous last night, census high-water exactly 4.'
  parked_alts:
  - 'Audit''s rename-38-entities-to-canonical option REJECTED as primary: every rename breaks every reference
    (URA config rows, staged riders, dashboards); code tolerance = one reviewed change, zero reference
    breakage'
  next: 'Builder in flight (strip-before-match at all strict sites + ambiguity guard + drills); 2 reviews;
    batches into the pending reload/opt-meta deploy. Post-deploy Live: census exceeds 4 during next multi-person
    traversal.'
  refs:
  - docs/planning/AUDIT_census_accuracy_regression.md
  operator_ruling_ash41b_2026_08_15: 'ASH41B (Study A): stays OUT of camera_person_entities (census) by
    operator ruling — camera is physically blocked by a screen unless operator is away, so zero-detection
    history is EXPECTED (not a Frigate pipeline fault; struck from the F2 punch list). Stays configured
    at room tier (Study A room_cameras, verified enabled). Do not re-raise.'
  live_validation_2026_08_15: 'v5.77.0 LIVE: L1 PASS; L2 organic — census at 4 post-boot pending first
    camera traversal; PASS = first recorder reading >4 (guests in house tonight = likely within hours).'
  l2_watch_redefined_2026_08_15: 'Guests departed before a >4 traversal registered (census max stayed
    4 post-boot). L2 proof redefined: INTERIM = any unidentified contribution (census reads identified+1
    on any visitor/delivery in census-camera view — was structurally impossible pre-fix); FULL = census
    tracks headcount at the NEXT gathering. Mapping itself is drill-anchored in-suite; only live magnitude
    remains organic.'
  l2_INTERIM_PASS_2026_08_16: 'ORGANIC PROOF (operator home, ~evening): census reads 6 = identified 4
    + unidentified 2, camera_unrecognized 2, source_agreement close, confidence medium, wifi_guest_floor
    6 (independent corroboration). Pre-fix this was STRUCTURALLY IMPOSSIBLE — census was pinned at identified
    count (max 4) because the _2 count sensors matched nothing. Interim criterion (any unidentified contribution)
    PASSED. Full criterion (headcount tracking at a gathering) still open but the mechanism is proven
    live.'
- id: D3-BEHAVIOURAL-COVERAGE-1
  title: D3 guest-room registry resolution has ZERO behavioural coverage — all six tests are source-shape
  thread: presence
  status: done
  approval: unreviewed
  tags:
  - test-authority
  - hollow-anchors
  origin:
    date: '2026-08-16'
    gist: Review C (guest-census cycle, d9a74e86e) observation while auditing test authority. Logged as
      follow-up; C was explicitly told NOT to expand the fix-up scope to cover it.
  problem_solution:
  - 'P1 all six D3 tests assert on SOURCE SHAPE (the registry call appears in the file), not on BEHAVIOUR
    (a guest room actually resolves to its occupancy entity). Review C proved this class is evadable:
    the sibling anchor test_unresolvable_room_warns PASSED with the production WARNING deleted, because
    _LOGGER.warning survived in a COMMENT (hollow-anchor variant 7). The same evasion applies to the remaining
    D3 anchors. S1 add behavioural tests that drive _discover_guest_rooms against a fake entity registry
    and assert the RESOLVED entity_id, including the unresolvable path.'
  - 'P2 D3 is now load-bearing: under cycle-1 D2, guest entry depends ENTIRELY on guest-room occupancy,
    so a silent D3 resolution failure means guest mode never arms at all. Source-shape tests cannot catch
    a registry lookup that returns the wrong entity or None. S2 treat D3 coverage as guest-mode safety
    coverage, not test hygiene.'
  why: The cycle-1 fix-up repairs the ONE anchor Review C proved hollow (C-MED-1, via caplog) but deliberately
    does not build out the rest. Without this card that gap disappears, and it sits under the newly load-bearing
    guest entry path.
  constraints:
  - Every anchor added must survive the variant-7 attack (comment-out of the production call) — apply
    the attack to the new tests before declaring them green.
  - 'Small and self-contained: tests only, no production change expected. If a production change turns
    out to be needed, that is a finding worth surfacing, not absorbing.'
  next: 'DONE (merged 7a2e73aa2, tests-only): 5 behavioural tests driving production _discover_guest_rooms
    + _handle_guest_room_occupancy_change vs a fake registry; 4 mutation drills (plain+variant-7) red-then-green;
    zero production change. D3 registry resolution now behaviourally covered (was source-shape only).'
  refs:
  - docs/reviews/code-review/guest_census_review_C.md
  - custom_components/universal_room_automation/domain_coordinators/presence.py
  updated: 2026-08-18 02:30
- id: CENSUS-ACCURACY-1
  title: 'Interior census accuracy: separate census decay from guest hysteresis + fix the _2-suffix fresh-face
    resolution (exterior dashboard wiring is a minor bonus)'
  thread: presence
  status: shipped_organic
  shipped_version: v5.80.0
  approval: implied
  approved_by: 'operator 2026-08-16 ("Census and guest are diff states... Census decay is also not the
    same as Guest decay. Separate concerns.") | MERGE APPROVED operator 2026-08-16: absorbed CENSUS-DEDUP-REPAIR-1
    into this card. Rationale: both are census-side ("make the number right") and share consumers, tests
    and reviewers; splitting cost a full extra review round for little isolation benefit. Cycle chain
    is now 1 CENSUS-GHOST-DEDUP-1 (guest correctness, in review) -> 2 CENSUS-ACCURACY-1 (this) -> 3 EXTERIOR-GUEST-EGRESS-1.'
  origin:
    date: '2026-08-16'
    gist: Operator ruling after the guest-phantom incident; full context in RESEARCH_census_vs_guest_separation.md
      (aa3e39aa8).
  problem_solution:
  - 'P5 one timer, two opposite needs — census wants freshness, guest wants hysteresis; shared hold+decay
    turns a 15s phantom into 480s of evidence and clears the 300s guest gate (the mechanism behind ~50
    spurious guest entries since 07-13). S5 give each its own timing: census adopts the instant-drop branch
    ALREADY RUNNING for the exterior zone; guest keeps its own 300s dwell + 300s exit damping.'
  - P6 decay never fires for systematic errors — the peak self-refreshes when fresh == peak, so a permanently-wrong
    value renews forever while only transients decay (wrong sign on both axes). S6 remove the self-renewing
    slope; make heldness visible.
  - P7 consumers cannot distinguish a fresh count from a 20-minute-old latched one. S7 publish peak_held
    / peak_age / count_as_of — all already computed internally and discarded.
  - 'P8 the exterior census is naive (per-camera-bit sum, one walker past 3 cams reads 3) while the track-deduped
    count already exists live. ADJUDICATED 2026-08-17 (AUDIT_exterior_census_supersession.md, eb2caa3c8):
    the swap I originally scoped (RETIRE the naive sum, source persons_on_property_exterior from census_counts())
    is HARMFUL — KEEP BOTH. Reasons: (a) the deduped answer is ALREADY live on sensor.exterior_person_tracks_active
    / exterior_unidentified_persons with correct unavailable lifecycle — nothing to build to obtain it;
    (b) the linker collapses two people on adjacent cameras within TRACK_LINK_WINDOW_S=180 into ONE track,
    an UNDERCOUNT that lands on the operator security priority (display blind spot only — per-camera NM
    alerts still fire off rising edges, not the count); (c) the naive sum is a conservative security FLOOR
    worth keeping. The exterior census is FULLY STRANDED (zero trust consumers, grep-verified — only 2
    display sensors + PWA card + a dead dispatch key), so no actuation is endangered either way. S8 REVISED:
    do NOT overwrite the naive sensor. If anything, surface the linker id/unid split as its OWN first-class
    number (largely already done) and leave persons_on_property_exterior as the conservative floor. This
    makes the exterior part of cycle 2 nearly a NO-OP — the main census work is the decay/self-refresh
    separation + the _2-suffix fix.'
  - P9 outdoor presence may pollute the interior headcount (outdoor-zone filter designed, unbuilt). S9
    apply the filter using the existing CONF_ZONE_IS_OUTDOOR flag.
  - 'ABSORBED FROM CENSUS-DEDUP-REPAIR-1 (merged 2026-08-16): P10 the per-area BLE-cancel subtraction
    reports ble_cancelled_count 0 every tick while residents ARE being double-counted — cause UNKNOWN.
    Ruled out: missing areas on the counting sensors (16 of 17 Frigate person-count/occupancy _2 sensors
    have effective areas; only the screened ASH41B lacks one). S10 PROBE FIRST using the diagnostics shipping
    in cycle 1 (pre-cancel scalar, _last_ble_by_area, _last_ble_cancel_enabled) which for the first time
    can distinguish ran-and-cancelled-zero from never-ran. No plan until the probe answers it.'
  - 'ABSORBED FROM CENSUS-DEDUP-REPAIR-1 (merged 2026-08-16): P11 the fresh-face -1 per-camera defense
    is inert because face recognition produces ~0 identities (face_recognized_persons empty with 4 known
    residents home). S11 gated on the EXTERNAL Frigate detect-stream + enrollment chain (homelab-side)
    — not fixable in URA code. Track the dependency; re-measure after the face pipeline produces.'
  - 'P12 (folded in per operator 2026-08-17) the two exterior counts serve DIFFERENT consumers and neither
    is wired to the dashboard correctly. S12 small deliverable: dashboard shows the DEDUPED count (exterior_person_tracks_active)
    as the headline "people outside" with the NAIVE sum (persons_on_property_exterior) as a degraded-mode
    FALLBACK (guard G1: never show 0 when the linker is inactive/booting) and a DIVERGENCE indicator (naive
    >> deduped = lingering/circling; deduped=0 & naive>0 = linker-not-ready health signal). Security/alerting
    stays on the never-miss floor + per-camera NM events; guest ingress/egress consumes identity, not
    a count. This is dashboard wiring only — does NOT touch the census producer and does NOT overwrite
    either sensor (per the KEEP-BOTH ruling).'
  why: 'Operator separation-of-concerns ruling: census = measurement (accuracy + freshness); guest = policy
    state (explicit entry/exit + hysteresis). Today guest is a function of a decaying measurement, which
    is the root architectural error. NOTE: the v4.7.18 plan scoped this separation (CensusHoldDecayService
    + presence peek()) and it was never built — every cycle since has patched measurement accuracy instead.'
  constraints:
  - 'MOSTLY COMPOSITION, NOT CONSTRUCTION — already built and just needs composing: the instant-drop branch
    (running for exterior), peak_held/peak_age, ExteriorTrackLinker.census_counts(), CONF_ZONE_IS_OUTDOOR,
    guest dwell/exit damping, the raw subtractive derivation.'
  - 'Do NOT conflate with the guest-census correctness cycle (CENSUS-GHOST-DEDUP-1): that one CAPS and
    RE-ROUTES; it does not repair or re-time anything. Decay and dedup repair are THIS card.'
  - 'MERGED CARD — the dedup repair (P10/P11) is probe-gated: AUDIT_census_accuracy_probe.md must land
    first and its Q1 (why per-area BLE-cancel returns zero) must identify a specific broken link. If the
    probe cannot establish the mechanism, the dedup repair does NOT get built on speculation — re-split
    it rather than guess.'
  next: 'RESCOPED by operator 2026-08-17 to DECAY + SUFFIX FIX (dedup repair dropped — probe measured
    it buys ~0). Plan: (1) decay/self-refresh separation, targeting the measured 74.5% of elevated time
    that had camera_unrecognized==0; (2) registry-resolution fix for the four bypassed entity-ID construction
    sites. Camera-area COVERAGE gap is a config task for the operator, not a build. Tier 2-DB plan review
    before build.'
  refs:
  - docs/planning/RESEARCH_census_vs_guest_separation.md
  - docs/planning/PLANNING_v4.7.18_census_service_shared_refactor.md
  - 'CARD: EXTERIOR-GUEST-EGRESS-1 (exterior->guest, split out of P8)'
  - 'CARD: EXTERIOR-DWELL-LOITER-1 (circling dwell gap, security)'
  - docs/planning/AUDIT_census_accuracy_probe.md (probe gate)
  - docs/planning/AUDIT_exterior_census_supersession.md (eb2caa3c8) — KEEP BOTH ruling
  operator_exterior_direction_2026_08_16: 'Operator ruling on the exterior work, THREE distinctions: (1)
    EXTERIOR -> HEADCOUNT is easy, do it (straight composition; sensor.universal_room_automation_persons_on_property_exterior
    is live, and a dashboard for it already exists at docs/dashboards/ura_v8_exterior_cycle2_cards.md).
    (2) EXTERIOR -> GUEST must be CAREFUL and causal, not thresholded: requires observing an UNKNOWN person
    transition OUTSIDE->INSIDE VIA AN EGRESS, then correlating with other guest signals. Never "someone
    is outside therefore guest". (3) Exterior count accuracy is MORE IMMEDIATELY COGENT TO SECURITY —
    check the conjunction with exterior camera person-tracking, circling (CIRCLING-LABEL-1 shipped v5.76.0)
    and switch.ura_security_coordinator_exterior_path_tracking. That count must be accurate for its OWN
    sake, not only as a guest input. NOTE: exterior census currently reports source_agreement=single_source
    while interior reports both_agree — the exterior leg has its own accuracy gap to characterise.'
  regression_context_2026_08_16: 'Operator: "I believe we regressed census with our prior work" — CONFIRMED
    with recorder data. Daily census max: Aug 9-12 = 6-7 (4 residents, chronic over-count of 2-3); Aug
    13-14 = 4 (LOOKED perfect, but only because the _2-suffix break PINNED the count at identified); Aug
    16 = 10 for 5 people (worst on record). We regressed from wrong-and-masked to wronger-and-visible:
    the suffix repair restored the additive path to full strength and the F2 detect tuning fed it more
    detections. The additive double-count is CHRONIC and predates this week. Lesson recorded: the broken
    state read as healthy (a pinned 4 in a 4-person house looks like a perfect census).'
  measured_incident_2026_08_16_guest_7h: 'MEASURED end-to-end retrace of a live false-guest episode (operator
    flagged "5 seen, guest mode, 4 known"). Recorder: house `guest` 13:38:33 -> `home_evening` 20:40:59
    = 7h02m of false guest with 4 known residents and zero guests. Entry was CENSUS-driven (Path A): census
    hit 6 at 13:38:03, guest 30s later. Census sawtoothed 4..10 all afternoon, never resting at 4 until
    20:35:59; guest released 5 min after census finally reached 4. Confirmed constants: CENSUS_PEAK_SUSTAIN_SECONDS=15
    (latch), DEFAULT_CENSUS_HOLD_INTERIOR_MINUTES=3, CENSUS_DECAY_STEP_SECONDS=300 (-1/5min), DEFAULT_GUEST_PERSISTENCE_SECONDS=300.
    THE ASYMMETRY IS THE BUG: 15s of phantom latches a peak costing 3min hold + 5min PER EXCESS PERSON
    to clear — the observed spike to 10 (identified=4) implies ~33 min of elevated count from a 15-second
    artifact, and re-spikes arrived faster than the decay drained (peak self-refreshes when fresh == peak,
    so a recurring artifact never decays at all). Decay measured exactly: 10->9->8->7->6->5 at 18:47/18:52/18:57/19:02/19:07.
    CYCLE-1 IMPACT (verified, not assumed): D2 KILLS this entry path (guest entry becomes guest-room-only,
    Path A removed) and D2b decouples the exit from the count — this exact episode cannot recur. D1''s
    clamp does NOT get the count to 4: review A''s per-value table (identified=4, held=6) gives pre_cancel
    0/unset/4 -> 4/0; 5 -> 5/1; 6 -> 6/2; 10 -> 10/6. When the camera body total is itself inflated the
    clamp is a NO-OP. So the NUMBER stays wrong until cycle-3 dedup repair. Cycle 2 must fix the latch/decay
    asymmetry; cycle 3 must fix the camera body total.'
  exterior_intersection_findings_2026_08_16: 'Context-wide read-only investigation answering "how does
    the circling/exterior-track work intersect the exterior census". ANSWER: IT DOES NOT — zero shared
    code, two independent readers of the same cameras. (1) Exterior census `_calculate_property_census`
    camera_census.py:1501-1580 sums one `person_binary_sensor` bit PER CAMERA with NO dedup of any kind
    — one walker past 3 perimeter cams reads 3. (2) `ExteriorTrackLinker` threads sightings across cameras
    via the operator-ratified adjacency graph (TRACK_LINK_WINDOW_S=180) — same walker reads 1. THE SECURITY
    WORK IS ALREADY THE BETTER COUNTER, and it exposes the answer on a plate: `census_counts()` at exterior_track_linker.py:766-777
    returns exterior_person_tracks_active + exterior_unidentified_persons and NOTHING consumes it; the
    census''s own `property_count` also has zero readers outside camera_census.py. (3) `single_source`
    MYSTERY CLOSED: it is HARD-CODED on every branch (camera_census.py:1544/1548/1551/1555); the exterior
    path never calls `_cross_validate_platforms()` (house-only, :1404-1407). NOT a platform outage — multi-witness
    legs exist on the same cameras (perimeter_alert.py:410-500 enumerates 7 engine legs); the census simply
    never asks. No platform repair is needed or possible here. (4) Interior/exterior camera classification
    is DUPLICATED across 3 unreconciled mechanisms: config-list membership (CONF_CAMERA_PERSON_ENTITIES
    vs CONF_EGRESS_CAMERAS+CONF_PERIMETER_CAMERAS, with NO cross-exclusion validation), the linker camera-key
    allowlist, and CONF_ZONE_IS_OUTDOOR (which camera_census.py never imports = gap G9). The 2026-08-06
    playroom interior-leak hotfix hardened the LINKER only — the census has NO equivalent guard, so an
    interior cam wrongly added to CONF_PERIMETER_CAMERAS inflates the exterior headcount silently. (5)
    Pool-overhead/Balcony area conflict is real (resolver returns `balcony` for the pool-overhead primary)
    but does NOT bite either exterior surface: the linker keys on camera NAME, and the exterior census
    does no area dedup at all. Watch `armcrestash41b` (interior Study A) never fusing with `armcrest`
    — a name-PREFIX classifier would collide; none exists in shipped code. (6) Exterior is FULLY STRANDED
    from house state; the only coupling is one-way severity shaping (NM_HAZARD_EXTERIOR_PERSON_GUEST_SEVERITY
    — guest DEMOTES exterior severity; exterior never promotes guest).'
  merged_from_2026_08_16: 'Absorbed CENSUS-DEDUP-REPAIR-1 wholesale. That card covered P10/P11: repair
    the BLE-cancel and fresh-face dedup defences at source (both currently return zero, which is WHY the
    additive derivation double-counts). Kept as one cycle because the D1 clamp shipping in cycle 1 is
    a NO-OP whenever the camera body total is itself inflated — so decay-fixing without dedup-fixing would
    still leave the number wrong, and the two share every consumer and test surface.'
  suffix_migration_finding_2026_08_17: 'OPERATOR CONFIRMED: the Frigate1->Frigate2 migration WAS done;
    this aspect was missed. VERIFIED against the live registry (/Users/okosisi/ha-config/.storage/core.entity_registry
    — note CLAUDE.md documents a STALE path /Users/ojiudezue/...): all 23 `last_recognized_face` entities
    belong to the Frigate 2 config entry, ALL 23 carry the `_2` suffix, and ZERO bare-slug entities remain.
    Mechanism: F2 was added alongside F1, slugs collided, HA appended `_2`; F1 was then decommissioned
    and its entities removed, freeing the bare slug — but HA NEVER retroactively renames existing entities.
    So F2 kept `_2` permanently and the bare slug sits free and unused. ROOT ISSUE IS NOT THE SUFFIX,
    IT IS BYPASSED MACHINERY: camera_resolver.py:291 `_strip_disambiguation_suffix()` already exists FOR
    EXACTLY THIS (added 2026-08-01 from a bench finding on camera.armcrestash41b_2), but four sites construct
    entity IDs by hand and never consult it — camera_census.py:2383, :2416, :2736 (f"sensor.{base_name}_last_recognized_face",
    derived by stripping _person_occupancy off a RESOLVED binary_sensor and assuming the face entity shares
    that base — it does not) and :3023 (f"sensor.frigate_{person_slug}_last_camera", wrong on the suffix
    AND on the name: live is frigate_oji_* vs configured oji_udezue). Measured consequence: fresh-face
    -1 has fired ZERO times in 7 days and structurally cannot. FIX = registry resolution, NOT string renaming
    (renaming re-breaks on the next collision); precedent is cycle-1 D3, which replaced constructed-slug
    lookup with entity_registry.async_get_entity_id for this exact failure class. NOTE prior art at perimeter_alert.py:1879-1880
    which already hardcodes BOTH _person_detected and _person_detected_2 — a local coping hack, not a
    shared fix. PATTERN (third instance tonight, = Bug Class #53 computed-but-not-consumed applied to
    RESOLVERS): we build the right primitive and then fail to consume it at the sites that need it — cf.
    ExteriorTrackLinker.census_counts() stranded while the exterior census sums raw per-camera bits.'
  scope_clarification_2026_08_17: 'Operator scope check 2026-08-17: "Isn''t cycle 2 about interior accuracy?
    The exterior was a bonus? Or does cycle 1 fix that?" — CONFIRMED. Cycle 1 (CENSUS-GHOST-DEDUP-1) fixes
    GUEST MODE, not the interior count (its D1 clamp is a no-op when the camera body total is inflated).
    THIS cycle is the INTERIOR-ACCURACY cycle. CORE (the point): (1) decay/self-refresh separation — targets
    the measured 74.5%% of over-count time that was the decay tail with no live camera evidence; (2) the
    _2-suffix fresh-face resolution fix — fresh-face -1 has fired ZERO times in 7 days because the code
    builds sensor.{base}_last_recognized_face but every live entity carries _2 (missed F1->F2 migration
    residue). BONUS (not the point, do not let it bloat scope): P12 exterior dashboard wiring (deduped
    headline + naive fallback + divergence). The original "swap the exterior headcount" is DEAD — KEEP
    BOTH per AUDIT_exterior_census_supersession.md. Dedup repair also DROPPED (probe: buys ~0; it is camera-area
    coverage = an operator config task, not a build).'
  updated: 2026-08-18 03:15
  d3_dashboards_done_2026_08_18: 'D3 (P12) exterior dashboards DONE (display-only, no producer change):
    composed card (deduped headline + G1 naive-floor fallback [never 0 when floor>0] + divergence badge)
    added to HA ura-v6 (Presence/Census Cross-Confirmation), ura-v8 (Security/Exterior), and the PWA (Presence/House-aggregates,
    branch census-p12-exterior-dashboard, built+dual-pushed). ENTITY-NAME CORRECTION: the AUDIT_exterior_census_supersession
    names sensor.exterior_person_tracks_active / exterior_unidentified_persons which DO NOT EXIST; the
    live entities are sensor.ura_security_coordinator_outside_people_being_tracked (deduped), sensor.universal_room_automation_persons_on_property_exterior
    (naive floor), sensor.ura_security_coordinator_outside_unidentified_people (unidentified). Use the
    live IDs, not the audit names. HA writes post_write_verified; PWA build+tests green. KEEP-BOTH preserved.
    NOTE: PWA change is on a branch/pushed — confirm PWA deploy to go live; HA storage-dashboard cards
    are live now.'
- id: EXTERIOR-DWELL-LOITER-1
  title: Circling classification has no dwell/loiter predicate — a 20-minute stationary watcher reads
    as pass_by
  thread: security
  status: parked
  approval: explicit
  approved_by: operator 2026-08-16 ("Card the security finding. May need more investigation before planning.
    Connect it to our work on Linker and this.") — carded for INVESTIGATION FIRST, not straight to planning.
  tags:
  - security
  - measure-before-build
  - no-fabrication-verify
  origin:
    date: '2026-08-16'
    gist: Surfaced by the exterior track-vs-census investigation while answering the operator's question
      on how the circling work intersects exterior census accuracy. Operator had just made exterior count
      accuracy a first-class SECURITY goal, not merely a guest input.
  problem_solution:
  - 'P1 `ExteriorTrackLinker.classify()` (exterior_track_linker.py:705-750) is PURELY TOPOLOGICAL. `circling`
    iff revisit_count >= 1 OR (camera_count >= EXTERIOR_TRACK_CLASSIFY_CIRCLING_CAMERAS AND non-monotonic
    path); `approach` iff egress-adjacent or camera_count >= EXTERIOR_TRACK_CLASSIFY_APPROACH_CAMERAS;
    else `pass_by`. `duration_s` IS computed and IS persisted in the episode attrs — but it NEVER gates
    a classification. Consequence: a person stationary at ONE camera for 20 minutes has revisit_count=0
    and camera_count=1, so it classifies `pass_by` and draws DIGEST severity, while a resident walking
    the dog past three cameras can out-rank it. S1 (candidate, NOT yet a plan) add a dwell predicate so
    sustained single-camera presence can reach `circling`/a new `loiter` class.'
  - P2 severity is keyed on (label, house_state, classification) via NM_HAZARD_EXTERIOR_TRACK_SEVERITY_MAP
    (const.py:1842-1875), so ANY new class is a severity-table change with NM blast radius — a new row
    set, not a one-line predicate. S2 scope the table change explicitly alongside the predicate; treat
    the map as the real interface.
  why: 'Loitering is the canonical security signal the current classifier is structurally blind to, and
    the blindness is not an oversight in the data (duration_s is right there) but in the predicate. Directly
    connected to the shipped linker work: CIRCLING-LABEL-1 (v5.76.0) fixed dispatch on circling TRANSITIONS,
    and CIRCLING-SEVERITY-1 tunes the severity map — both assume the CLASS ITSELF is assigned correctly.
    This card questions that assumption, so it is upstream of both.'
  constraints:
  - INVESTIGATION BEFORE PLANNING (operator). Do not write a planning doc off this card's hypothesis.
  - Any dwell threshold is a NUMBER and gets a knob with a rung — likely rung 1 (module constant) alongside
    the existing EXTERIOR_TRACK_* family, since changing it should require review.
  - 'False-positive cost is real and asymmetric: a dwell predicate that fires on a parked delivery driver,
    a gardener, or a stationary camera artifact converts a quiet DIGEST into a CRITICAL page. Measure
    before choosing.'
  next: PARKED pending prerequisites (camera-role gating + revived face-ID + track stitching). Face-ID
    revival is shared with CENSUS-ACCURACY-1 _2-suffix fix — revisit this card once that ships and face-ID
    is live.
  refs:
  - custom_components/universal_room_automation/exterior_track_linker.py:705-750
  - custom_components/universal_room_automation/const.py:1842-1875
  - docs/planning/AUDIT_memory_handbuild_compactor_exterior_track.md
  - docs/planning/PLANNING_circling_severity.md
  - docs/planning/PLANNING_circling_label_transition_dispatch.md
  - docs/planning/PROBE_exterior_dwell_loiter.md (491195ed9)
  probe_outcome_2026_08_17: 'PROBE DONE (PROBE_exterior_dwell_loiter.md, 491195ed9). RECOMMENDATION: DON''T
    build a raw duration predicate now. 623 person tracks / 11 days. Hypothesis PARTLY confirmed: dwell
    DOES leak into pass_by (41 tracks >5min, 4 >20min, max 70min) BUT circling already holds 71 of 133
    tracks >300s (median 407s), so loitering is only partly invisible. KILLER FINDING: of the 56 newly-catchable
    tracks (camera_count==1, >300s), 19 (34%) are INTERIOR cameras (master_hallway, upstairs_hall, staircase,
    playroom, family_room = residents INSIDE), and most perimeter ones are own back_yard/pool. Face-ID
    is DEAD (2/623 identified) so there is NO resident-vs-stranger signal. A raw predicate would convert
    quiet DIGESTs into CRITICAL pages on the household itself. Also 300s sits exactly on the TRACK_CLOSE_IDLE_S=300
    fragmentation seam (107 same-camera pairs <=600s gap) so duration_s is a LOWER BOUND. PREREQUISITES
    before any dwell predicate: (1) camera-role gating (interior/own-yard/perimeter-approach); (2) resident/stranger
    signal — REVIVE FACE-ID (this is the SAME _2-suffix fix in CENSUS-ACCURACY-1, so there is a real dependency)
    OR an allowlist; (3) track stitching across the idle-close seam. THEN threshold >=600s on perimeter-approach
    cameras only. Merit CONFIRMED but gated on prerequisites — parked, not dropped.'
- id: EXTERIOR-GUEST-EGRESS-1
  title: 'Exterior->interior guest admission: plumb identity through the egress event so an UNKNOWN person
    crossing inside can corroborate guest'
  thread: presence
  status: parked
  approval: explicit
  approved_by: operator 2026-08-16 ("Agree on recommendations... Card the one you said to park and do
    it next so we have the complete set") — approved as the cycle AFTER the census set (1 guest-census
    correctness, 2 decay separation, 3 dedup repair), making this the fourth and completing the set.
  tags:
  - tier-3
  - context-wide-scoping
  - producer-and-consumer
  - marginal-benefit-pushback
  origin:
    date: '2026-08-16'
    gist: 'Split out of CENSUS-DECAY-SEPARATION-1 P8 after the exterior investigation showed exterior->headcount
      and exterior->guest are different-risk problems. Operator''s own framing: "We would need to know
      the transition from outside to inside via an egress right? And that the person is unknown and then
      correlate with other guest signals - carefully."'
  problem_solution:
  - P1 the egress detector cannot say WHO. `EgressDirectionTracker` (transit_validator.py:829-1140) ALREADY
    resolves direction correctly — egress cam then interior cam within EGRESS_ENTRY_WINDOW_SECONDS =>
    `entry`; reverse => `exit`; else `ambiguous` + cooldown — and already fires `ura_person_egress_event`
    with a confidence (0.9/0.8 multi-platform, 0.4/0.3 ambiguous) and persists via database.log_entry_exit_event.
    BUT `person_id` is HARD-CODED None at :1106 and :1121, so the event can say "someone came inside"
    and never "an UNKNOWN person came inside" — which is the entire load-bearing predicate for the operator's
    guest rule. S1 plumb an identity verdict (known / unknown / indeterminate) onto the event.
  - P2 interior adjacency is unmodelled — `_get_interior_cameras_near()` (transit_validator.py:1130-1140)
    returns ALL interior cameras with an explicit "without explicit adjacency mapping from the user" comment,
    so an `entry` can be confirmed by a camera on the far side of the house. S2 model interior adjacency
    for egress-adjacent cameras only (hand-build the table first per measure-before-you-build, mirroring
    the ratified EXTERIOR_ADJACENCY_GRAPH).
  - 'P3 the exterior track and the egress crossing are never joined — the linker has NO reference to `ura_person_egress_event`
    and the egress tracker never reads tracks; a track that approaches an egress camera and vanishes just
    closes on `idle_timeout` and is never continued inside. S3 hand off: an approach-classified track
    terminating at an egress, followed by an `entry` verdict in-window, is an ARRIVAL with a carried identity
    verdict.'
  - 'P4 guest must not be thresholded off exterior presence. S4 exterior arrival is a CORROBORATOR only
    — it may raise confidence in, or shorten the dwell for, a guest-room-gated entry; it may NEVER solo-arm
    guest. Operator: "Never ''someone is outside therefore guest''".'
  why: 'Completes the operator''s (b) concern — the transition INTO guest — with a causal mechanism rather
    than a count threshold. Deliberately split from the cycle-2 headcount swap because the risk profiles
    differ sharply: the headcount swap is read-only composition of an existing better number, whereas
    this would create the FIRST exterior->house-state trust edge (today the coupling is strictly one-way:
    guest demotes exterior severity, exterior never promotes guest). Marginal-benefit ruling: the simple
    half captures the security/accuracy value now; this half buys guest-detection quality and is priced
    separately.'
  constraints:
  - Tier 3 — new cross-coordinator trust edge (security/exterior -> presence/house-state) plus identity
    handling. Four framing-disjoint reviews incl. the adversarial-completeness pass.
  - CORROBORATOR ONLY — must never solo-arm guest. The guest-room gate (cycle 1 D2) stays the primary
    arm.
  - Identity must come from LOCAL sources only (Frigate-2 + UniFi Protect). Note the linker's `identified`
    is a Frigate sub_label promoted only after >=2 hops (exterior_track_linker.py:521-551) — it is NOT
    the URA person/BLE identity and there is no cross-walk today. Building that cross-walk is part of
    the scope.
  - Do NOT start before cycle 1's guest-room gate is live and organically proven — this cycle corroborates
    that gate, so an unproven gate makes it unverifiable.
  next: 'PARKED (deferred): the face-independent nudge revisits only if the identity path proves insufficient.
    Trigger: identity-at-egress shipped + shown to miss real guests.'
  refs:
  - custom_components/universal_room_automation/transit_validator.py:829-1140
  - custom_components/universal_room_automation/exterior_track_linker.py:766-777
  - docs/planning/RESEARCH_census_vs_guest_separation.md
  - docs/PLANNING_v3.5.2_CYCLE_6.md:428-551
  - docs/planning/PLANNING_exterior_guest_egress.md (486627875)
  updated: 2026-08-18 01:45
  refinement_status: refined
  d0_probe_outcome_2026_08_17: 'D0 PROBE DONE (PROBE_exterior_guest_egress.md, 1970f6360) — REJECTS the
    identity path, FINDS a face-independent alternative. Numbers: egress events fire ~50/day (186 entry/wk,
    6651 rows over 166d in person_entry_exit_events; ambiguous filtered out pre-persist so 100% high-confidence;
    person_id 100% NULL = D1 not built). D1 (populate person_id) = NO-GO: face coverage at egress cameras
    is 0% today AND only ~7% even AFTER the cycle-2 _2-suffix fix (recognized faces cluster on INTERIOR
    cameras; only 18/218 face-changes in 7d at door/exterior cams) — below the 30% gate; would identify
    <1 in 10 entries. D2 (guest corroboration) = NO-GO (depends on D1 coverage). D3 (approach-track corroboration)
    = GO, strong: 220 approach+person exterior tracks, 207 (94%) terminate at an egress-adjacent/door
    camera, ~18/day, FACE-INDEPENDENT. RESCOPE: park D1/D2 (revisit trigger: door/exterior face coverage
    >=30% on a re-run); adopt D3 as the cycle deliverable — wire the approach-track->egress signal as
    a census_confidence CONTRIBUTION to the existing unidentified gate (INV-4 path b), NEVER a third arm.
    Needs operator go on the rescope.'
  operator_ruling_2026_08_17_build_both: 'OPERATOR RULING 2026-08-17 (overrides the D0 NO-GO on identity):
    "Build both face and face-independent solution and use face as a confidence boost." Plus critical
    context that INVALIDATES the D0 face measurement: (1) the D0 probe measured FRIGATE face ONLY — it
    MISSED the UniFi PROTECT face source entirely (probe blind spot). (2) Protect face is sparse but CONCENTRATED
    on the G6 ENTRY cam + (likely) family room — i.e. exactly the egress-relevant location the identity
    path needs. So real egress face coverage is HIGHER than the Frigate-only 7%. (3) Frigate-2 NVR tuning
    is still NEW, so Frigate coverage will improve too. (4) Cycle 2 is barely exercised because the house
    is empty. MANDATE: cross-correlate BOTH NVRs Face ID (Frigate-2 + UniFi Protect) — never one source
    alone. RESCOPE cycle 3 to build BOTH: (a) face-independent approach-track->egress corroboration (D0
    said 94% GO), AND (b) face identity via cross-correlated Frigate-2+Protect face, used as a CONFIDENCE
    BOOST (not a sole gate, not a third guest-arm). NEXT: re-probe face coverage INCLUDING Protect at
    egress cameras before replanning.'
  protect_reprobe_2026_08_18: 'PROTECT FACE RE-PROBE (PROBE_protect_face_egress.md) — the "build both"
    face premise does NOT hold on current sensing. Findings: (1) UniFi Protect exposes face to HA ONLY
    as event.<cam>_smart_detection {event_type:face} = face DETECTION, never a NAME. Protect contributes
    ZERO named-identity ENTITIES to HA. (2) The Protect NVR controller DOES know 4 residents and face
    events carry recognized_person_name — but that lives on the NVR REST API, NOT as an HA entity; URA
    cannot read it without a NEW integration. (3) Protect face DETECTION is 27x skewed interior: Family
    Room 276 vs Madrone G6 entry 10 over 7.7d; at the entry cam ~10/wk with 0 carrying a resident name.
    NAMED recognition is a Family-Room-INTERIOR phenomenon (people named AFTER inside, not at the door).
    (4) Combined Frigate+Protect egress face-identity coverage = ~7% (Protect adds ~0). D1 (identity on
    egress) = NO-GO (<30% gate). The operator belief "Protect concentrated on G6 entry + family room"
    is HALF-TRUE: detection is there, but recognition-WITH-NAME is interior-only and the entry cam sees
    no names. RECOMMEND: build the FACE-INDEPENDENT arm (approach-track->egress, 94% GO from the first
    probe) as cycle 3; PARK the face arm with unblock = a new Protect-name HA integration + re-probe (revisit
    trigger: named recognition at door/exterior >=30%).'
  operator_corrections_2026_08_18: 'OPERATOR 2026-08-18 (build face-independent + corrections): (1) SPIKE
    EXISTS — RESEARCH_protect_face_to_ha.md (2026-08-15) already evaluated getting Protect face NAME into
    HA. Protect API capability CONFIRMED (name+id+confidence verified live; Family Room AI Theta emits
    named face events). Core HA does not map it; recommended path = Protect Alarm Manager webhook -> HA
    webhook (4b, ~20 lines, near-zero maint, gets the name today). Alt: unifi-protect-bridge HACS, or
    URA->uiprotect direct. (2) GEOGRAPHY CORRECTION: the probe measured the FRONT DOOR (madrone_g6_entry)
    but MOST FAMILY ENTRIES ARE VIA THE GARAGE, not the front door; family room is the central interior
    area (covers living room + kitchen) and is where Protect names people. So the "interior-only recognition"
    is actually ON the real entry path (garage -> family room) — the face arm is VIABLE, the probe just
    looked at the wrong cam. (3) So: face arm = FAST-FOLLOW (not dead): wire the Alarm Manager webhook
    -> ura_face_identified event -> use family-room/garage named recognition as the interior-arrival identity.'
  cycle3_scope_final_2026_08_18: 'CYCLE 3 SCOPE (operator): BUILD the face-INDEPENDENT arm NOW (approach-track->egress
    corroboration, 94% GO from PROBE_exterior_guest_egress.md) as a census_confidence contribution to
    the unidentified gate (INV-4 path b, never a third arm). FACE arm = fast-follow via the Alarm Manager
    webhook path (EXTERIOR-GUEST-FACE-FASTFOLLOW-1, to card). Re-measure egress coverage against the GARAGE
    entries + family-room arrival, not the front door, when scoping the face fast-follow.'
  direction_2026_08_18: OPERATOR CHOSE IDENTITY PATH FIRST (over the planner's BUILD-the-nudge). The face-independent
    Tier-3 approach->census_confidence nudge (PLANNING_exterior_guest_egress.md rev-2, orchestrator dissented
    on marginal-benefit) is DEFERRED — revisit only if identity proves insufficient. The identity build
    moves to EXTERIOR-GUEST-FACE-FASTFOLLOW-1 (now primary). Plan rev-2 stays on file for the deferred
    nudge.
- id: EGRESS-INTERIOR-COUNT-REINFORCE-1
  title: Use exterior->interior egress transitions to STRENGTHEN interior count accuracy (scope 2 of egress)
  thread: presence
  status: planned
  approval: pre_approved_gated
  approved_by: 'operator 2026-08-17: "Seeing how exterior filters into interior and then using that to
    strengthen the interior count accuracy seems like a good thing. But let''s keep the scope to 1 and
    card 2. If 1 is accurate then we can build on it with 2. Card it, pre-approve it based on that gate."
    PRE-APPROVED, GATED on EXTERIOR-GUEST-EGRESS-1 D1 (identity on egress) shipping AND proving accurate.'
  origin:
    date: '2026-08-17'
    gist: 'Split from the egress design discussion: identity-on-egress (scope 1) is a prerequisite; using
      the resulting exterior->interior transitions to reinforce the interior headcount is scope 2.'
  problem_solution:
  - 'P1 the interior census derives from cameras+BLE+face and does not currently consume the fact that
    a specific person was OBSERVED crossing an egress from outside to inside. That crossing is strong,
    causal evidence a body entered the interior population — independent of whether interior cameras/BLE
    picked them up. S1 (scope 2, gated) once egress events carry identity (scope 1), feed confirmed exterior->interior
    transitions into the interior count as a corroborating/strengthening signal — a known crossing raises
    confidence, an UNKNOWN crossing is the guest-corroboration path. Never a blind increment: it must
    reconcile with what the interior substrate already sees, not double-count a person both surfaces observe.'
  why: 'The gate is deliberate: reinforcing interior count with egress data is only sound if the egress
    identity signal is itself accurate. Building it on an inaccurate scope-1 would inject a new error
    source into the interior count — the exact double-count class we spent this session fixing.'
  constraints:
  - 'GATE: do NOT build until EXTERIOR-GUEST-EGRESS-1 D1 has shipped AND organic evidence shows the egress
    identity verdict is accurate. If scope 1 is inaccurate, this card stays parked.'
  - 'PRODUCER/CONSUMER: producer is the egress event + track identity; consumer is the INTERIOR census.
    Must reconcile against interior substrate (no double-count of a person both see) — Bug Class #53/#22
    territory.'
  - 'Reuses the census dedup discipline: a crossing is evidence, not a raw +1.'
  next: 'BLOCKED on EXTERIOR-GUEST-EGRESS-1 D1 ship + accuracy proof. Then: measure how often egress crossings
    are NOT already reflected in the interior count (the gap this would fill) before designing.'
  refs:
  - docs/planning/PLANNING_exterior_guest_egress.md
  - 'depends-on: EXTERIOR-GUEST-EGRESS-1'
  updated: 2026-08-18 10:05
  d0_impact_2026_08_17: 'D0 probe impact: the gate ("D1 identity accurate") CANNOT be met via faces —
    face coverage at egress is ~7% even post-suffix-fix. So the identity-based interior-count reinforcement
    is not viable on current sensing. IF cycle 3 rescopes to the face-independent approach-track signal,
    revisit whether a FACE-INDEPENDENT interior-count reinforcement (an unknown approach-track that enters
    raises the interior unknown count) is worth it. Otherwise stays parked until face coverage improves.'
  coverage_ceiling_2026_08_18: 'CORRECTION 2026-08-18 (operator): the ~7% figure is NOT a coverage ceiling
    and must not be cited as one. It came from PROBE_protect_face_egress.md which measured the WRONG camera
    (front door madrone_g6_entry). Most family entries are via the GARAGE, and Protect names people in
    the FAMILY ROOM — i.e. ON the real entry path. Frigate is on ALL cameras (front overhead + doorbell
    + foyer; garage doorbell + inside-garage cam); Protect has face recognition on the front doorbell
    + family room, reachable via the LIVE Alarm Manager webhook (EXTERIOR-GUEST-FACE-FASTFOLLOW-1 D2).
    The identity path is VIABLE. Coverage must be RE-MEASURED against GARAGE entries + family-room arrival,
    not the front door. The face-INDEPENDENT approach-track arm is a DEFERRED, gated-on-value FALLBACK
    (EXTERIOR-GUEST-EGRESS-1, revisit only if identity proves insufficient) — operator ALREADY chose identity-first;
    do NOT re-recommend it off the 7%.'
- id: CENSUS-G6-RAW-PERSISTENCE
  title: 'G6 (PARKED, build only if needed): gate guest persistence on RAW unidentified, not the held/decayed
    value'
  thread: presence
  status: parked
  approval: implied
  origin:
    date: '2026-08-16'
    gist: 'Operator: "Do both [G1+G4]. Card G6 and only use it if needed."'
  why: Census hold(3min)+decay(-1/300s) makes a phantom structurally durable ~25 min, outlasting the 300s
    guest persistence gate — so "sustained" cannot distinguish sustained-because-real from sustained-because-held.
    ~15 LoC, D8 threading pattern.
  revisit_trigger: Build ONLY if, after G1+G4 ship, a phantom or a genuinely transient presence (delivery,
    brief visitor) still sustains guest mode. Under G4 this should be structurally blocked — no guest
    room sustaining 30min means no guest activation — so G6 is expected to be unnecessary.
  refs:
  - docs/planning/RESEARCH_guest_actuation_and_census.md
- id: CENSUS-GHOST-DEDUP-1
  title: Census double-counts residents as unidentified (4 known + 2 ghost bodies = 6) — BLE-cancel exists,
    is enabled, cancels nothing
  thread: presence
  status: shipped_organic
  shipped_version: v5.79.0
  approval: implied
  origin:
    date: '2026-08-16'
    gist: 'Operator home with family of 4; census read 6 (identified 4 + unidentified 2). Operator: "only
      4 of us — 2 are ghosts or unrecognized versions of us and should decay right?"'
  why: 'Cameras detect person-bodies but recognize NO faces (face_recognized_persons: [] with 4 known
    people home), so residents own bodies land in the unidentified bucket alongside their own BLE-identified
    selves = systematic double-count, and the house flips to GUEST state on its own occupants. The machinery
    built for exactly this — census_ble_cancel (per-area min(camera_raw_max, ble_here) subtraction, camera_census.py
    ~:2770-2822, kill switch ON in live config) — reported ble_cancelled_count 0 and area_contributions
    {} while camera_unrecognized was 2.'
  investigation_state_2026_08_16: 'RULED OUT: missing areas on the counting sensors (16 of 17 Frigate
    person-count/occupancy _2 sensors have effective areas; only the screened ASH41B lacks one, and it
    never detects). NOT YET EXPLAINED: why area_contributions is empty and cancelled_total 0 while unrecognized
    is 2 — suggests the contributions arrived via the unassigned_raw (null-area) path, which invariant
    I3 makes uncancellable BY DESIGN, or the enhanced-census path is not the one producing this count.
    No BLE-cancel INFO log lines observed at all.'
  next: 'Suite run + NAME-diff at tip 7e3fa18d0 (never completed — blocked all night by concurrent-pytest
    deadlock, now cleared and guarded by the new hook), THEN re-run the framings against the REPAIRED
    oracle (operator: "Fix, then re-run all framings"), then merge + deploy as v5.79.0, then write the
    observed table into README_v5.79.0.md. Operator 2026-08-17: "Don''t hold the cycle."'
  parked_alts:
  - Wait for face recognition to solve it (Frigate/Protect enrollment) — real but slower; the dedup should
    not DEPEND on faces working
  refs:
  - docs/planning/PLANNING_census_overcount_dedup_decay.md
  research_2026_08_16: 'RESEARCH_guest_actuation_and_census.md (8f55b243d) — root cause found: the ENHANCED
    census path (default ON) is ADDITIVE (total = identified + camera_unrecognized) and OVERWRITES the
    RAW subtractive path (total = max(camera, identified)). With face recognition at ~0, both dedup defenses
    return zero (fresh-face -1 AND per-area BLE-cancel), so residents are counted twice. Live: 10 = 4
    + 6 for ~5 real people; camera_unrecognized == frigate_count exactly. Guest composition is a plain
    OR whose asymmetries are backwards (confidence gates the census path only; inside GUEST the OR degenerates
    to the ungated guest-room path).'
  minimal_set_2026_08_16: 'OPERATOR-SCOPED MINIMAL SET = G1 + G4 only. G1 subtractive clamp (~10 LoC,
    no knob) because the count feeds away-inference/sleep-wake/NM/dashboards, not just guest. G4 invert
    composition so GUEST ROOMS LEAD and census corroborates (~5 LoC precedence, both operands already
    computed) because it survives the NEXT count regression — census has regressed twice in four days.
    G6 (persistence on raw not held) DEFERRED with trigger: build it only if a phantom or transient sustains
    guest mode after G1+G4 ship. G2 observability rides G1 as its acceptance instrument. G7 confidence->high
    REJECTED as useless: confidence already reads high (it measures platform agreement, not correctness).'
  guest_room_config_2026_08_16: 'Guest-room set audited when G4 (guest-rooms-lead) was proposed — only
    ONE of three can currently signal. (a) Guest Bedroom 1: WORKS. (b) Upstairs Guestroom: BROKEN — the
    listener slugifies the room name to binary_sensor.upstairs_guestroom_occupied but the real entity
    is binary_sensor.upstairs_guest_bedroom_occupied, so it subscribes to nothing and can never fire (slugify-guessing
    class; needs the code fix, presence.py:_discover_guest_rooms ~:4708). (c) Down Guest Bathroom: OPERATOR
    UNFLAGGED 2026-08-16 ("You are correct. Should not be") — bathrooms are transit space; 30min continuous
    occupancy is not a guest signal and would only ever fire on a stuck sensor. Staged as a flush-watcher
    rider for the next restart. OPEN: should Guest Bedroom 2 be flagged? (operator decision, not assumed).'
  build_state_2026_08_16: 'Branch feature/guest-census. Build c7c308a53 (D1 pre-cancel clamp, D2 guest-rooms-lead
    composition, D2b exit decoupling, D3 registry resolution, G2 diagnostics). THREE framing-disjoint
    reviews: A SHIP (6c89dc017), B SHIP-with-notes (f425a8de0, corrected — its first pass used a two-dot
    diff from the cycle''s own first commit and wrongly reported D1 absent, voiding its ripple framing
    until redone), C FIX-THEN-SHIP (d9a74e86e, found a CONFIRMED hollow anchor via variant-7 comment-out).
    Fix-up r1 0e0ea97a2 = 7 findings, suite 9182/26 name-diff empty, zero knobs. ORCHESTRATOR-FOUND HIGH
    in the fix-up itself: the B-MEDIUM-1 boot-seed created a false-guest path at boot (resident in a guest
    room + person_coordinator unpopulated -> first_seen seeded hours-old -> gate fires on first tick).
    Both claimed mitigations were the same event-driven mechanism and inert for a stationary resident.
    Fix r2 = live identity re-check in the gate (consumer verifies producer) + boot-seed clamp (GUEST_BOOT_SEED_MIN_RESIDUAL_S,
    rung 1). New invariant INV-GUEST-NO-RESIDENT added to the plan (702d2d472) because INV-GUEST-LEAD
    was SATISFIED by the defect and therefore did no work. README_v5.79.0.md drafted (fd1829d17) with
    discriminating acceptance criteria; L3 tests the boot regression for free because a resident is staging
    in a guest room tonight. KNOWN AND DOCUMENTED: this cycle fixes GUEST, not the census NUMBER — D1
    is a no-op when the camera body total is itself inflated (review A per-value table). Count repair
    is CENSUS-DEDUP-REPAIR-1.'
  fuller_pass_outcome_2026_08_17: 'FULLER ADVERSARIAL PASS (operator ruling "Fuller pass") — THREE MORE
    framing-disjoint reviews, ALL THREE DO-NOT-SHIP. This pass is the only reason the cycle did not ship
    broken. D (adversarial completeness, 13ba10861) + E (lifecycle, dcf4b3d09) INDEPENDENTLY found the
    same CRITICAL by different routes, and it is TWO separate bugs in one 20-line helper `_is_known_person_in_room`
    — (i) `manager.coordinators.get("person")` always None because the person coordinator is NEVER passed
    to register_coordinator() (only presence/safety/security/mf/energy/hvac/optimization are; all 7 sibling
    sites in the same file use hass.data[DOMAIN]["person_coordinator"] — :4814 was the lone outlier),
    and (ii) `_tracked_persons` does not exist repo-wide (one reference: the read itself; real store is
    person_coord.data[name]["location"]). It short-circuits on (i), masking (ii) — so a fix addressing
    only one would still have been silently broken. Orchestrator verified both by grep. CONSEQUENCE: the
    "known person -> disarm guest gate" Transition has NEVER fired since v4.7.2, so ANY 30-min occupancy
    of a designated guest room arms GUEST including by residents. This is LIVE, not latent — see guest_room_designation_correction
    below. F (test authority, 956e366cd) found a THIRD hollow-anchor variant beyond delete and comment-out:
    ORACLE-ECHO — `test_boot_seed_residual_clamp` imported GUEST_BOOT_SEED_MIN_RESIDUAL_S for its own
    expected bound, so setting that constant to 0 (its own documented kill-switch value) left the test
    GREEN. Also a second variant-7 anchor in test_registers_state_change_listener. FIX committed 7e3fa18d0:
    canonical hass.data lookup + real data shape, plus GUEST_KNOWN_STICKY_S=120 (rung 1) latch because
    BLE room location FLAPS (prior art: Jaya Bedroom flapping on Bermuda noise) and a flap during the
    gate re-check would otherwise fire GUEST on a resident. All 4 drills red-then-green incl. both revert-drills.
    Vocabulary verified live BEFORE coding: person `location` values are CONF_ROOM_NAME verbatim, so no
    mismatch.'
  guest_room_designation_correction_2026_08_17: 'ORCHESTRATOR ERROR, CORRECTED BY OPERATOR. I reported
    "zero rooms designated is_guest_room=True" and concluded D2 would silently DISABLE guest mode. WRONG
    — I queried a plausible key name instead of the one the code reads. The real key is CONF_ROOM_IS_GUEST_ROOM
    = "room_is_guest_room" (const.py:386, read at presence.py:4698). THREE rooms were designated: Guest
    Bedroom 1 (bedroom), Upstairs Guestroom / room_name "Guest Bedroom 2" (bedroom), and Down Guest Bathroom
    / room_name "Guest Bedroom 1 Bathroom" (bathroom - MISCONFIGURATION). The build agent made the SAME
    wrong-key error and I "verified" its claim using the same wrong key — two agents agreeing via one
    shared wrong method, which read as confirmation. Lesson: verify the METHOD, not just the claim. Because
    rooms ARE designated, the dead-oracle CRITICAL is a LIVE production bug, and it very likely explains
    the probe finding that 72% of guest minutes had unidentified_count==0 — that is Path B firing on residents
    through the dead identity check, with no census involvement. Two independent false-guest sources were
    running at once and we only had evidence of one. RESOLVED tonight: bathroom unflagged (see GUEST-ROOM-CONFIG-1).'
  empty_house_validation_window_2026_08_17: 'OPERATOR 2026-08-17 21:53 CT: house EMPTY until Tue afternoon/evening;
    operator back Wed afternoon. ABSENCE OVER THIS WINDOW IS EXPECTED — do NOT flag away/empty/census-0
    as anomaly. CONSEQUENCE for v5.79.0 live validation: L3 (resident staging in guest room must not flip
    guest at boot) and L8 (repaired identity check returns True for a resident in a designated guest room)
    are BOTH BLOCKED — they require a resident physically in a designated guest room, and nobody is home.
    These are the DISCRIMINATING tests for the dead-oracle repair, so the headline acceptance is ORGANIC-PENDING
    until people return (Wed PM at the latest). Tonight only proves: boot clean, zero URA ERROR, sensors
    present/correct, no watchdog restart, and house correctly reads away/empty. The empty-house "no false
    guest" observation is WEAK (the dead oracle would not have fired on an empty house either), so it
    must NOT be claimed as proof the fix works — that proof needs an occupied guest room.'
- id: CENSUS-GUEST-FLOOR-1
  title: Census blind to guests (read 4 with 10 in house) — re-admit the WiFi guest-VLAN count as a bounded
    FLOOR, gated to contain the FP problem that unplugged it
  thread: presence
  status: parked
  approval: unreviewed
  origin:
    date: '2026-08-15'
    gist: 'operator: "census says 4 but there are 4 of us and 6 guests — how is it missing this many people"'
  why: Census total = identified(BLE+face, structurally household-only) + camera_unrecognized(3-min hold).
    Guests only count while standing in view of transit-tier census cameras; the guest phones on the Revel
    VLAN ARE counted by _get_wifi_guest_count (camera_census.py:2779 — family-exclusion + MAC xref + recency)
    but a v2 decision EXCLUDED it from the formula (IoT false positives); diagnostics-only since. Tonight
    the one leg that knew ~6 guests was unplugged.
  parsimony:
    problem: the census cannot count unidentified people outside census-camera view even when their devices
      announce them
    verdict: SIMPLIFY
  parked_alts:
  - Add social-room interior cameras (living/dining) — operator hardware+privacy decision, not a cycle
  - Naive re-add of wifi count as additive term — rejected, recreates the v2 FP problem
  proposed_shape: 'total = max(total, identified + wifi_guest_floor), the floor admitted ONLY under guards:
    guest-mode active OR count stable >N min; existing family-exclusion layers retained; kill switch =
    existing exclusion default. Numbers get knobs.'
  next: ura-planner scope after current deploy queue clears; Tier 2 (presence/census shared primitive
    -> possibly 2-DB per standing policy).
  refs:
  - custom_components/universal_room_automation/camera_census.py
  parked_2026_08_15: 'SHRUNK by the regression root-cause (CENSUS-SUFFIX-FIX-1): the census was count-accurate
    until 08-13; the suffix fix restores that. Revisit trigger: AFTER the fix ships + one real gathering,
    if guest counts still under-read materially, revive the wifi-guest-floor leg.'
- id: MDNS-SERIAL-HOSTS-1
  title: mDNS-serial hostnames breaking cross-VLAN camera consumers — amcrest FIXED; audit F2 RTSP paths
    + any other serial-host configs
  thread: devices
  status: done
  approval: implied
  origin:
    date: '2026-08-15'
    gist: 'Operator: "some kind of mDNS error" on amcrest entry 01KFAEN928S190YEJ2V4SWY6S6 + "multiple
      integrations point at that camera — did the IP change?"'
  why: 'IP did NOT change (camera at 192.168.15.96 for 8.4d; sibling dahua entry pinned .96 all along).
    Cross-VLAN mDNS reflection broke, killing serial-hostname consumers. FIXED: amcrest host serial->IP
    (flush-watcher) + DHCP reservation pinning .96. FLAGGED: stale frigate config-of-record shows F2 RTSP
    for pooloverhead uses the SERIAL hostname — if live F2 config kept that pattern, its feed is dying
    the same death.'
  next: 'HOMELAB AGENT: check live F2 config for serial-hostname RTSP paths (pooloverhead at minimum;
    grep AMC serials); replace with pinned IPs. Also consider reservations for any other cameras without
    fixed IPs.'
  refs: []
  closed_2026_08_16: 'DEAD on verification (operator push: "why card it vs fix it"). The suspected second
    victim does not exist: zero frigate-platform entities for pooloverhead in the live registry — the
    camera is NOT in the current F2 config; the serial-hostname RTSP path lives only in the stale January
    snapshot. The one real casualty (amcrest entry) was fixed in-turn (host->IP + DHCP reservation, verified
    post-boot). LESSON: exhaust externally-checkable evidence before carding — the registry check that
    killed this card took 30 seconds and needed no F2 credentials.'
  final_resolution_2026_08_16: 'REAL root cause (operator kept pushing past my two wrong layers): the
    CUSTOM amcrest component performs a LIVE mDNS lookup at every setup whenever entry.data.mdns exists
    — it IGNORES host entirely (async_setup_entry: mdns key -> zeroconf resolve-or-raise; else -> data.url).
    Host repoint was a no-op. FIX: removed entry + re-added via config flow URL mode (http://192.168.15.96)
    — mDNS path bypassed permanently; all 3 camera streams STREAMING; same entity slugs reclaimed (no
    consumer breakage); no restart needed. DHCP reservation keeps the URL valid. Lesson: for custom components,
    read async_setup_entry before assuming standard host semantics.'
  operator_context_2026_08_16: Pool-overhead camera is watched by FOUR integrations — Protect, Frigate
    2, Dahua, Amcrest — a random artifact of broad integration coverage, NOT intentional per-integration
    roles. Do not be confused by multiple entity families for this one device; my earlier "not in live
    F2 config" registry read may reflect a different F2 slug rather than absence. Do not re-litigate.
- id: MEMORY-PROGRAM-EPIC
  STATUS_CORRECTED_2026_08_22: 'Was `in_progress`; the card own next field says "Epic foundation DELIVERED
    (writers + compactor live). Remaining is optional/parked (zone_phantom). Consider cl[osing]". Closing
    per that. The parked remainder is optional and recorded in the body — if it is revived it earns its
    own card rather than holding an epic open indefinitely. Reversible: reopen if the parked work is taken
    up.'
  title: 'EPIC — Hierarchical Entity Memory: every node (room/zone/house/coordinator) owns consultable,
    compressed history behind one queryable interface'
  thread: memory
  status: done
  approval: explicit
  approved_by: 'operator 2026-08-02 (founding concept: "What if each room had memory? ... zones ... house
    ... coordinators ... shared, with a standard interface queryable by any node at any level"); Stage-2
    go 2026-08-14 ("Do the compaction... finish")'
  origin:
    date: '2026-08-02'
    gist: Operator concept -> VISION + ARCHITECTURE + MVP doc set (all finalized 2026-08-02, self-critiqued
      DRAFT v2).
  why: Program-level card so the memory work has a home; children carry the stages. URA composition (devices->rooms->zones->house)
    applied to TIME.
  stages:
  - 'Stage 0 hand-build: DONE 2026-08-02 — AUDIT_memory_handbuild_study_a.md (Study A hand-ledger; kill
    gate PASSED, operator: "every")'
  - 'Stage 1 facade: SHIPPED v5.47.0 (Tier 2-DB, memory_mvp_tier2db.md; 5 HIGH fixed pre-ship). memory_facade.py
    + memory_episodes + memory_query (episodes/narrative/unusual/profile/facts) + diagnostics sensor +
    NM switch. Polish v5.47.2/v5.51.0/v5.53.0. 1,799 episodes accrued by 2026-08-14. Organic still open:
    next organic D2 demotion retro-adjudicates its creation episode.'
  - 'Stage 2 compactor: IN FLIGHT -> MEMORY-COMPACTOR-1 (plan-reviewed rev-2, builder running 2026-08-14)'
  - 'Stage 3 writer coverage: QUEUED -> MEMORY-WRITERS-1 (retro found writers-ride-detectors ceiling;
    0 FULL/2 PARTIAL/2 NONE)'
  - 'Doctrine: memory-first diagnostics -> MEMORY-FIRST-DIAGNOSTICS-1 (skills amended 2026-08-14)'
  - 'Future (architecture, unscheduled): batch adjacency-graph refresh; redaction enable (20k-row trigger);
    NM consumer deepening'
  next: Epic foundation DELIVERED (writers + compactor live). Remaining is optional/parked (zone_phantom).
    Consider closing the epic to done and opening a new epic for the room-to-room agentic layer when that
    becomes a priority.
  refs:
  - docs/planning/VISION_hierarchical_memory.md
  - docs/planning/ARCHITECTURE_hierarchical_memory.md
  - docs/planning/MVP_hierarchical_memory.md
  - docs/planning/AUDIT_memory_handbuild_study_a.md
  - docs/reviews/code-review/memory_mvp_tier2db.md
  - docs/planning/AUDIT_memory_retro_value.md
  updated: 2026-08-18 02:20
  created: 2026-08-05 00:00
  refinement_status: refined
  problem_solution:
  - 'Problem: we wanted every part of the house (each room, zone, the whole house) to keep a usable memory
    of what happened to it, instead of events scattered and never summarised. Solution: build it in stages
    — the raw event writers, then the nightly summariser (both shipped and live), then fill the gaps where
    events aren''t being recorded. Not blocked; the next piece is the gap-filling writers.'
  completion_2026_08_18: 'EPIC ESSENTIALLY COMPLETE: Stage-1 writers (D4-D7) shipped v5.78.0; Stage-2
    compactor shipped + running nightly (14 memory_facts 02:30 CT 08-17); MEMORY-WRITERS-1 substantially
    shipped (top-2 already live). Only optional remainder: zone_phantom writer (parked, MEMORY-ZONE-PHANTOM-WRITER-1,
    organic-gated). The epic delivered its foundation — consultable compressed history per node. Room-to-room
    agentic workflow (the operator vision) is a future epic on top of this, not part of MEMORY-WRITERS-1.'
- id: MEMORY-COMPACTOR-1
  parent: MEMORY-PROGRAM-EPIC
  title: 'Hierarchical memory — build the deferred daily compaction batch when memory_episodes has volume
    (trigger: any episode type >50 rows)'
  thread: memory
  status: done
  shipped_version: v5.76.0
  approval: explicit
  approved_by: operator 2026-08-02 (concept + MVP build approval); compactor deferred BY the MVP as a
    sequencing decision, not dropped
  origin:
    date: '2026-08-02'
    gist: 'operator: "What if each room had memory?" Hierarchical Entity Memory MVP Stage 1 SHIPPED v5.47.0;
      the daily compactor was deferred until episode volume exists.'
  why: Stage 1 facade + memory_episodes + memory_query service are LIVE (v5.47.0, Tier 2-DB). The distill/correct/redact
    compaction batch needs rows to compact; the architecture keeps it in full, the MVP deferred only its
    construction. Untracked on the board until now (shipped pre-board-hardening).
  revisit_trigger: 'ANY memory_episodes episode_type exceeds 50 rows (per MVP_hierarchical_memory.md Stage
    1 trim #1) OR facts() seeded set proves inadequate. Check episode row counts before dismissing.'
  organic_open: 'Stage-1 acceptance still open: the next organic D2 demotion must retro-adjudicate its
    creation episode within one cycle (live DB check) — verify + write back to this card.'
  next: 'ORGANIC CONFIRMED 2026-08-17: compactor runs nightly (wired __init__.py:2031, after incremental_vacuum),
    wrote 14 memory_facts at 02:30 CT today; memory_facts totals 53 rows across 08-15/16/17. Trigger long
    met (exterior_track 1327, actuation_conflict 999, occupancy_phantom 73, fan_transition_suppressed
    54 — all >50). Nothing pending here; move toward done.'
  refs:
  - docs/planning/MVP_hierarchical_memory.md
  - docs/planning/ARCHITECTURE_hierarchical_memory.md
  - docs/reviews/code-review/memory_mvp_tier2db.md
  trigger_FIRED_2026_08_14: 'Live memory_episodes count (mode=ro): 1799 rows; exterior_track=1044, actuation_conflict=639,
    occupancy_phantom=56 ALL exceed the 50-row build trigger (fan_transition_suppressed=41, comfort_fan_vetoed=19
    below). READY, not parked — the deferral condition is satisfied. The facade has been silently accruing
    adjudicated episodes since v5.47.0 with no compaction, so this is also mild unbounded-growth pressure.
    Next: plan the compactor (distill/correct/redact + batch adjacency) from ARCHITECTURE compactor section;
    Tier 2-DB (touches the shipped facade + DB).'
  go_2026_08_14: 'operator: "Do the compaction... Review that plan and see how quickly we can get started
    on the rest and finish. Do hand checked proofs if need be." -> plan (ura-planner from ARCHITECTURE
    compactor section) -> Tier 2-DB plan review -> build. Hand-checked proof authorized: hand-compact
    one episode type against live rows as the acceptance fixture (hand-build-fixture practice).'
  plan_review_2026_08_14: 'Plan review FIX-PLAN-FIRST (2 CRIT: same-transaction invariant unimplementable
    on per-acquisition write queue -> combined distill_memory_fact DAO; topic vocab gate bypass -> D0
    MEMORY_FACT_TOPICS registration + boot assert. 2 HIGH: retro-priority reconciliation, two-DAO read
    cap + drill 4). Rev-2 applied all 9 findings (711bec243), review record 9c19ac35f. Builder dispatched
    (worktree, checkpoint discipline).'
  live_validation_2026_08_14: 'L1/L2/L3/L5 PASS at boot (20 facts, 3 topics, coverage stamp verified,
    episodes preserved). Organic open: L4 first nightly 02:30 tick. Entity-id correction: device-prefixed
    ura_coordinator_manager_*.'
  updated: 2026-08-17 23:30
  created: 2026-08-11 00:00
  refinement_status: refined
  problem_solution:
  - 'Problem: the house records thousands of raw memory events but never boils them down, so a reader
    (or the rooms themselves) can''t consult a compact history — the volume just grows. Solution: a nightly
    job that reads the raw events and writes a small set of summarised facts. SHIPPED and running — it
    compressed today''s events into 14 summary facts at 2:30am.'
- id: MEMORY-FIRST-DIAGNOSTICS-1
  parent: MEMORY-PROGRAM-EPIC
  title: Memory-first diagnostics doctrine — memory_query is the FIRST surface checked in any investigation/trace,
    encoded in memory + skills
  thread: memory
  status: done
  approval: explicit
  approved_by: operator 2026-08-14 ("Add using memory as the first place to check for diagnostic work
    (amend memory and skills that invoke on investigation and traces)")
  origin:
    date: '2026-08-14'
    gist: 'Compactor go turn: operator noticed investigations (AWAY-BLOCK-1 etc.) hand-mined recorder/DB
      while 1799 adjudicated memory_episodes sat unconsulted.'
  why: 'The facade exists precisely to answer "what happened in this room/zone/house" — but no diagnostic
    workflow invokes it. Doctrine: episodes/narrative/unusual via memory_query BEFORE raw recorder mining;
    raw mining remains the verify step, not the entry point.'
  next: Amend memory (feedback file) + CLAUDE-adjacent investigation surfaces; retro-analysis quantifies
    the win.
  refs:
  - custom_components/universal_room_automation/memory_facade.py
  closed_2026_08_16: 'Doctrine shipped: all four investigation skills carry the mandatory memory-first
    section; feedback memory + MEMORY.md pointer written; retro (AUDIT_memory_retro_value.md) supplied
    the evidence. Nothing outstanding.'
- id: MEMORY-RETRO-VALUE-1
  title: Retro-check — which answers in the last few investigations were already derivable from memory_episodes?
  thread: memory
  status: done
  approval: explicit
  approved_by: operator 2026-08-14 ("Check to see what answers we could have derived from it in the last
    few investigations")
  origin:
    date: '2026-08-14'
    gist: Same push as compactor go.
  why: Evidence for the memory-first doctrine + input to the compactor plan (which episode types earn
    distillation priority).
  next: Agent replays AWAY-BLOCK-1, guest-FP, Frigate-ghost, fan-latch questions against live memory_episodes;
    report what memory would have answered vs what we hand-mined.
  refs:
  - docs/planning/AUDIT_away_transition_2026_08_13.md
  result_2026_08_14: 'AUDIT_memory_retro_value.md (commit 6a99575fa). Verdicts: AWAY-BLOCK-1 PARTIAL (all
    56 occupancy_phantom rows share mmwave_sole_fan_on_no_corroboration -> profile()/unusual() primes
    the fan-sustain hypothesis in minutes vs 4h recorder trace; but ZERO episodes for the 3 actually-latched
    rooms), guest-FP NONE (no house-state/census episode type), Frigate-ghost PARTIAL (1050 exterior_track
    hops answer attribution; Frigate-only witnesses so F2-Protect delta unanswerable), fan-latch NONE
    (Guestroom has zero rows ever; sustain captures out of writer scope). STRUCTURAL CEILING: writers
    ride the detectors (occupancy_phantom writer inside D2 demotion, coordinator.py:3455-3490) -> memory
    blind exactly in corroborator-less rooms where incidents live. Distillation priority for compactor:
    occupancy_phantom > comfort_fan_vetoed > exterior_track (baseline+outliers) > actuation_conflict (639
    rows mostly 5-min-tick repeats, aggressive compaction).'
- id: MEMORY-WRITERS-1
  parent: MEMORY-PROGRAM-EPIC
  title: 'Memory episode-writer coverage gaps — writers ride the detectors, so memory is blind where detectors
    fail (retro: 0 FULL / 2 PARTIAL / 2 NONE)'
  thread: memory
  status: done
  shipped_version: v5.78.0
  approval: unreviewed
  origin:
    date: '2026-08-14'
    gist: 'MEMORY-RETRO-VALUE-1 finding: occupancy_phantom writer inherits D2 fail-closed no-PIR gate;
      both recent incidents lived in rooms memory never heard about.'
  why: 'Memory-first diagnostics only pays if memory covers the question. Candidate writers from the retro
    (ranked by incident coverage): (1) D2-independent retro phantom writer keyed on fan-release correlation
    — would have captured ALL FIVE latches across both incidents; (2) away_transition_blocked; (3) tracker_trust_excluded;
    (4) house_state_transition with gate-input snapshot; (5) zone-tier divergence; (6) multi-source witnesses
    in exterior_track hops (unlocks F2-vs-Protect first-witness questions).'
  parsimony:
    problem: memory cannot answer diagnostic questions about the rooms/mechanisms where incidents actually
      occur
    verdict: SIMPLIFY
  constraints:
  - each writer is a NEW producer on the write queue — batch discipline (write-flood ghost)
  - writers must not couple to detector gates the way occupancy_phantom did
  next: DONE — top-2 writers shipped v5.78.0. Optional zone_phantom parked (MEMORY-ZONE-PHANTOM-WRITER-1).
  refs:
  - docs/planning/AUDIT_memory_retro_value.md
  folded_2026_08_16: 'FOLDED into the PATH-ALPHA cycle as D4-D7: phantom_retro, away_transition_blocked,
    tracker_trust_excluded (operator add), house_state_transition. Two candidates DROPPED with justification,
    zero parked (operator no-debt rule). Building now.'
  live_validation_2026_08_16: 'v5.78.0 LIVE 2026-08-16. L1 PASS (0 errors), L4 PASS (face_recognized_count
    + path_alpha_gate_source live on house-state sensor). L2 PASS-on-state / attribution organic: house
    is away with all 4 persons not_home and census 0 — but the transition coincided with the deploy restart.
    STRONG retrospective evidence: family left 09:29, house stayed home_day until 11:11 (~1h42m stuck-while-empty)
    on v5.77.0 the same morning — the bug was live right up to this deploy. L3 not verifiable with an
    empty house (no populated per-person rows); L5/L6 organic. Side-effect: census source_agreement now
    both_agree (was single_source).'
  updated: 2026-08-18 02:20
  created: 2026-08-11 00:00
  refinement_status: initial
  problem_solution:
  - 'Problem: the house only remembers an event if the detector that watches for it fires — so wherever
    a detector is missing or blind, that history is simply never recorded (a gap you can''t see). Solution:
    add a few targeted event-writers for the highest-value gaps, starting with the two most useful, so
    those histories get captured even when the usual detector misses them.'
  scope_reset_2026_08_18: 'PLAN (PLANNING_memory_writers.md) found the card premise STALE: the named "top
    1-2 writers" phantom_retro + away_transition_blocked are BOTH ALREADY SHIPPED in v5.78.0 (D4 memory_writers.py:101,
    D5 AwayBlockEpisodeTracker :186). The "4 rows" of away_transition_blocked is the SHAPE of its 300s
    min-hold open/close coalescing (one row per sustained block), NOT a coverage bug. D7 house_state_transition
    already carries census/unidentified/excluded/veto so guest-FP questions are natively answerable. So
    MEMORY-WRITERS-1 is SUBSTANTIALLY SHIPPED — closing as done. The ONE remaining actionable gap (zone_phantom,
    the F2 zone-vs-house divergence with zero witnesses) is LOW marginal benefit today (no active problem)
    — PARKED with organic trigger, see MEMORY-ZONE-PHANTOM-WRITER-1.'
- id: HOUSE-STATE-UTILIZATION-EPIC
  title: ROADMAP EPIC — give operational meaning to under-consumed house states (HOME_DAY dead, AWAY thin);
    rungs 2-4 unbuilt
  thread: presence
  status: parked
  approval: explicit
  approved_by: operator (per plan header) — "I like the recommendations even rung 4"
  origin:
    date: '2026-07-30'
    gist: 'Plan inventory audit 2026-08-14: PLANNING_house_state_utilization.md is an operator-ratified
      multi-cycle roadmap; rung 1 shipped in a parallel cycle, rungs 2-4 unbuilt.'
  why: Roadmap marker, not one build — several house states carry little operational weight. Carded as
    an epic so the rungs are not lost; each rung becomes its own Tier-2 cycle when pulled.
  revisit_trigger: Pull a rung when a concrete house-state-driven behavior is wanted; decompose per-rung
    then.
  next: Operator picks a rung to activate; until then held as roadmap.
  refs:
  - docs/planning/PLANNING_house_state_utilization.md
- id: GARAGE-EGRESS-APPLY-1
  title: APPLY the 2026-08-10 garage-camera ruling — garage_a/garage_b into CONF_EGRESS_CAMERAS at the
    NEXT deploy restart (operator said do not forget)
  thread: security
  status: done
  shipped_version: v5.76.0
  approval: approved
  origin:
    date: '2026-08-14'
    gist: 'Operator re-raised; ruling of 08-10 (garages -> egress list, NOT interior — noise) was left
      "config apply pending" for 4 days. Operator: "update config in house device config. And any other
      place its needed. Batch with next deploy restart and dont forget."'
  why: 'Egress list feeds perimeter_alert egress alerting; garages currently in NO camera list. Accepted
    consequence stands: D1 stuck-camera never covers garages.'
  apply_procedure: 'At next deploy restart: flush-watcher pattern edit of the parent URA entry options.egress_cameras
    += [camera.garage_a, camera.garage_b] (F2-owned base ids, verified live), applied in the stop->boot
    gap; post-boot verify list + perimeter_alert egress staging log. Check "any other place": grep CONF_EGRESS_CAMERAS
    consumers pre-apply and confirm no sibling list needs the same ids.'
  next: BLOCKS-CLOSE-OF next deploy — do not close the next deploy cycle without this applied + verified.
  closed_2026_08_14: 'Applied at v5.76.0 restart via flush-watcher (stop->boot gap); post-boot verified:
    egress_cameras contains garage_a+garage_b (README v5.76.0 L5 PASS).'
- id: SENSOR-FANINDEP-1
  title: Role matrix needs a fan-independence axis — 10GHz motion-mmWave fleet is corroborator-grade for
    stuck but NOT for fan-demotion
  thread: presence
  status: done
  approval: explicit
  approved_by: operator 2026-08-18 (board button)
  origin:
    date: '2026-08-14'
    gist: 'operator: the Hobeian "Millimeter wave motion detection" units (~20, transit areas) are 10GHz
      functional-PIRs (still-blind, long range). Registry confirms 3 Hobeian classes. But the Upstairs
      Guestroom unit of this EXACT model fan-latched twice 08-13 — doppler sees fans; the class is not
      fan-independent.'
  why: 'STUCK-SENSOR-1 (v5.75.0) role layer accepts list-derived motion kind as corroborator — correct
    for non-fan stuck pathology, fails-agree (=status quo, no regression) under fan latch. The deferred
    D5 role-migration should add fan-independence to the role matrix: fan-demotion legs require fan-independent
    corroborators (PIR/camera/BLE/bed); stuck-exclusion accepts motion-class incl. 10GHz doppler. Vehicle:
    CONF_SENSOR_CAPABILITIES declarations for the 10GHz fleet + a new failure-mode-style axis.'
  measurement_first: 'Passive recorder probe queued: does the NEW Living Room 10GHz unit latch the tower
    fan on its next run? (Placement/sensitivity may differ from Guestroom.) Result gates how urgent the
    axis is.'
  PROBE_RESULT_2026_08_23_CONFIRMED: 'Probe ran. FIRST: the card premise entity does not exist -- there
    is NO Hobeian unit in the Living Room (the tower fan fan.towerfan_dreopilotmaxs_wifi_livingroom is
    there, but no 10GHz unit is registered for that room). So the queued "does the NEW Living Room unit
    latch?" question is unanswerable as written. Ran the fan-independence test instead on the pair the
    repo itself confirms (PLANNING_fan_noise_mode2_ble_pause_recheck.md:61): hobeian_exercise_presence_2
    + fan.fan_switch_3, 7.2d of recorder history. TIME-WEIGHTED: P(presence=on | fan_on) = 99.9% (9.3h/9.4h).
    P(presence=on | fan_off) = 0.3% (0.6h/163.3h). Striking but CONFOUNDED on its own -- an occupancy-driven
    fan produces the same correlation, and an observation shared by every candidate mechanism is not evidence.
    So a discriminating edge-lag measurement was run, and it separates the mechanisms cleanly ON BOTH
    EDGES: RISING: 8 of 9 fan OFF->ON events are followed by a FRESH presence OFF->ON transition at 1.8/2.0/2.2/2.2/
    2.2/2.2/2.3/3.0 s (one outlier 5699s). Two facts kill the human explanation: (a) the fan LEADS presence
    by a machine-consistent ~2s -- an occupancy-driven fan would have presence PRECEDE it; (b) a fresh
    rising edge means presence read OFF at the moment the fan started, i.e. the fan was switched on while
    the room read vacant, and the sensor then lit up. FALLING: every one of 10 fan ON->OFF events is followed
    by presence ON->OFF at 60.5-74.2s -- the units occupancy clear-delay after its doppler target disappears.
    CONCLUSION: presence tracks the FAN, not the human, on both edges. This is a doppler latch, CONFIRMED,
    and it is a SECOND ROOM independent of the Upstairs Guestroom founding case (which rested on 2 anecdotes
    from 08-13). The class-level claim is now measured, not inferred. SHARPER THAN THE CARD ASSUMED: at
    0.3% presence-on across 163h of fan-off, this unit contributes almost NO genuine presence signal in
    this room -- nearly all of its on-time is fan-driven. The axis is not merely a correctness nicety
    for fan-demotion legs; for this deployment the sensor is close to a pure fan detector. That raises
    urgency and also argues the capability declaration should carry a still-blindness note, not only a
    fan-independence flag. OPEN: who turns fan.fan_switch_3 on while the room reads vacant (schedule?
    manual? another sensor?) -- worth one grep, since it is the trigger that exposes the latch.'
  FLEET_SCOPING_2026_08_23_CLASS_CLAIM_REFINED: 'Wiring census + per-unit edge-lag probe across the whole
    10GHz fleet. THIS OVERTURNS THE CARD FRAMING -- read before scoping. WIRING CENSUS (live core.config_entries):
    9 URA rooms have a Hobeian 10GHz unit wired, ALL of them in motion_sensors (none in occupancy_sensors
    or mmwave_sensors) -- so all 9 are corroborator-grade by list derivation. Only THREE of the 9 also
    have fans, i.e. only three are exposed to the fan-demotion leg at all: Upstairs Guestroom (fan.fan_switch_4),
    Guest Bedroom 1 (fan.guest_room_down_ceiling_fan), Breakfast Nook (fan.151732606487193_fan). The other
    six (Dining, Laundry Closet, Patio, Receiving, Guest Bedroom 1 Closet, Butler Pantry) have no fans
    -- zero blast radius today. NOTE the Exercise room unit measured earlier is NOT WIRED into its room
    config at all (its motion_sensors is the RGBW PIR). That is why it was a clean natural experiment:
    URA never acted on the Hobeian, so nothing confounded the correlation. It also explains the "fan turned
    on while the room read vacant" observation -- the room was NOT vacant; the PIR saw the person and
    started the fan, and the still-blind 10GHz unit only lit up once the blades moved. Direct confirmation
    of still-blindness, not just fan-coupling. PER-UNIT EDGE-LAG RESULTS (rising = fan_on -> presence_on):
    Upstairs Guestroom (founding case): 11/12 rising lags UNDER 10s (3.4-9.2s), falling 37-65s clear-delay.
    LATCH CONFIRMED -- the 08-13 anecdote is now measured. Guest Bedroom 1: 0/20 rising lags under 10s
    (100s to hours, uncorrelated), 570 presence transitions. NO LATCH. Breakfast Nook: 2/33 rising lags
    under 10s (8.0, 9.5 -- plausibly coincidence at n=33), 856 presence transitions. NO LATCH. CONCLUSION
    -- THE CLASS CLAIM IS TOO BROAD. Measured 2 of 4 units latch (Exercise, Upstairs Guestroom); 2 do
    not (Guest Bedroom 1, Breakfast Nook). This matches the operator origin note verbatim ("Placement/sensitivity
    may differ from Guestroom") -- that caveat turns out to be the headline, not a footnote. A CLASS-level
    fan-independence declaration would WRONGLY DEMOTE two well-behaved sensors and lose real corroboration.
    DESIGN CORRECTION: the vehicle must be PER-UNIT, not per-class. CONF_SENSOR_CAPABILITIES is already
    per-entity, so it already supports this -- no new axis granularity needed. Likely physical mechanism
    worth noting: the two non-latching rooms use CEILING fans (plausibly outside the sensor cone) while
    both latching rooms use switch/tower fans; not proven, but it predicts which future installs are at
    risk. SCOPE NOW: exactly ONE wired room needs a declaration today (Upstairs Guestroom). That is a
    very small fix. PARKED IDEA (do not build yet, marginal-benefit rule): the edge-lag signature is computable,
    so per-unit classification could be automated rather than hand-declared. Hand-built fixture exists
    now (this table) -- that is the acceptance fixture any automation must be diffed against. Revisit
    only if the fleet grows or units are relocated.'
  CENSUS_CORRECTED_BY_DEVICE_MODEL_2026_08_23: 'Operator supplied binary_sensor.mmwave_temp_lux_hum_zigbee_livingroom_presence
    -- the Living Room unit I had reported as NOT EXISTING. It exists, it is HOBEIAN "Millimeter wave
    motion detection", same model as the rest. MY ERROR, TWO CAUSES, BOTH METHOD: (1) I searched entity-name
    substrings; this units entity_id contains neither "hobeian" nor the room-prefix convention I assumed.
    (2) My %mmwave% search WOULD have caught it but I piped the output through head -50 and the list was
    longer than 50 lines, so I read truncation as absence. Same bounded-observation class as the earlier
    head -30 and sed-range errors. ** THE FLEET IS 35 UNITS, NOT 17. ** Enumerated by DEVICE MODEL via
    core.device_registry (manufacturer == HOBEIAN): 35 presence-ish binary_sensors. Only 17 carry "hobeian"
    in the entity_id; 18 do not and were invisible to every name-based search. STANDING RULE FOR THIS
    FLEET: enumerate by device model, never by entity name. ** WORSE, THE NAMES ACTIVELY MISLEAD ABOUT
    MODALITY. ** Three units are named binary_sensor.pir_zigbee_* (exercisecloset, mediacloset, ojivanity)
    but are HOBEIAN 10GHz DOPPLER devices, not PIRs. Anyone reasoning about which sensors are PIR vs mmWave
    from entity names -- a human, a future agent, or any capability-declaration work on this card -- would
    classify them backwards. ** CORRECTED BLAST RADIUS: 20 rooms wired (not 9), 4 fan-risk (not 3). **
    The Living Room is the fourth, and all four are now measured. ** LIVING ROOM RESULT -- the cards original
    queued question -- NO LATCH. ** 7.7d window, tower fan ran 47.9h. P(presence=on | fan_on) = 3.0% vs
    P(presence=on | fan_off) = 0.5%; rising lags under 10s: 1 of 79. Compare Exercise (LATCH): 99.9% vs
    0.3%, 8 of 9 rising under 10s. The Living Room unit is fan-INDEPENDENT despite a high-airflow tower
    fan in-room for 47.9 hours -- a strong negative test. ** MY FAN-TYPE HYPOTHESIS IS REFUTED. ** I proposed
    that switch/tower fans latch while ceiling fans do not. The Living Room TOWER fan does not latch.
    There is no clean fan-type predictor; it is per-unit placement/geometry. Retracted. ** FINAL TALLY,
    5 units of one model: 2 LATCH (Exercise unwired, Upstairs Guestroom wired), 3 DO NOT (Guest Bedroom
    1, Breakfast Nook, Living Room). Of the FOUR WIRED fan-risk rooms, exactly ONE latches: Upstairs Guestroom.
    ** The per-unit (not per-class) design conclusion is unchanged and now rests on a COMPLETE census
    rather than a partial one. Scope remains one declaration, one room. ** SEPARATE FINDING, not the latch
    question: ** the Living Room unit reads on for only ~2.1h of 178h (1.2%) in a main living space. That
    is very quiet and may indicate under-sensitivity or placement issues. Worth its own look; does not
    affect this cards conclusion.'
  RETRACTION_NAMES_ARE_RELIABLE_2026_08_23: 'OPERATOR CORRECTION, and they are right. I claimed three
    units named pir_zigbee_* are 10GHz doppler devices misrepresenting their modality, and that the fleet
    is 35 units. BOTH CLAIMS ARE FALSE. Retracted in full. MY ERROR: I enumerated by MANUFACTURER (HOBEIAN)
    and treated the whole result as one sensor class. Hobeian is a common manufacturer name shipping SEVERAL
    product lines. Breaking the same query down by device MODEL settles it: "Millimeter wave motion detection"
    -- 26 presence-ish binary_sensors. THIS is the 10GHz class this card is about. "24Ghz human presence
    sensor" -- 5 (mmwave_zigbee_gameroom / jayabedroom / mediaroom / studya / ziribedroom). DIFFERENT
    CLASS: 24GHz true mmWave with still-detection, NOT the still-blind 10GHz functional-PIR this card
    concerns. Must not be pooled with the 10GHz units. "24Ghz millimeter wave and T&H sensor" -- 1 (masterbedroom).
    Also 24GHz, also out of class. "Motion sensor" -- 3 (pir_zigbee_exercisecloset / mediacloset / ojivanity).
    GENUINE PIRs. ** THE ENTITY NAMES ARE RELIABLE AFTER ALL. ** pir_zigbee_* really are PIR; mmwave_*
    really are mmWave. The naming convention was never the problem -- my inference from manufacturer to
    modality was. Delete any follow-up card premised on names misrepresenting modality; there is no such
    defect. ** WHAT SURVIVES FROM THE EARLIER CENSUS NOTE. ** The genuinely useful half stands: entity-name
    SUBSTRING search is unreliable for FINDING the fleet, because in-class units use several prefixes
    (occupancy_lux_temp_humidity_hobeian_*, mmwave_temp_lux_hum_zigbee_*, mmwavemotion_*, and one raw
    0xa4c1... IEEE address). Enumerate by device MODEL -- which is also what distinguishes the classes.
    ** CORRECTED SCOPE, restricted to model == "Millimeter wave motion detection": ** 17 URA rooms wire
    a 10GHz unit; FOUR have fans -- Breakfast Nook, Guest Bedroom 1, Living Room, Upstairs Guestroom.
    Same four as before, all four measured, so the finding is UNCHANGED: exactly one latches (Upstairs
    Guestroom). All five units tested (incl. unwired Exercise) are confirmed in-class. Scope remains one
    per-unit declaration, one room.'
  PLACEMENT_EXPLAINS_THE_RESULT_2026_08_23: 'Operator: the Living Room unit is UNDER THE COUCH, deliberately,
    to catch traffic and movement in the core couch area. ** THIS UPGRADES THE NO-LATCH RESULT FROM CORRELATION
    TO MECHANISM. ** Under a couch, the units cone is aimed at floor traffic and the couch area and is
    physically shielded from the room volume where the tower fan stands. So the Living Room negative is
    not luck and not a quieter fan -- it is geometry, and it is the SAME variable that explains the positives.
    The per-unit (not per-class) conclusion now has a causal account, not just a 2-of-5 tally: what determines
    latching is whether moving fan blades fall inside the units cone. That also retires my refuted fan-type
    hypothesis properly -- fan TYPE was never the variable, fan-within-cone is. ** AND IT EXPLAINS THE
    1.2% ON-TIME I FLAGGED AS POSSIBLE UNDER-SENSITIVITY. ** Not a fault. A still-blind 10GHz doppler,
    aimed at traffic, in a room whose primary use is SITTING STILL, will read off most of the time by
    design. Withdrawn as a sensitivity concern. ** BUT IT SURFACES A REAL CONSEQUENCE, verified in the
    live room config. ** Living Room motion_sensors = [this one unit]; occupancy_sensors = []; no camera-person
    sensor and no BLE wired. So the rooms ONLY occupancy input is a still-blind sensor positioned for
    movement. A seated, still occupant (watching TV -- the rooms main use) is invisible to URA once movement
    stops, and the room manages lights (2) AND the tower fan. That is the classic lights-and-fan-off-on-a-seated-person
    shape. NOT a defect of the placement, which is doing its job for traffic; a COVERAGE GAP created by
    the room having exactly one input of this modality. Needs its own card -- corroboration for the seated
    case (camera person, BLE, media-player veto, or a still-capable 24GHz unit of which this house already
    has six on other rooms). ** SCOPE OF THIS CARD IS UNCHANGED: ** Upstairs Guestroom remains the single
    latching wired unit and the single declaration needed.'
  RETRACTION_LIVING_ROOM_COVERAGE_GAP_2026_08_23: 'I claimed the Living Room has ONE occupancy input,
    a still-blind 10GHz unit, and that a seated occupant would be invisible. FALSE. Retracted in full
    -- do NOT card the "seated-occupant coverage gap"; there is no gap. THE ROOM IS PROPERLY LAYERED,
    as the operator described it: presence_sensors = [binary_sensor.screek_human_sensor_l13_2412s_presence]
    -- a Screek L13 2412S, ESP/ESPHome 24GHz mmWave with TRUE STILL-PRESENCE detection. That is the MAIN
    sensor. The Hobeian 10GHz unit in motion_sensors is the BACKUP. Plus room_media_player = media_player.living_room_2
    (veto path), room_cameras = [camera.family_room], scanner_areas = [living_room, entry_way, dining_room]
    (BLE), and illuminance from the same Screek device. A seated TV-watcher is covered by the Screek,
    by the media player, and by BLE. ** MY ERROR, AND IT IS THE THIRD OF THE SAME SHAPE TODAY. ** I queried
    a HAND-PICKED LIST of key names (motion_sensors, mmwave_sensors, occupancy_sensors, camera_person_sensors,
    ble_trackers, ...) and read the absence of a hit as absence in reality. The actual key is `presence_sensors`,
    which I never asked for. Same class as (a) the head -50 truncation that made me declare the Living
    Room mmWave unit non-existent, and (b) the earlier head -30 / sed-range misses. In every case I bounded
    the observation myself and then treated the boundary as the world. ** STANDING RULE, adopt it for
    config checks: ** to answer "is X configured", DUMP ALL KEYS on the entry and search the VALUES. Never
    enumerate the key names you expect -- the schema has more fields than any of us holds in memory, and
    a guessed key list cannot distinguish "not configured" from "configured under a name I did not guess".
    ** WHAT THIS DOES NOT CHANGE. ** The Hobeian unit IS still wired as motion_sensors here, so a fan
    latch would still have polluted the room even as a backup input -- the no-latch measurement remains
    worth having, and the under-couch placement explanation still upgrades it from correlation to mechanism.
    The 1.2% on-time is fully explained (still-blind doppler, aimed at traffic, backup role) and is correct
    behaviour, not under-sensitivity. ** SCOPE UNCHANGED: ** Upstairs Guestroom remains the single latching
    wired unit needing a declaration.'
  SCREEK_LATCHES_2026_08_24_OPERATOR_HUNCH_CONFIRMED: 'Operator: "the screek is the one that latches imo".
    MEASURED AND CONFIRMED, and it is the consequential one -- I had tested the BACKUP sensor and reported
    the room fine. SUBJECT: binary_sensor.screek_human_sensor_l13_2412s_presence (Screek L13 2412S, ESP
    24GHz, TRUE still-presence) vs fan.towerfan_dreopilotmaxs_wifi_livingroom. 7.7d. P(presence=on | fan_on)  =
    83.4%  (42.0h of 50.3h) P(presence=on | fan_off) =  5.6%  (7.3h of 132.0h)   -- a 15x ratio rising
    lags: 25 of 84 under 10s, tightest cluster 1.0-2.2s (machine-consistent) falling lags: tight cluster
    11.3-15.7s = the 2412S absence-delay Both edges machine-timed, and the rising edges are FRESH transitions
    (presence read OFF when the fan started), so the fan LEADS presence. Causation direction is the same
    argument as the Exercise case. Corroborating: screek total on-time 49.3h vs fan total on-time 50.3h
    -- nearly identical. ** WHY THIS MATTERS FAR MORE THAN THE HOBEIAN RESULT. ** The Screek is presence_sensors,
    the rooms MAIN occupancy input; the Hobeian 10GHz is only the backup. And URA MANAGES this fan. So
    the feedback path fan-on -> presence-on -> room occupied -> fan keeps running is structurally present.
    ** BUT THE RUNAWAY LOOP IS NOT SUPPORTED BY THE DATA -- do not claim it. ** Overnight window 02:00-06:00
    across 7.7d: fan ON 406 min, room_occupied only 179 min. If the fan reliably held the room occupied,
    those would be close. Instead there are ~227 overnight minutes of fan-on-but-room-VACANT. The room
    does vacate while the fan runs. The latch is partial (83.4%, not 100%) and the rooms occupancy fusion
    has other inputs plus timeouts. ** SO THE DEFECT IS POLLUTION, NOT RUNAWAY: ** the main presence input
    is substantially fan-driven, which injects false occupancy during fan runs, distorts vacancy timing,
    and would corrupt any trust/corroboration or chatter logic that treats this sensor as independent
    evidence. Note the Screek also shows 619 sessions with a MEDIAN of 0.4 min (24s) -- extremely chattery
    for a sensor whose selling point is stable still-presence, consistent with responding to fan-induced
    micro-motion. ** CONSEQUENCE FOR THIS CARD: the fan-independence axis is NOT a 10GHz-class concern.
    ** It is a per-unit, per-placement property that crosses hardware classes -- the 24GHz still-capable
    main sensor latches while the 10GHz backup in the same room does not. Any capability declaration must
    therefore be per-entity and must NOT be inferred from model or class. Strengthens the per-unit conclusion.
    ** NEXT MEASUREMENT to size the pollution: ** find fan-on intervals where NO independent modality
    (Hobeian backup, BLE scanner_areas, media_player.living_room_2, camera.family_room) indicates anyone
    present, and measure how much of that time the Screek reads occupied. That is the false-occupancy
    budget and it discriminates pollution-magnitude from coincidence.'
  next: 'Scope the PER-UNIT fan-independence declaration for Upstairs Guestroom (the one latching wired
    unit). Census now complete by device model. SEPARATE follow-up worth carding: 3 HOBEIAN doppler units
    are named pir_zigbee_* and misrepresent their own modality.'
  updated: 2026-08-26 09:45
  closed_note: 'REFUTED FRAME (operator + measurements 2026-08-26). The role-matrix ''fan-independence
    axis'' is the WRONG frame. AUDIT_fan_signature_separability_probe.md measured: phantom mmWave onsets
    are TRANSITION-triggered — aligned to the exact second of a fan speed/power transition, while steady-state
    fan (hours) produces ZERO phantom edges, across 2 rooms + 2 fan types. The specific premise (a Living-Room
    10GHz latching unit) was also overturned (entity doesn''t exist). The real, measured fix is a fan-TRANSITION-COINCIDENCE
    onset gate, not a sensor-role axis. Superseded by FAN-TRANSITION-COINCIDENCE-GATE-1.'
- id: ROOM-NAME-DESYNC-1
  title: Options-flow room rename without data write-back — house tier permanently blind to 3 renamed
    rooms (substrate edges name-dropped)
  thread: presence
  status: shipped_organic
  shipped_version: v5.75.0
  approval: unreviewed
  origin:
    date: '2026-08-13'
    gist: 'ZONE-TIER-DIVERGE-1 thorough trace: presence house tier keys rooms by entry.data room_name
      (presence.py:2868); substrate dispatches under options-first merged name (occupancy_substrate.py:197-202).
      3 rooms renamed via options only -> every substrate edge silently dropped at presence.py:3082-3090.'
  why: 'BUG, live now (smoking gun: jaya_3_presence=on w/ substrate_kinds all-false). The 08-13 20:51
    away transition fired THROUGH occupied Upstairs precisely because the house tier could not see the
    two renamed rooms. Blast radius: away/veto/census, WAKING gate, v5.46.0 fan gate + fan-interference
    + D2 legs (all dead for the 3 rooms), zone confidence.'
  operator_decision: 'SEQUENCING TRADE: (a) config-mitigate NOW (re-align 3 entries names) = house tier
    regains sight, but away gets HARDER (3 more phantom-holdable mmWave zones until corroborators arrive
    — rec 1 hardware is operator-owned); (b) sequence the fix AFTER corroborators land. Then either way:
    Tier 2-DB cycle = options rename write-through to entry.data + one-shot sync migration + name-consistency
    invariant test.'
  next: Operator picks (a) now vs (b) after-sensors; then Tier 2-DB cycle (plan review first).
  build_dispatched_2026_08_13: 'Plan rev-2 (plan review: 4 HIGH fixed incl. double-reload + setup-reload-watchdog
    ordering + 3rd write site + CONF_ZONE fold-in). Build in flight (worktree). Hand-sync mitigation VERIFIED
    live same evening (Upstairs zone occupied w/ real provenance).'
- id: PATH-ALPHA-DENOM-1
  title: Path-alpha away inference structurally dead when all trackers LOST/STALE — trusted denominator
    empties; NO existing card fixes it
  thread: presence
  status: shipped_organic
  shipped_version: v5.78.0
  approval: approved_after_investigation
  origin:
    date: '2026-08-13'
    gist: 'Carded-coverage grading: the LOST-denominator gap (all 4 trackers LOST -> all_tracked_persons_away
      false-by-vacuity for hours) is owned by no card; v5.16.0 fixed the veto denominator, not this.'
  why: Path-alpha ignores zones entirely — with ACTIVE trackers it would have fired regardless of the
    fan latch. Fixing the vacuous-denominator case (all-LOST + all-entity-away => away-eligible) is an
    independent mitigation with its own balance questions (overlaps AUDIT_ble_house_level_weighing safe-shape).
  next: 'GATED on ZONE-TIER-DIVERGE-1 trace completing (same code region). Then: consumer enumeration
    of tracking_status (greps, all tiers) -> plan for decomposition path (1) with (2) as fallback if ripple
    too wide; Tier 2-DB minimum (trust-hierarchy).'
  operator_direction_2026_08_13: 'Operator: "we should find a way to say AWAY not LOST. Do we need a lost
    state at all? That way we can actually use this signal the way it is supposed to be used. And not
    overload it." I.e. the fix may not be patching the denominator arithmetic but DECOMPOSING the overloaded
    tracking_status: LOST currently conflates (a) confidently-away-no-BLE-fix (should just be AWAY), (b)
    home-but-BLE-silent (phone on charger — genuinely ambiguous), (c) truly unknown. If LOST is dissolved
    into honest states, path-alpha''s denominator never empties for case (a) and the vacuity bug disappears
    structurally instead of being special-cased.'
  alternate_paths: '(1) Dissolve LOST: away-with-no-fix => AWAY (trusted, counts in denominator); home-but-silent
    => new BLE_SILENT_HOME or stays ambiguous-excluded; keep LOST only for truly-unknown. Ripple: every
    consumer of tracking_status (H3 reliable-signal gating, excluded_persons, veto denominator v5.16.0,
    path-beta _tracking_active_or_lost_away — which becomes redundant/simplifiable). (2) Minimal: keep
    LOST, make path-alpha treat all-LOST+all-entity-away as away-eligible (the original card). (3) BLE-vacancy
    conjunct shape from AUDIT_ble_house_level_weighing.md. Operator leans (1) — un-overload the signal.'
  reconcile_2026_08_16: STALE STATUS FIXED (was inbox). Plan rev-3.5.1 committed fa31c6d45 after operator
    checkpoint + adversarial plan review; BUILDING NOW on feature/path-alpha (D1-D7 + Gap-B guard + EV
    riders).
  live_validation_2026_08_16: 'v5.78.0 LIVE 2026-08-16. L1 PASS (0 errors), L4 PASS (face_recognized_count
    + path_alpha_gate_source live on house-state sensor). L2 PASS-on-state / attribution organic: house
    is away with all 4 persons not_home and census 0 — but the transition coincided with the deploy restart.
    STRONG retrospective evidence: family left 09:29, house stayed home_day until 11:11 (~1h42m stuck-while-empty)
    on v5.77.0 the same morning — the bug was live right up to this deploy. L3 not verifiable with an
    empty house (no populated per-person rows); L5/L6 organic. Side-effect: census source_agreement now
    both_agree (was single_source).'
- id: AWAY-BLOCK-1
  title: House held home_day 2h with everyone away — fan->mmWave->occupancy->fan self-sustaining loop;
    both away paths structurally blocked
  thread: presence
  status: shipped_organic
  approval: unreviewed
  origin:
    date: '2026-08-13'
    gist: 'operator: "why not trust that signal and send the house to away mode? What are we getting wrong
      about this inability to transition?"'
  why: 'Traced (AUDIT_away_transition_2026_08_13.md): path-alpha dead (all 4 trackers LOST/STALE -> trusted
    denominator 0); path-beta vetoed by ONE zone occupied solely by the Living Room Screek mmWave, latched
    by the room''s own tower fan (fan ON 13:45 CT -> latch 6s later, held 2h01m; fan OFF -> release in
    37s -> away 5min later). The fan was URA''s OWN comfort fan, turned on BECAUSE the room read occupied
    — self-justifying loop. Shipped D2 fan-demotion is fail-closed OFF for the 6 rooms with no PIR (motion_sensors:
    []); v5.46.0 gate is creation-only; 4h continuous-stuck never met (flapping resets); duty-stuck detector
    fired 4 NM notes but is NOTIFY-ONLY (STUCK-SENSOR-1 gap, live again).'
  operator_decision: 'Ranked recs — pick any: (1) CONFIG-ONLY: add a PIR/corroborator to Living Room +
    the 5 other no-PIR rooms (re-enables shipped D2 demotion; highest marginal benefit, near-zero risk).
    (2) TIER-1: cap comfort-fan sustain on mmwave-sole provenance (break the loop from the fan side).
    (3) TIER 2-DB: path-beta discounts zones held only by fan-interference-flagged mmwave-sole signals.
    (4) STUCK-SENSOR-1 (complete fix, still gated; rec 1 is prerequisite for this room anyway). (5) phone
    tracker liveness (device-side).'
  next: Operator picks; orchestrator recommends 1+2 together (config turn + small loop-breaker), 3 only
    if evidence recurs after 1+2.
  operator_dispositions_2026_08_13: 'Rec 1: OPERATOR-OWNED — the existing Zigbee sensor is hallway-placed;
    operator adds a physical sensor himself. DO NOT RAISE AGAIN (explicit instruction); when new sensors
    appear in room configs, silently verify D2 arms. Rec 2: PARKED (adversarial audit: sleep-kill blocker
    + carve-out inversion; revive trigger = fan-latch recurrence in a D2-armed room). Rec 3 + BLE conjunct:
    PARKED (revive = phantom-zone away-block recurrence post-corroborators). STUCK-SENSOR-1: approved
    twice, fixture-supplement drafting IN FLIGHT. ZONE-TIER-DIVERGE-1 thorough trace IN FLIGHT.'
  reconcile_2026_08_16: Root fixes SHIPPED v5.75.0 (fan duty-flag exclusion + room-name write-through).
    Deeper structural causes are in flight as PATH-ALPHA-DENOM-1 (H3 over-reach) + GAP-A-CENSUS-HOLE-1
    (census half) + Gap-B guard. This card holds the incident record; closes when those ship and a real
    all-away transition is observed.
  shipped_version: v5.78.0
  updated: 2026-08-23 14:30
- id: CIRCLING-LABEL-1
  title: Circling loops page but are never LABELLED/escalated as circling (2-camera shape) — cooldown
    blocks the hop where classification forms
  thread: perimeter
  status: shipped_organic
  shipped_version: v5.76.0
  approval: unreviewed
  origin:
    date: '2026-08-13'
    gist: 'CIRCLING-SEVERITY-1 Review A MEDIUM-A1: founding shape pages at hops 1-2 as pass_by (LOW/MED);
      classification becomes circling at hop 3; per-camera 300s cooldown returns before severity re-resolves;
      continuation-coercion block is dead for this shape.'
  why: INV-M holds (pages happen, tripwire honest) but the operator's 08-08 complaint was about CIRCLING
    specifically. The dominant 2-camera alternating shape can never emit a HIGH circling-labelled page
    under current mechanics.
  operator_decision: '(A) surgical — allow ONE dispatch through the cooldown when a track''s classification
    TRANSITIONS (one extra HIGH page at the hop circling forms; ~persist last_dispatched_classification
    on ExteriorTrack). (B) tighten invariant + add circling-classified-but-no-HIGH counter to the D3 sensor.
    (C) accept as-is, update narrative. ORCHESTRATOR RECOMMENDS A (marginal-benefit: matches the founding
    ask, small; B is machinery without a consumer).'
  next: ura-planner -> plan review -> build.
  decision_2026_08_14: 'OPERATOR: Option A approved per recommendation — one dispatch allowed through
    the per-camera cooldown when a track''s classification TRANSITIONS (the hop circling forms => one
    HIGH circling-labelled page). Own Tier-2 cycle, plan review first (new cooldown exemption on the dispatch
    path).'
  plan_review_2026_08_14: 'FIX-PLAN-FIRST (0d30ee8bc): HIGH — plan''s XCORR-1 mechanism was WRONG; single-camera
    nighttime circling would demote the exemption dispatch to LOW (founding ask unmet in a reachable shape).
    Reviewer adjudicated fix: exemption_active early-return in _evaluate_burst_demotion + D5b single-camera-night
    test. +3 MED (missing is_life_safety_hazard import in perimeter_alert, NM dedup HIGH-HIGH collision
    unverified, I1 set-vs-bool underpin). Rev-2 in flight.'
  build_2026_08_15: 'feature/circling-label (6 commits, worktree): 21 new tests, 8/8 drills red-restored,
    0 HEAD-only suite failures (9026 pass). Notable builder find: plan''s I4 anchor was masked by I2 —
    added unique-anchor test. 2 framing-disjoint reviews dispatched (A correctness+invariants, B lifecycle+races+S1-S8).'
  reviews_2026_08_15: 'A SHIP (3f102e803) + B SHIP (ce9913b38), zero overlapping findings. Fix-up 4c1667f93
    (3 LOWs incl. B-LOW-1 cross-camera double-grant race -> optimistic seed + 4-path rollback, +3 load-bearing
    tests). Orchestrator re-drill: XCORR-1 short-circuit neuter -> 3 named red incl. D5b anchor; restored
    64/64. Base verified docs-only-ahead. MERGED to develop; awaiting deploy.'
  live_validation_2026_08_14: 'Shipped v5.76.0. Organic open: L6 next real escalating track -> one HIGH
    page at transition.'
- id: GAP-A-CENSUS-HOLE-1
  title: Path-alpha veto blocked by forgotten-phone BLE via census_count clause — replace with camera-provable-only
    evidence (face_recognized_count)
  thread: presence
  status: shipped_organic
  shipped_version: v5.78.0
  approval: implied
  approved_by: operator 2026-08-16 ("Gap A error is big and we have not offered a solution"; "Gap A should
    be folded in and shipped at the same time")
  tags:
  - tier-2db
  - no-fabrication-verify
  - context-wide-scoping
  origin:
    date: '2026-08-16'
    gist: AUDIT_away_transition_2026_08_13.md flagged the H1 census clause as latent; operator asked for
      the specific fix and required it ship in the same deploy as PATH-ALPHA.
  why: 'presence.py:1047-1057 gates path alpha on census_count == 0, whose intent-of-record (comment :1039-1042)
    is "Frigate face-IDs a resident -> phone trustworthiness irrelevant". But census_count = |ble_home
    union face_recognized| + held_unidentified (camera_census.py:3079-3103): a forgotten phone at home
    puts the resident in ble_home -> census_count >= 1 -> veto blocked even when H2 correctly excluded
    that person from the denominator. Independent SECOND path into the AWAY-BLOCK-1 cul-de-sac; H2 fixed
    the person half, the census half stayed open.'
  fix_sketch: 'Encoding A (~15-30 LoC, ONE deliverable): extend SIGNAL_CENSUS_UPDATED with face_recognized_count
    (existing CensusZoneResult.face_recognized_persons, camera_census.py:158/:3116); store on PresenceCoordinator
    via _handle_census_update (:4194-4207); new int kwarg face_recognized_count=0 on infer() (:961) preserves
    I3 byte-identity; replace the clause in path alpha. NO NEW KNOB — face freshness already bounded by
    CENSUS_FACE_RECOGNITION_WINDOW_SECONDS + the tracker cross-check (camera_census.py:3034-3055) that
    defeats the frigate last_camera re-stamp flap; that coupling gets a mutation-anchored test. Rejected:
    subtract-H2-at-veto-site (Bug Class #53 shape), redefine identified_count (sensor/DB ripple). Path
    beta symmetric clause :1123-1130 OUT OF SCOPE.'
  next: Plan review (running) -> build on feature/path-alpha AFTER its D1-D9 land (do NOT interleave presence.py
    edits) -> both covered by the same 3 framing-disjoint reviews -> ONE deploy.
  refs:
  - docs/planning/PLANNING_gap_a_census_hole.md
  - docs/planning/AUDIT_away_transition_2026_08_13.md
  - docs/planning/PLANNING_path_alpha_lost_dissolution.md
  plan_review_2026_08_16: 'SHIP (efec78928) — trace + consumer enumeration independently confirmed; circularity
    CLEAN (URA writes no person.* entity, so the matrix cannot feed back into the face cross-check — FENCE:
    re-audit if that ever changes); 3 text-only edits folded (face window = 1800s at const.py:2609; cross-check
    is fail-OPEN when person entity missing so the 30-min age gate is the sole bound in that mode — honest,
    still no knob; beta residual pattern named not built). CLEARED FOR BUILD after PATH-ALPHA D1-D9 on
    the same branch.'
  live_validation_2026_08_16: 'v5.78.0 LIVE 2026-08-16. L1 PASS (0 errors), L4 PASS (face_recognized_count
    + path_alpha_gate_source live on house-state sensor). L2 PASS-on-state / attribution organic: house
    is away with all 4 persons not_home and census 0 — but the transition coincided with the deploy restart.
    STRONG retrospective evidence: family left 09:29, house stayed home_day until 11:11 (~1h42m stuck-while-empty)
    on v5.77.0 the same morning — the bug was live right up to this deploy. L3 not verifiable with an
    empty house (no populated per-person rows); L5/L6 organic. Side-effect: census source_agreement now
    both_agree (was single_source).'
- id: PATHBETA-VESTIGIAL-1
  title: 'Path-beta infrastructure is now dead-code-like after D2b — scoped cleanup (reviewer B: do NOT
    bundle into PATH-ALPHA)'
  thread: presence
  status: pre_planning
  approval: unreviewed
  origin:
    date: '2026-08-16'
    gist: 'PATH-ALPHA review B finding F2 (62820d23c): after the relaxed-predicate retirement, path-beta
      infra at presence.py:1146-1215 + call site :5787-5799 is vestigial — lost_away_persons_present=False
      from the sole caller, immediate_engage limb unreachable, surviving gate strictly narrower than alpha.'
  why: Correctness is preserved (beta can only echo alpha or suppress; cannot fire spuriously), so this
    is hygiene not a defect. Reviewer B explicitly recommended a separate scoped cycle rather than bundling
    — bundling would have grown a Tier-2-DB cycle mid-review.
  next: 'Scope after PATH-ALPHA ships and its organic proofs land: delete or collapse the vestigial beta
    limbs with test migration, or document why they stay.'
  refs:
  - docs/reviews/code-review/path_alpha_review_B.md
- id: GUEST-FP-RESIDUALS-1
  title: Guest-FP audit residuals — path-alpha diagnostic classifier (A1, ~5 LoC) + camera-census outdoor
    filter (B1, latent)
  thread: presence
  status: shipped_organic
  shipped_version: v5.78.0
  approval: unreviewed
  origin:
    date: '2026-08-13'
    gist: 'AUDIT_guest_fp_fixes_wiring.md: core fixes SHIPPED + Outside zone correctly flagged outdoor;
      two residuals worth small fixes.'
  why: 'A1: path-alpha excluded_persons/tracked_persons_count_trusted still exclude LOST-away persons
    (diagnostic clarity only — guest gate does not read them). B1: camera-census has no room->outdoor
    filter; safe today (Patio has no camera person sensors) but latent if a camera person-detector is
    ever attached to an outdoor-zone room.'
  operator_question: 50 guest ENTRY episodes since 07-13 (1-7/day, daytime, flappy) — real summer guests
    or a daytime FP flavor? If the latter, escalate per audit §3.
  next: Fold A1+B1 into the next presence hotfix batch; await operator answer on the 50-episode pattern.
  folded_2026_08_16: A1 path-alpha diagnostic classifier folded into the PATH-ALPHA cycle as D3 (rider).
    Building now.
  live_validation_2026_08_16: 'v5.78.0 LIVE 2026-08-16. L1 PASS (0 errors), L4 PASS (face_recognized_count
    + path_alpha_gate_source live on house-state sensor). L2 PASS-on-state / attribution organic: house
    is away with all 4 persons not_home and census 0 — but the transition coincided with the deploy restart.
    STRONG retrospective evidence: family left 09:29, house stayed home_day until 11:11 (~1h42m stuck-while-empty)
    on v5.77.0 the same morning — the bug was live right up to this deploy. L3 not verifiable with an
    empty house (no populated per-person rows); L5/L6 organic. Side-effect: census source_agreement now
    both_agree (was single_source).'
- id: DP-REASON-NULL-1
  title: DP durable ledger logs reason:null on all 4,181 rows — carrier has no .reason field
  thread: energy
  status: shipped_organic
  shipped_version: v5.73.2
  approval: unreviewed
  origin:
    date: '2026-08-13'
    gist: 'Found by AUDIT_dp_live_behavior.md: _log_dp_eval_decision (energy.py:4002) reads getattr(carrier,"reason",None);
      field does not exist; real reasons live only in ~10-day recorder attrs.'
  why: Durable decision ledger is the long-horizon audit trail; null reasons make future DP forensics
    depend on recorder retention.
  next: One-line fix (log the eval snapshot decision.reason) + anchor test; fold into next URA deploy
    batch (Tier 1).
- id: ARRESTER-BOOT-BLIND-1
  title: Arrester boot-window manual blindness — manual holds predating the listener are unclassifiable
  thread: hvac
  status: parked
  approval: unreviewed
  parked_2026_08_12: 'OPERATOR: "Park #2 until another incident." Revisit trigger: next boot-coincident
    manual hold the arrester misses (same signature: zone flips to manual during boot window, setpoint
    walks within-manual, arrester overrides_today stays flat). Probe-first + Tier 2-DB when reopened.'
  origin:
    date: '2026-08-11'
    gist: 'operator: "The battery is not 97%. The arrester should be seeing this as a bad action" — up-hallway
      manual 75->71 cool during a 26->11 SOC collapse, arrester idle w/ overrides_today=0.'
  why: 'LIVE INCIDENT ~22:36-23:10: zone_2 flipped sleep->manual at 22:36:06 during the post-HA-upgrade
    boot window BEFORE the arrester listener attached (22:37:53); subsequent setpoint walks (75->71 at
    22:56) were within-manual = no classifiable transition. Battery collapsed 26->11 with the DEVICE honoring
    the WEDGED reserve=10 (not the commanded 26/27). Orchestrator manually reverted zone_2 to sleep 23:10
    (what the arrester should have done). Envoy went unavailable 23:06:45 (power-cycle? verify with operator).'
  sharp_problem: 'Gaps: (1) boot reconciliation — on listener attach, classify any zone ALREADY in manual
    as inherited-manual and start standard arrest evaluation; (2) verify _handle_climate_change classifies
    within-manual setpoint deltas (manual->manual temp change) as fresh override events.'
  related: 'Envoy reserve wedge (device=10 vs cloud=26/27) is the energy half — the write-verify self-heal
    alert was RIGHT to fire. RESOLVED 2026-08-12: operator power-cycled Enpower; all 3 reserve legs coherent
    at 10 (local number + envoy sensor + cloud HACS). Write-leg reconfirmation pending next write-verify
    cycle (URA commanding 99 peak-hold, backoff active).'
  next: 'Incident investigation: verify both gaps from source; probe-first (recorder: boot-coincident
    manual holds frequency); fix cycle Tier 2-DB.'
- id: NM-BB-IMAGE-1
  title: iMessage photo delivery unblocked — BlueBubbles v0.5/0.6 added attachment + media_url
  thread: notifications
  status: done
  shipped_version: v5.73.0
  approval: approved
  origin:
    date: '2026-08-11'
    gist: 'operator upgraded BlueBubbles to v0.6.0; release notes show send_message now takes attachment/media_url.
      Verified in installed source (__init__.py:100-165: attachment=local path w/ is_allowed_path gate,
      media_url=URL).'
  why: 'Closes SNAP-1-followup-bluebubbles-attachment: NM _send_imessage passes speculative keys (attachment_path
    / attachment-as-URL) the old integration dropped; new integration reads attachment/media_url. ~10
    LoC key rename + delete the one-shot WARN + tests. Security images then land on BOTH channels.'
  parked_alts: 'BB v0.5 also added INBOUND webhooks + phrase triggers (opt-in) — future: NM ack/silence
    via iMessage reply (safe-word flow). Not this cycle; if enabled, use sender allow-list + local-only.'
  next: 'Tier-1 build dispatched: rename keys, drop WARN, mutation-anchored tests; ride next deploy.'
  updated: 2026-08-17 23:55
  resolution_2026_08_17: 'Operator confirmed 2026-08-17: security images ARE arriving. The prune-audit
    "active fail" read was wrong — feature works. Closed.'
- id: SUITE-HYGIENE-1
  title: Kill the order-dependent flake families (sys.modules pollution) — every cycle pays a classification
    tax
  thread: quality
  status: done
  shipped_version: v5.72.0
  approval: approved
  origin:
    date: '2026-08-11'
    gist: Three consecutive cycles (ARREST-COMFORT, FAN-LAYER-1, FAN-LAYER-2 D1) each spent builder+reviewer
      effort re-classifying the same order-flakes; FAN-LAYER-2 D1 even had its own NEW test polluted on
      day one.
  why: 'Diagnosed root cause (v5.70.0 Review B / B-MED-2, deferred): test_freeze_floor.py + test_v4_6_9_hvac_intent_attrs.py
    install synthesized modules into sys.modules without snapshot/restore; collection-order shifts expose
    different victims each cycle (envoy_auto_derive, energy_restart_resilience, fan_layer_2_d1...). Also
    the pytest counter-vs-unique-names discrepancy (23 vs 14) suggests retry/dup counting worth understanding.'
  next: 'Small Tier-1/2 cycle: snapshot/restore fixtures around every sys.modules-stubbing test file (grep
    for the stub pattern, fix all instances, not just the two known); add a suite-level canary test that
    asserts sys.modules is unchanged across each file boundary if cheap. Acceptance: 3 consecutive full-suite
    runs with IDENTICAL failure name-sets.'
- id: NM-IMAGE-1
  title: NM image attachments not landing (WhatsApp + iMessage) — operator automation images DO land
  thread: notifications
  status: done
  shipped_version: v5.71.0
  approval: unreviewed
  origin:
    date: '2026-08-11'
    gist: 'operator: "The images are not landing in the NM messages even in whatsapp; the image-bearing
      ones are from my automation."'
  why: 'DIAGNOSED 2026-08-11: capture works (fresh files in /media/ura/snapshots), channel works (live
    media_path test delivered WITH image), perimeter dispatch threads snapshot_path. The drop is NM digest
    routing: operator delivery_pref=digest; digest rows do not persist snapshot_path and _deliver_digest
    calls _send_whatsapp/_send_imessage with NO snapshot args — digest pipeline is image-blind end-to-end.
    iMessage separately structural (BlueBubbles integration drops attachments; SNAP-1-followup). Adjacent
    defects same pass: 04:24 event delivered 09:26 (digest-lag timestamp semantics) + bare "[audit]" sentinel
    title at 10:01 (P24-shape).'
  design_pick_for_operator: 'Fix shape A: persist snapshot_path into digest rows + deliver images at flush.
    Fix shape B (recommended for security class): image-bearing perimeter alerts bypass digest as effectively-immediate.
    Pick rides the plan review.'
  next: Operator approves cycle -> plan (Tier 2, one adversarial plan review) -> build. Prerequisite for
    CONSOL-1 universal-llmvision (approved 2026-08-07).
- id: DP-OBSERVABILITY-1
  title: DP plan sensor presents stale eval snapshot as current (misled 2 diagnoses in one day)
  thread: energy
  status: done
  shipped_version: v5.71.0
  approval: unreviewed
  origin:
    date: '2026-08-11'
    gist: 'Found during EV-GARAGE-A-NOCHARGE-1: last_eval_at 4 days old + expired must_start_by rendered
      without staleness cues; pause_reason_human shows day-scoped reason strings that read as current
      at any hour.'
  why: 'The sensor is honest data, dishonest presentation: hold_only (resting state) + stale snapshot
    reads as "frozen/blocked". Cost: orchestrator misdiagnosed a stall; operator asked "how is that a
    sensor".'
  next: 'Small cycle: age-stamp the snapshot in attrs (eval_age_min), render must_start_by only when future,
    clarify hold_only naming/attr (state=resting vs active-pause), and consider a stale-eval WARN when
    off_peak+charging ticks pass without eval.'
- id: EV-GARAGE-A-NOCHARGE-1
  title: BMW on Garage A refuses charge overnight — vehicle-side or pilot fault; URA exonerated
  thread: energy
  status: done
  closed: '2026-08-20'
  closure_note: 'OPERATOR CLOSED 2026-08-20: "Done." Emporia excess-solar management removed; switch.garage_a
    now reports charging_rate 23 A (~5.5 kW) vs the ~6 A / 1.4 kW overnight throttle that started this
    whole investigation. URA was exonerated throughout — it behaved to spec against a physically throttled
    charger. Downstream effect worth remembering: this removal is what lifts the DP l1_only gate, which
    is why EVSE-DRAIN-PRECEDENCE-KNOB-80-1 now matters.'
  status_was: waiting_operator
  approval: unreviewed
  origin:
    date: '2026-08-11'
    gist: 'operator: "BMW on Garage A has not charged overnight." Investigated; initial DP-stall framing
      CORRECTED.'
  why: 'Recorder: URA lifted TOU pause at 21:00 off_peak, switch.garage_a ON 23:53-06:09, 41A limit —
    EVSE Connected all night, NEVER Charging, 0 kWh. L1 sockets charged fine 01:30-07:04. DP hold_only
    = resting state (paused_by_battery_drain empty), not a block.'
  sharp_problem: 'Suspects: (1) BMW in-car charge schedule/target-SOC met; (2) pilot/cable fault — six
    10s Connected->Disconnected blips overnight; (3) Garage A network degradation (Emporia unavailable-flap
    every 2-5min + Shelly overhead + Zigbee door also unavailable = AP-cluster symptom).'
  updated: 2026-08-20 14:20
  refinement_status: refined
  NEW_EVIDENCE_2026_08_20: 'SAME CHARGER, SAME CLASS, NOW QUANTIFIED — and this is the real overnight
    cost driver, not the DP. Overnight 2026-08-19/20 garage_a DID charge, but at a THROTTLED ~1.42 kW
    for 10 straight hours (5.9 A on a 240 V L2 circuit, i.e. the EVSE pinned at its ~6 A minimum). Cross-verified
    by two INDEPENDENT physical meters agreeing to ~1.5%: sensor.garage_a_power_minute_average (Emporia
    CT) 1387-1416 W at 60s cadence, and sensor.span_panel_car_charger_power (SPAN breaker CT) 1412-1440
    W at 5s cadence. The same charger read 5.80 kW at 13:45 the next day, and switch.garage_a reports
    charging_rate 23 A / max 48 A — so the hardware is capable and the sensor reports L2 power correctly
    when L2 power flows (6450/6390/5048 W hourly means on other days). CONSEQUENCE: a 1.4 kW charge cannot
    fit a 9h night, so URA''s drain-precedence machine correctly refused to sequence a battery drain around
    it (reason l1_only, every eval, all night) and evse_battery_hold pinned the battery reserve to SOC
    — battery flat at 30-31% for 6h12m while the house imported ~56 kWh of off-peak grid. URA behaved
    to spec throughout. Root cause is vehicle/EVSE-side: an amp limit or in-car charge-current setting
    for this location/schedule.'
  next: 'OPERATOR: this now covers TWO nights of the same charger misbehaving — 08-11 (Connected but never
    Charging, 0 kWh) and 08-19/20 (Charging but throttled to ~6 A for 10h). Check the BMW app AND the
    Emporia amp limit: in-car charge schedule / target SOC / charge-current setting for this location,
    plus any Emporia per-circuit amp cap. Reseat cable. Discriminating question worth answering: is the
    ~6 A a deliberate setting somewhere, or a pilot/negotiation fault degrading to minimum? ORIGINAL 08-11
    next follows. URA-side follow-ups split out: DP observability (stale last_eval snapshot presented
    as current, expired must_start_by shown) + garage-A network fix (homelab).'
  refs: switch.garage_a + evse status recorder history 2026-08-11; energy.py:4403 night-window gate; energy_drain_precedence.py
- id: FAN-LAYER-2
  CORRECTION_2026_08_23_tier_warning_is_a_BOOT_RACE_not_a_mismatch: 'THE INSTANCE BELOW IS WRONG AND THIS
    CORRECTS IT. I recorded that Living Room and Study A are "configured for HVAC-tier fan management
    but actually managed by the room tier", on the strength of a boot warning. That reading is REFUTED.
    THE WARNING NEVER VERIFIES WHAT IT REPORTS. automation.py:1892 does `if hvac_manages: return`, so
    the warning at :1922 is only reachable when `_is_hvac_managing_fans()` is False — but that helper
    (automation.py:2858-2866) returns False for FOUR different reasons: no coordinator manager, no hvac
    coordinator or hvac not enabled, no fan_controller, OR the room genuinely absent from `_room_fans`.
    Only the last is a config gap. The warning names only the last and instructs the reader to "Check
    Zone Manager zone_rooms wiring". IT FIRED 2 SECONDS AFTER BOOT (11:11:59), when the HVAC coordinator
    is very unlikely to be wired yet — i.e. almost certainly one of the first three reasons. It then latches
    `_fan_hvac_mismatch_warned = True` and NEVER re-evaluates or retracts. THE DISCRIMINATING EVIDENCE:
    at 11:42:13 hvac_fans logged "Living Room turned on during cooldown" — and that branch executes ONLY
    inside `for room_name, room_fan in self._room_fans.items()`. So Living Room IS in `_room_fans`. The
    warning was false by 11:42 and probably false when emitted. CONSEQUENCE: a diagnostic that reports
    a config defect it never tested, latched once per restart, sending its reader to fix wiring that is
    not broken. That is worse than no diagnostic. It cost this session two wrong mechanisms — mine and
    a planner''s — before being caught. THE REAL DEFECT HERE IS THE WARNING ITSELF: it must either test
    `_room_fans` membership specifically (distinguishing it from not-ready), or defer until the coordinator
    is up, or say "HVAC fan management not available (reason: X)" instead of asserting a wiring gap. The
    FanPolicyOracle fallback lines from the same boot are a SEPARATE observation and are NOT corrected
    — those name their own condition ("oracle unavailable ... check CoordinatorManager lifecycle") and
    remain valid for this card.'
  updated: 2026-08-23 11:35
  INSTANCE_2026_08_23_tier_ownership_mismatch_and_oracle_fallback: 'MEASURED FROM THE LIVE BOOT LOG after
    the v5.89.0 restart (2026-08-23). Two related observations on this card''s own surface. (A) TIER-OWNERSHIP
    MISMATCH — URA emits this about ITSELF, so it is self-reported, not inferred: 11:11:59 WARNING [universal_room_automation.automation]
    "Room Living Room expects HVAC fan management (hvac_coordination_enabled=True, climate_entity=climate.thermostat_bryant_wifi_studyb_zone_1)
    but is not in HVAC fan_controller._room_fans — room-tier is owning fans. Check Zone Manager zone_rooms
    wiring." The IDENTICAL warning fires for Room Study A at 11:12:07. So at least TWO rooms are configured
    for HVAC-tier fan management while actually being managed by the room tier. (B) FanPolicyOracle FALLBACKS
    at the same boot: 11:10:05 "FanPolicyOracle fallback: write_on served from RoomAutomation __dict__
    (oracle unavailable) for room=room:Upstairs Hallway — check CoordinatorManager lifecycle" Same for
    write_off, and again for room:Master Bath Toilet. The oracle this card exists to complete is not available
    at boot for at least two rooms and is being served from a fallback. WHY IT MATTERS BEYOND HYGIENE
    — HYPOTHESIS, NOT ESTABLISHED MECHANISM: if the manual-ON hold shipped in FAN-MANUAL-1 (v5.68.0) lives
    at one tier while the fan is actually owned by the other, that would explain BOTH the manual-ON revert
    measured on 2026-08-23 AND the fans_on sensor reading 0 while the fan ran. Both are recorded as instances
    on FAN-MANUAL-1. DO NOT TREAT THIS AS PROVEN. Four mechanisms asserted during the 2026-08-22/23 session
    were later refuted by measurement. The discriminating test is cheap: re-run the manual-ON test with
    fan_control_enabled=False as a control, and check whether the rooms named in the warning behave differently
    from a room that IS in fan_controller._room_fans. ADJACENCY SWEEP RUN before writing this (per skill
    rule 2): board grep for `_room_fans`, `zone_rooms`, `fan_controller`, `room-tier is owning`, `fans_on`
    -> ZERO existing cards. docs/BACKLOG.md fan entries are the fan-noise mmwave mitigation design, unrelated.
    PLANNING_fan_actuation_shared_layer_v2.md is THIS card''s own parent doc. Verdict: ADJACENT to FAN-LAYER-2
    (same primitive, same actuation layer) -> folded here as an instance, NOT a new card.'
  links:
    related:
    - FAN-MANUAL-1
    - FAN-LAYER-1
    context: FAN-MANUAL-1 carries the two adverse live measurements (manual-ON reverted, fans_on=0) that
      this tier-ownership mismatch is the leading hypothesis for. FAN-LAYER-1 is the shipped shared-layer
      increment this card completes.
  title: FanPolicyOracle completion — RoomFanState delegation + actuate-wrap remainder (W1-W3, W8-W10)
    + INV-FLA-T lock
  thread: fans
  status: shipped_organic
  shipped_version: v5.73.2
  approval: unreviewed
  origin:
    date: '2026-08-11'
    gist: 'Session 3 scoped-partial: builder deferred RoomFanState dataclass→property (34 sites), W1-W3/W8-W10
      actuate wraps, adjacency reverse-scan. Honest deferral, own blast radius.'
  why: State-in-one-place holds for RoomAutomation tier but HVAC-tier RoomFanState still carries its own
    hold fields; TOCTOU lock (INV-FLA-T) only covers W11/W12. Full oracle authority needs the remainder.
  next: 'After FAN-LAYER-1 increment ships + validates: plan review (Tier 2-DB), then RoomFanState conversion
    as its own cycle.'
  refs: build/fan-policy-oracle f334aeb3a session-3 report; PLANNING_fan_actuation_shared_layer_v2.md
    deferral list
- id: FAN-LAYER-1
  title: 'DOC-2 fan-actuation shared layer: REVIVED — FAN-MANUAL-1 fired 3 of its 4 park triggers'
  thread: hvac
  status: done
  shipped_version: v5.70.0
  approval: explicit
  approved_by: operator 2026-08-11 (\"Fanlayer 1 is high priority\")
  priority: high
  tags:
  - tier-3
  - institutional-context
  - context-wide-scoping
  origin:
    date: '2026-08-11'
    gist: 'operator, on the fan cycle''s 1-CRIT/6-HIGH review round: "Do we have a fan abstraction in
      our roadmap or kanban? This is why. I know we have a fusion camera abstraction and I think a presence
      sensor abstraction with intent and evidence delineated."'
  why: 'PLANNING_fan_actuation_shared_layer.md (DOC-2, 2026-08-01) parked the extraction behind a foundation
    gate + 4 evidence triggers. FAN-MANUAL-1 fired: (1) new-mechanic double-port — the manual-ON hold
    was ported room-tier + HVAC-tier and diverged same-cycle (A-MED-1 per-room knob ignored at HVAC tier);
    (2) real-world inconsistency — the Living Room complaint; (3) third-writer bypass IN THE WILD at actuator_reconciler.py:778,
    the exact line DOC-2 named; (4) piggyback — we were already open in hvac_fans.py/automation.py. Cameras
    have CameraResolver; presence has capability/role + intent/evidence; fans are the last multi-writer
    surface with no arbitration abstraction — 5 writers, and the FAN-MANUAL-1 review round (1 CRIT + 6
    HIGH, all N-way-port misses) is the drift cost DOC-2 was priced against.'
  seed_already_built: mark_fan_on_issued() (FAN-MANUAL-1 fix-up) is the first shared primitive — an authored-by
    channel across all URA ON sites. The extraction grows from it.
  gate_check_pending: DOC-2 foundation gate also requires H8 organic validation of the v5.31.0 manual-off
    cooldown (a real manual OFF observed not re-arming on the live house). Verify from ledger before build
    — if unproven, that is the one remaining gate.
  PLAN_2026_08_11: 'PLANNING_fan_actuation_shared_layer_v2.md (756 lines). Writer set is TEN sites across
    5 files, not five — W8 zone-vacancy sweep + W9 pre-arrival bypass ALL machinery (trigger #3 fired
    at TWO sites). RECOMMENDED SHAPE (b): FanPolicyOracle — thin may_turn_on/may_turn_off/ note_actuation
    predicates + unified ledger; writers keep their visible service calls but MUST consult+note. Full
    gateway (a) PARKED w/ trigger (new bypassing writer within 90d). Matches the SignalTrustLedger verdict-only
    precedent. INV-FLA: every URA fan actuation directly preceded by a consult, closed carve-outs (humidity,
    safety, kill switch). Sharpest risk: 7 readers of the two split fields must return byte-identical
    values post-delegation (Bug Class #22, the v4.6.3 shape). ~800 LoC, Tier 3. H8 PROBE RUN BY ORCHESTRATOR:
    zero organic cooldown rows in 31d — unproven and unprovable on any useful timescale; disposition (b)+(c)
    adopted (in-suite proof is the gate).'
  PLAN_REVIEW_1_2026_08_11: 'NEEDS-REVISION — TWO MORE MISSED WRITERS: C1 _stop_all_fans_safety (hvac.py:2330-2362,
    smoke/CO all-zones fan stop — legitimate but must consult w/ safety=True) and C2 hvac_predict._activate_zone_fans
    (:1038-1102, pre-arrival ON — would revive a fan the operator just turned off; must respect the manual-OFF
    cooldown). Writer set = 12 not 10 (was 5 two days ago; every enumeration wrong in the same direction
    — the oracle''s own justification). H2: the reader-enumeration seed cited 6 line numbers; real grep
    = ~34 sites — Review B would have audited a sixth of the sharpest-risk surface. H3 trigger vocabulary
    gaps; M1 adjacency check needs a mechanical procedure (AST/structured grep, not eyeballs); M3 six
    writers need NEW parity fixtures.'
  PLAN_READY_2026_08_11: 'Rev-2 committed: 12 writers (W11 safety-stop w/ safety=True always-ALLOW- but-logged;
    W12 pre-arrival ON defers under cooldown); FanDecisionSnapshot required-arg contract; INV-FLA-T temporal
    + per-room lock via oracle.actuate() context manager (atomic span structural); hard-remove field delegation
    (34-site reader enumeration real now); ONE PR/deploy; exception posture per direction; honest estimate
    1200 LoC / 45-50 tests / 3-5 days; humidity fully outside; edges-only ledger writes w/ write-volume
    regression (optimizer-flood memory cited).'
  next: 'BUILD DISPATCHED (Tier 3). Hard dep satisfied: FAN-MANUAL-1 merged at 1f5839c3a.'
  DEDUPE_2026_08_11: 'Sweep: DOC-2 planning doc is the PARENT (parked, triggers now fired -> READY per
    the skill rule). FAN-MANUAL-1 is the trigger-firing cycle, linked. ARREST-COMFORT-1 sibling class.
    THIRD instance of a parked plan''s fired trigger surfacing only on operator question (D6, ledger criterion,
    now DOC-2) — skill gains a cycle-close trigger check. NEW card, revival not duplicate.'
  organic_evidence: 'shipwatch 2026-08-11: v5.70.0 L2 no-fan-flap CONFIRMED (13.3h post-boot, all managed
    fans steady; Jaya 12.5h continuous). L3 holds + L4 safety still organic-open.'
  updated: 2026-08-17 23:30
- id: PLAN-TIER-1
  title: 'Tiered PLAN reviews: quality up front — plans reviewed before builds, like builds'
  thread: process
  status: done
  approval: explicit
  approved_by: operator 2026-08-11 ("We should start subjecting plans to a sort of tier review system.
    Quality up front in planning and in the build process")
  origin:
    date: '2026-08-11'
    gist: coined during the FAN-MANUAL-1 post-mortem — the plan missed 2 emission sites a one-line grep
      would have found, costing build + 3 reviews + CRIT fix-up
  why: 'A plan review is ~20 min; a build round is hours. Protocol now in CLAUDE.md: Tier 1 = none; Tier
    2/2-DB = one adversarial plan review (independent re-enumeration, greps not trust); Tier 3 = two framing-disjoint
    (completeness incl. parked-trigger check + adversarial build-prediction). Findings fixed IN THE PLAN
    before build dispatch.'
  first_subjects: FAN-LAYER-1 plan (Tier 3 -> 2 plan reviews) and ARREST-COMFORT-1 plan (likely Tier 3
    -> 2) — both in flight as this lands; they get the treatment on delivery.
  next: apply to the two in-flight plans on arrival; organic proof = a plan-review finding that demonstrably
    prevents a build round
  updated: 2026-08-17 23:55
  resolution_2026_08_17: 'Operator ruling 2026-08-17: "The cards that are judgement calls, resolve as
    done." Proof was a process/latent-event judgment not observable in live state; operator closed.'
- id: TABLET-FLEET-1
  title: 'Wall tablet fleet: URA integration (sensors, wake-on-occupancy, room quick-actions)'
  thread: tablets
  status: pre_planning
  approval: unreviewed
  tags:
  - institutional-context
  - measure-before-build
  - marginal-benefit
  origin:
    date: '2026-08-08'
    gist: 'operator: master tablet upgrades tested and working (sensors, lights, all over MQTT); thinking
      house-device tablet control, wake on URA room occupancy, conditional room quick-actions. NO ACTION
      YET - thoughts requested.'
  repo: ~/Code/wall-tablet (HALedController, v1.3 versionCode 5, 2026-08-01)
  verified_capabilities:
  - 'Per-room MQTT identity already fleet-safe: clientId wall-tablet-<room>, topics home/wallpanel/<room>/{led,sensors,status};
    LWT availability; self-registers via MQTT Discovery (no YAML).'
  - 'SCREEN CONTROL ALREADY EXISTS - ScreenController.kt: screenOn(), screenOff(), setBrightness(0-255),
    isScreenOn(). MQTTManager subscribes a displayCommandTopic with a JSON payload. => wake-on-occupancy
    needs ZERO tablet-side work; it is a URA publish.'
  - 'Per-room sensors already published: temperature, humidity, ILLUMINANCE (lux).'
  - BurnInPreventionManager.kt exists; headless adb provisioning of the room slug; scripts/deploy.sh does
    deterministic fleet deploy over adb.
  orchestrator_assessment:
  - HIGHEST VALUE IS THE SENSORS, NOT THE CONTROL SURFACE. Per-room lux is a first-class input URA's lighting
    logic already consumes; a tablet in every room is a lux+temp+humidity fleet arriving for free. That
    likely beats the quick-action UI on value and is far lower risk.
  - Wake-on-occupancy is cheap because the tablet side is done - URA publishes to displayCommandTopic
    on room-occupied. Because setBrightness exists, the wake can be DIM at night instead of full-blast,
    so house-state gating (never full brightness in sleep/home_night) is achievable rather than aspirational.
    Per-room opt-in required - a bedroom tablet waking at 3am is a regression, not a feature.
  - 'Room-context detection needs NO inference: each tablet is already provisioned with its room slug,
    so point its kiosk URL at a room-scoped dashboard (/ura-v8?room=<slug>). Avoid user-agent sniffing
    or a ''current tablet'' helper - both are fragile and unnecessary.'
  - 'MANUAL-VS-AUTOMATION TENSION (the important one): ''all on''/''all off'' room buttons will fight
    URA''s occupancy/arrester intent exactly like a manual thermostat change does. Per the 2026-08-08
    ruling (''manual control should be short lived... manual is wasteful; easy to re-engage''), room quick-actions
    should create a BOUNDED manual override with a visible expiry - reuse the Temp Arrester Override pattern
    (min-life + decay + notify-before-lapse) rather than inventing an indefinite hold.'
  - '''Manual night routine'': do NOT build a new routine concept. Wire it to the existing sleep house
    state via the established HOUSE_STATE_OVERRIDE path (operator already floated this). ''Manual away''
    likewise = existing away override.'
  - 'FLEET GATE (measure-before-build): v1.3''s own README says LED control was ''on-device verify pending
    first fleet deploy'' - the operator reports master tablet now working, which satisfies that gate.
    Before fleet: confirm the pre-v1.2 shared-topic migration is done (README says delete the old manually-configured
    home/ledcontroller entities in HA once) so discovery does not leave orphans.'
  next: operator thoughts/ruling; then likely sequence = (1) consume tablet lux/temp/humidity in URA,
    (2) wake-on-occupancy with night dimming + per-room opt-in, (3) room-scoped dashboard quick-actions
    as bounded overrides
- id: CIRCLING-SEVERITY-1
  title: A "circling" exterior person produced alert_count=0
  thread: perimeter
  status: shipped_organic
  shipped_version: v5.74.0
  approval: unreviewed
  tags:
  - no-fabrication-verify
  origin:
    date: '2026-08-08'
    gist: observed during v5.62.1 live validation
  why: 'Live track xt-000001-695c9e: back_yard -> front_side_ptz -> back_yard -> front_side_ptz -> back_yard,
    classification=circling, 133s, alert_count=0 at 09:22 CDT. Track linking worked correctly (one track,
    not five alerts). But CIRCLING is arguably the classification most deserving of a page, and it produced
    none. Probably correct-by-design (daytime + occupied + outside the 23:00-05:00 window) - NOT VERIFIED,
    do not assert. Needs the actual gating path traced.'
  next: trace why alert_count=0 for a circling classification; decide whether circling should escape pure
    clock-time gating
  refs:
  - exterior_track_linker.py classification
  - perimeter_alert.py alert-hours gating
  - CONSOL-1 contextual-severity ruling
  parsimony:
    problem: the most suspicious exterior behaviour may be silently unalerted outside night hours
    verdict: BUILD
    note: exactly what CONSOL-1 contextual severity is for - fold in rather than a separate cycle
- id: XCORR-1
  title: 'Burst-demotion for isolated single-camera night alerts (was: cross-engine corroboration gate)'
  thread: perimeter
  status: shipped_organic
  approval: explicit
  approved_by: operator
  approved_on: '2026-08-08'
  tags:
  - tier-2db
  - measure-before-build
  - numbers-get-knobs
  - no-fabrication-verify
  origin:
    date: '2026-08-08'
    gist: operator got 12 notifications 01:01-01:25 CDT from hot_tub; "this is what x-correlation looks
      like if we have multiple engines"
  why: 'A single engine asserting person while a CO-LOCATED engine on the same physical camera stays silent
    is strong false-positive evidence. Labelled example 2026-08-08: hot_tub frigate fired 5x in 18min;
    protect leg NEVER fired; zero adjacent cameras fired. URA correctly alerted on bad input - the input
    is the problem.'
  evidence:
  - 'hot_tub frigate _person_occupancy: 06:01:27, 06:06:10, 06:08:36, 06:10:29, 06:19:00 UTC'
  - 'hot_tub PROTECT _person_detected: zero firings in the window'
  - 'all other exterior cameras (pool_equipment, armcrest, back_yard, rear_ptz, front_side_ptz): zero'
  - 'per-camera alert counts 2 days: hot_tub 24, front_side_ptz 7, pool_equipment 5 -> hot_tub dominates'
  design_TRAP: 'DO NOT gate on corroboration generally - that would SUPPRESS REAL INTRUSIONS on single-engine
    cameras (many cameras have only ONE engine; and a real prowler may only be seen by one). The gate
    must be NARROW: only for cameras that HAVE >=2 engines (so silence from the sibling is meaningful),
    only in the deep-night window, and it must DEMOTE/delay - never silence. Aligns with the existing
    demote-never-silence invariant (INV-XP).'
  design:
  - 'REVISED: first alert ALWAYS fires at full severity (preserves intrusion guarantee).'
  - 'Demote the 2nd..Nth alert from the SAME camera within a window when ALL of: no sibling-engine corroboration,
    no adjacent-camera activity (linker adjacency), deep-night.'
  - This is the pattern that was actually diagnostic on 2026-08-08 (burst + isolation), not single-event
    corroboration.
  - 'Second half of the spam is CHANNEL FAN-OUT: 4 channels x 3 events = 12 buzzes. Demoted repeats ->
    single channel/digest (folds into CONSOL-1 contextual severity).'
  knobs:
  - name: PERIMETER_XCORR_REQUIRE_SECOND_ENGINE_NIGHT
    rung: 1
    why: enable the night corroboration gate; False = today behavior
  - name: PERIMETER_XCORR_WINDOW_S
    rung: 1
    why: how long to wait for the sibling engine before deciding
  - name: PERIMETER_XCORR_MIN_SOLO_RATIO
    rung: 1
    why: only gate cameras whose solo-firing ratio exceeds this (evidence-based, not blanket)
  parsimony:
    problem: one mis-tuned camera paged the operator 12x at 1am
    verdict: SIMPLIFY
    note: probe rejected the corroboration gate; the simple burst+isolation demotion captures the benefit
      without the false-negative risk
  next: build burst-demotion (first alert full severity, repeats demoted when isolated+uncorroborated+night);
    fold channel reduction into CONSOL-1
  refs:
  - perimeter_alert.py leg_firing_by_camera / _record_leg_fire
  - v5.59.0 disagreement telemetry
  probe_result: 'PROBE RUN 2026-08-08 (8d, 30s window) -> AUDIT_xcorr_engine_corroboration_probe.md. The
    naive corroboration gate is REJECTED: solo firing is the NORM on the exterior cameras that drive alerts
    (front_side_ptz 92% solo, back_yard 91%, pool_equipment 93%, hot_tub 77%). Gating on sibling silence
    would suppress 77-93% of exterior person detections INCLUDING REAL ONES. Engines do not share FOV/sensitivity
    per camera, so ''sibling silent'' means different things per camera - no house-wide threshold exists.
    Contrast: frigate on door cameras is near-perfectly corroborated (madrone_g6_entry 0% solo, front_door_aerial
    3%).'
  parked_alts:
  - alt: per-camera adaptive solo-rate baselining
    why: theoretically better than a global threshold
    revisit_if: burst-demotion proves insufficient after a season
  shipped_version: v5.64.0
  updated: 2026-08-23 14:30
- id: DIMMER-REBOOT-1
  title: Master bedroom Shelly Dimmer 2 reboots 89x since Aug 1 and returns ON (NOT thermal)
  thread: devices
  status: done
  approval: implied
  tags:
  - no-fabrication-verify
  origin:
    date: '2026-08-08'
    gist: 'operator: why is the master bedroom dimmer coming on in the morning?'
  why: 'light.shellydimmer2_24d7ebe93470 (area master_bedroom) reboots repeatedly: 89 `unavailable` events
    since Aug 1, accelerating 6/day -> 23/day, each ~33s (consistent = full device reboot, not a variable
    WiFi blip). 32 of those reboots came back ON — THAT is the ''dimmer comes on in the morning''. No
    context_user_id/parent on any transition => nothing in HA or URA commands it. Temperature is normal,
    so the cause is NOT thermal.'
  likely_causes:
  - Shelly power-on-default set to ON (or restore-last with stale value) -> every reboot turns the light
    on
  - WiFi/network instability causing reboots — memory records a prior house-wide Shelly/Sonoff off-WiFi
    event; this device is the only Shelly currently flapping
  - power quality or firmware
  next: DONE 2026-08-23 — operator applied debounce 150; 2 ghost-free nights confirmed. Operator-level
    device config, no URA version tag.
  CORRECTION: '2026-08-08: I FIRST REPORTED THIS AS A 117-130C FIRE RISK. THAT WAS WRONG — the sensor''s
    unit_of_measurement is degF, not degC. 116.7F = 47C; peak 129.6F = 54C. That is NORMAL for a wall
    dimmer and inside the Shelly Dimmer 2 range. NO fire risk; no breaker action needed. The device''s
    `overheating` binary_sensor never firing was CORRECT, not a failed guard. Root error: asserted a mechanism
    from an unlabeled number instead of reading the unit — violates the project''s no-fabrication/verify
    rule.'
  fix:
  - 'PRIMARY: set the Shelly power-on default to OFF so a reboot cannot turn the light on (device setting,
    operator or API)'
  - 'THEN: investigate the reboot cause (WiFi signal at that box, firmware version, power quality)'
  - 'optional: NM alert if reboot count per day exceeds a threshold — a device rebooting 23x/day is a
    real signal we currently only see by asking'
  rediagnosis_2026_08_15: 'REBOOT THEORY OVERTURNED for recent nights: device uptime 6.66 days (no reboot
    since ~Aug 8) yet uncommanded off->on 3s apart tonight 23:07 CDT (no HA context either row). Mechanism
    = PHANTOM WALL-SWITCH EDGES: btn_type=edge + 80ms debounce; electrical noise on the switch leg toggles
    the load. Aug 1-8 was real reboots (89x, default_state=last restored ON); that storm stopped. Daily
    unavailable blips (5-15/day) = connectivity drops, RSSI -56 healthy. default_state STILL =last (unfixed;
    write blocked by classifier — operator to run). Recommended order: (1) default_state=off, (2) raise
    btn_debounce 80->2000ms, (3) escalate to detached if ghosts persist.'
  operator_fix_2026_08_15: 'Operator: "Dimmer sorted." Device readback: btn_debounce 80->150ms; default_state
    STILL last, btn_type STILL edge — so the fix was partial via settings OR done elsewhere (app/physical).
    ORGANIC PROOF: two consecutive ghost-free nights (pattern was 2-of-4). If a phantom ON recurs: default_state=off
    + debounce 2000ms remain the queued escalation, then detached mode.'
  shipped_version: operator-device-config-2026-08-15
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: commit 253762297 records "operator fix applied (debounce 150) -> shipped_organic,
    proof = 2 ghost-free nights" on 2026-08-15. Not a URA version — Shelly device config fix. H1 confirmed.'
- id: BOOTSANITY-1
  title: Boot-sanity allowlist guard cannot fire on a cold boot
  thread: camera
  status: pre_planning
  approval: implied
  tags:
  - no-fabrication-verify
  - mutation-drill
  origin:
    date: '2026-08-07'
    gist: found during v5.61.0 live validation — I nearly read its silence as proof of success
  why: The F1(e) guard runs at the END of PerimeterAlertManager.async_setup() and is gated on `_linker_now`
    being present — but the linker registers AFTER that setup returns, which IS the bug. So on every cold
    boot it short-circuits and never warns. Its silence is not evidence of a successful install.
  fix: 're-run the sanity check from the READY handler AFTER the install attempt (and/or on a delayed
    post-boot check). Mutation drill: neuter the install -> the sanity WARNING must fire.'
  parsimony:
    problem: the tripwire for a CRITICAL class of bug is unreachable on the path that matters
    verdict: BUILD
    note: small; but it is the detector for a house-wide-outage bug
  next: move the check into the READY handler; pin with a test that asserts the WARNING fires when install
    fails
- id: ARREST-SUNSET-1
  title: Temp Arrester Override does not sunset on away/vacation (only sleep)
  thread: hvac
  status: shipped_organic
  approval: implied
  tags:
  - tier-2db
  - no-fabrication-verify
  origin:
    date: '2026-08-07'
    gist: operator turned Temp Arrester Override ON (master cold at home) 15:04 CDT; asked to watch the
      next boundary -> found the gap while verifying
  why: sunset_temp_arrester_override (hvac_override.py:606) hardcodes house_state == 'sleep'. Its SIBLING
    sunset_immune_holds (line ~487) correctly uses `house_state in DURABLE_HOUSE_STATES` = {sleep, away,
    vacation}. Both are invoked from the same call site (hvac.py:1908) with the same new_state. So going
    AWAY or VACATION does NOT release the override -> arrester stays suppressed while the house is EMPTY
    (wasted energy) and the toggle no longer matches reality.
  operator_requirement: '''the toggle has to flip itself off when a house state invalidates it. So the
    toggle always matches reality.'' - away/vacation invalidate a comfort override; current code honors
    only sleep.'
  fix: replace the hardcoded 'sleep' check with `house_state in DURABLE_HOUSE_STATES` (matching the sibling).
    Keep the 6h COMFORT_OVERRIDE_MAX_S decay as the other first-of. Anchor with a test per durable state
    + a mutation drill.
  parsimony:
    problem: override survives away/vacation -> arrester suppressed in an empty house
    verdict: BUILD
    note: one-predicate fix; sibling function already has the correct form
  next: fold into the SECC-1 build batch; Tier 2-DB (HVAC governance)
  refs:
  - domain_coordinators/hvac_override.py:584-624
  - domain_coordinators/hvac_const.py:206
  - domain_coordinators/hvac.py:1908
  refinement:
  - challenge: 'operator: "Home evening should also cancel it. Its a durable house state"'
    sharpened: verified home_evening IS in the canonical vocabulary and DURABLE_HOUSE_STATES has only
      ONE consumer (contained ripple) -> widened the allowlist
  - challenge: 'operator: "basically Arriving, and Guest are the things that should leave it alone"'
    sharpened: INVERTED allowlist->denylist. ARRESTER_HOLD_PRESERVING_STATES={arriving,guest}; everything
      else invalidates. Omission was the original bug, so enumerate only the 2 exceptions, not the 7 cancellers.
  - challenge: 'I warned home_evening cancelling might recreate the cold master right as he settles in;
      operator: "No. Predictability is better than guessing here"'
    sharpened: 'DESIGN PRINCIPLE: a temporary override releases on ANY real context change - one rule
      you can hold in your head. Rejected the heuristic that guesses when the operator still wants it.
      Do NOT re-complicate this with per-state special cases.'
  - challenge: 'operator: "Why not add home_night?"'
    sharpened: 'moot under the inversion - home_night is not in the preserve set so it already cancels.
      My earlier "do not add" was allowlist-era conservatism. Demonstrates the denylist virtue: no enumeration
      of cancellers = no omission risk.'
  - challenge: 'operator: "I think waking may not be durable. When is it?"'
    sharpened: 'VERIFIED from house_state.py: waking is the morning twin of arriving - reachable ONLY
      from SLEEP, exits only to HOME_DAY/AWAY, 60s hysteresis (same transient tier as arriving 60s; vs
      home_* 120s, guest 300s, sleep 600s, vacation 7200s). Preserve set -> {arriving, guest, waking}.'
  - challenge: verifying waking exposed that preserving transient states is DECORATIVE - an override set
      during waking/arriving dies ~60-120s later when the house settles into home_day (which invalidates)
    sharpened: 'added ARRESTER_OVERRIDE_MIN_LIFE_S=900 (15min) minimum-life grace. One-sentence contract:
      "flipping it on always buys at least 15 minutes; after that any real context change ends it." Solves
      the class without per-state cleverness (which the operator rejected). Grace never outranks the 6h
      max-age; 0 disables.'
  - challenge: 'operator: "if the house state changes before 15, when that elapses, it recognizes it should
      change immediately"'
    sharpened: 'CAUGHT A BUG IN MY OWN SPEC: the grace as written SUPPRESSED an event-driven sunset =
      DISCARDED it (nothing re-fires) -> override would silently survive to the 6h max-age. Grace must
      DEFER (record pending obligation + one-shot timer at expiry + periodic-sweep backstop), not cancel.
      Dropped-obligation class.'
  - challenge: my own design check on the discharge mechanism
    sharpened: discharge must NOT be "re-evaluate current state at expiry" - engaging during home_day
      with NO transition would then kill EVERY override at exactly 15min. Must stay TRANSITION-triggered;
      pinned by a dedicated regression-guard test.
  bug_precise: hvac_override.py:603 `if reason == 'durable_state' and house_state == 'sleep'` (INLINE
    LITERAL) vs its sibling hvac_override.py:487 `house_state in DURABLE_HOUSE_STATES` (SHARED CONSTANT).
    Both invoked from the SAME call site hvac.py:1908 with the SAME new_state, answering the SAME policy
    question. They diverge; nothing forces agreement.
  bug_class: 'DIVERGENT DUPLICATE PREDICATE (policy fork) - one policy expressed in two places, one drifts.
    THIRD instance in 2 days: v5.59.0 CRITICAL (resolver learned _smart_motion_human, dedup stripper kept
    its own narrower tuple) and SNAP-1 (media_url vs media_path). Recurring skeleton: a second site RE-IMPLEMENTS
    a rule instead of CONSUMING it.'
  guard:
  - '1. Policy exists ONCE: house_state_invalidates_arrester_hold() called by both sites.'
  - '2. Anti-fork source test: no `house_state == "` literal in hvac_override.py - the fork becomes unwritable,
    not merely absent.'
  - 3. Table-driven test parametrized FROM HOUSE_STATE_TRIGGER_VALUES over all 9 states for BOTH functions
    -> adding a 10th state FAILS until classified. Guards against reintroduction-by-omission, which is
    exactly how this bug appeared.
  - '4. Fail-safe default: unclassified state INVALIDATES (arrester regains governance) rather than holding
    a suppression forever.'
  knobs:
  - name: ARRESTER_HOLD_PRESERVING_STATES
    rung: 1
    why: '{arriving, guest, waking} - the only states that do NOT invalidate; everything else does (fail-safe
      default)'
  - name: ARRESTER_OVERRIDE_MIN_LIFE_S
    rung: 1
    why: 900 (15min) minimum life; 0 disables the grace
  - name: COMFORT_OVERRIDE_MAX_S
    rung: 1
    why: existing 6h decay - the other first-of; min-life must never outrank it
  known_limitations:
  - restart mid-grace may lose the in-memory pending-sunset obligation unless persisted - builder instructed
    to persist or explicitly document + report
  organic_open: engage the override, then confirm it releases on the next real context change (or 6h decay)
    and the switch flips OFF to match
  shipped_version: v5.61.0
  updated: 2026-08-23 14:30
- id: CONSOL-1
  title: Perimeter consolidation cycle
  thread: perimeter
  status: shipped_organic
  shipped_version: v5.73.0
  approval: explicit
  approved_by: operator
  approved_on: 2026-08-07
  tags:
  - tier-2db
  - institutional-context
  - audit-first
  origin:
    date: 2026-08-07
    gist: retire redundant manager surface; I need to weigh in — usability
  why: three parallel alerting stacks (URA NM, HA doorbell automation, zone_monitoring pagers) duplicate
    delivery
  rulings:
  - Option C surfacing (= A enhanced)
  - universal llmvision on ALL camera alerting
  - contextual severity replaces the day/night window (vehicle deep-night window retained)
  - perimeter stays a specific top-level config step; Security home only when a 2nd security surface joins
    the menu
  constraints:
  - doorbell automation carries daytime front-door coverage + llmvision + vehicle/animal classes — migrate-then-retire,
    not delete
  parsimony:
    problem: 3 stacks page the same event with no shared cooldown/routing
    verdict: BUILD
  next: fold SNAP-1 + TEST-1/2 in; Tier 2-DB
  refs:
  - PLANNING_perimeter_consolidation.md
  - AUDIT_ha_side_alerting_reconciliation.md
  plan_state: 'rev-2 PLAN-READY (1 adversarial review: 3 CRIT + 4 HIGH fixed in-plan incl. No-Soak violation
    + G4/G6 misname + vehicle-window orphan). D0 probe: doorbell llmvision SILENTLY BROKEN since 02-13
    (gpt-5-mini reasoning eats 300 tokens); build dispatched 2026-08-11 eve; D0.4 doorbell hotfix OPERATOR-GATED.'
- id: SNAP-1
  title: Snapshot mirror-and-improve
  thread: perimeter
  status: done
  approval: explicit
  approved_by: operator
  approved_on: 2026-08-07
  tags:
  - tier-2db
  - numbers-get-knobs
  - no-fabrication-verify
  origin:
    date: 2026-08-07
    gist: still no images -> Mirror and improve -> does it cleanup? -> I approve the purge
  why: URA sends media_url (URL fetch) so images drop; any live grab is stale
  design:
  - mirror = snapshot to local file, attach as file to every channel (media_path / attachment / image)
  - at-detection frame tiered - Frigate event-snapshot > Protect thumb > native rising-edge
  - retention/cleanup + privacy - write OUTSIDE web-served /config/www
  parked_alts:
  - alt: continuous pre-roll frame buffer
    why: exact-timestamp even for native
    revisit_if: rising-edge frames look late for fast walkers
  refinement:
  - challenge: my read external_url unset
    sharpened: find how I do WhatsApp -> media_path local file; root cause is media_url-vs-media_path
  - challenge: just mirror the automation
    sharpened: shot from when it fired -> mirror insufficient (camera.snapshot is a live grab too); at-detection
      event frame
  - challenge: does it cleanup?
    sharpened: retention + privacy (out of web-served www)
  - challenge: one snapshot, multiple integrations - which?
    sharpened: at-detection engine precedence, not identity order
  - challenge: operator - whatever is best practice / 1 week / your call
    sharpened: verified /media/llmvision/snapshots is the existing HA convention -> /media/ura/snapshots
      (auth-gated, not web-served); age-primary 168h; SHIP STANDALONE FIRST since SNAP-1 is the prerequisite
      for CONSOL-1 universal-llmvision
  knobs:
  - name: PERIMETER_SNAPSHOT_ENGINE_PRECEDENCE
    rung: 1
    why: Frigate event-snapshot > Protect thumb > native rising-edge (at-detection fidelity)
  - name: PERIMETER_SNAPSHOT_RETENTION_AGE_H
    rung: 1
    why: 168 (1 week) - age-primary per operator
  - name: PERIMETER_SNAPSHOT_RETENTION_COUNT
    rung: 1
    why: generous runaway backstop only, not routine pruning
  - name: PERIMETER_SNAPSHOT_DIR
    rung: 1
    why: /media/ura/snapshots - auth-gated, matches llmvision convention
  parsimony:
    problem: perimeter alerts arrive with no photo / a stale photo
    verdict: BUILD
  next: DONE 2026-08-23 — at-detection snapshots confirmed live (73 files). Shipped v5.63.0.
  RECONCILED_2026_08_09: status review -> shipped_organic; board had said "ready to build" for shipped
    work
  refs:
  - perimeter_alert.py
  - domain_coordinators/notification_manager.py
  decisions:
    snapshot_dir: '/media/ura/snapshots — operator: ''whatever is best practice''. VERIFIED convention:
      llmvision already uses /media/llmvision/snapshots. /media is HA''s auth-gated media dir (media browser),
      NOT the anonymous web-served /local//config/www — fixes the privacy exposure. Proven path for llmvision
      image_file + whatsapp media_path.'
    retention: 'operator: ''plenty of space, 1 week'' -> AGE-PRIMARY 168h (PERIMETER_SNAPSHOT_RETENTION_AGE_H=168).
      Count cap raised to a generous backstop only (guards runaway, not routine). 322GB free on /share
      verified.'
    sequencing: 'MY CALL: ship SNAP-1 STANDALONE FIRST, before CONSOL-1. Reasons: (1) it fixes a LIVE
      user-visible defect (alerts with no photo) - CONSOL-1 is a larger retirement cycle that would delay
      it; (2) SNAP-1 is a PREREQUISITE for CONSOL-1''s universal-llmvision ruling - llmvision needs a
      local image_file, which is exactly what SNAP-1 produces, so building it first DE-RISKS CONSOL-1
      rather than duplicating work; (3) folding in saves no meaningful review cost (both Tier 2-DB on
      perimeter_alert+NM).'
  build: build/snap-at-detection @ 7e28a2ea4 — 15 new tests, 6 detach-the-value drills all RED, gate 21/8405
    name-diff 0
  verification_results:
  - 'VERIFIED: frigate2 instance-scoped snapshot URL — instance id = MQTT client_id from hass.data[''frigate''][entry_id][''config''][''mqtt''][''client_id''];
    discovery tries each instance. Live: Frigate 1 (192.168.13.16:8971) + Frigate 2 (192.168.13.18:8971).
    CLOSES FRIG2SNAP-1.'
  - 'VERIFIED (real constraint): installed BlueBubbles integration exposes NO attachment field — bluebubbles/__init__.py:49-90
    POSTs only {addresses,message,method} to /api/v1/chat/new. iMessage PHOTO DELIVERY IS NOT POSSIBLE
    this cycle. WhatsApp media_path works. Acceptance criterion ''photo on BOTH WhatsApp AND iMessage''
    must be amended to WhatsApp-only.'
  - 'RUNTIME-ASSERTED: /media allowed-path checked via hass.config.is_allowed_path at setup; failure auto-engages
    the kill switch.'
  - 'WRONG — CORRECTED BY ORCHESTRATOR: builder concluded ''UniFi Protect NOT installed'' from absence
    in /config/custom_components. Protect is a CORE HA integration (not a custom component). ha_get_integration
    confirms domain=unifiprotect, title=''Phalanax NVR Pro'', state=LOADED. The Protect-thumb precedence
    tier was therefore skipped on a false premise and must be revisited.'
  followups:
  - 'SNAP-1-followup-protect-thumb — REOPENED: Protect IS installed (core integration); verify the smart-detect
    thumbnail API against HA core unifiprotect and implement the middle precedence tier'
  - 'SNAP-1-followup-bluebubbles-attachment — GENUINE gap: needs BB integration attachment support or
    a direct BB server-API upload path'
  - SNAP-1-followup-sensor — formal capture sensor deferred; observability via structured log + _last_snapshot_capture
  shipped_version: v5.63.0
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: next-field evidence from 2026-08-09 — 73 snapshots live in /media/ura/snapshots,
    none in Frigate backup; at-detection local-file snapshots shipping. git tag v5.63.0. H1 confirmed.'
- id: TEST-1
  title: Boot-time shadow diff (legacy vs resolver leg set)
  thread: resolver
  status: pre_planning
  approval: implied
  tags:
  - mutation-drill
  origin:
    date: 2026-08-07
    gist: we took hardened surface and gave it new methods; something is bound to fail
  why: live tripwire for silent coverage shrinkage that unit tests miss
  parsimony:
    problem: a camera's leg set silently shrank vs the retired helpers
    verdict: BUILD
    note: ~30 LoC
    fold into CONSOL-1: null
  next: WARN if a camera's new leg set doesn't superset legacy base+_2
- id: TEST-2
  title: '"Send Test Perimeter Alert" button'
  thread: perimeter
  status: done
  approval: implied
  tags:
  - numbers-get-knobs
  origin:
    date: 2026-08-07
    gist: same push as TEST-1
  why: delivery crosses into 3rd-party services; only a live end-to-end send proves it
  parsimony:
    problem: no way to prove channel delivery without waiting for a real intrusion
    verdict: BUILD
    note: would have caught the media_url bug instantly
  next: button entity -> canned snapshot through all 4 channels
  verified_shipped_2026_08_16: 'VERIFIED SHIPPED: SendTestPerimeterAlertButton present in button.py (2
    refs) and exercised live this week. Card was stale.'
- id: TRANSIT-1
  title: Interior traversal — Protect-sourced checkpoints via resolver
  thread: presence
  status: shipped_organic
  batch: resolver-correctness
  seq: 3
  approval: explicit
  tags:
  - institutional-context
  - tier-2db
  - hand-build-fixture
  - numbers-get-knobs
  plan: docs/planning/PLANNING_transit_protect_sourced_checkpoints.md
  progress:
  - '2026-08-07: INTERIM - all 5 checkpoints now wired in camera_person_entities (operator added upstairs_hall
    + stairs_top via Camera Census UI; count 9->11; both area-map correctly). NOTE stairs uses Frigate
    F2 entity (stairs_top_2) not Protect - sunset-exposed but TRANSIT-1 makes moot.'
  - '2026-08-07: PLAN written (Tier 2-DB). enumerate_platform_cameras(''unifiprotect'',''person'') ->
    collapse F1/F2 via resolve_detection_legs -> attribute by area -> transit/egress union with hand-list,
    filter to CONF_TRANSIT_CHECKPOINT_AREAS, kill switch CONF_TRANSIT_PROTECT_SOURCED_ENABLED. Drift-proof
    acceptance test.'
  - '2026-08-07: BUILT (commit 4ee912805, 7 tests, suite baseline-clean). 3 reviews dispatched.'
  - '2026-08-07: SHIPPED v5.60.0 + LIVE VALIDATED. All 5 checkpoint areas populated (protect_sourced=13/24
    subscribed); F5 registry self-heal PROVEN live (touched a Protect entity -> debounced rebuild re-emitted
    inventory 14:44:05). Reviews found 1 CRIT + 8 HIGH, all fixed; orchestrator re-ran the 2 load-bearing
    drills independently (both RED, restored clean).'
  review_findings:
  - 'A-CRIT-1 (Review A): Protect-sourced entities are subscribed + sightings recorded, but validate_transition
    filters via _get_shared_space_cameras() = hand-list ONLY -> the superset coverage is recorded then
    DISCARDED at the decision point. Cycle value not delivered. Test aimed at subscription set, not validate_transition
    (sham). FIX: union _get_shared_space_cameras (and the L416 activity check) with the Protect-sourced
    set under the same kill switch.'
  - 'A-HIGH-1 (Review A): enumerate_platform_cameras groups by device_id; Protect NVR devices host MULTIPLE
    physical cameras (staircase+garagehallway) -> collapse to 1 row/1 area -> wrong attribution / lost
    coverage. Existing _scan_device_entities uses stem_filter; enumerate doesn''t. FIX: group by (device_id,
    stem) or emit one row per (device_id, area).'
  - 'A-HIGH-2 (Review A): cross-leg area fallback can import a FOREIGN camera''s area (correlation ladder
    pulls siblings from a different physical camera). FIX: restrict fallback to same-integration legs
    / require primary area.'
  - 'A-MED 1-4, A-LOW 1-3: scalar checkpoint_areas -> per-char tuple; egress adopts protect entities regardless
    of role; kill-switch not byte-identical (attr always created); double registry walk; by_area double-count.'
  - 'B-H1 (Review B): DOUBLE-FIRE per physical camera - union subscribes BOTH the Protect and Frigate
    leg of the same camera; set() dedups by entity_id not physical camera -> 2 sightings per crossing
    -> _correlate_sighting_to_transition n_sightings>=n_transitions satisfied on 1 camera''s evidence
    -> inflates path_plausible/path_validated -> wrong presence trust. FIX: collapse sightings by device_id
    at record time (or keep 1 leg per physical camera at subscribe).'
  - 'B-H2: egress interior-side has NO stem dedup; Protect<->native slug divergence not bridged (contingent
    on future egress extension).'
  - 'B-H3: enumeration is one-shot at async_init, NO self-heal - if Protect not loaded yet at init, freezes
    empty until full restart -> silently reverts to hand-list. FIX: listen EVENT_ENTITY_REGISTRY_UPDATED(platform=unifiprotect)
    rebuild+resubscribe, or delayed rescan.'
  - 'B-H4: kill switch + checkpoint-areas knob latent unless reload-allowlisted (ties to RELOAD-WATCHDOG-HAZARD).
    FIX: signal-driven teardown+re-init on options change, no parent reload.'
  - 'CONVERGENCE: A-CRIT-1 == B-L1 (validate_transition filters via _get_shared_space_cameras hand-list
    only). Consolidated fix-up: (1) union the sighting-filter with Protect set; (2) collapse by device_id
    (H1); (3) registry-update self-heal (H3); (4) signal-driven kill/knob (H4); (5) NVR multi-cam per-area
    rows (A-H1); (6) same-integration area fallback (A-H2). Await Review C (test authority) before fix-up.'
  - 'Review C (test authority): 3 of 6 mutation drills stayed GREEN - kill-switch, checkpoint-area filter,
    and enumerator package/disabled exclusion are UNTESTED. kill-switch test is a pure tautology (set
    arithmetic, never calls production). ENTIRE transit_validator.py diff untested (C-H4). Confirms double-fire
    (C-M2) + empty-tuple checkpoint_areas silently reverts to defaults (adversarial #3).'
  - 'CONSOLIDATED FIX-UP (all 3 reviews): (1) union sighting-filter _get_shared_space_cameras + activity-check
    with Protect set [A-CRIT-1/B-L1 - THE core value]; (2) collapse sightings by device_id at record time
    [B-H1/C-M2 double-fire->presence inflation]; (3) enumerate one row per physical camera not per device
    (NVR multi-cam) [A-H1]; (4) same-integration area fallback [A-H2]; (5) registry-update self-heal [B-H3];
    (6) signal-driven kill/knob no parent-reload [B-H4]; (7) empty-tuple=kill via is-not-None + isinstance
    guard [C adv#3/A-MED-1]; (8) egress idempotency [C-M1]; (9) REAL behavioral tests driving _protect_sourced_checkpoint_entities
    + TransitValidator.async_init - every mutation drill must go red [C-H1..H4].'
  origin:
    date: 2026-08-07
    gist: we built exterior tracking inspired by interior census/known-persons traversal - find it; can
      resolver improve it
  why: transit_validator checkpoints fire from ~one integration; multi-engine legs = denser/earlier checkpoints
    = more path_confirmed
  refinement:
  - challenge: I said path tracking is exterior-only
    sharpened: we built it inspired by interior traversal - find it -> located transit_validator; already
      half-uses census cross-platform; framing corrected
  - challenge: operator - the quickest most trustworthy camera inventory is Protect; it sees every camera
      reliably
    sharpened: TRANSIT-1 should SOURCE its checkpoint inventory from Protect (authoritative), not the
      hand-maintained CONF_CAMERA_PERSON_ENTITIES list - which is exactly why upstairs_hallway + stairs
      silently drifted out of coverage
  findings:
  - 'OPERATOR: it''s 5 cameras. By the real bar (produces a room-attributed signal transit can use) only
    garage_hallway works. master_hallway + entry(foyer) are in camera_person_entities but have NO fused
    sensor; upstairs_hallway + stairs aren''t in the list at all. All 5 need it.'
  - 'Root cause: camera_person_entities is a hand-maintained list (drift). Protect sees every camera reliably
    (operator) -> source checkpoints from Protect + resolver area-attribution (areas now fixed) covers
    all 5 at once and is drift-proof.'
  parsimony:
    problem: 4 of 5 traversal checkpoints produce no usable room signal; hand-list drifts
    verdict: BUILD
    note: Protect-sourced resolver inventory fixes all 5 + prevents recurrence - the area work was the
      prerequisite
  next: build - resolver enumerates checkpoint cameras from Protect, attributes each by area, transit
    consumes that instead of camera_person_entities
  refs:
  - transit_validator.py
  - config_flow.py async_step_camera_census
  organic_open: one logical sighting per real crossing (F2 dedup, despite Protect+Frigate legs) + no path_validated
    inflation vs prior day
  followups:
  - expose checkpoint_cameras_by_area on a diagnostic sensor (validation needed log-level surgery - build
    scoped it out)
  - 'residue: icon mdi:stairs left on binary_sensor.stairs_top_person_detected from the registry-touch
    test (cosmetic)'
  shipped_version: not-shipped
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: build not shipped (next says "build - resolver enumerates checkpoint cameras").
    shipped_version recovered as not-shipped. UN-WATCHABLE.'
- id: FRIG2SNAP-1
  title: frigate2 instance-id snapshot URL
  thread: camera
  status: done
  approval: implied
  tags:
  - no-fabrication-verify
  origin:
    date: 2026-08-07
    gist: found mid-investigation
  why: endpoint is instance-scoped; URA builds only default shape -> frigate2-hosted cameras can't resolve
    a snapshot at all (latent since prefix-split)
  parsimony:
    problem: any camera on the 2nd Frigate host has never had a snapshot
    verdict: BUILD
    note: subsumed by SNAP-1 event-download
  next: fold into SNAP-1
  verified_shipped_2026_08_16: 'VERIFIED SHIPPED: instance-scoped snapshot machinery live in perimeter_alert.py
    (_discover_frigate_instance_ids / frigate-f2 refs) and validated during the F1 retirement. Card was
    stale.'
- id: KP-ESCALATE-1
  title: Known-person / face-alert path (no URA successor)
  thread: security
  status: done
  approval: blocked
  tags:
  - institutional-context
  - audit-first
  origin:
    date: 2026-08-07
    gist: discovered via purged Frigate_KnownPerson_* files + AUDIT rec 5
  why: face-recognition paging has no URA successor; lost when the doorbell automation retires unless
    built into perimeter NM
  parsimony:
    problem: retiring the doorbell automation silently drops face-alert paging
    verdict: BUILD
    note: gates CONSOL-1's doorbell retirement - do not retire until this exists
  refs:
  - PLANNING_exterior_person_escalation.md
  direction_2026_08_14: 'Operator agreed: exterior alerts today have ZERO member recognition (verified
    — perimeter_alert consults no face data). v1 direction = ANNOTATE not suppress ("Person detected —
    likely Oji") — preserves alert, kills operator cost; per-person opt-in suppression is phase 2 after
    annotation proves accurate. Fold the face-alert successor (this card) + member-annotation into ONE
    known-person pipeline plan.'
  operator_answers_2026_08_14: 'P1 privacy: LOCAL SOURCES ONLY (Frigate-2 + UniFi Protect face; llmvision
    EXCLUDED from identity — no household reference photos leave LAN). D3: FOLD IN NOW (stranger-alert
    / unknown-face leg builds in the same cycle as member-annotation — no coverage gap when doorbell automation
    retires). Timing: IN-FIRST-MESSAGE ONLY (annotate iff identity ready within budget; no follow-up-edit
    rare-fire path in v1). Still pending pre-D0: tracked_persons currency audit + message-format confirm
    (default: "Person detected — likely {names}." on its own line).'
  disposition_applied_2026_08_14: 'OPERATOR DECLINED via board button 2026-08-13T03:20 (queue apply was
    MISSED for ~1 day — session-start disposition check skipped across overnight passes; corrected now).
    Reconciled NOT relitigated: declined AS A STANDALONE card; its scope (unknown-face/stranger alert
    successor to the retiring doorbell automation) was ABSORBED into PLANNING_known_person_annotation.md
    per the 08-14 fold-in-now decision. This card is closed; the work lives in the known-person cycle.'
- id: RELOAD-WATCHDOG-HAZARD
  title: URA parent-entry reload cascades → event-loop stall → watchdog (~5min outage)
  thread: lifecycle
  status: shipped_organic
  shipped_version: v5.77.0
  approval: explicit
  approved_by: operator
  approved_on: 2026-08-07
  autonomy: overnight-agentic
  tags:
  - tier-2db
  - no-fabrication-verify
  origin:
    date: 2026-08-07
    gist: options-flow submit (camera_person_entities) reloaded the URA parent entry and blipped HA ->
      diagnose and fix this autonomously tonight
  why: routine options saves (Camera Census etc.) reload the integration/parent entry, which cascades
    to all ~40 room + coordinator entries synchronously, stalling the event loop until the supervisor
    watchdog restarts core (~5min outage). A config save must not cause an outage.
  diagnosis:
  - 'CONFIRMED (2026-08-07): _async_update_listener (__init__.py:5984) - for the INTEGRATION entry, if
    changed_keys NOT subset of OPTIONS_RELOAD_SUPPRESS_KEYS -> hass.config_entries.async_reload(entry.entry_id).
    Reloading the INTEGRATION (parent) entry cascades to ~40 room + coordinator entries -> event-loop
    stall -> supervisor watchdog restart (~5min).'
  - camera_person_entities / egress_cameras / perimeter_cameras are NOT in the suppress allowlist -> Camera
    Census save takes the full-reload path. This is the blip.
  - 'Consumers: camera_census reads the list LIVE (_get_integration_camera_list each call -> safe to suppress).
    transit_validator SUBSCRIBES at async_init (cached -> needs a re-subscribe signal, not a full reload).'
  fix:
  - Add Camera Census keys to an INTEGRATION-entry suppress set (mirror the CM/ROOM reload-suppression).
    Persistence already done by async_update_entry.
  - Dispatch a lightweight SIGNAL_CAMERA_LIST_CHANGED that transit_validator (and any cached consumer)
    subscribes to -> re-subscribe listeners in-place, no full integration reload.
  - Kill switch / rung-1 constant for the suppress set. Verify each suppressed key's consumers refresh
    (live-read or signal) before adding - do NOT suppress a key whose consumer caches with no refresh
    path.
  parsimony:
    problem: a routine config save causes a ~5min house outage
    verdict: BUILD
    note: safety/usability - config changes must be non-disruptive; fix mirrors existing CM/ROOM suppression
      pattern
  next: (tonight) build - INTEGRATION suppress set + SIGNAL_CAMERA_LIST_CHANGED re-subscribe path; Tier
    2-DB (lifecycle + presence)
  refs:
  - __init__.py:5984 _async_update_listener
  - OPTIONS_RELOAD_SUPPRESS_KEYS
  - transit_validator.py async_init
  - feedback_parent_entry_reload_watchdog_hazard memory
  planned_2026_08_15: 'Overnight pass: PLANNING_reload_watchdog_hazard.md written+committed. Central finding:
    v4.7.26 suppress branch is gated entry_type==COORDINATOR_MANAGER (__init__.py:6431); camera keys migrated
    to the INTEGRATION entry in v3.4.5 have NO branch -> fall through to full reload (:6502) cascading
    to ~40 children. Fix = mirror the shipped pattern to ENTRY_TYPE_INTEGRATION + wire the inert SIGNAL_URA_TRANSIT_CONFIG_CHANGED.
    Tier 2-DB. Next: adversarial plan review, then daytime build (custom_components off-limits overnight).'
  live_validation_2026_08_15: 'v5.77.0 LIVE: L1 PASS; L4 organic (next integration-entry save proves zero-reload
    + dispatch).'
- id: KHOST-1
  title: Homelab-hosted board, generated from data
  thread: dashboarding
  status: done
  approval: explicit
  approved_by: operator
  approved_on: 2026-08-07
  autonomy: overnight-agentic
  tags:
  - hand-build-fixture
  origin:
    date: 2026-08-07
    gist: make url live on webhost (homelab)... design it better... give yourself eyes like playwright...
      build it tonight while I'm sleeping
  why: the Artifact is hand-maintained HTML that can drift; a GENERATED board (pure function of this data)
    can't; homelab-hosted = durable, infra-native
  design:
  - source = this data file; generator -> {KANBAN.md view, html board, history}; page is a pure function
    of the data
  - Playwright (chromium installed) for self-iterated visual design - fix the column-balance gap seen
    in the interim Artifact
  - done cards age out to kanban.history.yaml
  decisions:
    host: urakanban.phalanxmadrone.com
    trigger: rebuild on every commit (self-hosted homelab = cheap; amortized compute/electricity/inet)
  constraints:
  - generated page must be a pure function of the data (no hand-editing output)
  - homelab deploy (DNS/reverse-proxy/TLS for the subdomain) may need operator hardware/DNS access - investigate;
    automate if wildcard-DNS + auto-TLS caddy allow, else stage a one-step for operator
  parsimony:
    problem: the reflected board is hand-maintained and can silently drift from the source
    verdict: BUILD
    note: eliminates the drift anti-pattern + gives a design surface
  next: DONE 2026-08-23 — homelab-hosted board confirmed live. No URA version tag (tooling).
  SHIPPED_2026_08_10: 'LIVE at https://urakanban.phalanxmadrone.com (verified HTTP 200 serving URA://KANBAN;
    Mac DNS cache may lag the new UDM record a few minutes). 5-min refresh cron installed on the dev-host
    crontab. Homelab commit eddf8e4. REDESIGNED same morning per operator ("don''t want generic slop"):
    ops-ledger identity — swimlane rows (no dead voids), approval as left stripe replacing pill soup,
    mono statusline, single amber accent, paper/gunmetal themes. Three real infra bugs found+fixed in
    deploy-static-site.sh during first apply: PVE SSH user must strip the @pam realm; PVE password is
    PROXMOX_PASS not SSH_PASS; standalone caddy validate via pct exec lacks the service EnvironmentFile
    so systemd reload is the validation. ORGANIC PROOF OPEN: (a) cron refresh picks up the next board
    commit within ~5 min; (b) rung-3 STALE banner appears on the hosted page the next time the board genuinely
    lags a release.'
  overnight_notes_2026_08_10:
  - 'STALE-BASE CLASS, SECOND INSTANCE, new variant: builder verified base against origin/develop but
    LOCAL develop was ahead (unpushed evening work), so its generated views rendered from an old board
    file. Caught at merge; views regenerated from current data. The skill rule says compare against develop
    — it must say LOCAL develop explicitly.'
  - 'SELF-REFERENTIAL DATA broke a naive oracle: test asserted "STALE" not-in output, but this very card''s
    prose describes the STALE banner. Fixed to assert the banner MARKER string; drill re-run proves the
    tightened oracle still load-bearing (2 named reds on neutered check).'
  ADDED_2026_08_09_staleness_forcing_function: 'The generator MUST compare meta.last_reconciled against
    the newest git tag AND the newest docs/readmes/README_v*.md, and (a) render a loud STALE banner on
    the board, (b) warn on the build. WHY: 2026-08-09 the board said "build" for XCORR-1 and SNAP-1 which
    had already shipped (v5.64.0, v5.63.0); meta.last_reconciled correctly read 2026-08-07 and nothing
    surfaced it. Root cause was NOT a missing rule — the skill already carries three hooks covering this.
    It is that board reconciliation is the ONLY step in the deploy ritual with no forcing function (deploy.sh
    refuses without tests/README; nothing refuses without a board update), so it is the only step running
    on willpower. This converts an existing tripwire from remembered-comparison into announced-state.
    Operator ruling 2026-08-09: do NOT add a new rule.'
  shipped_version: board-tooling-2026-08-10
  updated: 2026-08-23 14:30
  organic_evidence:
  - '2026-08-23 watch-pass: board live at urakanban.phalanxmadrone.com; generator merged cc9c0e3f8+3031487c0
    on 2026-08-10; data-driven views confirmed. H1 confirmed.'
- id: CAM-AREA-PENDING
  title: Camera area corrections — RESOLVED
  thread: camera
  status: done
  approval: explicit
  origin:
    date: 2026-08-07
    gist: found during the exterior+interior camera area-id correction sweep
  resolved:
  - 'Madrone G6 Entry -> front_porch (operator: front porch/entry; sits with front_door_aerial door overhead).
    DONE.'
  - 'Kitchen Pantry -> operator: NO camera here; the D3 fused sensor is an ORPHAN (no backing camera).
    Leave roomless; candidate for removal.'
  - 'Typos guest_bedroom_1_clo_set / guest_bedoom_2_bath -> operator: NO cameras; general registry hygiene,
    out of camera scope.'
  refs:
  - https://claude.ai/code/artifact/ef6dc227-8488-4b59-b745-f71e946da6a8
  updated: 2026-08-17 23:30
- id: D3-AREA-INHERIT
  INSTANCE_2026_08_23_fans_on_sensor_structurally_blind: 'SAME CLASS AS THIS CARD, found while investigating
    FAN-MANUAL-1. `sensor.living_room_fans_on` read 0 for the entire 10 minutes a managed fan was physically
    ON. Root cause is NOT the fan logic: coordinator.py:2724 counts fans via _get_entities_in_area() (:2695-2700),
    which filters ent_reg.entities on `entity.area_id` ONLY. This fan''s registry entry has area_id =
    null — its area is INHERITED from its device. So the sensor structurally cannot see it and will read
    0 whether the fan is on or off, permanently. That is this card''s defect (area not set on the entity,
    inherited from the device) showing up in a different consumer. The fix shape is shared: either set
    area_id on the entity, or make the area lookup fall back to the device''s area. The second is more
    general and fixes every consumer at once rather than per-entity. NOT an anomaly, for the record: binary_sensor.living_room_fan_should_run
    reading off was CORRECT throughout — 78-79F is below the 80F fan_temp_threshold, so URA legitimately
    never wanted the fan on. Only the fans_on counter was wrong.'
  title: URA D3 fused sensor should inherit room area on creation
  thread: camera
  status: done
  shipped_version: v5.74.0
  approval: implied
  tags:
  - numbers-get-knobs
  origin:
    date: 2026-08-07
    gist: 5 rooms had roomless CameraPersonDetectedSensor - manual entity-area set was a band-aid
  why: CameraPersonDetectedSensor (D3) does not set area_id from its room on creation, so new rooms silently
    ship roomless -> breaks resolver/transit room mapping. Durable fix so we do not hand-patch each new
    room.
  parsimony:
    problem: per-room fused camera sensors ship with no area
    verdict: BUILD
    note: small - set area at entity creation from room config
  next: set _attr area / registry area from room area on D3 sensor creation
  refs:
  - binary_sensor.py CameraPersonDetectedSensor
  updated: 2026-08-23 15:05
  resolution_2026_08_17: 'Operator ruling 2026-08-17: "The cards that are judgement calls, resolve as
    done." Proof was a process/latent-event judgment not observable in live state; operator closed.'
- id: F1-SUNSET
  title: Frigate-1 go/no-go
  thread: camera
  status: done
  approval: blocked
  tags:
  - audit-first
  origin:
    date: 2026-08-07
    gist: Remind me when we can go on f1 sunset tmr
  why: steps 1-6 remote (mine), step 7 = operator unplugs NUC; readiness = organic one-alert-per-multi-engine-traversal
  next: operator go/no-go (reminder Aug 8)
  refs:
  - AUDIT_frigate1_sunset.md
  closed_2026_08_16: 'STALE (was blocked). Frigate-1 fully decommissioned 2026-08-15: entry deleted, 965
    entities + 25 f1retired removed, F2 sole NVR, hardware repurposed by homelab agent. Go/no-go resolved
    months of soak ago.'
- id: SWEEP
  STATUS_CORRECTED_2026_08_22: Was `waiting_me` and untouched since 2026-08-17. Its own next says "Recurring
    morning-sweep placeholder; re-fires each session start. No standing action." That is a PROCESS placeholder,
    not work, and holding it in an active lane makes the active queue read as busier than it is. Parked.
    The sweep still happens at session start via the skill; it does not need a card to occur.
  title: Morning sweep
  thread: ops
  status: parked
  approval: implied
  why: reason-ledger first night, Frigate car/dog/cat first events, snapshot-fix organic proof, v5.57/58
    organic criteria
  next: Recurring morning-sweep placeholder; re-fires each session start. No standing action.
  updated: 2026-08-17 23:12
- id: v5.59.0
  title: resolver-legs
  thread: perimeter
  status: done
  origin:
    date: 2026-08-07
    gist: shipped + live-validated
  note: live PASS (zero multi-key WARN / _2 storm / URA ERROR; telemetry attr present)
  organic_open: 'CLOSED 2026-08-07: leg_firing_by_camera POPULATED from real events (rear_ptz shows frigate+frigate2+protect
    on one camera; back_yard frigate+frigate2); today''s exterior person-detects each = one alert per
    track, pass_by tracks alert_count=0. Acceptance met. = F1-sunset readiness signal.'
  refs:
  - README_v5.59.0.md
  updated: 2026-08-17 23:30
- id: MEMORY-ROADMAP-1
  title: Memory epic — forward roadmap + critique + what-survives
  status: waiting_operator
  thread: memory
  created: 2026-08-18 02:00
  updated: '2026-08-28 22:00'
  refinement_status: refined
  problem: Memory epic shipped its first tranche (episodic writers D4-D7 v5.78.0 + nightly compactor).
    Operator wants a possible FORWARD roadmap for memory, a CRITIQUE of it, and a clear layout of which
    memory layers/artifacts SURVIVE (durability/retention) vs are transient. Needs full context.
  origin: 2026-08-18 "outline a possible roadmap for memory. Critique it and the layout what survives.
    Full context"
  next: Delivered — operator to review the doc; drives roadmap rewrite / memory epic close-out.
  refs: docs/planning/PLANNING_memory_roadmap_and_critique.md
- id: ROADMAP-UNDONE-REVIEW-1
  title: Review ROADMAP/VISION — surface undone-but-worthwhile
  status: waiting_operator
  thread: planning
  created: 2026-08-18 02:00
  updated: '2026-08-28 22:00'
  refinement_status: refined
  problem: 'Roadmap is stale (ROADMAP-STALE-AGENTIC-LAYER-1: doc at v3.22.0 says Next=Bayesian v4.0.0
    while live is v5.80.0). Operator wants a review of the roadmap surfacing what has NOT been done that
    is still worthwhile — separating genuinely valuable undone work from obsolete/superseded items.'
  origin: 2026-08-18 "Review roadmap and surface what has not been done that is worthwhile"
  next: Delivered — operator to review the doc; drives roadmap rewrite / memory epic close-out.
  refs: docs/planning/AUDIT_roadmap_undone_worthwhile.md
- id: IOS-APP-PLAN-CARD-1
  title: iOS app for HAOS+URA — gated design blueprint (tracked)
  status: parked
  thread: dashboarding
  created: 2026-08-18 02:00
  updated: 2026-08-18 02:25
  refinement_status: refined
  problem: 'A real, detailed iOS app design EXISTS but was untracked. It is a BLUEPRINT, not a build:
    native iOS (SwiftUI + HAKit + TestFlight) is gated behind (a) PWA milestones M1/M2 and (b) the commercialization
    >=100-active-installs gate. Now carded so it is not lost and its gating is explicit.'
  origin: 2026-08-18 "Find and card the iOS app plan"
  next: No action now (gated). When PWA M2 lands + install-gate met, promote M3 to planning.
  refs: 'docs/planning/DESIGN_ios_app_ura_haos.md (2026-07-11, 100 lines); parent docs/planning/COMMERCIALIZATION_options.md
    Play #1; PWA repo ~/Code/ura-dashboard-pwa (M1/M2 vehicle)'
  parked_reason: 'GATED. Path: M1 PWA read-only -> M2 PWA writes+NM -> M3 SwiftUI native shell+HAKit+TestFlight
    -> M4 native writes+push. Native (M3/M4) blocked behind PWA M1/M2 + HACS distribution + >=100 installs.
    Revisit trigger: PWA M2 complete AND commercialization install-gate met.'
- id: EGRESS-IDENTITY-CONTROL-OBS-1
  title: Egress person-detection — enable/disable + observability (parsimony)
  status: shipped_organic
  thread: presence
  created: 2026-08-18 02:20
  updated: 2026-08-23 14:30
  refinement_status: initial
  problem: The new egress person-identity detection (D1) must be operator-enableable/disableable, and
    its behavior must be observable — but with PARSIMONY (one control, minimal observability, no knob
    sprawl). It just surfaced a phantom-guest CRIT in review, so it should ship behind a kill switch and
    be validated before it drives anything.
  origin: 2026-08-18 "make sure this egress person detection can be enabled or disabled. Build in observability
    and control with parsimony"
  knobs: EGRESS_IDENTITY_ENABLED (options-flow bool, default OFF until live-validated; kill switch — when
    off, resolver returns None + register is no-op => byte-identical to pre-cycle). Mirrors D2 PROTECT_CORROBORATION_ENABLED
    default-OFF pattern.
  constraints: 'Parsimony: ONE enable bool; observability via ATTRIBUTES on existing census/egress sensors
    (egress_face_ids size + cumulative egress_identities_stamped), NOT new entities. Kill switch documented
    on the field.'
  next: 'Folded into the in-flight D1 fix-up (builder a4d0f6ccc0e0eeb9d): add enable gate + observability
    attrs + kill-switch-inert test. Lands in the same Tier-2-DB re-review as the review fix-ups.'
  refs: PLANNING_egress_face_identity.md; feature/egress-face-identity-d1
  l1_validated_2026_08_18: 'SHIPPED v5.81.0 + LIVE. L1 PASS (boot clean, zero URA ERROR; switch OFF/dormant
    byte-identical — egress_identities_stamped=0, census as v5.80.0; observability attrs egress_face_ids_active
    + egress_identities_stamped live on persons_entered_today). L2/L3 ORGANIC-PENDING: Wed occupancy +
    flip CONF_EGRESS_IDENTITY_ENABLED=True, watch real person_id (URA slug) on entry crossing + no phantom
    guest on exit. D2 Protect corroboration = session-pickup gate (recorder holds payload).'
  shipped_version: v5.81.0
- id: SENSOR-HEALTH-SURFACING-1
  title: 'Sensor health: chatter QUARANTINE (untrust from occupancy fusion) — trust model'
  status: review
  thread: diagnostics
  created: 2026-08-18 02:30
  updated: 2026-08-19 10:35
  refinement_status: initial
  problem: URA detects stuck-ON sensors via a watchdog but has NO chatter/flapping detector and no surfaced
    "which sensor is unhealthy" signal. A live incident (INCIDENT_chatter_class_missed_by_watchdog_2026-08-09)
    already proved the gap. Cheapest high-value undone roadmap item.
  origin: '2026-08-18 roadmap-undone review (AUDIT_roadmap_undone_worthwhile.md #1)'
  next: 'Plan: chatter detector + ura_unhealthy_sensors sensor + sensor_health table + NM "replace this
    sensor" hook. Tier 2. Institutional-context grep first (chatter->0 files today).'
  refs: AUDIT_roadmap_undone_worthwhile.md; INCIDENT_chatter_class_missed_by_watchdog_2026-08-09
  phase_2026_08_18: 'IN PROGRESS (operator approved all 3 to execute 2026-08-18): planning (Tier 2, institutional-context-first)'
  pivot_2026_08_18: 'MAJOR PIVOT (operator, after 2 reviews DO-NOT-SHIP + quarantine-code consult). The
    surface+notify build is insufficient; move to a QUARANTINE/TRUST model mirroring the actuator D2.11
    flap-quarantine. DECISIONS: (1) QUARANTINE-ALWAYS (no corroboration gate on detection). (2) UNTRUST
    != drop — quarantine removes the sensor VOTE from the occupancy fusion; occupancy = fusion of REMAINING
    trusted inputs (if another working sensor/BLE/camera says present, stays present; if only the chattering
    sensor claimed present, correctly goes vacant). No force-vacant, no interlock. (3) The DEFINITION
    is the key + the safety: chatter = PHYSICS-BASED (transition faster than the sensor hardware off-delay/debounce
    floor) OR implausibly-SUSTAINED rate beyond any real occupancy episode — NEVER a raw transitions/min
    threshold (that would false-quarantine a busy room). Edge-count via a STATE-CHANGE LISTENER (A1 CRIT:
    tick-sampling cannot measure the rate). (4) Auto-release when quiet for a stability window + recovery
    NM (D2.11 pattern). Keep the surface (UnavailableEntitiesSensor reason=chattering) + NM (fire_stuck_signal)
    from the current build. REUSE the room-tier sensor-exclusion path (cf STUCK-SENSOR-1 / SENSOR-CAPABILITY-1)
    — do NOT invent a parallel exclusion. GATING FEASIBILITY QUESTION for the re-plan: does a room-tier
    "exclude a sensor vote from occupancy fusion" mechanism EXIST today, or is it the thing STUCK-SENSOR-1
    is blocked on building? Reverses the old non-goal. Now Tier 2-DB (occupancy/trust-hierarchy).'
  program_unification_2026_08_18: 'PROGRAM UNIFICATION (operator 2026-08-18): chatter, stuck-on, and flapping-mmWave
    are ASPECTS of ONE sensor-trust/exclusion program — a shared ROOM-TIER "untrust a sensor vote / exclude
    from occupancy fusion" primitive with multiple DETECTORS on top. Detectors differ by gate: CHATTER
    = physics-based, quarantine-always, NO corroboration -> NOT blocked on SENSOR-CAPABILITY-1; STUCK/flapping-mmWave
    = corroboration-gated -> blocked on SENSOR-CAPABILITY-1. Shared primitive = D1; chatter detector can
    land independently; stuck detector sequences after capability work. Do NOT build parallel exclusions.
    Members: SENSOR-HEALTH-SURFACING-1 (chatter), STUCK-SENSOR-1 (stuck/flapping mmWave), SENSOR-CAPABILITY-1
    (capability/role -> corroborator selection).'
  program: sensor-trust-exclusion
  definition_confirmed_2026_08_18: 'CHATTER DEFINITION GROUNDED + HAND-CHECKED + operator-confirmed. Chatter
    = >=K sub-T_floor impossibility events (interval below the device-family hardware blind-time) in-window,
    on BLIND-TIME-GATED sensors ONLY (PIR/mmWave/opener). PROVENANCE GATE excludes camera/AI (frame-cadence,
    no blind-time -> CHATTER-CAMERA-CONFIDENCE-FLAP-1), aggregates (union-rate; target the member), bed
    multi-state (SENSOR-MULTISTATE-FAULT-1) — justified: the un-fakeable criterion is UNDEFINED without
    a single hardware blind-time. T_floor from device-family blind-time (1-3s band), NEVER learned from
    the suspect itself (circular). Burst-of-K (not single event; K counts IMPOSSIBILITY events not transitions
    -> still never a raw rate). D0 GO-with-amendments: catches ratgdo (58,713 tx/7d, sustained 2-3s) +
    invisoutlet, ZERO healthy false-positives. Live acceptance fixture captured. Ready: fold done -> Tier
    2-DB plan review -> build.'
  phase_2026_08_19: 'IN PROGRESS: resuming STEP: applying plan-review 3 MEDs + Tier-3 elevation to make
    build-ready'
  plan_ready_2026_08_19: 'PLAN-READY (Tier 3). Both Tier-3 plan reviews passed after fixes: review-1 (3
    MED fixed), review-2/build-prediction (M2-MED-1 Reading-A byte-identity + 2 LOW fixed). Definition
    grounded+hand-checked. Awaiting Tier-3 BUILD (4 framing-disjoint reviews incl. adversarial-completeness
    + OPERATOR CHECKPOINT before deploy). Held pending the fan-recheck fix resolving (avoid running a
    4-review Tier-3 build in parallel with the 3 fan reviews + pending fixture-seam ship decision).'
  build_2026_08_19: 'TIER-3 BUILD dispatched (operator go: build STEP, ship 9am). D1 shared SensorExclusionSet
    + D2 chatter detector + D3 auto-release + D5 surface. 4 framing-disjoint reviews + operator checkpoint
    before deploy.'
  plan_gap_2026_08_19: 'BUILD STOPPED (correctly — anti-fabricate mandate) on a load-bearing PLAN GAP
    both Tier-3 plan reviews MISSED (producer-check failure): the plan has the chatter detector subscribe
    to OccupancySubstrate.subscribe() for per-entity edges, but that feed (occupancy_substrate.py:704)
    delivers per-KIND AGGREGATE (room,kind,on), NOT per-entity transitions — the detector cannot count
    per-entity sub-T_floor bursts from it. OPERATOR delegated the fix to correctness+efficiency reasoning.
    DECISION (orchestrator): Option 1 — the ChatterDetector owns its own async_track_state_change_event
    on the blind-time-gated entity set, self-contained + testable + makes the Bug-Class-#38 teardown REAL.
    The plan "no new registration/inherit subscribe" constraint is VOID (false premise). Amend plan: subscription
    mechanism + L-LOW-B teardown (self._chatter_unsub = HA unsub callable) + D6-test-1 wire + §3.1 producer
    + entity_id->kind derivation (via _KIND_TO_CONF) for the classifier + fix D0 filename drift (plan
    says AUDIT_chatter_physics_floor_probe.md; real doc is PROBE_sensor_chatter_definition_handcheck.md).
    Then focused re-review of the amended section -> re-dispatch build. STEP will NOT make 9am (real amendment
    + 6-10h build); FAN FIX ships 9am.'
  feed_fixed_2026_08_19: Feed amendment PLAN-READY (own async_track_state_change_event, re-review no CRIT/HIGH/MED,
    1 LOW folded to brief). Tier-3 build RESUMED in .claude/worktrees/step-chatter.
  tier3_reviews_2026_08_19: 'TIER-3 REVIEWS: A SHIP-WITH-FIX, B SHIP, C DO-NOT-SHIP, D DO-NOT-SHIP. The
    4-review gauntlet did its job. CONSOLIDATED FIX-UP SCOPE:

    SAFETY (D, HIGH): D-HIGH-1 healthy fast mmWave (FP1 ~1s) false-quarantined because T_floor mmwave=1.5s
    is TIGHTER than healthy cadence + the promised per-entity sensor_capability T_floor override is UNIMPLEMENTED
    -> the un-fakeable definition is NOT achieved as calibrated (operator hard constraint violated). D-HIGH-2
    boot-transient primes quarantine (deque append BEFORE boot-settle gate) -> fires every restart with
    motion.

    TEST-AUTHORITY (C, CRIT): the coordinator tick-site + 6 fusion sites + D2-raise Reading-A are HOLLOW-anchored
    (source-string .count/.read_text, not behavioral) — neutering P22/stuck/fusion leaves 40/40 green.
    Same class + same root cause as the fan-recheck bug (real coord un-constructable in-suite, v5.8.0).
    Corroborates A-M-A2.

    OTHER: D-MED-1 Z2M numeric entity_ids escape the classifier (chatterer not quarantined). D-MED-2 mitigation
    knobs all Final module-const + ships enabled -> no operator backout but the big toggle. A-M-A1 per-tick
    async_create_task write-flood (STUCK-1 already fixed this pattern). B: 4 doctrine LOWs (release clobbers
    stuck label; kill-switch no discharge).

    TWO DECISION-GATED PARTS: (1) T_floor recalibration = MEASURE real mmWave cadence (probe dispatched)
    then lower floor below healthy OR implement the per-entity override. (2) coordinator-integration tests
    hit the SAME v5.8.0 fixture seam as the fan fix — live-validate vs build a real-coord harness (operator
    call). STEP = next-work-block, not shippable soon. Fan fix v5.84.0 UNAFFECTED (staged, 9am).'
  probe_calibration_2026_08_19: 'CADENCE PROBE (PROBE_mmwave_healthy_cadence.md) RESOLVED decision #1
    with data. KEY REFINEMENT: the un-fakeable criterion is the BURST (K sub-floor events/window), NOT
    a single sub-floor event — healthy fast mmWave DOES produce sub-floor intervals (holds ON, re-detects
    on OFF edge in <1s; healthy Meross = 323-386 sub-1.5s/7d). CORRECTED CALIBRATION (data-backed, GO):
    T_floor=1.0s UNIFIED all families (was 1.5s mmwave/2.0 PIR/3.0 opener), CHATTER_BURST_K=10 (was 20
    — CRITICAL: build K=20 MISSES invisoutlet which chatters at only 13; and ratgdo=22), window=300s rolling.
    At 1.0s/K=10: zero healthy false-positives + both chatterers caught (healthy worst burst=7, chatterers
    >=13, clean band [8,12]). Opener separation empirically unverifiable (only in-window opener IS the
    chatterer ratgdo) — residual risk, band-sharing mitigates. Regression sentinel: Meross jaya/mediaroom
    (burst 7@1.0s). So the fix-up const changes are DATA-BACKED, no guess.'
  fixture_decision_2026_08_19: 'OPERATOR ACCEPTED LIVE-VALIDATION (option a) for the coordinator-integration
    surface (C-CRIT) — same as the fan fix. Real-coord harness (option b) deferred to TEST-STRATEGY-REARCH-1.
    STEP fix-up proceeds: de-hollow the C-CRIT tests (extract-and-make-real where feasible per the D3
    precedent; honestly label structural + live-validate the genuine v5.8.0 seam — do NOT leave source-string
    tests masquerading as behavioral) + the data-backed recalibration + all HIGH/MED fixes.'
  checkpoint_ready_2026_08_19: 'CHECKPOINT-READY (Tier-3). Reviews: A SHIP-WITH-FIX(fixed), B SHIP, C
    DO-NOT-SHIP->C2 SHIP (de-hollow genuine, ast-extraction mutation-verified), D DO-NOT-SHIP->D2 SHIP-WITH-CONDITIONS
    (all 2 HIGH + 2 MED closed, no new leak from refactor) -> ship-conditions fixed (tree-poisoning test
    hardened, T_floor live, 2 cleanups). Orchestrator verify DONE: grep-confirmed K=10/T_floor=1.0/promote
    sites/fusion routing/boot-gate; tree clean; C-re independently mutation-confirmed the fusion filter.
    96/96 tests, 36/36 stuck byte-identical. AWAITING OPERATOR CHECKPOINT before deploy v5.85.0. Live-validation
    is the integration authority (accepted). Residual accepted: opener family separation empirically unverifiable
    (per-sensor T_floor=0 backout); structural-only markers live-validated.'
  shadow_first_2026_08_19: 'OPERATOR ROLLOUT DECISION: ship SHADOW-FIRST, not default-on-acting. The acting
    quarantine is gated behind D7 (CHATTER-OBSERVE-CONTROL-D7-1: observe+control panel) + a HARD 2-DAY
    forcing gate (flip to acting by 2026-08-21 or declare moot — bazillion devices = trivial stat significance).
    Default the enable to SHADOW/OFF-acting on this ship. STEP core (D1 primitive + detector + recalibration)
    is checkpoint-ready; re-scope so the DEPLOYED default is shadow (detect+surface), acting-enable deferred
    to the gate. Fan fix v5.84.0 unaffected.'
- id: APPLIANCE-COST-DEFERRAL-1
  title: Appliance cost-deferral — LG ThinQ + Rainbird start-deferral/skip
  status: inbox
  thread: energy
  created: 2026-08-18 02:30
  updated: 2026-08-18 02:30
  refinement_status: initial
  problem: No appliance_coordinator exists (thinq/rainbird->0 files). Deferring washer/dishwasher starts
    and skipping sprinkler runs to off-peak/solar windows is recurring-$ value but ~30-40h of work.
  origin: 2026-08-18 roadmap-undone review (#2)
  next: MARGINAL-BENEFIT DECOMPOSITION before speccing the full framework — how much does the simplest
    single-appliance deferral capture vs the whole v3 framework? Then Tier 2-DB if it clears.
  refs: AUDIT_roadmap_undone_worthwhile.md
- id: UNLOAD-SYMMETRY-TASK-HYGIENE-1
  title: Setup/unload symmetry + tracked background tasks (tech-debt hardening)
  status: inbox
  thread: platform
  created: 2026-08-18 02:30
  updated: 2026-08-18 02:30
  refinement_status: initial
  problem: async_on_unload used in only 2 sites; untracked background tasks — both match known URA bug
    classes (reload-safety, task leak). One hardening cycle.
  origin: '2026-08-18 roadmap-undone review (#3, tech-debt #1+#2)'
  next: 'Plan a Tier 2 hardening cycle: audit setup/unload symmetry across platforms + wrap background
    tasks in tracked/cancellable handles. Cross-ref parent-reload-watchdog hazard.'
  refs: AUDIT_roadmap_undone_worthwhile.md
- id: CONFIG-SUBENTRIES-MIGRATION-1
  title: Config subentries migration (flat 34-entry -> subentries)
  status: parked
  thread: platform
  created: 2026-08-18 02:30
  updated: 2026-08-18 02:30
  refinement_status: initial
  problem: Still flat 34 config entries; 189 hass.data[DOMAIN] sites. HA subentries would clean topology
    but the migration carries real risk for MEDIUM value.
  origin: 2026-08-18 roadmap-undone review (#4)
  parked_reason: 'MEDIUM value, real migration risk. Revisit trigger: when a config-topology change is
    needed anyway, or HA deprecates the flat pattern.'
  per_item_reload_driver_2026_09_03: 'NEW DRIVER (operator, via DEVICE-ENTITY-REORG-1): the operator wants
    per-ZONE and per-COORDINATOR individual reload. That is impossible today because all zones share one Zone
    Manager entry and all coordinators share one CM entry — reload is per config-entry. This migration (subentries,
    or splitting into per-item entries) is the vehicle that would deliver it. VERIFY before scoping: whether HA
    subentries expose per-subentry reload, or whether a per-entry split is required (guaranteed path). Largest
    _2/orphan hazard at scale — Tier 3 when revived. This raises the value above MEDIUM if the operator elevates
    per-item reload.'
  sibling_of: [SCALE-LEAN-ROOM-PROFILE-1, DEVICE-ENTITY-REORG-1]
  next: 'Parked. Revive if the operator elevates per-zone/per-coordinator individual reload (see driver above)
    OR HA deprecates the flat pattern. Then: verify subentry-reload semantics vs per-entry split; probe first.'
  refs: [AUDIT_roadmap_undone_worthwhile.md, docs/planning/PLANNING_device_entity_architecture_2026_9.md, DECISION_LOG_device_entity_cycle_2026_09_03.md]
- id: ENTITYDESC-RUNTIMEDATA-HYGIENE-1
  title: EntityDescription + runtime_data hygiene (opportunistic)
  status: parked
  thread: platform
  created: 2026-08-18 02:30
  updated: 2026-08-18 02:30
  refinement_status: initial
  problem: EntityDescription + runtime_data patterns not adopted; low-value on its own.
  origin: '2026-08-18 roadmap-undone review (#5, tech-debt #3+#4)'
  parked_reason: Opportunistic — attach to the next coordinator touch rather than a dedicated cycle.
  next: Fold into the next platform/coordinator cycle that already edits the target files.
  refs: AUDIT_roadmap_undone_worthwhile.md
- id: SUITE-ORDER-POLLUTION-1
  INSTANCE_2026_08_26_WALLCLOCK_ALT_MECHANISM: 'FOURTH sighting + a MECHANISM CORRECTION from the
    HVAC-excursion Tier-3 reviewer C. The SAME two test_evse_drain_precedence_session_b2c2_fixup.py
    tests (+ 4 in test_dp_drain_target_value_stamp.py) go RED near 02:00 LOCAL wall-clock and GREEN
    at 01:56 and GREEN in isolation — root cause is must_start_by(06:00) minus now() dropping under
    drain+charge+margin as local midnight approaches, i.e. DAY-BOUNDARY / WALL-CLOCK-COUPLED tests,
    NOT (only) sys.modules import-order. The order-pollution framing above may be a mis-attribution
    for these specific tests. Whoever fixes this must test the wall-clock hypothesis first (freeze
    the clock in the fixture) before chasing import order. Bug family: wall-clock-coupled test
    (memory: the two worst recent bug families both lived at state-machine x time seams).'
  INSTANCE_2026_08_22_EVSE_DP_CARRIER: 'THIRD INSTANCE. Two tests in test_evse_drain_precedence_session_b2c2_fixup.py
    — test_transition_entry_pauses_actual_configured_evse_id and test_transition_entry_pauses_only_charging_evse_ids_multi_evse
    — FAIL in the full suite and PASS in isolation, on BOTH develop and any branch. Deterministic across
    repeated full runs (two identical runs, same two failures). Failure text: "tick failed to reach TRANSITIONED:
    state=hold_only" — the DP carrier state machine does not advance. NOT caused by the AC-reset-preset
    cycle: they are present on develop itself. NOTE PYTEST COLLECTION SEMANTICS, which is what makes this
    class so slippery: pytest IMPORTS EVERY TEST MODULE during collection BEFORE running any test. So
    a module-level side effect in a file that sorts LATER alphabetically still executes BEFORE an earlier
    file runs. "It sorts after, so it cannot affect it" is FALSE reasoning and was nearly used here to
    dismiss this. ORCHESTRATOR METHOD ERROR WORTH RECORDING: these were initially read as regressions
    from the AC-reset cycle because the develop BASELINE WAS STALE — captured before v5.88.0 merged into
    develop, then compared against a branch that contained v5.88.0. A stale baseline MANUFACTURES PHANTOM
    REGRESSIONS. Re-baselining develop at its current HEAD produced an empty name-diff both directions.
    RULE: re-baseline develop immediately before every name-diff; never reuse a baseline across a merge
    to develop. Belongs to the family this card tracks; the durable fix remains TEST-STRATEGY-REARCH-1.'
  INSTANCE_2026_08_21_GATE_BLOCKER_FIXED: 'SECOND INSTANCE, MORE SEVERE THAN THE FIRST, AND FIXED THE
    SAME DAY. This one did not merely fail tests — it made the ENTIRE SUITE UNRUNNABLE ON DEVELOP, so
    the mandatory pre-deploy name-diff gate was unavailable for EVERY cycle, not just the one in flight.
    Nobody had noticed because a passing targeted run looks the same as a passing suite from the outside.
    MECHANISM: quality/tests/test_reboot_pickup_d2.py read `<mod>.__path__[0]` off package modules fetched
    from sys.modules at IMPORT time (three sites: custom_components, universal_room_automation, domain_coordinators).
    A sibling file that had already registered a stub with an EMPTY __path__ made that raise IndexError
    — during COLLECTION, which pytest treats as fatal, aborting all ~9,600 tests. PROVENANCE: found by
    the orchestrator while attempting the pre-deploy gate for HVAC-GOVERNED-EXCURSION-1. VERIFIED pre-existing
    on develop and NOT caused by that branch, and NOT caused by the ~100 stale worktrees (reproduces with
    .claude ignored). FIX (commit 71a769cfb): derive every path from __file__, which no sibling can corrupt;
    use the sys.modules entries only for REGISTRATION; REPAIR a pre-existing module with a missing/empty
    __path__ via _ensure_pkg_path() rather than trusting it. Before: 9,610 collected, 1 error, interrupted.
    After: 9,615 collected, 0 errors. The file own 5 tests still pass. THE GENERAL LESSON, and it is the
    one worth carrying: DEFENSIVE CODE THAT READS POLLUTED STATE IS NOT DEFENSIVE. This file was carefully
    written to coexist with siblings (setdefault everywhere, do-not-clobber comments) and still broke,
    because it TRUSTED what it read back out of sys.modules. Compute from an authority nobody else can
    write to (__file__), do not read back from shared mutable state. STILL OPEN on this card: the reverse-order
    failures from test_override_arrester_ttl_suppression (see the other INSTANCE block) — a different
    file, same family, not fixed.'
  INSTANCE_2026_08_21_EXCURSION_CYCLE: 'CONCRETE MEASURED INSTANCE, found during the HVAC-GOVERNED-EXCURSION-1
    Tier-3 review (Review C framing: test authority). SYMPTOM: collecting quality/tests/test_override_arrester_ttl_suppression.py
    BEFORE the excursion test files produces 7 COLLECTION ERRORS that take EVERY excursion test file down
    — "ImportError: cannot import name HVACCoordinator" / "module hvac_override has no attribute OverrideArrester".
    Default ALPHABETICAL order happens to be safe (test_hvac_* sorts before test_override_*), so the suite
    reports green. That is an ACCIDENT OF NAMING, not a property. WHY IT MATTERS BEYOND THIS CYCLE, and
    this is the important part: a green run DOES NOT PROVE THE ANCHORS EXECUTED. Review C explicitly conditioned
    all 20 of its mutation-drill verdicts on that ordering holding. Any -p randomly, xdist distribution,
    -k subset, or a developer running two files by name can silently vaporise the anchors while the run
    still reports success. This degrades the evidentiary value of EVERY reviewer pass, not just one cycle.
    PARTIALLY FIXED 2026-08-21: the excursion cycle removed ITS OWN coupling (module-level sys.modules
    surgery replaced by a shared idempotent bootstrap at quality/tests/_excursion_harness.py which pops
    homeassistant.helpers.storage and homeassistant.util.dt on exit). STILL OPEN AND OWNED HERE: test_override_arrester_ttl_suppression.py
    installs homeassistant.helpers.storage in its own harness and NEVER POPS IT. In reverse order that
    pollution flips the D1 _HA_REAL gate True, so 6 D1 behavioural tests run against mocks and fail. VERIFIED
    PRE-EXISTING — the same 6-failure pattern reproduces on commit e6e69e8fe, before the fix-up. Explicitly
    left out of the excursion cycle scope (it does not own that file) and routed here instead. FIX SHAPE:
    pop the installed modules at that file tail, or better, move every such harness to the shared bootstrap
    pattern so no test file does import-time sys.modules surgery at all. The second is the real fix and
    belongs to [[TEST-STRATEGY-REARCH-1]].'
  links:
    related:
    - TEST-STRATEGY-REARCH-1
    - HVAC-GOVERNED-EXCURSION-1
    - TEST-SOURCE-MUTATION-KILL-UNSAFE-1
    context: TEST-STRATEGY-REARCH-1 is the parent re-architecture (the durable fix — no import-time sys.modules
      surgery anywhere). TEST-SOURCE-MUTATION-KILL-UNSAFE-1 is a sibling suite-hygiene hazard from the
      same family (a killed source-mutating test corrupts the tree). HVAC-GOVERNED-EXCURSION-1 is where
      this instance was found and where the partial fix shipped.
  title: Presence tests fail order-dependently in large batches (suite hygiene)
  status: inbox
  thread: platform
  created: 2026-08-18 03:00
  updated: 2026-08-19 07:45
  refinement_status: initial
  problem: test_presence_coordinator + test_presence_guest_latch_and_veto_gap (D3 edge/zone-log tests)
    PASS in isolation but FAIL when run inside a large multi-file batch — order-dependent pollution from
    some other test file leaking module state. Pre-existing on develop (confirmed pre-merge). Makes the
    suite baseline count noisy (contributes to the ~25 flaky failures) and can mask real regressions.
  origin: 2026-08-18 v5.81.0 pre-deploy gate — 6 batch failures traced to pre-existing order pollution,
    not the cycle
  next: Bisect the batch to find the polluting file; add autouse snapshot/restore or fix the leak. Folds
    under UNLOAD-SYMMETRY-TASK-HYGIENE-1 suite-hygiene thread.
  refs: UNLOAD-SYMMETRY-TASK-HYGIENE-1
  subsumed_note: Subsumed under TEST-STRATEGY-REARCH-1 (pollution = section B of that investigation).
    Keep as the concrete pollution instance; the broader re-arch owns the fix.
- id: CENSUS-TOGGLES-TO-DEVICE-SWITCHES-1
  title: Promote 3 Camera-Census feature toggles to device switches
  status: shipped_organic
  thread: presence
  created: 2026-08-18 03:10
  updated: 2026-08-23 14:30
  refinement_status: refined
  problem: Three feature toggles are buried in the Camera Census options dialog (options-flow rung 2).
    Operator wants them as one-tap DEVICE SWITCHES on the URA device (rung 3) for live control + dashboard
    access. Names must be simple, benefit-first, and SCOPED (not imply a global kill switch — URA uses
    face recognition in many places).
  origin: 2026-08-18 "propose device toggle switch names ... simplicity and easy to understand"; names
    iterated w/ operator
  knobs: 'LOCKED names (operator-approved): switch.ura_presence_face_matching "Presence Face Matching"
    (<- face_recognition_enabled; scope VERIFIED = transit_validator + presence zone confirm ONLY, not
    global); switch.ura_smart_people_counting "Smart People Counting" (<- enhanced_census); switch.ura_name_people_at_doors
    "Name People at Doors" (<- egress_identity_enabled).'
  constraints: Switch entity = SINGLE source of truth, persisted via URA switch-persistence machinery
    (single-user, no back-compat migration). Reconcile with the existing options-flow fields (avoid two-sources
    divergence). Enhanced Census is the HEAVY one — toggling swaps the census engine; may need integration
    reload rather than instant apply (planner decides). Keep the options-flow i18n label already committed
    for egress as backing.
  next: 'ura-planner: Tier 2 plan (3 SwitchEntity + persistence + options reconciliation + consumed by
    presence/transit/census). Then ONE plan review, then build.'
  refs: v5.81.0 (egress kill switch); config_flow.py async_step_camera_census; feedback_model_tiering
    / numbers-get-knobs ladder
  revised_2026_08_18: 'PLAN-REVIEW (PLAN-NEEDS-FIXES, 1 CRIT) reshaped this. REVISED SCOPE: TWO switches
    only — Presence Face Matching + Name People at Doors. Smart People Counting (enhanced_census) STAYS
    in options (heaviest/structural at __init__.py:2253, not live-refreshable) — parked w/ trigger. CRITICAL
    fix: the naive async_update_entry path RELOADS the parent integration = the ~5min-outage watchdog
    hazard just mitigated 2026-08-15. Correct design: extend INTEGRATION_OPTIONS_RELOAD_SUPPRESS_KEYS
    allowlist for the 2 keys + fire SIGNAL_URA_FACE_RECOGNITION_CHANGED to live-refresh the boot-cached
    presence/transit consumers (suppression-needs-a-discharge). egress already live per-call. DEFAULTS:
    operator wants all ON (face_recognition F->T, egress F->T, enhanced_census stays T) — flips live at
    deploy since options unset; egress L2/L3 still Wed-pending (validate in prod w/ kill switch). Ledger:
    +2 switches / +1 signal / +0 sensors. ELEVATED to Tier 2-DB. Next: planner revises -> re-review revised
    plan -> build.'
  shipped_2026_08_18: 'SHIPPED v5.82.0 + LIVE. L1/L2/L4 PASS: both switches present+ON (defaults flipped),
    smart_people_counting correctly NOT a switch (404); L2 no-reload PROVEN live (untoggled sibling last_changed
    stable at boot across two toggles) + zero ERROR; L4 kill path instant. L3 (real crossings named, no
    phantom guest on exit) organic-pending Wed. Orchestrator independently mutation-drilled the signal-refresh
    test (neuter dispatcher -> test fails -> restore -> 35/35).'
  shipped_version: v5.82.0
- id: CENSUS-IDENTITY-GROUP-README-1
  title: 'Cycle-group README: census/guest/presence-identity program'
  status: done
  thread: planning
  created: 2026-08-18 03:20
  updated: 2026-08-18 14:15
  refinement_status: initial
  problem: The census/guest/presence-identity arc (v5.79.0 guest correctness -> v5.80.0 census accuracy
    + dashboards -> v5.81.0 egress identity -> planned device switches -> gated D2) is coherent only in
    the operators head + scattered per-version READMEs. Needs ONE cycle-group README that tells the story
    and NAMES EVERY ENTITY (verified, not from memory), separating SHIPPED / PLANNED / GATED.
  origin: 2026-08-18 "we need a cycle group readme, so this collection is coherent and should name the
    entities"
  next: general-purpose writing docs/readmes/README_GROUP_census_guest_presence_identity.md with verified
    entity inventory.
  refs: README_v5.79.0/5.80.0/5.81.0; CENSUS-TOGGLES-TO-DEVICE-SWITCHES-1
  done_note: Delivered + reconciled to v5.82.0 (switches shipped). Sent to operator.
- id: CENSUS-IDENTITY-SUPERSESSION-DELETE-1
  title: Delete superseded census/identity code (gated on L3 validation)
  status: done
  thread: planning
  created: 2026-08-18 09:45
  updated: 2026-08-18 10:35
  refinement_status: initial
  problem: 'The census/identity arc superseded some code. Each delete-candidate has its OWN concrete,
    checkable gate (NOT a blanket "when validated fully"):

    - S1 CENSUS_DECAY_STEP_SECONDS (const.py:2777): superseded by v5.80.0 decay separation, which is LIVE
    + validated. GATE = one grep confirming zero readers remain. ACHIEVABLE NOW — not coupled to the v5.81/v5.82
    identity L3. Deletable in a Tier-1 cleanup once the grep confirms.

    - S7 legacy string-built Frigate face/last_camera paths: superseded by the v5.80.0 _2-suffix resolver
    (LIVE). GATE = a focused grep sweep confirming each site has no live reader / is fully replaced. ACHIEVABLE
    NOW via grep.

    - S2 old oracle: already removed in v5.79.0 (no vestige) — nothing to delete.

    - S3/S6 KEEP (load-bearing: face_recognized_count != identified_count, gates away-veto). S4/S5 KEEP
    (display-only).'
  origin: 2026-08-18 "Check for what this supersedes so we can mark for deletion when we validate fully"
  parked_reason: NOT gated on identity L3 (that was a spurious coupling). Each item gates on its OWN grep-check
    against ALREADY-shipped-and-validated cycles (v5.79.0/v5.80.0). Do the two greps (S1 zero-readers,
    S7 sweep) to DECIDE and delete — no Wed dependency.
  next: 'Tier-1 cleanup: grep-verify S1 zero readers -> delete CENSUS_DECAY_STEP_SECONDS; grep-sweep S7
    legacy Frigate paths -> delete confirmed-dead sites. Both decidable now.'
  refs: AUDIT_census_identity_supersession_and_consumers.md §1
  closed_noop_2026_08_18: 'CLOSED as NO-OP (correct outcome). Three-bucket triage: DELETE bucket EMPTY
    — no true dead-and-useless code in this cycle group. CENSUS_DECAY_STEP_SECONDS = bucket 3 (already
    documented retired-available in its tombstone comment, no edit needed). presence.py:4557 = bucket
    2 (useful face-confirmed-arrival helper lacking _2-suffix tolerance) -> spawned CENSUS-FACE-RESOLVER-MIGRATE-1.
    S7 all live/retained. Zero-delta cleanup branch discarded. The supersession HYGIENE proved its value
    by finding there was nothing to delete — and by catching that "dead" was being conflated with "deletable".'
- id: PERIMETER-ALERT-NAME-PERSON-1
  title: Perimeter alerts should NAME the person (consume egress/face identity)
  status: inbox
  thread: security
  created: 2026-08-18 09:45
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.75 confidence gate before build'
  sequence: 1  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  confidence_gate: '>=0.75 to name the person in the alert; graceful-anonymous below. Annotate/de-escalate
    only — a low-confidence name must never suppress or downgrade a real perimeter alert (§5.5 doctrine).'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  audit_2026_08_28: 'Post-ship consumer-gap audit (2026-08-28): the producer is now BUILT
    (feature/egress-identity-producer, v5.91.4 pending deploy). Wire-in site is perimeter_alert.py:1316
    (the "Person Detected" message builder). NOTE the geometry: the PERIMETER camera is an EXTERIOR-DETECTION
    source, NOT the egress-crossing camera — so this joins to identity via the FACE RESOLVER (census
    _resolve_face_entity_id / the identity union), not directly off the egress person_id crossing event.
    Keep it graceful-anonymous and >=0.75 to name. Measure real production rate first (§5.5 gate).'
  refinement_status: initial
  problem: perimeter_alert.py:1316 still emits anonymous "Person Detected" even when identity is known
    — the exact known-vs-unknown discriminator this arc built. Highest signal-to-noise payoff of the gaps.
    Consume identity gracefully (name when known, anonymous otherwise). Identity comes from Frigate face
    (all cams) + Protect named face at doorbell/family-room via the live webhook.
  origin: 2026-08-18 should-be-consuming audit G1
  next: 'Measure-before-build: probe the REAL egress identity rate against the GARAGE + family-room entry
    path (NOT the front door) and include Protect named face via the webhook, before scoping.'
  refs: AUDIT_census_identity_supersession_and_consumers.md §3 G1; perimeter_alert.py:1316
  coverage_note_2026_08_18: 'CORRECTION 2026-08-18 (operator): the ~7% figure is NOT a coverage ceiling
    and must not be cited as one. It came from PROBE_protect_face_egress.md which measured the WRONG camera
    (front door madrone_g6_entry). Most family entries are via the GARAGE, and Protect names people in
    the FAMILY ROOM — i.e. ON the real entry path. Frigate is on ALL cameras (front overhead + doorbell
    + foyer; garage doorbell + inside-garage cam); Protect has face recognition on the front doorbell
    + family room, reachable via the LIVE Alarm Manager webhook (EXTERIOR-GUEST-FACE-FASTFOLLOW-1 D2).
    The identity path is VIABLE. Coverage must be RE-MEASURED against GARAGE entries + family-room arrival,
    not the front door. The face-INDEPENDENT approach-track arm is a DEFERRED, gated-on-value FALLBACK
    (EXTERIOR-GUEST-EGRESS-1, revisit only if identity proves insufficient) — operator ALREADY chose identity-first;
    do NOT re-recommend it off the 7%.'
- id: GUEST-GATE-DOOR-IDENTITY-1
  title: Guest gate should consume door-identity (not just BLE room-location)
  status: inbox
  thread: presence
  created: 2026-08-18 09:45
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.9 confidence gate before build'
  sequence: 2  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  confidence_gate: '>=0.9. Suppressing the guest gate on a WRONG identity UNDER-secures the house (a
    mis-named intruder read as a resident), so this needs the highest bar — genuine multi-camera
    corroboration, not a single leg. Suppress-only at >=0.9; never suppress at 0.6/0.75 (§5.5 doctrine).'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  audit_2026_08_28: 'Post-ship consumer-gap audit (2026-08-28): producer now BUILT (v5.91.4 pending deploy).
    Wire-in target is _is_known_person_in_room at presence.py:4988 (today reads only BLE room-location).
    A resident door-identified at >=0.9 should firm up the guest gate / cancel a nascent guest FP. Because
    a wrong suppress UNDER-secures, the gate is >=0.9 and suppress-only — identity may confirm a resident,
    never admit an unknown. Coverage-gated on measured post-deploy yield (§5.5).'
  refinement_status: initial
  problem: _is_known_person_in_room relies solely on BLE room-location; a resident identified at the DOOR
    does not suppress a guest false-positive. Closest to the original census-double-count wound. Adjacent
    card EGRESS-INTERIOR-COUNT-REINFORCE-1 is count-focused, this is GATE-focused.
  origin: 2026-08-18 should-be-consuming audit G2
  next: 'Measure-before-build: probe the REAL egress identity rate against the GARAGE + family-room entry
    path (NOT the front door) and include Protect named face via the webhook, before scoping.'
  refs: AUDIT_census_identity_supersession_and_consumers.md §3 G2; EGRESS-INTERIOR-COUNT-REINFORCE-1
  coverage_note_2026_08_18: 'CORRECTION 2026-08-18 (operator): the ~7% figure is NOT a coverage ceiling
    and must not be cited as one. It came from PROBE_protect_face_egress.md which measured the WRONG camera
    (front door madrone_g6_entry). Most family entries are via the GARAGE, and Protect names people in
    the FAMILY ROOM — i.e. ON the real entry path. Frigate is on ALL cameras (front overhead + doorbell
    + foyer; garage doorbell + inside-garage cam); Protect has face recognition on the front doorbell
    + family room, reachable via the LIVE Alarm Manager webhook (EXTERIOR-GUEST-FACE-FASTFOLLOW-1 D2).
    The identity path is VIABLE. Coverage must be RE-MEASURED against GARAGE entries + family-room arrival,
    not the front door. The face-INDEPENDENT approach-track arm is a DEFERRED, gated-on-value FALLBACK
    (EXTERIOR-GUEST-EGRESS-1, revisit only if identity proves insufficient) — operator ALREADY chose identity-first;
    do NOT re-recommend it off the 7%.'
- id: ARRIVAL-DEPARTURE-NOTIFY-1
  title: '"Oji arrived/left" notifications from egress person_id'
  status: inbox
  thread: notifications
  created: 2026-08-18 09:45
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — no confidence gate today (display/notify class); operator to confirm no gate before build'
  sequence: 1  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  confidence_gate: 'None gating the FIRE (graceful-anonymous — notify on every crossing, name when known),
    though naming the person reads best at >=0.75. Lowest-risk consumer: a notification cannot escalate or
    actuate, so no safety gate applies.'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  audit_2026_08_28: 'Post-ship consumer-gap audit (2026-08-28): producer now BUILT (v5.91.4 pending deploy).
    LOWEST-RISK of the consumer gaps — the person_id is already on the ura_person_egress_event bus. Wire-in
    site is transit_validator.py:1279 (the egress event where person_id is resolved/emitted); hang an
    arrival/departure notification off it, graceful-anonymous ("someone" when NULL, the name when known).
    No trust surface. Sequence-first alongside PERIMETER-ALERT-NAME-PERSON-1.'
  refinement_status: initial
  problem: person_id is on the bus + DB row but nothing turns it into a presence notification. Lowest-risk
    build of the gaps. Fires when identity is present (Frigate face + Protect named face via webhook).
  origin: 2026-08-18 should-be-consuming audit G3
  next: 'Measure-before-build: probe the REAL egress identity rate against the GARAGE + family-room entry
    path (NOT the front door) and include Protect named face via the webhook, before scoping.'
  refs: AUDIT_census_identity_supersession_and_consumers.md §3 G3
  coverage_note_2026_08_18: 'CORRECTION 2026-08-18 (operator): the ~7% figure is NOT a coverage ceiling
    and must not be cited as one. It came from PROBE_protect_face_egress.md which measured the WRONG camera
    (front door madrone_g6_entry). Most family entries are via the GARAGE, and Protect names people in
    the FAMILY ROOM — i.e. ON the real entry path. Frigate is on ALL cameras (front overhead + doorbell
    + foyer; garage doorbell + inside-garage cam); Protect has face recognition on the front doorbell
    + family room, reachable via the LIVE Alarm Manager webhook (EXTERIOR-GUEST-FACE-FASTFOLLOW-1 D2).
    The identity path is VIABLE. Coverage must be RE-MEASURED against GARAGE entries + family-room arrival,
    not the front door. The face-INDEPENDENT approach-track arm is a DEFERRED, gated-on-value FALLBACK
    (EXTERIOR-GUEST-EGRESS-1, revisit only if identity proves insufficient) — operator ALREADY chose identity-first;
    do NOT re-recommend it off the 7%.'
- id: PWA-CENSUS-P12-RELEASE-1
  title: PWA main is ~12 commits behind — D3 exterior card (+ design/control work) unshipped
  status: waiting_operator
  thread: dashboarding
  created: 2026-08-18 10:20
  updated: 2026-08-18 10:20
  refinement_status: initial
  problem: The census D3 exterior KEEP-BOTH dashboard card lives on PWA branch census-p12-exterior-dashboard,
    which is ~12 commits AHEAD of main (main is stale). So the D3 card is NOT live on the PWA, and the
    branch also carries unrelated PWA work (design-skill rounds, PWA-CONTROL-LIST-1, M2 plan, v6.1.1).
    Shipping D3 = releasing the whole branch = a PWA-workstream release decision, not a census cleanup.
  origin: 2026-08-18 supersession/consumer cleanup surfaced the D3 "confirm PWA deploy" loose end as a
    full branch-behind-main gap
  constraints: Do NOT unilaterally merge 12 commits of PWA work to main + deploy — that is a PWA release
    with its own review/cadence.
  next: 'OPERATOR: decide the PWA release — is census-p12 THE working branch to promote to main + deploy
    (ura.phalanxmadrone.com)? If yes, run the PWA release properly (its own review). The HA dashboard
    D3 cards (v6/v8) ARE live; only the PWA leg is pending.'
  refs: ~/Code/ura-dashboard-pwa branch census-p12-exterior-dashboard; CENSUS-ACCURACY-1 D3
- id: CENSUS-FACE-RESOLVER-MIGRATE-1
  title: Route presence face-confirmed-arrival through the _2-suffix resolver (bucket-2 wire)
  status: done
  thread: presence
  created: 2026-08-18 10:35
  updated: 2026-08-23 14:30
  refinement_status: initial
  problem: presence.py:4557 (_get_face_for_camera, live caller at :4525, v3.19.0 face-confirmed arrival)
    builds f"sensor.{base}_last_recognized_face" WITHOUT _2-suffix tolerance, so it silently misses cameras
    whose Frigate face sensor exists only as the disambiguated _2 variant (per "frigate 1 retired 2 suffix").
    The feature is USEFUL — not dead — it just lacks the resolver the rest of the code got in v5.80.0.
    Bucket-2 (keep + wire), NOT a delete.
  origin: 2026-08-18 supersession three-bucket triage (bucket 2) surfaced this useful-but-underwired path
  constraints: Behavior change (starts hitting _2 faces on _2-only cams) — own reviewed hotfix, NOT a
    cleanup commit.
  next: DONE 2026-08-23 — L1 PASS confirmed in README_v5.82.1. Shipped v5.82.1.
  refs: presence.py:4557,4525; camera_census._resolve_face_entity_id:2470; reference_frigate1_retired_2suffix_permanent.md
  phase_2026_08_18: 'IN PROGRESS (operator approved all 3 to execute 2026-08-18): build (Tier-1, worktree
    face-resolver-migrate)'
  shipped_2026_08_18: SHIPPED v5.82.1 + LIVE. L1 PASS (boot clean, zero URA ERROR, house_state available).
    Route presence _get_face_for_camera through _resolve_face_entity_id; review SHIP-with-fix (hollow
    anchor -> real-PersonCensus smoke test, mutation-drilled; except narrowed). L2 (_2-only cam resolves)
    organic Wed.
  shipped_version: v5.82.1
  organic_evidence:
  - '2026-08-23 watch-pass: git commit 81ddfccb7 records "v5.82.1 live validation L1 PASS write-back;
    face-resolver card shipped_organic". README_v5.82.1 live-validation table confirms L1. H1 confirmed.'
- id: GUEST-COUNT-DEDUP-MIGRATE-1
  title: ZoneGuestCountSensor uses naive subtractive guest count — migrate to deduped union
  status: shipped_organic
  thread: presence
  created: 2026-08-18 11:00
  updated: 2026-08-23 14:30
  refinement_status: initial
  problem: aggregation.py:5983 ZoneGuestCountSensor._get_guest_count derives guest count the NAIVE SUBTRACTIVE
    way (max(0, camera_total - ble_total)) — the SAME additive/subtractive formula behind the historical
    GUEST double-count. It is superseded by the census deduped unidentified_count (the _cross_correlate_persons
    union). Its attribute-only sibling binary_sensor.py:1584 (audit S4) uses the identical formula. Found
    by the DOMAIN-WIDE supersession sweep — the cycle-scoped pass missed it because it is PRE-EXISTING
    code, not in the new cycle files.
  origin: '2026-08-18 domain-wide supersession sweep (operator: "it is the code that came before")'
  constraints: Tier 2-DB — census<->guest<->presence ripple, exactly the trust-hierarchy class of the
    original double-count. VERIFY whether ZoneGuestCountSensor is a LIVE registered entity first (the
    group README claimed no such sensor exists in sensor.py — it lives in aggregation.py; reconcile that
    discrepancy).
  next: Verify ZoneGuestCountSensor live-status; then migrate _get_guest_count (+ binary_sensor.py:1584
    sibling) to consume the deduped unidentified_count instead of the naive subtraction. Producer/consumer
    check both.
  refs: aggregation.py:5983; binary_sensor.py:1584; camera_census._cross_correlate_persons; AUDIT_census_identity_supersession_and_consumers.md
    ADDENDUM
  phase_2026_08_18: 'IN PROGRESS (operator approved all 3 to execute 2026-08-18): planning (Tier 2-DB,
    investigate-first: verify ZoneGuestCountSensor live-status)'
  plan_verdict_2026_08_18: 'PLAN DONE (PLANNING_guest_count_dedup_migrate.md, Tier 2-DB). ZoneGuestCountSensor
    IS live-registered (disabled-by-default per zone, aggregation.py:388/464) — NOT moot; corrects the
    group README "no such sensor" claim. Sibling guest_count attr on URAUnexpectedPersonSensor (binary_sensor.py:1510-1585)
    is ENABLED-by-default. Both migrate max(0,camera-ble) -> census unidentified_count. ZERO code consumers
    (display only) -> Tier 2-DB not Tier 3. Invariant I-GC + discriminating divergence test. No new knobs/consts/signals/DB.
    Adjacent is_on alert-path -> UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1. Next: plan review -> build.'
  shipped_2026_08_18: 'SHIPPED v5.83.0 + LIVE. L1 PASS (boot clean, zero ERROR). 3 naive max(0,camera-ble)
    sites -> deduped house.unidentified_count + shape-invariant guard. Tier 2-DB: 3 reviews (B found missed
    3rd site) + H1 fix-up + re-review SHIP. ble_identified attr retired (zero consumers). is_on -> UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1.'
  shipped_version: v5.83.0
- id: SECURITY-CENSUS-UNKNOWN-WIRE-1
  title: Security unknown-person auto-lock is designed but UNWIRED to the census
  status: parked
  parked_2026_09_03: 'DEFER + MEASURE (operator 2026-09-03). Auto-locking on an unknown-person count is only
    as safe as the unknown-count false-positive/false-negative rate — wiring it before that rate is measured
    risks spurious locks (FP) or missed intruders (FN). Revive trigger: measure the census unknown-count FP/FN
    rate (a read-only recorder/ground-truth probe) FIRST; wire only if the rate is acceptable. Gated on the
    egress-identity producer landing anyway.'
  thread: security
  created: 2026-08-18 11:20
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.9 confidence gate before build'
  sequence: 2  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  confidence_gate: '>=0.9 and SUPPRESS-ONLY for any identity role here. Escalation must come from the census
    unidentified_count (an UNKNOWN present), NOT from egress identity — identity only DE-ESCALATES ("that
    unknown is actually resident Oji, do not lock"). Never actuate the lock off identity (§5.5 doctrine).'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  audit_2026_08_28: 'Post-ship consumer-gap audit (2026-08-28): confirmed the security census path is
    DORMANT/UNWIRED today — _build_context never sets a "census" key, no census_update intent is emitted
    anywhere, and security is not a SIGNAL_CENSUS_UPDATED subscriber. So this is not just "identity not
    consumed", the whole census->security edge is inert. When wired: escalation comes from unidentified_count
    (footgun-gated per the existing constraints), and egress identity only de-escalates at >=0.9. Producer
    now BUILT (v5.91.4 pending deploy) but this stays investigate-first, Tier 2-DB.'
  refinement_status: initial
  problem: 'security.py SanctionChecker has an unknown-person path (has_unknown_persons / _handle_census_intent
    -> locks ALL doors) that is a DESIGNED-BUT-INERT consumer: unknown_present has ZERO producers repo-wide,
    no source="census_update" intent is emitted anywhere, and security is NOT a SIGNAL_CENSUS_UPDATED
    subscriber. A real security capability built to consume the census was never wired. Now that the census
    unidentified_count is reliable (post-arc), it COULD be wired — but must not be done naively. Found
    by the tier/coordinator semantic sweep (missed by all census-anchored passes).'
  origin: '2026-08-18 tier+coordinator supersession sweep T1 (operator: "did you look at room/zone/house
    code and coordinators?")'
  constraints: 'FOOTGUN — this AUTO-LOCKS ALL DOORS. A bare unidentified_count>0 -> lock-all trigger would
    auto-lock on a guest false-positive, given the arc''s own double-count history. INVESTIGATE-FIRST:
    must gate on guest-composition confidence + a kill switch + a measured false-positive rate BEFORE
    wiring. Do NOT wire a raw count->lock edge. Tier 2-DB (security x census x guest ripple).'
  next: 'Investigate-first: confirm the SanctionChecker intent contract + what a safe producer looks like
    (confidence-gated, kill-switched). Do NOT build a raw count->lock. Then Tier 2-DB plan.'
  refs: security.py SanctionChecker / has_unknown_persons / _handle_census_intent; AUDIT_census_identity_supersession_and_consumers.md
    ADDENDUM 2 (T1)
- id: UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1
  title: URAUnexpectedPersonSensor.is_on uses naive camera>ble substrate — ALERT path, dedup it
  status: inbox
  thread: security
  created: 2026-08-18 14:40
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.9 confidence gate before build'
  sequence: 2  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  confidence_gate: '>=0.9 for egress person_id used as CORROBORATION. This is a live ALERT path (drives NM),
    so a wrong identity that subtracts a real unknown would suppress a genuine alert — highest bar,
    corroboration-only, never sole authority (§5.5 doctrine).'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  audit_2026_08_28: 'Post-ship consumer-gap audit (2026-08-28): beyond the census-union dedup already carded,
    egress person_id at >=0.9 can serve as CORROBORATION to subtract a phone-left-behind resident from the
    "unexpected person" count (a resident whose phone stayed home reads as camera>ble => false unexpected;
    a door-identification of that same resident is strong evidence to subtract them). Because this is the
    live ALERT/NM path, identity is corroboration-only and >=0.9 — never the sole reason to suppress.
    Producer now BUILT (v5.91.4 pending deploy).'
  refinement_status: initial
  problem: binary_sensor.py:1540-1560 URAUnexpectedPersonSensor.is_on computes "unexpected person" via
    the naive substrate comparison camera_total > ble_total — the SAME additive/subtractive bug class
    as the guest double-count, but on a TRUST/ALERT path (enabled-by-default, drives NM alerts), not display-only.
    A guest-FP mechanism here fires a false "unexpected person" alert. HIGHER STAKES than the display-only
    guest_count migration (GUEST-COUNT-DEDUP-MIGRATE-1). Found by the guest-count planner as an adjacent
    finding.
  origin: 2026-08-18 guest-count-dedup planning surfaced the is_on alert-path naive-substrate use
  constraints: Own producer/consumer analysis — it drives the sensor core state + has an NM/alert consumer
    surface. Consider consolidating with GUEST-COUNT-DEDUP-MIGRATE-1 or sequencing right after. Tier 2-DB.
  next: Producer/consumer check on is_on + its NM alert consumers; then migrate the naive comparison to
    the deduped census signal. Likely higher priority than the display count.
  refs: binary_sensor.py:1540-1560; PLANNING_guest_count_dedup_migrate.md (adjacent finding)
- id: ROOM-NAME-DESYNC-RECOVER-1
  title: Recover stalled room-rename write-through build (worktree vanished, salvage branch exists)
  status: inbox
  thread: config
  created: 2026-08-18 16:40
  updated: 2026-08-18 16:40
  refinement_status: initial
  problem: 'The ROOM-NAME-DESYNC-1 builder (running ~5 days) reported complete D1+D2+D3 work but its worktree
    was REMOVED mid-run before it could commit — that fresh copy is LOST. A prior orchestrator WIP-salvage
    exists on branch main-local/build/room-rename-writethrough (2026-08-14, "state unverified"): 3 rename
    write-through sites in config_flow.py + _migrate_room_zone_name_writethrough boot migration in __init__.py
    + test_room_rename_writethrough.py (516 lines). Agent flagged: test-source-slice collision with test_v5_7_1_energy_precool
    TestD5Migration (fixed via string literals), FakeOptionsFlow lacks async_abort, and a D2 wiring simplification
    (single call at top of async_setup_entry vs 4 per-listener calls).'
  origin: 2026-08-18 stalled room-rename builder returned; worktree gone, uncommitted work lost; salvage
    branch found
  next: 'Recover: checkout main-local/build/room-rename-writethrough into a fresh worktree, VERIFY state
    (run the 13 tests + falsifier drills + suite baseline-diff — the agent claimed green but it is unverified
    post-vanish), reconcile the 3 report-before-deviating findings, then normal review->deploy. Do NOT
    trust the vanished agent summary as validation.'
  refs: main-local/build/room-rename-writethrough; ROOM-NAME-DESYNC-1
- id: CHATTER-CAMERA-CONFIDENCE-FLAP-1
  title: Camera/AI detection confidence-flap detector (STEP sibling)
  status: parked
  thread: diagnostics
  created: 2026-08-18 16:45
  updated: 2026-08-18 16:45
  refinement_status: initial
  program: sensor-trust-exclusion
  problem: Camera/AI person/motion detections are EXCLUDED from the physics chatter detector by the provenance
    gate (they flip at frame cadence on a confidence threshold — no hardware blind-time, so sub-T_floor
    is undefined; D0 shows binarygroup_camera_motion_zone1 = 14,216 sub-0.5s events working AS DESIGNED).
    But they CAN still malfunction — a confidence-flap fault class. Needs its own detector keyed on confidence
    oscillation / detection instability, NOT dwell-time.
  origin: 2026-08-18 chatter provenance-gate justification — excluded classes get their own detectors
    (coverage not dropped)
  parked_reason: Distinct fault class + criterion from physics chatter. Card after the STEP shared primitive
    + chatter client ship. Consumes the same SensorExclusionSet.
  next: 'After STEP D1/D2 ship: design a confidence-flap criterion for camera/AI detections; reuse the
    shared exclusion primitive.'
  refs: PLANNING_sensor_health_surfacing.md §D2 provenance gate; PROBE_sensor_chatter_definition_handcheck.md
- id: SENSOR-MULTISTATE-FAULT-1
  title: Bed/multi-state sensor fault detector (STEP sibling)
  status: parked
  thread: diagnostics
  created: 2026-08-18 16:45
  updated: 2026-08-18 16:45
  refinement_status: initial
  program: sensor-trust-exclusion
  problem: Bed / multi-state sensors (empty->sitting->lying) are EXCLUDED from the physics chatter detector
    — their transitions are not gated by a single motion blind-time, so the sub-T_floor criterion is ill-defined.
    They can still fault; needs a criterion suited to multi-state transition validity, not binary dwell-time.
  origin: 2026-08-18 chatter provenance-gate justification — excluded classes get their own detectors
  parked_reason: Distinct sensor model + criterion. Low priority (few such sensors). Card after STEP primitive
    ships; reuse SensorExclusionSet.
  next: 'After STEP D1/D2 ship: assess multi-state fault criteria; reuse the shared exclusion primitive.'
  refs: PLANNING_sensor_health_surfacing.md §D2 provenance gate
- id: FAN-RECHECK-NOT-CLEARING-1
  title: Fan-interference recheck not clearing held rooms — fans should pause to unhold, not happening
  status: done
  thread: presence
  created: 2026-08-19 03:15
  updated: 2026-08-19 03:40
  refinement_status: initial
  problem: 'Operator confirmed NO people in Study A or Living Room, but both are held occupied by fan-interference
    while fans run. The fan-recheck is SUPPOSED to briefly pause the fan to see if mmWave drops (proving
    fan-shake vs real person) and unhold the room — it is NOT working. Live 2026-08-19 03:xx (house=sleep):
    Living Room motion off 3.5h + camera off, held at L3 fan-interference (fan on); Study A mmWave held
    ON 52min (fan on, camera motion off 6h) — mmWave toggling every FEW MINUTES (not chatter). fan_interference_hold_s=300.
    Rooms not decaying.'
  origin: '2026-08-19 operator: "no people in those 2 rooms but fans are supposed to pause to unhold.
    Why isnt it working?"'
  next: 'Investigation (read presence_fan_recheck.py + live config): (1) is fan-recheck enabled + are
    these rooms fans mapped for it? (2) what triggers the pause + at which ladder rung (Living Room=L3,
    Study A=none)? (3) why has no pause fired / did it fire and mmWave not drop? (4) config-vs-code verdict
    + the fix. Do NOT assert mechanism without reading code + live traces.'
  refs: 'presence_fan_recheck.py; ura-presence-fusion-reference; memory: mmwave fan-noise mitigations
    (v4.7.21)'
  resolved_2026_08_18: 'INVESTIGATION DONE. Root cause: over-scoped house-wide SLEEP veto (presence_fan_recheck.py:373)
    + comfort-fan/mmWave feedback loop. Complete cause list: (a) guest window = substantive BLE/tick gate
    NOT a state block (no guest veto in code); (b) home_night 120s < 150s recheck minimum; (c) sleep veto.
    Fix = scope sleep veto to bedrooms -> FAN-RECHECK-SLEEP-VETO-SCOPE-1 (D0-verified GO). Latent precedence
    gap -> STUDYA-ZIGBEE-MMWAVE-MISCATEGORIZED-1. NOT caused by todays census/identity ships.'
- id: FAN-RECHECK-SLEEP-VETO-SCOPE-1
  title: Scope fan-recheck SLEEP veto to bedroom/keep-fan-on rooms (unhold empty non-bedroom rooms)
  status: parked
  thread: presence
  created: 2026-08-19 03:40
  updated: 2026-08-19 05:15
  refinement_status: refined
  problem: 'Fan-interference recheck is blocked house-wide by a hardcoded SLEEP veto (presence_fan_recheck.py:373-375,
    mirrored :854-855), so empty NON-bedroom rooms held by fan-shake mmWave (Study A, Living Room) never
    get their fan paused/rechecked during sleep and never decay. ROOT: over-scoped veto + comfort-fan
    feedback loop (false mmWave presence -> comfort fan on -> airflow shakes mmWave -> presence sustained).
    Pre-existing (v4.7.x), NOT caused by the census/identity cycles.'
  origin: 2026-08-19 operator live check "Study A + Living Room not decaying — did our work make occupancy
    worse?" (it did not)
  constraints: 'Tier 2-DB (presence<->hvac_fans). D0 VERIFIED (AUDIT_fan_recheck_managed_fan_d0.md): both
    fans FanController-managed (fix actuates, no no_managed_fan bail); recheck observes the correct mmWave
    per room (Study A Zigbee, Living Room Screek via CONF_MMWAVE_SENSORS=presence_sensors); pausing the
    fan breaks the loop. Guest-window (a) is NOT a state block (no guest veto in code) — do NOT widen
    eligible states. home_night-too-short (b) is CURED by the same fix.'
  knobs: Reuse the EXISTING v4.7.13 bedroom keep-fan-on-through-sleep predicate (hvac_fans.py:1205-1209
    area) — verify its exact callable, do NOT fabricate. No new knob unless the predicate is not reusable
    as-is.
  next: 'Plan the veto-scoping (Tier 2-DB): narrow SLEEP veto at :373-375 + :854-855 to bedroom/keep-fan-on
    rooms via the reused predicate. Then plan review -> build. Live-validate: Study A + Living Room decay
    during sleep once fan paused + mmWave drops; bedroom fans STILL held.'
  refs: AUDIT_fan_recheck_not_clearing.md; AUDIT_fan_recheck_managed_fan_d0.md; presence_fan_recheck.py:373/854;
    hvac_fans.py:1205
  rescope_2026_08_19: 'RE-SCOPED + PARKED pending FAN-RECHECK-OBSERVABILITY-1. The trace proved the sleep
    veto is NOT the only blocker: the recheck also failed to arm for an ELIGIBLE empty room during the
    guest window (no sleep veto then), for an UNOBSERVABLE reason. So the sleep-veto scope fix alone may
    NOT clear these rooms. Build observability FIRST, root-cause the silent non-arm, THEN scope the fix
    correctly (may be more than the sleep veto). The sleep-veto over-scope is still a real bug to fix
    — but not in isolation.'
  parked_reason: Gated on FAN-RECHECK-OBSERVABILITY-1 — do not build the veto scope until we can see why
    the recheck silently does not arm.
  studya_release_evidence_2026_08_19: 'LIVE NATURAL EXPERIMENT (Study A release, 2026-08-18/19): mmwave
    dropped ON ITS OWN at 23:00:17 while the fan was STILL running; comfort fan decayed off 23:14:21 (14min
    later). The RECHECK NEVER FIRED — the room self-cleared via the Zigbee mmwave sensors own internal
    timeout, NOT URA. Confirms the recheck silently is not arming (more evidence for observability-first).
    Quantified harm: false hold lasted ~1h43m (21:17->23:00) during SLEEP with the comfort fan needlessly
    running; a working recheck would clear in ~150s. Refines the model: fan-shake sustains a LONG time
    (~1.7h) but not literally forever. Value case for the fix: 1h43m -> 2.5min.'
  livingroom_release_evidence_2026_08_19: 'LIVING ROOM release (2026-08-18/19) — CLEAN fan-pause natural
    experiment: fan turned OFF 23:00:05 (HVAC/cooling satisfied, NOT the recheck, Screek still on), then
    Screek presence+still_target cleared 23:06:01 (~6min later). PROVES fan-shake causation (remove shake
    -> clears). Recheck NEVER fired here either. NEW DESIGN GAP: the Screek took ~6min to decay after
    shake stopped, but the recheck observation window is only ~90s (SPINDOWN 30s + WINDOW 60s). So even
    a WORKING recheck would pause the LR fan, check at 90s, find the Screek STILL ON, and FALSE-NEGATIVE
    (conclude real person). The recheck window is calibrated SHORTER than the Screek still-target decay
    -> must measure per-sensor post-shake decay and size the window accordingly, else the fix false-negatives
    on slow mmwave. (Study A Zigbee decayed differently — dropped with fan still on — so decay behavior
    is per-sensor.)'
  window_decay_gap_2026_08_19: 'OPEN QUESTION (not a proven requirement — operator: causality unclear):
    Living Room Screek cleared ~6min after fan-off. UNKNOWN why 6min — could be the Screek still-target
    hold, a real still person, HVAC airflow, or coincidence. Do NOT assume the recheck 90s window is "too
    short" as established fact. IF the 6min is genuinely the sensor post-shake decay, the window matters;
    but that causality is UNPROVEN. Measure per-sensor fan-off->clear lag before concluding.'
  superseded_2026_08_19: 'SECONDARY now — the PRIMARY bug is the D2 deadlock (FAN-RECHECK-D2-DEADLOCK-1,
    confirmed live): the recheck never arms at ALL because D2 starves it, independent of house state.
    The sleep-veto over-scope is real but moot until the deadlock is fixed (a recheck that never arms
    cannot be blocked by the sleep veto). Fold this into the D2-deadlock cycle or sequence after.'
- id: STUDYA-ZIGBEE-MMWAVE-MISCATEGORIZED-1
  title: Study A Zigbee mmWave mapped as motion_sensors — motion>presence precedence blocks recheck when
    it fires
  status: done
  thread: presence
  created: 2026-08-19 03:40
  updated: 2026-08-19 04:10
  refinement_status: initial
  problem: Study A Zigbee mmWave is mapped under motion_sensors; motion_detected outranks presence (coordinator.py:3065),
    so whenever that Zigbee reads ON the fused occupancy_source becomes "motion" and the fan-recheck (which
    arms only on source=="mmwave") will not arm that tick. Clean right now (Zigbee off) but a data-dependent
    hole. Latent, non-blocking for the veto fix.
  origin: 2026-08-19 D0 fan-recheck verification flagged the precedence gap
  next: 'Config/precedence review: recategorize the Study A Zigbee mmWave out of motion_sensors (into
    presence_sensors/mmwave) so a still-target read does not present as motion. Verify no other consumer
    depends on it being motion.'
  refs: coordinator.py:3065; AUDIT_fan_recheck_managed_fan_d0.md
  disproven_2026_08_19: 'DISPROVEN + CLOSED. Trace (AUDIT_fan_recheck_second_bug_and_transition_gate.md):
    Study A motion_sensors=[], the Zigbee mmwave is under presence_sensors -> occupancy_source is always
    "mmwave", never "motion". No precedence gap. Sensor categorization is CORRECT — do not change. The
    recheck non-arm has a DIFFERENT, unobservable cause.'
- id: FAN-RECHECK-OBSERVABILITY-1
  title: Surface fan-recheck veto reasons + eval counts + D2 demotion state (recheck non-arm is invisible)
  status: done
  thread: presence
  created: 2026-08-19 04:10
  updated: 2026-08-19 05:15
  refinement_status: refined
  problem: 'The fan-recheck stayed idle for a TEXTBOOK-ELIGIBLE empty room (Study A, guest window) — every
    documented veto ruled out against live state, yet it never armed. The cause is UNOBSERVABLE: _veto
    (presence_fan_recheck.py:136-139) only bumps an in-memory counter exposed on no entity/log; the per-tick
    fan-out swallows exceptions to DEBUG (presence.py:6904). Two sustain backstops (D2 mmwave-fan demotion
    mmwave_fan_demoted=false, fan-recheck idle) both SILENTLY no-opped. We cannot root-cause the silent
    non-arm without observability. MEASURE-BEFORE-BUILD: this is the prerequisite for the sleep-veto-scope
    fix + the sustained-shake work.'
  origin: 2026-08-19 fan-recheck trace — eligible room did not arm, cause invisible
  constraints: Diagnostic-only (surface existing internal state); parsimony — attributes on an existing
    recheck/diagnostic sensor, not new entities. No behavior change.
  next: Expose recheck veto reason + eval_count + last_attempt + D2 demotion state as attributes (computed
    at presence_fan_recheck.py:302-313, currently exposed nowhere) + raise the swallowed-exception log
    level. Deploy, then observe WHY the recheck does not arm on a live held-empty room.
  refs: AUDIT_fan_recheck_second_bug_and_transition_gate.md; presence_fan_recheck.py:136,302; presence.py:6904
  studya_release_evidence_2026_08_19: 'LIVE NATURAL EXPERIMENT (Study A release, 2026-08-18/19): mmwave
    dropped ON ITS OWN at 23:00:17 while the fan was STILL running; comfort fan decayed off 23:14:21 (14min
    later). The RECHECK NEVER FIRED — the room self-cleared via the Zigbee mmwave sensors own internal
    timeout, NOT URA. Confirms the recheck silently is not arming (more evidence for observability-first).
    Quantified harm: false hold lasted ~1h43m (21:17->23:00) during SLEEP with the comfort fan needlessly
    running; a working recheck would clear in ~150s. Refines the model: fan-shake sustains a LONG time
    (~1.7h) but not literally forever. Value case for the fix: 1h43m -> 2.5min.'
  livingroom_release_evidence_2026_08_19: 'LIVING ROOM release (2026-08-18/19) — CLEAN fan-pause natural
    experiment: fan turned OFF 23:00:05 (HVAC/cooling satisfied, NOT the recheck, Screek still on), then
    Screek presence+still_target cleared 23:06:01 (~6min later). PROVES fan-shake causation (remove shake
    -> clears). Recheck NEVER fired here either. NEW DESIGN GAP: the Screek took ~6min to decay after
    shake stopped, but the recheck observation window is only ~90s (SPINDOWN 30s + WINDOW 60s). So even
    a WORKING recheck would pause the LR fan, check at 90s, find the Screek STILL ON, and FALSE-NEGATIVE
    (conclude real person). The recheck window is calibrated SHORTER than the Screek still-target decay
    -> must measure per-sensor post-shake decay and size the window accordingly, else the fix false-negatives
    on slow mmwave. (Study A Zigbee decayed differently — dropped with fan still on — so decay behavior
    is per-sensor.)'
  operator_direction_2026_08_19: 'OPERATOR DIRECTION: do not just instrument around it — WORK THROUGH
    THE CODE and find the bug. Bet: it has a bug and "feels like it has never worked." Strong candidate:
    the per-tick recheck call is wrapped in a swallowed except -> DEBUG (presence.py:6904); if it throws
    every tick the recheck NEVER arms for ANY room = "never worked." A deep line-by-line lifecycle trace
    is running (arm/pause/observe/vacate). Observability is still useful but the code trace comes first.'
  bug_found_2026_08_19: 'CODE TRACE FOUND IT (AUDIT_fan_recheck_bug_hunt.md). CONFIRMED: (1) the recheck
    has NEVER vacated a room — apply_fan_recheck_release guards on outcome==VACATED and never ran; 08-13
    occupied_confirmed was the non-vacate path. (2) WHY IT SHIPPED BROKEN: hollow tests — the suite drives
    a hand-fed _FakeRoomCoord with occupancy_source="mmwave" hardcoded (test_fan_recheck_mode2_cycle.py:224),
    never exercising real occupancy-source production. SUSPECTED BUG (high): D2 mmwave-fan demotion (MMWAVE_FAN_CORROBORATION_ENABLED
    default on, coordinator.py:3451) sets occupied=False for exactly these rooms BEFORE the recheck (which
    needs occupied=True to leave idle, presence_fan_recheck.py:378) can arm; D2 defers-to-recheck only
    when recheck already non-idle -> chicken-and-egg deadlock -> never arms = "never worked". CONFIRM
    VIA: the disambiguator sensor.<study_a>_fan_recheck_state (sensor.py:15953) is ALREADY BUILT but registry-DISABLED
    — enable + read fan_recheck_veto_counts; not_occupied=deadlock, mmwave_history_short=ring issue C.
    Observability already EXISTS — do not build, just enable. Also Finding D: per-room driver shares one
    except->DEBUG (presence.py:6904) so one room raising skips all rooms after it.'
  CONFIRMED_LIVE_2026_08_19: 'CONFIRMED LIVE (no build needed — the disambiguator sensor already existed).
    sensor.living_room_living_room_fan_recheck_state: veto_counts={not_occupied:1}, eval_count=1, last_attempt
    2026-08-13. The recheck evaluated ONCE at boot, hit not_occupied, and NEVER armed again through 2h
    of held occupancy. CONFIRMS Candidate B: D2 mmwave-fan demotion sets occupied=False before the recheck
    can see occupied=True to arm; D2 defers to recheck only once recheck is non-idle -> deadlock -> recheck
    never arms = never worked. Operator instinct CONFIRMED. Card closed -> fix is FAN-RECHECK-D2-DEADLOCK-1.'
- id: FAN-SUSTAINED-SHAKE-DEMOTE-1
  title: Fan-RUNNING sustained-shake mmWave demotion (transition gate is wrong shape) — STEP sibling
  status: inbox
  thread: presence
  created: 2026-08-19 04:10
  updated: 2026-08-19 04:45
  refinement_status: initial
  program: sensor-trust-exclusion
  problem: 'The shipped v5.46.0 fan-transition gate (validated, firing, correct) covers the fan-transition-EDGE
    coincident-with-mmwave-onset phenomenon. It does NOT and cannot cover the SUSTAIN case: room goes
    occupied first, URA turns the fan on because occupied, and the fan-running shake SUSTAINS the hold
    indefinitely (until the sensor self-times-out ~1.7h later). Need an ADDITIVE fan-RUNNING sustained-shake
    demotion on the sustain path — untrust a fan-shaken mmwave vote while the fan runs and no corroboration.
    STEP sensor-trust sibling; distinct from the creation gate and from sub-T_floor chatter.'
  origin: '2026-08-19 operator: "why arent we filtering fan start impulse for mmwave as we built" — trace:
    gate is wrong shape'
  constraints: Gated on FAN-RECHECK-OBSERVABILITY-1 (see why the D2 demotion backstop no-ops first). Sibling
    of the STEP chatter detector — reuse the shared exclusion primitive. Do NOT just widen the 5s window
    (still creation-only edge); needs a sustain-path demotion.
  next: 'After observability: design a fan-running sustained-shake demotion (mmwave-sole + fan-on + no
    corroboration for a sustained window -> demote the mmwave vote). Reuse STEP SensorExclusionSet.'
  refs: AUDIT_fan_recheck_second_bug_and_transition_gate.md; const.py:828 FAN_TRANSITION_SUSPECT_WINDOW_S;
    coordinator.py:2890
  studya_release_evidence_2026_08_19: 'LIVE NATURAL EXPERIMENT (Study A release, 2026-08-18/19): mmwave
    dropped ON ITS OWN at 23:00:17 while the fan was STILL running; comfort fan decayed off 23:14:21 (14min
    later). The RECHECK NEVER FIRED — the room self-cleared via the Zigbee mmwave sensors own internal
    timeout, NOT URA. Confirms the recheck silently is not arming (more evidence for observability-first).
    Quantified harm: false hold lasted ~1h43m (21:17->23:00) during SLEEP with the comfort fan needlessly
    running; a working recheck would clear in ~150s. Refines the model: fan-shake sustains a LONG time
    (~1.7h) but not literally forever. Value case for the fix: 1h43m -> 2.5min.'
  livingroom_release_evidence_2026_08_19: 'LIVING ROOM release (2026-08-18/19) — CLEAN fan-pause natural
    experiment: fan turned OFF 23:00:05 (HVAC/cooling satisfied, NOT the recheck, Screek still on), then
    Screek presence+still_target cleared 23:06:01 (~6min later). PROVES fan-shake causation (remove shake
    -> clears). Recheck NEVER fired here either. NEW DESIGN GAP: the Screek took ~6min to decay after
    shake stopped, but the recheck observation window is only ~90s (SPINDOWN 30s + WINDOW 60s). So even
    a WORKING recheck would pause the LR fan, check at 90s, find the Screek STILL ON, and FALSE-NEGATIVE
    (conclude real person). The recheck window is calibrated SHORTER than the Screek still-target decay
    -> must measure per-sensor post-shake decay and size the window accordingly, else the fix false-negatives
    on slow mmwave. (Study A Zigbee decayed differently — dropped with fan still on — so decay behavior
    is per-sensor.)'
  correction_2026_08_19: 'CORRECTION (operator pushback + verify): the shipped v5.46.0 fan-transition
    gate is NOT broken/wrong-shape and is NOT gated off. Verified: FAN_TRANSITION_SUSPECT_WINDOW_S=5.0
    (not 0), no override, genuinely ACTUATING (sets any_sensor_active=False -> skips occupancy creation
    at coordinator.py:3059, not shadow), and it FIRED TWICE today (jaya_bedroom + exercise_room counters=1
    since 16:18 boot). Hand audit AUDIT_fan_signature_separability_probe.md (2026-08-01) validated the
    5s window for fan-transition-EDGE coincident with mmwave onset. It correctly covers that phenomenon.
    This card is for a DIFFERENT, uncovered phenomenon: onset-leads-fan + SUSTAINED running shake (room
    occupied first -> fan on because occupied -> shake sustains; Study A mmwave 1s BEFORE fan, Living
    Room latch 121s after). The creation gate legitimately cannot cover sustain. So this is an ADDITIVE
    new mechanism (fan-running sustained-shake demotion), NOT a fix to the transition gate.'
- id: FAN-RECHECK-D2-DEADLOCK-1
  title: Fan-recheck never arms — D2 mmwave-fan demotion starves it (occupied=False before it can arm)
  status: shipped_organic
  thread: presence
  created: 2026-08-19 05:15
  updated: 2026-08-23 14:30
  refinement_status: refined
  program: sensor-trust-exclusion
  problem: 'CONFIRMED BUG (live-verified): the fan-interference recheck has NEVER worked. Two mechanisms
    target the same fan-ghosted mmwave-sole rooms and deadlock: D2 mmwave-fan demotion (MMWAVE_FAN_CORROBORATION_ENABLED
    default on, coordinator.py:3451) sets occupied=False; the recheck needs occupied=True to leave idle
    (presence_fan_recheck.py:378); D2 only defers-to-recheck once recheck is non-idle. So D2 pre-empts
    occupancy every tick -> recheck never arms. Live proof: Living Room recheck sensor veto_counts={not_occupied:1},
    eval_count=1, last real arm 2026-08-13. Never vacated a room (apply_fan_recheck_release guards outcome==VACATED,
    never ran). Shipped undetected because the tests drive a hand-fed _FakeRoomCoord with occupancy_source
    hardcoded (hollow anchor, test_fan_recheck_mode2_cycle.py:224).'
  origin: 2026-08-19 operator "find out why its not working, it has a bug, feels like it never worked"
    — CONFIRMED
  constraints: 'Tier 2-DB (presence<->hvac_fans, two interacting trust mechanisms). Real design reconciliation,
    NOT a one-line veto tweak. FIX DIRECTION: let the room hold occupied long enough for the recheck to
    arm->pause->test, THEN D2 demotes/recheck vacates — resolve the precedence so D2 does not pre-empt
    the recheck. MANDATORY: replace the hollow _FakeRoomCoord tests with a fixture that drives REAL occupancy-source
    production + the D2/recheck interaction (the test gap is why it shipped broken). Also fix Finding
    D: the shared except->DEBUG (presence.py:6904) so one room raising cannot silently skip all rooms
    after it.'
  next: 'PENDING 2026-08-23: awaiting organic fan-ghost episode to observe O1 (eval_count increments),
    O3 (ARMED state), O4 (real vacate). Recorder outside 7-day window for older evidence. Watch sensor.<room>_fan_recheck_state
    next encounter.'
  refs: AUDIT_fan_recheck_bug_hunt.md; coordinator.py:3451/3370; presence_fan_recheck.py:378/688; test_fan_recheck_mode2_cycle.py:224;
    live sensor.living_room_living_room_fan_recheck_state
  phase_2026_08_19: 'IN PROGRESS: planning the D2<->recheck precedence reconciliation (Tier 2-DB, folds
    in sleep-veto)'
  staged_ready_2026_08_19: 'STAGED + SHIP-READY, DEPLOY HELD for non-hostile timing. Merged to develop;
    3 Tier-2-DB reviews ALL SHIP (A correctness, B deadlock-break PROVEN + no inverse deadlock, C SHIP-with-live-validation);
    F-C-2 hollow D3 test fixed (extracted helper + real behavioral test + mutation drill); 58 tests. README_v5.84.0
    written with the 8-observation live-validation spec (O1-O8, incl. O7/O8 over-defer discriminators).
    HOLD REASON: deploy = HA restart (~5min whole-house outage) and residents are asleep (house=sleep,
    ~6am). Deploy at a non-hostile hour / on operator go. Behavioral proof is LIVE (real-coord in-suite
    = v5.8.0 territory, seam adjudicated acceptable by Review C).'
  shipped_2026_08_19: 'SHIPPED v5.84.0 -> HOTFIX v5.84.1 (v5.84.0 shipped a presence-startup UnboundLocalError:
    D3 refactor moved a CONF_ENTRY_TYPE ref past a pre-existing shadowing function-local import; L1 caught
    it; root-caused + sibling-scanned + one-line un-shadow fix). v5.84.1 L1 PASS: fresh boot, zero URA
    ERROR, presence started + inferring (boot_settle real_input, census 2, tracked 4). Incident closed.
    L2 (recheck eval_count climb + real vacate) organic-pending a fan-ghost episode. Follow-up: SHADOW-IMPORT-AUDIT-1.
    Lesson: exactly the coordinator-integration test gap TEST-STRATEGY-REARCH-1 exists to close (real-coord
    harness would catch at author time).'
  shipped_version: v5.84.0
  organic_evidence:
  - '2026-08-23 watch-pass: fix shipped v5.84.0; acceptance criteria O1-O6 require a fan-ghost episode
    (sensor.ROOM_fan_recheck_state reaching ARMED+vacate). Pre-deploy last eval_count=1 and never vacated
    per README. Awaiting organic fan-ghost episode. H1 PENDING.'
- id: TEST-HARNESS-REAL-HA-DEFAULT-1
  TRUE_COST_MEASURED_2026_08_23: 'THE REAL NUMBER, replacing the misleading 9,733. Full suite under .venv-ha
    with the blocker disabled (`-p no:homeassistant`): stub run (Python 3.9.6)   76 failing IDs   3:41
    real-HA run (3.13.14)    273 failing IDs   7:01 fail ONLY under real HA  207 fail ONLY under stubs     10   <-
    real HA FIXES these So a wholesale switch costs 207 new failures and fixes 10. That CONFIRMS the strangler
    migration and RULES OUT a flag-day switch. The earlier reviews calling this "a large infrastructure
    project" were directionally right about the MIGRATION even though they were wrong about the BLOCKER
    (which is one plugin fixture, see THE_BLOCKER_NAMED). Real-HA runtime is ~1.9x slower (7:01 vs 3:41)
    — real imports cost real time. Relevant to any proposal that the whole suite runs on it by default.
    Failing-ID list saved: docs/planning/artifacts/test_strategy/baseline_real_ha_273.txt NOTE the 10
    stub-only failures are the most interesting rows in the whole exercise: tests that FAIL against fakes
    and PASS against the real thing. Those should be read before any of the 207 — they are cases where
    the stub architecture was actively lying.'
  title: Make the real-HA venv the default test harness — the blocker is ONE plugin fixture, not the "large
    infrastructure project" every review doc assumed
  thread: quality
  status: planned
  approval: explicit
  approved_by: operator 2026-08-23 ("We need to make this our default test harness and strategy")
  created: 2026-08-23 18:20
  updated: 2026-08-23 18:20
  refinement_status: initial
  tags:
  - measure-before-build
  - institutional-context
  problem_solution:
  - 'Problem: the test suite runs on Apple system Python 3.9.6 with no Home Assistant installed, so 230
    of 429 test files hand-build fake HA modules. That scaffolding is the direct cause of most of the
    suite''s recurring breakage. A working environment with real HA has existed since 2026-07-06 and nothing
    points at it. Solution: point the suite at the existing environment and let new tests import the real
    thing, migrating old files only when a cycle already touches them.'
  PRIOR_ART_RESPECTED: 'Nobody was wrong, and this card does NOT override earlier work. * `.venv-ha` was
    built 2026-07-06 with real HA and never wired in. * `_HA_REAL` exists in 3 files as a SKIP-WHEN-ABSENT
    gate — real HA was designed as an OPTIONAL enhancement for a few behavioural tests, never as the default.
    A coherent choice. * SUITE-HYGIENE-1 censused ~155 files / 98 sys.modules keys and built snapshot-restore,
    but DELIBERATELY excluded `homeassistant.*` on MEASURED grounds recorded in conftest.py: "measured:
    7 fixed flakes vs 7 new regressions = net zero, when included". A collection-time hook was prototyped
    and rejected: "broke 27 collection imports". Those measurements remain VALID — they describe the stub
    architecture, which keeps existing during migration. That work made stub-sharing safer; this card
    removes the need for stubs.'
  THE_BLOCKER_NAMED_2026_08_23: 'Every review doc calls this "a large infrastructure project" because
    switching appeared to break everything: the full suite under the real-HA venv gives 1 passed / 26
    skipped / 9,733 ERRORS. IT IS NOT THE TESTS. Individually they pass under real HA: test_energy_battery.py            166/166
    alone + test_notification_manager.py    274/274 together + a known stub-donor, both orders 174/174
    THE ACTUAL BLOCKER IS ONE FIXTURE: pytest_homeassistant_custom_component/plugins.py:342, autouse `enable_event_loop_debug`,
    calls `asyncio.get_event_loop().set_debug(True)`. Under Python 3.13 with HA''s HassEventLoopPolicy
    that raises, so EVERY test errors at SETUP. PROOF: `-p no:homeassistant` -> test_notification_manager.py
    108/108 passed (was 108 errors). NOTE THE NAME. phcc registers under the pytest11 entry-point name
    `homeassistant`, NOT its package name. I first tried `-p no:pytest_homeassistant_custom_component`,
    got a byte-identical 9,733-error result, and briefly concluded the phcc hypothesis was REFUTED. It
    was a false negative from disabling a plugin that does not exist under that name. Recorded because
    the same mistake would cost the next person the same hour. ALSO MISSING, and a real gap either way:
    there is NO pytest config in the repo — no pytest.ini, setup.cfg, tox.ini, or [tool.pytest]. So plugins
    autoload uncontrolled and asyncio_mode is unset. That is why the two environments diverge at all:
    under 3.9 phcc is not installed, so its autouse fixture never runs.'
  next: 'D1 (one file): add a pytest config pinning asyncio_mode and either disabling the phcc plugin
    or overriding enable_event_loop_debug, then re-run the full suite under .venv-ha and record the real
    failure count. That number — NOT the 9,733 — is the true cost of switching, and it decides everything
    downstream. Do not scope the migration before it exists.'
  PROPOSED_SHAPE: 'Strangler, not a switch. 1) Pin .venv-ha as the documented interpreter in requirements_test.txt
    + run instructions so nobody silently runs 3.9 again. 2) New tests import real HA; no new file adds
    sys.modules stubs. 3) Existing files migrate ONLY when a cycle already touches them. 4) Track BOTH
    numbers (stub-suite, real-suite) until parity. EVIDENCE THIS PAYS: requirements_test.txt records the
    same pattern twice — installing real pytest-asyncio dissolved ~180 phantom failures, and real aiosqlite
    ~30 more, because `sys.modules.setdefault("aiosqlite", MagicMock())` made db.initialize() appear to
    succeed while writing nothing. Its own words: "dev env drift hiding real failures behind mock no-ops."'
  links:
    related:
    - TEST-STRATEGY-REARCH-1
    context: TEST-STRATEGY-REARCH-1 measured the STUB suite (158 -> 76 failing IDs, hollow-anchor rate
      62%). This card removes the architecture those measurements describe. Its remaining 76 failures
      should be re-counted under the real harness BEFORE being worked individually — some will dissolve,
      exactly as the pytest-asyncio and aiosqlite installs did.
- id: TEST-STRATEGY-REARCH-1
  title: Investigate + possibly re-architect the automated test strategy (never examined; slow + collides
    + hollow at boundaries)
  status: inbox
  thread: platform
  created: 2026-08-19 07:45
  updated: 2026-08-19 08:15
  refinement_status: initial
  problem: 'The test strategy grew organically to ~9000 tests and has NEVER been examined as a whole.
    Three costs surfaced repeatedly this session: (1) 4+ MINUTE full-suite runs; (2) PARALLEL COLLISIONS
    — tests overwrite shared sys.modules / entity_registry functions (census pollution, energy-precool
    source-slice collision, presence order-dependent failures), so they cannot run in parallel and a PreToolUse
    hook serializes pytest; (3) HOLLOW FAKES AT THE COORDINATOR BOUNDARY — the _FakeRoomCoord pattern
    (occupancy_source hardcoded) let cross-coordinator INTEGRATION bugs ship (fan-recheck deadlock lived
    5 days; STEP C-CRIT tick-site/fusion source-string-anchored, not behavioral; census double-count).
    Could plausibly be SIMPLIFIED now — the growth was accretive, likely redundant/slow/polluting tests
    + a better shared-fixture model.'
  origin: '2026-08-19 operator: "card investigating and possibly rearchitecting our automated test strategy
    ... 4+ minute runs with collisions if we try to go parallel"'
  constraints: 'INVESTIGATION-FIRST — examine before re-architecting; do not write a re-arch plan off
    a hypothesis. Possibly IP-grade / foundational (like the load-shedding vision-doc-first treatment).
    Measure before prescribing: profile the 4min runtime (what is slow), enumerate the pollution sites
    (what monkeypatches shared module state + does not restore), and map the fake-coordinator usages.'
  knobs: ''
  next: 'Investigation deliverable (read-only audit): (A) RUNTIME profile — what makes the suite 4+min
    (slow modules, repeated heavy setup, real sleeps). (B) POLLUTION map — every test that mutates sys.modules/entity_registry/shared
    globals without restore (the parallel blocker) + whether autouse snapshot/restore or a shared real
    fixture fixes it. (C) FAKE-COORDINATOR inventory — where _FakeRoomCoord-style hardcoding hides integration
    (the hollow-boundary class) + whether a REAL-COORD HARNESS (drives real occupancy/fusion, stubs only
    DB/dispatcher/registry, with an anti-hollow mutation gate) is feasible and would left-shift the fan-recheck/STEP
    class. (D) SIMPLIFICATION — redundant/obsolete tests, could parallelize if pollution fixed (4min->?).
    Then a tiered re-arch proposal. Subsumes SUITE-ORDER-POLLUTION-1 + the real-coord-harness idea.'
  refs: SUITE-ORDER-POLLUTION-1; scripts/hooks/pytest_serialize.sh; AUDIT_fan_recheck_bug_hunt.md (hollow
    class); step_chatter_review_C.md (C-CRIT); v5.8.0 incident memory (real-coord construction risk)
  pytest_restore_hook_2026_08_19: 'CONCRETE INSTANCE for the re-arch (D2-MED-1): a STEP cycle test source-mutates
    coordinator.py during a normal pytest run without guaranteed restore -> the batch run leaves an uncommitted
    mutation (a test that edits production source is a tree-poisoning + false-evidence hazard). Two fixes
    belong in the re-arch: (1) any source-mutating test MUST restore via a finally/autouse-fixture (or
    better, not mutate source at all — use monkeypatch/injection); (2) a pytest_sessionfinish hook that
    unconditionally git-checkouts the component dir after a run, as a backstop. Also the C-re LOW test-order
    stub-completeness (homeassistant.util missing) is another pollution instance. These are the parallel-collision
    root cause in miniature.'
- id: CHATTER-OBSERVE-CONTROL-D7-1
  SHADOW_RESULT_2026_08_22: 'Was `in_progress`. SHADOW HAS RUN AND ITS RESULT IS IN — and it is not a
    tuning problem. Shadow mode shipped v5.85.0, T_floor was raised 1.0 -> 5.0 on 2026-08-20 to widen
    the net, and it detected NOTHING across the house. Measurement on 2026-08-21/22 explains why: the
    detector counts SUB-T_floor impossibility events, and this house actual chatter is SUSTAINED MODERATE
    RATE (kitchen mmWave: 731 presence transitions in 48h, median interval 44.4s, only 25 sub-5s events
    ~0.5/hr — it can never accumulate K=10 in a window). See CHATTER-RATE-VS-BURST-GAP-1. So this card
    is BLOCKED on a value decision (is a RATE-based signal worth building at all), not on more shadow
    observation. Continuing to run shadow accumulates no further information. Moved to waiting_operator.'
  title: 'STEP D7: chatter observe+control panel + shadow-first rollout (2-day forcing gate)'
  status: waiting_operator
  thread: diagnostics
  created: 2026-08-19 09:00
  updated: 2026-08-19 10:55
  refinement_status: refined
  program: sensor-trust-exclusion
  problem: 'STEP chatter shipped default-ON quarantine (ACTS on occupancy) but you can neither WATCH its
    performance nor REACH its knobs from where you would watch: control is buried in an options-flow step
    (async_step_coordinator_notifications_volume, config_flow.py:6554) — not device entities; observability
    is only binary quarantined-state + a one-shot NM (no burst distribution, no near-miss/FP-imminent,
    no false-negative visibility, no shadow mode). A feature built to be TUNED BY OBSERVATION must be
    observable and non-acting first.'
  origin: '2026-08-19 operator: "observe the flapping detection performance ... control and observability
    surfaces and are they sufficient" -> NO; then "co-locate ... house level or room level".'
  constraints: 'CO-LOCATION (grounded): CONTROL=HOUSE-level (scope follows what it governs) — enable SWITCH
    on ENTRY_TYPE_INTEGRATION beside the v5.82.0 census switches (switch.py:154); K + T_floor as NUMBER
    entities (rung-3, live) on ENTRY_TYPE_COORDINATOR_MANAGER beside PeakBufferTarget/OffPeakDrain (number.py);
    per-sensor T_floor=0 stays the sensor-level kill. OBSERVABILITY=ROOM-level (sensors are in rooms)
    — burst telemetry on the per-room UnavailableEntitiesSensor (sensor.py:1677, already the reason="chattering"
    home) + optional house-level rollup on CM. Do NOT put K/T_floor per-room (40 divergent knobs).'
  knobs: switch.ura_chatter_quarantine (enable, house, integration entry) + a shadow/act MODE (shadow=detect+surface,
    do NOT exclude the vote); number.ura_chatter_burst_k + number.ura_chatter_t_floor (house, CM entry,
    rung-3 live); per-room burst-count telemetry attrs on sensor.<room>_unavailable_entities; migrate
    the options-flow fields OUT of the notifications-volume step.
  next: 'SHADOW-FIRST rollout: build D7 (switch+Numbers+telemetry+shadow mode+config-flow migration) ->
    ship STEP shadow (detect+surface, no vote exclusion, enable defaults to shadow). HARD FORCING GATE
    (operator): the 2-day clock STARTS AT SHADOW DEPLOY — MUST flip to ACTING within 2 days of that deploy,
    or DECLARE MOOT (bazillion devices = trivial stat significance; if the burst distribution does not
    cleanly separate healthy from chatter in 2 days it is moot). SCHEDULE the gate AT shadow-deploy time
    (relative, not a fixed date now). The gate is a SESSION task (read shadow burst data from HA -> decide
    -> flip switch.ura_chatter_quarantine), so the schedule fires a reminder to a session with HA access;
    the durable record is THIS card + the STEP card.'
  refs: PLANNING_sensor_health_surfacing.md; switch.py:154 (census switches); number.py (CM Numbers);
    sensor.py:1677 (UnavailableEntitiesSensor); PROBE_mmwave_healthy_cadence.md (the distribution to surface)
  build_2026_08_19: D7 BUILD dispatched (additive on STEP core; shadow default; full re-review after).
  reviews_2026_08_19: 'D7 TIER-3 REVIEWS: A+D SHIP-WITH-FIX, B+C DO-NOT-SHIP — INDEPENDENTLY CONVERGED
    on the HIGH. Boot-safety CLEAN (no repeat of the v5.84.0 import-shadow incident class). HIGH: act->shadow/off
    mode-flip leaves stale chatter exclusions (occupancy suppressed up to 900s/indefinitely) — no test
    flipped modes. B-MED: orphaned CONF_CHATTER_QUARANTINE_ENABLED (no UI to clear pre-D7 False) + phantom
    switch.ura_chatter_enabled comment. Shadow seam CONFIRMED mutation-anchored. FIX-UP dispatched: mode-transition
    release + retire old enable bool + migrate + comment fix + LOWs + 3 mode-flip tests. PROCESS LESSON:
    dispatched 2 reviewers on the SAME worktree, one running mutation drills -> A+D saw B+C mutations
    mid-flight. Mutation-testing reviewers need ISOLATED worktrees (or run sequentially). B+C restored
    clean; no harm, but a rule to codify.'
- id: SHADOW-IMPORT-AUDIT-1
  title: Audit function-local const imports that shadow module-level names (v5.84.0 incident class)
  status: inbox
  thread: platform
  created: 2026-08-19 10:20
  updated: 2026-08-19 10:20
  refinement_status: initial
  problem: 'v5.84.0 shipped an UnboundLocalError: a function-local `from ..const import CONF_ENTRY_TYPE`
    inside _run_inference shadowed the module-level import for the WHOLE function, and a moved reference
    accessed it unbound on the startup path. This bug CLASS (local import shadows module-level name; a
    sibling reference on a path that skips the local import throws) can exist anywhere with the local-import-in-a-branch
    pattern (presence.py has ~8 such local const imports). py_compile does not catch it; only runtime
    does.'
  origin: 2026-08-19 v5.84.0 presence startup incident (fixed v5.84.1)
  next: 'Audit: grep every function-local `from ..const import` where the same name is also module-level-imported
    AND referenced elsewhere in the same function; either remove the redundant local import or ensure
    it always runs first. Consider a lint rule (F823/pylint) in CI. Ties to TEST-STRATEGY-REARCH-1 (a
    real-coord harness would catch these at author time).'
  refs: presence.py:5944/6955; TEST-STRATEGY-REARCH-1
- id: HVAC-GUEST-AS-ZONE-PERSON-1
  title: A guest in an occupied guest room should count as that zone's "person" — today a zone with no
    assigned residents loses three protections entirely
  thread: hvac
  status: pre_planning
  OPERATOR_DESIGN_INPUT_OCCUPANCY_GATE_2026_08_20: 'OPERATOR, on how to stop the synthetic guest from
    pinning the zone: "We have to gate the person on occupancy for this fix. Guest mode flips on if a
    room is continuously occupied for 30m. But that might not be enough for the night. We need to know
    a person is there unlike the rooms. Maybe its enough if a recheck of the room after sleep has started
    for some continuity and then we can trust even if the sensors fail?" READING OF THE PROPOSAL — it
    is a two-stage trust ladder, and the second stage is the clever part. Stage 1 (arm): the existing
    30-minute continuous-occupancy rule establishes a person is genuinely in the guest room, not passing
    through. Stage 2 (hold through sensor failure): a RECHECK after sleep onset re-confirms continuity;
    once that recheck passes, the synthetic person is trusted even when the room sensors subsequently
    go quiet — which is exactly the night-time degradation the sleep veto exists to paper over. So the
    guest-person inherits the same "trust the person, not the room" behaviour as a real resident, but
    only AFTER earning it twice. WHY THIS IS THE RIGHT SHAPE: it answers the operator''s own earlier caveat
    ("IFF they are actually around and don''t decay") without needing a decay timer at all. Rather than
    decaying trust, it gates ENTRY to trust behind two independent confirmations at different times. A
    single blip cannot arm it (30m continuous), and a person who left before sleep cannot hold it (the
    post-sleep-onset recheck fails). OPEN QUESTIONS TO SETTLE IN PLANNING, not now: (a) how long after
    sleep onset does the recheck run, and does it need its own continuity window or a single sample; (b)
    what happens if the recheck runs while the person is briefly out of the room (bathroom) — a single-sample
    recheck is fragile there, which argues for a short window rather than an instant; (c) what releases
    the trust in the morning — house-state exit from sleep is the obvious discharge and MUST be named,
    per the suppression-needs-a-discharge rule, or this becomes another indefinite hold like S14; (d)
    does the existing fan-recheck machinery (presence_fan_recheck.py) already implement a room-recheck
    primitive this can reuse rather than inventing a second one. Check (d) FIRST — institutional-context
    rule.'
  OPERATOR_CONSTRAINTS_2026_08_20: 'Two constraints from the operator that narrow this substantially and
    should be treated as spec, not preference. (1) APPLIES ONLY IF THE ZONE HAS NO ASSIGNED PERSON — operator:
    "IFF that zone does not have a person assigned. moot if it does." So this is NOT a general guest-presence
    feature; it is a fallback that arms only for person-less zones. Today that is zone_3 alone (zone_1
    has oji + ezinne, zone_2 has ziri + jaya). This makes the blast radius one zone and removes any risk
    of a synthetic guest competing with a real resident''s tracker. It should be written as a guard at
    the top of the feature, not as a scoring tweak. (2) THE SLEEP LATCH SHOULD ALREADY DEPEND ON ACTUAL
    OCCUPANCY — operator: "At any rate zone sleep latch is supposed to depend on actual occupancy." This
    is a CORRECTNESS CLAIM TO VERIFY BEFORE BUILDING ANYTHING, and it may shrink or kill the cycle. If
    the sleep latch already keys off real occupancy rather than zone_persons membership, then one of the
    three protections this card was written to restore is not actually missing, and the card''s premise
    is 1/3 wrong. ACTION: verify the sleep-veto path (aggregation.py:4017-4019) reads occupancy vs person
    membership BEFORE scoping. Do not build against the three-protections framing until that is checked
    — the framing came from my reading, not from a test.'
  created: 2026-08-20 15:10
  updated: 2026-08-20 15:10
  refinement_status: initial
  approval: unreviewed
  tags:
  - no-fabrication-verify
  - institutional-context
  problem_solution:
  - 'Problem: several safeguards that stop a zone being set to "away" while people are in it only work
    when named household members are assigned to that zone. One zone has no one assigned, so all of those
    safeguards are switched off for it — even though it contains a guest bedroom that someone may be sleeping
    in. Solution: when a room flagged as a guest room is occupied, treat the guest as that zone''s person
    so the same safeguards apply — provided we can tell the guest is genuinely still there and the signal
    has not gone stale.'
  why: 'OPERATOR IDEA (verbatim 2026-08-20): "One way to partially fix the away is if room is occupied
    and guest is armed from the rooms is_guest flag. Guest bedroom 1 is a guest room. So technically the
    guest is the home person IFF they are actually around and dont decay." MEASURED BASIS: empty zone_persons
    makes THREE suppressions inert for zone_3 — night-trust away-suppression (hvac.py:1788-1795 iterates
    zone.zone_persons or [] so home_persons stays empty and the away flip is ALLOWED during FAN_TRUST_STATES),
    sleep veto (aggregation.py:4017-4019 `if not zone_persons: continue`), and non-sleep person-home bias
    (aggregation.py:4152-4154). Plus no pre-arrival routing (hvac.py:3267-3273). zone_3 contains Guest
    Bedroom 1 + its bathroom (confirmed from core.config_entries).'
  THE_HARD_PART: 'The operator''s own caveat is the whole design problem: "IFF they are actually around
    and dont decay." A synthetic person that never decays would pin a zone `home` forever after one transit
    blip in the guest bedroom — strictly worse than today. This needs an explicit liveness/decay contract,
    which is the suppression-needs-a-discharge class (memory feedback_suppression_needs_discharge): what
    re-affirms the guest is present, what expires the claim, what the backstop is, and what happens across
    a restart. Note it must NOT reuse the guest-MODE arming predicate — see D2-CANARY-GUEST-PREDICATE-1
    and the census/guest cluster; this is a per-ROOM is_guest flag, a different thing from house-level
    GUEST state.'
  RELATIONSHIP: 'STRATEGIC counterpart to HVAC-PRESET-FLAP-1''s TACTICAL calming. Operator scoped this
    turn explicitly: "But lets focus on calming any hvac zone that doesnt have a person attached." So
    the flap tuning goes first; this is the general fix for the person-less-zone class and should be scoped
    after, informed by whether calming alone was sufficient.'
  sibling_of: HVAC-PRESET-FLAP-1
  parsimony:
    problem: zones with no assigned person silently lose three occupancy protections, and a guest room
      in such a zone is unprotected while genuinely occupied
    verdict: SIMPLIFY
    note: operator explicitly deferred this behind the calming work; build the simple calming first and
      re-measure before deciding this is still needed
  next: 'DEFERRED behind HVAC-PRESET-FLAP-1 calming per operator. When picked up: verify the per-room
    is_guest flag name and its existing consumers (institutional-context grep) BEFORE proposing anything;
    then design the liveness/decay contract explicitly.'
  refs:
  - hvac.py:1788-1795
  - aggregation.py:4017-4019, 4152-4154
  - memory feedback_suppression_needs_discharge
- id: AC-RAMP-SAVINGS-OVERSTATED-1
  title: AC-ramp kWh-avoided TECH_DEBT entry states the wrong bias direction — one-line doc correction
    (magnitude is NOT a defect; the figure is explicitly not billing-grade)
  thread: energy
  status: done
  created: 2026-08-21 11:40
  updated: 2026-08-26 09:30
  refinement_status: initial
  approval: unreviewed
  tags:
  - measured-not-inferred
  - refutes-an-accepted-rationale
  NOT_A_NEW_DEFECT_IT_IS_KNOWN_DEBT: 'IMPORTANT FRAMING — do NOT card this as a newly discovered bug.
    It is ALREADY DOCUMENTED in docs/TECH_DEBT.md ("v4.5.11 — AC ramp-down: rough kWh-avoided estimate"),
    which states the formula and explicitly acknowledges it is "not baseline-matched against a comparable-weather-day
    counterfactual". The ACTUAL formula, verified in source, is max(0, kwh_before - kwh_after) x min(30,
    remaining_overshoot_minutes) / 60 — a DELTA times a 30-min-capped projection. (A measurement agent
    characterised it as "before_kW x duration"; numerically close because kwh_after averages only 0.07-0.26
    kW, but the delta form is correct.)'
  WHAT_IS_ACTUALLY_NEW_TWO_THINGS: 'THE DEBT ENTRY IS WRONG ABOUT ITS OWN DIRECTION, and its revisit trigger
    has fired. Both are new. (1) THE JUSTIFICATION IS REFUTED. TECH_DEBT.md justifies the shortcut with:
    "Conservative bias is correct direction (better to under-claim than over-claim)." MEASURED, it OVER-claims.
    Recorder-measured net energy per nudge over +90 min is 0.087-0.465 kWh depending on zone, against
    a claimed ~0.7-1.5 kWh. That is roughly 3-10x overstatement, in the OPPOSITE direction from the one
    the entry assumes and accepts. The whole "why acceptable" argument rests on a sign that is backwards.
    Re-litigate the acceptance, do not just re-tune the number. (2) THE REVISIT TRIGGER HAS ALREADY FIRED.
    The entry says revisit "if Span integration exposes a historical API that lets us pull matched-weather
    day energy curves". That history EXISTS TODAY in the HA recorder and was used to produce the measurement
    above — control window E[-90,-45] chosen deliberately over E[-45,0] so that regression-to-the-mean
    is not baked in (the nudge fires BECAUSE power was high). The prerequisite the entry was waiting for
    has been sitting there.'
  CONSUMER_IMPACT_CONTEXT_ONLY: RETIRED AS A DEFECT per the operator ruling above — retained as context,
    NOT as work. sensor.ura_hvac_ac_kwh_avoided_today / _total and the AC-ramp $ family feed dashboards
    and ROI figures. NOTE shipwatch CONFIRMED ac_ramp_savings_today=$1.40 under v5.33.0 H4 — that confirmed
    the number was PRESENT and forward-only, NOT that it was ACCURATE. Any savings claim built on this
    sensor is overstated by the same factor. Producer/consumer sweep needed before deciding whether to
    correct the sensor or caveat it.
  METHOD_CAVEAT_BE_HONEST: The measurement controls for regression-to-the-mean via the -90/-45 window
    but does NOT control for diurnal trend — nudges cluster in the afternoon, so the +90 min comparison
    window sits later in the day than the control. That confound could push either direction and is UNQUANTIFIED.
    The 3-10x magnitude should be treated as strong but not final. Rebound IS independently acknowledged
    in code (hvac_override.py:3442,:3492-3495 describe variable-speed Bryant rebound-to-full-power in
    minutes 5-10 post-restore), which is consistent with the measured shortfall and is the mechanism the
    30-min projection ignores.
  OPERATOR_RULING_2026_08_21_NOT_A_DEFECT: 'OPERATOR PUSHBACK, ACCEPTED: "Saving is approximate. It is
    marked not billing grade explicitly. It needs to be directionally accurate not forensically." CORRECT,
    and this card was OVER-ESCALATED — the orchestrator relayed a measurement agent framing ("higher value
    than the threshold tuning") without applying the operator standard to it. A 3-10x magnitude error
    is a PRECISION complaint, and precision is explicitly not what this figure is for. Full recorder-baselined
    difference math is the elaborate fix to a problem that does not exist at the stated standard. THE
    MAGNITUDE FINDING IS RETIRED as a defect; it survives only as context. WHAT SURVIVES, and it is small:
    the entry justifies the shortcut with "Conservative bias is correct direction (better to under-claim
    than over-claim)". Measurement says it OVER-claims. The DECISION to accept the debt stands; the REASON
    given for accepting it is backwards and someone will reason from it later. ONE-LINE DOC CORRECTION.
    Do NOT touch the producer, do NOT do a consumer sweep, do NOT build baselining. THE DIRECTIONAL FAILURE
    LIVES ON THE OTHER CARD: see AC-RAMP-ZONE3-NO-BENEFIT-1. Everywhere but zone 3 the figure is imprecise
    but points the right way (it rises when the feature does more work) — which satisfies the directional
    standard. On zone 3 it reports savings where the effect cannot be distinguished from zero. That is
    a SIGN/EXISTENCE error, not a magnitude error, and it is the only place the directional standard actually
    fails.'
  parsimony:
    problem: the tech-debt entry gives a bias direction that measurement contradicts; future readers will
      reason from it
    verdict: DOC-FIX
    note: NOT a build. Magnitude retired per operator - the figure is explicitly not billing-grade and
      only needs to be directionally accurate.
  next: 'One-line correction to the docs/TECH_DEBT.md v4.5.11 entry: replace the "conservative bias is
    correct direction" justification with the measured direction (it over-claims, because the 30-min projection
    does not net the post-restore rebound that hvac_override.py:3492-3495 already documents). Keep the
    acceptance decision unchanged. Fold into whatever cycle next touches that file — this does not warrant
    its own cycle.'
  refs:
  - docs/TECH_DEBT.md "v4.5.11 — AC ramp-down: rough kWh-avoided estimate"
  - hvac_override.py:3442,3492-3495
  - sensor.py:12239-12276
  - PLANNING_hvac_kwh_avoided_savings.md
  closed_note: 'DOC-FIX applied 2026-08-26: docs/TECH_DEBT.md v4.5.11 bullet corrected — bias is HIGH
    (over-claims, doesn''t net post-restore rebound), not conservative. Acceptance decision unchanged
    (not billing-grade). No cycle.'
- id: KITCHEN-MMWAVE-STILL-THRESHOLD-EXPERIMENT-1
  title: 'Kitchen mmWave chatter — LIVE EXPERIMENT running: still thresholds reverted to stock (Study
    B control) 2026-08-21 ~18:00; re-measure in 48h before ANY hardware purchase'
  thread: presence
  status: investigating
  created: 2026-08-21 18:00
  updated: 2026-08-21 18:00
  refinement_status: initial
  approval: operator_directed
  tags:
  - live-experiment
  - measure-before-build
  - revert-values-recorded
  THE_CHANGE_AND_HOW_TO_REVERT: 'APPLIED 2026-08-21 ~18:00 CDT to number.mmwave_lux_wifi_esphome_kitchen_g{0..8}_still_threshold.
    BEFORE (operator-modified, revert to these if needed): 39, 71, 58, 53, 60, 39, 38, 44, 49 AFTER  (=
    Study B stock profile, verified applied): 0, 0, 40, 40, 40, 30, 30, 20, 20 DELIBERATELY UNCHANGED
    so the experiment has ONE variable: move thresholds g0-g8 (already IDENTICAL between the two units),
    max_move_distance_gate (kitchen 7 vs studyb 8), max_still_distance_gate (kitchen 6 vs studyb 8), timeout
    (kitchen 27 vs studyb 30), distance_resolution / out_pin_level / baud (already identical). Operator
    context: "I do think Kitchen was modified and I dont think I knew what I was doing and didnt think
    it would make much diff. The Study B looks relatively unmolested." So Study B is a genuine untouched
    control.'
  THE_HYPOTHESIS_AND_WHY_IT_INVERTS_THE_OBVIOUS_FIX: 'The chatter is DROP-OUTS, not false positives. MEASURED
    (48h): kitchen _presence ON-durations have ZERO episodes under 25s and a hard mode at 25-28s — the
    27s hold is governing cleanly and nothing cuts presence short. ALL the churn is on the OFF side: 94
    of 359 OFF-episodes under 25s, 24 under 5s. Study B has ZERO OFF-episodes under 30s. That is the signature
    of A PRESENT PERSON BEING REPEATEDLY LOST, not of phantom triggers. Higher still threshold = LESS
    still-sensitivity (INFERRED from LD2410 semantics, not read from the ESPHome config — flagged as the
    weak link in the chain). So the raised thresholds plausibly CAUSE the drop-outs: a person stands still,
    is no longer detected, presence times out after 27s, they shift, presence returns. The likely history
    is that the thresholds were raised to suppress false positives and instead produced drop-out chatter
    — i.e. THE FIX POINTS THE OPPOSITE WAY FROM THE INTUITIVE ONE (lower, not raise).'
  THE_EVIDENCE_THAT_RULED_OUT_HARDWARE: 'DO NOT BUY A REPLACEMENT ON THE CURRENT EVIDENCE. DECISIVE OBSERVATION:
    Study B (same model, same firmware) sees COMPARABLE RAW RADAR ACTIVITY — _moving_target 1,675 transitions
    at 1.8s median vs kitchen 3,740 — yet its _presence output stays calm (35 transitions vs 724). Same
    raw churn in, wildly different presence out. Occupancy- normalised (per hour-ON) the kitchen is still
    5x chattier: 116/hr vs 22/hr. NOTHING LOOKS LIKE FAILING HARDWARE: only 8 unavailable rows in 48h,
    no sub-second storms, no missing episodes, ON-durations perfectly obey the configured hold. It behaves
    like a correctly functioning LD2410 seeing more than we want it to. FAN IS REAL BUT SECONDARY: conditioned
    on activity (30-min buckets with >=4 _moving_target transitions) the fan multiplies presence chatter
    1.57x and raw motion 1.47x — a multiplier on an already-bad baseline, not the cause. The fan-OFF baseline
    alone is ~5x Study B. So URAs mmwave_fan_demotions_since_boot=7 suspicion is WELL-FOUNDED but a fan
    gate alone will NOT make this sensor calm. Caveat: only 15 fan-ON active buckets, so the direction
    is solid and the exact multiplier is not.'
  ACCEPTANCE_AND_NEXT_STEPS: 'RE-MEASURE IN 48H with the same script (interval histogram, sub-5s count,
    ON/OFF episode distributions, occupancy-normalised transitions per hour-ON) and compare against the
    recorded baseline: 724 transitions, median interval 44.0s, 116 transitions per hour-ON, 94 OFF-episodes
    under 25s. DISCRIMINATING OUTCOMES: (a) chatter collapses toward Study B numbers -> tuning was the
    cause, close this and leave stock; (b) chatter unchanged -> tuning exonerated, escalate to PLACEMENT
    (move/re-aim the unit, or narrow max_move/max_still distance gates from 7/6) BEFORE any hardware purchase;
    (c) chatter worsens -> revert to the BEFORE values above and reconsider. ORDER OF ESCALATION AGREED:
    still thresholds -> distance gates -> move/re-aim -> replace. HARDWARE LAST, NOT FIRST. NOTE: switch.kitchen_override_vacant
    was turned back ON by the operator while this runs, so the chatter is NOT reaching zone-3 occupancy
    in the meantime. The sensor still logs its transitions, so the experiment is unaffected by the override.'
  parsimony:
    problem: a mmWave sensor chattering 724x/48h was heading for hardware replacement on a guess
    verdict: INVESTIGATE
    note: free reversible config experiment with a real control (Study B), run before spending money
  next: Re-run the measurement 2026-08-23 and compare to the baseline above. Then follow the discriminating
    outcomes. Do NOT purchase hardware until (a)/(b) is settled.
  refs:
  - number.mmwave_lux_wifi_esphome_kitchen_g0_still_threshold
  - binary_sensor.mmwave_lux_wifi_esphome_kitchen_presence
  - binary_sensor.mmwave_lux_wifi_esphome_studyb_presence
  - KITCHEN-OCCUPANCY-DEAD-1
  - CHATTER-RATE-VS-BURST-GAP-1
- id: EGRESS-IDENTITY-PRODUCER-EMITS-NOTHING-1
  FRIGATE2_STORAGE_OUTAGE_INVALIDATES_PART_OF_THE_RULING_2026_08_23: 'OPERATOR: "Frigate 2 was storage
    bound and swallowing detections since at least the 20th or so. Fixed now." That window overlaps the
    measurements this card''s NO-GO rests on. Probe run 2026-08-23 12:30 CDT (scripts/probes/frigate_health_probe.py,
    piped over ssh — /config is read-only so it lives in the repo, not on the host). WHAT IS INVALIDATED,
    and what is NOT — the distinction matters and was nearly lost: NOT invalidated: person_id = 0 of 6,875
    rows ALL TIME. A 3-day storage outage cannot explain five and a half months of zeros. The producer-emits-nothing
    finding STANDS. INVALIDATED / in question: the post-ship coverage numbers ("112 crossings, ZERO with
    a face in the 60s window", the ~2% ceiling over 7.6 days, "only 8 crossings in 4 days post-Frigate-2").
    All were taken inside or overlapping the outage. MEASURED STEP-DOWN CONFIRMS THE OPERATOR''S DATING:
    named-face recognitions per day — 08-16: 91 | 08-17: 80 | 08-18: 131 | 08-19: 118 | 08-20: 47 | 08-21:
    72 | 08-22: 69 A clear drop at 08-20 against an 80-131 baseline. Person detections did NOT collapse
    in the same window (665-2264/day throughout), so the outage suppressed FACE events specifically, not
    all ingest — which is why the ratio was the right discriminator to ask for. THE FINDING THAT REFRAMES
    THE WHOLE CARD — EGRESS CAMERAS DO PRODUCE FACES: garage_a 29 named-face recognitions in 8 days, doorbell_lite
    22, front_door_aerial 22, madrone_g6_entry 22, g5_bullet 21, family_room 21. The superseding memo
    claimed "face recognition works on INTERIOR cams, fails on door/garage geometry". THAT IS REFUTED.
    Garage A and the door cameras recognise faces regularly. CONSEQUENCE: the producer is NOT starved
    of faces. Faces exist on the egress cameras AND crossings happen daily (11-114/day) AND person_id
    is still never populated. That moves the suspect from AVAILABILITY (geometry — candidate b) to the
    JOIN (window/ordering — candidate a), which this card''s own `next` already named as the cheap-fix
    branch. CAVEAT BOUNDING ALL OF THE ABOVE: recorder `states` reaches back only to 2026-08-16. Nothing
    before that date is measurable this way, so the outage may predate the 20th — "at least the 20th"
    is a floor, not a bound. The 24h rerun partly addresses this by measuring clean post-fix days.'
  RERUN_SCHEDULE_2026_08_23: 'Operator directed a rerun "this evening and in 24 hours" to measure recovery
    on clean post-fix data. Probe: scripts/probes/frigate_health_probe.py. RUN 1 (this evening) — establishes
    the first clean partial day post-fix. RUN 2 (+24h) — the first FULL clean day; this is the one that
    matters. WHAT WOULD CHANGE THE RULING: if post-fix named-face rates on garage_a / doorbell_lite /
    front_door_aerial return to or exceed the 08-18/08-19 baseline (131/118 house-wide) AND person_id
    is STILL 0 across a full clean day of crossings, then face availability is definitively NOT the blocker
    and the join logic is. That reopens the eight consumer cards as gated on a FIXABLE producer rather
    than closing them as NO-GO. WHAT WOULD CONFIRM THE EXISTING RULING: face rates recover but egress-camera
    faces specifically stay near zero while interior cameras recover — that would restore the geometry
    hypothesis. These two predictions are DISCRIMINATING and are written down BEFORE the runs so the data
    chooses, not the reader.'
  title: person_id has NEVER been populated — 0 of 6,763 rows across 5.5 months. The eight identity-consumer
    cards are blocked on a producer that emits nothing, not a sparse one. NO-GO.
  thread: presence
  status: done
  created: 2026-08-21 19:10
  updated: '2026-08-28 22:00'
  disposition_2026_08_28: 'MERGED into EGRESS-IDENTITY-JOIN-GAP-1 as the canonical producer card. Reason: same defect (person_id 0/7010 on egress crossings). All measured findings on this card carried across verbatim in DEDUPE_2026_08_28 on the survivor. Do NOT reopen; work continues on EGRESS-IDENTITY-JOIN-GAP-1 per PLANNING_egress_identity_producer.md.'
  refinement_status: initial
  approval: unreviewed
  tags:
  - measured-not-inferred
  - measure-before-build
  - blocks-a-card-cluster
  THE_MEASUREMENT_ORCHESTRATOR_VERIFIED: 'MEASURED and INDEPENDENTLY RE-VERIFIED by the orchestrator (not
    relayed on the agent word): table person_entry_exit_events in the LIVE URA DB — rows = 6,763, person_id
    NOT NULL = 0, DISTINCT person_id = 0, span 2026-03-04T23:53 -> 2026-08-21T22:35. FIVE AND A HALF MONTHS,
    NEVER ONCE POPULATED. This is not a coverage RATE. There is no coverage.'
  WHY_IT_IS_ZERO_AND_WHY_THAT_IS_NOT_A_WIRING_BUG: 'The feature is ARMED, not dormant: egress_identity_enabled
    = True on the integration entry and kill switch switch.ura_name_people_at_doors has been ON since
    08-18. Registry resolution of all five egress cameras is CORRECT, including the awkward case garage_b
    (person entity UNSUFFIXED binary_sensor.garage_b_person_occupancy while its face sensor IS suffixed
    _2) — so the Frigate-1 `_2` handling is NOT broken here. The zero is explained by ABSENCE OF FACES
    TO MATCH, not by a broken stamp. Split at the 08-18 ship boundary, counting a named face on the same
    camera stem within FACE_MATCH_WINDOW_S=60 (const.py:2162) BEFORE the crossing — exactly the condition
    _resolve_egress_face_identity tests: - PRE-ship 08-14..08-17: 252 crossings, 7 with a face in window
    (2.8%), 0 stamped. - POST-ship 08-18..08-21: 112 crossings, ZERO with a face in window. No opportunity
    to stamp. Widening the window barely helps: 60s->7/364, 120s->7/364, 300s->8/364, 900s->10/364 (2.7%).
    The faces are not there to find. No code defect was found in _resolve_egress_face_identity, register_egress_face,
    or log_entry_exit_event.'
  THE_REAL_SHAPE_FACE_RECOGNITION_WORKS_ON_THE_WRONG_CAMERAS: 'Named-face events over 7.6 days: INTERIOR
    cameras — master_hallway 68, playroom 30, upstairs_hall 18, staircase/stairs_top 20. EGRESS cameras
    — garage_a 10, front_door_aerial 5, madrone_g6_entry 3, doorbell_lite 3, garage_b 1. Frigate face
    recognition WORKS WELL where people face the lens at conversational distance. On door/garage cameras
    (overhead + aerial angles, people in motion, often back-of-head) it produces ~3 named events per camera
    per WEEK against 15-45 crossings. The capability is fine; the GEOMETRY is wrong. Also MEASURED: UniFi
    Protect named-face events are NOT in this path at all — no Protect person entity is linked to any
    of the five egress devices in the registry. Protect and Frigate person detection DO corroborate (deduped
    within 5s in _resolve_direction), but neither Protect leg carries identity into this table.'
  CEILING_AND_WHY_IT_KILLS_THE_CONSUMER_CARDS: 'REALISTIC CEILING ~2% (4 named episodes / 229 deduplicated
    crossing episodes over 7.6 days). Poisson noise alone spans roughly 0.5%-4%; call it 0-6% honestly.
    A consumer that names the person at the door would stay SILENT ON ROUGHLY 49 OF EVERY 50 ENTRIES —
    erratic, not present. THIS IS THE MEASURE-BEFORE-BUILD RULE PAYING OUT EXACTLY AS WRITTEN: "always
    measure the real production rate of the new value first — a sparse producer caps every consumer value
    and may argue for a different producer, not more consumers." It does here. NO-GO on scoping PERIMETER-ALERT-NAME-PERSON-1,
    GUEST-GATE-DOOR-IDENTITY-1, ARRIVAL-DEPARTURE-NOTIFY-1, CENSUS-FACE-RESOLVER-MIGRATE-1, GUEST-COUNT-DEDUP-MIGRATE-1,
    SECURITY-CENSUS-UNKNOWN-WIRE-1, UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1, EGRESS-INTERIOR-COUNT-REINFORCE-1.
    The work in front of them is RAISING EGRESS FACE YIELD, not wiring downstream readers. Do not close
    them — re-gate them behind this card.'
  PRODUCER_FIX_CANDIDATES_ALL_INFERRED: 'NONE OF THESE ARE MEASURED — they are hypotheses for a producer
    cycle, and the cycle should start with a measurement of WHY faces miss (the face-event-vs-crossing
    LAG DISTRIBUTION), which needs debug logs, not the recorder. (a) FACE_MATCH_WINDOW_S=60 with an age>=0
    constraint requires the face to land BEFORE the crossing. Frigate face inference may routinely land
    AFTER. A signed-lag histogram would settle it and is the cheapest first probe. (b) Camera angle/placement
    at the doors — the geometry finding above suggests this is the dominant term. (c) MOST PROMISING:
    fuse an INTERIOR face within a short window of an egress crossing rather than requiring the same camera
    stem. Interior cameras produce 136 named events/week vs 22 across all five egress cameras. That is
    a producer REDESIGN, not a consumer card.'
  SUPERSEDES_THE_7PCT_MEMORY: This measurement SUPERSEDES memory reference_egress_face_coverage_7pct_not_a_ceiling.
    That memo said the 7% figure was measured on the WRONG camera (front door) and that the identity path
    was therefore VIABLE. The first half was right; the CONCLUSION was wrong. Measuring the RIGHT cameras
    did not rescue the rate — it landed LOWER (~2%), by a different and better method (per-camera temporal
    join against actually-logged crossings, rather than pooled front-door face coverage). Memory corrected
    2026-08-21.
  links:
    blocks:
    - PERIMETER-ALERT-NAME-PERSON-1
    - GUEST-GATE-DOOR-IDENTITY-1
    - ARRIVAL-DEPARTURE-NOTIFY-1
    - CENSUS-FACE-RESOLVER-MIGRATE-1
    - GUEST-COUNT-DEDUP-MIGRATE-1
    - SECURITY-CENSUS-UNKNOWN-WIRE-1
    - UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1
    - EGRESS-INTERIOR-COUNT-REINFORCE-1
    related:
    - EGRESS-CAMERA-DEAD-CONFIG-1
    context: 'Blocks all eight identity-consumer cards — they read a value that is always NULL, so none
      can be scoped until egress face yield rises. EGRESS-CAMERA-DEAD-CONFIG-1 fixed the dead garage camera
      names, which was necessary but NOT sufficient: the cameras are alive and the producer still emits
      nothing.'
  parsimony:
    problem: eight cards are queued against a value that has never once been produced
    verdict: INVESTIGATE
    note: the producer is the finding; consumers are moot until yield rises above a usable floor
  FRIGATE2_RECOVERY_PROBE_RUN_1_OF_2_2026_08_23: 'Run 1 (partial evening, first day post-midday-fix).
    NO VERDICT DRAWN -- run 2 is the decider, per the pre-registered protocol. ** PROBE DEFECT FOUND AND
    FIXED FIRST -- IT INVALIDATES THE STATED BASELINE. ** The face filter _JUNK was CASE-SENSITIVE and
    contained lowercase "unknown", but the live state is "Unknown" with a capital U. So every transition
    into "Unknown" scored as a face recognition, and since the dominant post-outage cycle is unavailable
    -> Unknown, EVERY HA RESTART counted as a recognition on EVERY camera. Signature that exposed it:
    12 different cameras each reporting exactly 3 on 08-21, 3 on 08-22, 1 on 08-23 -- uniform counts are
    not organic face traffic. Confirmed on sensor.master_hallway_last_recognized_face_2, which on 08-22
    carried ONLY {unavailable:3, Unknown:3} and ZERO real names, yet scored 3. Fixed to a casefold comparison;
    probe committed. ** CORRECTED SERIES (real named-face recognitions/day, house-wide) ** 08-16:68 08-17:57
    08-18:16 08-19:26 08-20:24 08-21:3 08-22:0 08-23:2. The baseline quoted on this card and in the run
    brief (08-18:131, 08-19:118) came from the buggy probe and is WRONG -- true values are 16 and 26.
    Egress 8-day totals also collapse on correction: garage_a 29->10, doorbell_lite 22->3, front_door_aerial
    22->3. ** BOTH PRE-REGISTERED PREDICTIONS: NOT TRIGGERED. ** P1 needed face rates at/above baseline
    with person_id still 0 -- face rates did NOT recover (2 today vs 68 on 08-16). P2 needed recovery
    with egress specifically flat while interior recovered -- nothing recovered, interior included (master_hallway
    1 today vs 19-29 on 08-16/17). Run 1 does not discriminate. As expected for a thin partial evening.
    ** NEW FINDING NEITHER PREDICTION ANTICIPATED, and it matters more than the run. ** Face recognition
    is near-DEAD house-wide since 08-21 (3, 0, 2) and did NOT recover after the midday 08-23 storage fix.
    Meanwhile PERSON detections are healthy (662 today, 1575 on 08-21, 1675 on 08-20). Per this probe
    OWN discriminating logic, person-normal + face-zero means the FACE RECOGNITION SUBSYSTEM is the problem,
    NOT storage/ingest -- a storage bound would have collapsed BOTH. So the 08-23 fix addressed a real
    but DIFFERENT fault, and face recognition has a second, unfixed failure. ** CONSEQUENCE FOR RUN 2.
    ** Run 2 may also fail to discriminate, because the probe is currently measuring a DEAD PRODUCER --
    and a dead producer cannot distinguish "join logic is broken" from "no faces to join". Before treating
    run 2 as decisive, check whether real named recognitions have resumed AT ALL; if the series is still
    ~0, run 2 answers nothing about the JOIN and the right next step is diagnosing face recognition itself,
    not re-measuring the join. ** UNCHANGED. ** person_id is 0 of 6,876 rows ALL TIME, including all 23
    crossings today. ** HORIZON CAVEAT RESPECTED. ** recorder states reach back only to 2026-08-16 04:12,
    so the outage may predate 08-20; "at least the 20th" remains a floor, not a bound, and 08-16/08-17
    are the earliest observable days rather than known-good ones.'
  FRIGATE2_RECOVERY_PROBE_RUN_2_OF_2_DECIDING_2026_08_24: 'THE DECIDING RUN. First full clean day post-fix.
    VERDICT, stated against the two pre-registered predictions without rewriting them, ** NEITHER PREDICTION
    FIRES. BOTH WERE PREMISED ON FACE RECOGNITION RECOVERING. IT DID NOT. ** ** FIRST, THE BASELINE IN
    THE RUN BRIEF IS THE BUGGY PROBE RS OUTPUT. ** The brief quotes 08-18:131 and 08-19:118 house-wide
    and garage_a 29 over 8 days. Those came from the case-sensitive _JUNK filter fixed on 08-23, which
    scored every unavailable -> "Unknown" transition (i.e. every HA restart) as a face recognition. CORRECTED
    baseline is 08-18:16, 08-19:26, and garage_a 10. Predictions evaluated against the CORRECTED numbers;
    the predictions themselves are unchanged. ** RUN 2 RESULT (corrected probe). ** Named-face recognitions/day
    house-wide: 08-17:53 08-18:16 08-19:26 08-20:24 08-21:3 08-22:0 08-23:4 ** 08-24:2 **. The first full
    clean day post-fix produced TWO face recognitions house-wide, against a working-period baseline of
    53-68. Egress cameras on 08-24: ** ZERO **. garage_a last produced 08-19 (1). doorbell_lite last 08-20
    (1). front_door_aerial last 08-17 (2). The only camera producing anything on 08-23/08-24 is master_hallway,
    an INTERIOR camera, at 2/day against its own 22-27/day when healthy. person_id: ** 0 of 6,883 rows
    ALL TIME **, including all 7 crossings on 08-24. No category change. ** PREDICTION 1 (REOPEN) — NOT
    MET. ** It required face rates at or above baseline AND person_id still zero. Face rates did not recover
    (2 vs 53-68). The join-window hypothesis is NOT tested by this data. ** PREDICTION 2 (CONFIRM NO-GO
    via geometry) — NOT MET EITHER. ** It required house-wide recovery with egress specifically flat.
    Nothing recovered anywhere, interior included. The geometry hypothesis is NOT supported by this data,
    and was independently refuted on 08-23 (garage_a produced 10 corrected named faces over 8 days when
    the subsystem worked — door/garage geometry is not the blocker). ** THE ACTUAL STATE — a third case
    neither prediction anticipated, now confirmed on a full clean day rather than a thin evening. ** Face
    recognition is BROKEN HOUSE-WIDE, interior cameras included, and the 08-23 midday storage fix did
    NOT restore it. Person detections are healthy throughout (08-21:1575, 08-23:1004, 08-24:596 partial).
    Person-normal + face-zero is this probe OWN signature for "the face recognition subsystem is the problem,
    not storage/ingest" — a storage bound collapses BOTH. Collapse onset is ~08-21 (3, then 0), which
    PRECEDES the 08-23 fix and may be independent of the 08-20 storage event entirely. ** DISPOSITION:
    NO-GO STANDS, BUT THE STATED REASON IN THE MEMORY IS WRONG. ** Not geometry, not a coverage ceiling.
    The producer emits nothing because the producer is DOWN. That is a live fault, not a design limit
    — and a fault is fixable, which a geometry limit is not. ** THE EIGHT CONSUMER CARDS ARE NOT REOPENED.
    ** No hopeful reading. person_id is still zero, the producer is still emitting nothing, and this run
    gives no evidence about the join window because the upstream never produced anything to join. They
    stay gated. What CHANGES is what they are gated ON, from "a producer that may be structurally incapable"
    to "a producer that is broken and needs diagnosis". Re-run this probe only after face recognition
    is confirmed working. ** HORIZON. ** Recorder now reaches back only to 08-17 04:12, so 08-16 has aged
    out of measurability and 08-17 reads 53 (was 57 with a fuller day). The 5.5-month person_id zero is
    unaffected by any of this — it comes from the URA DB, not the recorder.'
  next: DIAGNOSE FACE RECOGNITION ITSELF — down house-wide since ~08-21 (3, 0, 4, 2 per day against a
    healthy 53-68) and the 08-23 storage fix did NOT restore it. Person detections stayed healthy throughout,
    which is this probe OWN signature for a face-subsystem fault rather than storage/ingest. The join-window
    question is UNTESTABLE until the producer emits — there is nothing to join. The eight consumer cards
    stay gated; what they are gated ON changes from a producer that may be structurally incapable to a
    producer that is broken. Re-run frigate_health_probe.py only after face recognition is confirmed working.
  refs:
  - person_entry_exit_events (URA DB)
  - const.py:2162 FACE_MATCH_WINDOW_S
  - switch.ura_name_people_at_doors
- id: CHATTER-RATE-VS-BURST-GAP-1
  title: The chatter detector cannot see the house's actual chatter — it detects BURSTS OF IMPOSSIBILITY,
    the real failure is SUSTAINED RATE (kitchen mmWave 731 flips/48h, only 25 impossibility events)
  thread: presence
  status: investigating
  created: 2026-08-21 17:40
  updated: 2026-08-21 17:40
  refinement_status: initial
  approval: unreviewed
  tags:
  - measured-not-inferred
  - explains-a-null-result
  OPERATOR_INSIGHT_2026_08_21: 'Operator, unprompted and correct: "I wonder if chatter is the right thing
    vs burst chatter. which would be time scoped and not continuous." The measurement below confirms it.
    This card exists because the operator named the distinction before the data was fully read.'
  THE_MEASUREMENT: 'Kitchen ESP mmWave, 48h from the HA recorder (MEASURED, computed from raw state rows
    on the live host): - binary_sensor.mmwave_lux_wifi_esphome_kitchen_presence (THE ONE WIRED INTO THE
    ROOM): 731 transitions, median interval 44.4s, min 0.22s. Interval histogram: <1s: 1 | 1-5s: 24 |
    5-15s: 36 | 15-60s: 364 | >=60s: 305. SUB-5s events = 25 out of 730 intervals (3.4%), i.e. ~0.5/hour.
    - binary_sensor.mmwave_lux_wifi_esphome_kitchen_moving_target (NOT wired into the room): 3,746 transitions,
    median 2.2s, SUB-5s = 2,741.'
  WHY_THE_DETECTOR_CANNOT_SEE_IT: 'The definition, verbatim from chatter_detector.py:11-15: a sensor is
    "chattering iff it emits >= CHATTER_BURST_K transitions WHOSE INTERVAL SINCE THE PRIOR TRANSITION
    IS BELOW that sensor per-family T_floor (impossibility events) within the rolling CHATTER_OBSERVATION_WINDOW_S
    window." With K=10 and T_floor=5.0s, the WIRED sensor generates ~0.5 impossibility events/hour. It
    will essentially never accumulate 10 inside one observation window unless they cluster. So the detector
    will NOT flag a sensor that flips 731 times in two days. Raising T_floor 1.0 -> 5.0 (done 2026-08-20)
    does not change this — it broadens the net but the events remain far too sparse. TWO DISTINCT SIGNALS,
    AND ONE CANNOT SEE THE OTHER: - BURST OF IMPOSSIBILITY (what is built): K sub-T_floor intervals in
    a rolling window. Detects a sensor doing something physically impossible. - SUSTAINED RATE (what the
    kitchen actually does): 731 flips/48h where EVERY INDIVIDUAL FLIP IS PHYSICALLY PLAUSIBLE. Absurd
    in aggregate, innocent in every instance. THIS EXPLAINS THE NULL RESULT. Chatter shadow mode "found
    nothing" across the house overnight. The detector is NOT mistuned — the houses actual chatter is the
    kind it was deliberately designed not to look at.'
  THE_DESIGN_TENSION_READ_THIS_BEFORE_FIXING: 'DO NOT simply add a rate threshold to the existing detector.
    The impossibility framing was chosen ON PURPOSE so the detector could QUARANTINE-ALWAYS WITH NO CORROBORATOR
    GATE (chatter_detector.py:8 — "quarantine-ALWAYS on a physics violation a correctly-working sensor
    CANNOT satisfy. No corroborator gate."). That licence is EARNED by impossibility: a working sensor
    cannot produce it, so no second opinion is needed. A RATE DETECTOR CANNOT INHERIT THAT LICENCE. A
    busy open-plan kitchen legitimately produces a high transition rate. So a rate-based detector needs
    EITHER a corroborator gate OR a softer response — DEMOTE, not quarantine. Getting this wrong would
    quarantine a healthy sensor in the busiest room in the house. The response class must be designed
    with the signal, not bolted on.'
  SECOND_FINDING_WRONG_LEG_WATCHED: The detector registers over "the room blind-time-gated tier-1 entities"
    — i.e. the CONFIGURED ones. The kitchen config wires only `_presence` (the slow chatterer, 3.4% impossibility).
    Its sibling `_moving_target` is wildly impossible (2,741 sub-5s events) and is watched by NOTHING
    because it is not in the room config. A multi-leg sensor whose unwired legs are the diagnostic ones
    is a general gap, not a kitchen quirk — worth checking whether other ESP/mmWave rooms have the same
    shape.
  parsimony:
    problem: the chatter detector cannot detect the failure mode the house actually exhibits
    verdict: INVESTIGATE
    note: do NOT bolt a rate threshold onto the impossibility detector — the response class (quarantine
      vs demote) is coupled to the signal class. Design them together.
  next: 'Decide whether a RATE-based sensor-health signal is worth building at all — decompose the benefit
    before speccing (marginal-benefit duty). Cheapest version may be a diagnostic-only transitions-per-hour
    surface with NO automatic action, letting the operator see a misbehaving sensor rather than having
    URA act on it. If it is built, the response must be demote-with- corroboration, never quarantine-always.
    Separately: check whether other rooms wire only the quiet leg of a multi-leg mmWave.'
  refs:
  - chatter_detector.py:8-15
  - binary_sensor.mmwave_lux_wifi_esphome_kitchen_presence
  - KITCHEN-OCCUPANCY-DEAD-1
  - CHATTER-CAMERA-CONFIDENCE-FLAP-1
  - SENSOR-MULTISTATE-FAULT-1
- id: KITCHEN-OCCUPANCY-DEAD-1
  title: 'RESOLVED — NOT A DEFECT: kitchen occupancy is pinned off by switch.kitchen_override_vacant (room
    deliberately disabled); three inferred mechanisms were wrong before the switch was checked'
  thread: presence
  status: done
  created: 2026-08-21 16:45
  updated: 2026-08-21 17:20
  refinement_status: resolved
  approval: n/a
  RESOLUTION_2026_08_21: 'NOT A DEFECT. The Kitchen room is DELIBERATELY DISABLED: switch.kitchen_override_vacant
    = ON, switch.kitchen_automation = OFF, switch.kitchen_ai_automation = OFF. The override (a v3.20.0
    feature, coordinator.py:4634-4637, _is_override_vacant) forces STATE_OCCUPIED=False and stamps occupancy_source="override"
    REGARDLESS OF SENSORS. All four switches share last_changed 2026-08-21T14:09:20, which is a RESTART
    timestamp (RestoreEntity rehydration), not four manual toggles — so this has been the standing configuration.
    EVERYTHING ELSE IS HEALTHY: the PIR (binary_sensor.motiontempluxhum_protect_blu_kitchen_motion, UniFi
    Protect) fires normally; the ESP mmWave (binary_sensor.mmwave_lux_wifi_esphome_kitchen_presence) is
    live and correctly wired as the room Presence sensor; capability declarations are correct. Confirmed
    from the operator screenshot of the Sensors config page plus live entity reads. OPEN QUESTION FOR
    THE OPERATOR (the only remaining item): is the kitchen MEANT to be disabled? If yes, close this permanently.
    If no, the fix is flipping three switches — no code, no config edit.'
  METHOD_POSTMORTEM_THREE_WRONG_MECHANISMS: 'KEPT DELIBERATELY — this card is a better example of a reasoning
    failure than of a bug. THREE mechanisms were asserted and refuted in sequence, each inferred from
    partial evidence, before anyone checked the room switches: (1) "missing/misconfigured motion source"
    — from a measurement agent seeing 100% off. REFUTED by history: the PIR fires constantly (1,161 state
    changes in 3 days). (2) "occupancy confidence gated by a dead mmWave" / "motion not in the substrate"
    — inferred from sensor.kitchen_unavailable_entities=1 plus unavailable looponunity entities. REFUTED:
    the looponunity entities are ORPHANED REGISTRY LEFTOVERS from a removed device and are NOT referenced
    by the kitchen config at all. (3) "stale config points at the removed looponunity, replacement unwired"
    — inferred from the operator noting the device had been replaced. REFUTED by the operator directly
    ("pretty sure the loopunity is not in config pages") and then by the config screenshot: the ESP mmWave
    was already wired. THE ACTUAL ANSWER WAS ONE STATE READ AWAY THE WHOLE TIME. Lesson, consistent with
    feedback_verify_claim_types_not_felt_uncertainty: a CAUSAL MECHANISM claim is contraband without evidence,
    and "is this thing simply switched off?" is the cheapest disconfirming check available for any "X
    never fires" symptom. CHECK THE OVERRIDE/ENABLE SWITCHES FIRST — before substrate, config, or code.'
  CONSEQUENCE_FOR_THE_ZONE3_MEASUREMENT: 'The zone-3 occupancy/coincidence analysis treated the kitchen
    as contributing nothing to zone occupancy. That remains TRUE and the numbers stand — but the REASON
    is the deliberate override, not a defect. Nuance that still matters: for the question "was a human
    physically present during a nudge", URA genuinely does not know about the kitchen, so the coincidence
    rate is still an UNDER-COUNT of real human presence. Design choice, not bug.'
  SIBLING_SWEEP_STILL_VALID_2026_08_21: 'The house-wide sweep stands on its own and is NOT invalidated
    by this resolution: sensor.<room>_unavailable_entities is non-zero in SIX rooms — EIGHT dead configured
    inputs: game_room 2, garage_a 2, kitchen 1, living_room 1, master_bedroom 1, upstairs_guestroom 1.
    Only 20 rooms expose the sensor, so coverage is PARTIAL. DO NOT assume a shared cause — "unavailable"
    covers both stale config pointing at removed hardware AND a device merely offline right now, which
    need different fixes. Also note the kitchen still shows 1 dead input whose identity was never established
    (it is NOT the looponunity, which is unreferenced). Worth its own small pass; tracked here rather
    than spawning a card until someone determines whether these are stale config or offline devices.'
  parsimony:
    problem: none — behaviour is as configured
    verdict: NO-BUILD
    note: resolved by inspection; retained for the method postmortem and the sibling sweep
  next: Ask the operator whether the Kitchen room is intended to stay disabled. If yes, close. If no,
    turn on switch.kitchen_automation and switch.kitchen_ai_automation and turn OFF switch.kitchen_override_vacant,
    then confirm binary_sensor.kitchen_occupied fires. Separately, work the six-room dead-input sweep,
    determining per room whether each input is stale config or an offline device.
  refs:
  - coordinator.py:4623-4645
  - switch.kitchen_override_vacant
  - binary_sensor.mmwave_lux_wifi_esphome_kitchen_presence
  - feedback_verify_claim_types_not_felt_uncertainty
- id: AC-RAMP-ZONE3-NO-BENEFIT-1
  title: Zone 3 AC ramp shows NO measurable savings across 79 nudges — candidate for disable-pending-evidence
    rather than retune
  thread: hvac
  status: investigating
  created: 2026-08-21 11:40
  updated: 2026-08-21 11:40
  refinement_status: initial
  approval: unreviewed
  tags:
  - measured-not-inferred
  - cannot-tell-from-this-data
  THE_MEASUREMENT: 'Zone 3 (3-ton; back rooms + hallways + kitchen) pooled net energy effect per nudge
    over +90 min is -0.087 +/- 0.128 kWh — the confidence interval SPANS ZERO. Implied value +$0.08/day,
    CI -$0.16 to +$0.32. No individual pre-nudge power bucket reaches significance either (1.5: +0.099;
    2.0: -0.253; 2.5: +0.017; 3.0: -0.234, all CIs spanning zero). Zone 3 runs ~11 nudges/day, peak 32.
    Compare zone 1 (-0.435 +/- 0.083, clearly saves) and zone 2 (savings proven only at pre-nudge >= 2.5
    kW). n=79 nudges with full recorder overlap over 7.5 days; ac_ramp_events span 31 days.'
  WHY_NO_THRESHOLD_WAS_SET: DELIBERATE NON-ACTION. Zone 1 and zone 2 thresholds were retuned 2026-08-21
    (1.3->1.5 and 1.2->2.2). Zone 3 was LEFT AT 1.3 because tuning a knob on an intervention with no measurable
    effect is optimising a number that does not matter. The plateau structure would suggest ~1.6 by analogy
    with zone 2, and that value would look confident and be unsupported. Explicitly not set.
  OPERATOR_OCCUPANCY_REFRAME_2026_08_21: 'OPERATOR: zone 3 differs from zone 2 in ONE way — "its a zone
    not as often occupied as the others... Mostly only when we have guests are entertain a lot." Same
    3-ton HVAC model as zone 2, so EQUIPMENT is not the variable; OCCUPANCY is. MEASURED (verified in
    source 2026-08-21): the AC-ramp gate block hvac_override.py:2660-2750 contains NO occupancy or preset
    reference whatsoever. Gates are ramp_zone_enabled -> cooling action -> at-setpoint -> kwh_rate > threshold
    -> 3 consecutive samples -> 10-min sustain. The ramp nudges regardless of whether anyone is in the
    zone. THIS CORRECTS AN ERROR IN THIS CARD AS ORIGINALLY WRITTEN. The card argued zone 3 was "paying
    ~11 comfort interruptions/day for nothing". That assumed SOMEONE WAS THERE TO BE INTERRUPTED. In a
    mostly-empty zone a nudge has ZERO comfort cost, so: (a) the comfort argument for suppressing zone-3
    nudges is largely VOID, and (b) the disable-pending-evidence option is WEAKENED — disabling would
    forfeit small-but-FREE savings. Do not disable on the strength of the null result alone. THE THRESHOLD
    CHANGE TO 2.2 STILL STANDS but on the EQUIPMENT argument only: 1.3 sat inside zone 3s Stage 2 modulation
    band (0.90-1.50 kW), where a nudge yields little regardless of occupancy. Same defect that was corrected
    on zone 2. NOT on the comfort argument.'
  OCCUPANCY_PROFILE_CORRECTED_AGAIN_2026_08_21: 'SECOND CORRECTION, and it reverses the first. OPERATOR:
    "Its not completely unoccupied. And its also the One that has sensitive drive bys by people in the
    house." Zone 3 is the TRANSIT zone — hallways + kitchen, HIGH-FREQUENCY SHORT occupancy by people
    passing through. Neither previous framing was right: - framing 1 (occupied, so nudges cost comfort)
    — wrong, assumed DWELL occupancy; - framing 2 (mostly empty, so nudges are free) — ALSO WRONG, an
    over-correction. THE ACTUAL PROFILE IS TRANSIT, and it inverts the comfort math a second time. In
    a DWELL zone occupants are present continuously: they experience a nudge as a gradual drift and are
    present for the recovery. In a TRANSIT zone someone arrives at an UNPREDICTABLE moment; if they walk
    in mid-nudge they meet the excursion with no warning and no adaptation, and they are gone before the
    restore. So per-nudge comfort cost in zone 3 may be HIGHER than in a dwell zone, not zero. Consistent
    with the operators presence-tuning sensitivity being concentrated in exactly these rooms — back hallway
    dwell was the setting they moved from 3 to 5 minutes. NET: both the "11 interruptions for nothing"
    framing AND the "nudges are free" correction are retired. The comfort cost is real, is concentrated
    in unpredictable short entries, and is NOT captured by any nudge-count metric.'
  DESIGN_IDEA_EARLY_RETURN_ON_OCCUPANCY_DECOMPOSE_FIRST: 'IDEA, NOT A SPEC — decompose before scoping
    (marginal-benefit duty). The ramp gate block has NO occupancy input (MEASURED, hvac_override.py:2660-2750),
    so a nudge runs to completion regardless of who arrives. The implication of the transit profile points
    OPPOSITE to the earlier suggestion: not "skip the nudge when the zone is empty" but "END THE NUDGE
    EARLY WHEN SOMEONE WALKS IN." NATURAL FIT FOR THE BORROW PRIMITIVE, not new machinery: an occupancy
    event becomes an EARLY-RETURN TRIGGER on an existing lease. return_excursion() already exists; this
    is one more discharge condition on it, not a new mechanism. That is a genuinely cheap version IF it
    is worth doing at all. DO NOT BUILD BEFORE MEASURING. The simple version already helps: a nudge is
    ~2 minutes, so the overlap window with any given drive-by is small. Whether early-return earns its
    complexity depends on HOW OFTEN A NUDGE AND AN ENTRY ACTUALLY COINCIDE — measurable from existing
    data, and it belongs in the same occupancy split described below. If coincidence is rare, the idea
    is parked with this note as the evidence trigger.'
  THE_REAL_QUESTION_NOW: 'The question is NOT "does zone 3 save?" but "DOES ZONE 3 SAVE WHEN THE HOUSE
    IS ENTERTAINING?" Those are the nudges that carry actual comfort cost and the only ones worth tuning
    for. A null pooled across mostly-empty hours may be WASHING OUT a real signal from the occupied minority
    — n=79 split by occupancy could be ~10 occupied and ~69 empty, and the pooled mean would be dominated
    by the empty ones. DISCRIMINATING TEST, runnable on data that ALREADY EXISTS (no 3-4 week wait): split
    zone 3s 79 nudges by occupancy/preset at fire time and compare net energy effect between the two halves.
    ADD A THIRD MEASUREMENT given the transit profile: the COINCIDENCE RATE — how often does an occupancy
    ENTRY event land inside a live nudge window (nudge start -> restore, ~2 min)? That number, not the
    nudge count, is the real comfort-cost metric for a transit zone, and it is the go/no-go gate on the
    early-return idea above. - If occupied-half looks like zone 2 and empty-half is ~zero -> occupancy
    IS the mechanism; tune for the occupied case and leave the free empty-zone nudges alone. - If BOTH
    halves are null -> occupancy is NOT the mechanism; look elsewhere (thermal rebound remains a candidate).
    This supersedes the earlier "accumulate another 3-4 weeks" suggestion as the FIRST step.'
  SUPERSEDED_HYPOTHESIS_UNTESTED: 'LOWER PRIORITY than the occupancy split above; retained as the alternative
    if both halves come back null. Original: Zone 3 covers kitchen + hallways — high internal gain, high
    infiltration, likely poor thermal mass. A nudged-off compressor may rebound almost immediately, which
    is what the near-zero and slightly-positive +90 min numbers hint at. PLAUSIBLE, UNTESTED. Do not treat
    as the mechanism until an observation discriminates it from the alternatives.'
  parsimony:
    problem: no demonstrated energy benefit on zone 3 — but the comfort-cost half of the original framing
      is void, since the zone is mostly empty
    verdict: INVESTIGATE
    note: disable-pending-evidence is now WEAKENED, not a live option — an empty-zone nudge is free, so
      disabling forfeits savings at no comfort gain. Run the occupancy split first.
  next: 'Two paths, not mutually exclusive. (1) Accumulate: another 3-4 weeks of ac_ramp_events tightens
    the CI roughly 2x and may resolve the null. (2) Test the rebound hypothesis directly against the recorder
    rather than waiting. If rebound is confirmed, the question becomes ramp_zone_enabled=false for zone
    3, NOT a threshold value. INTERIM option if comfort cost is worth reducing while the question is open:
    2.0 (7.7 nudges/day, -32%) — defensible only because there is no measurable savings to lose, not because
    it is optimal.'
  refs:
  - AC-RAMP-SAVINGS-OVERSTATED-1
  - number.ura_hvac_coordinator_ac_kwh_rate_threshold_back_hallway_2
- id: AC-RESET-PRESET-ASSERT-DROPPED-1
  title: AC hard reset never restores a preset — the plan specified a one-liner, it was dropped without
    accounting, and it is the last path that can still strand a zone in manual
  thread: hvac
  status: shipped_organic
  shipped_version: v5.88.1
  created: 2026-08-22 02:10
  updated: 2026-08-23 14:30
  refinement_status: initial
  approval: unreviewed
  tags:
  - dropped-deliverable
  - measured-not-inferred
  THE_GAP_VERIFIED: 'MEASURED 2026-08-22 on shipped v5.88.0: `_verify_restore` in hvac_override.py spans
    lines 2968-3062 and contains ZERO preset references — no emit_set_preset_mode, no pre_preset, nothing.
    The hard-reset ladder writes set_hvac_mode off/on and restores the MODE and SETPOINT, then leaves
    whatever preset results. After a raw setpoint write on a Bryant/Carrier that is `manual`. CONSEQUENCE:
    this is the SAME lockout mechanism v5.88.0 exists to eliminate, surviving in the reset path. should_change_preset
    refuses to act on a manual zone, so a zone hard-reset into manual is locked out of preset governance
    exactly as the nudge path used to leave it.'
  IT_WAS_SPECIFIED_AND_DROPPED: 'This is NOT a newly discovered defect — the plan called for it and it
    went missing. PLANNING_hvac_governed_excursion.md scope decisions: hard_reset_preset_assert is deliberately
    NOT a primitive-managed kind (EXCURSION_KIND_hard_reset_preset_assert MUST NOT exist so it cannot
    be wired by analogy later); INSTEAD a one-line emit_set_preset_mode(snapshot_preset) belongs in `_verify_restore`s
    SUCCESS branch. Review D caught the omission (F7: "plan Scope decisions required a one-line emit_set_preset_mode
    in _verify_restore success branch. Grep finds no emit_set_preset_mode anywhere in that region. Dropped
    without accounting.") and it never returned in any of the six fix-up rounds. ORCHESTRATOR MISS, recorded
    plainly: fix-up rounds were driven off REVIEW FINDINGS and the orchestrator never cross-checked the
    PLANs own scope decisions for un-built items. A finding raised as a LOW in one review can be lost
    between rounds when nobody holds the plan-to-build checklist. Lesson: before closing a cycle, diff
    the plan scope decisions against what shipped — not just the review findings against what was fixed.'
  parsimony:
    problem: the hard-reset path can still strand a zone in manual, the exact defect the cycle shipped
      to remove
    verdict: BUILD
    note: small and already specified - a one-liner in the success branch plus a snapshot to read from;
      do NOT make it a borrow kind (explicit non-goal)
  next: 'PENDING 2026-08-23: code verified, field evidence blocked on escalation mechanic not yet installed
    (v5.89.0 not deployed). Revisit after v5.89.0 install + 48h.'
  links:
    related:
    - HVAC-GOVERNED-EXCURSION-1
    - HVAC-MANUAL-PRESET-CONTRACT-1
    context: HVAC-GOVERNED-EXCURSION-1 shipped the primitive that fixed this class everywhere ELSE; this
      is the one site it deliberately excluded and then did not implement the alternative for. HVAC-MANUAL-PRESET-CONTRACT-1
      is the parent contract (every write should be a preset write).
  refs:
  - hvac_override.py:2968-3062 (_verify_restore)
  - PLANNING_hvac_governed_excursion.md (scope decisions)
  organic_evidence:
  - '2026-08-23 watch-pass: 0 hard resets in 6+ days (resets require failed nudges; escalation gated).
    Code and mutation drill verified per README_v5.88.1. Field proof awaiting escalation mechanic (AC-RAMP-NO-RECURRENCE-ESCALATION-1).
    H1 PENDING.'
- id: BORROW-FRIENDLY-NAME-RENAME-1
  title: Rename user-facing "Governed thermostat borrows" labels — ship with next batch
  thread: hvac
  status: done
  shipped_version: v5.88.1
  created: 2026-08-22 02:10
  updated: 2026-08-23 14:30
  refinement_status: initial
  approval: operator_decided
  OPERATOR_DIRECTION: 'Operator 2026-08-22: "I think we should rename the friendly names. Ship with next
    batch." Scope is USER-FACING TEXT ONLY — code, module names, table names, column names, log messages
    and comments all STAY as `excursion` per the earlier ruling.'
  IMPORTANT_ENTITY_IDS_WILL_NOT_MOVE: 'Renaming a friendly name does NOT change an already-registered
    entity_id. The sensor is registered as sensor.ura_hvac_coordinator_governed_thermostat_borrows (HA
    derived it from the label at first registration) and it will KEEP that id after a rename. Result:
    a clean label over a longer id. Harmless, but do not expect the id to follow, and do NOT attempt a
    registry rename to chase it — that risks breaking anything already referencing the id.'
  OPERATOR_DECIDED_2026_08_22: 'DECIDED. Operator: "B for the label. I like the verb version. Everything
    else leave as is." FINAL STRINGS: - SENSOR friendly name  -> "Temporary Thermostat Changes"   (option
    B noun) - CONFIG TOGGLE label   -> "Restore thermostats after temporary changes"   (verb form, from
    A) Noun for the sensor because it is a count of things; verb phrase for the toggle because it must
    say what turning it ON does, not name a subsystem. INTERPRETATION CONFIRMED BY OPERATOR 2026-08-22
    — QUESTION CLOSED, do not re-open. The original instruction paired "B for the label" with "I like
    the verb version", and B as presented had a NOUN toggle, so the orchestrator flagged its reading (B
    sensor + A toggle) rather than assume it. Operator then confirmed explicitly: "The toggle text is
    not quite right. The sensor is. Use the A version for the toggle." The recorded strings ALREADY matched
    that — sensor from B, toggle from A — so NO CHANGE was required; the flag is simply resolved. NOTE
    FOR NEXT TIME: the flag itself caused the round-trip. It led with the AMBIGUITY (noting that option
    B as presented carried a noun toggle) instead of leading with the CHOICE MADE, which made it read
    as though the option-B toggle might have been taken. Flagging an interpretation is correct practice;
    the flag should state what WAS chosen first and the residual ambiguity second, never the reverse.
    "EVERYTHING ELSE LEAVE AS IS" — do NOT change: the config description body (which opens with "URA
    occasionally BORROWS a thermostat..."), the log tag [GOVERNED BORROW RESTORE FAILED], the NM kind
    identifier borrow_restore_failed, any entity_id, or any code/table/column/comment. MILD INCONSISTENCY,
    NOTED NOT ACTIONED: with the label no longer saying "borrow", the description now introduces the borrow
    metaphor without the label setting it up. Judged acceptable — the description explains itself in its
    first clause — and explicitly out of scope per the operator instruction. Flagged so a future reader
    knows it was seen and left, not missed.'
  OPTIONS_PRESENTED: 'A (recommended): sensor "Thermostat Borrows" / config toggle "Restore thermostats
    after temporary changes". B: "Temporary Thermostat Changes" both. C: "Thermostat Hold and Restore"
    / "Hold and restore thermostats". RECOMMENDATION REASONING: drop "Governed" — it is internal vocabulary
    doing no work for the reader. Sensor should be a NOUN (it is a count of things); config toggle should
    be a VERB PHRASE (what turning it on does). A toggle naming a subsystem tells the operator nothing;
    one describing behaviour tells them what they are switching off at 3am.'
  parsimony:
    problem: user-facing labels use internal jargon
    verdict: BUILD
    note: text-only, batch it - not worth its own deploy
  next: DONE 2026-08-23 — rename confirmed live on HA instance.
  links:
    related:
    - AC-RESET-PRESET-ASSERT-DROPPED-1
    context: batch these two together - one is text-only, the other is a one-liner; neither warrants its
      own deploy
  organic_evidence:
  - '2026-08-23 watch-pass: friendly_name = "URA: HVAC Coordinator Temporary Thermostat Changes" confirmed
    live; validation table PASS 2026-08-22 in README_v5.88.1. H1 confirmed.'
- id: S14-CEILING-NEEDS-AN-ENDING-1
  title: S14 off-phase ceiling hold has no exit and blocks its own — give it an ending (operator chose
    option (a) 2026-08-21), preferably by making it a borrow kind
  thread: hvac
  status: planned
  created: 2026-08-21 10:20
  updated: 2026-08-25 22:20
  refinement_status: initial
  approval: operator_decided
  sibling_of: HVAC-PRESET-FLAP-1
  tags:
  - tier-2db
  - re-litigates-shipped-trade
  - suppression-needs-a-discharge
  OPERATOR_DECISION_2026_08_21: 'Operator chose option (a) "give it an ending" from the three costed in
    HVAC-MANUAL-PRESET-CONTRACT-1 / S14_OPERATOR_VERDICT (a: add ending, b: reduce to suppression-only,
    c: remove entirely). Operator suggestion for the mechanism: "Maybe reading its interventional setpoint?"'
  THE_DEFECT: During an energy coast/shed off-phase in an OCCUPIED zone, S14 (_apply_duty_off_phase, hvac.py
    ~:2972-2983) writes a RAW SETPOINT to hold the cooling ceiling instead of flipping the zone to away.
    The raw write puts the Bryant into preset `manual`. Its documented exit is "holds until the next preset
    transition" — and that exit is STRUCTURALLY UNREACHABLE, because should_change_preset (hvac_preset.py:202)
    returns False whenever the current preset is manual. S14 CREATES THE EXACT CONDITION THAT PREVENTS
    ITS OWN EXIT. No timer, no decay, no restore.
  RE_LITIGATES_A_DELIBERATE_SHIPPED_TRADE: 'IMPORTANT — the no-release behaviour is INTENDED, not accidental,
    and THREE artifacts encode it. Overturn all three deliberately and visibly: (1) PLANNING_preset_flap_offphase_honesty.md:184-195
    states the ceiling holds until the next preset transition BY DESIGN; (2) :280 is a LIVE ACCEPTANCE
    CRITERION asserting that NO follow-on restore write fires; (3) shipped test `test_ceiling_held_until_next_preset_transition`
    ENFORCES it. That test currently encodes the defect as a guarantee and must be INVERTED, with the
    parked plan annotated to record the operator revisiting the trade on 2026-08-21. Do NOT delete it
    silently. WHY the original trade wanted no release: a release write is another preset transition,
    i.e. flap — the exact thing PRESET-FLAP-1 was fixing. Any ending MUST therefore fire ONCE PER OFF-PHASE,
    not per tick. That is the anti-flap property and the criterion that discriminates a good fix from
    a re-opened flap.'
  TIMER_RESOLVED_BY_THE_BORROW_2026_08_21: 'FINAL SHAPE, after operator asked "Borrow framing good. But
    you want a timer still? Suggestions?" ANSWER: yes, but the borrow ALREADY HAS ONE — do not build a
    second. The lease expiry IS the timer: expiry_ts = min(started + duration_s + EXCURSION_LEASE_SLACK_S,
    started + EXCURSION_LEASE_MAX_S). As a borrow kind, S14 inherits ALL FOUR requirements for free: the
    timer (lease expiry), the hard backstop (LEASE_MAX), restart safety (async_startup_excursion_audit),
    and the preset-write restore (the return path). A bespoke S14 timer would duplicate existing machinery.
    THE ONLY REMAINING DECISION IS WHAT duration_s TO PASS AT begin(). RECOMMENDATION: pass the COMPUTED
    OFF-PHASE REMAINING, clamped by LEASE_MAX. Precedent already in the plan — §4.4 lists "preheat with
    computed hour boundary" as a duration_s case, i.e. the exact shape. THIS DISSOLVES THE EARLIER OBJECTION.
    The orchestrator argued against an off-phase-expiry ending because it would couple S14 release to
    energy-coordinator state (a categorically risky ingredient). But as a duration_s it is A VALUE HANDED
    OVER ONCE AT BEGIN TIME, NOT A STATE READ EVERY TICK. The coupling objected to was continuous; this
    is a single parameter. So the semantically-correct ending and the hard cap are obtained together,
    with no live cross-coordinator dependency. KNOBS: EXCURSION_LEASE_MAX_S already exists as the backstop,
    so S14 likely needs NO KNOB OF ITS OWN. This supersedes the earlier recommendation to add a Number
    entity for the duration — the system can compute it, and a knob for a computable value is an invitation
    to drift. STILL MANDATORY: the ONE-SHOT-PER-OFF-PHASE guard (below). If the duty cycle extends past
    the original estimate the lease expires early, the next tick re-arms, and the slow flap returns. ORIGINAL
    OPERATOR DECISION PRESERVED: "Give it a timer."'
  SUPERSEDED_OPERATOR_CHOSE_A_TIMER_2026_08_21: 'SUPERSEDED by TIMER_RESOLVED_BY_THE_BORROW above — the
    timer is the lease, not a new mechanism, and the duration knob is withdrawn in favour of a computed
    duration_s. The FLAP GUARD in this entry STANDS. Original: operator: "Give it a timer." The ending
    is a BOUNDED TIMER, not the off-phase-expiry discharge originally proposed below. THIS IS THE LOWER-RISK
    CHOICE and supersedes part (1) of the three-part proposal: off-phase-expiry would couple S14s release
    to the energy coordinators off-phase state (cross-coordinator state — a categorically risky ingredient
    per the marginal-benefit rule) to buy a semantic nicety. A timer always fires and adds no seam. Orchestrator
    had over-elaborated; operator picked the simple version. MANDATORY GUARD — A TIMER ALONE FLAPS. Timer
    expires -> release to preset -> next tick sees the off-phase STILL RUNNING -> S14 re-applies the ceiling
    -> manual again -> timer -> release. A slow oscillation replaces the permanent hold, which re-creates
    exactly what PRESET-FLAP-1 was fixing. FIX (keeps the operators simple design): ONE-SHOT PER OFF-PHASE.
    Release once when the timer expires; for the REMAINDER of that off-phase fall back to suppression-only
    (do not flip to away, do not re-write the ceiling). Re-arm on the NEXT off-phase. This preserves the
    discriminating acceptance criterion (release fires once per off-phase, not per tick) with no new coupling.
    STILL REQUIRED TO BE COMPLETE: (a) KNOB for the duration — per the Numbers Get Knobs ladder this is
    policy the operator would legitimately tune by observation, so a NUMBER ENTITY (same rung as drain
    target / arbitrage lead time), NOT a module constant. Kill-switch semantics documented on the knob.
    (b) DECLARED RESTART BEHAVIOUR — if HA restarts mid-hold the timer dies and the zone is stranded in
    manual again, i.e. the ORIGINAL BUG REINTRODUCED. Either PERSIST the timer, or have startup release
    any zone sitting in manual with no live hold record. Declare PERSIST/RESET/REBUILD explicitly; do
    not leave it implicit. (c) RELINQUISH still applies — see part (3) below; the operators setpoint-divergence
    idea is retained as the do-not-stomp check, unchanged.'
  THE_ENDING_NEEDS_THREE_PARTS_NOT_ONE: 'SUPERSEDED IN PART — see OPERATOR_CHOSE_A_TIMER above: part (1)
    is REPLACED by a bounded timer + one-shot-per-off-phase; parts (2) and (3) stand. ORIGINAL: Operator
    proposed reading S14s own interventional setpoint. CORRECT but it is a RELINQUISH signal, not an ENDING
    — it answers "do I still own this?", not "should this still be held?". If nothing else touches the
    thermostat the setpoint reads unchanged forever and the indefinite hold returns. Need all three: (1)
    ENDING / primary discharge — the JUSTIFYING CONDITION EXPIRES. The ceiling exists because of the off-phase;
    release when the off-phase ends. Fires once per off-phase (anti-flap). (2) BACKSTOP — bounded max
    duration, so the hold cannot outlive its reason if the discharge misfires or a restart eats the state.
    Declare restart behaviour (PERSIST/RESET/REBUILD). (3) RELINQUISH — the divergence check: if the current
    setpoint no longer matches what S14 wrote, someone else owns the zone; disarm SILENTLY rather than
    stomp them. Needs TOLERANCE (cloud echo + rounding make exact comparison flaky) — REUSE the v5.51.1
    echo-guard, do not hand-roll. THE RELEASE ITSELF MUST BE A PRESET WRITE per the operator hygiene rule.
    Restoring preset GOVERNANCE is the point; just putting the number back leaves the zone in manual.'
  RECOMMENDATION_MAKE_IT_A_BORROW_NOT_A_BESPOKE_ENDING: 'STRONG RECOMMENDATION — do NOT build a bespoke
    S14 ending. Bounded hold + snapshot + preset restore + relinquish-on-divergence + restart audit IS
    the governed-excursion ("borrow") primitive under HVAC-GOVERNED-EXCURSION-1. S14 was EXCLUDED from
    that cycle for exactly ONE reason — it was an INDEFINITE hold (plan §12 parked-plan trigger, verbatim:
    "If the operator wants the ceiling to self-release, that is a separate card that re-litigates PRESET-FLAP-1").
    The operator decision removes the indefiniteness, so the exclusion rationale evaporates. Building
    a second differently-shaped restore path is PRECISELY how this codebase reached 13 setpoint-write
    sites of which 9 never restore a preset. Sequence: let the primitive land and live-validate, THEN
    migrate S14 as a kind.'
  parsimony:
    problem: an energy-saving hold with no exit locks the zone out of preset control indefinitely
    verdict: BUILD
    note: 'operator 2026-08-21 — the borrow framing "makes sense honestly but you do not need to chain
      it. You can address that fix after the borrow work is done." So this is SEQUENCED AFTER the borrow
      primitive, NOT GATED ON IT: it does not block, and it must not be folded into that cycle. Migrate
      as a kind rather than hand-rolling a second restore path.'
  next: 'Scope S14 as a borrow kind: bounded-timer ending, one-shot-per-off-phase (discriminating acceptance),
    Number duration knob, restart behaviour; INVERT test_ceiling_held_until_next_preset_transition. Gate
    cleared 2026-08-25.'
  refs:
  - hvac.py:2972-2983
  - hvac_preset.py:202
  - PLANNING_preset_flap_offphase_honesty.md:184-195,:280
  - PLANNING_hvac_governed_excursion.md §12
  - test_ceiling_held_until_next_preset_transition
  - HVAC-MANUAL-PRESET-CONTRACT-1
  - HVAC-GOVERNED-EXCURSION-1
  unblocked_2026_08_25: 'GATE CLEARED: HVAC-GOVERNED-EXCURSION-1 is validated+done (live DB). S14 is now
    scopeable as a borrow kind (bounded timer + one-shot-per-off-phase, Number-entity duration knob, declared
    restart behaviour) per the operator''s 2026-08-21 decision. Ready to plan/build; still sequenced AFTER
    the borrow primitive, not folded into it.'
- id: HVAC-MANUAL-PRESET-CONTRACT-1
  DEDUPE_2026_08_23: 'RULING: DUPLICATE — a card `PRESET-RESTORE-DOES-NOT-TAKE-1` was created on 2026-08-23
    and has been DELETED; its content is folded into this card as the INSTANCE block below plus SCOPE_ONE_OR_MANY
    / CARRIER_MECHANISM_HYPOTHESIS / CANARY_BEFORE_BUILD. WHY IT MERGED: this card''s own TITLE already
    asks the question the new card claimed to raise — "do the sanctioned excursions return?" — and its
    existing `next` already scopes the exact work (enumerate every set_temperature site + whether it has
    a restore-to-preset path). Same problem, same fix surface. Per the skill: a newly-MEASURED occurrence
    of an already-carded defect is an INSTANCE, not a card. SWEPT AGAINST (four surfaces): board — HVAC-PRESET-RESTORE-MISS-1
    (inbox; itself already flagged as a duplicate of this card by AUDIT_card_merit_review_2026_08_20),
    AC-RESET-PRESET-ASSERT-DROPPED-1 (shipped v5.88.1, reset-path only), HVAC-PRESET-FLAP-1 (parked, blocked
    on THIS card — its stale assumption now refuted on that card). Code — hvac_override.py preset-restore
    sites, hvac_excursion.py:710, hvac_setpoint.py emit_set_preset_mode. Vendor — ha_carrier climate.py.
    Docs — AUDIT_card_merit_review_2026_08_20, README_v5.89.0. PROCESS NOTE, recorded because the failure
    is the reusable part: the duplicate was created by hand-editing kanban.data.yaml directly instead
    of invoking the ura-kanban skill. The skill''s SECOND RULE is the adjacency sweep before any new card
    is accepted, and it names this exact case (INSTANCE vs card). The skill was correct; it was bypassed.
    The operator caught the duplicate, then caught that the skill should have. Board writes go through
    the skill.'
  INSTANCE_2026_08_23_do_the_excursions_return: 'THIS CARD ASKS "do the sanctioned excursions return?"
    IN ITS OWN TITLE. MEASURED ANSWER: NO. Sampled the live climate entities at T+1/2/5/10/20/30 min after
    every nudge_restored, 47 paired nudges, from the recorder: intent == "manual" (restore writes manual
    OVER manual — a NO-OP):  97% "match" at T+1m intent is a REAL preset (away/home/sleep):  0/10 at T+1m
    AND T+2m; 1/10 (10%) from T+5m onward MORE SETTLE TIME DOES NOT HELP: 77% aggregate at T+1m vs 72%
    at T+30m, flat-to-declining. The aggregate only ever looked healthy because 37 of the 47 intents were
    manual-over-manual no-ops. So the excursion RETURNS ITS SETPOINT (912/914 nudges pair with a restore)
    but DOES NOT RETURN ITS PRESET when there is a real preset to return to. That is the precondition
    this card names, now quantified rather than inferred. NOT A DEAD MECHANISM: over the same window zone_1
    held `away` 43.8h and `home` 22.8h across 381 preset changes. The house-state writer (hvac.py:2063)
    writes presets successfully, through the SAME emit_set_preset_mode chokepoint, and the nudge path
    passes no gate that could drop the write. The failure is specific to restoring immediately after a
    nudge. LIKELY THE MECHANISM BEHIND THIS CARD''S OWN HEADLINE NUMBER — duration-weighted dwell in `manual`:
    zone_1 62.1% (114.3h), zone_2 46.3%, zone_3 26.1%; `sleep` holds only 1.7% / 2.1% / 8.9%. Every nudge
    flips the zone to manual and the restore does not bring it back.'
  SCOPE_ONE_OR_MANY_2026_08_23: 'OPERATOR ASKED: "Is the restore via a borrow or its own mechanism? Just
    want to understand if we fix one thing or many." ANSWER: MANY. The borrow/excursion primitive is BOOKKEEPING
    ONLY. hvac_excursion.py:710 says it verbatim: "each site performs its own (a) set_temperature -> (b)
    set_preset_mode". return_excursion clears the lease and increments counters; it issues NO thermostat
    write. So the setpoint-then-preset sequence is duplicated at every restore site — this card''s existing
    `next` (enumerate every set_temperature site + whether it has a restore-to-preset path) is exactly
    the right scope and should be run as written. Sites seen in one grep of hvac_override.py: _revert_override,
    _restore_after_reset, _restore_after_nudge, cancel_nudge, plus the house-state writer in hvac.py which
    is the one that WORKS. TREAT THAT LIST AS A HYPOTHESIS and re-enumerate independently — Bug Class
    #53 is the "one missed site" shape and a partial fix is the failure mode. STRONG CANDIDATE: move the
    (a)->(b) sequence INTO return_excursion so there is one implementation and the borrow becomes the
    chokepoint it already appears to be from outside.'
  CARRIER_MECHANISM_HYPOTHESIS_UNMEASURED_2026_08_23: 'READ FROM ha_carrier SOURCE, NOT TESTED. Labelled
    explicitly because four mechanisms asserted on 2026-08-22/23 were later refuted by measurement — do
    not build on this without the canary below. (1) `async_set_preset_mode` (climate.py:381) for a NAMED
    preset calls `set_config_hold(activity_type, hold_until)` — it PINS A HOLD on that activity, and applies
    that activity''s OWN setpoints (`selected_activity.cool_set_point` / `heat_set_point`), then optimistically
    writes local state. (2) THEREFORE the separate set_temperature write in the restore sequence may be
    REDUNDANT — the activity carries its setpoints — and may itself be the cause: two writes to the same
    zone seconds apart, where the setpoint write is what creates the manual hold. (3) The optimistic local
    write explains why T+1m already reads `manual`: the next real coordinator refresh (MEASURED 42-79s
    median, NOT the 30-min DEFAULT_UPDATE_INTERVAL_MINUTES constant — that misreading caused a wrong ruling
    this cycle) overwrites it with cloud truth. (4) OPERATOR DECISION NEEDED BEFORE BUILD: the integration
    exposes a special `"resume"` preset calling `resume_schedule`, which CLEARS the hold and returns the
    zone to its programmed schedule. Restoring `home` PINS A HOLD on the home activity. These are different
    actions and today we do the first. Operator directive is "exit via preset if possible" — which one?'
  CANARY_BEFORE_BUILD_2026_08_23: 'One night, one zone: restore via PRESET ONLY (skip the set_temperature
    write) and sample preset at T+1/2/5 min against the measured 0/10 baseline. DISCRIMINATING: preset-only
    succeeding while setpoint+preset fails CONFIRMS the collision hypothesis and REFUTES "Carrier rejects
    activity holds". If it takes, the fix is DELETING a write, not adding machinery. Cheap and reversible.
    Do NOT add settle-delay machinery — time is measured NOT to be the variable. Do NOT patch a single
    site.'
  title: Design spec says control the thermostats via PRESETS, never raw manual setpoints — reality is
    zones sitting in manual for hours; do the sanctioned excursions return?
  thread: hvac
  status: planned
  PRECONDITION_AND_SEVERITY_2026_08_20: 'OPERATOR AGREED this is a PRECONDITION for the calming work,
    not a sibling. Quantified from the same 48h preset series: zone 1 sat in manual from 01:45 to 16:20
    on 2026-08-20 — 14h35m unbroken, LONGER than the 10.5h instance the handoff attributed to the AC-nudge
    race. Zone 2: 00:18->09:40 then 14:41->16:20, ~11h. While a zone is in manual, should_change_preset
    (hvac_preset.py:202-217) returns False for everything, so vacancy_grace and zone_entry_dwell are downstream
    of a CLOSED GATE and change nothing for that zone. On today''s numbers zone 1 was tunable for roughly
    9 of 24 hours. Any calming result measured before this lands will report a suppression that was really
    an outage — the exact non-discriminating-observation error that caused the EVSE misdiagnosis.'
  S14_OPERATOR_VERDICT_2026_08_20: 'OPERATOR ON THE OFF-PHASE CEILING HOLD (S14, the v5.73.0 PRESET-FLAP-1
    fix): "Probably why i instinctively disabled it. It bothered me subconsciously. We need to fix it.
    I think of it as a marginal feature we possibly should not have built at all." TWO THINGS THIS SETTLES.
    (1) It explains a previously unexplained fact — the operator disabling the honesty mechanism was not
    arbitrary, it was a response to a real defect they could feel but had not localised. (2) It downgrades
    the feature from "shipped capability to preserve" to "marginal, fix or remove" — which changes the
    fix-up calculus. The cheapest correct outcome may be REMOVAL rather than repair, and that option must
    be costed alongside the fix. THE DEFECT, restated for whoever scopes it: S14 writes a raw setpoint
    to hold the cooling ceiling during a coast/shed off-phase in an occupied zone (hvac.py:2972-2983,
    emit_set_temperature). The thermostat therefore goes to `manual`. Its documented exit is "holds until
    the next preset transition" — and that exit is STRUCTURALLY UNREACHABLE, because should_change_preset
    (hvac_preset.py:202-217) returns False whenever the current preset is `manual`. S14 creates the exact
    condition that prevents its own exit. It ends only when something OUTSIDE the preset path moves the
    zone: a house-state change routing around the gate, a human at the thermostat, or a restart. No timer,
    no decay, no explicit restore anywhere in the module. UNDER THE OPERATOR HYGIENE RULE this is the
    canonical example: S14 IS a sanctioned energy-saving excursion, so the raw setpoint is legitimate
    — but the rule also says the REVERT must be a preset write, and S14 has no revert at all. So the fix
    is not "stop using setpoints here", it is "give it an ending": a preset write on exit plus a bounded
    duration so the hold cannot outlive the off-phase that justified it. MARGINAL-BENEFIT DECOMPOSITION
    (the standing pushback duty, applied): the SIMPLEST version of what S14 was built to achieve is "do
    not flip an occupied zone to away during a duty off-phase" — i.e. suppress the dishonest away, and
    do nothing else. That captures the whole founding complaint (nine flips in two hours of confirmed
    occupancy, room parked at ambient). The ADDITIONAL thing S14 does is actively hold a raised ceiling
    via a setpoint write, and THAT is what introduced the indefinite-manual ingredient. So the marginal
    benefit of the ceiling hold over plain suppression is a couple of degrees of coast precision; the
    marginal cost is a permanent-manual writer that locks the zone out of preset control. On that comparison
    the ceiling hold does not obviously pay for itself, which is consistent with the operator calling
    it marginal. RECOMMEND the scoping doc cost three options, not one: (a) add an ending (timer + preset
    revert), (b) reduce to suppression-only and delete the ceiling write, (c) remove S14 entirely and
    accept the original flip. Do not assume (a).'
  OPERATOR_RULINGS_2026_08_20: 'THREE OPERATOR RULINGS that resize this cycle. (1) "URA reverts deliberate
    human adjustments" is NOT a risk — it is INTENDED. Operator: "Its supposed to. I''m happy with that.
    The arrester already has its own grace and mid point math." So my second-order concern (a) is WITHDRAWN,
    and the _suppress_kind TTL work is no longer a blocking prerequisite on those grounds (it may still
    matter for attribution/telemetry, but it does not gate the fix). (2) THE GOVERNING DESIGN PRINCIPLE,
    operator verbatim: "I prefer all thermostat writes to appear as presets vs manuals. Its just better
    hygiene. Except when we need to save energy and then the reverting should be a preset write." This
    is stronger and simpler than the framing the card carried. It converts the cycle from "do the sanctioned
    excursions return?" into a flat rule: EVERY write is a preset write; the ONLY sanctioned raw-setpoint
    excursions are energy-saving ones; and the revert at the end of an energy excursion is ITSELF a preset
    write. That gives the builder a single testable invariant instead of a 13-site case analysis. (3)
    Tier 3 confirmed by operator.'
  CLOUD_FEEDBACK_LOOP_RETRACTED_2026_08_20: 'RETRACTED — I asserted a third-order feedback loop (more
    preset writes -> more Carrier 504s -> more phantom overrides -> more manual -> re-lockout) and the
    operator asked whether I had actually found evidence of calls not landing. I had not, and looking
    properly, there is NONE. A structured scan of the full HA error log window (2026-08-20 13:28 -> 18:45,
    14,102 raw lines, 12,652 parsed entries, 191 unique issues across 32 components) returns ZERO entries
    from any carrier / bryant / infinitude thermostat component — the component list does not contain
    one at all. What I actually had was the 2026-08-19 observation that all three climate entities went
    unavailable 12:14:15 -> 12:15:17 together. That is a READ-SIDE availability gap, not a failed write.
    I generalised an outage into a write failure mode and then built a feedback loop on top of it — the
    same infer-mechanism-from-thin- evidence error class as the zone_cameras mistake. CONSEQUENCE: the
    write rate limit LOSES its urgency justification. It may still be worth having as a cloud-call budget,
    but it is NOT a loop-breaker and must NOT be sold as one, and it no longer has to ship in the same
    cycle. IF someone wants to revive the loop hypothesis, the discriminating evidence would be logged
    write failures or set_preset_mode calls that do not land, and neither exists today.'
  DPM_LAST_ON_AND_SIGNATURE_2026_08_20: 'OPERATOR HYPOTHESIS — "I think I turned it off today, it might
    be the main culprit." SPLIT VERDICT: refuted on timing, STRONGLY SUPPORTED on mechanism. TIMING —
    REFUTED. It was not turned off today, it was turned off SEVEN DAYS AGO. The switch reads `off` at
    2026-08-13 23:17:07 CDT and every record after that is `off` (the later `unavailable` entries are
    restart artifacts, not state changes). The better oracle agrees and is more precise: sensor...dynamic_preset_overrides_applied
    held 3 (all three zones overridden) from 2026-08-12 23:34 and dropped to 0 at 2026-08-14 02:31:06,
    where it has stayed for six days. So DPM cannot explain today''s manual — zone 1 sat in manual 01:45
    -> 16:20 TODAY with DPM long since idle. NOTE the recorder shows no switch state-changes between 08-10
    and 08-13 18:20, so the applied-count is the authoritative signal for when DPM was actually acting.
    MECHANISM — SUPPORTED, and this is the valuable half. Comparing zone 1 while DPM WAS applying overrides
    against today shows two DIFFERENT pathologies, which is exactly the discriminating observation this
    thread has been missing. With DPM active on 08-13, zone 1 oscillated manual <-> away EVERY FIVE MINUTES
    from 16:10 to 17:25 — manual 16:15, away 16:20, manual 16:25, away 16:30, manual 16:35, away 16:40,
    manual 16:45, away 16:50, manual 16:55, away 17:00, manual 17:01. That is the exact signature of a
    raw setpoint write and a preset write fighting on every tick: S10_dpm_apply pushes a setpoint (thermostat
    -> manual), the preset path pushes `away` back, and the next tick repeats it. With DPM off today,
    zone 1 instead shows ONE CONTINUOUS 14h35m manual block — a lock, not an oscillation. Different shape,
    different cause. CONCLUSION: DPM is not the current culprit, but it is a PROVEN manual-inducer with
    production evidence, not just a code-reading inference. That upgrades "DPM should emit presets, not
    setpoints" from a tidy-up to an evidenced deliverable, and it is a strong reason to leave Auto-Adjust
    OFF until this cycle ships.'
  DPM_KILLSWITCH_RETRACTION_GAP_2026_08_20: 'SMALLER FINDING, needs verification before it is treated
    as a defect. The switch went `off` at 08-13 23:17:07 but overrides_applied stayed at 3 until 08-14
    02:31:06 — a three-hour-fourteen-minute gap, and the moment it finally cleared coincides with a restart
    (`unavailable` at 02:31:02). Two candidate readings, not yet distinguished: either (a) flipping the
    kill switch does NOT retract overrides already applied, leaving them in force until a restart, or
    (b) the evaluate loop is skipped when the switch is off so the sensor simply went stale at its last
    value and nothing was actually still applied. (a) would be a real kill-switch defect of the "suppression
    needs a discharge" class; (b) is a cosmetic observability bug. The discriminating check is whether
    the thermostats held DPM ranges during that window. Do NOT card as a defect until that is checked.'
  DPM_RECON_2026_08_20: 'Operator instructed: reconnoitre Dynamic Preset Auto-Adjust and Custom Preset
    ranges before doing any of this, noting they can be gated off. DONE. FINDING: DPM IS CURRENTLY OFF
    and is therefore NOT a contributor to today''s manual. switch.ura_energy_coordinator_dynamic_preset_overrides
    ("HVAC Coordinator 02 · Dynamic Preset Auto-Adjust") = off; sensor...dynamic_preset_overrides_applied
    = 0; all three per-zone range sensors (back_hallway, entertainment_master_suite, upstairs) = unknown;
    all three bucket sensors = mild. Knobs live: Settle Time 60.0 min, Preset Margin 1.0F. BUT IT IS A
    LANDMINE FOR THIS CYCLE, and this is the reason the operator was right to make me look: DESPITE ITS
    NAME, DPM DOES NOT WRITE PRESETS. Its apply site S10_dpm_apply (hvac.py:2274-2285) calls emit_set_temperature
    with target_temp_low/high — a RAW SETPOINT write, which is exactly what puts a Bryant/Carrier zone
    into manual. It suppresses the ARRESTER (hvac.py:2258) so URA does not book its own write as a human
    override, but suppressing the arrester does nothing about the thermostat''s own preset state. So the
    moment Auto-Adjust is switched on, DPM becomes a recurring manual-inducer on every range change, on
    a 60-min settle cadence, per zone. Under operator ruling (2) above — every write should be a preset
    write — DPM is squarely IN SCOPE and is arguably the clearest example of the hygiene problem: a feature
    literally named "preset" that emits setpoints. Custom Preset ranges are the per-zone CONF_ZONE_DYNAMIC_PRESET_COOL_*
    knobs consumed by the same module. RECOMMENDATION: leave Auto-Adjust OFF until this cycle lands, and
    make "DPM emits presets, not setpoints" an explicit deliverable rather than discovering it later.'
  SECOND_AND_THIRD_ORDER_2026_08_20: 'PARTIALLY SUPERSEDED — item (a) withdrawn per operator ruling (1)
    above, item (d) RETRACTED for lack of evidence per CLOUD_FEEDBACK_LOOP_RETRACTED above. Items (b)
    solar-banking interruption, (c) S14 coupling and (e) recorder rows STAND. ORIGINAL: Operator asked
    for consequence analysis and flagged Tier 3. SECOND ORDER: (a) THE ARRESTER''S MEANING INVERTS. Some
    of today''s manual is URA''s OWN writes. If URA reasserts presets aggressively it will also revert
    DELIBERATE HUMAN thermostat adjustments — the fix turns a lockout into URA-fights-the-occupant. It
    therefore REQUIRES the provenance tag to actually work first: _suppress_kind (hvac_override.py:185)
    is consulted at exactly ONE site (:1635-1638, a counter) and carries a ~5 SECOND TTL while manual
    persists for HOURS. Extending provenance lifetime is a hard prerequisite, not a nice-to-have. (b)
    SOLAR BANKING / PRE-COOL / PRE-HEAT BECOME INTERRUPTIBLE — this is the biggest risk. hvac_predict.py
    owns S11 (:957), S12 (:1047), S13 (:1286) and contains ZERO emit_set_preset_mode calls. The manual
    state is very likely what PROTECTS an in-flight pre-cool from being overwritten by the next tick''s
    preset. Reasserting preset control could CANCEL SOLAR BANKING MID-FLIGHT — a direct energy regression,
    in the opposite direction from the cycle''s intent. The sanctioned-excursion list must be enumerated
    and exempted BEFORE any restore logic ships. (c) S14 COUPLING (hvac.py:2974) deliberately replaced
    a preset write with an indefinite raw setpoint hold, dated to HVAC-PRESET-FLAP-1 (2026-08-11). Restoring
    preset control there re-arms the flap S14 was built to stop. THIRD ORDER: (d) THE FEEDBACK LOOP —
    this is the headline. Unlocking 9-14h/day of suppressed preset writes across three zones sharply increases
    Carrier/Bryant CLOUD API calls, into a cloud that ALREADY 504s (see ARRESTER-CLOUDFLAP-FALSEPOS-1:
    a 504 drops all three zones together and books a phantom override 20s after reconnect). More writes
    -> more 504s -> more phantom overrides -> MORE MANUAL -> re-lockout. The fix can manufacture the very
    condition it removes. CONCLUSION: the write rate limit is not a follow-on, it must ship WITH the lockout
    fix as the loop-breaker. (e) More preset changes also mean more recorder rows, on flash at 51% life
    (RECORDER-BLOAT-LOGFLOOD-1). Tier 3 confirmed: shared primitive, cost-AND-comfort impacting, and the
    failure mode is one-missed-site across 13 setpoint-write sites of which 9 never restore a preset.'
  ARCHITECTURAL_REFRAME_2026_08_21_DECIDER_VS_WRITER: 'OPERATOR REFRAME, and it renames the root defect.
    Operator: "if the preset writer is just a method, then it doesn''t decide anything. Its called by
    a function that has made a decision." The principle is right; the code does NOT honour it, and checking
    that is what produced this entry. WHAT IS ACTUALLY TRUE (verified 2026-08-21, not inferred). _apply_house_state_presets
    (hvac.py:1411) is called BARE from the tick at hvac.py:1333 — no arguments, no target passed in, no
    decision handed down. It DERIVES the target from house state AND writes it, in one method. So it is
    neither a pure writer nor a clean decider: the two roles are FUSED. The upstream decider the operator
    is describing does not exist yet. THE DECISION IS SMEARED ACROSS FIVE PLACES: (1) the tick''s unconditional
    call, hvac.py:1333; (2) the derive-from-house-state inside the method; (3) gates INSIDE the method
    — D6 consensus defer, observation mode, two switch-driven skips; (4) should_change_preset in a DIFFERENT
    FILE (hvac_preset.py:202), whose own docstring says the quiet part out loud — "arrester handles this,
    not preset manager"; (5) hvac.py:1892, a branch that explicitly BYPASSES (4): "# Bypass should_change_preset()
    manual guard for vacancy (RH3 fix)". ITEM (5) IS THE TELL. When one caller must route around its own
    predicate, the predicate is holding POLICY that belongs to the caller. should_change_preset is named
    as a predicate and behaves as a policy authority — which is also exactly why it is a PURE TWO-STRING
    FUNCTION that cannot consult provenance (see SELF_LOCKOUT_CONFIRMED). The self-lockout is not a bug
    IN should_change_preset; it is the predictable consequence of putting a policy decision somewhere
    that structurally cannot see the facts the decision needs. THIS RENAMES THE CYCLE. Not "zone 1 got
    stuck in manual" but: NO SINGLE THING OWNS THE PRESET DECISION, so five things partially own it, and
    the manual lockout is one emergent symptom of that. The target architecture is three layers with opinions
    in exactly one: DECIDER (owns policy: what should this zone be in, given house state, provenance,
    and any active excursion) -> WRITER (mechanism: emits, no opinion) -> BORROWER (excursion: snapshots
    and restores what it found, no opinion). The unopinionated ruling of 2026-08-20 scopes to RESTORE
    paths and WRITER paths; it does NOT mean nobody decides. ALSO: THERE ARE FOUR PRESET EMIT SITES, NOT
    ONE — hvac.py:2013, hvac_override.py:2554 and :3258, hvac_egress.py:677. Any plan claiming to gate
    "the preset write" must enumerate against that list, not against a singular mental model. SCOPE RULING
    (operator agreed 2026-08-21): the decider/writer split is ITS OWN cycle and must NOT be folded into
    the governed-excursion build, which is already Tier 3. Folding would double the blast radius. The
    excursion lands its lease at the emit merge point (correct regardless of how the layering is later
    resolved); this card owns the layering.'
  STALE_LINE_REFS_2026_08_21: 'WARNING TO WHOEVER SCOPES THIS: several line refs in the older entries
    below have DRIFTED by roughly +32 lines since they were written on 2026-08-20. Verified examples:
    should_change_preset sole call site recorded as hvac.py:1864 is now hvac.py:1896; the vacancy bypass
    recorded as hvac.py:1860 is now hvac.py:1892. The SYMBOL names are still correct and are the reliable
    way in — grep the identifier, do not seek the line number. Re-verify every file:line in this card
    before relying on it.'
  created: 2026-08-20 14:40
  updated: 2026-08-25 22:30
  refinement_status: reframed_architectural_root
  approval: unreviewed
  tags:
  - no-fabrication-verify
  - institutional-context
  - tier-2db
  problem_solution:
  - 'Problem: the thermostats are supposed to be driven by named presets that each carry a fixed temperature
    range, so the house always returns to known settings. Instead one zone spent over ten hours sitting
    on a hand-set temperature pair, straight through the morning changeover. Only two features are supposed
    to set a raw temperature at all (banking solar energy, and pre-cooling), and it is not confirmed that
    either of them puts the thermostat back on a preset when it finishes. Solution: establish which features
    write raw temperatures, confirm every one of them restores a preset afterwards, and close any path
    that can leave a zone stranded off-preset.'
  why: 'OPERATOR SPEC (verbatim 2026-08-20): "The design spec was to not use manual but to use our presets
    as control. That way it always returns to same ranges. In addition we always want to use heat_cool.
    Have seen that part hold so far. Manual setpoint is messy. I think what''s allowed as manual is solar
    banking and pre-cool. But wonder if they escape back to preset when they revert." MEASURED AGAINST
    IT: zone_1 held preset `manual` continuously 01:42 -> 12:15:17 on 2026-08-20, through ~6h of home_day,
    and shows 13 DISTINCT setpoint pairs under manual on a 5-min grid vs exactly ONE pair under `home`.
    That is URA writing raw setpoints at volume, which is the opposite of the stated contract.'
  AUDIT_RESULT_2026_08_20_INVARIANT_VIOLATED: 'OPERATOR''S QUESTION ANSWERED: NO, they do NOT escape back
    to preset. 13 setpoint-write sites (S1-S14, all funnelling through the hvac_setpoint.py chokepoint
    emit_set_temperature:121 / emit_set_preset_mode:180). NINE induce manual and NEVER restore a preset.
    hvac_predict.py — which owns solar banking (S11 :957), pre-cool (S12 :1047) and pre-heat (S13 :1286)
    — contains ZERO emit_set_preset_mode calls; proof: grep _nudge_pre_preset|pre_preset hits ONLY hvac_override.py.
    They write setpoints back to baseline and return, leaving manual with correct numbers but no preset
    governance. The ONLY preset-restore in the codebase is S7 (hvac_override.py:3232, AC soft nudge) and
    it is racy. Note S13 pre-heat is NOT in the operator''s sanctioned pair (banking + pre-cool) yet writes
    setpoints anyway.'
  SELF_LOCKOUT_CONFIRMED: hvac_preset.py:202-217 should_change_preset returns False whenever current_preset
    == "manual" ("Do not fight manual — that is the arrester's job"). It is a PURE TWO-STRING FUNCTION
    — no hass, no entity_id, no arrester ref — so it CANNOT consult provenance even in principle. Sole
    call site hvac.py:1864; only bypass is vacancy (hvac.py:1860, RH3 fix). The provenance tag DOES exist
    — _suppress_kind (hvac_override.py:185, set :1942) — but is consulted at exactly ONE place (:1635-1638,
    the mid-window manual-passthrough COUNTER) and has a ~5s SUPPRESS_TTL_SECONDS while manual persists
    for HOURS. So URA knows an induced manual is not genuine, but only for COUNTING it; the preset manager
    that decides whether to ACT never sees the tag. Genuine self-lockout with no self-recovery path.
  ZONE1_STRANDING_ATTRIBUTED: 'AC SOFT NUDGE (S5/S6/S7), losing a write-ordering race. High confidence.
    Recorder, climate.thermostat_bryant_wifi_studyb_zone_1: 01:42:05.138 sleep 70/75 (S7 preset restore
    LANDS) -> 01:42:05.647 manual 70/75 (509ms later, clobbered) = last non-manual of the day; an identical
    212ms flip at 01:12:05. MECHANISM (two defects compounding): (1) RACE — _restore_after_nudge issues
    its setpoint restore blocking=False (hvac_override.py:3197-3204) then immediately emits the preset
    restore (:3221-3237); on a cloud-polled Bryant the setpoint lands AFTER and its manual side effect
    clobbers the preset. (2) LATCH — _nudge_pre_preset only snapshots a NON-manual preset (:3083-3089),
    so once the race left the zone in manual every later nudge stored NO snapshot and its restore did
    nothing. THE FIX-B2 REPAIR MECHANISM DISARMS ITSELF THE FIRST TIME IT FAILS. should_change_preset
    then refused to re-assert, the S1 home_day transition was silently skipped, and nothing in the codebase
    can exit that state — 10.5h manual through ~6h of home_day, ended at 12:15:17 by something outside
    URA. The 25-min 75<->76 cadence = nudge start/restore pairs (CONF_HVAC_AC_NUDGE_SIZE/_DURATION hvac_const.py:505-509),
    matching ac_nudges_today=43. The operator''s "13 distinct pairs under manual" is the direct fingerprint.'
  IRONY_FLAP_FIX_CREATED_A_MANUAL_WRITER: S14 hvac.py:2974 _apply_duty_off_phase DELIBERATELY replaced
    a preset write with an indefinite RAW SETPOINT hold — and its own comment dates it to "HVAC-PRESET-FLAP-1
    (2026-08-11)". So the flap fix introduced a permanent-manual writer. Its comment also misattributes
    WHY the S1 preset path no-ops (claims target==current; actually because S14 just induced manual).
    Direct coupling between this card and HVAC-PRESET-FLAP-1 — they MUST be scoped together, and the flap
    fix must not be re-tuned without accounting for S14.
  HEAT_COOL_INVARIANT_HOLDS: 'OPERATOR CORRECT. grep set_hvac_mode: no path ever writes cool/heat/ fan_only/auto.
    hvac.py:1489 and hvac_override.py:2526 write the "heat_cool" literal. Only non-heat_cool value written
    is "off", always paired with a restore: AC Reset (hvac_override.py:2769 -> :2836/:2871 restore with
    30s verify + 2 retries + ERROR on failure) and egress window-pause (hvac_egress.py:572 -> :644). coordinator.py:1036
    refuses raw climate.set_hvac_mode from generic action execution. RESIDUAL HAZARD: hvac_egress.py:644
    restores the SAVED mode rather than forcing heat_cool, and :625-638 aborts the resume on a missing
    saved_mode, leaving the zone off until the next tick guard.'
  SANCTIONED_EXCURSION_KNOBS: 'Both exist and are individually disableable. Solar banking/pre-cool: switch.ura_energy_energy_precool
    (switch.py:1216-1218) backed by CONF_ENERGY_PRECOOL_ENABLED (migrated from retired CONF_HVAC_SOLAR_BANK_ENABLED,
    __init__.py:1228-1231); tuning via CONF_HVAC_SOLAR_BANK_FLOOR (number.py:2319), CONF_HVAC_SOLAR_BANK_SOC_MIN,
    CONF_ENERGY_PRECOOL_OFFSET. Predictive pre-cool/pre-heat: switch "28 HVAC Predictive Conditioning"
    (switch.py:1823) — ONE switch covers BOTH legs, pre-heat not separately disableable. Unsanctioned
    writers also have knobs: S14 switch.ura_hvac_coordinator_offphase_honesty_enabled (switch.py:5953);
    S5 CONF_HVAC_AC_NUDGE_SIZE/_DURATION; S10 _guest_mode_actuation_enabled (hvac.py:2162).'
  DEFECT_CHAIN_SUMMARY: '1) Nine of thirteen setpoint sites induce manual and never restore a preset.
    2) The single restore that exists races its own non-blocking setpoint write and can lose. 3) When
    it loses, the "only snapshot non-manual" guard PERMANENTLY disarms the repair. 4) should_change_preset
    then refuses to re-assert any preset because it sees manual, and structurally cannot tell URA-induced
    from human-induced. Net: URA locks itself out of preset control indefinitely with no self-recovery
    path.'
  SUPERSEDED_SUSPECTED_SELF_LOCKOUT: 'Candidate mechanism worth confirming: a URA set_temperature flips
    Bryant/Carrier to preset manual as a SIDE EFFECT (hvac_override.py:186-195, :3070-3097). Separately
    hvac_preset.should_change_preset (~:212-217) deliberately SKIPS acting when it sees `manual` ("do
    not fight a dial-set hold — that is the arrester''s job") and is NOT aware whether the manual was
    human-caused or URA-caused. If both hold, URA can induce manual and then refuse to act on it — a self-lockout
    from its own preset control. UNCONFIRMED; audit dispatched 2026-08-20.'
  RELATIONSHIP_TO_SIBLINGS: 'This is very likely the ROOT of HVAC-PRESET-RESTORE-MISS-1 (zone_1 stranded
    through the 06:01 home_day boundary) — that card may collapse into this one once the audit lands.
    It is the mirror image of HVAC-PRESET-FLAP-1: the flap is TOO MANY preset writes on zone_3, this is
    preset writes NOT HAPPENING on zone_1 because manual blocks them. Read the two together; they are
    plausibly two faces of one precedence defect.'
  sibling_of:
  - BORROW-BANKING-LEASE-NOT-RELEASED-1
  - S14-CEILING-NEEDS-AN-ENDING-1
  parsimony:
    problem: URA writes raw setpoints at volume and can strand a zone off-preset, against an explicit
      operator design contract
    verdict: BUILD
    note: gate on the audit — if every excursion already restores, this reduces to a guard + a test rather
      than a redesign
  next: 'Scope as one excursion-restore cycle with BORROW + S14: restore-path for every raw-setpoint excursion
    + fix/guard the should_change_preset manual self-lockout. Buildable; audit done 2026-08-25.'
  refs:
  - hvac_preset.py:212-217
  - hvac_override.py:186-195, 3070-3097
  - HVAC-PRESET-RESTORE-MISS-1
  - HVAC-PRESET-FLAP-1
  audit_2026_08_25_orchestrator: 'Fresh-look audit (partial): MECHANISM CONFIRMED in current code. should_change_preset
    (hvac_preset.py:214-219) returns False when current_preset==''manual'' (''Don''t fight manual — that''s
    the arrester''s job'') — so once a zone is in manual the preset MANAGER will never restore it. A raw
    set_temperature write flips preset_mode -> manual (hvac_override.py:220). NUDGE has restore paths
    (hvac_override.py:4327 soft-nudge restore, :5525/5539 cancel_nudge restore — matches ''nudge returns
    cleanly''). NOT YET ENUMERATED: whether solar-banking / pre-cool / the S10/S14 emit_set_temperature
    sites (hvac.py:2445, :3149; hvac_override.py:3125, :4155) each restore a preset — the memory ''resume
    2026-08-20'' claim of 9/13 sites never restoring needs the full per-site table. Problem is REAL (measured:
    zone_1 manual 10.5h). VERDICT: holds up as a real problem with a confirmed self-lockout, but per the
    card''s own gate it is NOT drop-in-buildable — the full set_temperature-site restore-path table must
    complete first to decide guard+test vs redesign.'
  disposition_2026_08_25: 'CONFIRMED buildable (audit no longer gating). Mechanism proven: (1) should_change_preset
    self-lockout (hvac_preset.py:214-219 returns False on manual); (2) banking — a sanctioned excursion
    — provably does NOT restore (BORROW finding: 0 ended events, 2 stuck-open). So NOT every sanctioned
    excursion restores -> this is NOT just guard+test, it needs real restore paths. ADJACENT to BORROW-BANKING
    (banking release) and S14 (borrow-kind ending): all three are the same family — ''every raw-setpoint
    excursion needs a bounded ending that restores the preset, and the preset manager must recover a manual
    zone no owner holds''. Recommend scoping them as ONE excursion-restore cycle, not three. Remaining
    detail (not gating): the full per-site table (pre-cool, S10, S14) — enumerate during the build''s
    institutional-context.'
- id: HVAC-ANOMALY-BLIND-1
  title: The HVAC anomaly detector reports "nominal" while blind on 3 of its 5 metrics — including the
    one that would have caught the zone-3 flap
  thread: hvac
  ROOT_CAUSE_CORRECTED_2026_08_21: '⚠️ READ THIS BEFORE ACTING ON THIS CARD. The root cause recorded here
    previously — inherited from docs/BACKLOG.md (v4.6.10 Tier-2 review) — is "AnomalyDetector._baselines
    is an in-memory dict that resets to {} every restart, so minimum_samples=10 is structurally unreachable".
    THAT ATTRIBUTION IS WRONG. Do not build against it. I repeated it to the operator earlier in this
    session before checking it. WHY IT IS WRONG — ARITHMETIC, from measured data. I pulled homeassistant_start
    events from the HA recorder for the last 14 days: 20 restarts in 6.9 days = 2.9/day, MEDIAN INTERVAL
    5.55h (min 0.18h, p25 1.52h, p75 12.0h, max 34.6h; 10 of 19 gaps under 6h). A metric that samples
    on the 5-minute HVAC decision tick reaches minimum_samples=10 in ~50 MINUTES — comfortably inside
    a 5.55h median. So restart-reset CANNOT explain a metric that has never received a sample. The restart
    hazard is real but it applies to EVENT-DRIVEN accumulators (sampling on rare events), not tick-driven
    ones. See RESTART-SAFETY-DOCTRINE-1 for the general rule: the hazard is "in-memory AND event-driven",
    not "in-memory". WHAT THE ACTUAL CAUSE APPEARS TO BE: the 2026-08-21 restart-safety audit found independently
    that this is a `record_observation` WIRING gap — the metrics are never fed, which is a different defect
    entirely from baselines being lost. That also matches the live symptom better: sample_count is 0,
    not "resets to a low number". A baseline-reset story would show counts climbing then dropping; a wiring
    gap shows a flat zero, which is what metrics_active_ratio "2/5" reports. STATUS: the FIX is being
    folded into the RESTART-SAFETY-DOCTRINE-1 cycle rather than run as its own cycle (operator directive:
    do not spawn cards for things fixable in-cycle). This card stays open as the RECORD so a future session
    does not re-derive the wrong mechanism — but the work happens over there. Verify the wiring claim
    yourself before building: it came from an agent audit and I have confirmed the arithmetic that REFUTES
    the old story, not the evidence that PROVES the new one. Two of that audit''s four seed instances
    were themselves refuted on inspection, so treat its positive findings as hypotheses with citations,
    not conclusions. DISCRIMINATING CHECK if you need to settle it in one step: if baselines-reset were
    the cause, sample_count would be non-zero shortly after a restart and reset later; if wiring is the
    cause, sample_count is zero at ALL times regardless of uptime. Read sample_count on the three dead
    metrics at >1h uptime.'
  DIAGNOSIS_VERIFIED_2026_08_23: 'BUILD-READY. Read-only investigation, code + live entity + URA DB +
    HA recorder. VERDICT ON THE PRIOR ROOT CAUSE: the restart-reset / baselines-lost mechanism is REFUTED
    — definitively, and now by a second disjoint line of evidence beyond the 5.55h-median cadence arithmetic
    already on this card. The URA DB table metric_baselines HOLDS PERSISTED HVAC ROWS: (hvac, zone_call_frequency,
    house, mean 1.342, sample_count 2497) and (hvac, override_frequency, house, mean 1.241, sample_count
    2493), both last_updated 2026-08-21T16:19Z. Baselines therefore survive restarts for this coordinator
    (hvac.py:1189 load_baselines on setup, hvac.py:3951 save_baselines on teardown — the save leg safety.py
    and manager.py lack). The same table carries (hvac, short_cycle_rate, 0.0, sample_count 0), (hvac,
    comfort_deviation_hours, sample_count 0) and (hvac, egress_pause_frequency, sample_count 0). A reset
    story cannot produce 2497 on one metric and a durable 0 on another in the same dict with the same
    save path. The card DISCRIMINATING CHECK is also satisfied: live sensor.ura_hvac_coordinator_hvac_anomaly
    read at 2026-08-23 12:52 CDT, uptime well over 1h, shows sample_count 0 on all three at ALL times
    — flat zero, never climbing. OLD ROOT CAUSE = WRONG. Delete it from the mental model; it is retained
    on this card only as the record of the refutation.'
  MECHANISM_CONFIRMED_2026_08_23: 'THE THREE BLIND METRICS ARE NEVER SAMPLED — a producer-absent wiring
    gap, not a NULL dependency, not a computed-then-discarded value, not Bug Class 53 (computed-but-not-consumed).
    The whole HVAC coordinator contains exactly TWO record_observation call sites: hvac.py:3660 zone_call_frequency
    (fed by a live sum over zone_manager.zones hvac_action in cooling/heating) and hvac.py:3704 override_frequency
    (per-cycle delta of zone.override_count_today). A repo-wide grep for record_observation returns zero
    further hits in hvac.py. The five metric slots are declared at hvac_const.py:971-979 HVAC_METRICS.
    There is no producer at all for short_cycle_rate, comfort_deviation_hours or egress_pause_frequency
    — no call, no computation, no discarded value. The code already SAYS this in two places (hvac.py:3635-3640
    and hvac_const.py:994-1003, the latter deferring egress_pause_frequency to a follow-up cycle that
    never came). So the fix is a PRODUCER fix in the decision cycle, and it is cheap: the loop the short_cycle_rate
    producer needs is already open at hvac.py:3657 reading z.hvac_action per zone.'
  NOMINAL_IS_ACTIVELY_MISLEADING_2026_08_23: 'QUANTIFIED, and it is worse than 2-of-5. Two independent
    facts. (1) The aggregate label launders the blindness: get_learning_status (coordinator_diagnostics.py:1031)
    reports ACTIVE when floor(n/2) metrics have a full baseline — for n=5 that is 2, which the two wired
    metrics satisfy — so get_anomaly_status (hvac.py:3764) falls straight through the insufficient_data/learning
    guard into get_worst_severity and prints a bare nominal. The v4.5.13 relaxation was deliberate and
    its docstring even names never-wired call sites as the reason; the consequence is that the ONLY surface
    carrying the truth is the metrics_silent attribute, which no consumer gates on. (2) The severity surface
    is narrower still: get_worst_severity filters through _persisted_active_anomalies (coordinator_diagnostics.py:934),
    which drops every metric in HVAC_SUPPRESSED_FROM_PERSISTENCE — and that frozenset (hvac_const.py:1004)
    holds FOUR of the five, including the wired zone_call_frequency. So the sensor state can be moved
    by exactly ONE metric of five: override_frequency. Effective coverage is 1/5, not 2/5. HOW OFTEN IT
    HAS SAID NOMINAL, AND WHETHER IT EVER FIRED: anomaly_log in the URA DB holds 28,239 rows spanning
    2026-05-15 to 2026-08-23 across nine coordinators (energy 18,019; transit 5,512; compliance 2,799;
    notification 755; stuck_signal 384; presence 363; bayesian 357; coordinator_manager 44; safety 6).
    Rows with coordinator_id hvac: ZERO. Rows for any of the five HVAC metric names: ZERO. The HVAC detector
    has never written an anomaly in the entire 100-day life of the table. HA recorder over its retained
    7.4-day window: 2,310 state writes of the sensor, all nominal, plus 11 not_configured, 9 not_initialized
    and 1 unavailable boot transients — never once advisory, alert or critical. Absence beyond that window
    is unobservable in the recorder, but anomaly_log is the durable oracle and it is empty for hvac across
    the full span.'
  HIGHEST_LEVERAGE_FIX_2026_08_23: 'WIRE short_cycle_rate FIRST, and give sample_count 0 a visible fault
    state. The number that supports it, measured from the HA recorder over the last 7 days by counting
    hvac_action TRANSITIONS (not state rows — the climate entities write 3,572-8,247 state rows per zone
    per week, which would badly overstate activity): zone_1 352 action transitions / 174 compressor on-cycles
    / median on-time 20.2 min / 6 cycles under 10 min; zone_2 218 / 103 / 25.2 min / 9 under 10 min; zone_3
    259 / 128 / 22.7 min / 11 under 10 min. That is 405 real compressor cycles in seven days with 26 of
    them under ten minutes — a live, well-shaped, continuously-varying signal, exactly the distribution
    shape the v4.6.5 cardinality doctrine asks for and exactly what zone_call_frequency was rejected for
    lacking. The detector observed zero of those 405 cycles. The producer needs no new data source: hvac.py:3657
    already iterates the zones reading hvac_action every decision tick; short_cycle_rate is a per-zone
    on-cycle duration or cycles-per-hour derived from that same read. Because it is tick-driven at 5 minutes
    and reaches HVAC_ANOMALY_MIN_SAMPLES=336 in about 28 hours against a measured 5.55h median restart
    interval, it needs the persisted baseline that HVAC already has — which is why wiring it works today
    and would not have worked in a coordinator lacking the save leg. Second, smaller: metrics_silent is
    non-empty and get_anomaly_status ignores it — a metric with sample_count 0 should surface as degraded
    rather than be laundered into nominal by the floor(n/2) gate. NOT RECOMMENDED IN THIS PASS: comfort_deviation_hours
    and egress_pause_frequency are both event-driven accumulators over rare events, which per RESTART-SAFETY-DOCTRINE-1
    is the hazard class that stays structurally inert; wire them only with an explicit statement of expected
    samples-per-day, or leave them declared-and-suppressed.'
  status: shipped_organic
  shipped_version: v5.90.0
  created: 2026-08-20 14:15
  updated: 2026-08-24 18:40
  refinement_status: refined
  BUILD_REVIEW_2026_08_24: 'Build 1f4d36325 (branch feature/hvac-shortcycle-producer) orchestrator-verified
    (fresh base, diff=plan scope, restart-guard correct, midday-restart + guard tests present). Tier-2-DB
    code reviews (A data-integrity / B async-lifecycle / C mutation) dispatched. REVIEW B back = FIX-BEFORE-SHIP,
    2 HIGH + 3 MED (all source-verified, all fixable): B-HIGH-1 clear_active_anomalies_filtered runs AFTER
    record_observation -> deletes the anomaly it just created -> sensor/worst_severity never show short-cycle
    (de-suppression buys nothing); fix = clear BEFORE record + a real-detector ordering test. B-HIGH-2
    unavailable/unknown climate state read as active->idle -> fabricates short cycles on WiFi flaps (fires
    on network not compressor); fix = gate on new_state.state, drop on_since. B-MED-1 untracked async_create_task
    from callback + midnight increment/reset off-by-one. B-MED-2 multi-day-down restart emits stale partial
    day. B-MED-3 detector-None branch advances date without resetting counter -> unbounded growth. Reviews
    A + C still running. Plan: consolidate ALL 3 reviews into ONE fix-up (build-implies-ship, Tier-2-DB,
    no operator checkpoint), re-verify, then deploy. NOT ship-ready yet.'
  BUILD_REVIEW_A_2026_08_24: 'Review A (data-integrity) back = FIX-BEFORE-SHIP, 2 CRITICAL + 1 HIGH +
    3 MED, CONVERGES with B on the top two. C1 (=B-HIGH-1) clear-after-record deletes the anomaly -> de-suppression
    dead-on-arrival (CRITICAL). C2 (=B-MED-1) callback async_create_task = double-emit + increment/reset
    day misattribution + untracked task (A rates CRITICAL: violates the once-per-(z,d) invariant; the
    tick already covers it so preferred fix is DELETE the callback rollover). H1 (=B-HIGH-2) unavailable/unknown
    -> phantom short cycles; A notes the PLAN REQUIRED this guard AND a named test (test_short_cycle_unavailable_hvac_action_
    drops_on_since, plan :571-575/:740) and NEITHER was built = missed deliverable. M1 zone_id in extra
    not the canonical top-level build_context_json(zone_id=) key. M2 get_status_summary per-metric entry
    lacks the minimum_samples gate (dashboard would show 14/336 while active=true) — add gate to per-metric
    entry. M3 detector-None branch discards a day (converges with B-MED-3). HOLLOW TESTS confirmed: the
    de-suppression test hand-assigns _active_anomalies (never runs the emitter); the rollover test stub
    can-t observe ordering. Verified-GOOD: _min_samples_for routing, clear_active_anomalies_filtered filter
    semantics, D1b nested scopes, restart-safety of on_since, the __-prefix skip, LOCAL-day clock. Review
    C (mutation) still running -> then ONE consolidated fix-up folding A+B+C (delete callback rollover;
    clear-before-record + real-detector test; unavailable guard + its named test; M1/M2/M3 one-liners),
    re-verify with the mutation drills, then deploy.'
  BUILD_REVIEW_C_2026_08_24: 'Review C (mutation/test-authority) back = FIX-BEFORE-SHIP, 1 CRITICAL. C-CRITICAL-1:
    the D1a WIRE-IN is untested — deleting minimum_samples_by_metric={"short_cycle_rate":HVAC_SHORT_CYCLE_MIN_SAMPLES}
    from the HVAC AnomalyDetector ctor (hvac.py:1251-1253) leaves the full suite GREEN (62 passed). Constant
    tested, param tested, CONNECTION untested = wire-in-anchor bug class; if it regresses the gate falls
    back to 336 -> 336-day maturation -> feature silently never fires. Fix: add a ctor-args anchor modeled
    on test_all_coordinators_pass_suppression_set_to_anomaly_detector (test_v465_observability_gap.py:1312).
    C-HIGH-1: the de-suppression test is HOLLOW (hand-copies frozenset({"zone_call_frequency"}) instead
    of importing HVAC_SUPPRESSED_FROM_PERSISTENCE; stayed green under its own mutation) — re-point at
    the real frozenset. C-MED-1: sites 2B/2C/2D (get_learning_status/get_status_summary gates) only source-count
    coverage — add behavioral per-site tests. GOOD: mutations 1a/1b/2A/4/5/6/E1 all bit for the right
    reason with independently-authored oracles; the D2 behavioral tests AST-extract the real methods;
    no shape regression from the nested scopes addition (324/325 in 9 sibling AnomalyDetector harnesses,
    the 1 fail pre-existing in safety.py). Full-suite name-diff was still running (434 files); orchestrator
    must confirm 61 not higher before ship. ALL 3 REVIEWS NOW IN -> consolidated fix-up.'
  FIXUP_DONE_VERIFYING_2026_08_24: 'Consolidated fix-up done, commit d332c3b (origin; builder pushed it
    — untidy but a feature branch, no release fired; local ref still at 1f4d36325, will sync at deploy).
    Orchestrator- verified: d332c3b IS a superset of the original build, base fresh (0 behind develop),
    diff scope intact (same 5 files). All A/B/C findings fixed; 36 cycle tests pass; the 3 key mutations
    BITE (ctor wire-in -> test_hvac_anomaly_detector_ctor_wires_short_cycle_min_samples; clear-before-record
    -> behavioral+source; unavailable guard -> named test). Full-suite name-diff RUNNING in background
    (target 61, not higher). If clean -> DEPLOY (build-implies-ship, Tier-2-DB, no operator checkpoint).
    NOTE the builder pushing to origin is a process deviation to flag.'
  NAMEDIFF_CORRECTION_2026_08_24: 'CORRECTION: my background run reported 61 failed = baseline COUNT,
    but this suite has ORDER-DEPENDENT failure counts (loader shims pollute sys.modules; collection order
    cascades — the SUITE-ORDER-POLLUTION class). Review C got 158 under -p no:randomly (different deterministic
    order). So the 61-count-match is NOT a rigorous name-diff. SOLID evidence the cycle is clean: under
    BOTH orderings all 25 cycle tests + the amended v465 test PASS and NO cycle-surface test (short_cycle/coordinator_diagnostics/v465)
    is in the failure set; review C targeted 9 AnomalyDetector/get_status_summary harnesses = 324/325
    (1 pre-existing). The AUTHORITATIVE order-matched name-diff (branch vs develop under identical order)
    is still RUNNING via review C. DECISION: HOLD the HVAC deploy until that name-diff confirms — "very
    likely clean, final name-diff pending", not "ready to ship". Incidental suite-infra bug (card-worthy):
    pytest hangs in Py_FinalizeEx on a leaked non-daemon thread; `pytest | sort > file` yields empty files
    (sort never sees EOF) — redirect raw output for baseline diffs.'
  NAMEDIFF_RESOLVED_SHIP_READY_2026_08_24: 'Untangled the two orderings: my background full-suite run
    WAS on the shipping commit d332c3b under STANDARD order = 61 = the 61 default-order baseline (valid
    count compare); review C-s 158=158 CLEAN name-diff was under -p no:randomly on the superset-PARENT
    1f4d36325 (local ref was never advanced to d332c3b — origin had it). d332c3b is a verified superset
    of 1f4d36325 adding only cycle-surface fixes + tests + ONE additive summary key (A-M2 per-metric minimum_samples);
    the nested-scopes shape change was ALREADY in 1f4d36325 and covered by C-s clean name-diff. Under
    BOTH orderings: cycle tests pass, NO cycle-surface test fails. C-CRITICAL-1 (wire-in untested) was
    on 1f4d36325 and is FIXED in d332c3b (ctor-anchor test added + mutation bites, orchestrator-verified).
    Targeted-in-isolation harness run hit the pre-existing order-pollution ImportError (test_coordinator_diagnostics
    cannot collect outside full-suite order) — confirms that class, not a regression. CONCLUSION: d332c3b
    has NO regressions; ship-ready. Holding the live-restart deploy for operator presence (hostile-timing)
    + explicit ship-go; one command out (merge d332c3b->develop, README_v5.90.0, deploy.sh --cards HVAC-ANOMALY-BLIND-1).'
  SHIPPED_v5_90_0_2026_08_25: 'SHIPPED v5.90.0 (operator: "ship hvac"). deploy.sh: commit d746bd7d5, PR
    #527 merged to master, release v5.90.0, GitHub+Gitea pushed; BOARD-CURRENCY-1 auto-reconcile (this
    card -> shipped_organic, vibememo entry 070). HACS installed v5.90.0, config-check valid, HA restarted.
    LIVE VALIDATION day-0 PASS (sensor.ura_hvac_coordinator_hvac_anomaly, README_v5.90.0.md table): short_cycle_rate
    registered with minimum_samples=14 (the ctor wire-in / C-CRITICAL-1 fix CONFIRMED LIVE in production,
    not just tests), sample_count=0/active=false (correctly immature; first emit at next local-day rollover),
    sensor nominal / 0 anomalies (no false fire), de-suppressed (in live metrics dict, 2/5 active). ORGANIC-PROOF
    still pending (stays shipped_organic): per-zone accrual over 1-2 days + real short-cycle detection
    at ~14-day maturation -> then done.'
  BUILD_GO_2026_08_24: 'Operator: "HVAC producer build go." Plan clean + both reviews resolved + restart-guard
    re-verified. Dispatched to ura-builder (Tier 2-DB, worktree). Producer only (short_cycle_rate) per
    the fixed plan.'
  PLAN_REVIEW_ROUND2_2026_08_24_FIX_PLAN_FIRST: 'Second (post-D0) Tier-2-DB plan review: FIX-PLAN-FIRST,
    1 CRITICAL + 3 HIGH, all source-verified. CRITICAL: F7 rollover insertion (hvac.py:1290-1305) is gated
    on _last_daily_reset which is init "" at :421 and NEVER persisted -> fires first cycle after EVERY
    restart (~2.9/day) -> partial-day emits + zeroes the persisted counter, poisoning D0 calibration.
    Fix: guard on the tracker''s own persisted date (short_cycles_today_date not in ("", today)); add
    a mid-day-restart-emits-zero test. HIGH: local/UTC clock mixing (branch + fixture are LOCAL, invariant/tracker
    are UTC) — pick LOCAL. HIGH: D1a 5-site drill unsatisfiable at site 4 (which correctly keeps scalar)
    — rescope to 4 routed sites. HIGH: restart persistence loss windows (snapshot >4h skip + ~25min cadence)
    violate the no-loss invariant — bound it or add a store. MEDIUMs: REUSE _unsub_listeners/_cancel_listeners
    (drop invented list); drop D1b order-hedge (zones available); reconcile site-3 active_count; add de-suppression
    consumer enumeration (worst_severity/notification); re-justify MIN_SAMPLES=14 on time-to-usefulness
    (8-day window, not 14); epistemics wording (n=8 can''t call mean REAL + std unchanged both). D0 fold
    itself verified CORRECT. Fix-up dispatched to ura-planner 2026-08-24; needs targeted re-verify of
    the restart guard before build.'
  ROUND2_FIXUP_APPLIED_AND_REVERIFIED_2026_08_24: 'ura-planner applied all round-2 findings (LOCAL clock
    throughout, 4-site drill + site-4 shape test, bounded persistence loss windows named as invariant
    exceptions, REUSE _unsub_listeners/_cancel_listeners, dropped D1b order-hedge, site-3 all-scopes-gate,
    de-suppression consumer enumeration + propagation test, MIN_SAMPLES re-justified on time-to-usefulness,
    n=8 epistemics). Orchestrator PERSONALLY re-verified the CRITICAL restart-guard pseudocode (plan lines
    ~605-645): (a) gate reads persisted _short_cycles_today_date NOT _last_daily_reset; (b) mid-day restart
    prev_date==today is a strict no-op (no emit/no counter mutation/no date write); (c) first-boot prev_date==""
    seeds date only; (d) discriminating mid-day-restart-emits-zero test present. PASS. Plan is CLEAN and
    build-ready. Tier 2-DB. Awaiting operator go-to-BUILD (new capability on a shared primitive consumed
    by 6 coordinators + 14-day maturity cost, so surfacing a build checkpoint rather than auto-dispatching).'
  D0_PROBE_2026_08_24_FAIL_BUT_SAFE: 'D0 fidelity probe (hvac_shortcycle_daily_probe.py, recorder-event
    fidelity = the D2 event-listener fidelity, 8-day window) FAILED the ±10% gate: per-zone means 21-30%
    BELOW the plan fixture (z1 0.62/0.86 vs 0.88/0.78; z2 1.12/1.27 vs 1.50/1.32; z3 1.38/1.22 vs 1.75/1.71).
    BUT the shift is DOWNWARD (fewer short cycles) so separation IMPROVES: worst-normal z 1.3-1.6 vs fault-day
    z 5.4-13.3, cleanly separable. Distribution shape unchanged (near-Poisson 0-3/day) so HVAC_SHORT_CYCLE_MIN_SAMPLES=14
    has no shape-driven reason to move. Option (c) HOLDS with more margin. ACTION: re-freeze the plan
    z-scores/fixture from these numbers and run the mandatory SECOND plan review before build (per plan
    §D0). Fidelity question (F2) is RESOLVED — the divergence is a rate difference, not fidelity. Probe
    agent did not write the audit doc or edit the plan (measurement-only scope); numbers ready to drop
    into docs/planning/AUDIT_hvac_shortcycle_recorder_fidelity.md.'
  PLAN_REVIEW_2026_08_24_DO_NOT_BUILD: 'The producer plan (PLANNING_hvac_short_cycle_producer.md) got
    its mandatory Tier-2-DB adversarial plan review this session. VERDICT: DO-NOT-BUILD / FIX-PLAN-FIRST.
    Two CRITICALs invalidate the design as written; a re-review is required after fix-up (the fix changes
    the fixture, which changes every downstream number). Operator approved the plan (#3), but the gate
    rejected it — findings must be fixed IN THE PLAN before any build. BLOCKING findings (all source-verified
    with file:line): F1 (CRIT): clear_active_anomalies at coordinator_diagnostics.py:1310 takes NO args
    and clears ALL active anomalies for the detector. The plan calls clear_active_anomalies(metric=,scope=)
    "REUSED" — that signature does not exist. As written a builder wipes zone_call_frequency + override_frequency
    house-wide daily. Needs a NEW filtered variant with backward-compat default. F2 (CRIT, the killer):
    the observable zone.hvac_action is a 5-MINUTE POLLED snapshot, but the measured fixture (z=9.13/4.91/3.65,
    the whole Option-(c) case + minimum_samples=14) came from FULL-FIDELITY recorder rows. The entire
    <600s detection band is 1-2 sample intervals wide — Nyquist failure: the shortest, most diagnostic
    cycles are invisible, durations quantize to ~300s multiples. The fixture is NOT the distribution the
    producer can emit. Fix: drive the cycle tracker from async_track_state_change_event on the 3 climate
    entities (reviewer: almost certainly correct, not a builder choice) OR re-run the probe downsampled
    to 5-min and rebuild every z-score. Then the Option-(c) argument must be re-derived. F4 (CRIT): _short_cycles_today
    is in-memory, resets on restart -> emitted "daily" counts are really "since-last-restart"; a fault
    day interrupted by a restart is truncated below threshold (false negative in the fault direction).
    The plan has the persist/reset decision INVERTED: it persists on_since (creating a phantom-cycle hazard
    it then patches with a nonexistent restart_epoch) while leaving the counter unpersisted (the hazard
    that actually degrades the metric). restart_epoch/boot_id does not exist in the repo. F5 (HIGH): per-metric
    minimum_samples override must thread through 5 read sites (:994/:1057/:1118/ :1135/:1149), not just
    record_observation — Bug Class #53; D1s own Live criterion reads :1057/:1149. F6 (HIGH): short_cycle_rate
    is zone-scoped but both status surfaces call at scope="house", so the metric is invisible + _get_baseline
    creates-on-read materializes phantom house baselines. First zone-scoped HVAC metric; surfaces are
    not scope-aware. D1 Live names an attribute that does not exist. F7 (HIGH): the rollover block would
    land after the early return in the delta<0 midnight-reset branch of _record_anomaly_observations ->
    skipped on exactly the boundary cycles. Insertion point must be named. SHOULD-FIX: F3 (the 2.9/day
    denominator is cited to the audit which says UNVERIFIED — but note THIS card carries the actual measurement,
    so it is a citation-provenance fix, not a fabrication; downgrade from the reviewers HIGH), minimum_samples=14
    needs a rung (HVAC_SHORT_CYCLE_MIN_SAMPLES beside HVAC_ANOMALY_MIN_SAMPLES=336), 5-vs-6 coordinator
    count (manager.py:232 missed). CLEAN: maturation (minimum_samples) vs forgetting (max_samples) are
    correctly NOT conflated — the plan never touches the Welford bound (that is HVAC-BASELINE-MAXSAMPLES-1).
    MetricBaseline already carries max_samples:int=0 at :148, which is that other cards surface. Full
    review in the session transcript; key files coordinator_diagnostics.py, hvac.py, hvac_zones.py, hvac_const.py,
    scripts/probes/hvac_shortcycle_daily_probe.py.'
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: ''Fix the [detector ... never received a sample on 3 of 5 metrics].'')'
  tags:
  - no-fabrication-verify
  - measure-before-build
  problem_solution:
  - 'Problem: URA watches the heating/cooling system for unusual behaviour and reports "all normal" —
    but three of the five things it is supposed to watch have never received a single measurement, so
    "all normal" partly means "not looking". One of the three is the check for the air conditioner switching
    on and off too rapidly, which is exactly the problem that was found running unnoticed on one zone
    all day. Solution: feed the three silent checks real data so the alarm can actually fire, and treat
    a check with no samples as a visible fault rather than as a pass.'
  why: 'Live 2026-08-20: sensor.ura_hvac_coordinator_hvac_anomaly = nominal, 0 anomalies, but metrics_active_ratio
    "2/5" — short_cycle_rate, comfort_deviation_hours and egress_pause_frequency all sample_count 0 /
    active false. Only zone_call_frequency (2662) and override_frequency (2658) are live. short_cycle_rate
    is precisely the trip-wire for HVAC-PRESET-FLAP-1''s regression, and comfort_deviation_hours is precisely
    the trip-wire for the zone-2 212-min +8F excursion. BOTH real events happened; the detector reported
    nominal through both.'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board + BACKLOG.md + planning docs. NOT NEW as a root cause — docs/BACKLOG.md
    carries a CRITICAL item "D3 anomaly detection persistence + dispatch" (from the v4.6.10 Tier-2 review)
    stating AnomalyDetector._baselines is an IN-MEMORY dict that resets to {} on every HA restart, so
    minimum_samples=10 is structurally unreachable and the baseline can never mature. That is very likely
    the same defect presenting on the HVAC metrics. THIS CARD IS THE LIVE EVIDENCE THAT THE BACKLOG ITEM''S
    TRIGGER HAS FIRED — it is no longer theoretical. Adjacent to WATCHDOG-INERT-1 (shipped_organic, same
    "detector exists but is structurally inert" class). Verdict: ADJACENT, card kept as the HVAC-side
    evidence + consumer, with the BACKLOG item as the producer-side fix.'
  sibling_of: WATCHDOG-INERT-1
  blocks: HVAC-PRESET-FLAP-1
  REVIEW_A_DO_NOT_SHIP_2026_08_23: 'Build v1 reviewed DO-NOT-SHIP on two findings, both premises orchestrator-verified.
    (A-1) Unsuppressing short_cycle_rate floods: simulated against production Welford (abs z-score, _MIN_VARIANCE=0.01,
    minimum_samples=336, 3 zones, 5-min tick) the metric has NO advisory band -- silent at new_starts=1,
    CRITICAL at new_starts=2. Two zones starting in one 5-min bucket is a routine morning warm-up. Blast
    radius is permanent because clear_active_anomalies() has ZERO production callers (verified: only test_coordinator_diagnostics.py:460)
    and get_worst_severity() is a max() over an unpruned list -- so the first warm-up after baseline maturity
    pins BOTH sensor.ura_hvac_coordinator_anomaly AND the house-wide system anomaly state to critical
    until restart. (A-2, the reframing one) The metric STRUCTURALLY CANNOT DETECT SHORT CYCLING: hvac_action
    is a 5-minute point sample (hvac.py:1104 tick, hvac_zones.py:444 single read), and short cycling is
    BY DEFINITION a sub-5-minute period -- exactly the band aliasing destroys. A compressor cycling 5-on/5-off
    whose sample lands on the idle half reads new_starts=0 forever while short-cycling 144x/day; the pathological
    case reads maximally nominal, and the metrics_silent gate cannot catch it because sample_count keeps
    incrementing. CONSEQUENCE FOR THIS CARD: the founding value claim -- "the metric that would have caught
    the zone-3 flap" -- is NOT delivered by a poll-based producer. Correct producer is event-driven async_track_state_change_event
    on the climate entities hvac_action, not a 5-min poll. This is a rethink of D1, not a fix-up. Also
    confirmed: A-4 hvac_action is stale-sticky (hvac_zones.py:440 early-returns on unavailable WITHOUT
    clearing the field). D2 metrics_silent half is sound and shippable unchanged.'
  REVIEW_B_AND_SYNTHESIS_2026_08_23: 'Review B (lifecycle/write-volume framing) independently reached
    DO-NOT-SHIP, and CORRECTS Review A on one point: write volume is CLEAN. record_observation is pure
    in-memory (coordinator_diagnostics.py:988); baselines persist only at teardown; anomaly rows are hard-bounded
    at 288/day, ~16x below the suppressed census_count rate. So there is NO write-flood risk and the persistence-gate
    case rests on distribution, not volume -- realistic steady state is ~5 CRITICAL rows/day, all false.
    Do not repeat the flood framing. B1 (decisive, and it is what A missed): the build observes a DIFFERENT
    RANDOM VARIABLE than the card probe measured. The probe measured on-cycle counts+durations (405 cycles/7d,
    median on-time 20-25min) and prescribed "per-zone on-cycle duration or cycles-per-hour". The build
    ships a HOUSE-WIDE INTEGER COUNT per 5-min tick -- nobody measured that variable. Consequence: ONE
    zone short-cycling yields at most new_starts=1 -> z=1.78 -> SILENT, and sustained flapping RAISES
    the baseline mean toward 1.0, so the metric SELF-NORMALISES and the anomaly RECEDES as the fault worsens.
    It is anti-correlated with the fault it was built to catch. B2: the "336-sample baseline absorbs the
    boot transient" comment is a CATEGORY ERROR -- record_observation evaluates the z-score BEFORE baseline.update(),
    so the transient FIRES. Baselines persist across reload (metric_baselines live at sample_count 2497/2493)
    while the anchor resets to {} -- mature mean + empty anchor is the worst pairing. At the measured
    2.9 restarts/day this fabricates ~1 CRITICAL/day. Fix exists 5 lines above: mirror the previous_total-is-None
    first-observation skip used by override_frequency (hvac.py:3785-3790). NO TEST COVERS IT -- every
    D1 test pre-seeds _prev_zone_hvac_action, so the only path that runs in production after every reload
    is untested. B3 (5-line): metrics_silent returns BEFORE get_worst_severity(), so a degraded-telemetry
    state MASKS a concurrent CRITICAL. B4: HVAC save_baselines runs only in async_teardown -- watchdog
    kill discards everything (pre-existing; card it). B6: tests use a _RecordingDetector, so no test touches
    real z-score arithmetic -- B1 would have been caught by one test feeding 336 real observations at
    the measured mean. SYNTHESIS (neither reviewer stated this alone; A-2 + B1 joined): A-2 says a 5-min
    POLL structurally aliases sub-5-min cycling; B1 says the VARIABLE is wrong and prescribes duration/cycles-per-hour.
    Together the correct producer is EVENT-DRIVEN -- async_track_state_change_event on the climate entities
    hvac_action, accumulating real on-cycle DURATIONS. A polled cycles-per-hour metric still fails A-2.
    This is a producer redesign with a new listener lifecycle surface, NOT a fix-up of the current build.
    CLEAN per B: payload shape matches override_frequency byte-for-byte (no v4.6.3 sentinel repeat); exception
    isolation correct; unload symmetry fine; the parametric guardrail meta-test was NOT weakened (B mutated
    HVAC_METRICS and it failed correctly).'
  CYCLE_DURATION_PROBE_2026_08_23_DECIDES_THE_REDESIGN: 'Measured the on-cycle duration distribution from
    recorder hvac_action, because ONE number decides event-driven vs poll and nobody had it. RESULT (7d,
    complete cycles): zone_1 187 cycles median 20.2m, sub-5min 5 (2.7%); zone_2 124 cycles median 21.1m,
    sub-5min 6 (4.8%); zone_3 137 cycles median 22.5m, sub-5min 5 (3.6%). Across all three, 16 of 448
    cycles (3.6%) fall in the sub-5-minute band a 5-minute poll cannot resolve. ** THIS DOWNGRADES REVIEW
    A-2. ** A-2 said a 5-min poll "structurally cannot detect short cycling". True in principle, but it
    applies to only ~3.6% of this house cycles. Short cycling here lives in the 5-20 minute band, which
    a 5-min poll CAN see. ** AND A DURATION METRIC DEGRADES GRACEFULLY, which kills the objection in practice.
    ** A metric that watches on-time SHRINK (20m -> 12m -> 8m -> 6m) raises the alarm well before the
    fault reaches the aliased band, because it must pass through the visible band first. Detecting the
    sub-5min endpoint is not required to catch the degradation. ** DECISION COLLAPSES TO THE CHEAP OPTION.
    ** Option B (keep the existing 5-min tick, repoint the producer to PER-ZONE ON-CYCLE DURATION, which
    is what the card OWN founding probe prescribed) captures ~96% of cycles and the whole degradation
    path. Option A (event-driven async_track_state_change_event on hvac_action) buys the remaining 3.6%
    plus exact durations, at the cost of a NEW LISTENER LIFECYCLE SURFACE on a coordinator with a live
    restart/teardown history. Per the marginal-benefit rule, that margin does not pay for that ingredient.
    ** I WAS WRONG AND THE MEASUREMENT CORRECTED ME. ** I recommended the event-driven redesign to the
    operator before measuring, reasoning from A-2 + B1 in the abstract. Measure-before-build existed exactly
    for this; I skipped it and proposed the expensive option. ** REVISED RECOMMENDATION. ** (1) Rebuild
    D1 as a per-zone on-cycle-duration metric on the existing tick -- Tier 2, no new lifecycle surface.
    This ALSO fixes B1 self-normalisation (per-zone + continuous, not a house-wide low-integer count)
    and B2 (a duration accumulator has no empty-map boot edge to fabricate). (2) Ship D2 metrics_silent
    separately after the B3 five-line reorder -- it is sound and independent. (3) Keep the suppression-registry
    entry until the new distribution is measured against real baselines.'
  DURATION_ZSCORE_IS_ALSO_BLIND_2026_08_24: 'Operator gave the GO on the cheap redesign (per-zone on-cycle
    duration on the existing 5-min tick). Before dispatching I measured whether a duration z-score would
    actually FIRE. IT WOULD NOT. The spec I asked them to approve is falsified. ** RAW DURATION IS BLIND
    ON THE LOW SIDE. ** The distribution is heavily right-skewed -- std is at or above the mean in all
    three zones (z1 mean 41.8 std 48.8; z2 mean 59.1 std 87.7; z3 mean 46.4 std 65.0), because a long
    tail of multi-hour cycles inflates std. Consequence: a 3-MINUTE cycle -- severe short cycling -- scores
    z = -0.79 / -0.64 / -0.67 against thresholds ADVISORY 2.0 / ALERT 3.0 / CRITICAL 4.0. It can never
    fire. Nothing on the short side can reach 2 sigma when std exceeds the mean and the variable is bounded
    below by zero. I would have shipped a SECOND dud, blind in the opposite direction from the count version.
    ** LOG-SPACE IS MARGINAL, NOT A FIX. ** log-duration z for a 3-min cycle: -2.38 / -2.03 / -2.09 --
    barely crosses ADVISORY, never ALERT. And the detector uses an abs() z-score, so it would ALSO fire
    on unusually LONG cycles, which are not this fault. ** THE SHAPE THAT DOES WORK: per-zone COUNT OF
    SUB-THRESHOLD CYCLES OVER A 24h WINDOW. ** Measured sub-10-min cycles per day: z1 [1,1,0,2,0,1,2,0]
    mean 0.88 std 0.78; z2 [3,0,0,2,0,3,3,1] mean 1.50 std 1.32; z3 [5,2,0,1,3,0,0,3] mean 1.75 std 1.71.
    A fault day with 8 short cycles scores z = 9.13 / 4.91 / 3.65; with 12, z = 14.25 / 7.94 / 5.98. Fires
    decisively. And the worst NORMAL day observed (5, z3) scores 1.90 -- just under ADVISORY, so ordinary
    variation stays silent. Good separation both ways. ** BUT IT COLLIDES WITH A MACHINERY CONSTRAINT
    -- this is the open design question. ** minimum_samples is PER-DETECTOR (coordinator_diagnostics.py:879/906,
    self.minimum_samples), NOT per-metric, and is 336. A once-per-day observation would need 336 DAYS
    to mature. Lowering it would lower it for override_frequency and zone_call_frequency too -- unacceptable.
    Two candidate resolutions, neither free: (a) observe the rolling-24h count EVERY TICK -- matures in
    ~28h like the others, but consecutive observations are heavily autocorrelated so the learned std is
    artificially small and the metric could become hair-trigger; (b) observe once per COMPLETED CYCLE
    using log-duration one-sided -- matures in ~17 days at ~20 cycles/day/zone, and relies on repeated
    ADVISORY firings as the signal rather than one decisive CRITICAL. Also needs per-metric minimum_samples
    or a one-sided z variant, neither of which exists today. ** RECOMMENDATION -- do NOT dispatch a build.
    ** This is now a real design question with a machinery dependency, not a variable swap. Write a short
    plan carrying these measured distributions as its acceptance fixture, decide (a) vs (b) on the autocorrelation-vs-maturity
    trade, and state whether per-metric minimum_samples or one-sided detection is in scope. Two reviews
    already killed one producer here; a third guess is not worth the round trip. ** D2 metrics_silent
    is UNAFFECTED and still shippable after the B3 five-line reorder.'
  next: Write a short plan for the short-cycle producer carrying the measured distributions as acceptance
    fixture; decide rolling-24h-per-tick vs per-completed-cycle-log, and whether per-metric minimum_samples
    is in scope. D2 metrics_silent shippable separately.
  refs:
  - docs/BACKLOG.md — CRITICAL "D3 anomaly detection persistence + dispatch"
  - sensor.ura_hvac_coordinator_hvac_anomaly (live attrs 2026-08-20)
  - HVAC-PRESET-FLAP-1 DETECTOR_WAS_BLIND
- id: D2-CANARY-GUEST-PREDICATE-1
  title: URA's own planted tripwire is firing ~4x/min saying the guest-arming logic was re-composed —
    3300+ hits since boot, nobody notified
  thread: presence
  status: done
  tier: 1
  created: 2026-08-20 14:15
  updated: 2026-08-23 13:10
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: ''Fix the canary if it has outlasted its use.'')'
  tags:
  - no-fabrication-verify
  - institutional-context
  problem_solution:
  - 'Problem: a warning we deliberately planted in the code to detect a specific kind of mistake has been
    going off continuously since the last restart — thousands of times — and it says the logic that decides
    whether the house treats someone as a guest may have been quietly rewired. Nobody was alerted, because
    it only writes to the log. Solution: work out whether the logic really was changed (and fix it), or
    whether the tripwire itself is wrong and should be corrected, then make a firing tripwire something
    that actually reaches a human.'
  why: 'Live log scan 2026-08-20: presence.py:5766 WARNING "D2 canary: _d5_guest_confidence census-only
    branch reached (should be unreachable — guest_armed depends on room only). A recent change may have
    re-composed the arming predicate." Count 3278-3304 and climbing across two independent scans, sustained
    since the 00:12 boot — NOT a boot transient. This is a deliberately-planted invariant assertion, i.e.
    the highest-signal class of warning URA emits, and it is asserting its own invariant is broken.'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board (all guest/census cards), BACKLOG.md, planning docs. Related
    but NOT duplicate: CENSUS-GHOST-DEDUP-1 (shipped_organic), CENSUS-ACCURACY-1, GUEST-IDENTITY-PHONE-LEFT-BEHIND-1,
    GUEST-ROOM-LOCATION-MATCH-1, CENSUS-G6-RAW-PERSISTENCE (parked). Those concern census COUNTS and guest-room
    designation; this concerns the ARMING PREDICATE composition and is an active runtime assertion failure,
    not a design gap. Given the memory record of the census double-count-into-GUEST incident (additive
    derivation overwriting a subtractive one), this canary may be the same family resurfacing — so it
    must be checked AGAINST those cards, not merged into them. Verdict: NEW (own defect), sibling_of the
    census cluster.'
  sibling_of: CENSUS-GHOST-DEDUP-1
  TRIAGE_VERDICT_2026_08_20: 'OUTCOME (2) — THE CANARY IS STALE, NOT THE CODE. Operator called it: "fix
    the canary if it has outlasted its use." It has. NO REGRESSION: guest_armed is still exactly guest_room_gate_armed
    (presence.py:5730, :5738) or False (:5742) in all three branches — no census term; self._census_confidence
    feeds only unid_gate_armed, which D2 deliberately removed from the arming predicate. THE CANARY ASSERTION
    IS TRUE. What is FALSE is its GUARD PREMISE: the comment claims the if/elif/else sits inside an outer
    `if guest_armed:` block — NO SUCH BLOCK EXISTS. The chain at :5752 sits at the same 8-space indent
    as :5715 and runs unconditionally EVERY inference tick, so every normal no-guest cycle falls into
    the else. That fully explains ~4x/min. GIT DATING IS DECISIVE: predicate inverted in 7f7c15d20 (2026-08-16
    19:35), canary planted in 0e0ea97a2 (2026-08-16 22:18) — 2h45m LATER, and merge-base confirms ancestry.
    The author described the new shape correctly but asserted an enclosing guard they assumed rather than
    read. Nine later commits touched presence.py; git log -S "guest_armed = " shows none altered the composition.'
  BEHAVIOURAL_REACH_NONE: _d5_guest_confidence has exactly ONE consumer (presence.py:6302) and it is itself
    gated on guest_room_gate_armed — the same term whose falsity produces the 0.8. So the census-only
    value is NEVER READ. Does not arm GUEST, does not alter confidence, does not touch the census. Priority
    is LOG HYGIENE (~5700 WARNING lines/day into a 31GB recorder, drowning real warnings — see RECORDER-BLOAT-LOGFLOOD-1),
    not correctness. Tier 1.
  NOT_THE_CENSUS_DOUBLECOUNT_FAMILY: Explicitly checked and REFUTED. That incident was a PRODUCER defect
    (additive census derivation overwriting a subtractive one with dedup inert, feeding a real GUEST arm).
    This is the opposite — D2 removed census from arming precisely to close that family, and this is a
    leftover assertion about the post-fix shape. Different mechanism, no shared producer, no behavioural
    reach.
  next: 'Tier 1 fix, two options: (a) delete the _LOGGER.warning, keep = 0.8 as the shape default; or
    (b) if the invariant is still worth watching, demote to debug AND re-guard so it fires only on real
    violation (`if guest_armed and not guest_room_gate_armed:`) — the check the author intended. EITHER
    WAY correct the misleading "outer if guest_armed block" comment. Recommend (b): keeps the tripwire,
    kills the flood. Superseded text: Read presence.py around 5766 + the _d5_guest_confidence composition;
    determine whether guest_armed still depends on room only (canary correct, code regressed) or whether
    the predicate legitimately changed (canary stale, must be updated). Producer AND consumer check on
    the arming predicate per CLAUDE.md. Do NOT silence the canary.'
  refs:
  - presence.py:5766
  - memory feedback_cross_investigation_synthesis (census double-count into GUEST)
  VERIFIED_AND_CLOSED_2026_08_23: 'Independent re-verification of the 08-20 triage. It holds, and the
    fix has ALREADY SHIPPED —

    this card is closed, not open work.


    WHAT THE CANARY ASSERTED: presence.py, the _d5_guest_confidence if/elif/else. Invariant claimed:

    "the census-only (else) branch is unreachable because guest_armed depends on the room gate only".

    CONFIRMED STALE, NOT A LIVE DEFECT. The assertion about the ARMING PREDICATE is true; the GUARD

    PREMISE was false. Re-read at presence.py:5730 / :5738 / :5742 — guest_armed is exactly

    guest_room_gate_armed or False in all three branches, zero census term. But the confidence chain

    ran UNCONDITIONALLY every inference tick (no enclosing `if guest_armed:` block ever existed), so

    every normal no-guest tick fell into the else and logged. That is the whole ~4x/min.


    BLAST RADIUS: NIL, re-verified by grep. _d5_guest_confidence has exactly ONE consumer,

    presence.py:6306, gated `if new_state == HouseState.GUEST and guest_room_gate_armed:`. When that

    gate is True the value is 0.9 or 0.95 by construction — the 0.8 census-only value is structurally

    UNREADABLE. It never armed GUEST, never altered confidence, never touched the census.


    PRODUCER/CONSUMER (per CLAUDE.md): producer of the guarded value is the 3-branch chain above, one

    derivation, no competing additive/subtractive pair. self._census_confidence feeds ONLY

    unid_gate_armed, which D2 deliberately removed from arming. NOT the census double-count family —

    that was a producer defect with real behavioural reach; this has none.


    ALREADY FIXED AND SHIPPED: commit e2e747469 (2026-08-21 13:53) applied option (b) exactly —

    re-guarded to `if guest_armed and not guest_room_gate_armed:` (the check the author intended),

    demoted WARNING to debug, and corrected the misleading phantom-outer-block comment. Contained in

    tags v5.88.0 and v5.88.1; live manifest is v5.89.0. Tripwire KEPT, flood killed.


    CURRENT RATE: ZERO. Re-measured 2026-08-23 over the live 8h window 04:57:54-12:55:59 (17,163 raw

    lines, 1,518 distinct issues, 73 components). Zero ''D2 canary'' lines. Control for the

    absence-is-not-evidence trap: the same window shows URA itself at 1,146 occurrences / 301 distinct

    issues, so URA logging is alive and the window is real; at the old rate this canary would have been

    the single largest issue in the log (~2,000 lines) and is instead absent. Down from 2,525-in-5h.


    LIVE GUEST STATE CORRECT: three independent house-state sensors all read ''away''

    (sensor.universal_room_automation_house_state, sensor.ura_coordinator_manager_house_state,

    sensor.ura_presence_coordinator_presence_house_state); people-home census = 0. House is NOT in

    GUEST and is not being armed wrongly. Note ''away'' is precisely the condition that drove the old

    else-branch every tick, so silence now is the discriminating observation, not a lucky quiet house.


    FRIGATE-2 CONTAMINATION: not applicable. The predicate is guest_room_gate_armed (sustained room

    occupancy), not a camera/face input, so the 08-20..08-23 Frigate storage stall could not have

    driven the rate. The rate change is explained by the 08-21 code fix, which lands between them.


    LOG-FLOOD CONTRIBUTION (quantified, now eliminated): ~4/min = ~5,760 WARNING lines/day, and it was

    one of the three per-tick floods that commit e2e747469 edge-triggered together. Material to

    RECORDER-BLOAT-LOGFLOOD-1 (31GB/7d on flash at 51% life); that card is already shipped_organic.

    Its remaining un-fixed feeders are visible in today''s window and are NOT URA: pywattbox at 2,852

    occurrences/8h (~8,500/day, the single largest source) and the Sonoff mqtt number-range ERROR at

    598/8h. Recommend the recorder-bloat re-measure be taken against pywattbox, not URA.


    FRAMING CORRECTION (card title is now misleading and is superseded by this block): the tripwire is

    NOT firing, the guest-arming logic was NEVER mis-composed, and the ''3300+ hits'' condition ended

    2026-08-21. Any future session reading the title alone would chase a defect that does not exist.


    RESIDUAL: none blocking. The card''s original ''make a firing tripwire reach a human'' idea is NOT

    addressed by this fix (the canary is now debug-level, so a real violation would be even quieter).

    That is a separate observability question, deliberately not scoped here — a genuine violation of

    this particular invariant has nil behavioural reach, so it does not justify a notification path.

    READ-ONLY investigation; no production code changed by this session.'
- id: HVAC-STALE-ACTUATOR-FRESHNESS-1
  title: A thermostat can go stale-but-"available" and URA keeps deciding on 90-minute-old data with nothing
    flagging it
  thread: hvac
  status: done
  created: 2026-08-20 14:15
  updated: 2026-08-20 14:15
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-20 (chat: ''fix Zone 2 telemetry if a problem.'') — CONDITIONAL on the
    benign-vs-real triage'
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: one of the three thermostats stopped sending updates for an hour and a half while still
    reporting itself as working and showing plausible numbers, so nothing anywhere flagged it and the
    system kept making heating/cooling decisions for that whole zone using old readings. Solution: check
    how fresh an actuator''s data is, not just whether it says it is online, and surface a stale one the
    same way an offline one would be.'
  why: 'Live 2026-08-20: climate.up_hallway_zone_2 produced no attribute/last_reported update from 12:15:17
    onward (~90 min) while climate zones 1 and 3 updated every 1-2 min through 13:42-13:43. State read
    healthy (heat_cool / away / 80-68 / idle / 79F) so it is NOT unavailable and nothing alerts. Bug Class
    #7 (stale data source) presenting as a healthy entity.'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board, BACKLOG.md, planning docs, CLAUDE.md. This is the ACTUATOR-side
    twin of the known documented gap in CLAUDE.md''s troubleshooting section: "sensor.<room>_unavailable_entities
    only tracks INPUT sensors, not actuators, so a dead light is invisible there (gap tracked in BACKLOG)."
    That gap is about UNAVAILABLE actuators; this extends it to STALE-BUT-AVAILABLE actuators, which is
    strictly harder to see. Verdict: ADJACENT to the known actuator-visibility gap — should be scoped
    WITH it, not separately.'
  TRIAGED_BENIGN_2026_08_20: 'CLOSED — NOT A PROBLEM. Operator scoped this "fix Zone 2 telemetry IF a
    problem"; triage says it is not. MY ERROR: I read `last_changed` as a freshness signal. It is not
    — it only moves when the STATE STRING changes. All THREE climate entities share last_changed 12:15:17.095
    to the sub-millisecond, because all three went unavailable 12:14:15 and restored 12:15:17 on the Carrier
    cloud 504, then all stayed `heat_cool`. There was no zone-2-specific event. DISCRIMINATOR APPLIED
    AND NEGATIVE: current_temperature DID advance (79 -> 80 at 14:05:01) — a genuinely stuck poll would
    freeze it. zone_2 was idle/away with no attribute deltas to record; zone_3 logged 59 rows over the
    same window only because it was actively cooling, and when zone_3 itself went idle at 13:57:53 its
    cadence dropped to the same 5-10 min gaps. Symmetric behaviour = suppression, not a fault. Live 16:31:
    zone_2 last_reported advancing AHEAD of both siblings, now home/cooling.'
  DURABLE_LESSON: 'last_changed is NOT a liveness oracle for climate entities — last_reported / last_updated
    is. If an actuator stuck-poll trip-wire is ever built, it must key on last_reported AGE, not on attribute
    churn. Recording this so the same misread does not recur; the underlying actuator-visibility gap (CLAUDE.md:
    room unavailable_entities tracks INPUTS not ACTUATORS) remains real and is unaffected.'
  next: 'CLOSED benign 2026-08-20. Superseded question (still open elsewhere): the unavailable-ACTUATOR
    visibility gap in CLAUDE.md/BACKLOG is untouched by this. No action. Original text retained: Confirm
    whether zone_2 staleness persists or self-heals (could be legitimate no-change suppression on an idle/away
    zone — that is the discriminating question, and it must be answered before building anything). If
    real, scope an actuator freshness check alongside the known unavailable-actuator gap.'
  refs:
  - climate.up_hallway_zone_2 last_reported (live 2026-08-20)
  - CLAUDE.md troubleshooting — actuator-visibility gap
- id: HVAC-PRESET-RESTORE-MISS-1
  DUPLICATE_OF_2026_08_23: 'AUDIT_card_merit_review_2026_08_20 already flagged this as "substantially
    the same defect as HVAC-MANUAL-PRESET-CONTRACT-1 — two cards, one bug. Merge into the contract card
    as its founding case." That recommendation stands and is reinforced: the 2026-08-23 measurement (0/10
    real-preset restores take) lives on the contract card and explains THIS card''s founding observation
    (zone 1 held in manual through six hours of home_day). Keep as the founding CASE; do not scope separately.'
  title: Zone 1 sat in a held manual setpoint through six hours of home_day — the house-state transition
    never wrote it back
  thread: hvac
  status: inbox
  created: 2026-08-20 14:15
  updated: 2026-08-20 14:15
  refinement_status: initial
  approval: unreviewed
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: when the house woke up in the morning, one zone never switched from its overnight held temperature
    to the normal daytime one — it stayed put for six hours. It cost comfort nothing (the held setting
    was cooler than the daytime target) but it means a scheduled changeover silently did not happen, and
    the same miss on a hotter day or in the other direction would cost real money. Solution: find out
    why that changeover did not fire and make it reliable.'
  why: 'Live 2026-08-20: house waking 06:00:23 -> home_day 06:01:23. zone_1 (climate.thermostat_bryant_wifi_studyb_zone_1)
    held manual 75/70 continuously from 01:42 until 12:15:17 — through ~6h of home_day — instead of moving
    to its observed home pair 76/70. Disproof check run: the reviewer pulled the full attribute history
    across the window specifically to see whether a restore fired late; it did not fire at all. A second,
    softer instance: 08-19 22:00:20 sleep transition, zone_2 did not reach sleep 75/70 until 23:53:17
    (+1h53m).'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board + planning docs. Distinct from HVAC-PRESET-FLAP-1 (that is
    TOO MANY writes; this is a MISSING write — plausibly the same precedence machinery seen from the other
    side, so they should be read together). Distinct from ARREST-SUNSET-1 (that is the arrester override
    toggle not sunsetting; this is a preset restore not firing). Note the institutional fact that a URA
    set_temperature flips Bryant to preset manual as a side effect, and hvac_preset.should_change_preset
    deliberately SKIPS manual ("do not fight a dial-set hold") without knowing whether the manual was
    URA-caused or human-caused — that is a strong candidate mechanism for exactly this miss. Verdict:
    NEW, sibling_of HVAC-PRESET-FLAP-1, read together.'
  sibling_of: HVAC-PRESET-FLAP-1
  next: Read hvac_preset.should_change_preset (skips manual, ~line 212-217) + the _nudge_pre_preset /
    _restore_after_nudge FIX-B2 snapshot path. Determine whether a URA-induced manual can strand a zone
    past a house-state boundary when the restore path does not fire. Verify against the 06:01 window before
    proposing anything.
  refs:
  - hvac_preset.py:212-217 (should_change_preset skips manual)
  - hvac_override.py:186-195, 3070-3097 (URA set_temperature induces manual; FIX B2 snapshot)
- id: ARRESTER-CLOUDFLAP-FALSEPOS-1
  title: A Carrier cloud timeout books a phantom thermostat "override" — the arrester counts a human that
    was never there
  thread: hvac
  status: inbox
  created: 2026-08-20 14:15
  updated: 2026-08-20 14:15
  refinement_status: initial
  approval: unreviewed
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: when the thermostat cloud service times out, all three thermostats drop offline together
    for a minute and come back, and the system that watches for someone manually changing a thermostat
    mistakes that blip for a real human override. It then counts an event that never happened, which pollutes
    the very numbers we use to judge whether that watcher is working. Solution: ignore override detections
    for a short grace period after a reconnect, so a cloud hiccup cannot be booked as a person.'
  why: 'Live 2026-08-20: Carrier cloud returned 504 Gateway Timeout on getInfinityEnergy at 12:14:15;
    ALL THREE climate entities went unavailable 12:14:15->12:15:17 (simultaneous to the second); the arrester
    logged override #3 at 12:15:37, 20s after reconnect, and it never entered grace. Same 3-zone-simultaneous
    signature at 08-19 15:25:18 (62s) and 08-19 23:30:56 (2s). Discriminator applied: a REAL external
    override moves ONE zone''s setpoint while siblings hold; these moved together on a cloud fault. So
    today''s true override count is likely 2, not 3.'
  CORRECTION_2026_08_20_operator: 'PARTIAL CORRECTION. I attributed override #3''s "counted but never
    entered grace" to the temp_arrester_override suppression AND implied the suppression itself was suspicious.
    OPERATOR: "I did use the arrester override this am, just turned it off." So the 3.2h suppression was
    DELIBERATE and is now resolved — NOT a stuck flag, NOT a defect, nothing to investigate there. What
    SURVIVES on this card is narrower and still real: the Carrier 504 cloud flap at 12:14:15 caused a
    phantom override DETECTION (all 3 zones dropping together on a cloud fault, booked as an override
    20s after reconnect). The no-grace behaviour is explained by the operator''s override; the phantom
    detection is not. Card scope narrowed to the false-positive DETECTION only.'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board + planning docs. Same CLASS as BATTERY-RESERVE-CLOUD-ORACLE-FLAP-1
    (inbox) — "cloud oracle flap pollutes URA''s own diagnostics" — but a different oracle (Carrier climate
    vs Enphase battery) and a different consumer (arrester override counter vs write-verify). Verdict:
    ADJACENT, not duplicate. RECOMMEND BATCHING: one cloud-flap-immunity cycle covering both, since the
    fix shape (post-reconnect grace before trusting a diff) is identical and the two cards would otherwise
    re-derive it separately.'
  sibling_of: BATTERY-RESERVE-CLOUD-ORACLE-FLAP-1
  batch: cloud-flap-immunity
  next: 'Scope a shared post-reconnect grace with BATTERY-RESERVE-CLOUD-ORACLE-FLAP-1. Discriminating
    acceptance criterion required: the fix must suppress the 3-zone simultaneous case WITHOUT suppressing
    a genuine single-zone override during the same window — state what each looks like before building.'
  refs:
  - Carrier 504 getInfinityEnergy 12:14:15 2026-08-20
  - sensor.ura_hvac_coordinator_override_arrester (live attrs)
- id: RECORDER-BLOAT-LOGFLOOD-1
  INSTANCE_2026_08_23_pywattbox_top_feeder_and_a_real_fault: 'MEASURED over a live 7.7h window (05:17-12:58):
    16,854 raw log lines, 1,505 distinct issues, 73 components. THE LARGEST FEEDER IS NOT URA: pywattbox.http               2,747
    occurrences / 7 issues    ~8,500/day custom_components.wattbox      913 occurrences / 3 issues    ~2,800/day
    homeassistant.components.mqtt  558 occurrences / 2 issues    ~1,700/day custom_components.universal_room_automation
    1,016 across 289 DISTINCT issues URA is chatty in VARIETY, not repetition — a different and far cheaper
    shape than 913 copies of one line. The wattbox pair alone is ~11,300 lines/day. SUPPRESSING BLINDLY
    WOULD HAVE HIDDEN A REAL FAULT — checked before acting. There are TWO WattBoxes and only one is healthy:
    wattbox2 (media room) has a full live outlet set (balun/denon/emotiva/fan/switch all on); wattbox
    (main rack) has NO outlet entities at all, only auto_reboot_enabled and ups_connected. So "No outlets
    available in client" is literally TRUE for the main-rack unit, repeating every poll. The flood is
    a genuine integration failure, not noise. ACTION TAKEN (runtime, reversible): pywattbox.http and custom_components.wattbox.api_wrapper
    raised to ERROR via logger.set_level — removes ~11,300 repeated WARNING lines/day while leaving real
    ERRORs visible. Matters against this card''s 31GB/7days on flash at 51% life. NOT DURABLE, AND THIS
    IS THE IMPORTANT CAVEAT: logger.set_level is runtime-only and is LOST ON RESTART. The permanent fix
    is a `logger:` block in configuration.yaml, which cannot be written from this session (/config is
    read-only over ssh; only /tmp is writable). The flood WILL return on the next HA restart unless the
    operator adds it. THE UNDERLYING DEFECT REMAINS UNFIXED: the main-rack WattBox returns no outlets.
    That is a device/integration problem and suppressing its log does not address it. SECOND FEEDER, also
    a real defect rather than noise: MQTT logs "Invalid value for number.switch_sonoffduo_zigbee_garagebporchinside_delayed_power_on_time_l1/l2:
    0 (range 0.5 - 3599.5)" 279x each per 8h. A device publishes 0 into a number entity whose schema forbids
    it — a Sonoff config fix, not a logging fix.'
  ATTRIBUTE_CHURN_AMPLIFICATION_2026_08_22: 'NEW MEASURED SOURCE, found incidentally while checking a
    Garage Hallway sensor swap, and it is large. URA ROOM OCCUPANCY SENSORS WRITE 46-89 RECORDER ROWS
    PER ACTUAL STATE CHANGE, entirely from ATTRIBUTE churn: binary_sensor.garage_hallway_occupied     4,638
    rows/24h ->   52 real flips = 89.2x binary_sensor.kitchen_hallway_occupied    2,952 rows/24h ->   64
    real flips = 46.1x binary_sensor.staircase_person_occupancy    160 rows/24h ->  155 real flips =  1.0x  (control)
    The plain Frigate binary sensor writes 1:1. The URA room sensors carry ~30 attributes, several changing
    continuously (idle_duration, last_motion, timer/elapsed-style fields), and HA RECORDS ATTRIBUTE CHANGES
    — so every attribute tick is a row. SCALE: ~40 rooms. If the two measured rooms are representative
    that is on the order of 1e5 rows per day from room occupancy alone, on flash at 51 percent life. NOT
    among the three floods fixed in v5.87.0 — those were per-tick LOG floods; this is per-tick ROW churn,
    a different mechanism. METHOD NOTE, because the naive query is WRONG and I nearly reported it as a
    defect: counting rows in `states` does NOT count state changes. The first measurement read 4,639 and
    looked like catastrophic occupancy flapping. Comparing each row against the previous state gives 52.
    Any recorder-volume work must count TRANSITIONS, not rows, or it will chase phantom flapping and miss
    the real cause. SIDE FINDING: this DEFLATES the room-level flap story — 52 real flips/24h in a 12-room
    transit corridor is not catastrophic. Relevant to HVAC-PRESET-FLAP-1, whose re-measure should count
    transitions at the ROOM level rather than infer from raw volume. OPERATOR NOTE 2026-08-22: "Ura has
    an occupancy state" — i.e. room occupancy is a richer STATE than the binary on/off the entity exposes,
    so some attribute richness is intentional and carries real meaning. DO NOT strip attributes blindly.
    The fix should target CONTINUOUSLY-CHANGING DERIVED values (countdowns, elapsed timers) which can
    be recomputed from an absolute timestamp, NOT the state-carrying attributes. Same discipline already
    applied deliberately to the new borrow sensor, which uses absolute `started` / `expires_at` rather
    than countdowns precisely to avoid this. Candidate mechanisms to cost: recorder exclude_attributes
    for the churny keys, or replace countdowns with absolute timestamps at source (preferred — it fixes
    the cause, not the symptom).'
  title: 31 GB of recorder database for only 7 days of history, on flash at 51% life — fed by three log
    floods
  thread: platform
  status: shipped_organic
  shipped_version: v5.87.0
  created: 2026-08-20 14:15
  updated: 2026-08-23 15:45
  refinement_status: initial
  approval: unreviewed
  tags:
  - measure-before-build
  problem_solution:
  - 'Problem: the history database has grown to about 31 GB while only holding a week of data, and the
    drive it lives on reports half its write life used. Three separate things are spamming thousands of
    repeated error lines a day, and every one of those lines gets written to that database. Solution:
    fix or silence the three noise sources, then review what is worth recording at all — the write volume
    and the database size are the same problem.'
  why: 'Live 2026-08-20 system_health: estimated_db_size 31722.88 MiB, oldest_recorder_run 2026-08-13
    (7 days), disk_life_time 51%. Top floods: mqtt.number 1030 errors (Sonoff garage-B porch delayed_power_on_time_l1
    range mismatch, config-level and trivially fixable), camera_census 4866 (camera.garage_a / garage_b
    not in registry — the known Frigate _2-suffix class), Alexa refresh ~2089, plus the D2 canary 3300+
    (see D2-CANARY-GUEST-PREDICATE-1). Also 45 hunterdouglas KeyError and 52+13 pantry_plug_in_adaptive_lighting
    failures (extra keys not allowed @ data[color_temp] — a broken automation failing every run).'
  ADJACENCY_SWEEP_2026_08_20: 'Swept board + BACKLOG.md. FRIGATE-LEG-NAMING-1 (inbox) covers the Frigate
    live/dead leg naming inconsistency and is the likely home for the camera_census garage_a/garage_b
    flood — fold that flood in there rather than duplicating. The MQTT/Sonoff and pantry-automation items
    are HA-config, not URA code. Verdict: NEW as a platform-hygiene card, with the camera_census component
    pointed at FRIGATE-LEG-NAMING-1.'
  sibling_of: FRIGATE-LEG-NAMING-1
  next: 'Cheapest first, all config-level and outside URA code: fix the Sonoff number range, fix or disable
    the pantry adaptive-lighting automation, resolve the camera_census entity ids via FRIGATE-LEG-NAMING-1.
    THEN re-measure db growth before touching recorder excludes — measure-before-build applies.'
  refs:
  - ha_get_system_health 2026-08-20
  - FRIGATE-LEG-NAMING-1
- id: ENVOY-PRODUCTION-STALE-1
  title: Envoy solar-production sensor read 0 kW for ~16.5h while the house was exporting 6 kW — a stale
    live sensor that URA's solar entity derives from; does any decision path trust it?
  thread: energy
  status: shipped_organic
  shipped_version: v5.93.1
  plan_ready_2026_09_01: 'Rev 5 §BUILD-READY. Two plan-reviews (Rev3→FIX 2 CRITs gating-None-fails-OPEN;
    Rev4→FIX D4-H hit dead code + drain over-scope) + confirm-review (Rev5 SHIP, 2 doc fixes). Design cleared:
    per-consumer None-direction table with safety guards (3 inline breaker sites, drain-pause) fail-CLOSED,
    releases still evaluate (battery_power_unknown kwarg), unconfigured-vs-stale split, D-OBS unions with existing
    envoy_status. Tier 2-DB. Queued behind pytest lane. Next: build.'
  status_note: in_progress — SCOPE-SPLIT rework in flight
  tier3_review_2026_09_01: 'BUILT then 4 Tier-3 build-reviews (A correctness / B state-machine / C test-authority
    / D adversarial). Build initially shipped a real safety bug (drain release-path DROP under blind CT — found by
    the mutation drill, fixed). Then A=2MED, B=CRIT+2HIGH, C=pervasive neuter-deletable anchor gap, D=2HIGH. CORE
    of B+D (disjoint-convergent): the breaker-guard + grid-cap FAIL-CLOSED over-corrected — B-CRIT-1 locks arbitrage
    chunks on a DEFAULT-DISABLED guard; D-HIGH-1 _effective_import_kw substitutes 0 for unknown battery → false
    over-cap even guard-enabled; D-HIGH-2 sticky chunk lock (first stall = whole night lost); B-HIGH-1 grid-cap skip
    strands EV (no discharge); B-HIGH-2 ATTAIN unwind reverses v5.17.5 anti-abort. C: most gates neuter-deletable;
    B/C-site anchors were source-greps (C wrote real ones, ~100 LoC, proving it was doable — hard gap).
    OPERATOR DECISION (2026-09-01, "continue" = proceed w/ recommendations): SCOPE SPLIT. KEEP D1/D2/D3(the original
    solar ask)/D4-D(drain HOLD, real safety)/D4-E/F/G/D-OBS + add C''s real stale-but-numeric + wire-in anchors;
    DROP D4-H(breaker×3)+D4-B(grid-cap) → revert to develop, carded as BREAKER-GRIDCAP-STALE-TELEMETRY-1; ADD
    arm-on-unknown for drain (D-MED-3); fix A-MED-1/B-MED-3 (bind D-OBS thresholds to consts + honor ≤0 kill-switch),
    B-MED-2 (D4-F cache power cols), docstrings. Re-review the reduced surface before ship.'
  refinement:
  - 'Rev-3 PLAN-REVIEW = FIX-REQUIRED, two make-or-break CRITICALs — the key lesson: GATING A READ TO None
    IS NOT UNIVERSALLY SAFE; the None-fallback direction (fail-open vs fail-closed) is per-CONSUMER, and
    several TRUST consumers treat None as "no problem" (fail-OPEN), catastrophic for safety guards.
    CRIT-1: gating battery_power_w -> drain-protection (energy_pool.py:2207/3711) reads None -> discharging=False
    -> EVSE drain-pause NEVER fires -> EV drains house battery through the floor (WORSE than the freeze bug).
    CRIT-2: net_power_w consumer enum incomplete -> the 12kW breaker guard (_grid_import_guard_triggered
    returns False on None) + load-shedding (energy.py:7381 early-returns) fail OPEN -> breaker guard silently
    disarmed. HIGH-3: cloud-SOC fold (energy_battery.py:891) silently flips fail-open->closed on a trust path.
    HIGH-4: D-OBS marks UNCONFIGURED sources stale -> envoy_status pinned "stale" forever on installs where
    net_power/battery_power are unset. MED-5: plan named classes that don''t exist (BatteryStrategy not
    EnergyBatteryCoordinator; CostTracker not EnergyBillingCoordinator). MED-6: a 6th in-family gate
    (energy_battery.py:1136) unlisted + drops a shipped negative-age clamp. CLEARED: last_reported semantics,
    zero .state decision consumers (D-OBS display-safe), union closes the 16h window, knobs/invariant OK.'
  - 'Rev 4 in flight (ura-planner): add a per-VALUE per-CONSUMER None-handling table with safety guards
    (breaker, drain-protection, load-shed) explicitly fail-CLOSED; fix class names; add max(0.0,...) age clamp;
    split unconfigured vs stale. Then FRESH plan-review before build.'
  consolidate_decision_2026_09_01: 'Operator: CONSOLIDATE. Scope expanded from solar-only to a shared
    staleness helper (_state_age_s / read-with-freshness) applied to ALL frozen-valid power reads —
    solar_production_w, net_power_w, battery_power_w, PRIMARY battery_soc — folding in the 4 existing
    hand-rolled gates. FROZEN-POWER-READ-STALENESS-CLASS-1 merged in here. One Tier-2-DB cycle.'
  consumer_check_2026_09_01: 'Producer/Consumer: the staleness SENSOR (sensor.ura_energy_envoy_status
    ''stale'') is DISPLAY-ONLY — derived from _envoy_data_anomaly_at (hourly CONSUMPTION cross-check,
    energy.py:3025), consumed by no decision. The TRUST flag envoy_available IS consumed by blind_hold
    (DP+EVSE guard, energy.py:3753 / energy_pool.py:571) + NM offline alert (energy.py:2934), but it is
    computed from SOC+storage_mode ONLY, NOT solar — so a frozen-0 solar read never reaches it. The frozen
    value is trusted DIRECTLY by _read_power_w consumers (excess-solar admit + battery strategy). DESIGN:
    the helper must GATE THE READ (return None -> LKG envelope engages), NOT add an unconsumed staleness
    sensor. Optionally annotate envoy_status per-source.'
  no_dup_audit_2026_08_31: 'Context-wide no-duplication audit: solar staleness gate is NEW (no equivalent
    after grep of energy_battery/energy/energy_pool/aggregation/sensor). NO generic staleness helper exists
    — 4 hand-rolled per-site gates (battery_soc cloud-fallback A1 energy_battery.py:895-925; EVSE per-bay
    SOLAR_POWER_FRESH_S energy_pool.py:4691; grid SOLAR_FOLLOW_GRID_FRESH_S :4401; AC-kWh sensor.py:12488).
    Reuse the v5.17.5 A1 last_updated PATTERN (not verbatim — it gates the cloud tier, not a primary read);
    place the gate in _read_power_w/solar_production_w (energy_battery.py:1572/1599); NEW const
    DEFAULT_SOLAR_PRODUCTION_MAX_AGE_S joins the MAX_AGE family (energy_const.py ~318-326/974-975), rung-1.
    All 4 existing stale-detection surfaces (envoy_available, NM offline alert, EnvoyStatusSensor stale,
    solar_production_w_envelope) MISS the frozen-valid-0 case — gap confirmed. Bug Class #7 is process-scoped;
    this is a candidate NEW class (frozen-valid-value trusted).'
  created: 2026-08-24 16:45
  updated: 2026-09-01 00:15
  refinement_status: refined
  approval: unreviewed
  tags:
  - measure-before-build
  - no-fabrication-verify
  - tier-2db
  investigation_result: 'CONFIRMED (read-only probe 2026-08-31). The frozen entity is
    sensor.envoy_482543015950_current_power_production (URA CONF_ENERGY_SOLAR_ENTITY). It FREEZES
    at a valid 0.0 for 13-21h while sibling sensor.envoy_482543015950_production_ct_power climbs to
    ~17 kW; self-heals within a day (NO reload). 08-29->30 event = 16.2h, matches the symptom. Not a
    device drop (siblings keep updating) — entity-subset-specific to the Enphase current_* aggregate.
    BLAST RADIUS (real): _read_power_w (energy_battery.py:1572) rejects only unknown/unavailable, so a
    frozen 0.0 is TRUSTED as "0 W producing" AND poisons _solar_prod_lkg_w->0 (stamped every non-None
    read, energy_battery.py:1614); the Envoy-blind envelope only fires on None so it never engages for
    frozen-0. Consumers: excess-solar admit (energy_pool.py:1483) + battery strategy. Existing stale
    detector + NM Envoy-offline alert both miss it (fire only on unavailable).'
  problem_solution:
  - 'Problem: the sensor that reports how much power the solar panels are making went stuck at zero for
    about 16 and a half hours on 2026-08-24, even though the house was actually pushing 6 kW back to the
    grid at the time. If any part of the energy brain trusts that stuck number to make a decision (how
    much to charge the battery, whether to let the car draw solar), it would have been acting on a false
    "no sun" reading. Solution: first MEASURE whether any decision path actually consumes the derived
    value, or whether it is only used to discover the Envoy serial number (in which case the staleness
    is harmless). Then decide if a freshness guard is warranted.'
  origin:
    date: '2026-08-24'
    gist: session handoff live-fault
  why: A stale-at-zero solar reading feeding a real decision is the exact failure class the energy invariants
    campaign exists to catch. The handoff notes config derivation is unaffected (energy_envoy_entity is
    only a serial-discovery seed, __init__.py:3026 -> extract_envoy_serial -> derive_envoy_config), but
    whether any EC decision path reads the DERIVED value was flagged as an open question not chased this
    session.
  constraints: []
  next: 'BUILD (Tier 2-DB, QUEUED behind charge-onset — collides on energy files + suite). Fix:
    (1) add a last_updated staleness gate to _read_power_w (energy_battery.py:1572) mirroring the
    battery_soc v5.17.5 A1 precedent (DEFAULT_SOC_CLOUD_FALLBACK_MAX_AGE_S, energy_battery.py:~887) — a
    frozen production reading older than ~2 decision intervals returns None so the built
    solar_production_w_envelope()/LKG fallback engages instead of trusting frozen 0. (2) trip-wire
    anomaly->NM: production frozen/0 while production_ct_power>0 (the discriminating signal — sibling
    live while primary frozen). NO periodic reload (self-heals; reload = watchdog hazard).
    Acceptance (discriminating): inject frozen current_power_production=0.0 with last_updated 30 min old
    while production_ct_power=15 kW -> solar_production_w returns None (not 0.0) and excess-solar admit
    falls through to the LKG envelope, not "0 solar".'
  refs:
  - docs/planning/SESSION_HANDOFF_2026-08-24_evse_split.md (live fault)
  - __init__.py:3026
  - 'PRECEDENT: energy_battery.py:~887 battery_soc staleness gate (v5.17.5 A1)'
  - 'BUG: energy_battery.py:1572 _read_power_w + :1614 LKG stamp + :2287 envelope; consumer energy_pool.py:1483'
- id: FROZEN-POWER-READ-STALENESS-CLASS-1
  title: 3 more power reads trust a frozen-valid value (net_power, battery_power, PRIMARY battery_soc) — same class as the solar freeze
  thread: energy
  status: pre_planning
  created: 2026-08-31 20:45
  updated: 2026-08-31 20:45
  refinement_status: initial
  approval: unreviewed
  sibling_of: ENVOY-PRODUCTION-STALE-1
  tags: [no-fabrication-verify, tier-2db]
  problem_solution:
  - 'Problem: the same defect the solar freeze exposes (a sensor stuck at a valid number is trusted because
     only unknown/unavailable is rejected) exists on THREE more energy reads that drive real decisions:
     net grid power, battery power, and the PRIMARY battery SOC (the v5.17.5 gate only protects the cloud
     FALLBACK SOC, not the primary). Fixing solar alone leaves these three, and hand-rolling a 5th, 6th, 7th
     staleness gate duplicates code. Solution: build ONE shared staleness helper (_state_age_s / read-with-
     freshness) and apply it to all frozen-read power sites at once — consolidating the 4 existing hand-rolled
     gates too — instead of N one-off copies.'
  origin: {date: '2026-08-31', gist: Envoy no-duplication audit — adjacencies section}
  why: 'The no-dup audit for ENVOY-PRODUCTION-STALE-1 found no generic staleness helper and 3 sibling reads
    with the identical frozen-valid hazard (energy_battery.py:1628 net_power, :1546 battery_power, :785 primary
    SOC). Highest-value = primary SOC (frozen SOC re-stamps LKG, poisons battery strategy). Consolidating avoids
    the exact duplication the operator flagged.'
  constraints: ['Energy trust path -> Tier 2-DB. A shared helper touching 4+ decision reads is regression-prone.']
  next: 'Operator decision: fix solar-only (narrow ENVOY-PRODUCTION-STALE-1) vs build the shared staleness
    helper + apply to all 4 frozen reads in one cycle. Then plan -> plan-review -> build.'
  refs: ['Envoy no-dup audit 2026-08-31', 'energy_battery.py:1572/1599/1628/1546/785; energy_const.py:318-326,974-975']
- id: DP-VERYPOOR-DRAIN-VALIDATOR-1
  title: On a "very poor" solar-forecast night the EV drain target comes from a value NO slider can change
    — the live-update validator accepts only 4 of the 5 forecast qualities
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.2
  created: 2026-08-24 16:45
  updated: 2026-08-24 16:45
  refinement_status: initial
  approval: implied
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: the four "off-peak drain target" sliders let the operator tune how deep URA will drain the
    home battery to charge the EV, one per solar-forecast quality. But there are FIVE qualities in the
    code (excellent/good/moderate/poor/very_poor) and the validator that accepts live slider updates only
    knows four — it rejects "very_poor". So on a very-poor-forecast night the drain target is a value
    the operator cannot change through any control. Solution: add very_poor to the accepted set in the
    update validator so all five targets are tunable.'
  origin:
    date: '2026-08-24'
    gist: DP drain-target plan §5 flagged this pre-existing gap and said it needs a card
  why: Pre-existing, but the DP drain-target cycle (EVSE-DRAIN-PRECEDENCE-KNOB-80-1) is what makes the
    very_poor target LOAD-BEARING for DP — before the fix DP never read the composed target at all. energy.py:8647
    validates quality against {excellent, good, moderate, poor}; _drain_targets carries very_poor (energy_battery.py:466)
    and classify_tomorrow_solar can return it.
  constraints:
  - Small validator/config fix; do NOT change drain arithmetic or the DP cycle surface.
  links:
    sibling_of: EVSE-DRAIN-PRECEDENCE-KNOB-80-1
    context: Same knob family; DP cycle makes very_poor load-bearing but explicitly parks this fix out
      of its own scope. Ship after the DP cycle lands.
  parsimony:
    problem: A legal forecast class (very_poor) yields a drain target no live control can adjust.
    verdict: BUILD
  BUILT_2026_08_25: 'BUILT on branch feature/dp-verypoor-drain-validator @ 87e047ac (off develop). Surgical:
    energy.py 1-line (add very_poor to the accepted-qualities set at set_offpeak_drain) + 75-line test
    test_dp_verypoor_drain_validator.py. Reported 114 tests pass, mutation drill bites. NOT batched into
    v5.91.1 (operator said "Ship midnight" only). Ride risk flagged by operator 2026-08-26.'
  RIDE_ALONG_2026_08_26: 'Operator confirmed it must not drop. RIDING the ARBITRAGE-GATE-D2-OFFBYONE-1
    ship (same energy.py surface) — merges + deploys together. Re-verify clean merge + tests at deploy.'
  next: 'Ride the arbitrage D2 ship: rebase/verify feature/dp-verypoor-drain-validator on current develop,
    confirm the 1-line + test still green, merge alongside arbitrage, deploy together. Then card -> done.'
  refs:
  - docs/planning/PLANNING_dp_drain_target_mis_sourcing.md (§5)
  - energy.py:8647
  - feature/dp-verypoor-drain-validator @ 87e047ac
- id: EC-SOC-LADDER-XVALIDATE-1
  title: No cross-field validation on the EC SOC ladder — inverted operator sliders can flip a gate polarity
    and oscillate EV pause/resume; the parked fix's trigger has now fired
  thread: energy
  status: pre_planning
  created: 2026-08-24 16:45
  updated: 2026-08-24 16:45
  refinement_status: initial
  approval: unreviewed
  tags:
  - institutional-context
  - numbers-get-knobs
  problem_solution:
  - 'Problem: the energy coordinator has several SOC thresholds the operator sets independently (reserve
    floor, pause-EV-until SOC, resume/drain floors, excess-solar confirm/resume, drain targets vs the
    inclement floor). Nothing checks they are ordered sensibly relative to each other. Documented failure
    mode: if two are inverted (e.g. fill_priority_soc > excess_solar_soc) the middle band flips polarity
    and the EV oscillates pause/resume at every SOC change. Solution: extend the existing validate_threshold_ladder
    to cross-check the pairs that must be ordered, rejecting or warning on inversions.'
  origin:
    date: '2026-08-24'
    gist: handoff live-fault
  why: This is NOT new work — it is a PARKED deliverable whose trigger has fired. Parked at PLANNING_dp_sticky_yields_to_excess_solar.md:521-525
    (D3 LOW / S5); underlying analysis in BACKLOG_part2_cross_field_invariants_unenforced.md:15-27 (O3).
    AUDIT_excess_solar_and_evse_prior_art.md (P1 row, :822) confirms the solar-follow amp cycle turns
    the 80/95 band into an actively-modulated region and "fires its trigger directly". validate_threshold_ladder
    currently takes only reserve_soc, drain_targets, arbitrage_trigger, peak_buffer_target — it never
    sees resume/pause/drain-floor/excess-solar knobs.
  constraints:
  - Cross-check pairs, do not silently clamp; a rejected/inverted config must surface to the operator.
  links:
    related:
    - EVSE-SOLAR-FOLLOW-AMPS-1
    - EVSE-DRAIN-PRECEDENCE-KNOB-80-1
    context: The solar-follow amp cycle is what actively modulates the 80/95 band, firing this parked
      fix's trigger. Best sequenced after solar-follow so the ladder covers the live surface.
  parsimony:
    problem: Independent SOC sliders can be set to inverted values that flip an EV gate polarity, with
      no guard.
    verdict: BUILD
  next: Harvest the parked D3/S5 spec + the O3 analysis into a plan; enumerate the exact ordered pairs
    to enforce (fill_priority < excess_solar; drain targets vs inclement floor; etc.). Tier 2-DB (touches
    a shared validator consumed across EC).
  refs:
  - docs/planning/PLANNING_dp_sticky_yields_to_excess_solar.md:521-525
  - docs/planning/AUDIT_excess_solar_and_evse_prior_art.md:822
  - energy_const.py:980
- id: HVAC-BASELINE-MAXSAMPLES-1
  title: HVAC anomaly baselines never forget — an accumulator matured on August cooling will misjudge
    October; scope a bounded/windowed sample count into the shared detector
  thread: hvac
  status: pre_planning
  created: 2026-08-24 16:45
  updated: 2026-08-24 16:45
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-24 (chat: "4 - scope in a max_samples")'
  tags:
  - tier-2db
  - numbers-get-knobs
  problem_solution:
  - 'Problem: the anomaly detector learns each metric''s "normal" by accumulating every observation forever
    (unbounded Welford, no cap). So a baseline trained through the August cooling season keeps averaging
    in summer data indefinitely — by October, when normal HVAC behaviour has shifted, the stale baseline
    will flag normal autumn operation as anomalous (or mask real faults). Solution: give the shared detector
    a bounded/windowed sample count (a max_samples / rolling window) so old observations age out and the
    baseline tracks the current season.'
  origin:
    date: '2026-08-24'
    gist: operator decision
  why: 'Operator-approved (#4). Distinct from the short-cycle producer''s minimum_samples override: that
    governs MATURATION (how many samples before firing); this governs FORGETTING (bounding the accumulator
    so it tracks the current regime). The detector is a shared primitive consumed by 5 coordinators, so
    changing its accumulation model is regression-prone across all metrics — hence Tier 2-DB and its own
    scoping, NOT a lump-in to the producer cycle.'
  constraints:
  - Shared AnomalyDetector (coordinator_diagnostics.py) is consumed by 5 coordinators — additive, backward-compatible
    default (unbounded stays the default unless a metric opts in).
  - Windowed variance is not free — a naive rolling recompute changes cost/behaviour for every consumer;
    scope the accumulation model deliberately (Welford-with-decay vs fixed ring buffer).
  links:
    related:
    - HVAC-ANOMALY-BLIND-1
    context: Same detector primitive as the short-cycle producer cycle, but the forgetting change is orthogonal
      to that cycle's maturation override — scope and review separately.
  parsimony:
    problem: An unbounded baseline trained on one season will misjudge the next, with no forgetting mechanism.
    verdict: BUILD
  next: Scope the accumulation model (decay vs fixed window vs max_samples cap), decide default and per-metric
    opt-in, and enumerate blast radius across the 5 consumers. Then Tier 2-DB plan + review before build.
  refs:
  - docs/planning/SESSION_HANDOFF_2026-08-24_evse_split.md (decision
  - coordinator_diagnostics.py (AnomalyDetector)
- id: DASH-SOLAR-EV-CENSUS-1
  title: 'Enrich v6+v8 dashboards: Solar-Aware EV panel + v8 House Census (counts + per-person path &
    predicted next room)'
  thread: dashboarding
  status: shipped_organic
  created: 2026-08-24 17:10
  updated: 2026-08-24 17:10
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-24 (chat: "ura v6 and ura v8 need more information from EVSE Solar Aware
    charging sensors... ura v8 needs information about census... Yes finish the dashboard")'
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: the EV panels on the v6 and v8 dashboards did not show the solar-aware charging picture
    (is there surplus, is the car charging on it, what will the battery drain to, when does fill- priority
    release), and the v8 People tab had no house-census summary — how many people are on the property
    vs inside vs at the perimeter, who is identified, where each person is and where they are predicted
    to go next. Solution: added a Solar-Aware EV Charging card to both v6 and v8 EV sections, and a House
    Census card at the top of the v8 People tab, both grounded in real live sensors and applied live via
    the dashboard API.'
  origin:
    date: '2026-08-24'
    gist: operator push to enrich EVSE panels (v6+v8) and add census to v8
  why: Surfaces already-shipped solar-aware + census producers the dashboards were not consuming.
  constraints:
  - Applied via ha_config_set_dashboard python_transform, NOT a .storage hand-edit.
  - Markdown cards carry explicit entity_id watch-lists (Jinja-variable entities are not auto-detected).
  next: When EVSE-SOLAR-FOLLOW-AMPS-1 ships, add its amp-modulation attributes (solar_follow_surplus_kw,
    solar_follow_state, solar_follow_original_amps, solar_follow_blind_since) to the Solar-Aware EV card.
  links:
    related:
    - EVSE-SOLAR-FOLLOW-AMPS-1
    - EVCARD-1
    context: EVSE-SOLAR-FOLLOW-AMPS-1 will add the amp-modulation telemetry this card should later surface;
      EVCARD-1 is the sibling v8 EV detail card this extends.
  refs:
  - docs/dashboards/ura_v6_v8_solar_aware_ev_and_census_cards.md
  - docs/dashboards/ura_v8_energy_ev_detail_card.md
- id: DRAIN-TARGET-DAY-STALENESS-1
  title: Off-peak drain target reads CALENDAR-tomorrow (solcast_tomorrow) not the peak-anchored target
    day, so after local midnight in the cross-midnight off-peak window it forecasts a day too far
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.1
  created: 2026-08-24 18:10
  updated: 2026-08-26 01:45
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-24 (chat: "Good with the split" + "card... identifies what does not use
    the fix and what the fix is")'
  tags:
  - no-fabrication-verify
  - institutional-context
  problem_solution:
  - 'Problem: the off-peak battery drain target (how deep URA will drain the battery overnight, chosen
    from the next daylight day''s solar forecast) is keyed to CALENDAR tomorrow. The off-peak window crosses
    midnight (summer 21:00->14:00, winter 21:00->05:00). Before midnight "tomorrow" correctly means the
    coming daylight day; AFTER midnight the calendar has rolled, so "tomorrow" now points a day too far
    and the coming daylight day is actually "today" — the drain target is then computed from the WRONG
    day''s forecast. The arbitrage part of the same decision already fixed this; the drain part never
    did. Solution: make the drain target use the same peak-anchored day resolver the arbitrage path uses,
    so it always forecasts the day whose upcoming peak the battery must attain for.'
  origin:
    date: '2026-08-24'
    gist: split out of the DP drain-target cycle; operator asked which day the drain target reflects and
      when it refreshes
  why: Surfaced while resolving the DP drain-target source. The DP cycle deliberately consumes current_offpeak_drain_target()
    UNCHANGED (to match the emitter exactly, avoiding divergence); this staleness is PRE-EXISTING in the
    strategy's own off-peak drain emit and must be fixed for the emitter AND the accessor TOGETHER so
    DP's floor and the strategy's drain stay identical.
  forensic_what_does_NOT_use_the_fix:
  - current_offpeak_drain_target() at energy_battery.py:1738 — calls self.classify_tomorrow_solar() (=
    solcast_tomorrow, calendar tomorrow). This is the DP release-floor accessor AND is consumed by compose_release_floor
    (:296), current_park_floor fallback (:1773), the sensor (:6043), energy.py:5831, energy_pool.py:1797/:3203.
  - 'Off-peak drain emitter: tomorrow_class = self.classify_tomorrow_solar() at energy_battery.py:5111
    (and the sibling path at :5909). drain_class_for_target derives from tomorrow_class (~:5301) and,
    with multi_day_horizon, max()s against classify_solar_day_n(2). This is the value the battery actually
    drains toward — so the accessor mirrors it (its docstring says so).'
  - classify_tomorrow_solar() itself (energy_battery.py:1690) reads solcast_tomorrow blindly and uses
    (now()+1day).month for thresholds — no target-day anchoring.
  forensic_what_DOES_use_the_fix_precedent:
  - '_classify_target_day(now) at energy_battery.py:2395 — peak-anchored: reads get_next_high_rate_transition(now),
    offset<=0 -> classify_solar_day() (today), offset==1 -> classify_tomorrow_solar() (tomorrow), offset>=2
    -> classify_solar_day_n(offset). Its docstring names the exact bug ("at 08:00 the next morning the
    same chunk''s target is now TODAY... read today''s Solcast, not tomorrow''s"). Already used by the
    arbitrage/attain legs at :2454 (_recheck_forecast_on_charge_entry), :5026 (mid-peak), :5112 and :5911
    (target_day_class in the SAME off-peak decision whose drain leg is stale — the split is visible on
    adjacent lines).'
  fix:
  - Replace classify_tomorrow_solar() with _classify_target_day(now) for the DRAIN target selection in
    BOTH current_offpeak_drain_target() (:1738) and the off-peak emitter drain-class computation (:5111
    / :5909 / the drain_class_for_target logic ~:5301), preserving the multi_day max(d1,d2) but anchoring
    d1 on the target day. current_offpeak_drain_target() takes no now today (classify_tomorrow uses dt_util.now()
    internally) — the fix must thread now (or call dt_util.now()) so the peak-anchored resolver can be
    used. Emitter and accessor change TOGETHER so DP's floor == the strategy's drain on every tick incl
    after midnight.
  constraints:
  - Emitter + accessor MUST change together (divergence between them is worse than the shared staleness).
  - Keep the multi-day max(D+1, D+2) conservative-selection behavior; only the day-anchoring changes.
  PLAN_REVIEW_2026_08_24_FIX_PLAN_FIRST_BLAST_RADIUS: 'Tier-3 plan review: FIX-PLAN-FIRST. BIG REFRAME
    (CRITICAL-1): the "midnight" name is wrong — the TOU table (energy_const.py:15-64) has DAYTIME off-peak
    (summer 0-14+21-24, shoulder 0-17+21-24, winter 0-5+9-17+21-24), so offset==0 (target=TODAY) covers
    ~17 of ~20 daily off-peak hours; only the 21-24 evening block (offset 1) stays byte-identical. So
    the fix changes the DOMINANT off-peak drain-release path (feeds compose_release_floor/current_park_floor
    every pool tick), NOT a cross-midnight edge. The bug is correspondingly bigger (drain forecasts wrong
    day during daytime off-peak too, ~37% days) so the fix is MORE valuable AND higher blast-radius. SURFACED
    TO OPERATOR for a scope decision given their earlier nervousness about perturbing this code. Other
    findings to fix in-plan: HIGH-1 two MORE drain-target derivations (_threshold_position :5669, _next_action_estimate
    :5700, called :6079/:6080) contradict the fixed accessor on the same sensor + hardcode 40/bypass _get_offpeak_drain_target
    + ignore multi-day; HIGH-2 _tou=None fixtures make tests hollow (mandate wired TOU + named C-drill
    failure); MED-1 new raise path via get_next_high_rate_transition needs try/except in the accessor;
    MED-2 analogous off-by-one in arbitrage gate (:2458/:2878 pair target_day with hardcoded classify_solar_day_n(2))
    — card as non-goal (framing-D would find it). CONFIRMED-GOOD: offset>=2 unreachable so d1_offset+1
    re-pairing is correct+complete (CRITICAL-2); trust/display split complete (only :5300/:5308 are trust);
    TOU seam clean; no intra-day staleness (classify_solar_day reads full-day solcast_today). Re-target
    T-boundary tests to 14:00/17:00/05:00/21:00 not midnight. 7 minimum plan edits listed in the review
    before build.'
  ORCHESTRATOR_CONFIRMED_AND_RIGHTSIZED_2026_08_24: 'Operator asked me to CONFIRM the offset-0 claim myself
    using the energy system note, not the reviewer. DONE via ENERGY_COORDINATOR_MANUAL §2.1/§2.2 + the
    TOU table (energy_const.py:15-64) read directly: off-peak DOES include daytime (summer 0-14, shoulder
    0-17, winter 0-5+9-17), so offset==0 (target=TODAY) covers ~17 of ~20 off-peak hours; classify_tomorrow
    is live-per-tick so the target flips at midnight to the wrong day for the same peak. CONFIRMED. The
    note §2.2 documents the intended driver as target_day_class (peak-anchored _classify_target_day) —
    the accessor never adopted it, so the fix aligns code with the documented model. RIGHT-SIZED (operator
    instinct validated): the drain target is a DISCHARGE FLOOR (§2.2: battery still charges from solar),
    so during daylight off-peak (~sunrise->14:00) the battery is charging and the floor rarely binds;
    the real money-impact window is the DARK discharge hours (~00:00->sunrise) on class-disagreement nights
    where the battery is still discharging — a SUBSET of the probe 37%, NOT the full 17h. So: real issue,
    worth fixing, but modest + concentrated, NOT a dominant-path change. Operator: PROCEED, ensuring all
    consumers correct at build (the two _threshold_position/_next_action_estimate helpers folded into
    D3). Fix-up dispatched to ura-planner with the right-sizing + all review findings.'
  REREVIEW_2026_08_24_4_EDITS_THEN_BUILD: 'Focused re-review: FIX-PLAN-FIRST but only 4 surgical text
    edits, no re-architecture; all 5 prior flags RESOLVED (offset PROVEN in {0,1} via the TOU walk; TOU
    hours confirmed; D3 trust-site list COMPLETE — only 5 _get_offpeak_drain_target call sites exist;
    seam clean). Load-bearing find H-1: _get_offpeak_drain_target (:1722) is a bare dict lookup with NO
    multi-day max (open-coded twice at accessor :1740 + emitter :5306), so the narration-helper route
    would return D+1-only and FAIL INV-DTDS-3 equality under multi_day_horizon. FIX = one shared _drain_target_for(now)
    helper all 4 sites call (also collapses the 2 duplicate max copies = real #53 closure). M-1 manual
    §2.1 DOES contradict the fix (says "tomorrow"), needs 2 one-line edits (authority is §2.2 alone).
    M-2 offset==1 also covers in-progress high-rate hours, not just 21-24 (harmless, restate). M-3 INV-DTDS-3
    falsifiable on non-drain paths — scope to the drain-fallback branch, compare PRE-clamp drain_target,
    gate live-check on arbitrage n/a + allow_discharge. 3 LOWs (widen D grep to package incl inclement.py:432;
    add :8625/:8638/:8651 mutators to consumer table; stale citations). Reviewer: NO second full review
    — orchestrator diff-check the 4 edits then build. Micro-fix-up dispatched. NEXT: diff-check -> build
    (operator said proceed).'
  PLAN_WRITTEN_2026_08_24: 'Plan written to docs/planning/PLANNING_offpeak_drain_target_day_staleness.md
    (pure additive helper _resolve_target_day + one accessor signature change with default now=None; no
    new CONF_/sensor/knob). Tier-3 plan review DISPATCHED (tees it up). Planner CORRECTED this card''s
    consumer list: energy.py:5831, energy_pool.py:1797/:3203 are DOC/COMMENT mentions NOT runtime calls
    — real runtime consumers of current_offpeak_drain_target() are energy_battery.py:296, :1773, :6043
    + the emitter drain path :5300-5313. Multi-day re-pairing spec: d2_class = classify_solar_day_n(d1_offset+1)
    (not hardcoded 2); FLAGGED pre-existing limitation that classify_solar_day_n falls back to classify_tomorrow
    for days_ahead!=2 (d2 collapses to D+1 when offset>=2) — plan-reviewer to adjudicate.'
  BUILD_PAUSED_PLAN_ONLY_2026_08_24: 'Operator: "pause midnight. Just write the plan and review it and
    tee it up." So: ura-planner writes the Tier-3 plan (reserve-affecting; the subtle part is the multi-day
    max(d1,d2) re-pairing once the anchor moves from calendar-tomorrow to _classify_target_day(now));
    then ONE Tier-3-style plan review; then STOP — build-ready, awaiting explicit operator go. DECOUPLED
    from DP (DP reads-only, mirrors emitter). Probe already confirmed BUILD (37%/~10pp/20pp).'
  links:
    related:
    - EVSE-DRAIN-PRECEDENCE-KNOB-80-1
    context: 'DECOUPLED from the DP fix (operator 2026-08-24): DP just reads/mirrors the emitter, so it
      is NOT blocked by this. Superseded the earlier "PREREQUISITE" framing: DP is NOT evening-only —
      its only gate is off_peak AND dp_on (energy.py:4416), and off-peak crosses midnight (summer 21:00->14:00,
      winter 21:00->05:00), so DP fires post-midnight too (overnight charge into AM, or post-midnight
      plug-in). Therefore DP needs the SAME-DAY target after midnight exactly like attain — peak-anchored:
      tomorrow before midnight, today after. So DP must consume the FIXED (peak-anchored) accessor, NOT
      the stale calendar-tomorrow one. This SUPERSEDES the earlier "DP consumes the stale accessor to
      match the emitter, fix later" compromise (which would make DP consistently WRONG after midnight).
      Sequence: midnight fix FIRST -> DP consumes the corrected accessor -> the day-classification slice
      of the DP round-2 staleness findings dissolves (accessor is correct by the time DP reads it). Probe
      (see PROBE_RESULT) confirmed it bites 37% of days / ~10pp / 20pp worst -> BUILD.'
  PROBE_RESULT_2026_08_24_BITES: 'Probe done (536 days of long-term solcast statistics 2025-03-07..2026-08-24;
    representative = forecast value nearest 06:00 when post-midnight off-peak decisions run). VERDICT:
    BITES OFTEN + MATERIALLY. class(D) != class(D+1) on 37.4% of days (200/535 pairs; 36.8% under median-of-day,
    not a sampling artifact). Drain-target delta among differing days: mean 9.9 / median 10 SOC pts (Δ5:83,
    Δ10:63, Δ15:27, Δ20:27); worst case 20pp, recurs across seasons (excellent<->poor swings, e.g. 2025-05-01->02
    10->30, 2025-12-01->02 30->10). Both directions occur: hold-too-much before a sunny day = lost value;
    drain-too-deep before a poor day = peak grid-import risk. NOT correct-but-low-value — clears the bar
    for a near-term fix. Fix is low-risk (route the off-peak drain path through existing _classify_target_day(now))
    but is a cross-site Bug Class #53 change (accessor + emitter multi-day max(d1,d2)) so it gets normal
    tier review, not a one-liner. Recommend sequencing BEFORE the DP fix so DP consumes the corrected
    accessor from the start.'
  parsimony:
    problem: After local midnight in a cross-midnight off-peak window, the drain target forecasts a day
      too far.
    verdict: BUILD
  VALIDATED_2026_08_26: 'SHIPPED v5.91.1, live-validated post-restart at 01:39 CDT (a post-midnight,
    class-disagreement read). target_day_source=solcast_today + target_day_class=excellent while
    tomorrow_solar_class=good — anchors to TODAY, not calendar-tomorrow (the core fix, observed in the
    dark cross-midnight window). INV-DTDS-3 convergence confirmed: current_offpeak_drain_target=15 reads
    15 identically across current_park_floor / current_commanded_reserve / effective_release_floor /
    threshold_position / next_action_estimate (no naive-vs-composed divergence). 15 = multi-day max
    (today excellent->10 lifted by d1/d2 good->15). Zero URA errors post-restart. See README_v5.91.1.md
    validation table. ORGANIC WATCH REMAINING: the strict reason-string parity discriminator needs a
    night where arbitrage_phase in {n/a,wait} AND hold_depth==allow_discharge (tonight was attain).'
  next: 'Organic close-out: on the next cross-midnight class-disagreement night with arbitrage_phase in
    {n/a,wait} + hold_depth==allow_discharge, confirm reason names TODAY class and current_offpeak_drain_target
    matches; then card -> done. Core fix already live-proven 2026-08-26.'
  refs:
  - energy_battery.py:1738
  - energy_battery.py:2395
  - energy_battery.py:5111
  - docs/planning/PLANNING_dp_drain_target_mis_sourcing.md
  - docs/readmes/README_v5.91.1.md
- id: ARBITRAGE-DRAIN-TODAY-UNKNOWN-DEGENERATE-PAIR-1
  title: When today's Solcast is transiently unknown at offset 0, the target-day resolver returns tomorrow's
    class so the multi-day broadening leg pairs tomorrow with tomorrow (n=1) — a silent duplicate that
    contributes nothing; affects BOTH the arbitrage gate AND the shipped drain path identically
  thread: energy
  status: inbox
  created: '2026-08-26 03:10'
  updated: '2026-08-26 03:10'
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify, producer-consumer]
  problem_solution:
  - 'Problem: at ~00:05 rollover with the today Solcast entity unavailable and no cached class,
    _resolve_target_day returns (classify_tomorrow_solar(), offset 0) — so the target class IS tomorrow''s,
    and the multi-day leg then reads classify_solar_day_n(1)=tomorrow AGAIN. The broadening check becomes
    a duplicate of the primary and silently adds nothing (pre-fix it consulted day_3). A sunny-today/poor-day_3
    edge could gate differently than intended. Solution: when the resolver falls back to tomorrow at
    offset 0, either broaden against day_3 explicitly or record that the broadening leg is degenerate.'
  why: Found by arbitrage review B (B6). Shared with the DRAIN path (energy_battery.py:1765/:5463) which
    has the IDENTICAL degeneracy — fixing the gate alone would reintroduce drain/gate divergence, so fix
    BOTH together or neither.
  next: Reproduce the ~00:05 today-unknown case (probe the resolver); decide broaden-vs-record; fix drain
    + gate together. Tier 2-DB (shared resolver, both decision paths).
  refs:
  - energy_battery.py:2536-2538 (_resolve_target_day today-unknown fallback)
  - energy_battery.py:1765 (drain), :3017 (gate)
- id: ARBITRAGE-D2CLASS-ATTR-SEMANTICS-1
  title: The battery-strategy sensor's d2_class attribute now means "D+1-of-target" not calendar D+2 —
    at offset 0 it publishes tomorrow's class under a key a future diagnosis could read as day-after-tomorrow
  thread: energy
  status: inbox
  created: '2026-08-26 03:10'
  updated: '2026-08-26 03:10'
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify]
  problem_solution:
  - 'Problem: after the arbitrage D2 off-by-one fix, get_status publishes d2_class from target_offset+1,
    so at offset 0 the value is TOMORROW''s class, not calendar day-after-tomorrow. No in-repo consumer
    reads it (display-only), but it is operator-facing diagnosis surface on
    sensor.ura_energy_coordinator_battery_strategy and could mislead a future trace. Solution: add a
    sibling d2_offset attribute (or a README/manual note) so the second-day class is self-describing.'
  why: Found by arbitrage review B (B7). Display-lies-about-ground-truth shape — same family as tonight's
    authoritative-telemetry theme. Cheap; fold into the next energy sensor touch.
  next: Add d2_offset attr to get_status alongside d2_class, or document the semantics in the EC manual.
    Tier 1 (additive display attr).
  refs:
  - energy_battery.py:6119 (get_status d2_class)
- id: ARBITRAGE-GATE-D2-OFFBYONE-1
  title: Arbitrage gate pairs the peak-anchored target day with a HARDCODED classify_solar_day_n(2), so
    at offset 0 it forecasts today + D+2 and skips tomorrow — the same mis-pairing the drain path is fixing
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.2
  created: 2026-08-24 19:20
  updated: 2026-08-24 19:20
  refinement_status: initial
  approval: approved
  tags:
  - no-fabrication-verify
  problem_solution:
  - 'Problem: the arbitrage gate decides whether to grid-charge for the coming peak using the correct
    (peak-anchored) target day for D+1 but a HARDCODED "2 days ahead" for its second day — so when the
    target day is today (offset 0), it compares today against the day-after-tomorrow and skips the actual
    next day entirely. Same class of day-mis-pairing bug the off-peak drain fix (DRAIN-TARGET-DAY-STALENESS-1)
    addresses, on a different decision. Solution: derive the second day relative to the target-day offset
    (target_offset + 1), mirroring the drain fix.'
  origin:
    date: '2026-08-24'
    gist: found by the midnight-plan review (MED-2) as an analogous off-by-one; declared non-goal of that
      cycle
  why: Declared out-of-scope for the drain-staleness cycle to keep it surgical, but it is a real analogous
    defect a framing-D reviewer would otherwise flag mid-cycle. Card it now.
  links:
    related:
    - DRAIN-TARGET-DAY-STALENESS-1
    context: Same day-mis-pairing pattern; fix mirrors the drain fix (offset+1 instead of hardcoded 2).
      Best done after or alongside the drain-staleness cycle so the pattern is settled.
  parsimony:
    problem: At offset 0 the arbitrage gate pairs today with D+2 and skips the actual next day.
    verdict: BUILD
  GO_2026_08_26: 'Operator: "build. Finish." Prereq DRAIN-TARGET-DAY-STALENESS-1 shipped v5.91.1. Full
    Tier-2-DB cycle: scope -> 1 adversarial plan review -> build -> 3 framing-disjoint reviews -> orchestrator
    verify -> deploy. DP-VERYPOOR-DRAIN-VALIDATOR-1 RIDES this ship (same energy surface). Starts once the
    HVAC-D1 suite lane clears (no concurrent pytest).'
  next: 'Scope + plan (mirror the drain fix offset+1 derivation at energy_battery.py:2454/:2871), 1 plan
    review, build, 3 reviews, verify, deploy WITH dp-verypoor. Tier 2-DB.'
  refs:
  - energy_battery.py:2454
  - energy_battery.py:2871
  - docs/planning/PLANNING_offpeak_drain_target_day_staleness.md
  - rides-with: DP-VERYPOOR-DRAIN-VALIDATOR-1
- id: NEXT-ACTION-ESTIMATE-NAIVE-TARGET-1
  title: Battery strategy next_action_estimate reports naive single-day drain target (10) while the real
    composed floor is 15
  thread: energy
  status: done
  created: 2026-08-25 21:30
  updated: 2026-08-25 21:40
  refinement_status: refined
  approval: implied
  tags:
  - no-fabrication-verify
  - producer-consumer
  problem_solution:
  - 'Problem: the battery strategy sensor''s next_action_estimate says ''drain to 10.0% (tomorrow=excellent)''
    while the actual drain floor in use is 15% (the multi-day-composed target). It shows the single-day
    class target (excellent->10) instead of the composed max(D+1,D+2). Same naive-vs-composed shape the
    DP value-stamp just fixed, leaking into a display string. Confirmed live 2026-08-25 21:25 during the
    DP organic test: current_offpeak_drain_target=15, reason ''target 15.0%'', but next_action_estimate
    said 10.'
  - 'Solution: make next_action_estimate read the same composed drain target the decision uses (current_offpeak_drain_target
    / the value-stamp), not the naive per-day class lookup. Discriminator: on a day where D+1 and D+2
    classes differ, the estimate must equal the composed value, not the D+1 raw.'
  origin:
    date: '2026-08-25'
    gist: 'operator: ''fix some sensor bugs'' — spotted next_action_estimate=10 vs real 15 during DP validation'
  why: A display that disagrees with the decision misleads diagnosis (operator caught it live). Same class
    as the DP mis-sourcing.
  next: 'Fold into the midnight drain-target-day-staleness cycle (same surface): one-line fix at :5759
    + discriminating test (D+1!=D+2). Do NOT standalone-deploy tonight (avoids a redundant restart mid-DP-drain).'
  forensic_fix: 'energy_battery.py:5759 in _next_action_estimate: replace `drain = self._drain_targets.get(tomorrow_class,
    ...)` (naive single-day) with `drain = self.current_offpeak_drain_target()` (:1735, the composed multi-day-max
    the decision + the current_offpeak_drain_target attribute already use). Display-only, no decision-path
    change. Discriminating test: D+1=excellent(10) / D+2=good(15) with multi_day_horizon enabled and soc>15
    -> estimate must say ''drain to 15%'', not 10.'
  depends_on:
  - DRAIN-TARGET-DAY-STALENESS-1
  DEDUPE_2026_08_25: DUPLICATE of PLANNING_offpeak_drain_target_day_staleness.md D3/H-1, which already
    routes _next_action_estimate through _drain_target_for and has test_next_action_estimate_uses_shared_helper().
    Not a new fix — folded into the midnight cycle (DRAIN-TARGET-DAY-STALENESS-1). Tonight's live sighting
    (next_action_estimate=10 vs current_offpeak_drain_target=15 on the same sensor, 2026-08-25 21:25 CDT)
    is the empirical repro of the H-1 divergence and is carried into that plan's D3 Live acceptance. Closing
    as dedup-merged, not shipped-standalone.
- id: DP-BATTERY-AUTHORITATIVE-TELEMETRY-1
  title: Surface DP + per-EVSE decision state as always-on authoritative telemetry (not disabled sensor
    / prose) — validation should read truth, not reconstruct it
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.1
  created: 2026-08-25 21:30
  updated: 2026-08-25 21:50
  refinement_status: refined
  approval: implied
  tags:
  - observability
  - no-fabrication-verify
  problem_solution:
  - 'Problem: validating DP required reading pause_reason_human prose (''grid import cap'', ''off-peak
    proactive turn-on'') and next_action_estimate, which can be inaccurate or lag the real decision —
    the orchestrator mis-read ''grid import cap'' as current-limiting when it means paused, and next_action_estimate
    reported the wrong floor. The authoritative signals (command_trail hold_owner/effective_desired/live_desire,
    the DP carrier state, the actuator switch+power) exist but are buried; the DP state sensor is disabled-by-default.'
  - 'Solution: expose the DP decision as first-class, non-prose telemetry — DP state (HOLD/eval/transitioned),
    the latched SOC, and the drain floor it settled on — so validation reads truth, not reconstructed
    prose. Consider enabling/surfacing sensor.ura_energy_drain_precedence_state (drain_target_soc) without
    a reload cost.'
  origin:
    date: '2026-08-25'
    gist: 'operator: ''terrible we have to rely on sensors vs better telemetry. They could be lying or
      inaccurate'''
  why: Diagnosing a money/safety decision path off display strings that can lie is a verification hazard;
    the operator wants ground-truth telemetry.
  next: 'Plan: inventory the authoritative signals (command_trail, DP carrier to_attrs, solar_follow_*)
    vs what each disabled/prose surface hides; spec the always-on DP attrs + the per-EVSE structured state;
    sequence after midnight (or fold #2 into the solar-follow follow-up).'
  scope_refined_2026_08_25: 'Key insight: this is a SURFACING problem, not new instrumentation — the authoritative
    data already exists (command_trail hold_owner/effective_desired/live_desire/cloud_oracle; the DP carrier
    state+drain_target_soc; solar_follow_* attrs). Ranked deliverables: (1) DP decision as ALWAYS-ON attributes
    on sensor.ura_energy_coordinator_ev_charging_status (or battery_strategy): dp_state (HOLD_ONLY/HOLD_PRE_EVAL/TRANSITIONED),
    dp_latched_soc, dp_drain_floor, dp_reason/gate, dp_eval_countdown_s — NOT via the disabled sensor.ura_energy_drain_precedence_state
    (enabling it needs a reload -> restart -> warm-up hold, the exact trap on 2026-08-25). (2) Per-EVSE
    STRUCTURED state, not pause_reason_human prose: {state: paused|throttled|charging, owner, commanded_amps,
    actual_kw}. Kills the pause-vs-throttle ambiguity (orchestrator misread ''grid import cap'' as throttle
    when it means paused) and is REQUIRED once solar-follow throttling is live (a bay at 24A is neither
    paused nor charging; prose has no word for it). Same surface as solar-follow observability -> folds
    with the solar-follow follow-up. (3) Display-reads-decision principle (H-1 in the midnight cycle)
    — extend beyond drain-target. NON-GOALS: heavy telemetry framework; per-tick logging (write-flood
    incident history); dashboards before the data model. Tier: likely Tier-2 (additive surfacing, no decision-path
    change) — confirm at plan time.'
  adopted_2026_08_25: ADOPTED by operator; rides WITH the sensor cosmetic fixes (the midnight drain-target
    cycle) asap. Folded into PLANNING_offpeak_drain_target_day_staleness.md as additive deliverables D6
    (always-on DP decision attrs) + D7 (per-EVSE structured paused|throttled|charging state). Additive-surfacing
    only — no drain-target decision-path change; Tier-3 rigor stays on the drain-target logic, telemetry
    gets additive review. Triggers a midnight plan re-review of the additions before build (per Plan Review
    tiering).
  depends_on:
  - DRAIN-TARGET-DAY-STALENESS-1
- id: HVAC-GOVERNED-RESTORE-FAIL-TAIL-1
  title: ~4% of governed-write restores genuinely fail (immediate=0 AND delayed=0) — a small real tail
    behind HVAC-GOVERNED-EXCURSION-1
  thread: hvac
  status: investigating
  created: 2026-08-25 22:20
  updated: 2026-08-25 22:20
  refinement_status: initial
  approval: implied
  tags:
  - measure-before-build
  problem_solution:
  - 'Problem: after HVAC-GOVERNED-EXCURSION-1 validated, 4 of ~103 restores show BOTH the instantaneous
    and delayed restore failing (restore_ok_immediate=0 AND restore_ok=0) — a genuine ~4% restore-failure
    tail, distinct from the 24 benign delayed-poll artifacts. Solution: pull those 4 rows'' context (zone,
    timestamp, mode, error), find the common failure mode, and decide guard vs accept.'
  origin:
    date: '2026-08-25'
    gist: surfaced disposing HVAC-GOVERNED-EXCURSION-1 via live DB cross-tab
  why: A restore that fails on both signals strands the zone off-preset — the exact harm the primitive
    exists to prevent, just rarer.
  next: SELECT the 4 immediate0/delayed0 rows with full context from ac_ramp_events; classify the failure
    mode; guard or accept.
  refs:
  - HVAC-GOVERNED-EXCURSION-1
  - ac_ramp_events
- id: FAN-TRANSITION-COINCIDENCE-GATE-1
  title: mmWave-only occupancy onset within ±N s of a fan speed/power transition = fan-suspect — route
    to the existing recheck ladder instead of granting occupancy
  thread: presence
  status: planned
  created: 2026-08-26 09:45
  updated: 2026-08-26 09:45
  refinement_status: initial
  approval: implied
  tags:
  - measure-before-build
  - numbers-get-knobs
  problem_solution:
  - 'Problem: an mmWave-only room grants a phantom occupancy the instant a fan changes speed/power (measured:
    onset within ≤1-2 s of a fan transition; steady-state fan never triggers it) — the existing fan-interference
    recheck only rechecks LATER, not at onset. Solution: add an ONSET-TIME discriminator — when an mmWave-only
    onset (no motion/BLE/camera corroboration) falls within a tunable window (~±5 s) of a power/speed
    transition on that room’s registered fan entity, tag it fan-suspect and route to presence_fan_recheck
    instead of granting occupancy; treat mmWave-only occupancy that clears within fan-off fade-time as
    retroactive phantom confirmation.'
  origin:
    date: '2026-08-26'
    gist: operator refuted the fan-independence frame; the AUDIT probe measured transition-coincidence
      as the real signal
  why: Geometry-/hardware-agnostic, all-rooms, no new hardware; builds on the existing presence_fan_recheck.py
    + fan_recheck_state machinery (adds an onset-time gate to a later-recheck-only system). Measured,
    not assumed (AUDIT_fan_signature_separability_probe.md §d GO).
  knobs:
  - 'FAN_TRANSITION_COINCIDENCE_WINDOW_S (rung: module const or Number; ~5s; the ± window around a fan
    transition) — why: tunable per the probe caveats (2 labeled events, single-week confidence)'
  next: 'Scope onto presence_fan_recheck.py: read the room’s registered fan entity transitions; gate mmWave-only
    onsets in the window; the fade-time retroactive-clear. Tier 2 (presence trust decision). Confirm the
    fan-entity registration path per room first.'
  refs:
  - docs/planning/AUDIT_fan_signature_separability_probe.md (§d GO/NO-GO)
  - presence_fan_recheck.py
  - fan_recheck_state table
  - SENSOR-FANINDEP-1 (refuted frame)
- id: OFFPEAK-DRAIN-VERYPOOR-SLIDER-1
  title: No very_poor off-peak-drain Number entity — the 5th quality target is tunable via the validator
    now (DP-VERYPOOR) but has no operator slider
  thread: energy
  status: shipped_organic
  shipped_version: v5.91.4
  created: 2026-08-26 09:45
  updated: 2026-08-26 09:45
  refinement_status: initial
  approval: implied
  tags:
  - numbers-get-knobs
  problem_solution:
  - 'Problem: DP-VERYPOOR-DRAIN-VALIDATOR-1 made the very_poor off-peak drain target ACCEPT live updates,
    but there is no very_poor Number ENTITY — OffPeakDrainNumber._conf_map covers only 4 qualities and
    there is no CONF_ENERGY_OFFPEAK_DRAIN_VERY_POOR. So the operator still has no slider for the 5th quality.
    Solution: add CONF_ENERGY_OFFPEAK_DRAIN_VERY_POOR + extend OffPeakDrainNumber._conf_map + _OFFPEAK_DRAIN_QUALITY
    dispatch + the entity-setup loop + the reload-suppression allowlist.'
  origin:
    date: '2026-08-26'
    gist: surfaced by the DP-VERYPOOR build grep — validator unblocked, entity still missing
  why: 'Numbers-get-knobs: a load-bearing DP drain target the operator legitimately tunes by observation
    should have a live slider, not only a validator that accepts a value nothing writes.'
  next: Add the 5th CONF + Number + dispatch + setup-loop + allowlist. Tier 2 (config surface round-trip).
    Mirror the 4 existing OffPeakDrain sliders.
  refs:
  - number.py:1084-1089 (OffPeakDrainNumber)
  - __init__.py:6635 (_OFFPEAK_DRAIN_QUALITY)
  - energy.py:8866 (validator, now 5-set)
  - DP-VERYPOOR-DRAIN-VALIDATOR-1
- id: OPTIMIZER-NOTIFY-FLOOD-DEDUP-1
  title: Optimizer comfort finding re-sends 8+ identical alerts back-to-back — no per-finding dedup / cooldown on the notification path spams the operator
  thread: notifications
  status: inbox
  created: '2026-08-28 22:00'
  updated: '2026-08-28 22:00'
  refinement_status: initial
  approval: unreviewed
  tags: [operator-observed, live-instance, nm, numbers-get-knobs]
  problem_solution:
  - 'Problem: tonight the operator screenshotted 8+ back-to-back identical iMessage alerts from URA Optimizer — comfort ("Multiple rooms assigned to same thermostat zone climate.thermostat_bryant_wifi_studyb_zone_1 in Study A, Study B, Master Bedroom prevents independent zonal control and violates comfort goal"). The optimizer emits the SAME finding repeatedly with no per-finding dedup or cooldown on the notification path. Solution: add per-finding dedup (keyed on finding-identity — coordinator + finding type + subject entity/zone) and a cooldown (a knob: minimum interval between re-fires of the same finding, e.g. 6h default). Reuse the existing NM cooldown / dedup primitives if present; do not build a parallel one.'
  origin:
    date: '2026-08-28'
    gist: operator screenshotted 8+ identical iMessage comfort alerts back-to-back
  why: 'A notifier that repeats the same finding is worse than one that fires once — the operator starts tuning out real alerts. This is a hygiene-critical NM path change and interacts with the false-positive comfort finding (see OPTIMIZER-COMFORT-HVAC-ZONE-MAPPING-FP-1) — the two together produced tonight''s flood.'
  constraints:
  - 'Verify prior art in NM before adding new dedup: notification_manager.py already has per-day dedup on stuck-sensor rule (_fire_stuck_sensor_nm) and severity routing / ack/cooldown/re-fire (v3.6.29-v3.6.35). Reuse; do not add a parallel cooldown surface.'
  knobs:
  - 'NM_OPTIMIZER_FINDING_COOLDOWN_S (rung 3 Number entity, default 21600 = 6h, live-tunable — operator legitimately tunes this by observation)'
  next: 'Grep notification_manager.py for existing cooldown / dedup primitives (severity routing + per-recipient cooldown are already there). Determine the RIGHT surface for finding-level dedup (optimizer-side coalesce vs NM-side dedup). Scope small (~50 LoC).'
  refs:
  - custom_components/universal_room_automation/domain_coordinators/notification_manager.py
  - OPTIMIZER-COMFORT-HVAC-ZONE-MAPPING-FP-1
  parsimony:
    problem: an optimizer finding fires N times in a row instead of once + a cooldown
    verdict: BUILD
- id: OPTIMIZER-COMFORT-HVAC-ZONE-MAPPING-FP-1
  title: Optimizer flags Study A + Study B + Master Bedroom on one thermostat zone as a comfort VIOLATION — but multiple house rooms on one HVAC zone is BY DESIGN
  thread: optimization
  status: shipped_organic
  shipped_version: v5.91.4
  created: '2026-08-28 22:00'
  updated: '2026-08-28 22:00'
  refinement_status: initial
  approval: unreviewed
  tags: [operator-observed, live-instance, false-positive, architecture-doctrine]
  problem_solution:
  - 'Problem: URA Optimizer — comfort emits a violation whenever >=2 house rooms share one thermostat zone, on the grounds that this "prevents independent zonal control." But per operator-owned architecture (memory project_house_zones_vs_hvac_zones), one thermostat-keyed HVAC zone maps to MULTIPLE house rooms BY DESIGN — the house has 3 Carrier zones and ~40 rooms. So the check is flagging the intended architecture as a defect, and did so 8+ times in a row tonight (see OPTIMIZER-NOTIFY-FLOOD-DEDUP-1). Solution: teach the optimizer comfort check that HVAC-zone -> multiple house-room is legitimate (either drop the check, or gate it on an operator-configured "these rooms should have independent zones" list). SUBORDINATE VERIFY: the specific instance names Master Bedroom on climate.thermostat_bryant_wifi_studyb_zone_1 — the operator is the oracle on the physical mapping; flag for operator confirm before the fix, because if Master IS on studyb_zone_1 that MAY be a real misconfig separate from the general check.'
  origin:
    date: '2026-08-28'
    gist: 'operator screenshotted the alert: Multiple rooms assigned to same thermostat zone (studyb_zone_1 in Study A, Study B, Master Bedroom) prevents independent zonal control and violates comfort goal'
  why: 'The check has been silently emitting a false-positive against intended architecture — any operator following the recommendation would go rip apart working zoning. This is a producer-side defect (the check itself), not a consumer-side notification defect (that is OPTIMIZER-NOTIFY-FLOOD-DEDUP-1); the two shipped together tonight.'
  constraints:
  - 'Do NOT delete the check without confirming with the operator that Master Bedroom on studyb_zone_1 is intentional and not a real misconfig. The operator is the oracle on the physical HVAC->room mapping (memory reference_hvac_zone_tonnage doctrine).'
  next: 'Two-step. (1) Operator-verify: is Master Bedroom actually served by studyb_zone_1 physical duct, or is it config drift? (2a) If BY DESIGN: change the comfort check to accept multiple rooms per HVAC zone as legitimate (grep the optimizer comfort-check source for the emitter). (2b) If MISCONFIG: fix the Master-Bedroom config and keep the check.'
  refs:
  - memory project_house_zones_vs_hvac_zones
  - memory reference_hvac_zone_tonnage
  - OPTIMIZER-NOTIFY-FLOOD-DEDUP-1
  parsimony:
    problem: the optimizer emits a comfort violation for an intended architecture pattern
    verdict: INVESTIGATE
  links:
    related:
    - OPTIMIZER-NOTIFY-FLOOD-DEDUP-1
    context: 'These two shipped together tonight — the FP finding (this card) is what got flooded (dedup card).'
- id: HVAC-CAMERA-FACE-ARRIVAL-SOURCE-1
  title: HVAC pre-arrival ignores the camera_face "who just arrived" signal — wire it in for faster zone
    preconditioning
  thread: hvac
  status: inbox
  tier: 1-2
  created: '2026-08-28 12:00'
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — no confidence gate today (arrival signal, not egress producer); operator to confirm before build'
  sequence: 1  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  refinement_status: initial
  origin: 2026-08-28 post-ship egress-identity consumer-gap audit — adjacency sweep (arrival side)
  confidence_gate: 'None — this is an ARRIVAL signal (SIGNAL_PERSON_ARRIVING), not the egress producer;
    it consumes a presence arrival event, no person_id trust threshold applies. Cheapest high-signal win.'
  problem_solution:
  - 'Problem: when a camera recognizes a face at a door/interior cam, presence fires SIGNAL_PERSON_ARRIVING
    with source="camera_face" (presence.py:4702) — a strong "someone just arrived" cue HVAC could use to
    start preconditioning a zone sooner. But HVAC drops it: its arrival source filter defaults to
    [geofence, ble] (hvac.py:529/977), so the camera_face source is silently ignored. Solution: add
    "camera_face" to the HVAC arrival source filter so a face-at-the-door triggers pre-arrival zone
    preconditioning — faster and more accurate than waiting on geofence/BLE alone.'
  why: 'This is INDEPENDENT of the egress-identity producer (arrival side, not the egress crossing join) —
    the signal already exists and already fires; HVAC just does not subscribe to that source. The cheapest,
    highest-signal identity/presence win in the audit: a one-line source-filter extension, no new producer.'
  next: 'Confirm the HVAC arrival source filter at hvac.py:529/977 and the SIGNAL_PERSON_ARRIVING emit at
    presence.py:4702; scope whether camera_face should be gated (e.g. only for interior/near-door cams) or
    admitted unconditionally. Tier 1 if a pure filter add; Tier 2 if it changes preconditioning timing
    across zones.'
  refs:
  - presence.py:4702 (SIGNAL_PERSON_ARRIVING source="camera_face")
  - hvac.py:529,977 (arrival source filter defaults [geofence,ble])
  - AUDIT_census_identity_supersession_and_consumers.md (adjacency sweep)
- id: SECURITY-ENTRY-VERDICT-NAME-1
  title: Security entry-verdict messages name the DOOR but never the PERSON, even when the verdict already
    derived "known person"
  thread: security
  status: inbox
  tier: 2
  created: '2026-08-28 12:00'
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.75 confidence gate before build (name gate; must never downgrade an ALERT)'
  sequence: 2  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  refinement_status: initial
  origin: 2026-08-28 post-ship egress-identity consumer-gap audit — adjacency sweep
  confidence_gate: '>=0.75 to NAME the person in the message. Naming is a notification-class effect, not a
    security trust decision — but a low-confidence name must NEVER downgrade an ALERT. De-escalate/annotate
    only; per the §5.5 safety doctrine identity may not suppress a security action below 0.9.'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  problem_solution:
  - 'Problem: the security entry-verdict messages — security.py:1480 NOTIFY ("known person, unusual timing")
    and security.py:1573 ALERT — name the DOOR entity that was crossed but never the PERSON, even though the
    verdict logic already concluded "known person". The operator gets "unusual entry at the side door" when
    the house could say "Oji entered at the side door, unusual timing". Solution: join the egress producer''s
    person_id onto the verdict message and name the person when confidence >=0.75; keep it anonymous below,
    and never let a low-confidence name turn an ALERT into a softer NOTIFY.'
  why: 'The verdict already derives "known person" internally; the name is thrown away at the message-build
    step. A named entry-verdict is a large signal-to-noise gain for the operator with no new trust surface,
    provided the name only annotates and never de-escalates below the 0.9 doctrine line.'
  next: 'Measure-before-build: re-measure real person_id production on the garage/family-room entry path
    (per §5.5 gate). Then wire person_id into the security.py:1480/:1573 message builders, graceful-anonymous,
    with the >=0.75 name gate and an explicit "name never downgrades an ALERT" guard. Tier 2 (touches a
    security notification path).'
  refs:
  - security.py:1480 (NOTIFY known person unusual timing)
  - security.py:1573 (ALERT)
  - docs/Coordinator/IDENTITY_FUSION_CAMERAS_MANUAL.md §5.5
  - AUDIT_census_identity_supersession_and_consumers.md
- id: LAST-RESIDENT-EGRESS-ARM-1
  title: Last named resident departing (census residents_home->0) is an earlier/stronger arm-away trigger
    than the occupancy timeout — investigate identity-accelerated arm
  thread: security
  status: parked
  parked_2026_09_03: 'PARSIMONY PARK (operator 2026-09-03: "the house already generally knows"). The existing
    all-away arm already covers the need; the only marginal gain is arming a few minutes earlier via egress
    identity, gated on the ~0%-attach producer + adds a security-actuation ingredient. Margin does not pay for
    the ingredient risk now. Revive trigger: egress producer lands (>30% attach) AND a measured arm-lag gap
    between last-resident-egress and the existing all-away arm justifies the earlier trigger.'
  tier: 2-DB
  priority: low
  created: '2026-08-28 12:00'
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — operator to confirm the >=0.9 confidence gate before build and CONFIRM-ONLY; census residents_home==0 stays sole authority'
  sequence: 3  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  refinement_status: initial
  origin: 2026-08-28 post-ship egress-identity consumer-gap audit — adjacency sweep
  confidence_gate: '>=0.9 and CONFIRM-ONLY. Identity may only ACCELERATE/CONFIRM the arm-away transition;
    census residents_home==0 stays the sole authority. NEVER arm-away on identity alone — a wrongly-attributed
    departure while a resident is still inside would arm the house on an occupant (false-alarm footgun).'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  problem_solution:
  - 'Problem: security arms away off an occupancy timeout (security.py:1114-1360) — it waits out a
    no-occupancy window before arming. But the last named resident crossing OUT a door (census
    residents_home transitioning to 0) is an earlier and stronger "everyone left" signal than the timeout.
    Solution: let a >=0.9 egress-identified departure that takes residents_home to 0 ACCELERATE/CONFIRM the
    arm-away transition — but only as a corroborator: census residents_home==0 remains the authority, and
    identity never arms the house on its own.'
  why: 'Arming sooner when the house genuinely empties is real security value, but this is the highest-blast-radius
    consumer in the audit — a wrong arm-away on a still-present occupant is a false alarm on people inside.
    Hence investigate-first and a hard ">=0.9, confirm-only, census stays authority" invariant. Trust-hierarchy
    ripple (security <-> census <-> presence) => Tier 2-DB.'
  next: 'Investigate-first: confirm the arm-away trigger contract at security.py:1114-1360 and how census
    residents_home is produced; design the confirm-only accelerator (never a raw identity->arm edge), a
    kill switch, and a discriminating test (resident-still-inside must NOT arm). Then Tier 2-DB plan.
    Low priority — gated behind the producer proving its yield.'
  refs:
  - security.py:1114-1360 (arm-away occupancy timeout)
  - census residents_home producer
  - docs/Coordinator/IDENTITY_FUSION_CAMERAS_MANUAL.md §5.5
  - AUDIT_census_identity_supersession_and_consumers.md
- id: EGRESS-IDENTITY-DASHBOARD-TILE-1
  title: Surface the egress observability attrs as a "who entered today, named" dashboard tile
  thread: dashboarding
  status: inbox
  tier: 1
  created: '2026-08-28 12:00'
  updated: '2026-08-29 13:20'
  threshold_status: 'PROVISIONAL — no confidence gate today (display/notify class); operator to confirm no gate before build'
  sequence: 1  # gating order among identity consumers (post-D1 + measured-coverage-gated)
  refinement_status: initial
  origin: 2026-08-28 post-ship egress-identity consumer-gap audit — gap G5
  confidence_gate: 'None — display class. Show name-or-"unidentified"; never a trust decision. Per §5.5
    display consumers carry no confidence threshold.'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1  problem_solution:
  - 'Problem: the egress observability attrs already exist — egress_face_ids_active + egress_identities_stamped
    on the persons_entered_today sensor (sensor.py:4260), plus the D3 attach-rate / ambiguity-rate signals —
    but nothing surfaces them. Live validation has to read them by hand and the operator has no "who entered
    today, named" view. Solution: build a small dashboard tile that surfaces the named-entries list + the
    attach/ambiguity rates, so identity production is visible at a glance.'
  why: 'Small display win and the natural home for validating the producer''s live yield (L2/L3). No trust
    surface — pure observability. Tier 1 dashboard-only.'
  next: 'Build the tile against sensor.py:4260 attrs (egress_face_ids_active, egress_identities_stamped) +
    the D3 attach/ambiguity-rate signals. HA dashboard leg first; PWA leg per the dashboarding workstream.'
  refs:
  - sensor.py:4260 (egress_face_ids_active, egress_identities_stamped)
  - AUDIT_census_identity_supersession_and_consumers.md §3 G5
- id: TRANSIT-PERSON-CHECKPOINT-1
  title: Key TRANSIT-1 traversal checkpoints to person_id for per-person movement paths (agentic-layer feed)
  thread: census
  status: parked
  tier: 2
  created: '2026-08-28 12:00'
  updated: '2026-08-28 12:00'
  refinement_status: initial
  revisit_if: the agentic room-to-room layer is scoped
  origin: 2026-08-28 post-ship egress-identity consumer-gap audit — speculative gap
  confidence_gate: '>=0.9 for any per-person path attribution (a mis-keyed checkpoint corrupts a movement
    path); advisory/annotate only until the agentic layer defines how paths are consumed.'
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1
  problem_solution:
  - 'Problem: the TRANSIT-1 traversal checkpoints (shipped v5.60.0) record room-to-room movement but are
    person-AGNOSTIC — they know a body traversed, not who. Solution: key the checkpoints to the egress
    producer''s person_id so URA can build per-person movement paths, which would feed the future agentic
    room-to-room layer (who tends to go where, when). Speculative until that layer is scoped.'
  why: 'Genuinely useful IF the agentic layer materializes, but there is no consumer for per-person paths
    today — building it now would be speculation ahead of the design. PARK with an explicit revival trigger
    rather than build or drop (per the soak-exit / park-don''t-delete doctrine).'
  next: 'PARKED. Revisit only when the agentic room-to-room layer is scoped. At that point: confirm the
    TRANSIT-1 checkpoint writer, scope keying it to person_id at >=0.9, and design the per-person path store.'
  refs:
  - TRANSIT-1 (traversal checkpoints, shipped v5.60.0)
  - AUDIT_census_identity_supersession_and_consumers.md
- id: D1-PROTECT-FACE-BRIDGE-ADDON-1
  title: Build a Home Assistant ADD-ON that bridges UniFi Protect recognized-face NAMES into sensor.<cam>_face_recognized for the egress-identity producer to consume
  thread: identity
  status: parked
  parked_2026_08_29: 'PARKED (operator marginal-benefit call 2026-08-29). Frigate 0.17 recognizes ALL 4 residents on ALL 23 cameras in near-real-time; Protect covers only 2 cameras with ~60-80s async latency — so a full Protect add-on buys only a DISCOUNTED same-camera corroboration leg (0.75 tier) for large effort. Not worth it now. Scaffold preserved at ~/Code/ura-protect-face-bridge (D0 knowledge banked: name at metadata.detectedThumbnails[].group.matchedName, ~60-80s async, back-fills at original timestamp). REVIVE only if same-camera Protect+Frigate corroboration boost is wanted. PRIMARY PATH is now Frigate face consumption pending the sub_label-promotion research.'
  tier: 2
  created: '2026-08-29 13:20'
  updated: '2026-08-29 13:20'
  refinement_status: initial
  approval: unreviewed
  tags: [no-fabrication-verify, institutional-context, measure-before-build]
  depends_on:
  - EGRESS-IDENTITY-JOIN-GAP-1
  links:
    parent: EGRESS-IDENTITY-JOIN-GAP-1
    blocks:
    - PERIMETER-ALERT-NAME-PERSON-1
    - ARRIVAL-DEPARTURE-NOTIFY-1
    - GUEST-GATE-DOOR-IDENTITY-1
    - SECURITY-CENSUS-UNKNOWN-WIRE-1
    - UNEXPECTED-PERSON-IS-ON-DEDUP-MIGRATE-1
    - SECURITY-ENTRY-VERDICT-NAME-1
    - LAST-RESIDENT-EGRESS-ARM-1
    - EGRESS-IDENTITY-DASHBOARD-TILE-1
    related:
    - HVAC-CAMERA-FACE-ARRIVAL-SOURCE-1
    context: 'D1 is the Protect NAME producer for the identity-consumer cluster. Each consumer is coverage-gated on D1 producing named Protect events at the measured rate needed to justify the wire-in. HVAC-CAMERA-FACE-ARRIVAL-SOURCE-1 is related, not blocked — it consumes an arrival signal, not the egress producer.'
  problem_solution:
  - 'Problem: URA egress identity needs the NAME of a face UniFi Protect recognized, but the Home Assistant unifiprotect integration exposes face DETECTION events without the person NAME, and the operator wired an Alarm-Manager -> HA webhook whose payload comes through empty {}. So even though Protect DOES recognize named faces (verified: Oji at 82% at 21:32 via the Protect API), URA cannot see the name and the egress-identity join stays Frigate-only. Solution: build a small Home Assistant ADD-ON (Supervisor container) that polls / subscribes to the UniFi Protect API using ONE local Protect user, and publishes a normalized sensor.<cam>_face_recognized entity per camera carrying the recognized person name + confidence + timestamp. URA reads it through the existing multi-platform face resolver (camera_resolver.DetectionLeg + FACE_SUFFIXES already includes Protect suffixes), no URA API coupling — vendor coupling stays in the bridge, transport swappable.'
  why: 'This is the shortest path to unblocking the identity-consumer cluster without rebuilding fusion (per the ARCHITECTURE_2026_08_28_EXTEND_NOT_REBUILD ruling on EGRESS-IDENTITY-JOIN-GAP-1). Feasibility was verified vs HA developer docs and the uiprotect library — an add-on with a local Protect user can call the same list_smart_detections / list_events endpoints the MCP tool proved out live. SSO is unsupported for the API path and the public API-key path cannot return names, so a LOCAL Protect user is required.'
  constraints:
  - 'One LOCAL Protect user provisioned by the operator (SSO cannot be used; public API-key cannot return recognized names).'
  - 'Reachable NVR from the HA host (192.168.15.173 is reachable today; the api key the operator gave earlier returned 401 and is not the answer).'
  - 'URA does NOT call the Protect API directly at runtime — coupling stays in the add-on so the transport is swappable and URA remains vendor-agnostic (per §5.5 EXTEND-NOT-REBUILD doctrine).'
  - 'Phased build with a probe first: D0 probe -> Phase 1 REST poller -> Phase 2 websocket standard -> Phase 3 hardening. Do NOT jump straight to websocket.'
  research_2026_08_28: 'Feasibility research is DONE and verified against HA developer docs + the uiprotect library — the add-on route is viable. Phased plan: D0 = one-shot API probe (does a local user token list named smart-detection events?), Phase 1 = REST poller add-on publishing sensor.<cam>_face_recognized, Phase 2 = migrate to the Protect websocket for lower latency, Phase 3 = hardening (reconnect, backoff, health entity, kill switch).'
  blocked_on_2026_08_29: 'BLOCKED on operator provisioning: (a) a LOCAL UniFi Protect user (username + password) — SSO will not work; (b) confirmation of which NVR host + port the add-on should target (192.168.15.173 is reachable; the previously supplied api-key returned 401 and is not sufficient). Until both land, D0 cannot run.'
  knobs:
  - 'PROTECT_NVR_HOST (add-on option, operator-set) — the NVR the add-on connects to.'
  - 'PROTECT_LOCAL_USER / PROTECT_LOCAL_PASS (add-on option, operator-set) — local Protect credentials.'
  - 'PROTECT_POLL_INTERVAL_S (add-on option, default TBD post-D0 probe) — REST poll cadence for Phase 1; retired when Phase 2 websocket lands.'
  - 'PROTECT_BRIDGE_KILL_SWITCH (HA input_boolean, default ON) — operator can disable the bridge without uninstalling the add-on.'
  parsimony: {problem: 'Protect names faces but URA cannot see the name today — every identity consumer is blocked on this ONE producer', verdict: BUILD}
  next: 'WAITING on operator: provision one local Protect user + confirm the NVR host/port. Then run D0 (one-shot API probe) to confirm the local user returns named smart-detection events, measure the real per-camera named-event rate, and only then start Phase 1 REST poller add-on. Do NOT build Phase 1 before D0 answers yes.'
  refs:
  - docs/Coordinator/IDENTITY_FUSION_CAMERAS_MANUAL.md (§5.5 consumer doctrine + EXTEND-NOT-REBUILD)
  - EGRESS-IDENTITY-JOIN-GAP-1 (shipped v5.91.4 — the producer this bridge feeds through the census resolver)
  - camera_resolver.py DetectionLeg (:165) + FACE_SUFFIXES (:247-251) already multi-platform
  - camera_census.py _resolve_face_entity_id (:2615) — the single Frigate-only face-NAME reader that the bridge lets us extend to Protect

- id: EVSE-CHARGE-ONSET-TIME-1
  title: EVSE charge-onset gate — hold EV charging until the battery drain target is reached AND a configurable onset hour has passed (whichever is later)
  thread: energy
  status: shipped_organic
  shipped_version: v5.92.0
  created: '2026-08-29 20:30'
  updated: '2026-08-29 20:30'
  refinement_status: initial
  approval: explicit
  approved_by: 'operator 2026-08-29 (high-priority request); asked for plan outline'
  priority: high
  tags: [tier-2db, numbers-get-knobs, day-boundary, cross-coordinator, extend-existing]
  problem_solution:
  - 'Problem: EV charging currently begins as soon as the off-peak drain gate allows, but the operator wants to control WHEN charging starts overnight — the car should not start charging until BOTH (a) the home battery has drained to its off-peak drain target AND (b) a configurable onset hour has passed. Solution: add a charge-onset time gate. Charge start = the LATER of (drain-target-reached time, onset hour). If the drain target is reached before the onset hour, wait to the onset. If the onset hour passes before the drain target is reached, start as soon as the drain target is reached after that time. Onset hours commonly cross the midnight boundary (e.g. 11pm, 1am, 2am).'
  origin:
    date: '2026-08-29'
    gist: 'operator: add EVSE charge onset time to (maybe) the DP code; charge starts after drain target reached AND a set hour, whichever later; crosses midnight'
  why: 'Lets the operator align EV charging with the cheapest/latest TOU window and sequence it AFTER the battery has extracted its overnight discharge value, instead of the car charging too early or competing with battery drain.'
  constraints:
  - 'MUST be midnight-boundary safe (onset 23:00-02:00 spans the day boundary) — reuse the day-boundary-safe TOU period logic (Bug Class #51).'
  - 'MUST NOT starve the car: keep/compose with a must-start-by backstop so an onset that never clears (drain target never reached) still charges before departure.'
  - 'Kill-switch: onset unset = current behavior byte-identical.'
  knobs:
  - 'CONF_EVSE_CHARGE_ONSET_TIME (rung: config/options or a time entity — operator tunes by observation; time-of-day, kill-switch = unset)'
  parsimony: {problem: 'EV charging starts before the operator-preferred hour / before battery drain completes', verdict: BUILD}
  next: 'PLAN: institutional-context verify the drain-target-reached signal (energy_pool.py:622-651, consumed at :849/:954-962) + the EVSE start gate; design the onset-time gate composed with the existing drain gate; midnight-boundary handling; must-start-by backstop; discriminating acceptance criteria (onset-before-drain, onset-after-drain, midnight-cross, kill-switch). Tier 2-DB min (time x state-machine seam, battery<->EVSE<->TOU cross-coordinator).'
  refs:
  - custom_components/universal_room_automation/domain_coordinators/energy_pool.py (drain-target-reached :622-651; EVSE determine_actions :849)
  - memory project_ev_drain_precedence_cycle (adjacent must-start-by machinery)

parked:
- title: Pre-roll frame buffer
  revisit_if: rising-edge frames look late for fast walkers
- title: Anticipatory TOU tick
  revisit_if: boundary-lag data shows real cost
- title: Adjacency config-flow (adjacency-as-data / TOU pattern)
  status: approved-queued (exterior-stragglers batch, seq 3)
- title: Security config home
  revisit_if: a 2nd security-config surface would join the top menu
broader_backlog_refs:
- EV drain-precedence (queued)
- Load-shedding foundations (vision doc first)
- Fusion paper (gated)
- Shipwatch v1.2.0 deploy.sh hook
- Forecaster wire-up (LightGBM + BatteryStrategy)
- Dashboarding workstream (ura-v6 rebuild + PWA)
- Memory week-one gate + first coordinator-consumer proposal
