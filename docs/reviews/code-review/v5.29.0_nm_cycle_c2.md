# Code Review Record — v5.29.0 NM Cycle C-2 (routing UI + life-safety tunable, Tier 3)

**Branch:** build/nm-cycle-c2 (4-commit build → 834b68f5 fix-up). Baseline
tag `pre-review-v5.29.0`. **Tier 3** (operator-elevated): A/B/C/D reviews +
orchestrator mutations. **Status: fix-up complete, awaiting checkpoint.**

## Build highlight
The plan enumerated 5 life-safety consumer sites; the builder's
institutional grep found **8** (3 missed by the plan — both bucket-bypass
wrappers + the dry-run check). Bug Class #53 caught at build time.

## Findings ledger (all fixed)

| id | sev | finding |
|---|---|---|
| C2-HIGH-1 | HIGH | Extras-awareness behaviorally untested at 7/8 sites — a grep-evading inline copy of the base set left the suite green (the textual guard is aggregate-level only). Fixed: per-mechanism behavioral anchors (boot-settle, repeat mute/DND, bucket ×3 parametrized, DND floor, router mute) + structural helper-call-count guard; all 8 site mutations + orchestrator's own grep-evading mutation at the DND floor site killed by named tests. |
| C2-HIGH-2 | HIGH | Vocabulary guard (sole defense of extras vocabulary; runtime deliberately permissive) untested. Fixed: options-save unknown-token error test. |
| D-HIGH-1=H-B1+A1-A4+L-B1 | HIGH | Rename script was a guaranteed silent no-op (filter matched nothing real), edited live .storage (registry-flush clobber), no collision guard, misnamed backup, doc-vs-code filter mismatch. Fixed: rebuilt against the LIVE registry (no umbrella entity exists; unique_ids not uniformly prefixed — platform-filtered prefix rename `ura_notification_manager_*` → `ura_nm_*`), STOP-HA-FIRST refusal (probes HA API), collision abort, correct backup naming, dry-run default, comment matches code. |
| M-B1 | MED | D4 card + audit doc referenced a phantom entity/attr/method (aspirational doc asserted as implemented). Fixed: real bounded ring `_routing_audit_log` fed by `_emit_audit_row` (RAM-only, no new DB writes), `nm_routing_audit_recent` on the real diagnostics sensor with `_unrecorded_attributes`; card doc repointed. |
| C2-MED-1, A5, A7, C2-LOW-1 | MED/LOW | default-drop test; malformed-row silent truncation now errors (`nm_c2_coercion_dropped_row`); error-key renamed; coercion test. |
| A6 | LOW | Extras promotion scope = environment hazards (HazardType) only; security tokens (intruder etc. already base; investigate/entry_alert not promotable) — DELIBERATE, documented in the helper. |

## Invariants (Review D, diff-blind)
- I-C2-1 byte-identical with extras empty: HOLDS (helper reduces exactly to the base-set test).
- I-C2-2 no demotion by any legal options value: HOLDS by construction (base checked before knob; rejected saves lose nothing).
- I-C2-3 promotion completeness: HOLDS — repo-wide zero consumers outside helper/const/selector-authority.

## Statistics
CRITICAL 0 · HIGH 3 (fixed 3) · MEDIUM 4 (fixed 4) · LOW 4 (fixed 3, 1 documented-scope).

## Verification
- Reviewer C mutation matrix post-fix-up: 8/8 site bypasses + vocab-guard + default-drop ALL KILLED.
- Orchestrator personally ran the grep-evading DND-floor bypass: killed by BOTH the behavioral anchor and the structural count guard; tree restored.
- Full suite: 36 failed + 14 errors = exact env-drift baseline; zero new.

**Checkpoint items:** rename script is deploy-time manual (stop-HA required) — operator schedules its run separately from the code deploy; audit card applies via MCP post-deploy.
