# v5.5.4 Freeze-Floor Chokepoint — Review C

**Framing:** TEST AUTHORITY + PARSIMONY + Action-2-deferral
**Branch:** `feature/freeze-floor`  **Diff:** `git diff develop..HEAD`
**Reviewer C** of a Tier-3 (four-framing-disjoint) review. A/B/D cover helper, integration, completeness.

---

## LEAD: Per-CLASS mutation result (independently confirmed)

I neutered the floor at `hvac_setpoint.py:50` (`if False and freeze_active...`), ran the
freeze suite, restored, then neutered the deadband at `hvac_setpoint.py:55` and ran again.

**Floor-neuter → 9 FAIL (matches orchestrator's 9).** A SPECIFIC, REAL-code-driving test
fails for EACH emission class — not just preset:

| Emission class | Failing test (file:line) | Real production path driven |
|---|---|---|
| **preset** | `test_clamp_fires_writes_floor_low` (test_freeze_floor.py:318) | `HVACCoordinator._async_apply_preset_overrides` (hvac.py:1476) |
| **predict** | `test_predict_preheat_floored_via_chokepoint` (:622) | `HVACPredictor._execute_pre_heat` (hvac_predict.py:1040) |
| **override** | `test_override_compromise_floored_via_chokepoint` (:660) | `OverrideArrester._apply_compromise` (hvac_override.py:866) |
| **full-cycle leak (D-HIGH)** | `test_full_cycle_preset_then_preheat_final_low_floored` (:696) | preset-apply THEN pre-heat in one cycle; asserts FINAL low ≥50 |

Plus pure/emit guards (`:470,:483,:493,:502,:537`). **Deadband-neuter → exactly 2 FAIL**
(`test_apply_guards_deadband_fix_no_inversion` :483, `test_emit_deadband_never_inverts` :537) —
matches orchestrator's 2. Source restored; `git diff HEAD` on hvac_setpoint.py is EMPTY.

**Conclusion: predict and override each carry their own failing test, not just preset.** Per-class
coverage is genuine, not preset-only convergence.

---

## VERIFY checklist

1. **Tests drive REAL code — PASS.** Loader (test_freeze_floor.py:69-203) execs the real
   `hvac.py`, `hvac_setpoint.py`, `hvac_predict.py`, `hvac_override.py`, `preset_overrides.py`
   from source; only HA + leaf sibling stubs are faked. Tests bind unbound production methods
   (`__new__` + real method object), so the genuine `OverrideEngine` resolution + chokepoint run.
   These are NOT mirrors. The full-cycle leak repro (:696-717) drives preset-apply AND pre-heat in
   one sequence and asserts the FINAL (pre-heat) low == 50 — the D-HIGH scenario. It fails on
   floor-neuter (confirmed above).

2. **Real mutation per class — PASS.** See LEAD. One specific failing test per preset / predict /
   override, independently reproduced.

3. **Deadband test — PASS.** `test_apply_guards_deadband_fix_no_inversion` (:483) asserts
   `(47,49)+freeze → (50,52)` with `high > low` (no inversion). `test_emit_deadband_never_inverts`
   (:537) asserts the same through the wire path.

4. **Parsimony — PASS.** Toggle removal (config_flow.py:5697, strings.json, translations/en.json)
   removes the `hvac_on_hazard_emergency_heat` field and adds NOTHING. No new CONF_/options field
   for freeze. `CONF_HVAC_ON_HAZARD_EMERGENCY_HEAT` deliberately kept in const.py (back-compat, no
   options migration) — documented at config_flow.py:5716. `FREEZE_FLOOR/TRIGGER_TEMP/HYSTERESIS`
   (hvac_const.py:363-365) and `MIN_DEADBAND` (:157) are module-level `Final`. Chokepoint constants
   are not surfaced as runtime knobs — operator-confirmed parsimony.

5. **Action 2 deferred — PASS.** Grep of the full diff for `diverg|faulty|alert.*thermostat`
   returns nothing. No faulty-thermostat divergence/alert detector built. The only "Action 2"
   touched is the old emergency-heat hazard branch + `_set_emergency_heat`, correctly REMOVED
   (hvac.py ~1685-1730), replaced by the unconditional chokepoint floor.

6. **No new full-suite failures — PASS.** `PYTHONPATH=quality pytest quality/tests/ -q` →
   **35 failed, 5957 passed, 28 skipped, 14 errors** = baseline exactly (35+14). Isolated
   `test_freeze_floor.py` → 23 passed.

---

## Findings

- **LOW-C1 (docstring drift, test_freeze_floor.py:16,320-321):** docstrings reference
  `_apply_freeze_floor` and "deleting the clamp in `_apply_freeze_floor`" but the production
  function is `apply_setpoint_guards`. Stale name from an earlier design; harmless (the mutation
  gate still works against the real function) but misleads a future reader. Fix the docstrings.

- **LOW-C2 (parsimony/cleanup, observation only):** `CONF_HVAC_ON_HAZARD_EMERGENCY_HEAT` is now
  fully vestigial (defined, never read). Intentionally retained to avoid an options migration —
  acceptable, but worth a one-line backlog note so it's reclaimed in a future cleanup cycle.

No CRITICAL / HIGH / MEDIUM from this framing.

---

## Summary

| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 0 | — | — |
| HIGH | 0 | — | — |
| MEDIUM | 0 | — | — |
| LOW | 2 | 0 | 2 (docstring + vestigial CONF) |

Test authority is strong: real code paths, genuine per-class mutation kills (9 on floor, 2 on
deadband, independently reproduced), and the D-HIGH full-cycle leak is asserted end-to-end.
Parsimony is clean (a field removed, nothing added; constants stay Final). Action 2 (faulty-
thermostat detector) remains deferred. Two LOW docstring/cleanup items, no blocker. **Ship.**
