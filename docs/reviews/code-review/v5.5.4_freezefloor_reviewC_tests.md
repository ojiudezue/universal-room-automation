# v5.5.4 Freeze-Floor — Review C (Tier-3, framing: TEST AUTHORITY + PARSIMONY + Action-2-deferral)

**Reviewer:** C
**Branch:** `feature/freeze-floor` @ `7eb38733`
**Diff:** `git diff develop..HEAD` (hvac.py, hvac_const.py, test_freeze_floor.py)
**Lane:** test authority / mutation-anchoring / parsimony / Action-2 deferral. Clamp-correctness, integration, completeness owned by A/B/D.
**Spec:** `docs/planning/PLANNING_freeze_safety_range_shift.md` "FINALIZED DESIGN (Option B)".

---

## LEAD — REAL MUTATION RESULT (PASS)

I actually mutated production source, not just read the assertion.

**Mutation applied** to `custom_components/universal_room_automation/domain_coordinators/hvac.py:1430-1432`
— replaced the clamp body of `_apply_freeze_floor` with `return cool_low` (clamp neutered, freeze floor reverted to a no-op).

**Command:** `PYTHONPATH=quality python3 -m pytest quality/tests/test_freeze_floor.py -q`

**Result:** **1 failed, 10 passed.** The test that caught it:

```
FAILED quality/tests/test_freeze_floor.py::test_clamp_fires_writes_floor_low
E       assert calls[0]["target_temp_low"] == 50
E       assert 48.0 == 50
quality/tests/test_freeze_floor.py:316
```

The observed `48.0` is precisely the un-clamped baseline_low (`cool_setpoint 55 − 7.0 = 48.0`) flowing through the **real** `OverrideEngine.resolve_range`
(`preset_overrides.py:157-201`) to the captured `set_temperature` call. With the floor intact it is raised to `50`. This is a genuine end-to-end mutation gate, NOT a v5.5.2-style tautology.

**Source restored** via `git checkout -- …/hvac.py`. Post-restore: diff back to 102 insertions / 54 deletions; `test_freeze_floor.py` → 11 passed. No source left mutated.

**Verdict on the builder's claim:** CONFIRMED. Neutering `_apply_freeze_floor` fails `test_clamp_fires_writes_floor_low` exactly as claimed.

---

## 1. Tests drive REAL code (PASS)

- The loader (`test_freeze_floor.py:69-192`) stubs HA + sibling coordinators but loads the **real** `hvac.py` and the **real** `preset_overrides.py` (`:164-172`), then binds the unbound production `HVACCoordinator._async_apply_preset_overrides` to a bare instance (`_make_coord`, `:278-294`).
- The resolution path is genuine: `resolve_range` performs real per-field composition + the MIN_DEADBAND invariant (`preset_overrides.py:174-196`). No hand-rolled mirror of the clamp or the resolver exists in the test.
- A revert of the whole floor IS caught (proven by the mutation above). The clamp is also reachable via its real call site (`hvac.py:1517` inside the genuine apply loop), so a call-site deletion would fail the same test.

## 2. Real mutation (PASS) — see LEAD.

## 3. Coverage (PASS — each asserts the right thing)

| Behavior | Test | Asserts | Note |
|---|---|---|---|
| clamp-fires | `test_clamp_fires_writes_floor_low` :307 | low raised 48→50, high untouched at 55 | mutation gate |
| no-op when warm | `test_no_op_when_already_warm` :322 | low=53 (≥floor) written byte-for-byte, high=60 | proves no spurious clamp |
| no-clamp above trigger | `test_no_clamp_above_trigger` :334 | outdoor 70 → low=48 unchanged | freeze never arms |
| hysteresis arm + stay-in-band | `test_hysteresis_stays_active_until_above_ceiling` :346 | arm@35, stay@37, stay@38, clear@39 | exact band boundary |
| hysteresis no-arm-in-band | `test_hysteresis_does_not_arm_in_band_without_trigger` :364 | 37 cold-start → NOT armed | guards the latch direction |
| fail-open missing temp | `test_fail_open_missing_outdoor_temp` :372 | None → low=48 unchanged, `_freeze_active False`, no crash | |
| fail-open clears seeded | `test_fail_open_clears_seeded_active_state` :384 | seeded-True + None → clears | never holds fabricated freeze |
| `_set_emergency_heat` removed | `test_set_emergency_heat_method_removed` :402 | method absent from source | |
| freeze branch no single-mode | `test_freeze_hazard_no_longer_sets_single_mode_heat` :408 | no `self._set_emergency_heat()` call; `_apply_freeze_floor` present | |
| smoke/CO preserved | `test_smoke_co_fan_stop_branch_preserved` :419 | `_stop_all_fans_safety()` + `("smoke", "carbon_monoxide")` intact | |
| constants | `test_hvac_const_freeze_constants_present` :426 | FLOOR=50, TRIGGER=35, HYST=3 | |

All acceptance criteria from spec §29-36 are covered. Mapping is complete; no acceptance criterion lacks a test.

**MINOR (LOW) — C-LOW-1:** Two source-text-grep tests (`test_set_emergency_heat_method_removed`, `test_freeze_hazard_no_longer_sets_single_mode_heat` at :402/:408) assert against `hvac.py` raw text, not behavior. They are legitimate as removal-guards (you cannot exercise a deleted method), and the *floor* behavior is mutation-anchored, so this is acceptable — flagged only as a known brittleness (a rename of the helper would trip them). No fix required.

**MINOR (LOW) — C-LOW-2:** `test_smoke_co_fan_stop_branch_preserved` (:419) asserts on source substrings rather than driving `_handle_safety_hazard`. Within my lane this is adequate (the smoke/CO path is out of this cycle's behavior change), and Reviewer B owns the integration trace. Noted, no fix.

## 4. Parsimony (PASS)

- Greps over `const.py`, `config_flow.py`, `options_flow.py`, `number.py` for `FREEZE_FLOOR` / `FREEZE_TRIGGER` / `FREEZE_TRIGGER_HYSTERESIS` → **zero hits**. No CONF_, no NumberSelector form field, no NumberEntity, no options-flow step was added.
- The three constants live as module-level `Final` in `hvac_const.py:362-364` with a parsimony rationale comment (`:354-361`) — exactly where the spec §44 said to put them and matching the operator's "v1 hardcoded, not exposed" directive (spec §23).
- `CONF_HVAC_ON_HAZARD_EMERGENCY_HEAT` (`const.py:1457`, `config_flow.py:5697/5718-5719`) is **pre-existing** (not introduced here) and is now vestigial; the builder left it for back-compat and documented that at `hvac.py:1685-1687`. Acceptable — removing a shipped CONF would be a migration risk; leaving an inert flag is the parsimonious choice.

## 5. Action 2 genuinely deferred (PASS)

- Grep of `hvac.py` for `current_temp` / `divergence` / `faulty` / `cross_check` / `independent…sensor` / `room_temp` divergence logic → **no detector exists**.
- `_handle_safety_hazard` (`hvac.py:1680-1687`) replaces the old freeze action with a comment-only stub; no aggressive-setpoint-on-divergence, no thermostat-lie detection, no alerting was built.
- This matches spec §25-27 (Action 2 = faulty-thermostat detect-and-alert) being explicitly deferred to a Safety-Coordinator follow-up. **No half-built detector to flag.**

## 6. No new full-suite failures vs baseline (PASS)

- Baseline (stated): **35 failed + 14 errors.**
- Observed full suite (`PYTHONPATH=quality python3 -m pytest quality/tests/ -q`): **35 failed, 5946 passed, 28 skipped, 14 errors in 38.54s.** Identical failed/error counts. The pre-existing errors are unrelated `ModuleNotFound` collection errors (e.g. `test_activity_logger.py`), present on baseline.
- New file isolated: **11 passed in 0.02s.** Adds 11 passes, zero new failures/errors.

---

## Severity summary

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 0 | — |
| LOW | 2 | C-LOW-1 (source-grep removal guards), C-LOW-2 (smoke/CO source assert) |

Both LOWs are accepted-as-designed (no behavioral way to assert a deleted method / out-of-lane branch). No fix required in this cycle.

## Verdict (Review C lane): **APPROVE**

Test authority is real (mutation-proven), coverage is complete and correctly-asserted, parsimony is respected (no new config), Action 2 is genuinely deferred (no half-built detector), and no new suite regressions. Clamp/integration/completeness correctness deferred to Reviewers A/B/D.
