# v5.53.0 Exterior Track Linking — Review Record

Five reviews total: A/B/C/D in parallel (Tier 2-DB elevated with a
Tier-3-style adversarial-completeness D pass — security surface), plus a
focused fifth on the post-review control-surface commit. Build
cf34a6505 → extension b3ef19f1f → consolidated fix-up 660e7eee4 →
control surface f9b62b344/64d79bedb/75ddb3797.

## Findings summary

| Sev | Found | Fixed | Adjudicated/Deferred |
|---|---|---|---|
| CRITICAL | 4 (A-CRIT-1, D-CRIT-1/2/3 — one family: suppression-as-silence + unconfident demotion broke INV-XP under default config) | 4 (demote-never-silence redesign; per-repro anchor tests) | 0 |
| HIGH | 9 (A×4, B×3, C×2) | 9 | 0 |
| MEDIUM | 12 | 11 | 1 (walker-fixture timing adjudicated: synthetic boundary deltas retained alongside real-delta e2e test) |
| LOW | 8 | 6 | 2 (L-2 census read caching; LOW-2 is_on-while-unavailable) |

Full detail lives in the four review transcripts (task outputs) and the
fix-up's per-finding table; key structural outcomes:

1. **Invariant amendment, adjudicated:** the plan's "at most one alert
   thread" is unachievable under INV-XP (first event per camera passes
   the cooldown gate before track history exists; circling deliberately
   escalates). Amended invariant: exactly one OPEN track per person;
   alert volume strictly collapsed (≤ distinct cameras); NOTHING is ever
   fully silenced; demotion only on confident multi-camera pass_by.
2. **Review D earned its seat again** (2nd time after v5.5.3): three
   concrete-repro CRITICALs that A/B framings did not surface, on the
   exact "one missed path = silent security loss" shape that justifies
   the 4th pass on security/cost surfaces.
3. **#62 ledger:** C's independent battery found 2 silent-green sites
   (episode writer, enrichment); orchestrator later found none surviving
   in the control surface (both new gates red under personal mutation).
4. **Process findings:** Review C's agent dropped a git stash whose
   provenance is uncertain (all 9 known stashes verified present;
   unreachable commits recoverable via fsck — no known loss). A piped
   test gate masked a red suite and let a py39-annotation regression
   commit; caught by full-suite name-diff, fixed, and the pre-deploy
   gate memory hardened ("gate in its own call, look at the count").
5. **Cross-file test pollution** (asyncio loop consumption) found by
   orchestrator during verification — fixture made loop-independent.

## Mutation ledger (cumulative, all red-verified, pycache-cleared)

Builder pass 1: 6 sites. Extension pass: 2 sites + 3 D-repro behavioral
anchors. Review C independent: 6 mutations (2 silent-green → fixed).
Consolidated fix-up: 2 sites. Control surface: 2 sites (builder) + 1
re-run (focused reviewer) + 2 personal (orchestrator). Focused reviewer
spot-check: 1. Orchestrator personal: first-alert guard, tracking gate,
smart-alerts gate.

## Bug-class notes

- D-CRIT family = Bug Class #53 at system level (guard placed at the
  wrong conceptual layer: dispatch suppression instead of severity
  refinement). Recommend QUALITY_CONTEXT addition: "cadence/severity
  refinements on alerting surfaces must be provably silence-free —
  enumerate the zero-output paths and require each to be impossible."
- B-H1/H2 recurrence of #19/#42 (untracked tasks, missing timer) in a
  NEW module — the classes recur most in greenfield files where no
  existing pattern anchors the builder.
