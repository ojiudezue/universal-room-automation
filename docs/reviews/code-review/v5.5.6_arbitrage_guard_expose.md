# Code Review — Arbitrage grid-import guard: expose in config flow + default OFF

**Branch:** `feature/arbitrage-guard-expose-default-off`
**Commits:** `04920de5` (expose) + `381b2a35` ((c) required-when-enabled) + `b5eff24d` (review fix-up)
**Baseline tag:** `pre-review-arbitrage-guard-expose`
**Tier:** 2-DB (battery/arbitrage CHARGE path — cost+safety; 3 framing-disjoint reviews)
**Plan:** `docs/planning/PLANNING_arbitrage_import_guard_expose_and_default_off.md`

## Invariant
When DISABLED (the new default), no battery grid-charge tick is ever aborted/throttled/chunk-locked for an import threshold — Enphase hardware curtailment is the only limit. When ENABLED at kw=N, behavior is byte-identical to the pre-change always-on guard at N.

## Three parallel reviews

| Review | Framing | Verdict | Findings |
|---|---|---|---|
| A | Local correctness | SHIP | A-MED-1 (non-coercible stored kw silently disables), A-LOW-1 (dead import), A-LOW-2 (kw≤0 unvalidated) |
| B | State-machine / restart | SHIP | B-LOW (dead helper `_grid_import_guard_triggered` — no production callers) |
| C | Config round-trip + test authority | FIX-FIRST | **C-HIGH-1 (sites 3&4 mutation tests tautological)**, C-MED-1 (hand-edited kw=0 → effective 0.0 bricks arbitrage), C-MED-2 (rejected form re-render — pre-existing idiom), C-LOW-1 (cross-test dep fragile) |

## Fix-up (`b5eff24d`)
- **C-HIGH-1** — site-3/site-4 tests rewritten to drive the real `_run_attain_branch` and assert on production trip-counter / chunk-completed / aborted-at state (not a compare evaluated in the test body).
- **C-MED-1 / A-LOW-2** — runtime collapse now treats a configured value that is None, ≤0, or non-finite as DISABLED (effective `inf`, configured-surface `None`). New tests for kw∈{0,−5}.
- **A-MED-1 (FIX 3)** — one `_LOGGER.warning` at init when guard is enabled but no valid threshold was supplied (silently treated as disabled → now observable).
- **A-LOW-1 (FIX 4)** — removed dead `DEFAULT_ARBITRAGE_GRID_IMPORT_GUARD_KW` import.

## Orchestrator independent verification (per high bar; not trusting reviewer/builder summaries)
Per-site real source mutation (each guard site rewritten to read the raw `_configured` field instead of the inf-collapsed effective field), `PYTHONDONTWRITEBYTECODE=1` + cache cleared per run to defeat a macOS `.pyc` mtime-staleness false-negative that initially masked sites 2 & 4:

| Site | Location | Bypassed → test result |
|---|---|---|
| 1 helper | `energy_battery.py:1120` | `test_site1` **FAILS** ✓ |
| 2 arbitrage CHARGE tick | `:1473` | `test_site2` **FAILS** (`'wait'=='charge'`) ✓ |
| 3 attain CHARGE re-check | `:2594` | `test_site3` **FAILS** ✓ |
| 4 attain ENTRY precedence | `:2705` | `test_site4` **FAILS** ✓ |

All four guard sites genuinely route through the single inf-collapsed effective field, and each has a test that fails if that site is bypassed (Bug Class #53 — computed-but-not-consumed — foreclosed across the whole surface).

## Deferred (not ship-blocking)
- **C-MED-2** rejected-form re-render wipes in-progress edits — pre-existing step-wide idiom, note in README.
- **B-LOW / FIX-5** dead helper `_grid_import_guard_triggered()` kept (tested + harmless; no production callers). Hygiene-delete or wire-as-canonical-predicate in a future pass.
- **C-LOW-1** test reuse of `_OptionsFlow` from `test_v4743` — consider hoisting to conftest.

## Tests
Cycle file `test_arbitrage_grid_import_guard_expose.py`: 24 passed. Full suite: 35 failed / 6008 passed / 28 skipped / 14 errors — the 35 failures + 14 errors are pre-existing and unrelated; +6 passed vs branch baseline, zero new regressions.

## Bug class frequency
- #53 computed-but-not-consumed (the cycle's whole charter; foreclosed) — test-net gap was the real finding (C-HIGH-1), not a production defect.
- Test authority / tautological assertion (C-HIGH-1).
- Falsy-collapse + incomplete input validation (C-MED-1).
