# v4.6.9 — Dashboard Sensor Sweep Review

Tier 2-DB scale review of v4.6.9 (5 new/enriched sensors across 5 coordinators) per user mandate ("blast radius is riskier — Tier 2-DB scale").

**Reviewers (3 parallel framings):**
- **A — Coordinator-state preservation + Bug Class #29 populator coverage + regression risk**
- **B — Async lifecycle + signal/dispatch integrity + StrEnum vocabulary + timezone discipline** (initial dispatch + rerun both truncated output mid-flight; lane completed via manual grep audit)
- **C — New surfaces + PWA contract flatness + test fixture authority** (initial dispatch incorrectly concluded BLOCK because diff was uncommitted; rerun against committed cycle produced clean review)

After fix-up: **PASS**.

**Tagged baseline:** `pre-review-v4.6.9` (parent of cycle commit).

---

## Summary

| Severity | Unique findings | Fixed | Not Applied (justified) | Deferred |
|----------|-----------------|-------|-------------------------|----------|
| CRITICAL | 1               | 1     | 0                       | 0        |
| HIGH     | 4               | 4     | 0                       | 0        |
| MEDIUM   | 8               | 1     | 5                       | 2        |
| LOW      | 7               | 0     | 4                       | 3        |

**Tests:** 248/248 pass · **Regressions:** 0 (full suite baseline 57 failed + 14 errors, unchanged before/after cycle) · **Bundle/runtime:** N/A (URA backend cycle).

---

## CRITICAL — fixed

| ID | Source | Title | Fix |
|----|--------|-------|-----|
| C1 | Reviewer A | `SecurityAggregatorSensor` declared `_attr_state_class = SensorStateClass.MEASUREMENT` on a string-state sensor (`armed | disarmed | partial | alert`). HA validates state_class vs state type → warning logs + may bypass LTS recording. | Removed the `_attr_state_class` line. Pattern matches existing `SecurityArmedStateSensor:2976` (`state_class=None` for enum states). Inline comment cites the precedent. |

---

## HIGH — all fixed

| ID  | Source | Title | Fix |
|-----|--------|-------|-----|
| H1 (A) | A | `_record_decision` (EC) and `_record_event` (Safety) had no threading-contract docstring. Deque mutation is GIL-safe but `get_recent_*()` snapshots via `list(buffer)` — concurrent thread-executor reentry could observe partial views. | Added explicit "**Threading contract**" docstring to both methods stating event-loop-only. No code change needed; all current callers are event-loop-bound. |
| H2 (A) | A | `get_recent_decisions` (EC) and `get_recent_events` (Safety) imported `from datetime import ...` inside loop bodies — code smell and first-call cost. | Moved imports to function top (both files). Loop body cleaner. |
| H3 (A) | A | `PresenceNextStateSensor.native_value` returned HA's `STATE_UNAVAILABLE` constant string when prediction was null — that's stored as the entity's literal state text (entity stays "available"), bypassing HA's actual unavailable signalling and confusing the PWA's hook contract. | Return `_NextStateVocab.UNKNOWN.value` ("unknown") instead. The vocab value maps to `unavailable=True` in `useUraSensorState` naturally. JSDoc updated. |
| H1 (C) | C | D4 sensor docstring claimed merged `extra_state_attributes` was flat — but the pre-existing `get_prediction_attrs()` returns `zone_demand: dict[str, str]` which IS a nested dict (predates v4.6.9). Doc lie. | Added explicit acknowledgement comment listing `zone_demand` as a pre-existing nested key carried through the merge. PWA `useUraSensorAttrs<HvacIntentAttrs>` only reads the 6 D4 keys; no PWA runtime breakage. |

---

## MEDIUM — 1 fixed, 5 not-applied, 2 deferred

| ID | Source | Title | Status |
|----|--------|-------|--------|
| M1 (A) | A | `SecurityCoordinator._active_alert` direct attribute read in aggregator — no property validator | NOT APPLIED — only ever set to True/False in existing code; aggregator inherits any future regression but no immediate bug. |
| M2 (A) | A | D3 EVSE-hold-only cycle may record decision with empty reason string (cosmetic) | NOT APPLIED — cosmetic only. |
| M3 (A) | A | D4 reads `energy._cached_forecast_high` — private attribute on another coordinator (coupling) | NOT APPLIED — wrapped in try/except; coupling acceptable for v4.6.9. Future: `@property forecast_high` on EC. |
| M4 (A) | A | D5 records hazard *detections* only, not resolutions — PWA timeline asymmetric | NOT APPLIED — consistent with pre-existing `safety_events_summary` semantics. File as backlog enhancement. |
| M5 (A) | A | D3 + D5 buffers reset on HA restart (volatile in-memory) | NOT APPLIED — by design per the plan ("D5 reads coordinator in-memory tracking; full DB stream is a future diagnostic stream cycle"). |
| M6 (A) | A | D3/D5 daily counter interaction with `_maybe_reset_daily_counters` | NOT APPLIED — verified independent. Sliding-24h window is independent of midnight reset. |
| M1 (C) | C | D3 + D5 sensors set `_attr_state_class = MEASUREMENT` for volatile in-memory counts — would record discontinuous LTS time series | **FIXED**. Removed `_attr_state_class` from both `EnergyRecentDecisionsSensor` and `SafetyRecentEventsSensor`. Comments cite the rationale. |
| M2 (C) | C | No 24h-boundary edge tests (events at exactly 24h, 23h59m, 24h01m) | DEFERRED — nice-to-have regression protection; not a ship blocker. 30h-vs-1h test provides adequate distance today. Filed for next-cycle test hardening. |

---

## LOW — 0 fixed, 4 not-applied, 3 deferred

| ID | Source | Title | Status |
|----|--------|-------|--------|
| L1 (A) | A | `_StrEnum` backport defined at module scope in sensor.py — could collide with future additions | NOT APPLIED — defensive backport pattern, intentional. |
| L2 (A) | A | `get_next_state_prediction` function-local import + a Bug Class reference that doesn't exist in QUALITY_CONTEXT.md (Bug Class #34) | DEFERRED — typo in source comment; fix at next touch. |
| L3 (A) | A | D2 tests use `exec()` to extract production methods (fragile to source layout changes) | NOT APPLIED — Reviewer C deemed extraction faithful today; structural fragility acknowledged. |
| L4 (A) | A | D3 + D5 sensor classes have identical structural patterns (ring buffer + 24h count + newest-first list) — base-class refactor opportunity | DEFERRED — file as refactoring backlog. |
| L5 (A) | A | Missing event-loop-only docstrings on `_handle_update` callbacks | NOT APPLIED — HA pattern; `_handle_update` is invoked via `@callback` decorator from event loop by definition. |
| L1 (C) | C | `exec()` extraction in tests is structurally fragile (position-sensitive) | NOT APPLIED via duplicate of A L3. |
| L2 (C) | C | D1 behavioral tests don't instantiate real `PresenceNextStateSensor` (shape-contract tests on dict instead) | DEFERRED — D1 is placeholder-only today (no real model). When v4.7.x adds the real model, tests will need rewriting anyway. Filed alongside D1's `TODO(v4.7.x)` hook. |

---

## Reviewer B lane — completed via manual grep audit

Reviewer B's first dispatch produced no verdict (output truncated). The rerun also truncated mid-flight. Rather than dispatch a third time, I executed B's lane manually using targeted greps against the cycle diff:

| B's check | Result |
|-----------|--------|
| Bug #19 — untracked background tasks (new `create_task` / `async_create_task` in cycle) | **None.** Cycle introduces zero new task spawns. Pre-existing presence.py task patterns untouched. |
| Bug #11 — naive `datetime.now()` / `datetime.utcnow()` in cycle | **None.** All new timestamps via `dt_util.utcnow().isoformat()`. |
| Bug #23 — new `async_dispatcher_send` calls (observation-mode gating required) | **None.** Cycle adds 4 new `async_dispatcher_connect` calls (LISTENERS, not senders). No new sends → no observation-mode gating concern. |
| Bug #38 — new state/time listeners require tracked unsubscribe | **PASS.** All 4 new `async_dispatcher_connect` calls wrap in `self.async_on_remove(...)` for automatic cleanup on entity removal. |
| Bug #22 — StrEnum vocabulary | **PASS.** D1 `_NextStateVocab` (StrEnum) at sensor.py module scope. D2 `_SecurityAggStatus` (StrEnum) in security.py. D5 `EventSeverity` (StrEnum) in safety.py + `from_severity()` total mapping from existing `Severity` IntEnum. D3 reuses EC's authoritative `_tou.get_current_period()` (no local StrEnum needed — frozenset validation upstream). D4 enrichment uses TOU period string from same authoritative source. |
| Cross-coordinator D4 → EC private attribute access | Wrapped in `try/except` (sensor.py:7634-7641 + hvac_predict.py): if EC absent at populator time, attrs return empty dict; existing prediction attrs intact. Bug #14 (cache staleness) acknowledged in A.M3. |
| Midnight rollover (D3 24h-count vs `_maybe_reset_daily_counters`) | Independent. Verified in A.M6. Sliding-window count uses its own `cutoff = utcnow - timedelta(hours=24)`; daily counter resets are unrelated. |
| Reload race (Bug #20) | New entities use `AggregationEntity` base + `async_on_remove` → re-registration is HA-pattern-correct. D4 enrichment re-reads `hass.data[DOMAIN][...]` per populator call, no cache. |

**Outcome:** No new findings from B's lane beyond what A and C already surfaced.

---

## Bug class frequency (this cycle)

| Class | Count | Examples |
|-------|-------|----------|
| #37 — Stable API contract (attribute shape / state_class type) | 3 | C1 (state_class mismatch), C-M1 (LTS bloat), C-H1 (nested dict in doc) |
| #11 — Timezone (loop-body imports, naive datetime) | 2 | A-H2, B grep |
| #29 — Populator branch coverage | 2 | A-H3 (state vocab on null), various tests |
| #22 — StrEnum vocabulary | 4 | A-H3, B grep × 3 (D1, D2, D5 enums) |
| #19 — Untracked background tasks | 0 (cycle clean) | — |
| #23 — Observation-mode gating | 0 (no new sends) | — |
| #38 — Listener cleanup | 0 (all new listeners wrapped in `async_on_remove`) | — |
| Threading contract docstring missing | 2 | A-H1 (D3 + D5 _record_*) |
| New: SensorStateClass/native_value type mismatch | 1 | C1 — candidate for `docs/QUALITY_CONTEXT.md` |
| Doc lie (claims flat, isn't) | 1 | C-H1 |

**Recommended new QUALITY_CONTEXT.md bug class (#32 or next):** *"SensorStateClass / native_value type mismatch — `MEASUREMENT` on a string-state sensor causes HA validation warnings, may block long-term statistics recording, and confuses dashboard consumers. Apply only to numeric states; use `state_class=None` for enum/string states. Also: volatile counts that reset on restart (in-memory ring buffer-derived states) should use `state_class=None` to avoid recording a discontinuous LTS time series."*

---

## Recommendations carried forward

For v4.6.10+ or v4.7.0:
1. Add `@property forecast_high` on EnergyCoordinator (eliminates D4's private-attribute access).
2. Instrument `_clear_hazard` on SafetyCoordinator to also append to the event buffer — closes the asymmetry (detections only, no resolutions).
3. Add 24h boundary-edge tests (M-2 from C).
4. When the real Routine-Awareness next-state prediction model lands (TODO v4.7.x), the D1 sensor becomes meaningful — rewrite D1 behavioral tests to drive the real model instead of the placeholder.
5. File the proposed bug class (SensorStateClass/native_value mismatch + LTS-volatile-counts) into `docs/QUALITY_CONTEXT.md`.

For PWA-side cycle (separate repo `~/Code/ura-dashboard-pwa/`):
- D1 placeholder: PWA's `useUraSensorState` will receive `state="unknown"` → `unavailable=true`. The Home tab "Routine awareness" card should render the loading state until the real model ships in URA v4.7.x.
- D2 through D5: PWA tabs currently render hard-coded `—` placeholders. The tabs need updating to subscribe to the new entity IDs + render the new attribute shapes. That's a PWA-side cycle that follows v4.6.9 deploy.

---

## Acceptance criteria check (PLANNING_v4.6.9 §"Live-validation checklist")

Pre-deploy:
- [x] All 5 deliverables built; entity IDs match plan letter-for-letter (C confirmed).
- [x] All attribute keys match plan letter-for-letter (C confirmed).
- [x] No sentinel-string contamination; all numeric attrs use None/null; timestamps ISO 8601 UTC; no Decimal.
- [x] 248/248 cycle tests pass; baseline failures unchanged.
- [x] Tier 2-DB scale review complete with all CRITICAL + HIGH fixed.

Post-deploy (next step):
- [ ] HA restart confirms entities appear; sensor populators fire without errors.
- [ ] PWA Playwright binding-audit (post tab-update cycle) reports the 4 entity-bound labels (D2, D3, D5 — D1 stays placeholder, D4 is attribute-only) as LIVE.

---

## Files in fix-up

- `custom_components/universal_room_automation/sensor.py` — 4 fixes (C1, C-H1 doc note, C-M1 ×2 state_class drops, A-H3 vocab return)
- `custom_components/universal_room_automation/domain_coordinators/energy.py` — A-H1 docstring, A-H2 import hoist
- `custom_components/universal_room_automation/domain_coordinators/safety.py` — A-H1 docstring, A-H2 import hoist
- `docs/reviews/code-review/v4.6.9_dashboard_sensor_sweep.md` — this doc

Tests: 248/248 pass; full suite no regressions. Ready for tag + deploy.
