# Code Review — v5.35.0 Stuck-Signal Watchdog

**Tier:** 2 (two framing-disjoint reviews + live validation). **Baseline:** `pre-review-v5.35.0`.
**Framings:** A = detection correctness + corroboration semantics + discount math; B = async/lifecycle + fail-open + NM plumbing + no-actuation.

**Both reviews returned DO-NOT-SHIP on the first pass** — the strongest first-pass result against a build whose own 14 tests were green. The findings cluster on exactly the classes the framings were designed to catch.

## Findings

| ID | Sev | Class | Summary | Status |
|---|---|---|---|---|
| A-CRIT-1 | CRIT | inverted trust hierarchy (#48-adjacent) | D3 disagreement predicate structurally blind to the Ezinne incident — the frozen tracker DRIVES person=home, so tracker/person always agree → silent | **FIXED** — predicate redesigned: frozen-at-home/unknown ≥N days fires per se; sibling disagreement = context, not gate. Ezinne repro named test |
| A-CRIT-2 | CRIT | #62 test authority | D3 tests drove a test-file REIMPLEMENTATION, not production code — mutations invisible | **FIXED** — tests rewritten to drive the real method; orchestrator mutation drill proves authority (inverted predicate → 5 fail) |
| A-CRIT-3 | CRIT | #62 bug-codifying test | `silent_on_agreement` test locked the A-CRIT-1 bug in as intended | **FIXED** — deleted; replaced with frozen-at-not_home-silent (the correct asymmetry) |
| B-H-1 | HIGH | #48-adjacent actuation reach | D2 fed the SAME exclusion set as Fix #9 → a sleeping person (~100% mmWave duty, 0 PIR) could be excluded → bedroom vacated via home_night trust gap | **FIXED** — D2 demoted to notify+diagnostic only; exclusion deferred to a trust-gated future cycle; mutation-anchored test |
| B-H-2 | HIGH | boot transient | No boot-settle gate (plan claimed one) — cold boot could fire+exclude within a minute | **FIXED** — all three detectors + emits gate on the canonical presence boot-settle predicate (ActuatorReconciler's source) |
| A-HIGH-1 | HIGH | corroboration decay | Single PIR blip anywhere in 60 min disabled D2 detection entirely | **FIXED** — ≥2 transitions in-window OR 1 fresh (≤5 min), named constants |
| A-HIGH-2 | HIGH | false-discount | D1 could census-drop a stationary lone guest in a camera-only area | **FIXED** — areas with no interior tier are notify-only, never discounted; residual tradeoff documented in plan |
| A-HIGH-3 | HIGH | silent failure | Area-less cameras auto-discounted with no log | **FIXED** — explicit skip + one-time WARN + diag marker |
| A-MED-1 | MED | spec gap | Oscillating non-zero counts evade the "unchanged" rule | **ACCEPT-WITH-NOTE** in plan (future "never-zero" sibling rule) |
| A-MED-2 | MED | observability | raw_pre_dedup_sum included discounted counts | **FIXED** — sum moved after discount |
| A-MED-4 | MED | date semantics | Per-day latch used UTC date | **FIXED** — local date |
| A-MED-5/B-L-1 | MED | knob-naming | rung-1 constants wore CONF_ prefixes + read entry options | **FIXED** — renamed STUCK_*/FROZEN_* module constants |
| B-M-2 | MED | test authority | fail-open tests didn't drive production | **PARTIAL** — builder judged call-site contract tests sufficient; orchestrator independently verified the try/except + discount-clear at the census call site by source read |
| B-M-3 | MED | task spam | per-tick no-op NM tasks before latch | **FIXED** (coordinator by builder; person_coordinator **by orchestrator directly** — builder had misreported completeness) |
| B-M-4 | MED | unpaired notify | X7 recovery emit without prior stuck emit | **FIXED** — notified-set gate |
| B-M-5/6/7, A-LOW-1/2/3, B-L-2/3 | LOW/MED | hygiene | docstring, latch GC, tz branches (kept guarded), unknown-state, fake hours, dead field, motion-state purge, private-attr accessor | **FIXED** (tz branches kept as guarded defensive code — noted) |

## Summary statistics
| Severity | Found | Fixed | Accept-with-note |
|---|---|---|---|
| CRITICAL | 3 | 3 | 0 |
| HIGH | 5 | 5 | 0 |
| MEDIUM | 8 | 7 | 1 |
| LOW | 7 | 7 | 0 |

## Orchestrator independent verification
- **Real source mutation on the D3 predicate:** inverted `not in ("home","unknown")` → 5 D3 tests FAIL; restored → 18/18 pass. Test authority over production code proven (the exact thing A-CRIT-2 said was missing).
- Grep-verified: no `stuck_sensors.add` on the D2 path; D1 fail-open try/except clears the discount sets at the census call site.
- **Caught a builder misreport:** B M-3 was fixed in coordinator.py only; person_coordinator.py D3 still spammed a task per stale tracker per tick — patched directly (caller-side notified-set + recovered-tracker sweep + lazy init for fixtures).

## Bug-class notes for QUALITY_CONTEXT
- The A-CRIT-1 shape deserves a name: **"detector blind to its own motivating incident"** — when the audited signal is UPSTREAM of the evidence used to judge it, agreement is guaranteed and the detector is decorative. Check: is the corroborating evidence causally independent of the signal under test?
- Recurrence of #62 (tests reimplementing production) — second cycle in a row (v5.33.0 A-MED-3 was the same family). Consider a review-checklist line: "does each behavioral test import the production symbol it claims to test?"
</content>
