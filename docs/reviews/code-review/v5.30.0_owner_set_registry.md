# Code Review Record — v5.30.0 Owner-Set Registry Refactor (Tier 3, behavior-frozen)

**Branch:** build/owner-set-registry (db18833d oracle → 101729ca golden v2 →
2c12cfc2 big-bang migration → 0014ba7b phase-3 fix-up). Baseline tag
`pre-review-v5.30.0`. **Tier 3** (operator-elevated from the BACKLOG's 2-DB):
A/B/C/D + orchestrator verification. **Status: AT OPERATOR CHECKPOINT.**

## What this cycle is
17 owner surfaces (12 EV + 5 plug) declared once in `energy_pool_owners.py`;
five surfaces (persist, restore, prune, peer-holds, get_status classifier)
derive from declarations. S3 preserved: sets stay separate, attr names
untouched (the 8 external test files unaffected). Acceptance = byte-identical
behavior, held by a 3,158-row golden oracle + a permanent 8-row mutation
matrix test + production-path persistence round-trips.

## Findings ledger (all closed)

| id | sev | finding |
|---|---|---|
| C-HIGH-1 (=A-MED-1=B1) | HIGH | Golden's save surface was a hand-copied shim; restore uncovered — 4 semantic breaks survived the full suite (key rename, quirk drop, blind-window restore neuter, DP reinstall neuter). FIXED: production `_save/_restore_registry_owner_lists` helpers driven via FakeKVDB; all 8 key→attr bindings literal-pinned; shim deleted. |
| C-HIGH-2 | HIGH | Blind-window restore wiring untested. FIXED: behavioral test through production restore (pre-engaged + epoch asserted). |
| C-HIGH-3 | HIGH | DP reinstall guard was AST substring-theater. FIXED: behavioral `_dispatch_owners ⊇ {"dp"}` assert. |
| C-HIGH-4 | HIGH | load_shed quirk "protection" vacuous (fixture never seeded the pruned id). FIXED: both ids seeded; quirk mutation now falsifies. |
| D-1 | MED | Restore exception envelope narrowed: corrupt non-list KV aborted ALL later restores (vs per-key swallow pre-refactor). FIXED: isinstance guard + corrupt-KV test. |
| C-MED-1 | MED | No golden integrity guard (silent regen = tautology). FIXED: content SHA-256 + pinned source_commit asserted. |
| D-3/D-4 | MED | `_load_shed_was_on_at_shed` (both tiers) + 5 plug dicts undeclared; plug prune comment falsely implied none exist. FIXED: 6 non-participant declarations + corrected comment. |
| A-LOW-2/D-5/C-LOW-1/D-2/B3/A-LOW-1 | LOW | Unknown-restore-hook now raises; unconsumed plug fields documented; KeyError-safe diagnostics; KV write-order change documented as accepted (order-independent, verified by A and C independently); benign iteration-order change chased to ground. |

## Statistics
CRITICAL 0 · HIGH 4 (fixed 4) · MEDIUM 4 (fixed 4) · LOW 6 (fixed/documented 6).

## Verification
- Reviewer C ran 8 real-source mutations pre-fix (4 GREEN escapes → the
  HIGHs); post-fix the permanent in-suite mutation matrix
  (`test_owner_registry_mutation_matrix.py`, subprocess-isolated, ~4 s)
  re-proves 6 kills + 2 designed survivors every run.
- Orchestrator personally executed the extraction-tautology check the
  phase-3 fix itself could have introduced: severed BOTH production call
  sites (`_save_evse_state:1817`, `_restore_evse_state:1370`) — each
  severing fails 3-5 named tests (behavioral round-trips enter through the
  full production path). Restored clean. This bounded re-check stands in
  for a second full D pass: phase-3's production diff is one guarded line +
  extracted helpers + doc-shaped declarations, all now empirically anchored.
- Golden v3: 3,158 rows, byte-identical across runs, SHA-256 + commit
  pinned. Full suite: 36F+14E env-drift baseline preserved through all
  four commits; +19 net tests.

## Follow-ups filed
- Tier-1 immediately post-merge (operator-ratified): add load_shed to the
  EV prune path (the preserved quirk's fix) + arbitrage reason-map ⊆
  invariant policing (D danger-spot #4) — one small cycle, golden
  re-baselined intentionally with documented divergence.
- LKG wave 1 unblocks behind this merge (its keys land as declarations).

**Live validation at deploy:** restart round-trip (owner pauses survive),
plus the standard clean-boot gates. No behavior change expected — the
ledger entry IS "nothing observable changed."
