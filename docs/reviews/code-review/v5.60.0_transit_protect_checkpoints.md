# Code Review Record — v5.60.0 TRANSIT-1 "Protect-sourced traversal checkpoints"

**Scope:** interior traversal checkpoints were sourced from a hand-maintained
`CONF_CAMERA_PERSON_ENTITIES` list that silently drifts (2 of 5 checkpoints had fallen out).
This cycle sources them from UniFi Protect via a new `CameraResolver.enumerate_platform_cameras`,
attributed by area, unioned with the hand-list, behind a kill switch.
**Tier:** 2-DB (feeds presence trust). Three framing-disjoint reviews + orchestrator drills.
**Baseline tag:** `pre-review-transit-1` (4ee912805). Fix-up: `aea62bdcc`.
**Enabler:** 39 live camera area corrections earlier the same day (see
`AUDIT_resolver_ground_truth_manual.md`) — without correct areas this feature cannot attribute rooms.

## Findings

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| A-CRIT-1 / B-L1 | **CRITICAL** | Protect-sourced entities were subscribed and their sightings recorded, but `validate_transition` filtered sightings through `_get_shared_space_cameras()` = **legacy hand-list only** → the cycle's entire value was recorded then discarded at the decision point. Protect-only-house scenario delivered nothing. | #53 computed-but-not-consumed | ✅ F1: union the sighting filter + activity check with the Protect set, under the same kill switch |
| B-H1 / C-M2 | HIGH | Double-fire: a camera's Protect **and** Frigate legs both subscribed (set() dedups by entity_id, not physical camera) → 1 crossing = 2 sightings → `n_sightings >= n_transitions` satisfied on one camera's evidence → wrong `path_plausible`/`path_validated` **inflating presence trust** | double-count / trust inflation | ✅ F2: collapse by `device_id` at record time, `TRANSIT_DOUBLE_FIRE_DEDUP_SECONDS=5.0` (rung-1) |
| A-H1 | HIGH | `enumerate_platform_cameras` grouped by `device_id`; a Protect NVR device hosts MULTIPLE physical cameras (staircase + garagehallway) → collapsed to one row/one area → wrong attribution + lost coverage. `_scan_device_entities` already had the stem_filter guard; the new call site didn't. | F1-regression at a new call site | ✅ F3: group by `(device_id, stem)`, filter legs per-stem |
| A-H2 | HIGH | Cross-leg area fallback could import a **foreign camera's** area (correlation ladder pulls siblings from other physical cameras) → checkpoint dropped or an outdoor camera admitted as interior | wrong-attribution | ✅ F4: fallback restricted to same-platform legs |
| B-H3 | HIGH | Enumeration one-shot at `async_init`; if Protect loads after URA the checkpoint set freezes **empty until full restart** — silently reverting to hand-list | boot-ordering, no self-heal | ✅ F5: `EVENT_ENTITY_REGISTRY_UPDATED` (platform=unifiprotect) → debounced rebuild+resubscribe |
| B-H4 | HIGH | Kill switch + checkpoint-areas knob latent unless reload-allowlisted → unusable during a live incident (or triggers the parent-reload watchdog hazard) | fire-axe unusable | ✅ F6: `SIGNAL_URA_TRANSIT_CONFIG_CHANGED` → local idempotent teardown+rebuild, no parent reload |
| C-H1..H4 | HIGH ×4 | **3 of 6 mutation drills stayed GREEN**: kill switch, checkpoint-area filter, and enumerator package/disabled exclusion were untested. The kill-switch test was a pure **tautology** (set arithmetic, never called production). The entire `transit_validator.py` diff was untested. | #62 / sham anchor | ✅ 10 real behavioral tests driving `_protect_sourced_checkpoint_entities` + `TransitValidator` |
| C adv#3 / A-MED-1 | MEDIUM | Empty-tuple `CONF_TRANSIT_CHECKPOINT_AREAS` fell through a falsy check → silently expanded to all 5 defaults (operator "none" became "everything"); bare-string input became a per-char tuple | config-boundary | ✅ F7: `is not None` + `_normalize_checkpoint_areas` |
| C-M1 | MEDIUM | `EgressDirectionTracker.async_init` extended `_interior_entities` non-idempotently (accumulates on re-init) | idempotency | ✅ F8: `dict.fromkeys` dedup |
| A-MED-3 | MEDIUM | Kill-switch OFF still created the diagnostic attr / walked registries | not-byte-identical | ✅ F9: early return before resolver construction (verified) |
| A-MED-2/4, B-M1..M3, A-LOW-1..3, C-L1/L2 | MED/LOW | Egress role assumption if checkpoint areas extended to doorways; per-init resolver rebuild cost; area_id-vs-area_name key mismatch in the diagnostic; by_area double-count; family typo silently disables | — | Deferred, documented (≤6) |

## Statistics

| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 1 | 1 | 0 |
| HIGH | 8 | 8 | 0 |
| MEDIUM | 6 | 4 | 2 |
| LOW | 5 | 0 | 5 |

## Mutation ledger

Builder ran 6 drills (all red on the correct test). **Orchestrator independently re-ran the two
most load-bearing sites** (do not trust reviewer/builder summaries):

| Site | Mutation | Result |
|---|---|---|
| F1 union (`transit_validator.py` `_get_shared_space_cameras`) | force `return legacy` | **RED** — `test_f1_get_shared_space_cameras_unions_legacy_and_protect_sourced` |
| F2 dedup (`_on_camera_state_change` window check) | `if False:` | **RED** — `test_f2_double_fire_one_physical_camera_records_one_sighting` |

Both restored; `git status` clean after each. `PYTHONDONTWRITEBYTECODE=1`, `__pycache__` cleared.

## Gate evidence

- Full suite: **21 failed / 8332 passed / 45 skipped**; failing-name diff vs develop baseline = **empty** (zero regressions). +10 new tests.
- Conflict-marker grep clean; `py_compile` clean on all touched files.

## Bug-class frequency + QUALITY_CONTEXT recommendation

- #53 computed-but-not-consumed — 1 (the CRITICAL; *subscribed-but-filtered* variant)
- Untested load-bearing site (drill-green) — 3
- Sham/tautological anchor — 1 (kill-switch test was set arithmetic)
- **Recurring pattern worth codifying: "new call site skips an existing guard."** A-H1 was the
  F1 NVR-multi-camera guard already present in `_scan_device_entities` but absent from the new
  `enumerate_platform_cameras`. Recommend a QUALITY_CONTEXT entry: *when adding a second call
  site to a shared primitive, enumerate the guards the first call site applies and justify each
  omission.*
- **Also recurring: a test that pins the wrong layer.** The drift test asserted the subscription
  set while the defect lived in the decision function — green test, broken feature.
