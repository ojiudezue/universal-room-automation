# v5.5.2 — heat_cool enforcer + attain reason-string — Consolidated Review Summary

**Cycle:** continuous heat_cool enforcer (live-found: upstairs zone drifted to `cool` with no restore path) + attain HOLD reason-string wording fix.
**Tier:** 2-DB (HVAC decision-cycle, regression-prone) — 3 framing-disjoint reviews + builder fix-up + orchestrator mutation-verify.
**Branch:** `feature/heatcool-enforcer-reason-fix` | **Baseline:** `pre-review-v5.5.2`
**Per-review docs:** `v5.5.2_heatcool_review{A,B,C}_*.md`

## Process ledger
- Build (ura-builder) → `13285cd2` (enforcer + reason string + tests). Builder ADDED an unrequested emergency-heat exemption with a 2h timer.
- 3 framing-disjoint reviews (A=enforcer + freeze safety, B=lifecycle/integration, C=test authority).
- Operator directive: drop the freeze special-casing entirely (works via presets/ranges; Safety not a trusted vector).
- Fix-up (ura-builder) → `5f2aec68` (removed all emergency-heat machinery; kept enforcer + egress/AC-reset skips; rewrote tautological tests to drive real code).
- Orchestrator mutation-verify: reverting the guard to `== "off"` → 2 test failures (proves non-tautological).

## Findings & disposition

| ID | Sev | Bug class | Finding | Disposition |
|---|---|---|---|---|
| A-CRIT-1 | CRITICAL | Unsafe blind timer / safety regression | The builder's `_emergency_heat_zones` 2h hold clobbers an ongoing freeze: the freeze hazard is edge-emitted (`safety.py:1698` `is_new`), never re-arms the hold within 2h → enforcer forces `heat`→`heat_cool` mid-freeze | **RESOLVED — entire emergency-heat machinery deleted** per operator (range-based philosophy; Safety not a trusted vector). Freeze-range consistency tracked as a follow-up (`PLANNING_freeze_safety_range_shift.md`). |
| A-HIGH-1 | HIGH | Fabricated spec | Builder comment + orchestrator framing claimed "≤35°F = CRITICAL" freeze; actual `NUMERIC_THRESHOLDS[FREEZE_RISK]` tops at HIGH (`safety.py:184-188`) | Noted; moot after deletion. (No-Fabrication lesson — even the review framing repeated it.) |
| B-HIGH-1 | HIGH | Restart resilience | `_emergency_heat_zones` RAM-only, lost on reboot → clobber window mid-freeze | **RESOLVED — dict removed.** |
| C-HIGH (C1-C3) | HIGH | Test fixture authority (C5) | Tests reimplemented the guard as a hand-rolled mirror; reverting the fix left all green | **RESOLVED — tests rewritten to drive real `_apply_house_state_presets`; mutation-verified (2 fail on guard revert).** |
| B-CRIT/feedback-loop | — | — | suppress() handshake clean; override detection keys on preset/setpoint not `hvac_mode` (belt+suspenders) | ✅ PASS (no change needed) |
| cross-writer | — | — | egress(`off`)/AC-reset(`off`) skips; D5 coast `away` preset orthogonal to mode | ✅ PASS |
| B-MED-1 | MED | Re-enforce loop | A thermostat that silently rejects `heat_cool` → enforcer re-issues `set_hvac_mode` every 5 min (log noise) | **DEFERRED** (harmless; tracked). Add a downgrade-counter/log later if observed. |
| A-MED-1 / C-LOW | LOW | — | `.pop` vs `del`; `dict[str,datetime]` type; test literal copy | Moot (emergency-heat code removed). |

Blocking findings at deploy: **0 CRITICAL / 0 HIGH** (all resolved by the simplification).

## What ships
- `hvac.py::_apply_house_state_presets`: restores `heat_cool` on ANY non-heat_cool mode for heat_cool-capable zones, keeping the pre-existing egress-`off` and AC-reset-`off` skips. suppress() handshake wraps the write.
- **Behavioral note (operator-accepted):** if the Safety Coordinator sets single-mode `heat` on a freeze, the enforcer reverts it to `heat_cool` next cycle (heat_cool heats via the low setpoint). Freeze-range consistency is the queued follow-up that makes the freeze response range-based so this is moot.
- `energy_battery.py:2031-2033`: attain HOLD reason wording — "holding at target X% (SOC now Y%)" instead of the contradictory "SOC Y% reached target X%".

## Framing-disjoint value note
The panel earned its cost: Review A caught a CRITICAL safety regression (and corrected a fabricated freeze-severity claim the builder AND the orchestrator's own framing repeated); Review C caught that every test was tautological (reverting the whole fix left them green) — a Bug Class C5 the passing suite hid. Neither would have surfaced from a single-framing review.
