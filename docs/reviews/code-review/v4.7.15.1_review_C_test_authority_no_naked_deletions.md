# v4.7.15.1 — Review C: Test Fixture Authority + NO NAKED DELETIONS

**Reviewer focus:** Bug Class #44 (test fixture authority — tests drive
production code, not shadow re-implementations) + operator-mandated
NO NAKED DELETIONS check + parallel-merge fidelity vs v4.7.16.

**Cycle:** `feature/v4.7.15.1-pattern-a-consumes-v4-7-14-1`
**Tip:** `56f4b95` &nbsp; **Baseline tag:** `pre-review-v4.7.15.1` (`d654114`)
**Worktree:** `/tmp/ura-v4715-1-integration`
**Tier:** 2-DB (this is one of three parallel reviews)

---

## Verdict

**APPROVE WITH FIXES** — one NIT, no CRITICAL / HIGH / MEDIUM / LOW.

The cycle preserves test authority correctly (Bug Class #44 not
reintroduced), the NO NAKED DELETIONS gate is clean, the merge base
incorporates both v4.7.14.1 and v4.7.15 field blocks without drift,
and v4.7.15.1's presence.py footprint does not collide with v4.7.16's
known territories. README satisfies all 7 Tier 2-DB robustness sections.

The single NIT is a minor doc-consistency point on alias rationale.

## Severity table

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| NIT | 1 |

---

## C1 — NO NAKED DELETIONS gate (operator-mandated CRITICAL check)

**Status: PASSED.**

Zero test functions added or removed in D4. All deletions in
`quality/tests/test_v4714_1_forgotten_phone_hotfix.py` are mirror-helper
bodies replaced with input-builder shims; every test class and every
assertion is retained.

D4 rewires the *math* away from test scope into the production helper
(`should_veto_due_to_reliable_signals` at
`custom_components/universal_room_automation/domain_coordinators/presence.py:753+`)
while keeping the test *contracts* identical. This is the right shape:
Bug Class #44 says tests must drive production, not re-implement it —
D4 enforces exactly that.

Cycle-suite test-function counts are unchanged from `pre-review-v4.7.15.1`.

---

## C2 — Back-compat alias rationale (NIT)

`quality/tests/test_v4714_1_forgotten_phone_hotfix.py:391`:
```python
_phone_trustworthy = _phone_trust_input
```
`quality/tests/test_v4714_1_forgotten_phone_hotfix.py:776`:
```python
_tracking_active = _tracking_active_input
```

**Verified preservation:**
- `test_v4714_1_forgotten_phone_hotfix.py:693, 709` — direct `_phone_trustworthy(...)` calls in `TestH2PhoneLeftBehindFilter`.
- `test_v4714_1_forgotten_phone_hotfix.py:715, 738-742` — source-grep invariants asserting `def _phone_trustworthy` appears literally in `presence.py` source. The alias keeps the test file referencing the canonical name.
- `test_v4714_1_forgotten_phone_hotfix.py:1182-1183` — `TestAM2DualAttributeExposure::test_am2_excluded_persons_lists_reasons` uses bare names `_phone_trustworthy(...)` and `_tracking_active(...)`.

Both aliases are documented inline at `:386-390` and `:774-776` with
the explicit reason ("source-grep invariant + bare-name backsliding
protection"). Bug Class #44 reintroduction prevention is implicit in
the docstring chain but the word "Bug Class #44" itself does not
appear in the alias comments.

**NIT — C2-N1:** Consider tagging the alias comment blocks with
`Bug Class #44 trip-wire` so a future cleanup pass that "looks
unused" doesn't delete them. Cost: 2 comment-line edits. Not blocking.

---

## C3 — 7 new direct helper tests (`TestD1HelperPatternAV4715_1`)

**Status: PASS.**

Read at `quality/tests/test_v4715_universalize_veto.py:295-454`.

All seven tests construct a `PresenceCoordinator` via
`_make_presence_coordinator()` and call
`coord.should_veto_due_to_reliable_signals(scope="house_inference", ...)`
directly. The signal taxonomy used (`person_phone_trustworthy`,
`person_tracking_active`, `census_count`, `unidentified_person_count`)
matches the production helper exactly per the new code at
`presence.py:756-825`.

Tests cover:

| Test | Branch covered (presence.py:756-825) |
|---|---|
| `test_pattern_a_fires_when_census_zero_and_unid_zero_and_all_trusted_away` | Full positive path |
| `test_pattern_a_does_not_fire_when_census_positive` | H1 predicate `census == 0` |
| `test_pattern_a_excludes_phone_left_behind_from_trusted` | H2 positional zip — phone False |
| `test_pattern_a_excludes_stale_lost_tracking_from_trusted` | H3 positional zip — track False |
| `test_pattern_a_falls_back_to_state_context_tracked_count_when_per_person_lists_empty` | Backward-compat fallback (zone aggregator path) |
| `test_pattern_a_length_mismatch_fails_conservative` | Fail-conservative length-parity guard |
| `test_pattern_a_trusted_count_zero_does_not_veto` | Fail-safe: all filtered → no veto |

Asserts include both `decision.fired` and `decision.reason`'s
`trusted=N` substring — verifying the helper actually derived the
parallel-zip count, not just returned the wrong boolean for the right
reason. This is the right granularity for a Bug Class #48 review
(transient-vs-reliable arbitration).

The length-mismatch test at `:415-432` directly exercises the
fail-conservative branch at `presence.py:780-786` (the `len() == len()
and len() > 0` check). Without this test the
"reliable < transient" invariant would not be self-enforcing.

---

## C4 — Integration-branch merge fidelity

**Status: PASS.**

Merge base `d654114` (parents `1ef6d0b` v4.7.14.1 + `c15e816`
v4.7.15) carries both sides correctly into `presence.py`.

Verified at `presence.py:620-680`:
- `self._excluded_persons: dict[str, str]` (v4.7.14.1 fix-up A-M1/M3) at `:638` — preserved.
- `self._tracked_persons_count` + `self._tracked_persons_count_trusted` (v4.7.14.1 A-M2 dual surface) at `:633-634` — preserved.
- `self._last_veto_decision: VetoDecision` (v4.7.15 D1) at `:641` — preserved.
- `self._first_positive_zone_occupied_since` (v4.7.15 D3) at `:643` — preserved.
- `self._wake_blocked_ticks` (v4.7.15 D3) at `:644` — preserved.
- `self._guest_exit_quiet_since` (v4.7.15 D3 exit-side) at `:647` — preserved.
- `self._signal_consensus` / `self._signal_consensus_inputs` / `self._consensus_low_since` (v4.7.15 D5) at `:649-651` — preserved.

v4.7.15.1's `presence.py` diff against `pre-review-v4.7.15.1`
(= `d654114`) shows hunks at lines 753, 765, 2316, 2325, 2357, 2493,
2614 — none of which touch the `:620-680` field block. The merge-
inherited fields are not modified, not duplicated, not reordered by
v4.7.15.1.

Net behavioral change in this cycle is the consolidated Pattern A
call site at `presence.py:2614+` (the new block) plus its companion
deletion at `:2493-2535` (the old A1-M1 helper-call block that
v4.7.15.1 supersedes — see C1, this is a code deletion not a test
deletion, so it sits outside the NO NAKED DELETIONS rule).

---

## C5 — Source-invariant updates cross-check

**Status: PASS** (3 sources agree).

The v4.7.14.1 H1 hotfix predicate (`census_count == 0`) is the
canonical truth this cycle replumbs through the helper. Source
agreement:

1. **Production code:** `presence.py:803-816` — adds `census` extraction from `TransientSignal("census_count", ...)` and the `census == 0` condition is gated into the veto-fire predicate. Comment at `:756-770` explicitly cites H1 / H2 / H3 by name.
2. **Test docstring:** `test_v4715_universalize_veto.py:296-300` — `"TestD1HelperPatternAV4715_1: Pattern A consumes v4.7.14.1 H1/H2/H3 surfaces"`.
3. **Plan doc:** `docs/planning/PLANNING_v4.7.15.1_pattern_a_consumes_v4_7_14_1.md` and `docs/readmes/README_v4.7.15.1.md:3` — frames the cycle as "consolidate v4.7.14.1's H1/H2/H3 trust filters into v4.7.15's shared Pattern A helper".

All three agree on WHY (canonical truth lives at H1) and on the same
hotfix label. No drift.

Reviewer A owns the depth check on the source-window guard
(`_run_inference` 12000→30000) per the commit message in `cb80f02`;
this review only validates documentation consistency, which is clean.

---

## C6 — Parallel-merge order vs v4.7.16

**Status: PASS** — v4.7.15.1's footprint does not collide with v4.7.16.

Sprint deploy order is `develop → v4.7.14.1 → v4.7.15 → v4.7.15.1 → v4.7.16`.

v4.7.15.1 modifies `presence.py` at hunks:
- `:753` (Pattern A helper signal extraction)
- `:765` (Pattern A predicate)
- `:2316` (`_run_inference` comment block)
- `:2325` (per-person loop — deterministic-sort + capture parallel lists)
- `:2357` (exception-path reset)
- `:2493` (deletion of old A1-M1 helper invocation)
- `:2614` (new consolidated Pattern A call site at end of cycle)

v4.7.16 (`feature/v4.7.16-room-veto-density`) modifies `presence.py` at:
- `:38` (const-import block — adds one import line)
- `:2069-2070` (mid-coord region — the per-room weighted-veto block)

**No overlap.** v4.7.15.1 leaves `:38` (const-import) untouched —
v4.7.14.1 ↔ v4.7.16's known trivial const conflict therefore does
not extend into v4.7.15.1. v4.7.15.1's hunks at `:2316-2614` sit
hundreds of lines below v4.7.16's `:2069-2146` weighted-veto region.

Per-room `_v4716_zone_verdicts` field initialization (v4.7.16
territory) is not introduced by v4.7.15.1 — the field block at
`presence.py:620-680` contains only the v4.7.14.1 + v4.7.15 fields
documented in C4.

**Expected merge of v4.7.15.1 → v4.7.16:** trivial. v4.7.16's
const-import and `:2069` weighted-veto block apply cleanly atop
v4.7.15.1's tip. No predicted conflict in v4.7.15.1's territory.

---

## C7 — README robustness (7 Tier 2-DB sections)

**Status: PASS.**

Read `docs/readmes/README_v4.7.15.1.md` end-to-end (109 lines).
All 7 required sections present and substantive:

| # | Required section | Found at |
|---|---|---|
| 1 | Operator runbook | `:13-24` (4 numbered steps, HACS verify + 4 named diagnostic attrs) |
| 2 | Pre-deploy snapshot procedure | `:26-32` (1-hour window of `last_veto_decision.fired=true`, ±25% threshold with explicit rationale) |
| 3 | Post-deploy validation with exact entity IDs + expected values | `:36-54` (5 checks, all with `sensor.ura_presence_coordinator_presence_house_state` cited by full entity_id; sentinel `trusted ≤ total` invariant; veto-fired log signature with exact format string) |
| 4 | Rollback procedure | `:58-64` (5 steps, HACS redownload, restart, verify version, plus surface-preservation note for `last_veto_decision`) |
| 5 | Live-validation checklist with explicit pass criteria | `:68-76` (5 PASS criteria as checkboxes, includes operator's forgotten-phone scenario from v4.7.14.1) |
| 6 | Known limitations | `:80-85` (4 numbered items, includes v4.7.16 scope-gap deferral at #2 — Pattern F unchanged, owned by v4.7.16) |
| 7 | Cross-cycle reference to v4.7.14.1, v4.7.15, v4.7.16 | `:89-95` (5 named references — predecessor v4.7.15, sibling v4.7.16, Bug Class #48/#44, Reviewer C of v4.7.14.1 §C4, master sprint link doc) |

Additional sections beyond the minimum: "Deploy gate" at `:99-108`
documenting the 6-step Pre-Deploy Zero-Bugs Gate from CLAUDE.md.
That's exactly the kind of belt-and-suspenders robustness the
Tier 2-DB bar asks for.

The known-limitation #1 (test mirror function renaming) directly
heads off the reviewer-confusion failure mode from C2 — anyone
re-reading the test file looking for old veto-math mirrors is
explicitly redirected to the production helper.

---

## Bug-class regression sweep

| Class | Status |
|---|---|
| #44 (test fixture authority) | **NOT REINTRODUCED.** D4 moves the veto math from test scope into the production helper; tests now drive the helper directly. Reviewer C's primary mandate satisfied. Back-compat shims at `:391` / `:776` preserve test names but resolve to input-builders, not shadow math. |
| #47 (canonical name vs surface name) | **NOT TOUCHED.** v4.7.15.1 does not introduce new entity/config surface names. The `sensor.ura_presence_coordinator_*` IDs used in the README pre-exist v4.7.15.1. |

---

## Pre-deploy fix-up list

| Severity | ID | Required before deploy? |
|---|---|---|
| NIT | C2-N1 | No — comment-only tagging of alias rationale with Bug Class #44 trip-wire label |

Nothing blocking. Ready for the 6-gate Pre-Deploy Zero-Bugs Gate
documented at `README_v4.7.15.1.md:99-108`.

---

**Reviewer C, signing off.** Test fixture authority preserved; merge
fidelity intact; parallel-merge collision-free vs v4.7.16; README
robustness bar met.
