# Code Review — v5.8.0 Reconcile-on-Return (Tier 3)

**Cycle:** Reconcile-on-Return (D2 of offline-actuator visibility + recovery)
**Plan:** `docs/planning/PLANNING_reconcile_on_return.md`
**Branch:** `feature/reconcile-on-return-v5.8.0`
**Baseline tag:** `pre-review-v5.8.0`
**Review tier:** Tier 3 — four framing-disjoint reviews (A/B/C/D) + orchestrator independent verification + operator checkpoint.
**Date:** 2026-07-05

## Reviewers & framings
- **A — local correctness / edge cases** (resolver branch-table parity, timer arithmetic, flap-window math)
- **B — integration / state-machine / async-lifecycle** (listener lifecycle, rebuild-hook re-arm, quarantine state machine, restart)
- **C — test authority via real per-site source mutation + new-surface round-trip**
- **D — adversarial completeness / diff-blind** (falsify the §2.1 invariant across the whole surface incl. pre-existing code)

All four returned **FIX-FIRST** on the first pass.

## Findings

| ID | Sev | Bug class | Finding | Status |
|---|---|---|---|---|
| A-CRIT-1 | CRITICAL | #53 parity | Resolver reconciled `alert_lights`, but canonical entry/exit handlers never control alert lights → drove an unowned surface | FIXED — removed `CONF_ALERT_LIGHTS` from `_LIGHT_KEYS` |
| A-CRIT-2 | CRITICAL | #53 parity | Resolver reconciled `humidity_fans` with temperature logic; humidity fans are driven solely by `handle_humidity_based_fan_control` (spike/baseline state machine) | FIXED — removed `CONF_HUMIDITY_FANS` from `_FAN_KEYS` + belt-and-braces `None` guard in `_resolve_fan` |
| A-HIGH-1 | HIGH | #53 parity | Vacant exit-off returned `off` for any tracked light; canonical exit turns off `CONF_LIGHTS` only (night-only entities untouched) | FIXED — vacant-off gated on `entity_id in CONF_LIGHTS` |
| A-HIGH-2 | HIGH | #53 parity | `_resolve_fan` dropped canonical fan sleep-policy (`FAN_SLEEP_OFF/REDUCE`) + vacancy-hold | FIXED — `_resolve_fan` returns `None` (defer to organic) under sleep-mode or vacant |
| B-HIGH-1 | HIGH | #50 | D2.9 re-arm was in `async_config_entry_first_refresh`, not the `_update_signal_subscriptions` rebuild hook; only accidentally covered by full-reload today; a future in-place ROOM rebuild would orphan the listener | FIXED — re-arm moved to top of `_update_signal_subscriptions`; `async_register_listeners` also cancels stale coalesce/grace timers + clears pending set |
| D-HIGH | HIGH | #53 (missed-path) | Post-boot grace window armed only from inside `_async_update_data` after boot-settle already True → on the **reload path** (`hass.is_running==True`) grace never armed and a mid-flight `unavailable→available` dispatched `light.turn_on` with no grace. D2.7 secondary mitigation inert on every reload | FIXED — added implicit construction-age grace (`_in_post_boot_grace` / `_created_monotonic`) covering the reload path |
| A-MED-1 | MEDIUM | dead guard | Debounce edge stamped only on successful reconcile → guard measured from last dispatch, not last available-edge (§2 clause 4) | FIXED — edge stamped at accept-time in `_consider_reconcile` |
| A-MED-3 | MEDIUM | clock-jump | Interval math on wall-clock `utcnow()`; backward clock step defeats debounce | FIXED — `_now()` → `time.monotonic()` for all interval math |
| B-MED-2 | MEDIUM | async race | Coalesce could double-fire; pending set cleared too late | FIXED — `_pending_reconcile` cleared synchronously in `_fire`, snapshot passed to the task |
| B-MED-3 | MEDIUM | #38 | Re-arm on same instance left stale coalesce/grace timer | FIXED — covered by B-HIGH-1 fix (timers cancelled on re-arm) |
| C-GAP-1 | MEDIUM | #52 / test authority | `test_auto_recovery_switch_unavailable_last_state_falls_back_to_on` was tautological (tested a local closure, not production `AutoRecoverySwitch.async_added_to_hass`) — mutating the real guard passed all tests | FIXED — rewritten to drive the real switch; mutation-verified |
| C-GAP-2 | MEDIUM | test authority | `reconcile_advanced` flatten save-path untested — dropping the flatten passed all config-flow tests | FIXED — new test submits the section and asserts `flap_sensitivity` flattens |
| D-MED | MEDIUM | robustness | Dead `_maybe_release_quarantine` no-op; quarantine release only via a poll whose try/except swallows errors → degraded poll strands a quarantined device | FIXED — dead method deleted; poll guards grace-arm and quarantine-release in separate try/excepts |
| B-LOW-5 | LOW | #47 | Auto-Recovery switch read is slug-coupled, fails open (ON) on lookup miss | ACCEPTED — consistent with standard URA switch-read pattern; fail-open-ON is intended |
| D-clause5 | LOW | UX | `AutoRecoverySwitch.entity_registry_enabled_default=False` → the documented dry-run lever requires enabling a hidden entity | NOTED — surface for operator; not a dispatch leak |

### Orchestrator-caught (independent verification)
| OV-1 | MEDIUM | test authority | The D2.9 wiring test (`test_rearm_is_wired_into_update_signal_subscriptions_not_first_refresh`) was a bare source-grep — a semantic neuter (`if False and …`) left the string present and passed. Caught by orchestrator mutation pass. | FIXED — test now pins the exact guard snippet; mutation-verified it fails on the neuter |

## Summary statistics
| Severity | Found | Fixed | Accepted/Noted |
|---|---|---|---|
| CRITICAL | 2 | 2 | 0 |
| HIGH | 3 | 3 | 0 |
| MEDIUM | 7 (6 review + 1 OV) | 7 | 0 |
| LOW | 2 | 0 | 2 |

## Bug-class frequency
- **#53 computed-but-not-consumed / parity** — 5 (A-CRIT-1, A-CRIT-2, A-HIGH-1, A-HIGH-2, D-HIGH). Dominant class again; confirms the plan's own D2.10 self-risk framing. The D2.10 parity test was *itself* incomplete (didn't exercise alert_lights / humidity_fans / night-only-exit / sleep-fan cells) — strengthened in fix-up with gating `None` assertions.
- **#50 subscription-rebuild orphan** — 1 (B-HIGH-1).
- **#52 RestoreEntity unavailable→OFF** — 1 (C-GAP-1, test authority).
- **#38 listener/timer cleanup** — 1 (B-MED-3).
- **Test authority (source-grep / tautology)** — 3 (C-GAP-1, C-GAP-2, OV-1).

## Independent verification (Tier 3 mandatory)
Orchestrator personally:
- Re-grepped all 5 fixed load-bearing sites — confirmed in place.
- Ran the full-suite baseline diff vs a clean `develop` worktree — **zero regressions unique to the branch**.
- Mutation-tested 3 load-bearing sites: grace-on-reload guard (test fails on neuter ✓), humidity-fan parity gate (test fails on scope regression ✓), D2.9 re-arm wiring (surfaced OV-1, then fixed + re-verified ✓).
- Re-ran **Reviewer D completeness enumeration after the grace fix** → **SHIP** (no N+1 arming site; grace has exactly two well-scoped sources; D2.9 re-arm correctly leaves grace untouched; all interval math monotonic; quarantine release single-sourced).

## QUALITY_CONTEXT.md recommendation
No new bug class required — all findings map to existing classes (#53, #50, #52, #38). Reinforces #53 as the dominant recurring class and the value of the Tier-3 adversarial-completeness pass (D-HIGH was reachable only on the reload path, missed by A/B/C and the build).

## Outcome
After fix-up + independent verification + Reviewer-D re-enumeration: **cleared to operator checkpoint.** Deploy pending operator go (Tier 3 requirement).
