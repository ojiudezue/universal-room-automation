# v5.5.3 — Arbitrage-WAIT inclement floor — Consolidated Review Summary (Tier 3)

**Cycle:** thread the inclement `partial_hold` reserve floor through the off_peak arbitrage + attain emission sites.
**Tier:** 3 (first cycle under the new Tier-3 protocol — FOUR framing-disjoint reviews).
**Branch:** `feature/arbitrage-wait-floor` | **Baseline:** `pre-review-v5.5.3`
**Invariant (falsifiable):** *under an inclement `partial_hold`, the battery's reserve_level is ≥ effective_reserve in every reachable off_peak/mid_peak path.*

## Process ledger
- Build (ura-builder) → `df57ab1e`: `_floor_reserve` helper + 6 sites + 6×mutation matrix.
- 4 framing-disjoint reviews (A clamp / B state-machine / C test-authority / D completeness).
- Fix-up (ura-builder) → `ed1a126e`: D-HIGH-1 (7th site) clamped + regression test.
- Orchestrator independent verify: re-enumerated all 16 emission lines; real source mutation on the new clamp → `2 failed` (test-covered).

## Findings & disposition

| ID | Sev | Reviewer | Finding | Disposition |
|---|---|---|---|---|
| D-HIGH-1 | HIGH | D (completeness) | 7th unclamped `reserve_level` site — summer mid_peak peak-ahead hold (`energy_battery.py:2921`, `hold_reserve = int(soc)`). Latent v5.5.0 gap; partial_hold floor leaks (repro `target=30, floor=60, soc=45 → reserve 45`). Bug Class #53. | **FIXED** — routed through `_floor_reserve`; mutation-anchored regression test; orchestrator-verified. |
| (A) | — | A | `_floor_reserve` max()-only, all sites route through it, byte-identical gating | ✅ SHIP |
| (B) | — | B | charge never suppressed (phase transition reads `peak_buffer_target`); no C2-CRIT-1; HOLD pins; reboot only raises | ✅ SHIP |
| (C) | — | C | real per-site source mutation: all 6 original sites individually test-covered; no tautology; `_result` |Δ|<2 harness accommodation sound | ✅ APPROVE |

Blocking at deploy: **0** after the D-HIGH-1 fix. Full re-enumeration (16 emission lines) confirms no 8th leak.

## Why Tier 3 mattered here
Reviewers A/B/C — the standard Tier-2-DB panel — all returned SHIP/APPROVE. The change *looked* complete. The dedicated **4th adversarial-completeness pass** (diff-blind, whole-invariant-surface, legal-config repro requirement) found the one reachable path the other three missed — and it was a *pre-existing* gap, not in the diff. This is the cycle that coined the Tier 3 protocol (CLAUDE.md). Lessons codified:
- 4th reviewer = adversarial completeness with a falsifiable invariant + reachable repro, reviewing pre-existing code too.
- Test authority via REAL per-site source mutation (not aggregate monkeypatch).
- Config-boundary testing (independent sliders → `floor > target` is legal and broke the naive assumption).
- Orchestrator independent verification before ship.
