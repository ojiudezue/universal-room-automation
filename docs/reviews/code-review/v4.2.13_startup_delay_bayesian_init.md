# Code Review: v4.2.13 Startup Catch-Up Delay + Bayesian Init Hardening

**Date:** 2026-04-28
**Reviewer:** 1x staff-engineer adversarial (Tier 1 hotfix)
**Scope:** `__init__.py` only — 3 changes (catch-up delay x2, Bayesian init)

## Findings

| # | Severity | File:Line | Description | Fixed |
|---|----------|-----------|-------------|-------|
| 1 | MEDIUM | __init__.py:729,829 | `async def` passed to `async_call_later` — pre-existing since v4.2.8, not introduced by this diff. Known bug class. | Defer |
| 2 | LOW | __init__.py:749,849 | 30-min delay means catch-up skipped on frequent restarts — acceptable, nightly is primary | Accept |
| 3 | LOW | __init__.py:1025-1036 | Empty beliefs after init failure — all downstream methods verified safe | Accept |
| 4 | LOW | __init__.py:1025-1036 | Button available but non-functional if DB is None — safe no-op | Accept |
| 5 | LOW | __init__.py:1038-1049 | Transition listener registered after init completes — no race | Accept |
| 6 | LOW | __init__.py:2032-2034 | Unload path correct — `.pop()` + cancel timer | Accept |
| 7 | LOW | __init__.py:1066 | `save_beliefs()` on unload after failed init — returns early, harmless | Accept |

## Key Verifications

- All Bayesian methods handle empty beliefs: `predict_room()` → None, `save_beliefs()` → returns early if no rows/no DB, `clear_and_reinitialize()` → returns early if no DB, `get_accuracy_stats()` → returns `{brier_score: None}`
- Unload path `unsub_startup_catchup` uses safe `.pop()` with default
- No new DB reads, writes, or cleanup methods introduced

## Statistics

| Severity | Found | Fixed |
|----------|-------|-------|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 (pre-existing) | Deferred |
| LOW | 6 | Accepted |
