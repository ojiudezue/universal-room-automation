# v5.73.2 — FAN-LAYER-2 B-LOW-1 + DP-REASON-NULL-1 (Tier 1 batch) Review Record

## FAN-LAYER-2 B-LOW-1 (orphan fan-oracle sweep)
One adversarial review: **SHIP**, 0 CRIT/HIGH.

| ID | Sev | Finding | Bug class | Disposition |
|---|---|---|---|---|
| R-LOW-1 | LOW | Lock split-brain if a room's lock is popped while a coroutine holds the old reference | Lock-map coherence | ACCEPTED — only reachable for a room being deleted mid-hold; blast radius one hold window on a room being removed |
| R-INFO-1 | INFO | INFO sweep log dumps full orphan list on one line (rename-storm verbosity) | — | Accepted |
| R-INFO-2 | INFO | Wire-in test uses `__new__`-bypass (FAKE-coordinator shape, v5.8.0 lineage) | Test-shape caution | Accepted — sweep lives in discover_fans, not construction; neuter-drill red is the load-bearing property |

Key dismissal (with trace, not trust): the partial-`entry_to_room` false-sweep risk is unreachable —
HA loads the config-entry registry before any component setup; `discover_fans`' only caller is HVAC
coordinator setup; options-flow edits mutate in place (room_key stable). Finding-vs-reality: store is
RAM-only (orphans session-scoped, not persisted) — sweep retained for snapshot honesty + bounded locks.

## DP-REASON-NULL-1 (ledger reason)
Orchestrator-verified one-liner (diff read directly; fix exactly matches the AUDIT_dp_live_behavior.md
prescription). Wire-in drill: reverting to the dead `getattr` reds
`test_dp_reason_null_1_post_eval_row_has_non_null_reason`; restored green.

## Statistics
| Severity | Found | Fixed | Accepted/Deferred |
|---|---|---|---|
| CRITICAL/HIGH/MEDIUM | 0 | — | — |
| LOW | 1 | 0 | 1 (documented blast radius) |
| INFO | 2 | 0 | 2 |

Suite: 23 pre-existing failures unchanged across both branches (byte-identical), +8 new passes.

## Bug-class notes
- Original B-LOW-1 finding said "persisted" store — actual store is RAM-only. Reviewer-finding
  descriptions are hypotheses too; builder's finding-vs-reality note prevented over-building.
- `getattr(obj, field, None)` against a field that never existed = silent-null telemetry (cousin of
  computed-but-not-consumed #53). Worth a grep pass for other `getattr(..., None)` ledger writes in a
  future hygiene sweep.
