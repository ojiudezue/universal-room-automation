# v5.51.1 NM Echo-Loop Guard — Review Record

Two parallel framing-disjoint reviews (autonomous Tier-1 protocol),
uncommitted diff on develop, 2026-08-05.

## Findings

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| A-H1 | HIGH | Buffer len 20 undersized under per-recipient fan-out + digests; echo could return post-eviction | #7 stale data source / #22 silent no-op | YES — len 100 + dedup-on-append (identical bodies refresh one slot) |
| A-H2 | HIGH | `safe_word_unauthorized` deny reply swallowed by rail 2 → repeat unauthorized attempt indistinguishable from success | security UX | YES — `NM_REPLY_RATE_LIMIT_EXEMPT_COMMANDS` |
| B-B1 | HIGH | Rail 2 gated companion/pushover which cannot self-echo → interactive ack UX regression | #53-adjacent (guard at wrong surface) | YES — `NM_REPLY_RATE_LIMITED_CHANNELS=("imessage","whatsapp")` |
| A-M1 | MED | Echo drops invisible in inbound totals | telemetry | YES — counts + `echo` pseudo-command |
| B-B2 | MED | `_record_outbound_text` before service call → failed send seeds buffer | proof-of-send | YES — moved after successful dispatch (both senders) |
| A-M2 | MED | Ack-complete-before-gate invariant only implicit | docs | YES — pinned in rail-2 comment |
| A-M3 | MED | Ordering test didn't pin echo < silence branch (the actual pathology) | test authority #62 | YES — added assert |
| A-M4 | MED | Sender anchor didn't pin record-on-success/same-variable | test authority #62 | YES — ordering + variable asserts |
| A-M5 | MED | No anchor that `_send_reply` routes through recording senders | test authority #62 | YES — anchor test |
| B-B3 | MED | Restart residual: pre-restart echo processes once post-boot | #7 restore path | ADJUDICATED — accepted residual, documented in README (marginal-benefit: boot-settle gate declined) |
| A-L1/L2/L3, B-B4 | LOW | Exact-match false-positive risk (defensible), BB payload-mutation assumption, dry-run placement (clean), kill-switch asymmetry (dissolved by B-B1 fix) | — | No action / dissolved |

## Statistics

| Sev | Found | Fixed | Adjudicated/Deferred |
|---|---|---|---|
| CRITICAL | 0 | — | — |
| HIGH | 3 | 3 | 0 |
| MEDIUM | 8 | 7 | 1 (B-B3 accepted residual) |
| LOW | 4 | 0 | 4 (no-action by review) |

## Mutation verification (orchestrator, post-fix-up)

| Mutation | Failing test |
|---|---|
| `_is_self_echo` → always False | test_echo_exact_match_dropped (+2) |
| rate-limit condition → False | test_rate_limit_behavior |
| rail-2 scope+exempt → True (gate everything) | test_rate_limit_behavior |
| dedup-on-append neutered | test_ring_buffer_dedup_on_append |

## Bug-class notes

Recurrence of #62 (zero-coverage-shaped findings A-M3/M4/M5) caught by
reviewer, fixed with anchors in-cycle. No new bug class proposed: the
echo loop itself is an instance of trusting an upstream direction flag
(BB `isFromMe`) — closest to #7 (stale/unreliable data source); the fix
deliberately does NOT depend on the flag.
