# v4.6.9 Code Review — Boot-State Robustness

**Tier:** 1 (single staff-engineer adversarial review)
**Date:** 2026-05-18
**Pre-review baseline tag:** `pre-review-v4.6.9`
**Reviewer:** general-purpose adversarial agent (Claude)
**Build:** ura-builder (third dispatch — two prior agents flagged spec issues which were verified + corrected in-flight)
**Branch:** `feature/v4.6.9-boot-state-robustness`

## Scope

Per `docs/planning/PLANNING_v4.6.9_boot_state_robustness.md`:
- D1: RestoreEntity on `PersonPreviousLocationSensor` + `PersonPreviousSeenSensor` + new idempotent `PersonTrackingCoordinator.seed_previous_location*` methods
- D2: Coordinator-ready dispatcher signal subscriptions on 4 CM-device buttons + 2 new signals (`SIGNAL_NM_READY`, `SIGNAL_BAYESIAN_READY`) + dispatch sites
- D2 expanded (during build): one-line fix at `__init__.py:1978` registering `hass.data[DOMAIN]["notification_manager"] = nm` — closed a pre-existing latent bug that made the NM button permanently unavailable AND 3 NM service handlers always log warnings
- D3: BACKLOG + planning doc updates

## Findings — verdicts

| # | Severity | Issue | Verdict | Fix |
|---|---|---|---|---|
| HIGH #1 | HIGH | `_SKIP_STATES` sentinel set missing `"not_home"` / `"home"` — HA person entities can persist these states, would be stored as restored "Not Home" / "Home" room names visible in UI | **FIXED** — added to sentinel sets in both sensors + the inlined test mirror | review-fix commit |
| MEDIUM #1 | MEDIUM | `seed_previous_location_time` doesn't coerce tz-naive datetime to UTC — downstream subtraction would raise `TypeError` if a restored timestamp lacks tzinfo (Bug Class #21) | **FIXED** — added `if time.tzinfo is None: time = dt_util.as_utc(time)` | review-fix commit |
| MEDIUM #2 | MEDIUM | `TestSeedPreviousLocation*` use inlined seed-method bodies (HA-import env limitation) — drift risk if production seed methods change. **Bug class: proposed "Test inlines production logic (drift risk)"** | **DEFERRED** to v4.6.10 — extract seed logic to `person_seed_helpers.py` leaf module, have tests import the helper directly. Filed in BACKLOG.md. | — |
| MEDIUM #3 | MEDIUM | NM latent-bug fix is a behavior-change surface — 3 service handlers that previously logged "NM not available" now execute against a real NM that's never been exercised via that path | **ACCEPTED** — post-deploy live validation step invokes each NM service once to confirm no traceback | live-val checklist |
| LOW #1 | LOW | `_SKIP_STATES` is a local var in both sensors — promote to module-level constant | **DEFERRED** to v4.6.10 — cosmetic | — |
| LOW #2 | LOW | Comment typo `self._data` should be `self.data` (`person_coordinator.py:1011-1012`) | **DEFERRED** to v4.6.10 — cosmetic | — |
| LOW #3 | LOW | All `async_dispatcher_connect` calls properly wrapped in `async_on_remove` — clean | **PASS** | — |
| LOW #4 | LOW | Function-local signal imports — match `__init__.py:763-764` precedent (Bug Class #34 justified) | **PASS** | — |
| LOW #5 | LOW | MRO check `class Foo(AggregationEntity, SensorEntity, RestoreEntity)` — `super().async_added_to_hass()` resolves correctly through MRO | **PASS** | — |
| LOW #6 | LOW | Concurrent reload race (Bug Class #10) — each sensor restores into distinct `_data[person_name]` key; HA event loop serializes; no race | **PASS** | — |
| Bug Class #21 | — | Tz-naive datetime audit: all new code uses `dt_util.now()` / `dt_util.as_utc()` / `dt_util.parse_datetime()` (after MEDIUM #1 fix) | **CLEAN** | — |
| Bug Class #19 | — | No new background tasks | **N/A** | — |

## Statistics

| | Count |
|---|---|
| Bugs found | 8 |
| CRITICAL | 0 |
| HIGH | 1 |
| MEDIUM | 3 |
| LOW | 4 |
| Fixed this cycle | 2 (HIGH #1, MEDIUM #1) |
| Deferred to v4.6.10 | 3 (MEDIUM #2, LOW #1, LOW #2) |
| Accepted as deploy-checklist | 1 (MEDIUM #3) |
| Pass without action | 2 (LOW #3 dispatcher cleanup verified, LOW #4-6 pattern checks) |

## Bug class frequency

| Bug class | Count | Notes |
|---|---|---|
| #22 (Enum/sentinel vocabulary mismatch) | 1 (HIGH #1) | HA person-entity state sentinels missed in URA sentinel set |
| #21 (tz-naive datetime) | 1 (MEDIUM #1) | Fixed via `as_utc` coercion |
| New: "Test inlines production logic (drift risk)" | 1 (MEDIUM #2) | **Proposed for QUALITY_CONTEXT.md** — when test env can't import the production module, tests re-implementing the logic create a drift risk. Mitigation: extract logic to a leaf module without the heavy imports. |
| New: "Latent registration gap with widespread silent failure" | 1 (NM bonus fix) | **Proposed for QUALITY_CONTEXT.md** — when a `hass.data[DOMAIN]["X"]` slot is read by multiple sites but the corresponding write site was lost in a refactor, each read returns `None` and degrades silently. Detection: any `hass.data[DOMAIN].get("X")` read should grep-verify the matching write site exists. |

## Tests

- `quality/tests/test_v4_6_9_boot_state_robustness.py` — 37 tests (33 from initial build + 2 sentinel-coverage + 2 tz-coercion from review fixes), all pass
- Baseline failure comparison: 56 failed / 14 errors at `pre-review-v4.6.9` baseline; same count post-fix — **zero regressions introduced**

## Live validation criteria (post-deploy)

Per planning doc acceptance criteria + the MEDIUM #3 NM latent-bug-exposure check:

- [ ] `sensor.universal_room_automation_ezinne_previous_location` is NOT "Unknown" after HA restart (was BROKEN pre-v4.6.9; should reflect last-seen room when away across restart)
- [ ] `sensor.universal_room_automation_ziri_previous_location` same
- [ ] `button.ura_coordinator_manager_clear_bayesian_beliefs.state` is NOT "unavailable" within ~30s of URA setup post-restart
- [ ] NM Acknowledge button enables within ~30s
- [ ] Acknowledge Routine Changes button enables within ~30s
- [ ] Anomaly Diagnostic Dump button enables within ~30s
- [ ] **NEW (MEDIUM #3):** invoke `service.universal_room_automation.acknowledge_notification` once — verify no traceback (NM service was a silent no-op pre-v4.6.9)
- [ ] No HA log warnings tied to URA setup ordering
- [ ] No new failures in the 24h soak window

## Verdict

**PASS WITH FIXES** — HIGH #1 and MEDIUM #1 both addressed in the review-fix commit (same branch). All other findings either deferred to v4.6.10 with explicit BACKLOG.md entry, or accepted as a post-deploy live-validation step (MEDIUM #3).

## Recommendations for QUALITY_CONTEXT.md

Two new bug classes proposed (see Bug class frequency):
1. **"Test inlines production logic (drift risk)"** — when test env can't import the production module
2. **"Latent registration gap with widespread silent failure"** — `hass.data[DOMAIN]["X"]` read sites without matching writes

Add to `docs/QUALITY_CONTEXT.md` on next touch.
