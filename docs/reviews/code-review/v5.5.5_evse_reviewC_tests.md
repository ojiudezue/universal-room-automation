# v5.5.5 EVSE off-peak fill-release — Reviewer C (Test Authority + day/night mutation coverage + new const)

**Branch:** `feature/evse-offpeak-fill-release` @ `187e1c59`
**Framing:** Test authority, D1/D2 mutation anchoring, cross-midnight + cloudy-daytime reality, the new constant.
**Method:** Inspected via `git show`; executed against a non-destructive detached worktree (`.claude/worktrees/reviewC-evse`, removed after). Ran the real mutations to prove anchors fail on revert.

## VERDICT: PASS — ship.

The D1 and D2 mutation anchors are **genuinely anchored** (proven by executing the reverts, not by reading), and the cross-midnight + cloudy-daytime tests are **real** (they drive the real `TOURateEngine.peak_ahead_before_offpeak` / `get_current_period` and the real `EVChargerController` methods). No new full-suite failures. No CRITICAL/HIGH findings.

## Lead findings (the asks)

1. **Tests drive REAL methods — confirmed.** `test_evse_offpeak_fill_release.py` imports and calls `EVChargerController.determine_fill_priority_actions` / `determine_battery_drain_actions` (energy_pool.py:1092, :875) and the real `TOURateEngine` (no hand-rolled phase mirror). `_peak_ahead()` calls `_TOU.peak_ahead_before_offpeak` directly. No v5.5.2-style tautology. **PASS.**

2. **D1 mutation anchored — PROVEN.** Reverting `tou_period in ("peak", "off_peak")` → `("peak",)` at energy_pool.py:1163 makes `TestD1MutationOffPeakRelease::test_held_evse_released_on_off_peak_tick` FAIL, plus `test_off_peak_releases_never_holds[51.0]` and **all 7** `TestD1CrossMidnight::test_no_midnight_relock` cases (9 failed). The anchor is real, not decorative. Full D1 matrix {peak, mid_peak±peak_ahead, off_peak, legacy-None} × {soc 51, 85} present (lines 173–274). **PASS.**

3. **D2 solar-gate anchored — PROVEN.** Reverting the `solar_replenishing and …` gate at energy_pool.py:1042 makes `TestD2MutationSolarGate::test_night_85_must_not_release` AND `TestD2DrainSolarGate::test_night_high_soc_holds_no_solar_release` FAIL (2 failed). Matrix covers {day-solar, night-no-solar, reserve+2, mid-SOC} (lines 350–426). **PASS.**

4. **Cross-midnight test — real.** `TestD1CrossMidnight` parametrizes hours [23,0,1,2,3,4,5], derives `period`/`peak_ahead` from the real engine (month+hour-derived `get_current_period`), seeds a stale hold, and feeds the `140 kWh` rolled-forecast trap. `peak_ahead_before_offpeak` (energy_tou.py:247) walks hour-by-hour via `get_current_period`, so the rollover is exercised in the engine, not faked. **PASS.**

5. **Phase-invariant (cloudy daytime) — real & meaningful.** `TestPhaseInvariantCloudyDaytime::test_cloudy_daytime_pre_peak_still_holds` uses the real engine at 15:00 (`pa is True`) and asserts a hold; the mirror `test_cloudy_night_off_peak_still_releases` passes `remaining_forecast_kwh=0.0` (PV≈0) in off_peak and asserts release — proving phase is TIME-anchored not PV-anchored. The caller in energy.py:2698 confirms `solar_replenishing` derives from time-windowed surplus / live battery power, **never `solcast_remaining`** (the original bug). **PASS.**

6. **New const `DEFAULT_EV_SOLAR_REPLENISH_SURPLUS_PCT = 1.0`** (energy_const.py:572) — used only at energy.py:2699. Tests are **not over-fit**: they inject the boolean `solar_replenishing` directly into the controller, so assertions never depend on the `1.0` value. Sensible decoupling. **PASS.**

7. **No new suite failures — verified.** Full suite on branch: `35 failed, 5960 passed, 28 skipped, 14 errors` — matches the reported baseline exactly. The 35 failures + 14 errors are pre-existing `ModuleNotFound` collection issues in unrelated files (e.g. `test_activity_logger.py`); none in the EVSE file. The 25 new tests all pass; adjacent `test_energy_pool_fill_priority.py` (8) green. **PASS.**

## Minor observations (LOW / non-blocking)
- **L1:** `TestPhaseInvariantCloudyDaytime::test_cloudy_daytime_pre_peak_still_holds` passes a *healthy* `remaining_forecast_kwh=18.0` while naming itself "cloudy" — the PV≈0 condition is only truly exercised in the sibling off_peak test. The class docstring acknowledges this ("we pass a healthy forecast to isolate the phase gate"). Honest, but the daytime test name slightly oversells; the phase-invariant claim is carried by the pair, which is fine. No fix needed.
- **L2:** The plug-EVSE controller (energy_pool.py:1863–2161) carries identical day/night logic but the new test file only exercises the `EVChargerController`. Both paths share the same `fill_priority_inert` / `soc_recovered` shape; coverage of the plug path is via the existing suite. Acceptable for this cycle; note for future if the two drift.

## Summary statistics
| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 0 | – | – |
| HIGH | 0 | – | – |
| MEDIUM | 0 | – | – |
| LOW | 2 | 0 | 2 (non-issues) |

**Test-authority bug class:** none recurred. Mutation anchoring is exemplary — both anchors were empirically proven to fail on revert.
