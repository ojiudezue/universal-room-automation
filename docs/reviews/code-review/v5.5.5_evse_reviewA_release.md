# v5.5.5 EVSE Off-Peak Fill-Priority Release — Review A

**Framing:** D1 fill-priority release correctness + `peak_ahead_before_offpeak` REUSE + the PHASE INVARIANT (TIME not PV).
**Branch:** `feature/evse-offpeak-fill-release` @ 187e1c59 (read via `git diff develop...` — no checkout).
**Reviewer:** A of a Tier-3 (4 framing-disjoint) pass.

---

## 1. Release-logic correctness — ✅ (1 LOW)

The `fill_priority_inert` gate (energy_pool.py:1166-1170, mirror 2054-2058) matches the spec matrix exactly:
`tou_period in ("peak","off_peak") or (tou_period == "mid_peak" and peak_ahead is False)`.
- peak → inert (release); off_peak → inert (release, NEW); mid_peak → inert only when `peak_ahead is False` (post-peak); mid_peak+peak_ahead → falls through to the pause loop (hold). `peak_ahead is None` (legacy) → NOT inert → hold preserved. The `is False` identity check (not truthiness) correctly excludes `None`.
- **80% daytime fill UNCHANGED:** `pause_conditions_global = soc < soc_threshold AND forecast_healthy` (energy_pool.py:1180-1184) is byte-identical to v4.7.6; only reached in mid_peak+peak_ahead. ✅
- **Inert CLEARS, not skips:** energy_pool.py:1172-1174 (`discard` + `_release_pause_dispatch_owner`) actually empties `_paused_by_fill_priority`. The off_peak ensure-on / carry-over veto (energy_pool.py:558-560) and excess-solar turn-on veto (754-756) both gate on set membership, so clearing genuinely un-vetoes the off_peak charge. ✅ Mutation-anchored by `test_held_evse_released_on_off_peak_tick`.

**⚠️ LOW A-L1 (energy.py:2724-2725):** the call-site `peak_ahead = (self._tou is not None and self._tou.peak_ahead_before_offpeak(...))` coerces to **`False`** (not `None`) if `self._tou` were None — which would flip the pool method's legacy always-hold into release. **Not live:** the EC constructor raises `ValueError` when `tou_engine is None` (energy.py:145-149), so `self._tou` is a hard production invariant. Latent only. Recommend `peak_ahead = self._tou.peak_ahead_before_offpeak(_now_phase) if self._tou is not None else None` so the legacy sentinel is honored if the invariant ever weakens.

## 2. PHASE INVARIANT (the hard axis) — ✅

D1 decision uses **only** `tou_period` + `peak_ahead`. `peak_ahead` is computed purely from `peak_ahead_before_offpeak(now)` (clock walk). No solar/PV/`solcast_remaining` touches the inert gate. A cloudy daytime mid_peak (PV≈0, peak ahead) still HOLDS — proven by `test_*phase_invariant` driving the real TOU engine. `solcast_remaining` still feeds `forecast_healthy` for the *pause* trigger only (downstream of the gate), which is correct and pre-existing. No instantaneous-solar-as-phase input. ✅

## 3. `peak_ahead` wiring — ✅

Computed once at the call site (energy.py:2722-2727) from the shared `_now_phase` snapshot; try/except→None; pool method stays pure (param in). `peak_ahead_before_offpeak` (energy_tou.py:247) is the right midnight-safe primitive — walks `get_current_period(cursor)` hour-by-hour, deriving season from `dt.month` each step, returns True on first peak / False on first off_peak. None-fallback semantics are safe in the *pool* method (legacy hold); see A-L1 for the call-site coercion gap.

## 4. Cross-midnight — ✅

A 23:00→05:00 off_peak window is `off_peak` at every hour (summer 0-14 & 21-24), so `fill_priority_inert` is True throughout → released, no midnight re-lock. The original `solcast_remaining` rollover cannot re-lock because off_peak release is unconditional and PV-independent. Proven by `test_no_midnight_relock` parametrized across overnight hours against the real engine. ✅

## 5. SmartPlugController mirror — ✅

`SmartPlugController.determine_fill_priority_actions` (energy_pool.py:2054-2063) carries the identical `fill_priority_inert` expression and clear-on-inert loop. D2 `solar_replenishing` mirrored on `determine_battery_drain_actions` (energy_pool.py:1970-1976). Consistent with the EV path. ✅

---

## Summary

| Item | Verdict |
|---|---|
| Release matrix / clear-not-skip / 80% unchanged | ✅ (1 LOW) |
| PHASE INVARIANT (TIME not PV) | ✅ |
| `peak_ahead` wiring + REUSE | ✅ |
| Cross-midnight no re-lock | ✅ |
| SmartPlug mirror | ✅ |

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 (A-L1, latent) |

The D1 release logic is correct and the PHASE INVARIANT holds — the decision is TIME-anchored end to end, cloud-proof, and cross-midnight safe; the off_peak deadlock is genuinely broken via set-clear. The 80% daytime pause is untouched. Tests drive the real `peak_ahead_before_offpeak` and the production code paths (no hand-rolled phase logic, no self-INSERT).

**A-L1 is a defensive-correctness LOW** masked by the EC constructor's hard `tou_engine` guard — fix in this cycle per the fix-LOWs-in-cycle policy (≤3 LoC), no re-review needed.

**Verdict (Review A): APPROVE.** No CRITICAL/HIGH/MEDIUM in my framing. Fix A-L1 in the fix-up pass.
