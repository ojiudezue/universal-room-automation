# Code Review — v5.9.0 Census Over-count Fix (Tier 2-DB)

**Cycle:** Census over-count — same-area spatial dedup + sustain-before-latch + hold re-tune (+ rider: AutoRecoverySwitch → "Device Auto Recovery")
**Plan:** `docs/planning/PLANNING_census_overcount_dedup_decay.md` (2026-07-07 Revision)
**Branch:** `feature/census-overcount-v5.9.0` · **Baseline tag:** `pre-review-v5.9.0`
**Reviews:** 3 framing-disjoint (A local-correctness / B cross-coordinator integration / C test-authority-via-mutation), per Tier 2-DB. **A=SHIP, B=FIX-FIRST, C=BLOCK.**
**Date:** 2026-07-08

## Findings

| ID | Sev | Bug class | Finding | Status |
|---|---|---|---|---|
| B-C1 | CRITICAL | #53 | D-A dedup wired into `_calculate_house_census`, but with enhanced census ON (live default) `_apply_enhanced_house_census` overwrites the result via `_get_unrecognized_camera_count()` which still summed naively — **the fix never reached the shipping `unidentified_count` path** | FIXED — single shared `_dedup_by_area` helper (camera_census.py:1361) now used by BOTH paths (:954-955, :1731); enhanced-path test added |
| C-BLOCK | CRITICAL | test authority | The entire new test file was **tautological** — zero production imports; every test drove hand-copied stubs. Reviewer C reinstated the ORIGINAL over-count bug in production and all 154 tests stayed green | FIXED — test file rewritten to import real `PersonCensus` and drive `_calculate_house_census` / `_get_unrecognized_camera_count` (enhanced path) / `_apply_hold_decay`; `_sustain_gate_enabled` stub seam deleted; D-C default pinned from const; 17 tests |
| B-H1 | HIGH | timing | 15s sustain window sampled at 30s census cadence → real-second-person latch worst case ~60s; plan's "within ~30s" claim wrong | FIXED — promote-on-next-call verified; plan's live criterion corrected to honest ~60s worst case |
| B-M1 | MEDIUM | semantics | Shared `_apply_hold_decay` imposed the upward sustain gate on the PROPERTY zone, changing its documented instant-rise semantics | FIXED — `sustain_applies = zone == "house"`; property instant-rise/instant-drop preserved + tested |
| B-M2 | MEDIUM | stale state | D-E observability attrs only written on the cross-validation branch → stale after toggle-off | FIXED — cleared/set in else branch |
| A-L1 | LOW | #53-adjacent | Pending-latch state left stale on the two sustain→decay fall-throughs (self-healing) | FIXED — `_clear_pending` at both exits |
| A-L2 | LOW | clock | Sustain/hold/decay on wall-clock (inherited from pre-existing peak_ts semantics, no new exposure class) | ACCEPTED — noted; monotonic migration would be its own cycle |
| C-9 | MEDIUM | test authority | D-C default (15→3 min) pinned by nothing; two test files hardcoded contradictory copies | FIXED — asserted from `const.DEFAULT_CENSUS_HOLD_INTERIOR_MINUTES` |
| C-10 | LOW | coverage | Rider rename unasserted | FIXED — display-name assertion added |

## Summary statistics
| Severity | Found | Fixed | Accepted |
|---|---|---|---|
| CRITICAL | 2 | 2 | 0 |
| HIGH | 1 | 1 | 0 |
| MEDIUM | 3 | 3 | 0 |
| LOW | 3 | 2 | 1 |

## Bug-class frequency
- **#53 computed-but-not-consumed** — 2 (B-C1: dedup computed on a discarded path; A-L1 stale pending). B-C1 is a textbook instance: the fix existed, the live path didn't consume it.
- **Test-authority / tautology** — 3 (C-BLOCK, C-9, C-10). **Second consecutive cycle** with a stub-mirror/tautological test defect (v5.8.0 C-GAP-1, OV-1; now v5.9.0 C-BLOCK). Recommend QUALITY_CONTEXT.md gains an explicit bug class: **"Stub-mirror test — a test file that mirrors production logic without importing it certifies nothing; every cycle test MUST import the production module and a mutation of the real site MUST fail a named test."**

## Verification
- Builder self-verified 2 mutations (dedup max→sum; sustain neutered) — named tests failed, byte-identical restores.
- **Orchestrator independently re-ran the decisive mutation** (reinstated the original same-area sum in `_dedup_by_area`): **4 named tests failed** incl. the enhanced-path anchor `test_unrecognized_count_dedupes_same_area_cameras`; byte-identical restore; 17/17 green.
- Full suite: `35 failed / 6297 passed / 14 errors` = documented pre-existing baseline + 17 new passing; **zero regressions vs a clean develop baseline diff**.
- `quality/real_construction/`: 1 passed under the HA venv.

## Outcome
All CRITICAL/HIGH/MEDIUM fixed and re-verified. **Cleared to operator checkpoint → deploy.**
