# Code Review: v4.7.2.1 Occupancy-Weighted Prediction Restore Hotfix

**Tier:** 1 (Hotfix)
**Reviewer:** ura-reviewer (adversarial, single pass)
**Branch:** worktree-agent-aa5e2a61407622e09
**Baseline tag:** pre-fixup-v4.7.2.1 (274f055)
**Final commit:** f2874a6
**Date:** 2026-05-28

---

## Summary

- Files changed: 4 (switch.py, energy.py, test_v4503_ec_switch_restore.py, test_v4721_occupancy_weighted_restore.py)
- Net: +340 / -92 lines (bulk is bespoke class removal + new test file)
- Test suite: 54 failed / 3924 passed / 14 errors (unchanged from baseline -- zero regressions)
- New tests: 12 passing (test_v4721_occupancy_weighted_restore.py)

---

## Finding Summary

| ID | Severity | Status | Bug Class | Description |
|----|----------|--------|-----------|-------------|
| R1 | LOW | OPEN | -- | Stale docstring "all 5" in sub_switches_synced() |

---

## File-by-File Analysis

### switch.py (lines 422-689)

**Change:** Bespoke OccupancyWeightedPredictionSwitch class (95 lines) deleted. Replaced with single factory call at line 683.

**Verification:**

1. **Factory call signature** (switch.py:683-689):
   - `attr_name="occupancy_weighted"` -- matches property + setter at energy.py:3959-3966. The `setattr(energy, "occupancy_weighted", value)` in the factory invokes the setter which writes `self._occupancy_weighted`. CORRECT.
   - `unique_suffix="occupancy_weighted_prediction"` -- factory constructs `f"{DOMAIN}_energy_{unique_suffix}"` at switch.py:477, producing `universal_room_automation_energy_occupancy_weighted_prediction`. The deleted bespoke class had `f"{DOMAIN}_energy_occupancy_weighted_prediction"` at old line 445. **MATCH CONFIRMED.** No entity_id orphaning. No RestoreEntity state loss.
   - `name="Occupancy Weighted Prediction"` -- matches bespoke class's `self._attr_name`. User recognition preserved.
   - `icon="mdi:account-clock"` -- unchanged from bespoke class.
   - `default=False` -- matches bespoke class behavior (no explicit default seed, `getattr(energy, "occupancy_weighted", False)` returned False).

2. **Registration:** switch.py:142 -- `OccupancyWeightedPredictionSwitch(hass, entry)` remains in async_setup_entry entity list. No change needed; the factory produces a class with the same constructor signature `(hass, entry)`.

3. **Bug Class #5 (startup race) closure:** The bespoke class had only RestoreEntity + single 5s timer retry. The factory provides:
   - SIGNAL_ENERGY_COORDINATOR_READY subscription (switch.py:524-538) -- unbounded signal-based restore.
   - Timer retry chain at 5s/30s/120s (switch.py:567-572, 614-646) -- fast-path backup.
   - Both paths idempotent via `_deferred_restore` flag.
   This fully closes the startup race.

4. **Bug Class #38 (listener cleanup):** Factory registers signal subscription and timer via `async_on_remove()` at switch.py:533 and 568. Cleanup on entity removal is handled. CORRECT.

5. **Bug Class #11 (None handling):** Factory's `_get_energy()` returns None when coordinator_manager or energy coordinator is absent. All callers guard with `if energy is not None`. `is_on` returns `self._default` when energy is None. CORRECT.

**Classification:** ADOPT
**Issues:** None

---

### energy.py (line 378)

**Change:** `_pending_sub_switch_restores: int = 5` changed to `6`.

**Verification:**

1. **Counter accuracy:** Pre-fixup had 5 factory-generated switches (ECGridImportCap, ECLoadShedding, ECExcessSolar, ECArbitrage, ECEvTou). Post-fixup adds OccupancyWeightedPredictionSwitch as 6th factory switch. Counter 6 is CORRECT.

2. **Comment updated:** The inline comment at energy.py:377-378 correctly documents the v4.7.2.1 change.

3. **Stale docstring (R1):** energy.py:3944 `sub_switches_synced()` docstring still reads "all 5 EC sub-switches" -- should say "all 6". The counter logic itself is correct (checks `== 0`), so this is cosmetic only.

**Classification:** ADOPT
**Issues:** R1 (LOW) -- stale docstring, no runtime impact

---

### test_v4503_ec_switch_restore.py (line 322)

**Change:** `switch_source.find("_ec_switch_factory")` changed to `switch_source.find("def _ec_switch_factory(")`.

**Verification:** The more-specific pattern targets the function definition rather than any occurrence of the string (e.g., in comments). The pre-fixup code worked because no comment contained `_ec_switch_factory` before the definition, but the new factory call's comment at switch.py:676 introduces the substring earlier in the file relative to the inner method. The fix is defensive and minimal. All 14 TestSourceMirrorContract tests pass in both orderings.

**Classification:** ADOPT
**Issues:** None

---

### test_v4721_occupancy_weighted_restore.py (292 lines, 12 tests)

**Verification of critical tests:**

1. **test_v4721_uses_ec_switch_factory:** AST-level string assertion that `OccupancyWeightedPredictionSwitch = _ec_switch_factory(` exists. CORRECT.

2. **test_v4721_factory_call_preserves_unique_id_suffix:** Asserts `"occupancy_weighted_prediction"` appears in the factory call body. This guards against the unique_id mismatch regression (the CRITICAL concern from the review checklist). CORRECT.

3. **test_v4721_off_state_also_survives_restart:** Bidirectional restore -- explicitly-OFF survives restart even when EC seed is True. CORRECT.

4. **test_v4721_first_install_leaves_default:** last_state=None path -- no clobber, no deferred state. CORRECT.

5. **test_v4721_occupancy_weighted_survives_restart_via_ec_ready_signal:** Signal-path restore. Tests the exact bug scenario that caused the original issue. CORRECT.

6. **Mirror contract:** _OccupancyWeightedSwitchMirror replicates factory logic faithfully. Retry chain delays (5, 30, 120), deferred_restore flag, signal handler -- all match production.

**Test quality:** Good. Covers all three restore paths (fast, timer, signal), both ON and OFF states, and first-install. Source-grep tests guard against regressions.

**Classification:** ADOPT
**Issues:** None

---

## Bug Class Compliance

| Bug Class | Status |
|-----------|--------|
| #5 (Startup Race) | FIXED -- entire point of this hotfix |
| #38 (Listener Cleanup) | PRESERVED -- factory uses async_on_remove |
| #11 (None Handling) | PRESERVED -- factory guards all coordinator access |
| #42 (Lambda+async_create_task) | N/A -- factory uses bound methods, not lambdas |

---

## Suite Results

```
Post-fixup:  54 failed, 3924 passed, 2 skipped, 14 errors (17 warnings)
Baseline:    54 failed, 3924 passed, 2 skipped, 14 errors (21 warnings)
Delta:       0 new failures, 0 new errors
New tests:   12 (all passing)
```

Both test orderings (v4721 first, v4503 first) pass 26/26.

---

## Findings Detail

### R1 (LOW): Stale docstring in sub_switches_synced()

**File:** energy.py:3944
**Current:** `"""Return True when all 5 EC sub-switches have completed deferred restore."""`
**Should be:** `"""Return True when all 6 EC sub-switches have completed deferred restore."""`
**Impact:** Cosmetic only. The counter logic checks `== 0` and is correct.
**Bug Class:** None (documentation)

---

## Verdict

**SAFE TO COMMIT**

The unique_id suffix `"occupancy_weighted_prediction"` matches the prior bespoke class's unique_id exactly (`{DOMAIN}_energy_occupancy_weighted_prediction`). No entity_id orphaning, no RestoreEntity state loss. The factory conversion gives the switch the full v4.5.3 retry chain + v4.7.x SIGNAL_ENERGY_COORDINATOR_READY subscription, closing the Bug Class #5 startup race. Counter bump is correct. Tests are thorough. Zero new suite failures.

R1 (stale docstring) is LOW and can be fixed in-flight or deferred to next cycle.
