# v5.5.0 Inclement-Weather Reserve — Consolidated Review Summary

**Cycle:** Robust Inclement-Weather Detection + TOU/Solar-Horizon-Aware Battery Hold
**Tier:** 2-DB (3 framing-disjoint reviews + focused fourth pass)
**Branch:** `feature/inclement-weather-reserve` | **Baseline tag:** `pre-review-v5.5.0`
**Per-review docs:** `v5.5.0_inclement_review{A,B,C}_*.md`

## Process ledger
- Build (ura-builder) → `b3870b8`
- In-cycle correctness fix (orchestrator, EV-audit §2 falsified) → `ee749b5`
- 3 framing-disjoint reviews (A=detection/fusion/surplus, B=integration/precedence/EV-safety, C=config/test-authority)
- Fix-up pass (ura-builder) → `f06e48c`
- Focused fourth review (fix-up surfaces) → SHIP

## Findings & disposition

| ID | Sev | Bug class | File:line | Found by | Disposition |
|---|---|---|---|---|---|
| EV-§2 | HIGH | Cross-coordinator reserve precedence | energy.py:2480 | Build | FIXED in-cycle (`max()`-safe; EVSE hold can only raise reserve) |
| A-CRIT-1 | CRITICAL | **Computed-but-not-consumed control value** (new) | energy_battery.py:2947 | Review A | FIXED (off_peak drain/hold clamp to effective_reserve, gated on partial_hold) |
| A-HIGH-1 | HIGH | Enum/domain mismatch (#22) | inclement.py:312 | Review A | FIXED (`{good, excellent}`; "fair" never existed, "moderate" over-permitted) |
| C-1 | HIGH | Config clarity / named-bucket rule | config_flow.py:3398 | Review C | FIXED (`{label,value}` SelectOptionDicts; enum values preserved) |
| A-MED-1 | MED | Classifier precedence | inclement.py:235 | Review A | VERIFIED working-as-intended; pinning test + comment added |
| A-MED-2 | MED | off_peak floor (condition-only) | energy_battery.py:2955 | Review A | COVERED by A-CRIT-1 clamp; test added |
| A-MED-3 | MED | Risk-window inflation post-sunset | inclement.py (compute_solar_horizon) | Review A | FIXED (`_today_sunset` cap; post-sunset → today_recoverable=False) |
| arb-WAIT | MED | Computed-but-not-consumed (arbitrage path) | energy_battery.py:1521 | Review 4 | **DEFERRED** — tracked follow-up (not a regression; see README known-limitations + OC Appendix B + memory) |
| B-LOW-1 | LOW | Observability | energy_battery.py:894 | Review B | FIXED (debug log in as_dict except) |
| C-4 | LOW | Defensive coercion | energy_battery.py:727 | Review C | FIXED (`_coerce_int_or_default`) |
| C-2/C-3/C-5, A-LOW-1/2 | LOW | Cosmetic | — | Reviews A/C | DEFERRED (cosmetic; noted in plan completion tracking) |

## Severity stats
| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 1 | 1 | 0 |
| HIGH | 3 | 3 | 0 |
| MEDIUM | 4 | 3 | 1 (arb-WAIT, tracked) |
| LOW | 7 | 3 | 4 (cosmetic) |

Outstanding blocking findings at deploy: **0 CRITICAL / 0 HIGH.**

## Bug-class frequency
- Computed-but-not-consumed control value ×2 (A-CRIT-1 off_peak; arb-WAIT) — **candidate new QUALITY_CONTEXT class**
- Enum/domain mismatch (#22) ×1
- Cross-coordinator reserve precedence ×1

## QUALITY_CONTEXT.md recommendation
Add a bug class: **"Computed-but-not-consumed control value"** — a decision field (e.g. `effective_reserve` / `reserve_floor`) is threaded into some branches but silently dropped in a sibling branch while still surfaced in attributes, so the attribute claims a protection the code doesn't enforce. Surfaced twice in this cycle (off_peak drain branch; arbitrage WAIT phase). Detection: grep every consumer of a newly-threaded control value across ALL sibling branches of the same decision function, not just the obvious ones.

## Pre-deploy snapshot (Tier 2-DB mandate)
Captured 2026-06-15 ~23:27 UTC from the live HA instance (current deploy v5.4.2):
- `sensor.span_panel_battery_battery_level` = **97.2%**; `sensor.span_panel_battery_battery_power` = −228 W (charging/idle).
- `select.enpower_..._storage_mode` = **self_consumption**; `number.iq_battery_hacs_battery_reserve` = **20%**.
- Live NWS fixture: `Flood Watch` (Severity=Severe, Certainty=Possible) — the headline correctness fixture (must classify as NOTICE → allow_discharge post-deploy).
- NOTE: the URA `battery_mode` decision sensor's exact entity_id was not pinned pre-deploy (the plan's assumed `sensor.ura_energy_battery_coordinator_battery_mode` does not exist on the live instance; URA uses the `universal_room_automation_*` / `ura_*` prefix). Pin the precise entity during Review D and use it for the post-deploy ±25% `battery_mode` distribution comparison.

## Framing-disjoint value note
Reviewer B (no-regression framing) certified the `effective_reserve` threading as drift-free; Reviewer A (does-partial_hold-actually-work framing) caught that the off_peak branch never *reads* it — a CRITICAL that B's framing structurally could not surface. Textbook demonstration of why three disjoint framings beat three thorough reviewers sharing one framing.
