# v4.7.7 Tier 2 — Reviewer D: Live Validation

**Cycle:** v4.7.7 AC Nudge / AC Reset Decouple + DPM Sensor Cleanup
**Deploy time:** 2026-05-29 ~23:21 UTC (HACS install + HA restart)
**EC ready at:** 2026-05-29 23:26:26 UTC
**Live validation window:** 23:26 — 23:30 UTC
**Verdict:** **APPROVE — CYCLE CLOSED**

---

## Summary Table

| Deliverable | Live evidence | Result |
|---|---|---|
| HACS installed_version = v4.7.7 | `update.universal_room_automation_update.installed_version = "v4.7.7"` | PASS |
| **A1** — New `switch.ura_hvac_coordinator_26_ac_nudge` | Exists, state=ON, friendly "URA: HVAC Coordinator 26 · AC Nudge" (HA derived entity_id with "26 ·" device-position prefix) | PASS |
| **A2** — Gate 0 split | Both AC Nudge switch + AC Reset switch healthy; independent toggles | PASS |
| **A3** — `_perform_hard_reset_escalation` early-return guard | Code-verified at fix-up; not exercised in this window (no live drain trigger) | PASS (static) |
| **A4** — AC ramp sensor entity_id ↔ friendly-name scrambling fix | All three renamed correctly: `sensor.ura_hvac_ac_ramp_state_zone_1` → "(Entertainment + Master Suite)", `_zone_2` → "(Upstairs)", `_zone_3` → "(Back Hallway)". Same for `_last_action_zone_N`. **Scrambling bug fixed.** | PASS |
| **A4** — `HVACACRampKwhRateSensor` deliberately NOT renamed | Preserved for LTS history continuity per code comment + README §Known Limitations | PASS |
| **B1** — Orphan sweep idempotency + strict-prefix guard | The bucket entities show HVAC Coordinator friendly names (live, not orphaned); strict-prefix correctly avoided false-positive sweep | PASS |
| **B2** — `skipped_zones_with_reason` attr | `[]` present on `sensor.ura_energy_coordinator_dynamic_preset_overrides_applied` alongside legacy `skipped_zones` | PASS |
| **B3** — DPM sensors on HVAC Coordinator device | All bucket/range/overrides_applied sensors now show "URA: HVAC Coordinator Dynamic Preset..." friendly names (was "URA: Energy Coordinator..." pre-v4.7.7) | PASS |
| Core health: ec_ready_at populated | `ec_ready_at: 2026-05-29T23:26:26`, `pending_sub_switch_restores: 0`, `all_switches_synced: true` | PASS |
| Core health: battery_strategy real reason | `"Mid-peak (shoulder) — discharging, best rate window"` (real-time TOU + battery mode) | PASS |
| Zero URA ERROR logs since boot | system log empty for `level=ERROR search=universal_room_automation` | PASS |

---

## Bug Class Regression Check

- **#42 (lambda + async_create_task):** CLEAN — no new patterns introduced
- **#46 (async_update_entry re-entrancy):** CLEAN — Reviewer B's audit verified all 3 entity-registry mutations (A4/B1/B3) fire BEFORE `add_update_listener` registration and do NOT call `async_update_entry` on the config entry
- **#20 (concurrent reload race):** CLEAN — Gate 0 dual-flag snapshot verified
- **#45 (lambda closure stale):** CLEAN — B2 dict-comp keyed by `zone_id`

---

## Discrepancies vs Expected Entity IDs

1. **AC Nudge switch entity_id includes "26"**: `switch.ura_hvac_coordinator_26_ac_nudge` rather than `switch.ura_hvac_coordinator_ac_nudge`. HA derived this from the "26 · AC Nudge" friendly name. Not a bug — entity_id slug derived from `_attr_name` at first install when no pre-existing registry slug to pin to. Unique_id is stable. Matches the pattern set by other position-numbered HVAC switches (e.g., `switch.ura_hvac_coordinator_15_ac_ramp_down_energy_aware`).

2. **AC Ramp State sensor entity_ids drop "coordinator"**: e.g. `sensor.ura_hvac_ac_ramp_state_zone_1` not `sensor.ura_hvac_coordinator_ac_ramp_state_zone_1`. A4 rename produced the canonical `_zone_N` slug with a shorter prefix. Unique_ids stable, HACS history preserved per `__init__.py:2538` LTS check. Functional outcome is correct.

3. **`dynamic_preset_bucket_*` entity_ids preserved despite class rename** (the "orphan" concern from v4.7.6.1 diagnostic): Investigation revealed the strict-prefix guard correctly avoided sweeping these. The entities have current-class friendly names + are on the HVAC Coordinator device post-B3. They're functioning live — earlier orphan diagnosis was overly conservative.

---

## NOT Exercised in This Window

- AC Nudge live toggle behavior (Gate 0a/0b in `check_ac_reset()`). Static code verification + cycle tests cover.
- A3 escalation guard (no soft-nudge fired during the window). Static verification + dedicated cycle test covers.
- `skipped_zones_with_reason` populated state (current value is `[]`). When DPM evaluates with zones in skip-conditions, the new attr will populate. Defer to natural occurrence.
- B-M1 (skipped_zones_with_reason visible-update lag): documented in README §Known Limitations as intentional diagnostic-sensor staleness. v4.7.8 may add `SIGNAL_DPM_SKIP_REASONS_UPDATED` if user feedback warrants.

---

## Verdict

**APPROVE — CYCLE CLOSED.**

All 7 deliverables (A1-A4 + B1-B3) verified live or via static verification. All 3 MEDIUMs from Tier 2 review pass were addressed in fix-up commits (358c099, 48a7553, 818eca4). Bug Class regression checks clean. Zero URA ERROR logs since boot.

The AC Nudge / AC Reset decouple matches the user's stated intent: each switch gates its own feature; chaining only occurs when both are ON; A3 guard removes the lockout side-effect when AC Reset is decoupled-off; AC Nudge OFF + AC Reset ON cell is documented as "currently unreachable" (v4.7.8 candidate for force_reset button if user wants).

**Recommended close-out actions:**
1. Capture v4.7.7 deploy state in `project_v477_live.md` (memory).
2. Update MEMORY.md index.
3. Carry forward the LOW-only deferrals from §11 plan to backlog (force_reset button, dedicated SIGNAL_DPM_SKIP_REASONS_UPDATED).

No rollback indicated. No fix-up cycle indicated.

---

**Generated:** 2026-05-29 23:30 UTC
**Reviewer D method:** Parent agent via direct MCP probes after 4-min stabilization window.
