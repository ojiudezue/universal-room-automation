# Code Review Record — v5.27.0 NM Cycle C (routing matrix, Tier 3)

**Branch:** build/nm-cycle-c (f6e53a96 build → 3fe7ff01 fix-up 1 → 3ab1e426
orchestrator test → 4b5cc15d fix-up 2). Baseline tag `pre-review-v5.27.0`.
**Tier:** 3 — four framing-disjoint reviews (A local correctness, B
integration, C mutation-execution, D adversarial completeness) + D RE-PASS
after fix-up (N+1th-site rule) + orchestrator independent mutations.
**Plan:** `docs/planning/PLANNING_nm_cycle_c_routing_matrix.md`
(invariants C-INV-1/2/3). **Status: AT OPERATOR CHECKPOINT — not deployed.**

## Findings ledger (all fixed unless noted)

| id | sev | finding |
|---|---|---|
| D1=A-CRIT-1=B-CRIT-1 | CRITICAL | `_repeat_alert` referenced undefined `_router_allowed` → NameError on first repeat tick with companion/whatsapp/imessage → repeat chain dies (life-safety regression). Found independently by A, B, D. |
| D2 | CRITICAL | Repeat path bypassed matrix/override/mute/DND entirely (falsified build's deviation claim). Rebuilt with per-person intersection + life-safety exception. |
| D3=B-HIGH-1 | HIGH | Global quiet-hours gate pre-empted per-recipient DND-bypass → widened bypass sets unreachable below CRITICAL. Fixed with any-recipient union check. |
| D4=B-MED-1↑ | HIGH | In-memory migration froze routing vs live options changes AND mutated entry-aliased dicts. Fixed: coordinator-owned `_materialized_matrix` + hash-triggered rebuild (re-pass verified hash covers all `_channel_qualifies` config reads). |
| D5 | HIGH | `nm.mute_person_channel` service never registered (build claim false). Registered + unload removal. |
| B-HIGH-2 | HIGH | Token burn before router/mute intersection → burn-without-send on muted channels. Fixed: union intersection before `_take_channel_once`. |
| C-1 | HIGH | async_notify per-person intersection untested (mutation GREEN). Fix-up added single-person e2e test — **orchestrator re-run proved it STILL GREEN** (union gate masks the per-person site); orchestrator authored two-person asymmetric-mute test (3ab1e426) with fixture self-check; mutation now KILLED. |
| C-2 | HIGH | Audit-row emission zero coverage. Tests added. |
| C-3 | HIGH | Cycle B alert-lights dry-run test tautological (async_create_task MagicMock; recurrence #9). De-tautologized; guard mutation now killed. |
| C-4 | HIGH | Ack-announce TTS dry-run guard untested. Test added. |
| D-R1 | HIGH (re-pass) | D3 fix over-corrected: one recipient's bypass opened the GLOBAL gate → house-wide TTS/lights during quiet hours. Fixed: global channels gated on the global predicate (`_global_dnd_ok`); orchestrator mutation at both use sites killed by named test. |
| D-R2/D-R3 | MED | Digest rows nested under channel/token gate → muted immediate peer (new) or all-digest household (pre-existing) starved digests. Fixed: digest writes lifted out from under the gate — ratified behavior improvement vs v5.26.0. |
| C-5, C-6, A-MED-1, A-MED-2, D6, D7, D8, B-MED-2, D-R4, D-R5 | MED/LOW | Migration empty-matrix self-check; anchors table corrected; branch-label single-source; missing-row semantics documented; tts/lights mute rejected; light-pattern per-iteration dry-run re-check; mute-duration Number built; service unload; dry-run no-burn on global channels; test flake root-caused (MagicMock hasattr harness bleed — same class as A-2 C-HIGH-1). |

## Statistics

| severity | found | fixed | deferred/open |
|---|---|---|---|
| CRITICAL | 2 | 2 | 0 |
| HIGH | 9 | 9 | 0 |
| MEDIUM | 7 | 6 | 1 (A-MED-2 policy: overheat/high_co2 — operator) |
| LOW | 8 | 5 | 3 (documented) |

## Invariant status (post D re-pass)

- **C-INV-1** (legacy byte-identical): HOLDS, with TWO documented ratified
  exceptions — (a) non-CRITICAL life-safety hazards bypass the global
  quiet-hours gate (safety-positive); (b) digest rows now written for
  digest-pref recipients regardless of the token gate (starvation fix).
- **C-INV-2** (dry-run zero-outbound): HOLDS across all 17 enumerated sites.
- **C-INV-3** (DND determinism + life-safety floor): HOLDS both directions
  after D-R1.

## Policy rulings awaiting operator ratification (checkpoint items)

1. Life-safety hazards ignore mutes AND DND on messaging channels (initial
   + repeat); explicit matrix/override still authoritative.
2. Non-CRITICAL life-safety quiet-hours bypass (C-INV-1 exception a).
3. overheat/high_co2 life-safety classification (carried from Cycle B).
4. Mute-button fan-out: primary-channel-only buttons + service for the rest.
5. Options-flow UI for matrix/overrides/bypass NOT built this cycle
   (router honors persisted keys; UI is a follow-up cycle).

## Verification

- 5 reviews total (A/B/C/D + D re-pass). Reviewer C: 16/18 builder anchors
  killed, 5 extra mutations → 4 HIGHs, all closed.
- Orchestrator personally ran 4 mutations: per-person intersection (found
  live gap, closed with own test), life-safety mute-skip, D-R1 global
  predicate (both sites), plus malformed-mutation lesson (truncated
  multi-line expr → collection error, not a verdict — redo at use sites).
- Full suite ×2 consecutive: 36 failed + 14 errors = exact env-drift
  baseline, stable; 46/46 cycle tests green.

**Bug-class notes:** tautological-fixture at recurrence #9 (C-3) — the
async_create_task-MagicMock shape joins the catalogue; MagicMock-hasattr
harness bleed (D-R5) is the same family as A-2's C-HIGH-1 — candidate
QUALITY_CONTEXT consolidation: "test-harness sys.modules hygiene".
