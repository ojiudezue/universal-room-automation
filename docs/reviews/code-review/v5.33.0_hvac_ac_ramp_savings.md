# Code Review — v5.33.0 HVAC AC-Ramp Savings (deferred #7 D6)

**Tier:** 2 (two framing-disjoint reviews + live validation). Additive, display-only,
restart-safe (DB-re-derived). Earns two reviews for the persisted-notes payload extension
+ the new $ surface. **Baseline:** `pre-review-v5.33.0`.

## Framings
- **A — Accuracy + rate-capture + notes back-compat.**
- **B — Additive wiring + scope/reset + persistence + registration + no-double-count.**

## Findings

| ID | Sev | Class | Summary | Status |
|---|---|---|---|---|
| B-HIGH-1 | HIGH | #41 brittle slice-test | `test_fp_rate_hidden_until_sample_size_5` sliced `_refresh_impact_cache[idx:idx+3000]`; build moved the asserted token past the window → FAILS (orchestrator-confirmed live) | **FIXED** — whole-function-body search; 5 slice sites in the file converted; 6 sibling slices in `test_v4_7_17_1` widened |
| A-M2 | MED | #60/#62 test authority | only behavioral proof of `kwh×rate` sum was `@_ha_only`-gated → silent skip on non-HA hosts | **FIXED** — pure `_sum_savings_from_rows` helper + 8 non-gated mutation tests |
| A-M1 | MED | rate-at-wrong-boundary | rate captured at eval time (~10 min post-nudge) → peak-boundary nudges valued at lower post-boundary rate (pessimistic) | **DOCUMENTED** (rough-estimate mandate; methodology attr + README) — not fixed |
| B-M1 | MED | #55 private cross-coord reach | `ec._billing._get_cycle_start` reached through two private attrs | **FIXED** — public `EnergyCoordinator.get_billing_cycle_start()` accessor + routed through it |
| A-L1 | LOW | poisoned-total | `rate=nan/inf` would permanently poison cached lifetime total | **FIXED** — `math.isfinite & >0` guard; else omit key ($0, forward-safe) |
| B-L1 | LOW | observability | silent DEBUG-only fallback to local-midnight masks degraded cycle scope | **FIXED** — `cycle_start_source: ec\|fallback` attr + INFO log on fallback |
| B-L2 | LOW | #7 dt idiom | manual `datetime.combine(...).replace(tzinfo=)` for midnight | **FIXED** — `dt_util.start_of_local_day()` |
| A-L2 / B-L3 | LOW | docstring | `get_ac_ramp_savings` overstated savings≡kWh×rate | **FIXED** — states forward-only subset |

## Summary statistics
| Severity | Found | Fixed | Documented |
|---|---|---|---|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 1 | 1 | 0 |
| MEDIUM | 3 | 2 | 1 |
| LOW | 4 | 4 | 0 |

## Bug-class frequency
- #41 brittle slice-test: 1 (HIGH) + several sibling widenings — recurring pattern in `test_v4512_observability` / `test_v4_7_17_1` whenever a function grows. **Recommend QUALITY_CONTEXT note: string-slice source tests are fragile under code motion; prefer whole-function-body extraction.**
- #60/#62 test-authority: 1. #55 private-reach: 1. #7 dt-idiom: 1.

## Orchestrator independent verification
- **B-HIGH-1 confirmed live** by running the specific test (the build agent had misreported it as passing) — the value of not trusting summaries.
- Post-fix re-verified personally: touched test files 132 passed/10 skipped; FP-rate test passes; **no-double-count still holds** (grep: no `ac_ramp_savings` in any `EnergySavingsTotal*` context); **delta math unchanged** (`delta = kwh_rate_before - post_min`, D3 deferred); pure helper + mutation test present.

## QUALITY_CONTEXT.md recommendation
Add a note under the test-authority / brittle-test area: **source-slice assertions (`src[idx:idx+N]`) silently break when the target function grows** — the fp-rate + eval-window tests hit this twice this cycle. Prefer a `_extract_function_body()` helper (now in `test_v4512_observability.py`) over fixed windows.
</content>
