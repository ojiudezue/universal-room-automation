# Code Review Record — v5.25.0 NM Cycle A-2 (knob surface)

**Branch:** build/nm-cycle-a2 (74fc468e build → 43f6bb56 fix-up). Baseline tag `pre-review-v5.25.0`.
**Tier:** 2-DB (three framing-disjoint reviews) — elevated for the reload-suppression
shared-primitive touch and five-coordinator ripple through `nm_cycle_a_knob`.
**Plan:** `docs/planning/PLANNING_nm_cycle_a2_knob_surface.md`.

## Reviews

- **A — correctness + coercion + kill-switches:** FIX-FIRST. Verified clean: knob
  coercion, numeric round-trip, L4 both-sides normalization, allowlist value
  fidelity, default-path equivalence, working kill-switches.
- **B — reload-suppression + cache + lifecycle:** SHIP. Verified: all 13 keys in
  both suppress sets, invalidation-before-subset ordering on mixed saves, no
  async race, no consumer boot default-poisoning (all 5 consumers read at tick
  time under a guaranteed-present CM entry).
- **C — round-trip + back-compat + mutation testing:** FIX-FIRST. Mutations
  a/b/c CAUGHT; d/e GREEN (findings); round-trip and legacy-entry back-compat PASS.

## Findings

| id | sev | class | finding | outcome |
|---|---|---|---|---|
| C-HIGH-1 | HIGH | test-harness leakage | new test file's sys.modules HA stubs leak; 3 safety tests fail in co-run | FIXED — snapshot/restore in `_load_nm_cycle_a`; 4-file suite 218 passed |
| C-HIGH-2 | HIGH | tautological fixture (#7th recurrence) + untested load-bearing site | L4 normalization untested (mutation d GREEN); prior test hand-copied the coercion | FIXED — extracted `high_finding_allowlisted` helper + behavioral test with Enum-shaped dim; mutation d now fails that test (orchestrator-verified) |
| C-HIGH-3 | HIGH | untested load-bearing gate | HIGH-defer gate bypass (mutation e) stayed GREEN; wiring test was string-grep | FIXED — extracted `should_defer_high_to_digest`, both-branches behavioral test + source-anchored wire test; mutation e now fails (orchestrator-verified) |
| A1 | MEDIUM | #7 stale data / inert kill-switch | `CONF_SAFETY_DISCOVERY_BLOCKLIST` read only at discovery; reload-suppressed edit = no effect until restart | FIXED (documented) — field labeled applies-after-restart; plan D2 claim amended; live-re-read parked in deferred register. Ruling: NOT removed from suppress set (CM reload → event-loop-stall/watchdog hazard) |
| A2 | MEDIUM | #21 severity miscall | humidity ladder had no ordering validation; inverted entry inflates severity | FIXED — save-time monotonicity check + errors dict + test |
| B1 | MEDIUM | #7 stale cache | knob cache flushed only on options-update, not entry lifecycle | FIXED — invalidate on CM setup + unload |
| C-MED-1 | MEDIUM | Number-Fields=Form-Fields trap | open-and-save froze all 13 defaults as persisted overrides | FIXED — save-side default-drop (numeric-aware); reset-to-default removes key |
| C-MED-2 | MEDIUM | tautological fixture | hand-typed conf-key literals in two harness tests both sides of assertion | FIXED — self-check asserts literals == const source |
| A3 | LOW | selector scope | blocklist selector excluded binary_sensor | FIXED |
| C-LOW-2 | LOW | silent failure | cache-flush except logged at DEBUG | FIXED — WARNING |
| B2 | LOW | cache keyed by conf_key only | single-CM assumption | DEFERRED (documented on cache) |
| B3 | LOW | exception path caches default | | DEFERRED |
| C-LOW-1 | LOW | floats cosmetic | | DEFERRED |
| A4 | LOW | dup of B1/B2 | | covered by B1 fix |

## Statistics

| severity | found | fixed | deferred |
|---|---|---|---|
| CRITICAL | 0 | — | — |
| HIGH | 3 | 3 | 0 |
| MEDIUM | 5 | 5 | 0 |
| LOW | 6 | 3 | 3 |

**Recurring class note:** tautological-fixture is now at 7 recurrences
(C-HIGH-2) — the fixture self-check countermeasure (v5.24.0 pattern) was
applied to the harness literals too (C-MED-2). Already a QUALITY_CONTEXT class.

## Verification

- Orchestrator independently re-ran mutations d and e post-fix-up: each fails
  exactly one named test; tree restored; combined A-2+safety run 120 passed.
- Full suite: 7225 passed, 36 failed + 14 errors = exact pre-existing env-drift
  baseline (byte-identical failure set check done at build).
- Cycle invariant (no options set → byte-identical to v5.24.0) affirmed by
  Reviews A and B independently.
