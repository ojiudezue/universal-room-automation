# v5.75.2 — NM-RECOVERY-AGEBOUND-1 (Tier 1) + SAFEWORD-WINDOW-1 (Tier 2-DB) Review Record

## NM-RECOVERY-AGEBOUND-1
1 review: SHIP + 1 MED folded (zombie-REPEATING restore guard). Adjudication of note: caller-side
bound chosen because enumeration showed a SQL bound would break the two ack-path callers.

## SAFEWORD-WINDOW-1
Plan review: BUILD-READY (settled re-page-bypass sidestep, reject-not-clamp, RAM-only). Reviews:
A DO-NOT-SHIP / B SHIP / C DO-NOT-SHIP — near-total disjointness again:

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| A1 | HIGH | Window-open unauthorized — any known person could silence perimeter 3h (auth only in CRITICAL branch) | Authorization-scope gap | ✅ same gate as acks + drill |
| C-CRIT-1 | CRIT | The suppression action itself untested — drill 1 (remove the return) left suite GREEN; test's downstream marker wired to a nonexistent method | **Hollow anchor (dead-spy variant)** | ✅ real _is_deduplicated spy; drill 1 now reds 2 |
| A2 | HIGH(latent) | auth_reason UnboundLocalError booby-trap (pre-existing) | Unbound-on-branch | ✅ initializer |
| A4/C-LOW-4 | LOW | Cap-reject pre-auth + safeword-existence side channel | Consistency | ✅ uniform gate, silent fall-through |
| B-LOW-1 | LOW | Suppression trail counter-only — tuning grain | Telemetry grain | ✅ folded: last-10 ring on diagnostics (serves the operator's stated goal) |
| B-LOW-2 | LOW | Open/expiry notes DND-dropped at night | Visibility | ACCEPTED + doc'd (SMS reply is guaranteed feedback) |
| C-MED-1/LOW-2/LOW-3 | MED/LOW | Dead spy plumbing; tautological reply test; cap-reject rate-limit hole | Test hygiene | ✅ all |
| A3 | doc | Plan D5 prose wrong | Docs-of-record | ✅ |

Drills: 6 post-fix-up (gate, parse, life-safety, perimeter-scope, auth-open, auth-cap), every one
discriminating with named reds; builder self-caught a bogus drill-4 anchor (water_leak's
life-safety overlap) and fixed the test BEFORE trusting the drill. Orchestrator re-drilled the A1
auth gate: 1 named red, restored 25/25, tree clean.

## Statistics (both cycles)
| Severity | Found | Fixed | Accepted |
|---|---|---|---|
| CRITICAL | 1 | 1 | 0 |
| HIGH | 2 | 2 | 0 |
| MEDIUM | 2 | 2 | 0 |
| LOW | 7 | 6 | 1 |

Suite: failure set byte-identical to baseline; +35 net new passes.

## Bug-class notes → QUALITY_CONTEXT candidates
- **Dead-spy hollow anchor** (C-CRIT-1): a test can wire its observation to a method that doesn't
  exist and silently observe nothing — hasattr-guarded spy installation is the tell. Sixth
  hollow-anchor variant this month; the catalog earns a consolidated QUALITY_CONTEXT entry.
- **Authorization-scope gap**: new privileged actions added to an existing command arm must
  re-derive the auth boundary, not inherit the arm's (branch-scoped) check.
