# Code Review Record — v5.26.0 NM Cycle B (safety rails)

**Branch:** build/nm-cycle-b (aa68f9df build → 8d7bc10c fix-up). Baseline tag `pre-review-v5.26.0`.
**Tier:** 2-DB (literal trigger: database.py DAO change — additive `dry_run` column — plus safety-contract machinery).
**Plan:** `docs/planning/PLANNING_nm_overhaul_2026_07.md` Cycle B.

## Reviews

- **A — life-safety correctness + DB integrity:** FIX-FIRST. Clean: additive
  idempotent migration, name-keyed readers unaffected, dry-run rows
  non-polluting, no priority inversion for correctly-classified hazards.
- **B — lifecycle + restart + persistence + kill-switches:** FIX-FIRST. Clean:
  dry-run fail-safe direction (unknown restore → sends ENABLED — deliberate),
  unacked-restart resumes repeating, timer cleanup, alert-light restore
  exemption honest.
- **C — bucket math + gate completeness + test authority:** SHIP-with-fixes.
  Gate-completeness enumeration: all 8 outbound sites gated or justified-exempt
  (dry-run ON → zero outbound PROVEN); 5/5 planned mutations killed; no
  tautological fixtures.

## Findings

| id | sev | class | finding | outcome |
|---|---|---|---|---|
| A-CRIT-1 | CRITICAL | #22 enum/string mismatch | `NM_LIFE_SAFETY_HAZARDS` had `"intrusion"`; emitted token is `"intruder"` → intruder CRITICAL triple-demoted (300s cadence, bucket-droppable, boot-collapsible) | FIXED — token corrected; vocabulary-authority test asserts every frozenset member is actually emitted in production source (orchestrator-verified: member mutation fails 5 tests) |
| B-B1 | HIGH | reload-suppression gap | bucket Number keys absent from OPTIONS_RELOAD_SUPPRESS_KEYS → every knob turn reloaded the whole CM | FIXED — 3 keys added to both sets; membership tests extended (74→77) |
| B-B2 | HIGH | #10 restore ordering | acked-episode skip in `_recover_state_from_db` was dead (registry restores AFTER recovery) | FIXED — late cancel in `restore_persistence_state`; real-ordering behavioral test; DB `acknowledged=0` filter remains primary |
| C-HIGH-2 | HIGH | rate-cap semantics | `_channel_ready` inside person loop → N-person token multi-burn; burns on no-send paths and under dry-run | FIXED — hoisted single take per channel post-pref-checks; dry-run evaluates but does not burn (orchestrator-verified: always-True mutation fails 3 tests) |
| C-HIGH-1/B-B5 | HIGH | #53 computed-not-consumed | overflow deque never drains; comments/plan promised lazy drain | FIXED as honest drop counter (`overflow_dropped_total` + recent-drops ring); provenance fixed; plan acceptance amended; real drain DEFERRED to Cycle C |
| B-B4 | MEDIUM | gating window | dry-run only restored via entity sync → boot/reload window where NM believes dry-run OFF | FIXED — switch writes CONF_NM_DRY_RUN to CM options (reload-suppressed); NM reads at construction |
| B-B3 | MEDIUM | unbounded growth | ack registry never pruned | FIXED — 20-episode cap |
| C-MED-1 | MEDIUM | knob doc | refill-rate-0 kill-switch undocumented | FIXED — documented on Number |
| C-MED-2 | MEDIUM | test gap | no end-to-end capacity test (why C-HIGH-2 escaped) | FIXED — behavioral 3-person/capacity-2 test |
| A-MED-1 | MEDIUM | #22 | dead `"co"` frozenset entry | FIXED — removed |
| A-MED-2 | MEDIUM | policy | `overheat`/`high_co2` CRITICAL not life-safety-cadenced — unratified | OPEN QUESTION recorded in plan for operator |
| B-B6/C-LOW-1 | LOW | #26 wall-clock | refill uses utcnow not monotonic (clamped; caps bound damage) | DEFERRED |
| C-LOW-2 | LOW | bounded residue | `_boot_settle_seen` never cleared post-window | DEFERRED |

## Statistics

| severity | found | fixed | deferred/open |
|---|---|---|---|
| CRITICAL | 1 | 1 | 0 |
| HIGH | 4 | 4 | 0 |
| MEDIUM | 6 | 5 | 1 (policy question) |
| LOW | 2 | 0 | 2 |

## Verification

- Reviewer C gate-completeness table: 8/8 outbound sites gated/justified; 5/5
  mutations killed by named tests.
- Orchestrator personally re-ran two mutations post-fix-up: frozenset member
  `"intruder"→"intrusion"` (5 tests fail — note: first attempt hit a comment
  and stayed green; always mutate the load-bearing token, not the first grep
  hit) and `_take_channel_once` always-True (3 tests fail). Restored green.
- Full suite 7275 passed; failure/error set = pre-existing baseline (36+14).

**Bug-class note:** A-CRIT-1 is Bug Class #22's hand-authored-vocabulary flavor;
the vocabulary-authority test (assert every set member is emitted somewhere in
production source) is the reusable countermeasure — candidate pattern for other
hand-copied token sets (QUALITY_CONTEXT update recommended).
