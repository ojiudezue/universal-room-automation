# v4.3.1 Code Review — Tech Debt Cleanup

**Tier:** 1 (Hotfix — 3 mechanical cleanups, no new features, no DB schema changes)
**Date:** 2026-05-06
**Pre-review baseline tag:** `pre-review-v4.3.1`

## Scope

Three deferred items from v4.2.29 + v4.3.0 review docs, batched into one Tier 1 hotfix:

1. **A1**: `DEFAULT_*_ENTITY` structural removal (full deletion of 13 envoy placeholder constants + None-handling at consumer sites).
2. **A2**: Magic-string error codes → `Final` constants in `energy_const.py`.
3. **A3**: Repair issue `is_fixable=True` + fix-flow handler in new `repairs.py` module.

Files touched (post-review):
- `domain_coordinators/energy_const.py` — delete 13 constants + `_UNCONFIGURED_ENVOY` block; add 5 `ENVOY_ERR_*` constants
- `domain_coordinators/energy.py` — None-safe `_get_state_float`, refactor 6 `_get_lifetime_*` methods, None-guard `_crosscheck_consumption` and `_get_battery_capacity_kwh`
- `domain_coordinators/energy_battery.py` — `_get_entity(default=None)`, None-safe `_get_state_*`, drop default args from 4 envoy properties
- `domain_coordinators/energy_billing.py` — drop `or DEFAULT_*` fallbacks; None-guard `_get_net_power`
- `domain_coordinators/energy_forecast.py` — drop envoy fallbacks; `_get_float` accepts `str | None`
- `repairs.py` — NEW (~130 LoC) — `EnvoyValidationRepairFlow` + `async_create_fix_flow`
- `__init__.py` — `is_fixable=True`, `data={"entry_id": ...}`
- `strings.json` — new `fix_flow.step.confirm` + `fix_flow.error.envoy_validation_still_failing`
- `config_flow.py` — import + use `ENVOY_ERR_BASE_DERIVED_MISSING`
- `quality/tests/test_energy_battery.py` — test-local fixture constants + harness `entity_config` wiring
- `quality/tests/test_energy_consumption.py` — test-local fixture constants + `_make_predictor` explicit kwargs

## Reviews completed (per CLAUDE.md Tier 1 protocol)

### Single adversarial review

| Severity | Bug Class | Finding | Status |
|---|---|---|---|
| (none CRITICAL) | — | — | — |
| (none HIGH) | — | — | — |
| MEDIUM | RepairsFlow lifecycle | `EnvoyValidationRepairFlow.__init__()` didn't call `super().__init__()` — base class internal state may not initialize | **Fixed** — added `super().__init__()` |
| MEDIUM | #19 Untracked Tasks | `repairs.py:88-91` used `async_create_task(async_reload(...))` — orphan if user closes repair UI mid-reload | **Fixed** — switched to `async_create_background_task` with named task `ura_envoy_repair_reload_<entry_id>` |
| MEDIUM | RepairsFlow API | `ConfirmRepairFlow()` no-args constructor flagged as uncertain | **Accepted** — standard HA pattern, the class takes no constructor args by design (it's an instance-state-free confirm/cancel flow) |
| LOW | Test maintenance | `test_envoy_auto_derive.py` asserts against magic strings instead of imported `ENVOY_ERR_*` constants | **Deferred** — string values unchanged, tests still pass; can be cleaned up opportunistically in any future cycle that touches these tests |
| LOW | Documentation | `_get_entity` docstring doesn't note the v4.3.1 signature change (default arg now optional) | **Deferred** — minor doc improvement, signature change is backward-compatible |
| LOW | False positive | f-string `eid or "(not configured)"` flagged but is correct Python idiom for None-handling | **No action** — not a bug |

**Verdict (post-fix): READY TO DEPLOY.**

### Bug class summary

| Bug Class | Triggered? | Notes |
|---|---|---|
| #1 (lifecycle) | No | Fix flow opens during runtime; doesn't add a coordinator |
| #11 (UTC vs local timezone) | No | No datetime in diff |
| #19 (untracked tasks) | **Was triggered** | Fixed via named `async_create_background_task` |
| #21 (timezone naive/aware) | No | No datetime |
| #25 (unbounded DELETE) | No | No DB ops |
| #27 (orphaned cleanup) | No | Repair issues now have a fix flow + the v4.2.29 unload-time delete still applies |
| #28 (sync update_listener) | No | No new listeners |
| #29 (stub sensor) | No | No new sensors |

## Live validation plan (post-deploy)

After HA restart:

1. **HACS install verification** (per `feedback_verify_hacs_install.md`): confirm `installed_version: v4.3.1` via `ha_hacs_repository_info` BEFORE declaring success.
2. **A1 transparency**: `sensor.ura_energy_coordinator_battery_strategy` continues to produce normal values for the user's valid config — A1 should be invisible at runtime when validation passes.
3. **A1 grep verification**: `grep '_UNCONFIGURED_ENVOY' /config/custom_components/universal_room_automation` returns 0 hits.
4. **A2 logs**: any envoy validation failure produces the same error codes as before (since string values unchanged).
5. **A3 fix flow (manual, optional)**: temporarily clear `energy_envoy_entity` to trigger the repair issue → "Fix" button now appears in HA Repairs UI → modal opens with description text → set envoy entity correctly in another tab → submit → issue auto-clears + integration reloads automatically.

## Verdict

**READY TO DEPLOY** after the 2 MEDIUM fixes applied. All findings either resolved or explicitly deferred with reasoning. 0 CRITICAL / 0 HIGH.

## Tests

- All 112 tests pass (22 envoy + 60 battery + 30 consumption — no regressions vs `pre-review-v4.3.1`).
- AST clean for Python 3.9.
- No new tests added (mechanical cleanup; existing tests cover consumer behavior. New test coverage for the fix flow itself is deferred — it's a UI flow that's hard to unit-test without HA's full test infrastructure; live validation suffices for hotfix scope).

## Statistics

| Metric | Value |
|---|---|
| Files modified | 11 (production + test + docs) |
| Files created | 2 (`repairs.py`, this doc) |
| LoC removed | ~50 (the 13 deleted constants + their usage patterns) |
| LoC added | ~180 (`repairs.py` + None-safety + fix-flow strings + test fixtures) |
| Reviews | 1 (Tier 1) |
| Findings: CRITICAL/HIGH/MEDIUM/LOW | 0/0/3/3 |
| Findings fixed in cycle | 2 MEDIUM (super().__init__, untracked task) |
| Findings deferred | 1 MEDIUM (ConfirmRepairFlow — accepted as standard pattern), 3 LOW |

## Draft commit message

```
v4.3.1: Tech debt cleanup (A1: DEFAULT removal, A2: error consts, A3: fix flow)

domain_coordinators/energy_const.py: delete 13 envoy DEFAULT_*_ENTITY
constants + the _UNCONFIGURED_ENVOY placeholder block. Add 5 ENVOY_ERR_*
Final constants for validator error codes (string values unchanged so
strings.json keys still match).

domain_coordinators/energy_battery.py: _get_entity now has optional
default=None; _get_state_* helpers accept str | None and short-circuit
to None. The 4 envoy properties drop the second arg.

domain_coordinators/energy.py: 7 ec.get(CONF_X, DEFAULT_X) patterns
simplified to ec.get(CONF_X). Six _get_lifetime_* methods refactored to
use _get_state_float (DRY). _crosscheck_consumption + _get_battery_capacity_kwh
None-guarded.

domain_coordinators/energy_billing.py: _net_power_entity / _solar_entity
no longer fall back to defaults; _get_net_power None-guarded.

domain_coordinators/energy_forecast.py: battery_soc / battery_capacity
entities no longer fall back; _get_float accepts str | None.

repairs.py (NEW): EnvoyValidationRepairFlow opens a modal, re-runs
validate_envoy_config, deletes the issue and reloads the entry on pass.
async_create_fix_flow dispatches by issue id prefix.

__init__.py: is_fixable=True; data={"entry_id": entry.entry_id} added
to ir.async_create_issue. strings.json: new fix_flow block.

config_flow.py: import + use ENVOY_ERR_BASE_DERIVED_MISSING.

quality/tests: test-local fixture constants + harness entity_config
wiring; production no longer leaks fixture defaults.

Tier 1 review: 0 CRITICAL, 0 HIGH, 3 MEDIUM (2 fixed: super-init,
untracked task; 1 accepted: ConfirmRepairFlow standard pattern), 3 LOW
deferred.

Closes deferred items from v4.2.29 + v4.3.0 reviews.
```
