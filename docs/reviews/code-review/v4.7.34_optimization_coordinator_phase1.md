# Code Review: v4.7.34 — Optimization Coordinator Phase 1 (Tier 2-DB)

**Tier:** Tier 2-DB — three framing-disjoint reviews + live validation (operator-elevated from cycle one; DB triggers also fire: new `optimization_findings` table + new write site).
**Baseline tag:** `pre-review-v4.7.34`
**Build commit:** `b94a8ae` (D1–D8 skeleton) → fix-up `5c88d9d`
**Planning doc:** `docs/planning/PLANNING_OPTIMIZATION_COORDINATOR_v2_agentic.md`
**Date:** 2026-06-09

---

## Summary

Phase 1 delivers an end-to-end agentic optimizer running at **L1 (Shadow)** by default: substrate read → Tier-1 rule engine (Sensor-Health + Comfort dimensions) → autonomy-matrix gate → handshake broker → `optimization_findings` DB → sensors/NM. No real actuation at the default ship rung. ~3,380 LoC across 12 files (1,325 in `optimization.py`, 1,014 test).

Three parallel reviews were run with disjoint framings:
- **Review A** — Cross-coordinator handshake + actuation safety
- **Review B** — Autonomy ladder + kill-switch integrity
- **Review C** — DB schema + findings DAO + new surfaces + test fixture authority

The framing-disjoint protocol earned its keep: each reviewer surfaced CRITICALs the others did not look for. **6 CRITICAL, 11 HIGH** found; all fixed in-cycle (fix-up `5c88d9d`). One cross-review conflict adjudicated (see below). Full suite parity preserved (5327 passed / 44 failed / 14 errors — all failures pre-date this cycle in unrelated files). Optimizer test file: 20 → 40 passing.

**Recommendation: FIX-THEN-SHIP — fixes applied; ready for deploy + Review D.**

---

## Cross-review conflict adjudicated

**Review C (C-MED-3)** recommended *removing* `RestoreEntity` from `OptimizerKillSwitch`, citing plan line 346. **Review B (C2)** recommended *keeping* it and making restore fail-closed. Plan line 346 is about the **comfort sliders**, not the kill switch; plan D2 line 248 explicitly mandates `RestoreEntity` on the kill switch ("belt-and-suspenders"). **Ruling: keep RestoreEntity, apply Review B's fail-closed restore.** Review C misattributed the line. This is exactly the kind of blind-spot divergence the three-framing protocol exists to surface.

---

## Findings

### CRITICAL (6 — all FIXED)

| ID | Bug class | Finding | Fix |
|---|---|---|---|
| A-CRIT-1 | Phantom hass.data slot (new; sibling of #7 stale data) | `_get_arrester` read `hass.data[DOMAIN]["hvac_coordinator"]`, a slot **nothing ever writes** → HVAC suppress handshake silently no-ops; test masked it by hand-wiring the slot. Latent at L1, breaks at first L2. | Resolve via `coordinator_manager.get_coordinator("hvac")`; test rewired to exercise the real lookup (`test_optimizer_arrester_via_coordinator_manager`). |
| A-CRIT-2 | Cross-coordinator state-precedence | EgressManager `is_paused(zone_id)` guard (plan §133 mandatory) absent before HVAC actuation. | Egress check at top of `_dispatch_device_action` for climate targets → `OUTCOME_DISALLOWED reason=egress_paused`; `test_optimizer_skips_actuation_when_egress_paused`. |
| C-CRIT-1 | #46 (reload storm) | 6 optimizer CONF keys absent from `OPTIONS_RELOAD_SUPPRESS_KEYS` → **every kill-switch flip / autonomy change full-reloaded the CM entry (~90 entities). Active at L1, not latent.** | Keys added to suppress set + `_NO_LIVE_ATTR_KEYS`; `test_options_reload_suppress_includes_optimizer_keys`. |
| C-CRIT-2 | #44 (test fixture authority) | D4 DAO tests hand-wrote raw INSERT/DELETE instead of driving `log_finding`/`prune` — the v4.6.1.1 masking shape. | Both tests rewritten to drive the real DAO against the fixture-extracted schema (StrEnum coercion exercised). |
| B-C2 | RestoreEntity split-brain (#46-adjacent) | Kill-switch restart-restore could fail **open** (released) if options drifted False while last entity state was ON. | Restore now engages on EITHER source (fail-closed) + unconditional `async_write_ha_state`; `test_optimizer_kill_switch_split_brain_fails_closed`. |
| B-C3 | TOCTOU on autonomy gate | Kill-switch flip during an in-flight L3 veto window (≤30s) did not abort the subsequent dispatch. | Post-`await_veto` live re-read of effective level in both dispatch paths → `OUTCOME_KILL_SWITCH`; `test_optimizer_kill_switch_aborts_after_veto_window`. |

### HIGH (11 — all FIXED unless noted)

| ID | Bug class | Finding | Fix |
|---|---|---|---|
| A-HIGH-1 | Silent-failure-into-actuation | `fire_intent` swallowed dispatcher failure then actuated anyway. | Returns bool; failure → WARNING + `OUTCOME_FAILED`, no service call. |
| A-HIGH-2 | Untracked-growth | `_pending_vetoes` unbounded on unmatched vetoes. | `{action_id: (ts, vetoed_by)}` + 5-min TTL eviction + 256 cap. |
| A-HIGH-3 | TOCTOU state leak | Late veto could stick after successful dispatch. | `discard_pending(action_id)` on success path. |
| A-HIGH-4 | Dead-code-in-default-path | Phase-1 dimensions emit `proposed_action=None`, so `_apply_action` never runs in live L1 → chokepoint untested by live traffic until Phase 2. | DOCUMENTED + `test_optimizer_l1_synthetic_proposed_action_is_inert` drives a synthetic action through the chokepoint at L1, asserts intent emitted + zero service calls. README caveat. |
| B-H1 | Gate-applied-late | Confidence gate bypassed when `proposed_action is None`. | Gate applied uniformly in `_consider_apply` before the proposed-action branch. |
| B-H2 | State-on-restart cap-bypass | Rate-cap deque reset to 0 on restart → cap bypass. | Deque seeded from DB (`applied` rows in rolling hour) at setup. |
| B-H3 | Outcome-shape lossy | `RATE_CAPPED` / `QUIET_CLAMPED` outcomes never written; clamps indistinguishable from normal shadow rows. | `_resolve_effective_level()` returns `(level, clamp_reason)`; outcomes + activity-log reasons wired. |
| B-H5 | Numeric clamp edge-cases | ±20% clamp silently clamped-to-0 on zero current, no-clamp on None current, ignored entity min/max. | Rejects None/unavailable (`OUTCOME_FAILED`), refuses zero, intersects band with entity `min`/`max`. |
| B-H6 | #46 re-entrancy | `effective_level` re-read per finding (mid-cycle race + cost). | Single `_resolve_effective_level()` per `_apply_action`; deeper whole-cycle snapshot deferred (kill-switch must stay live per B-C3) — see follow-ups. |
| C-HIGH-2 | #50 + #44 | `test_optimizer_sensor_subscriptions_survive_rebuild` was vacuous. | Rewritten to drive a real signal through the surviving sub. |
| C-HIGH-3 | #46 (reload thrash) | Comfort-slider write-back full-reloaded the **room** entry (room entries weren't in any suppress path). | New `ENTRY_TYPE_ROOM` branch + `_ROOM_SUPPRESS_KEYS` in `_async_update_listener`. |

### MEDIUM / LOW (fixed in-cycle per "Fix LOWs In-Cycle")

- **M1 / C-MED (NM private method):** added public `NotificationManager.is_quiet_hours_active()`; optimizer calls the shim, not `_is_quiet_hours()`.
- **C-MED-1 (None coercion):** `log_finding` rejects None dimension/severity with WARNING (sibling of v4.7.12 C-M2).
- **Broker private→public:** `suppress_climate`/`unsuppress_climate`/`discard_pending` (old names aliased for test seams).
- **B-C1 (per-dim cap is ceiling-only):** documented in the per-dimension cap docstring.

### DOCUMENTED-DEFERRED (tracked, not silently dropped)

| Item | Why | Track |
|---|---|---|
| C-HIGH-1 — `CONF_OPTIMIZER_DIMENSION_AUTONOMY` has no config-flow UI selector | Read path works via raw options key; a per-dimension selector matrix + strings/en.json is its own surface. Parsimony. | Phase-1 follow-up |
| B-H6 deeper — whole-cycle effective-level snapshot threaded through `_consider_apply` | Kill-switch check must remain live (B-C3); per-finding single-read already removes the worst case. | Follow-up |
| A-HIGH-4 finalizer — remove test-only `noqa` hooks | Become production when Phase-2 LLM lands. | Phase 2 |

---

## Summary statistics

| Severity | Found | Fixed in-cycle | Documented-deferred |
|---|---|---|---|
| CRITICAL | 6 | 6 | 0 |
| HIGH | 11 | 10 | 1 (A-HIGH-4 doc+test; B-H6 partial) |
| MEDIUM | ~6 | 6 | 0 |
| LOW | ~8 | cheap ones | rest noted |

## Bug-class frequency

| Bug class | Count | IDs |
|---|---|---|
| #46 async_update_entry re-entrancy / reload | 4 | C-CRIT-1, B-C2, B-H6, C-HIGH-3 |
| #44 test fixture authority | 2 | C-CRIT-2, C-HIGH-2 |
| TOCTOU on autonomy/cross-coord state | 3 | B-C3, A-HIGH-2, A-HIGH-3 |
| Cross-coordinator handshake integrity | 2 | A-CRIT-1, A-CRIT-2 |
| Outcome/observability shape | 2 | B-H3, A-HIGH-4 |
| Numeric edge-cases | 1 | B-H5 |
| #50 subscription survival | 1 | C-HIGH-2 |

---

## QUALITY_CONTEXT.md recommendation

Candidate **new bug class — "Phantom `hass.data` slot"** (A-CRIT-1): code reads a `hass.data[DOMAIN][key]` slot that no code path ever writes; the call silently degrades to a no-op, and a test that pre-populates the slot masks the gap. Sibling of #7 (stale data source). Add after live validation confirms the pattern is worth formalizing. The reload-storm findings reinforce **#46**; no new class needed there.

---

## Live validation (Review D)

Populated post-restart in `docs/readmes/README_v4.7.34.md` per the 2026-06-05 README-writeback rule. Key sentinel checks: (1) `sensor.ura_optimizer_status.state` reaches `healthy` ≤5 min; (2) `optimization_findings` carries non-meta rows with non-NULL severity/dimension within 24h (sentinels-only = broken); (3) kill-switch flip does NOT trigger a CM reload (tail `_async_update_listener` for the suppress line); (4) zero URA ERRORs post-boot attributable to `optimization`.
