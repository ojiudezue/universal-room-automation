# v4.7.7 Tier 2 Review A — Correctness + State-Machine + B2 Root-Cause Quality

**Reviewer:** A (Tier 2 parallel)
**Cycle:** v4.7.7 AC Nudge / AC Reset Decouple + DPM Sensor Cleanup
**Commit reviewed:** `771630c` (= `pre-review-v4.7.7`)
**Diff command:** `git diff pre-review-v4.7.7^..pre-review-v4.7.7`

**Verdict: APPROVE WITH FIXES.**

---

## Summary

| Severity | Count |
|----------|------:|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 2 |
| LOW | 6 |
| NIT | 3 |

---

## Headline findings

### A-M1 (MEDIUM) — `canonical_label_mismatch` precedence mislabels skip reasons

Location: `energy.py:2764-2768`.

When the " + " split fails BUT the zone_id fallback at line 2723-2724 supplies non-empty data, the current precedence rule overwrites the real downstream reason (e.g., `dwell_pending`, `home_range_not_configured`) with `canonical_label_mismatch`. This defeats the entire purpose of B2 root-cause diagnosis — the sensor will report a misleading reason when the real skip cause was something else entirely.

**Fix:** gate the precedence with `if _canonical_resolution_failed and not zone_data:`. 4-LoC fix. Add a regression test that exercises the path where canonical resolution fails but zone_id fallback succeeds — verify the downstream reason is preserved.

### A-M2 / L1 (MEDIUM-effective) — `force_reset` button referenced but doesn't exist

Two locations reference a `force_reset` button that doesn't exist:
- `hvac_override.py:866-868` (docstring)
- `hvac_override.py:897-898` (inline comment in Gate 0b)

In cell (N=OFF, R=ON), AC Reset is functionally inert today — no path to invoke a hard reset because soft-nudge auto-detection is skipped at Gate 0b and no manual button bridges to `_perform_hard_reset_escalation`. Either strike the misleading references (recommended for v4.7.7) or implement the button (v4.7.8 backlog).

**Fix:** strike refs in v4.7.7; file force_reset button as v4.7.8 candidate if wanted.

---

## State-Machine Table (4-cell × 7-event matrix)

| AC Nudge | AC Reset | Auto 5-min tick | Manual force_nudge | Manual force_reset (n/a today) | Day rollover | Restart, both toggled | In-flight nudge, Nudge→OFF | Mid-cycle Reset OFF |
|---|---|---|---|---|---|---|---|---|
| ON | ON | Full chain: detect → eval → escalate | Soft nudge fires; eval+escalation if fails | n/a | Prune events | Both restored | Restore timer completes, no cancel (per plan) | Live re-read at escalation → A3 IDLE |
| ON | OFF | Detect → eval → A3 guard IDLE on escalation | Soft nudge fires; eval; A3 guard IDLE | n/a | Prune events | Both restored | Restore timer completes | n/a |
| OFF | ON | **Gate 0b: skip soft-nudge.** Hard reset functionally unreachable today (no force_reset button) | Path TBD (M2 finding — currently undocumented) | n/a | Skipped | Both restored | Restore timer completes | n/a |
| OFF | OFF | Gate 0a: full skip | Path TBD (M2 finding) | n/a | Skipped | Both restored | Restore timer completes | n/a |

State-machine completeness verified for all 4 cells × the 7 event rows above. Plan §10 item 1 requirement satisfied.

---

## A3 lockout-removal trace — CLEAN

With `_ac_reset_enabled=False` and `_perform_hard_reset_escalation` called:
- ✅ No `_db.save_ac_reset_state` call
- ✅ No `_db.log_ac_ramp_event(lockout_triggered=True)` call
- ✅ No `_engage_lockout` call
- ✅ No DB reads at all (early return at line 1539 precedes the DB-fetch block at line 1546)
- ✅ `zone.ramp_state = AC_RAMP_STATE_IDLE` set
- ✅ Legacy `_hard_reset_daily_limit=0` lockout-on-first-eval bug is now harmless because the A3 early-return at line 1539 precedes the `>= 0` check at line 1548

---

## Bug Class #20 — Gate 0 snapshot — VERIFIED

`_nudge_on` and `_reset_on` captured into locals at `hvac_override.py:891-892`. Zero re-reads of `self._ac_nudge_enabled` / `self._ac_reset_enabled` inside the function body through line 1020. Snapshot held consistently across the tick.

`_perform_hard_reset_escalation` re-reads `_ac_reset_enabled` fresh at line 1532 — **correct per plan intent.** Mid-flight toggle should affect deferred escalation (the 10-min outcome eval is a separate decision boundary, not a snapshot violation). Worth a 1-line code comment clarifying the deliberate intent (Reviewer B noted as B-L1).

---

## A4 ramp-sensor migration — CORRECT

- Renames the two no-LTS sensors (state, last_action)
- **Correctly excludes `hvac_ac_ramp_kwh_rate`** (has `SensorStateClass.MEASUREMENT` at `sensor.py:9006`, NOT `:8968` as the comment in `__init__.py:2538` says — LOW L7 finding)
- Idempotent (skip if entity_id already canonical)
- Slug-collision guard present

---

## B1 orphan sweep — CORRECT

- Strict-prefix exclusion of `dynamic_preset_active_bucket_*` (lines 2509-2511) prevents sweeping live entries
- Platform guard against non-URA platforms
- Mutate-during-iterate avoided via materialized `_to_remove` list

---

## B2 instrumentation surface — COMPLETE (modulo A-M1)

Six taxonomy labels reachable in `dynamic_preset.py`; two caller-side labels (`canonical_label_mismatch`, `evaluation_failed`) added in `energy.py`. Sensor exposes both `skipped_zones` (back-compat) and `skipped_zones_with_reason` (new). Bug Class #45 safe (dict-comprehension keyed by `zone_id`).

**Caveat A-M1 above:** precedence rule overwrites real reasons in the zone_id-fallback-succeeded case. Fix before deploy.

---

## B3 device migration — CORRECT

All three classes use `_hvac_device_info()`. Migration list extended with static + per-zone (via `iter_canonical_hvac_zones`) entries. Entity_ids preserved → LTS history preserved.

---

## Tests

- 81 passed / 3 skipped (HA-import-required, appropriately gated)
- All passing under `PYTHONPATH=quality python3 -m pytest quality/tests/test_v477_*.py -q`

---

## Pre-deploy fix-up list (Reviewer A)

1. **A-M1** — tighten `canonical_label_mismatch` precedence (4 LoC + regression test)
2. **A-M2 / L1** — strike `force_reset button` references (4 LoC, 2 locations)
3. **A-L7** — correct `sensor.py:8968` → `sensor.py:9006` stale line ref (1 LoC)
4. **A-L8** — README_v4.7.7.md note about intentional kwh_rate scrambling residual (2 sentences)

**Defer to backlog:**
- L2 (zone.ramp_state preservation on Gate 0b)
- L3 (force_nudge bypasses N flag)
- L4 (rollover skipped when gated off)
- L5 (silent Gate 0a return)
- L6 (docstring precedence clarity)
- L9 (`energy_coordinator` literal in entity_id)

---

## Bug-class regression sweep

None re-introduced. #20, #43, #44, #45, #47 clean. #46 placement evidence is clean on Reviewer A's surface; Reviewer B owned the formal #46 audit (also clean).
