# v5.72.0 — FAN-LAYER-2 (Tier 2-DB) + SUITE-HYGIENE-1 (Tier 1) Review Record

## FAN-LAYER-2

Plan: 3 revisions / 2 adversarial plan reviews (round 1 completeness, round 2 build-prediction).
Build: staged D1/D2 with honest pre-trigger checkpoint. Code reviews: A SHIP, B DO-NOT-SHIP,
C DO-NOT-SHIP (authored anchors). Consolidated fix-up f2f0d6fc2. Orchestrator re-drills: W1
wrap + (pre-fix-up) MUT-STOP-class checks.

### Findings

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| PLAN-R1-HIGH-1 | HIGH (plan) | §5.2 key amendment would orphan every live ledger row at switchover + re-open name collisions | Key-scheme migration blindness | ✅ rev-2 Option A (prefixed) |
| PLAN-R1-HIGH-3 | HIGH (plan) | Plan claimed grep-verified line refs; W1/W2 pointed at cover-close code | **Plan-layer hollow-anchor** (claimed-verification-not-run) | ✅ rev-2 re-grep |
| PLAN-R2-HIGH-1 | HIGH (plan) | Plain-class `__init__` as specced TypeErrors 10 existing constructors incl. parity gates | API-narrowing | ✅ rev-3 13-field kw-only |
| B-HIGH-1 | HIGH | `migrate_legacy_entry_keys` built but NEVER WIRED — live-upgrade reload orphans holds. Reviewer's own quick fix (`{}` mapping) would have DROPPED holds; orchestrator corrected to real entry→room mapping | Lifecycle-state loss + computed-but-not-consumed (#53) | ✅ wired from discover_fans w/ real map |
| A-MED-1 | MED | Migration collision last-writer-wins could overwrite fresh row with stale | Merge-direction | ✅ field-wise MAX |
| B-MED-1/A-LOW-2 | MED/LOW | Divergent per-tier fallbacks on control-char names (shared root fix: sanitize not raise) | Fail-mode divergence | ✅ |
| C-HIGH (blanket) | HIGH | ALL 7 new wraps shipped scan-verified only — semantic neuter left suite green (builder disclosed honestly) | **Hollow anchors #14-era** | ✅ C authored W8/W9/W4; fix-up built RoomAutomation harness + W1/W2/W3×2 anchors; 11/12 drills load-bearing, 1 disclosed non-load-bearing (D11, oracle-side locked) |
| A-LOW-1/3, B-LOW-1 | LOW | Descriptor type coercion; sleep-axis unreachable on dynamic trigger strings; orphan rows on rename (deferred, carded) | various | ✅/⏸ |

### Statistics
| Sev | Found | Fixed | Deferred |
|---|---|---|---|
| HIGH | 5 (2 plan + 3 code incl. blanket) | 5 | 0 |
| MED | 3 | 3 | 0 |
| LOW | 5 | 4 | 1 (B-LOW-1 orphan rows — carded) |

### Bug-class notes
- **Hollow anchors continue as the dominant class** — but the pipeline now catches them
  pre-ship every time (builder self-disclosure + Reviewer-C re-execution + orchestrator
  re-drill). New wrinkle this cycle: the PLAN itself exhibited the pattern (claimed greps not
  run) — plan reviews now check verification claims, not just content.
- **Reviewer remedies need review too**: B-HIGH-1's suggested one-liner would have deleted the
  data it meant to save. Orchestrator adjudication of remedies is load-bearing.

## SUITE-HYGIENE-1

Tier 1, one review: SHIP w/ 2 LOWs (fixed in-cycle: probe-trio anchor + QUALITY_CONTEXT #44
update). Census: 155 files / 1292 writes / 98 keys. Determinism: 3× byte-identical failure
lists (sha ce3a35f7…). Honest scope cut: pollution-DEPENDENT tests (7-fix/7-break measurement)
→ SUITE-HYGIENE-2 card.

## Open organic
- v5.72.0 L4 = v5.68.0 L2: the operator's manual Living Room fan-ON test (now proves both tiers).
