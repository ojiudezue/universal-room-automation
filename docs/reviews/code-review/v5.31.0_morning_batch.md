# Code Review — v5.31.0 morning fix batch (2026-07-26)

Four Tier-1-sized fixes touching regression-prone / shared surfaces (arrester primitive,
presence census, presence↔HVAC fan seam) → reviewed with **3 framing-disjoint passes +
a fix-up + orchestrator independent mutation verification**.

## Fixes reviewed
1. AC kWh Avoided → daily accumulator (database.py, hvac_override.py, sensor.py)
2. HVAC override self-count B1 (kind-tagged suppression) + B2 (preset-preserving restore) (hvac_override.py)
3. Fan room-tier manual-off cooldown + reconciler defer + mismatch diagnostic (automation.py, const.py, hvac_fans.py, actuator_reconciler.py)
4. Census face cross-check (camera_census.py)

## Reviews

| Pass | Framing | Verdict | Headline |
|---|---|---|---|
| Avoided (pre-batch) | Tier-1 single | SHIP | Monotonic/DST/restart correct; 1 MED = grep-only tests |
| A | local correctness + edge cases | SHIP | 0 CRIT/HIGH; fixes fail-safe; 1 MED + 6 LOW |
| B | cross-coordinator / lifecycle / shared-primitive | HOLD→fixed | **HIGH-1** reconciler re-arms room-tier fan behind the cooldown; `suppress()` signature change clean; "45-vs-82" mystery = `test_freeze_floor` ordering pollution, not lost coverage |
| C | test authority via source mutation | HOLD→fixed | **HIGH-C1** face guard tested vs a hand-copied stub; **HIGH-C2** B2 restore tested by source-greps only |

## Findings & disposition

| ID | Sev | Bug class | Disposition |
|---|---|---|---|
| B-HIGH-1 | HIGH | cross-coordinator / computed-but-not-consumed | **FIXED** — `_resolve_fan` defers via new `RoomAutomation.is_fan_in_manual_cooldown()` while cooldown live; mutation-anchored `test_fan_reconcile_defers_when_room_tier_cooldown_live` |
| C-HIGH-1 | HIGH | tautological fixture (~#10) | **FIXED** — new `test_face_cross_check_behavioral.py` drives real `PersonCensus._get_face_recognized_person_names`; fail-on-neuter verified |
| C-HIGH-2 | HIGH | grep-only test | **FIXED** — `TestRestoreAfterNudgeBehavioral` drives real `_restore_after_nudge`; fail-on-neuter verified |
| B-LOW-1 | LOW | stale test window | **FIXED** — `test_v4512` impact_cache slice 6000→8000 (the one net-new failure) |
| A/B-MED | MED | happy-path narrowing | **ACCEPTED + documented** — genuine user manual flip inside the ≤5 s post-nudge window is swallowed; alternative = 85 false counts/night |
| A-LOW ×6 | LOW | cosmetic | Deferred (dead `_fan_off_issued_this_tick`; hvac_fans kill-switch log; slug re-lower; away-zone fail-open miss; B2 kind overwrite — all benign) |

## Orchestrator independent verification (not trusting summaries)
Personally re-ran source mutations on the two most load-bearing sites:
- Neutered arrester B1 `_suppress_kind == "temp"` (hvac_override.py:705) → `test_induced_manual_under_temp_suppression_stays_suppressed` **failed** ✓
- Neutered reconciler defer (actuator_reconciler.py:789) → `test_fan_reconcile_defers_when_room_tier_cooldown_live` **failed** ✓
Both restored; batch intact.

## Summary stats
- CRITICAL: 0 · HIGH: 3 (all fixed + re-verified) · MED: 1 (accepted) · LOW: 7 (1 fixed, 6 deferred cosmetic)
- Cycle test files: 269 passed. Full-suite deltas = pre-existing `test_freeze_floor` ordering pollution (25/25 in isolation).

## Bug-class notes for QUALITY_CONTEXT.md
- Reinforces **tautological-fixture / grep-only-test** class: a "N tests pass" count overstated coverage on 2 of 4 sites until behavioral tests were added. Recommend a standing rule: every load-bearing fix ships with a fail-on-neuter mutation anchor.
- **Cross-coordinator re-assertion** (reconciler independently re-arming behind a coordinator's cooldown) — same shape as computed-but-not-consumed (#53) inverted: consumed-and-re-asserted-elsewhere. Worth a QC note.
