# Code Review Record — v5.59.0 cycle-3 "resolver-legs"

**Scope:** `build/resolver-legs` — CameraResolver.resolve_detection_legs() replaces three
generations of hand-rolled leg-derivation helpers in perimeter_alert.py
(`_fused_sibling`, `_protect_person_legs`, `_derive_sibling_sensor`); native
Reolink/Amcrest/Dahua AI legs; per-engine disagreement telemetry; kill switch
generalized to `PERIMETER_MULTI_ENGINE_LEGS_ENABLED`.
**Tier:** 2-DB (shared primitive — resolver consumed by census, fused sensors, fan_veto,
now perimeter alerting). Three framing-disjoint reviews in parallel.
**Baseline tag:** `pre-review-resolver-legs` (b78aee02e). Fix-up: `6b0531db9`.
Orchestrator drill-gap fix: `6a9fe95ce`.

## Pre-review incident

The builder worktree contained **leftover mutation-drill edits from a prior Tier-3
drill** (DP L1-only auto-hold `if False:`-ed, DP-yield gate neutered, BLE occupancy gate
altered) — uncommitted, so nothing could ship, but the builder's "matches baseline"
full-suite evidence was collected with those mutations live. Orchestrator restored the
three files and re-ran the suite clean before dispatching reviews: 21 failed / 8302
passed, failing-name set **identical** to the develop baseline (symmetric diff = 0).
Lesson (bug-class candidate, see below): **a drill that isn't restored poisons every
subsequent suite run in that tree** — the restore step is part of the drill, and any
inherited worktree must be `git status`-checked before use.

## Findings

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| A-CRIT-1 / B-HIGH-1 | CRITICAL | Dedup vocabulary lag: resolver learned `_smart_motion_human` but `_camera_key_for_sensor` didn't → Dahua + Frigate legs on same camera get different keys → TWO alerts; also breaks snapshot-key resolution on Dahua-triggered alerts | #53 computed-but-not-consumed (vocabulary fork) | ✅ F1: perimeter suffix set now DERIVED from resolver vocabulary (single source), longest-first; `_smart_motion_animal` added to animal suffixes; collapse test extended w/ Dahua + bare Reolink |
| A-HIGH-1 | HIGH | `EXTERIOR_CAMERA_KEY_ALIASES` missing the Reolink porch entry the commit/docstring claimed bridged | doc-vs-code drift | ✅ F2: alias added |
| A-HIGH-2 / B-HIGH-2 / C-HIGH-2 | HIGH | Kill-switch OFF reverted to *pre-v5.58.0* (base+`_2` only), silently dropping Protect stem-probed legs and Dahua bases — a kill switch that loses shipped coverage | fire-axe scope regression | ✅ F3: OFF path restores v5.58.0-equivalent Protect probe + recognizes Dahua base; contract re-documented; OFF subscription-set tests added |
| C-HIGH-1 | HIGH | Alias-bridge block entirely untested (drill B stayed green — every fixture put legs on the same device_id, so rung-1 masked the alias arm) | untested load-bearing site | ✅ F5: cross-device alias fixture; drill re-run RED |
| C-HIGH-3 | HIGH | Channel-strip divergence (Protect `_high_resolution_channel` camera vs Frigate stem) untested; retired test's scenario dropped | #62 test-env can't exercise path | ✅ F6: divergence fixture |
| C-HIGH-4 | HIGH | 3 load-bearing tests silently SKIP in canonical env (no HA import); telemetry + collapse invariant unpinned | #62 | ✅ F4: HA-stub prelude; module now 0 skips |
| ORCH-1 | HIGH | Post-fix-up drill re-run: flipping the shipped kill-switch default left ALL suites green — OFF-path tests monkeypatched the flag themselves; default + gate site unpinned | untested load-bearing site | ✅ 6a9fe95ce: default anchor (`is True` on const + module) + gate test through `_resolve_legs` with real flag; drill re-run RED both directions |
| ORCH-2 | MEDIUM | F11's coverage-log test was a tautology: it called `_LOGGER.info` itself and asserted on its own output (zero production code exercised) | sham anchor | ✅ 6a9fe95ce: rewritten to pin the production call site + kill-switch gating window |
| B-MED-1 | MEDIUM | Missing-alias failure mode invisible (alias table is a hidden alerting-correctness knob) | observability gap | ✅ F7: per-camera multi-key WARN tripwire in `_wire_camera` |
| B-MED-2 | MEDIUM | "no `_2` sibling" WARN storms at boot on native-AI bases | log hygiene | ✅ F8: gated on Frigate-shaped base |
| C-MED-1 | MEDIUM | Duplicated byte-identical allowlist-install block (paste artifact from the 2026-08-06 interior-leak hotfix) | paste-and-forget | ✅ F9: one block deleted |
| C-MED-2 | MEDIUM | End-to-end wiring (resolver leg → `_on_perimeter_event` → dispatch) uncovered | test authority | ✅ F10 |
| C-MED-3 | MEDIUM | Engine-tagged coverage log unpinned | test authority | ✅ F11 (rewritten per ORCH-2) |
| C-M-2 | MEDIUM | Kill-switch rename anchor was source-string presence only | sham anchor | ✅ F12: import-level `is`-identity assert |
| A-MED-3 | MEDIUM | `_infer_integration`=="" bypassed package filter; silent "unknown" engine | defense-in-depth | ✅ F13 |
| A-MED-4 | MEDIUM | Sole-fire counter incremented per rising edge, not per episode | telemetry inflation | ✅ F14 |
| A-MED-1 | MEDIUM | `_engine_tag` folds `_3+` into `frigate2`/`protect2` | two-host assumption | Deferred — single-deployment; comment anchor added (F16) |
| A-MED-2 | MEDIUM | Exact-stem fusion assumes operator naming discipline | latent | Deferred — documented assumption; multi-key WARN (F7) is the tripwire |
| A-LOW-1 | LOW | Case-sensitive alias lookup | robustness | ✅ F15: lowercased both sides |
| C-LOW-2 | LOW | Wall-clock-coupled sole-firing test | wall-clock-coupled test | ✅ F17: dt injected |
| C-LOW-3 | LOW | No-`_N`→"frigate" fold undocumented | doc | ✅ F16 |
| A-LOW-2..5, B-LOW-1..2 | LOW | Defensive notes (prune ref, sole-only bootstrap branch, suffix-contract doc, OFF-path INFO log silence, late vehicle/animal rescan asymmetry) | — | Deferred, ≤6 entries, tracked here |

## Statistics

| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 1 | 1 | 0 |
| HIGH | 6 | 6 | 0 |
| MEDIUM | 10 | 8 | 2 |
| LOW | 8 | 3 | 5 |

## Mutation ledger (reviewer C + orchestrator re-runs)

| Site | Pre-fix | Post-fix |
|---|---|---|
| resolver family-suffix match (camera_resolver.py:937) | RED (8 tests) | — |
| alias bridge block (camera_resolver.py:896) | **GREEN** → C-HIGH-1 | RED (`test_alias_bridge_pulls_cross_device_native_leg`) |
| kill-switch default flip (const.py) | **GREEN** → C-HIGH-2/ORCH-1 | RED (`test_kill_switch_default_on_and_gates_resolve_legs`) |
| camera-key collapse (engine-suffixed keys, perimeter_alert.py) | RED (`test_triple_fire_...one_alert`) | — |
| disabled-entity exclusion (camera_resolver.py:922) | RED | — |
| perimeter suffix derivation → legacy triple | n/a (new site) | RED (collapse + derivation tests) |

All drills: `PYTHONDONTWRITEBYTECODE=1`, `__pycache__` cleared, restored via
`git show HEAD:`, green re-verified after each.

## Bug-class frequency + QUALITY_CONTEXT recommendation

- #53 computed-but-not-consumed — 1 (the CRITICAL; vocabulary-fork variant)
- Untested load-bearing site (drill-green) — 3 across the cycle (alias, kill switch ×2)
- Sham/tautological anchor — 2 (source-string rename anchor; self-emitted log assert)
- **NEW CLASS CANDIDATE: "Unrestored mutation drill"** — a prior session's drill edits
  left in a worktree poison later suite evidence. Mitigation now standing: (1) drills
  end with `git status` clean-check; (2) any inherited worktree is status-checked before
  building or running suites in it. Recommend adding to QUALITY_CONTEXT.md.
- Recurring: monkeypatch-scoped kill-switch tests don't pin the SHIPPED default — pin
  the default with an `is True/False` anchor whenever a kill switch ships.

## Gate evidence

- Full suite (clean tree, bytecode off): **21 failed / 8315 passed / 45 skipped**;
  failing-name symmetric diff vs develop baseline = **0**.
- `test_resolver_legs.py` standalone: **24 passed / 0 skipped**.
- Conflict-marker grep clean; `py_compile` clean on all touched files.
