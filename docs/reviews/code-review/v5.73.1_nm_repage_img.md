# v5.73.1 — NM-REPAGE-IMG-1 (Tier 1) Review Record

One adversarial review: **SHIP**, 0 CRITICAL, 0 HIGH, 2 LOW — both fixed in-cycle per the
fix-LOWs-in-cycle rule (commit 240ea5337 on top of build commit aee318b0e).

| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| LOW-1 | LOW | `os.path.exists` called directly on the event loop in `_repeat_alert` — a stalled network mount could block the loop | Sync-I/O-on-loop | ✅ `async_add_executor_job` offload; fallback semantics preserved |
| LOW-2 | LOW | Persistence restore merged snapshot fields with no alert-identity check — crash between DB-ack and persistence-save could graft alert A's stale snapshot onto recovered alert B's re-pages | Cross-episode data bleed (latent) | ✅ `active_alert_snapshot_created_at` identity stamp; mismatch or legacy blob → skip merge |

## Verified clean by review
Transport kwarg contracts on all four dispatches (Pushover `attachment_url`, Companion
`data.image`, WhatsApp `media_path`/`media_url`, iMessage BB-v0.6 `attachment`/`media_url`);
every ack/silence/cooldown path drops the whole `_active_alert_data` dict (no snapshot leak);
persistence None/sentinel handling; test authority (tests drive `async_notify → _enter_alerting →
_repeat_alert` production paths, only transport emitters mocked).

## Wire-in anchor drills (2)
1. Build: transport-kwarg neuter → `test_repage_reattaches_snapshot_on_whatsapp_and_imessage`
   red (1 failed / 2 passed) → restored green.
2. Post-fix-up (executor path): same neuter → same test red (1 failed / 4 passed) → restored
   green (5 passed).

## Statistics
| Severity | Found | Fixed | Deferred |
|---|---|---|---|
| CRITICAL | 0 | — | — |
| HIGH | 0 | — | — |
| MEDIUM | 0 | — | — |
| LOW | 2 | 2 | 0 |

Suite: 8767 passed, 21 pre-existing failures (identical set to baseline), 0 new.

## Bug-class notes
Both findings are existing QUALITY_CONTEXT classes (sync-I/O-on-loop; cross-episode/restore
bleed is a cousin of RestoreEntity poisoning #: restore paths must verify identity before
grafting persisted state). No new class proposed.
