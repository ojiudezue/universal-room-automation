# v4.6.8 Code Review — EC TOU Rate Reconciliation + Zone/House Cost Surface

**Tier:** 1 (single staff-engineer adversarial review)
**Date:** 2026-05-18
**Pre-review baseline tag:** `pre-review-v4.6.8`
**Reviewer:** general-purpose adversarial agent (Claude)
**Build:** ura-builder
**Branch:** `feature/v4.6.8-ec-tou-rate-reconciliation`

## Scope

Per `docs/planning/PLANNING_v4.6.8_ec_tou_rate_reconciliation.md`:
- D1: Delete dead sync TOU JSON loader
- D2: New `_get_effective_rate_kwh()` helper
- D3: Migrate 8 cost calculation sites to use helper
- D4: Reword `electricity_rate` UI labels (fallback semantics)
- D5: Two new zone cost sensors (`ZoneEnergyCostTodaySensor`, `ZoneCostPerHourSensor`)
- D6: New `WholeHouseCostTodaySensor`
- D7: BACKLOG.md surgery (5 removed, 3 added)

## Findings — verdicts

| # | Severity | Issue | Verdict | Fix commit |
|---|---|---|---|---|
| HIGH #1 | HIGH | `_get_effective_rate_kwh` step-3 fallback was dead code (`energy_billing.py:71-88`). Loop assumed dict-wrapped values; `__init__.py:612` stores integration entry directly as ConfigEntry. Silently fell through to DEFAULT in any EC-absent path with global override configured. | **FIXED** — read canonical slot `hass.data[DOMAIN]["integration"]` directly | `3f0a015` |
| MEDIUM #2 | MEDIUM | Three `except Exception: pass` blocks silently swallowed errors. Partial `tou_engine` construction would drop to DEFAULT with no log line. | **FIXED** — added `_LOGGER.debug` to all three tiers | `3f0a015` |
| MEDIUM #3 | MEDIUM | `native_value` and `extra_state_attributes` each independently call `_get_effective_rate_kwh()` — microsecond race window across TOU boundary could surface a different rate in the attribute than was used in state. | **DEFERRED** — likelihood sub-millisecond; impact is one cycle of stale `rate_source` attribute (no cost mis-calc). Defer to a polish cycle. | — |
| MEDIUM #4 | MEDIUM | Semantic shift: pre-v4.6.8 room override was always honored; post-v4.6.8 EC's rate wins when configured. Documented in planning doc but lacked a test. | **FIXED** — added `test_effective_rate_ec_wins_over_room_override`; documented in release notes | `3f0a015` |
| LOW #5 | LOW | Function-local imports of `DOMAIN`, `ENTRY_TYPE_INTEGRATION`, `CONF_ENTRY_TYPE` in `energy_billing.py`. Refactorable to module-top. | **DEFERRED** — promote at next touch of this file. Step-3 fix removed two of the three imports as a side-benefit. | — |
| LOW #6 | LOW | Skipped test `test_tou_engine_sync_loader_removed` guards on module-load `ImportError`. Justified but `importlib.util.spec_from_file_location` would let it run in CI. | **DEFERRED** — test-env limitation, not a production bug | — |
| Bug Class #21 | — | Tz-naive datetime audit: no new `datetime.now()` calls; all use `dt_util.now()`. | **CLEAN** | — |
| Bug Class #34 | — | Function-local imports — see LOW #5 | **ACCEPTED** | — |

## Statistics

| | Count |
|---|---|
| Bugs found | 6 |
| CRITICAL | 0 |
| HIGH | 1 |
| MEDIUM | 3 |
| LOW | 2 |
| Fixed this cycle | 3 |
| Deferred | 3 (1 MEDIUM, 2 LOW) |

## Bug class frequency

| Bug class | Count | Notes |
|---|---|---|
| New: "Silent fallback skipping a tier of the resolution chain" | 1 | HIGH #1. **Proposed for QUALITY_CONTEXT.md** — when a multi-tier fallback chain has a tier that's structurally unreachable, the silent-default behavior masks the gap. Detection: write a test for each tier exclusively. |
| New: "Silent failure swallowing programmer error" | 1 | MEDIUM #2. **Proposed for QUALITY_CONTEXT.md** — `except Exception: pass` without even debug-level logging means partial-construction bugs in upstream code never surface. |
| New: "Attribute/state desync from independent recomputation" | 1 | MEDIUM #3. **Proposed for QUALITY_CONTEXT.md** — HA sensor `native_value` + `extra_state_attributes` are called independently; surfaces should cache derived values in `native_value` and read from the cache in attributes, not recompute. |
| #21 (tz-naive datetime) | 0 | Clean |
| #34 (function-local imports) | 1 LOW | Deferred |

## Tests

- New file: `quality/tests/test_v4_6_8_rate_reconciliation.py`
- 18 tests covering D1–D6 acceptance criteria, 17 pass + 1 skip (HA-import env limitation)
- Migrated `test_energy_tou.py` 13 call sites from deleted `from_json_file` to `_read_json_file` + `_from_parsed_data`; all 30 tests pass
- Combined run: **49 passed, 1 skipped, 0 failed**

## Live validation criteria (post-deploy)

Per planning doc acceptance criteria:
- [ ] `sensor.universal_room_automation_predicted_cost_today` attribute `rate_source` shows `"ec_tou"` post-restart
- [ ] When TOU period rolls peak → off-peak, `sensor.<room>_energy_cost_today` value reflects new rate within one cycle
- [ ] `sensor.universal_room_automation_energy_cost_per_occupied_hour` does NOT report a value implying 0.1 $/kWh during any startup transient
- [ ] `sensor.<zone>_energy_cost_today` and `sensor.<zone>_cost_per_hour` enabled-by-default on Zone Manager device, non-None for at least one zone with `energy_today > 0`
- [ ] `sensor.ura_whole_house_cost_today` enabled-by-default on house device, reports non-None USD value
- [ ] No HA log warnings about blocking I/O on event loop from `energy_tou.py`
- [ ] All updated UI labels show "fallback" framing in options flow re-open

## Verdict

**PASS WITH FIXES** — All HIGH and MEDIUM findings that block deploy have been addressed in commit `3f0a015`. Three deferred items (MEDIUM #3, LOW #5, LOW #6) are non-blocking; tracked in the v4.6.8 review log only (not pushed to BACKLOG.md per feedback_review_bug_visibility — these are accepted-as-deferred polish, not silent acceptance).

## Recommendations for QUALITY_CONTEXT.md

Three new bug classes proposed (see Bug class frequency above):
1. **"Silent fallback skipping a tier of the resolution chain"** — detection via per-tier exclusivity tests
2. **"Silent failure swallowing programmer error"** — require `_LOGGER.debug` minimum in `except Exception: pass`
3. **"Attribute/state desync from independent recomputation"** — cache derived values in `native_value`

Add to `docs/QUALITY_CONTEXT.md` on next touch.
