# ROOM-NAME-DESYNC-1 (Tier 2-DB) Review Record — v5.75.0

Chain: plan rev-1 → plan review FIX-PLAN-FIRST (4 HIGH: double-reload C1, setup-reload ordering C2,
3rd write site C3, CONF_ZONE exclusion C4; 3 MED) → rev-2 → build (stalled 4h zero-commits →
orchestrator salvage 1c81bcdd8 → fresh builder verify-and-complete 71fd61de9) → 3 framing-disjoint
reviews → consolidated fix-up 6ce3f3b17 → orchestrator re-drill → merge 2697c59a9.

## Plan review (pre-build)
| ID | Sev | Finding | Fixed in rev-2 |
|---|---|---|---|
| C1 | HIGH | Two sequential entry-writes = double listener fire/double reload | ✅ single combined call + async_abort recipe per site (+title per M1) |
| C2 | HIGH | Migration after add_update_listener = reload from inside setup (watchdog class) | ✅ before-all-four-sites checklist |
| C3 | HIGH | Third write site (7943 else-branch) missed | ✅ added |
| C4 | HIGH | CONF_ZONE excluded despite data-first reader at aggregation.py:502 | ✅ folded into scope |

## Code reviews
| ID | Sev | Finding | Bug class | Fixed |
|---|---|---|---|---|
| B-HIGH-1 | HIGH | Zone-rooms assignment/removal loops (7868/7881) still options-only — the plan's OWN §4.3 named them "exactly where drift is created"; every zone-rooms save minted fresh desyncs, reload-suppressed, silent until next boot | **Known-but-not-carried-through** (plan→build fidelity) | ✅ combined writes + 2 real-flow tests, drilled per-site |
| B-MED-1 | MED | Site-3 else-branch reachable with zone_entry=None → AttributeError breaks flow | None-guard | ✅ guard-and-abort |
| B-MED-2 | MED | Desync NM awaited inside setup; swallowed when NM not yet up (nondeterministic entry order) | Boot-order diagnostic loss | ✅ background task + one 60s retry (enforcer test forced entry.async_create_background_task — bare create_task reddened test_no_untracked_async_create_task_in_init) |
| M2-C-MED-1 | MED | D3a substrate read-shape test is simulate-not-drive (variant #5) — plan-authorized fallback | Hollow-anchor (contained) | DOCUMENTED — README live table carries "simulated in-suite; runtime proven live" |
| D-MED-1 | MED | Room-rename name collision unguarded — name-keyed maps silently merge two rooms; latent, pre-existing, blast radius moved save-time | Join-key uniqueness | CARDED ROOM-NAME-UNIQUE-1 |
| A-L1/L3 | LOW | Dead import; per-loop import | Hygiene | ✅ |

## HA-source verifications (Review A)
Combined async_update_entry(data,options,title) fires listeners exactly once (_async_update_entry
accumulator → single _async_save_and_notify); async_abort early-returns in
OptionsFlowManager.async_finish_flow before the second entry-write → the C1 recipe is provably
single-fire. update_listeners empty during setup-time migration (registered later) → ordering alone
is sufficient.

## Drill ledger
Builder: 4 drills (+2 on the fix-up sites). Reviewer C: re-ran all 4 + 1 independent (site-2) —
drill 1 red-count STRONGER than reported (6 vs 3). **Orchestrator: personally re-drilled the
B-HIGH-1 assignment site — 1 named red, restored, 18/18 green.** All 5 producer sites empirically
anchored.

## Statistics
| Severity | Found | Fixed | Carded/Documented |
|---|---|---|---|
| HIGH | 5 (4 plan + 1 code) | 5 | 0 |
| MEDIUM | 6 | 3 | 3 (2 documented, 1 carded) |
| LOW | 3 | 2 | 1 deferred |

Suite: 23 pre-existing failures unchanged; +2 passes.

## Bug-class notes
1. **Known-but-not-carried-through** (B-HIGH-1): a plan can NAME the critical site and the build
   still miss it — reviewer enumeration must re-derive the site list from the plan's ANALYSIS
   sections, not just its deliverable list. Recommend QUALITY_CONTEXT note.
2. The untracked-background-task enforcer test caught a real regression mid-fix-up — enforcement
   tests earn their keep exactly here.
3. Builder-stall salvage pattern (checkpoint-demand → WIP salvage-commit → verify-and-complete
   charter) recovered ~4h of work losslessly; checkpoint discipline (≤45min commits) now in the
   standard builder brief.
