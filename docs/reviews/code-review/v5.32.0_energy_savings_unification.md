# Code Review — v5.32.0 Energy Savings Unification (#7)

**Tier:** 2 (two framing-disjoint reviews + live validation). Rescoped from an
initially-proposed Tier 2-DB after foundation verification removed the two
regression-prone ingredients (see planning doc "TIER RE-SCOPE" / "FOUNDATION
VERIFICATION"). Additive, display-only; no decision consumer.

**Baseline:** `pre-review-v5.32.0`. **Reviews run in parallel.**

## Framings
- **A — Correctness + formula edge cases:** signs, kW/W units, rate = tick tier, double-count guard, resets, test authority.
- **B — Persistence + restart + additive-wiring byte-identity + DB + sensor registration.**

## Findings

| ID | Sev | Class | Summary | Status |
|---|---|---|---|---|
| A-HIGH-1 | HIGH | #7 stale/unknown source treated as authoritative | `net_import_kw=None` (Envoy blind) credited export as served-locally → over-credit | **FIXED** — `if net_import_kw is None: return` (mirrors CostTracker), test added |
| B-HIGH-1 | HIGH | #7 / I7 lifetime-must-not-shrink | `_pa_lifetime_delta` in-RAM → `*_lifetime` sensors reset to $0 on every restart | **FIXED** — midnight roll-in to `savings_lifetime_baseline` (≤2 writes/day) + restart test |
| B-HIGH-2 | HIGH | #7 restart-persistence | `_pa_today`/`_cycle` in-RAM → today/cycle drop mid-scope on restart | **FIXED** — snapshot/restore via existing `energy_state` idiom (mirrors CostTracker) + restart test |
| A-MEDIUM-3 | MED | #60/#62 test authority | byte-identity guard tests self-seeded to an untracked file → could silently skip | **FIXED** — SHA constants inlined in-test, fail-loud; external file deleted |
| B-MEDIUM-3 | MED | #34 swallow | bare `except: pass` in predicted-bill attr extension | **FIXED** — `_LOGGER.debug(exc_info=True)` |
| B-MEDIUM-1 | MED | #34-flavor | baseline-load retry storm on persistent DB error | **FIXED** — set loaded=True + WARNING on failure |
| A-MEDIUM-2 | MED | #53 cross-consumer normalization | pre-existing `_get_displaced_rate` mid_peak/peak asymmetry; guard inherits it | **ACCEPT-WITH-NOTE** (byte-frozen; code comment added) |
| B-MEDIUM-2 | MED | #22-adjacent | prune-before-seed race could shrink arbitrage lifetime baseline | **ACCEPT-WITH-NOTE** (low risk; WARNING/comment added) |
| LOW-1 | LOW | dead code | dead `import time` wall-clock fallback branch | **FIXED** — removed |
| LOW-2/3 | LOW | robustness | negative-solar clamp / broad-except debug trail | noted |

## Summary statistics
| Severity | Found | Fixed | Accept-with-note |
|---|---|---|---|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 3 | 3 | 0 |
| MEDIUM | 5 | 3 | 2 |
| LOW | 3 | 2 | 1 |

## Bug-class frequency
- #7 (stale/unknown source, restart-persistence): 3 — the dominant class this cycle (all restart/blind-window state).
- #34 (swallow): 2. #53, #60/#62, #22-adjacent: 1 each.

## Orchestrator independent verification (not trusted from summaries)
- Byte-identity of `_get_displaced_rate` + `CostTracker.accumulate` re-verified by **AST source-diff against `pre-review-v5.32.0`** (IDENTICAL, 1292/2288 chars) — authoritative over hash-method variance.
- None-guard presence confirmed at `energy_billing.py:569`; restart-persistence surface confirmed (snapshot_state/restore_snapshot/pop_lifetime_delta_for_rollup/update_savings_baseline/_rollup_savings_lifetime_deltas).
- Targeted suite re-run by orchestrator: **17/17 pass**. Full suite: 26-baseline, zero new failures.

## QUALITY_CONTEXT.md recommendation
No new bug class needed — all findings map to existing classes (#7 restart/stale-source is the recurring one; consider a sub-note that "in-RAM lifetime accumulators reset on restart" is a recurring #7 instance for any future money-number sensor).
</content>
