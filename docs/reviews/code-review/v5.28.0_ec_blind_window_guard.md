# Code Review Record — v5.28.0 EC Blind-Window EVSE Guard (Tier 3)

**Branch:** build/ec-blind-window-guard, e707ba2b (build) → 2e564918
(seven fix-up batches). Baseline tag `pre-review-v5.28.0`.
**Tier 3:** A/B/C/D reviews + D re-pass + bounded D re-check + orchestrator
mutations. **Trigger:** 2026-07-21 incident (84-min Envoy outage; battery
fed a 5 kW car with no enforceable protection). Plan:
`PLANNING_ec_blind_window_evse_guard.md` + probe + operator adjudication.

## Findings ledger — 23 findings across 6 review passes, ALL closed

| Pass | id | sev | finding (abbrev) |
|---|---|---|---|
| A | A-CRIT-1 (=B1=D-CRIT-1) | CRIT | `is_reserve_verifiable` accepted frozen OK/STALE → guard could never engage in a quiet outage (its own acceptance scenario) |
| D | D-CRIT-2 (=B2) | CRIT | restart mid-outage discarded the restored pause set via debounce re-arm → one blind ensure-on flap |
| D | D-HIGH-1 | HIGH | NINE resume/release sites bypassed the guard (grid-cap, drain, fill-priority, arbitrage, 3× release_all, load-shed, DP reversion) |
| D | D-HIGH-2 (+D-MED-2) | HIGH | max-defer fell through to unconditional ensure-on; must-start-by blind-unaware |
| B | B3 | HIGH | force-charge escape deadlocked for an already-paused EVSE |
| B | B4 | HIGH | daytime recovery stranded EVSE (no excess-solar drain) |
| A | A-HIGH-1 | HIGH | retention const dead; unbounded dp_eval writer |
| C | C-HIGH-1 | HIGH | SOCEnvelope tests exercised a shim (tautological-fixture recurrence #10) |
| C | C-HIGH-2 (+D-MED-1) | HIGH | D4 witness live-unreachable; test fed impossible state |
| C | C-HIGH-3 | HIGH | accessor chain (incl. is_reserve_verifiable) zero direct tests |
| D-re | D-HIGH-3 | HIGH | must-start-by release re-captured next tick by the pause leg (timer destroyed → car stranded) — the Batch-4×Batch-1 intersection |
| A/B/C/D | 10 MED | MED | unit norm (#30), 100W ensure-on threshold, defer rows unwritten, B7 dual-truth, migration-in-place hazard, D-MED-1 idiom-blind tripwire, D-MED-2 cap=0 semantics, D-F1 DROP-leg latch skip, C-MED-1/2 witnesses |
| all | 6 LOW | LOW | task handle, fail-open predicate → fail-closed, ownership honesty, row dedup, backup naming class, D-F2 accepted-with-note |

## Accepted semantics (documented in code + here)
- Two sanctioned liveness escapes: force-charge; max-defer/must-start-by via
  `blind_window_liveness_release` (envelope-consulting, row-writing, never
  silent). NEVER-PAUSE-THIS-EPOCH ride authority after a pressure release.
- `CONF_RESERVE_VERIFIABLE_MAX_AGE_S=0` emergency backout reopens the
  quiet-outage hole ONLY while the oracle stays readable — documented price.
- cap<=0 = true guard no-op (releases existing pauses; no helper, no rows).
- D-F2: stale cross-epoch ride latch is inert + self-healing (eager clear
  would risk stranding a mid-outage-restart car).
- drain-target-None ⇒ liveness release permitted (no fabricated floor).

## Incident replay (final code) — the acceptance narrative
20:50 outage → debounce (≤120 s exposure; 66% of real outages end here) →
20:52 engaged, EVSE claimed, defer row → 21:00 ensure-on DEFERRED (the
incident's failure moment — now protected, all 12 enumerated sites) →
mid-charge cars ride while the LKG envelope's lower bound clears the drain
target, pause when it decays → 21:52 max-defer: envelope-proven release or
logged refusal awaiting must-start-by → 22:14 recovery: epoch closes,
normal charging resumes. Restart mid-outage: pre-engaged restore, no flap.
Exposure: 84 min blind charging → ≤120 s + envelope-bounded ride.

## Verification
- Enumeration contract: 12/12 EVSE emission sites independently re-derived
  by D (twice); tripwire now catches both call idioms.
- Mutation matrix: 15+ mutations, every one killed by a named test
  (14-table Batch 5 + 7 spot Batch 6 + D-F1). Orchestrator personally ran:
  A-CRIT-1 STALE re-admission, ride-latch neuter (after fixing an invalid
  first attempt that didn't match source — recorded as method note), plus
  Batch-2/3 checks. All killed; tree clean each time.
- Full suite ×2 consecutive: 36 failed + 14 errors env-drift baseline,
  stable; +72 net new passing tests across the cycle.

## Live validation (prospective, for the README at deploy)
L1 clean boot; L2 dp_eval rows appear (~12/hr) + retention prune runs;
L3 organic: next >2-min Envoy blip → guard engages, defer/liveness rows in
decision_log, no EVSE turn_on inside the window (probe says ~2-3
chances/day); L4 `mains_vue_*` unit_of_measurement ∈ allowlist; L5 LKG
snapshot survives a restart.

**Status: AT OPERATOR CHECKPOINT — ships as v5.28.0 only on explicit go.**
